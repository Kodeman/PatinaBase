# Track 6 · Slice 5 — the Discovery section (R66) · screenshots

Captured during the live Chrome walk (designer@patina.dev, :3000, `the-document-pilot`).
The browser-automation env in this session yields no file on `save_to_disk` (a known
constraint, same as Track 5), so the desktop ≥1280 states were captured **in-conversation**
in the implementing session rather than written here. States verified:

1. **Discovery section · empty** — spine `Discovery · IN DISCOVERY`; readiness header
   `0 of 5 essentials captured — keep going` with the StrataMark and the **disabled**
   "Begin the Direction →" CTA; toolrow (Run the discovery call · Attach the room scan ·
   Add inspiration); 5 structured essentials + 3 deepening blocks; notes-only margin.
2. **A block open in place** — Scope & rooms expands (D1, no nav) with the project-type
   select + the room-list editor (name / type / sq ft / KEEP); status reads "1 room".
3. **Discovery · ready** — spine `Discovery · READY`; StrataMark line 1 full; header
   `Ready for Direction — essentials captured`; the CTA **enabled**; all five essentials
   ticked with summaries (2 rooms · $60,000–$80,000 · dated · 2 tags · 2 captured).
4. **Direction opens pre-seeded** — after "Begin the Direction": spine `Discovery · SETTLED`
   + `Direction · DRAFTING`; the Direction body shows **"Seeded from Discovery · budget
   60,000–80,000"** and **PER-ROOM BUDGETS: Living, Dining** (the scope rooms carried
   field→field).
5. **Margin Note at Discovery** — the notes-only margin holds the unstructured call read
   ("She hesitated on budget — wants to 'see options first.'") via the relationship key.

⚠ Owed: ~390px mobile captures (deferred with the desktop-to-disk constraint).
