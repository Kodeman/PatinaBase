{
  "extends": ["../../../.eslintrc.json"],
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "MemberExpression[object.name='page'][property.name='waitForTimeout']",
        "message": "waitForTimeout is not allowed. Use deterministic waits from WaitHelpers instead (e.g., waitForElement, waitForNetworkIdle, waitForApiResponse)."
      },
      {
        "selector": "MemberExpression[property.name='waitForTimeout']",
        "message": "waitForTimeout is not allowed. Use deterministic waits from WaitHelpers instead (e.g., waitForElement, waitForNetworkIdle, waitForApiResponse)."
      }
    ]
  },
  "overrides": [
    {
      "files": ["*.spec.ts", "*.test.ts"],
      "rules": {
        "no-console": "off"
      }
    }
  ]
}
