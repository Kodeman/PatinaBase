export { StoryTimeline } from './StoryTimeline'
export type { StoryTimelineProps } from './StoryTimeline'

// Note: Animation hooks are internal to StoryTimeline
// They're not exported to avoid conflicts with existing ImmersiveTimeline hooks
