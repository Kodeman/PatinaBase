{
  "name": "patina-inference-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker fronting the aesthete-inference container (migration phase 3d)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.107.0"
  }
}
