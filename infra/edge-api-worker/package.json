{
  "name": "patina-edge-api-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare edge router and typed API for Patina",
  "scripts": {
    "deploy": "wrangler deploy --env production",
    "dev": "wrangler dev --env local",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "types": "wrangler types",
    "deploy:dry-run": "wrangler deploy --dry-run --env production"
  },
  "dependencies": {
    "jose": "^6.1.3",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "4.123.0"
  }
}
