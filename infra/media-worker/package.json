{
  "name": "patina-media-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Queues consumer + media processor container (BullMQ replacement, migration phase 3b)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test test/*.test.mjs",
    "types": "wrangler types",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.107.0"
  }
}
