{
  "name": "patina-media-processor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Container: pure Sharp image processor (bytes in → renditions out); R2 I/O is done by the Worker",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "blurhash": "^2.0.5",
    "sharp": "^0.33.5"
  }
}
