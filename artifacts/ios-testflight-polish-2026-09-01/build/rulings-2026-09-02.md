# First Flight — Kody's rulings (interview, 2026-09-02)

These amend `build/PROGRAM.md` §6. Where a ruling differs from the charter's recommendation the
difference is stated; every lane brief in W0 and W1 reads this file after PROGRAM.md.

| # | Ruling | Consequence for the program |
|---|---|---|
| **D1** | Round one = **Leah's active design clients**. **`house-first` (the four-tab bar) is ON for every tester.** `direct-orders` and `house-widget` stay OFF. | **Differs from the charter.** The four-tab root (Today · Spaces · Pieces · Studio, the hoisted tour, the Studio door) is the shipped product. Every finding observed only on the flags-on root moves up to the tier it deserves for a tester who sees it on day one; findings that exist only on the flags-off root keep their tier but note that the old root is now a kill-switch fallback. A **re-tier pass** (RETIER, W0) rewrites `findings.json` / `findings-by-lane.md` with `retieredBy: "D1 2026-09-02"` on every changed row. |
| **D1a** | The tab bar appears on the **first** launch: `FeatureFlags` resolves `house-first` **true when PostHog has no answer** (fresh install, no cached payload, no `-PatinaFlags` argument). A PostHog payload that says `false` still wins (kill switch). `-PatinaFlags` stays authoritative in DEBUG; `--uitesting` stays all-off unless named. The other two flags keep the fail-closed default. | Implemented in **W0 · L0.1** (it is build configuration): `Core/State/FeatureFlags.swift` gains a per-flag default table (`house-first: true`, others `false`) and `PatinaTests/FeatureFlagsDefaultTests.swift` pins the four cases. W1's walkers launch **without** `-PatinaFlags` and must see the bar. |
| **D2** | **Leah supplies ≥ 30 Patina-grade pieces this week**; an agent builds the pipeline (CSV → rows, image upload, spectrum rows) and proves it locally by day 2; Kody runs the production seed. If the manifest is not in hand by **end of day 6**, build 1 ships the honest "still curating" state (L1-B). | As recommended. L0.3's manifest template goes to Leah on day 1. |
| **D3** | **Drop Google for round one.** The Welcome screen renders only the providers `GET /auth/v1/settings` reports enabled (Apple + email today). | As recommended. W1 · L1-A. |
| **D4** | **iPhone-only** (`TARGETED_DEVICE_FAMILY = 1` on both targets). | As recommended. W0 · L0.1; the 13 GAP5 iPad rows drop out of W2. |
| **D5** | **Ship the widget in build 1, fixed**: it renders its snapshot regardless of `house-widget` (the flag gates in-app promotion only); medium family; per-row deep links; no mid-word truncation. | As recommended. W1 · L1-F. |
| **D6** | **Deployment target 26.0.** | As recommended. W0 · L0.1. |
| **D7 + D11** | **Wire the `test-account-login` fallback into the app** (allow-listed emails only, Vault-configured, fail-closed) **and mint a clean, purpose-built client account** as the demo / walk account, with a real-looking house (one project with a live designer, one decision awaiting the client, one sent proposal, one open invoice, one document, one thread). | As recommended. The app half is W1 · L1-A (`AuthService.verifyOtp`). The account mint is a **Kody-run SQL script** prepared by W0 · L0.2 (`build/waves/w0/demo-account.sql`) plus the Vault allow-list update; proposed identity `firstflight@patina.cloud` (no mailbox needed — the code is the Vault `test_login_code`). L0.5's beta-review notes use that identity. `tester@patina.cloud` is retired from the app's story; `A3-15` needs no W1 row. |
| **D8** | **00555 goes to Strata after L0.2b's portal follow-ups merge and the designer portal is redeployed — day 2, not day 1.** | As recommended. The exposure stays live ~24 h longer; The Document never breaks. |
| **DM-1** | **Close the anon read now; accept counterparty column visibility for round one; the `profile_private` PII split is a W2 migration.** | As recommended. 00555 ships as drafted (anon closed, self-elevation closed, tightened legs). W2 · L0.2 gains the split; the charter's W2 table records it. |
| **D9** | **APNs credentials are set on Strata.** The device pass tests a real push round trip from `apns-send` and a cold launch from the notification. | Device row D-07 is live, not blocked. |
| **D10** | **A live Stripe key lands on Strata before build 1** — testers can pay a real invoice. The device pass includes Apple Pay inside hosted Checkout with a real small invoice. | Device row D-12 is live. Kody-run: the key on the edge runtime + the tax/shipping setting; the runbook carries the step. |
| **D12** | The twelve promoted T1 rows stay in W1. | As written. |
| **D13** | Write `increment_scan_upload_attempt` as **00556** (mirror `mark_scan_upload_complete`'s shape and grants). | As written. W0 · L0.2. |
| **D14** | W1 merge order **L1-C → L1-D → L1-B → L1-F → L1-A → L1-E**. | As written. |
| **D15** | The widget ships its own `PrivacyInfo.xcprivacy`. | As written. W0 · L0.1. |
| **V7** | Log the D1 exception in `docs/vision/VISION-DECISIONS.md` as **V7**: the iOS app (surface #2, the studio's front door) may use a tab bar; The Document (surface #1) still may not. Append-only entry, dated today, referencing this file. | W0 closer writes the entry; committed with `build/waves/w0/`. |

## Standing assumptions the interview confirmed by silence

- `direct-orders` OFF: the Buy path stays dark; with a live Stripe key, money moves only through invoices.
- Round one is invited through Leah, 1:1; the `MiddleWest Client` external group receives build 1 only after beta review; Kody and Leah get every build internally first.
- Nothing in this file authorises a production write. Every prod mutation is a Kody-run step in `build/waves/w0/KODY-RUNBOOK.md`.
