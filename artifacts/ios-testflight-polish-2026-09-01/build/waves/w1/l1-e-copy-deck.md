# W1 · L1-E — copy deck

Reviewed against `.claude/skills/patina-brand-voice/SKILL.md`: sentence case except proper nouns;
one voice on failure (modelled on `Features/Money/MoneyFailureCopy.swift` and
`Features/Purchase/OrderFailureCopy.swift` — never interpolate a thrown error or a server string);
one noun per thing (**Piece · Room · Studio · Companion · Record**); no "curated", "journey",
"elevated", "disrupt", "revolutionize"; no "AI"/"A.I."/"artificial intelligence"/"machine
learning"/"GPT"/"LLM" anywhere. Every row below was checked against that list before it was written.

**Format:** `finding id · file:line · today's string · final string · owning lane`. The owning lane
applies its own rows inside its own worktree as numbered tasks (PROGRAM.md §3 · L1-E's ownership
rule). L1-E applies only the rows marked **L1-E** below, in the three files it owns outright plus
`ARPlacement/**`, `Services/DesignServices/**` and `DesignRequestFlowView+Steps.swift` — see
`build/waves/w1/l1e-tasks.md`.

Covers all 18 W1 findings in L1-E's table (`build/findings-by-lane.md`), plus four copy halves other
lanes flagged: `A-101` (L1-A's delete-account sentence), L0.1's seven permission strings
(`build/waves/w0/l01-notes.md` N1 — recorded as a **W2 carry-forward**, not applied this wave, per
`build/waves/w1/steward.md` §5.6), the tour's "Studio" wording (L0.4, `build/waves/w0/sanity-tour-copy.md`
— already content-only and already correct in the binary, recorded for completeness), and the "still
curating" empty-state copy flagged against L1-D's `PatinaEmptyState.swift`. `C5-05` is also included —
it is L1-C's own W1 row (routed `⇢L1-E`) but it needs a string from this deck the same way `A-60`/`C-30`
do.

---

## Rows by owning lane

### L1-A applies

| id | file:line | today | final | note |
|---|---|---|---|---|
| `A-52` | `Features/Companion/Services/CompanionActionRows.swift:213-214` (`pieceActRow`, `.askDesigner`) | hint `"\(firstName ?? "Your designer") will see the piece and the price"` | **unchanged for a signed-in relationship** — only reachable when `relationship.isLive`, which cannot be true for a guest (`PieceActResolver.act`). No guest exposure; not part of this row. | Named so the three `PieceAct` cases are read together — only `.askAboutPiece` below is the guest-visible one. |
| `A-52` | `Features/Companion/Services/CompanionActionRows.swift:220-223` (`pieceActRow`, `.askAboutPiece`) | hint `"A designer will come back to you"` — drawn for both a guest and a signed-in stranger with no designer yet | **Guest:** `"Sign in and a designer will get back to you"` · **Signed-in, no designer yet:** `"A designer will get back to you"` (unchanged in substance; "will come back to" → "will get back to" for the small tense-neutral cleanup, applied everywhere this phrase appears so the two states read as one voice) | Requires threading `isAuthenticated` into `pieceActRow(_:isAuthenticated:)` — `PieceActResolver.entry(for:isAuthenticated:)` (`Features/Purchase/PieceAct.swift:114-127`, not an L1-A file, already auth-walls the tap itself; this row only fixes what the guest reads *before* the wall). |
| `A-52` | `Features/Companion/Services/CompanionActionRows.swift:32-34` (`homeRow`) | label `"Home"`, hint `"Back to your space"` — drawn identically for a guest who has never scanned a room | **Guest:** hint `"See what's on Patina"` · **Signed-in / guest with local rooms:** hint unchanged, `"Back to your space"` | `homeRow()` needs the same `isAuthenticated` (or a `hasLocalWork` check reusing `LocalStoreClaim.hasGuestWork`) threaded in as the row above; both belong in the same task since they share the auth-state parameter. |
| `A-52` | `Features/Notifications/Views/NotificationFeedView.swift:193` (`guestInviteView`, already branched on auth state — the empty-state VIEW is correctly guest-only; only the sentence inside it still presumes a designer) | `"Updates from your designer will land here. Sign in to stay in the loop."` | `"Sign in to see updates on your projects and messages here."` | The title `"Nothing yet"` (`:192`) is fine — leave it. Only the message changes; it now says what signing in unlocks instead of asserting an existing designer relationship. |
| `A-79` | `Features/Collections/Views/LocalStoreClaimSheet.swift:17` | `"Keep the room and the pieces you saved on this phone?"` (fixed string regardless of what was actually saved) | Composed from the real counts, sourced from `LocalStoreClaim` (add `roomCount`/`pieceCount`, computed the same moment `hasGuestWork` is): <br>• rooms only: `"Keep the {n} room{s} you saved on this phone?"` <br>• pieces only: `"Keep the {n} piece{s} you saved on this phone?"` <br>• both: `"Keep the {r} room{s} and {p} piece{s} you saved on this phone?"` <br>Concrete example (evidence session — 0 rooms, 1 piece): `"Keep the 1 piece you saved on this phone?"` | `s` = `""` when the count is 1, else `"s"` — plain ternary, no need for `LocalizedStringResource` inflection at this size. The sheet is **already** never shown at zero (`LocalStoreClaim.shouldAsk` requires `hasGuestWork`), so the fix's "omit entirely when there is nothing to migrate" half needs no change — noted so the applying task doesn't duplicate it. |
| `A-79` | `Features/Collections/Views/LocalStoreClaimSheet.swift:23` | `"They were saved before you signed in. Keep them and they become yours; start fresh and this phone keeps nothing from before."` | **Unchanged.** It never claims a specific count, so it stays true at any count > 0. | — |
| `A-101` | `Features/Account/AccountDeletionService.swift:41` | title `"Close your account?"` | `"Delete account"` | One verb everywhere — the row (`SettingsView.swift:89`, already "Delete account"), the confirmation title, and the button all read the same word. "Close" reads as suspend/pause, which is not what this does. |
| `A-101` | `Features/Account/AccountDeletionService.swift:42-43` | `"This removes your account and everything Patina keeps on this device. It can't be undone."` (scopes deletion to the device only) | `"This deletes your Patina account, including your saved rooms, pieces, and messages. Any project you completed with a designer stays in our records — with your name and contact details removed — as required for our legal and accounting obligations. This can't be undone."` | Grounded in `supabase/functions/delete-account/index.ts` + `supabase/migrations/00538_client_account_anonymize.sql`: the auth user is soft-deleted (email/phone/password destroyed, every session dropped) and `purge_client_account` deletes rooms, room scans, saved items, the threads the client started, notification/push-token/style-profile/companion rows — but **never** writes to `proposals`, `projects`, `invoices`, `client_decisions` or `designer_clients`; those survive indefinitely, pointing at a tombstoned profile "which carries nothing of the person" (00538's own comment). There is no fixed purge window in the code for those records — do not invent one ("30 days" etc.); "as required for our legal and accounting obligations" is the true, unembellished reason, matching the migration's own rationale. |
| `A-101` | `Features/Account/AccountDeletionService.swift:39` (`failureCopy`, already correct — recorded so no lane rewrites it) | `"We couldn't close your account just now. Try again, or write to hello@patina.cloud."` | `"We couldn't delete your account just now. Try again, or write to hello@patina.cloud."` | Same one-verb sweep as the title/body above — only "close" → "delete", nothing else. |
| `A-06` | `Features/Onboarding/Views/OnboardingFlowView.swift:31` | `"Let's discover yours."` (straight U+0027) | `"Let's discover yours."` (curly U+2019) | One apostrophe glyph app-wide. Line 37 already uses U+2019 ("the room's shape") — that is the standard this sweep adopts, matching how the rest of the app's newer copy is typed. |
| `A-06` | `Features/Onboarding/Views/OnboardingFlowView.swift:57` | `"Five quick questions, then we'll show you pieces that fit. Your camera comes later — only when you choose to scan a room."` (straight) | Same sentence, U+2019 in `"we'll"`. | — |
| `A-06` | `Features/Onboarding/Views/OnboardingFlowView.swift:58` | `"Let's begin"` (straight) | `"Let's begin"` (curly) | Also `C5-20`'s replacement CTA below reuses this exact string — write it once, curly, and both rows match byte-for-byte. |
| `C5-20` | `Features/Onboarding/Views/OnboardingFlowView.swift:32` | `"Start Your Journey"` (Title Case; "journey" is a lexicon violation) | `"Let's begin"` | Reuses page 3's own CTA verbatim (the fix's own suggestion) — after this, both page 1 and page 3 read "Let's begin", which is intentional: it is the one confident, unpretentious thing to say before either variant of the flow. |
| `C5-20` | `Features/Authentication/Views/AuthenticationView.swift:134` | `"Join the furniture discovery journey"` (sign-up subtitle; "journey" + marketplace-speak, and says nothing about what an account buys) | `"Save your rooms and pieces, and pick them up on any device."` | States the actual benefit (device sync — the same promise `signInRow`'s hint already makes elsewhere: "Save rooms · Sync across devices") instead of brand-voice filler. |
| `C5-10` | `Features/Onboarding/Views/OnboardingFlowView.swift:38` (already sentence case, recorded so it is not re-cased by mistake) | `"Continue"` | **Unchanged.** | — |
| `C5-10` | `Features/Account/AccountView.swift:184` | `PatinaButton("Sign Out", style: .secondary)` | `"Sign out"` | Sentence case sweep — pairs with `:188`'s already-correct "Delete account" two lines below. |
| `C5-10` | `Features/QRAuth/Views/QRScannerView.swift:201` | `PatinaButton("Open Settings", …)` | `"Open settings"` | Matches `StudioHubView.swift:131`'s already-correct "Open settings". |
| `C5-10` | `Features/FirstLaunch/Views/CameraPermissionView.swift:223` | `Text("Open Settings")` | `"Open settings"` | Same sweep, same reason. |
| `C5-10` | `Features/Authentication/Views/AuthScreenView.swift:136` (already sentence case, recorded as the standard the other rows below are set to) | `"Sign in"` | **Unchanged.** | — |
| `C5-10` | `Features/Authentication/Views/AuthenticationView.swift:526,528,530,532` (`submitButtonTitle`) | `"Sign In"` / `"Create Account"` / `"Email me a code"` / `"Send Reset Link"` | `"Sign in"` / `"Create account"` / `"Email me a code"` (unchanged) / `"Send reset link"` | — |
| `C5-10` | `Features/Authentication/Views/AuthenticationView.swift:632` (mode-switcher button) | `"Sign Up"` / `"Sign In"` | `"Sign up"` / `"Sign in"` | — |
| `C5-10` | `Features/RoomScan/Shared/Components/PauseMenuView.swift:63-64` | `"Discard Scan"` / `"Keep Scanning"` | `"Discard scan"` / `"Keep scanning"` | — |

### L1-B applies

| id | file:line | today | final | note |
|---|---|---|---|---|
| `C4-09` | `Features/RoomScan/Shared/Components/ScanUploadProgressView.swift:57-63` | `Text(err)` where `err = package.lastError` — printed raw (storage/Postgres text) | Route through a new `ScanUploadFailureCopy` mapping, modelled **exactly** on `Features/Purchase/OrderFailureCopy.swift` (L1-E's file — read it as the pattern: a typed error → a fixed `MoneyFailure`-shaped sentence, the raw detail logged in `#if DEBUG` and never interpolated). Minimum viable mapping for the phases `RoomScanSyncService`/`+AdvancedBundle` actually throw: <br>• a network/transport failure → `"Upload paused — check your connection. It'll pick up automatically."` <br>• storage/Postgres write failure → `"We couldn't finish uploading your scan. Try again from here."` <br>• anything unclassified → `"We couldn't finish uploading your scan. Try again from here."` | `package.lastError: String?` stays as the on-disk diagnostic column (unit-tested, logged) — only the **view** stops printing it verbatim; it renders `ScanUploadFailureCopy.message(for: package)` instead. |
| `C5-16` | `Core/Models/SavedItem.swift` (new computed property, mirroring `ProductModel.swift:222-229`'s `resolvedMakerName`) | — | ```swift\nvar resolvedMakerName: String? {\n    let vendor = makerName.trimmingCharacters(in: .whitespacesAndNewlines)\n    guard !vendor.isEmpty,\n          vendor.caseInsensitiveCompare("Unknown Maker") != .orderedSame,\n          vendor.caseInsensitiveCompare("Unknown") != .orderedSame else { return nil }\n    return vendor\n}\n``` | `SavedItem` has no `brand` field (unlike `ProductModel`), so the guard is the vendor-string check only. |
| `C5-16` | `Features/Rooms/Components/RoomItemRow.swift:43` | `Text(item.makerName)` (prints `"UNKNOWN MAKER"` verbatim, uppercased) | `if let maker = item.resolvedMakerName { Text(maker) }` — the whole `Text` is dropped when `nil`, same as `RecommendationsView.swift:334-340`'s pattern. | Layout: the row's `VStack` already has three optional-height children; dropping this line at `nil` is the same shape SP-10 already shipped for the Browse grid. |
| `C5-16` | `Features/Rooms/Components/RoomItemRow.swift:89` (`rowAccessibilityLabel`) | `"by \(item.makerName)"` unconditional | `if let maker = item.resolvedMakerName { parts.append("by \(maker)") }` | Drops the "by Unknown Maker" AX string the same way the visible line drops. |
| `C5-16` | `Features/Rooms/Views/ItemActionMenu.swift:53` | `Text("\(item.makerName) · \(item.fullFormattedPrice)")` | `Text([item.resolvedMakerName, item.fullFormattedPrice].compactMap { $0 }.joined(separator: " · "))` | At `nil`, renders price alone with no dangling `" · "`. |
| `C5-16` | `Features/Rooms/Views/MoveOrCopyItemSheet.swift:80` | `Text("\(item.makerName) · \(item.fullFormattedPrice)")` | Same pattern as `ItemActionMenu.swift:53` above. | — |
| `C5-09` | `Features/Rooms/Views/ItemActionMenu.swift:31` | `row("arrow.up.right", "View Product Detail", .viewDetail)` — the `ProductDetailView` class name printed as a button label | `"See the piece"` | Reuses the exact phrase `OrderPlacedView` already uses (the fix's own instruction) — this is the one line C5-09 is T0 for; see the casing sweep for the other four rows in this same menu, below. |
| `C5-09` / `C5-10` | `Features/Rooms/Views/ItemActionMenu.swift:30,32-34` (the rest of the same menu — Title Case, and two use "Item"/"Room" inconsistently with the app's "Piece" noun) | `"View in AR"` / `"Move to Another Room"` / `"Copy to Another Room"` / `"Remove from Room"` | `"View in AR"` (unchanged — AR is an acronym, not a casing violation) / `"Move to another room"` / `"Copy to another room"` / `"Remove from room"` | Sentence case; none of the four names "Item" so the noun sweep needs no change here. |

### L1-C applies

| id | file:line | today | final | note |
|---|---|---|---|---|
| `A-60` | `Features/Companion/Services/CompanionActionRows.swift:36-39` (`profileRow` — the row that actually routes to `.profile`, the screen the Studio tab/pill/tour all point at) | label `"Your profile"`, hint `"Style · Settings · Portal"` | label **`"Your studio"`**, hint **`"Style · Settings"`** | The label change is A-60's ask (one name, used in the Companion menu too — the tab, the tour and the pill already say "Studio"); the hint change is C-22's (below) — the "Portal" clause is dropped because `PatinaPortalLinks.swift` has zero call sites, nothing named Portal exists to open. Two findings, one edit, done together so the row is only touched once. |
| `A-60` / `C-22` | `Features/Companion/Services/CompanionActionRows.swift:51-54` (`studioRow` — routes to `.projectList`, a plain project list, **not** the hub the row's old name implied) | label `"Your studio"`, hint `"Projects · Messages · Decisions"` | label **`"Your projects"`**, hint **`"Projects"`** | Necessary consequence of the row above: once `profileRow()` correctly claims "Your studio", this row (which goes somewhere else entirely) cannot keep the same word. Its new label matches `projectsRow()`'s existing label exactly (`CompanionActionRows.swift:56-58`) — both rows now point at the same screen with the same name, which is a real redundancy C-22's own judge note leaves as T1/T2 structural work ("the structural split... belongs in T1/T2"). Flagging it here rather than merging the rows myself — that is not a copy change. |
| `A-60` | `Features/Profile/Views/ProfileView.swift:148` | `Text("YOUR PROFILE")` (section header inside the screen the tab/pill/tour now all call "Studio") | `Text("MORE")` | Not literally "YOUR STUDIO" — the screen's own tab title already carries that word once (`:176`, `PatinaTab.studio.canonicalName`), and this section's actual rows (Retake Style Quiz / Get design help / Settings) are not studio business objects the tour's "projects, proposals, invoices and files" describes. Repeating "Studio" a second time on the same screen would trade one collision for a smaller one. "MORE" removes the retired word ("profile") without asserting a claim the section doesn't back up — matches the plain, unadorned mono-label style `"YOUR ROOMS"` (`:134`) already sets two sections above it. |
| `C-30` | `Features/Profile/Views/ProfileView.swift:201,207` (`statItem(value: "\(viewModel.roomCount)", label: "Rooms")` — static label, renders `"1 ROOMS"` at count 1; the accessibility label at `:234` is already correct) | `label: "Rooms"` (always) | `label: viewModel.roomCount == 1 ? "Room" : "Rooms"` | `MonoLabel` applies the uppercase tracking; passing the already-singular/plural word is the whole fix — no `LocalizedStringResource` machinery needed for a two-way English plural. |
| `C-38` | `Features/Recommendations/Views/RecommendationsView.swift:413-421` (`recommendationRationale`) | `return "Selected from Patina's room-aware edit for \(scopedRoomName)."` (repeats verbatim on every card, truncates mid-word in the grid, "room-aware edit" is internal jargon) | `return nil` | Deletes the fallback line entirely, matching what the Pieces tab already does (the fix's first option) — the `tastePortrait` branch above it is untouched and still returns a real per-card rationale when one exists. |
| `B-20` | `Features/Rooms/Views/RoomProjectView.swift:254` (`emptyBlock`, the room's empty-state CTA — not the room detail's standing primary button) | `cta(primary: "Browse pieces for the \(room.name)")` — breaks on any room name ("Browse pieces for the Audit Room B") | `cta(primary: "Browse pieces for this room")` | Fixed label rather than interpolating the article — the fix's second option, chosen because a room can be named anything ("Mum's Room", "1"), and no article rule is safe for all of them. |
| `C5-05` | `Features/Settings/Views/SettingsView.swift:153-155` | `settingsButtonRow(..., label: "Help Center") { openLink("https://patina.cloud/help") }` — `/help` 404s and silently lands on the marketing home page | Row label **unchanged** ("Help Center" is a proper-noun destination name, not a casing violation). **Structural, not a string:** point the row at the in-app `HelpPanelSheet` (`surfaceKey: SurfaceKeys.IOSApp.Settings.root` or the nearest existing key) instead of the dead external URL, or remove the row until `/help` exists. No new copy is needed either way — this is why the row is here rather than in the `L1-C applies` table's main findings: it is the structural half of an L1-C row that needed an L1-E word only to confirm the label itself was not the problem. | `C5-05`'s owning table is L1-C's own (`findings-by-lane.md`, routed `⇢L1-E`); recorded here because PROGRAM.md §3 · L1-E's integration notes name it explicitly. |
| `C5-10` | `Features/Settings/Views/SettingsView.swift:81` | `label: "Sign Out"` | `"Sign out"` | Pairs with `:89`'s already-correct "Delete account". |
| `C5-10` | `Features/Settings/Views/SettingsView.swift:121` | `label: "Haptic Feedback"` | `"Haptic feedback"` | — |
| `C5-10` | `Features/Settings/Views/SettingsView.swift:153` | `label: "Help Center"` | **Unchanged** — proper noun (the destination's name), not a casing violation. | Distinguishing this from the Title-Case sweep below it was the one judgement call in this row; "Help Center" reads as a named place the same way "Settings" does. |
| `C5-10` | `Features/Settings/Views/SettingsView.swift:156` | `label: "Contact Us"` | `"Contact us"` | — |
| `C5-10` | `Features/Settings/Views/SettingsView.swift:159` | `label: "Terms & Privacy"` | `"Terms & privacy"` | Ampersand kept (it is doing the job of "and" inside a short label); only the second word drops case. |

### L1-D applies

| id | file:line | today | final | note |
|---|---|---|---|---|
| — (proactive, no W1 finding id — see "still curating" note below) | `apps/mobile/PatinaDesignKit/Sources/PatinaDesignKit/Components/PatinaEmptyState.swift:66-67` (`#Preview` default — the shape any lane reaches for when it needs a "the catalogue is empty" empty state, e.g. D2's fallback if Leah's ≥30-piece manifest is not in hand by day 6) | `title: "No products yet", message: "Products you capture will appear here, ready to add to a room."` | `title: "Still building the collection", message: "New pieces are added by hand — check back soon."` | Not a W1 finding — flagged proactively because PROGRAM.md §1/§8 name "L1-D's placeholder work" as the D2-fallback UI and the existing default uses "Products" (the noun `C5-09` retires) and implies an AR-capture flow ("Products you capture") that has nothing to do with an empty catalogue. Brand-voice check: "curating" itself is on the lexicon's avoid-list (overused) even though the planning docs use the word informally — "building the collection" says the same thing without it. Apply only if/when a real call site needs this copy; the `#Preview` default is not itself user-facing. |

### L1-E applies (own worktree, own files)

| id | file:line | today | final | note |
|---|---|---|---|---|
| `C5-06` | `PatinaDesignKit/Sources/PatinaDesignKit/Tokens/TimeOfDay.swift:29-41` (`greeting`) | dawn `"Early morning."` · morning `"Good morning."` · day `"Good day."` · afternoon `"Good afternoon."` · evening `"Good evening."` · night `"Good night."` | dawn `"Good morning"` · morning `"Good morning"` · day `"Good afternoon"` · afternoon `"Good afternoon"` · evening `"Good evening"` · night `"Good evening"` | Collapses six windows onto three natural English greetings (dawn folds into morning, day into afternoon, night into evening — "Good night" is a farewell, not a greeting, and nobody says "Good day" or "Early morning" in this register) and drops every terminal period, per the fix. `DailyRoomView.swift:253` reads `TimeOfDay.current.greeting` directly — no other file needs an edit; confirmed no other literal copy of these six sentences exists anywhere else in the app (`grep -rn "Good night\|Early morning\|Good day\b"` returns only this file). |
| `C4-08` | `Features/ARPlacement/ViewModels/ARPlacementViewModel.swift:87` | `self.saveState = .failed(error.localizedDescription)` — for a `RoomsAPIError` (plain `Error`, no `LocalizedError`), `.localizedDescription` renders `"The operation couldn't be completed. (Patina.RoomsAPIError error 2.)"` | `self.saveState = .failed(Self.saveFailureMessage)` where `static let saveFailureMessage = "We couldn't save this. Try again."` (fixed, no interpolation — the underlying `error` is still available to log in `#if DEBUG` at the call site) | — |
| `C4-08` | `Features/ARPlacement/Views/ARPlacementView.swift:111-113` | `case .failed(let msg): toastPill(text: "Save failed: \(msg)", tint: PatinaColors.clay)` | `case .failed(let msg): toastPill(text: msg, tint: PatinaColors.clay)` | Drops the `"Save failed: "` prefix now that `msg` is always the complete sentence above — concatenating would otherwise read "Save failed: We couldn't save this. Try again." |
| `C4-08` (note out, not applied by L1-E) | `Core/Network/RoomsAPIClient.swift:415-419` (`RoomsAPIError`, not an L1-E file — `Core/Network/**` is L1-B's) | `public enum RoomsAPIError: Error { case notAuthenticated, emptyResponse, http(status: Int, body: String) }` — no `LocalizedError`, so any *other* caller that reads `.localizedDescription` repeats this bug | Conform to `LocalizedError`: `.notAuthenticated → "Please sign in to continue."` · `.emptyResponse → "We didn't get a response. Try again."` · `.http → "Something went wrong. Try again."` (never the status code or body) | Sent as an integration note to L1-B (`build/waves/w1/l1-b-notes.md`) — the fix's own second half ("make `RoomsAPIError` conform to `LocalizedError` so no other caller can repeat it") lives in a file L1-E does not own. |
| `C4-09` | `Features/DesignServices/DesignRequestFlowView+Steps.swift:169-171` | `Text(error.errorDescription ?? "Something went wrong.")` where `error: DesignServicesError` | `Text(error.errorDescription ?? "Something went wrong. Try again.")` | Once `DesignServicesError.errorDescription` (below) never returns `nil` in practice, this `??` branch is a defensive default only — unified to the app's one canonical generic sentence so it reads the same as every other unclassified failure. |
| `C4-09` / `C5-11` | `Services/DesignServices/DesignServicesService.swift:201` (`DesignServicesError.invalidRequest`) | `case .invalidRequest(let message): return message` — the raw Postgres/RPC message, verbatim | `case .invalidRequest: return "We couldn't process your request. Try again."` | One of the finding's two "raw arms." The associated `message` stays on the enum case (still available to `#if DEBUG` log) — only `errorDescription` stops returning it. |
| `C5-11` | `Services/DesignServices/DesignServicesService.swift:203-204` (`DesignServicesError.networkError`) | `case .networkError(let message): return "Network error: \(message)"` | `case .networkError: return "Check your connection and try again."` | The other raw arm. Reuses the exact phrase `CompanionAPIError.networkError` already uses (below) so the two services read as one voice. |
| `C5-11` | `Services/DesignServices/DesignServicesService.swift:205-206` (`DesignServicesError.submissionFailed`) | `"Failed to submit your request. Please try again."` | `"We couldn't send your request. Nothing was lost — try again."` | Exact text from the fix. |
| `C5-11` | `Services/Companion/Models/CompanionAPIModels.swift:280-281` (`CompanionAPIError.badRequest`) | `case .badRequest(let message): return message` — raw server text, same defect class as the two arms above, found while in the file for this finding | `case .badRequest: return "That didn't go through. Try again."` | Reuses `NotificationFeedView.swift:144`'s existing phrase verbatim. Not separately cited in the finding's evidence, but it is the identical bug in the file the finding names — fixing it here rather than filing a new id. |
| `C5-11` | `Services/Companion/Models/CompanionAPIModels.swift:286-287` (`CompanionAPIError.serverError`) | `"Something went wrong (error \(code)). Please try again."` — an HTTP status code shown to the reader | `"Something went wrong. Try again."` | Never a status code in front of a homeowner. |
| `C5-11` | `Services/Companion/Models/CompanionAPIModels.swift:290-291` (`CompanionAPIError.decodingError`) | `"Something went wrong. Please try again."` | `"Something went wrong. Try again."` | Drops "Please" to match the app's other failure copy (`AccountDeletionService.failureCopy`: "Try again, or write to…") — one register, not a formal one. |
| `C5-11` | `Features/RoomScan/Views/ScanReviewView.swift:128` (not an L1-E file — `Features/RoomScan/**` is L1-B's; sent as a note) | `Text("Something went wrong")` (no terminal period; the line below it, `loadError`, already carries real app copy — not a raw string) | `Text("Something went wrong.")` | Sent to L1-B — one canonical headline text everywhere a generic failure has no more specific thing to say. |
| `C5-11` | `Design/Components/PatinaErrorState.swift:41,49` (`#Preview` only — not shipped) | `"Something went wrong loading this."` | `"Something went wrong."` | Matches the one canonical headline above; the component itself takes `message` from its caller and has no default of its own, so this only changes the example shown in Xcode's canvas. |

---

## Not applied this wave, with reasons

| id | reason |
|---|---|
| `A3-28` | The fix's migration half — flip `handle_new_user`'s `COALESCE` fallback — is **explicitly reverted by ruling B2 v3** (`build/waves/w1/l1-a-notes.md`): "Two earlier cuts of migration 00555 tried to fix this in the trigger... Ruling B2 v3 reverted both. `handle_new_user` is now 00313 verbatim." `profiles.role` is ruled a label, not an authorization input, and the OAuth-mislabelling half of this finding's own evidence (Apple sign-in landing `role = 'designer'`) is separately closed by `A3-07`'s app-side self-downgrade PATCH (W1 · L1-A, not part of this deck's 18 rows). The remaining "three words for two kinds of person" complaint has no live user-facing string in the iOS app: `grep -rn "\.role\b\|\"designer\"\|\"homeowner\"\|\"client\"" Patina/` finds no rendered role word except `Services/API/ProfileLookupService.swift:39-44`'s `bestName` fallback, which **already** collapses `"client"`/`"homeowner"` to one word, `"Client"` — used only when a thread counterpart has no display name at all. No deck row is needed; the vocabulary reconciliation at the schema layer is W2 · L0.2's `DM-1` `profile_private` split, not a copy change. |
| `C5-14` | Owned by **L1-D** per PROGRAM.md §3 · L1-E's own routing table ("L1-D: `C5-14`... the money formatter's output strings") — L1-D's finding, L1-D's row; not part of this deck because the money-format collapse (`$4,200` vs `$4.2K`) is a formatter-selection problem, not a wording one. No string for L1-E to supply. |
| L0.4's tour "Studio" wording | **Already correct, and already applied — no code change.** `Features/Help/FirstLaunchTour.swift:291-297`'s binary fallback for step 3 already reads heading `"Your Studio"` / body `"Your studio — projects, proposals, invoices and files"` — byte-for-byte what `build/waves/w0/sanity-tour-copy.md` §3 proposes publishing to Sanity. The stale `"Your profile"` text a walker saw came from Sanity's *current production content*, which Sanity overrides the binary with; fixing it is a Kody-run CMS publish (`sanity-publish-steps.md`), not an app-code change, and is recorded here only because it was named as one of the four copy halves to cover. |
| L0.1's seven permission sentences | Recorded in full below; **not applied this wave.** `A2-12`'s deck row is a W2 carry-forward (`build/waves/w1/steward.md` §5.6: "`L0.1` is closed — its deck row (`A2-12`) is a **W2** carry-forward, not a W1 apply target"), because `project.pbxproj`'s `INFOPLIST_KEY_NS*UsageDescription` build settings are not a W1-owned file for any lane. |

---

## L0.1's seven permission sentences — reviewed, held for W2

Source: `build/waves/w0/l01-notes.md` N1. The build settings win over `Info.plist` (already enforced by
`PermissionStringTests` — L0.1 shipped that); these are the strings for whoever owns
`project.pbxproj`'s `INFOPLIST_KEY_NS*UsageDescription` entries in W2 to paste in, unchanged from what
is reviewed here.

| key | today (shipped) | final (reviewed) | note |
|---|---|---|---|
| `NSCameraUsageDescription` | `Patina uses your camera to walk through your space together and visualize furniture in your room.` | `Patina uses your camera to walk through your space and show furniture in your room.` | "together" is a stray collaborative-voice word with nothing on the other end of it (this is a solo camera walk); "visualize" → "show" (plain-spoken, not a design-tool verb). |
| `NSFaceIDUsageDescription` | `Patina uses Face ID to securely confirm sign-in requests from the web` (no terminal period) | `Patina uses Face ID to confirm sign-in requests from the web.` | "securely" is filler — Face ID confirming a sign-in *is* the security, saying so adds nothing; added the missing period. |
| `NSMicrophoneUsageDescription` | `Have a voice conversation with Patina about your space and style.` | `Patina uses your microphone for voice conversations about your space and style.` | Rewritten from an imperative ("Have a...") to "Patina uses X to Y." per `A2-12`'s own fix line — this is the app's first sentence about itself on a system dialog, not a feature pitch. |
| `NSMotionUsageDescription` | `Patina uses motion data to detect when your device is steady for capturing the best room photos.` | `Patina uses motion data to tell when your phone is steady enough for a clear room photo.` | "detect"/"capturing the best" is measurement-instrument language; "tell... clear" says the same thing the way a person would. |
| `NSPhotoLibraryAddUsageDescription` | `Patina saves AR previews and room captures to your photo library when you ask` (no terminal period) | `Patina saves AR previews and room photos to your library when you ask.` | "captures" → "photos" (one noun for the same thing this permission set already calls "room photos" elsewhere); added the missing period. |
| `NSPhotoLibraryUsageDescription` | `Save room designs and furniture visualizations to your photo library.` | *(no rewrite — recorded, not deleted)* | L0.1's own note confirms this permission has **no real call site** (`grep` for `PHPickerViewController`/`PHPhotoLibrary`/`UIImagePickerController`/`PhotosPicker` returns nothing app-wide) and is a W2 candidate for deletion, not a rewording. Rewriting a string for a permission the app does not use would be polishing something that should be removed instead — held rather than rewritten. |
| `NSSpeechRecognitionUsageDescription` | `Speak naturally with Patina instead of typing.` | `Patina uses speech recognition so you can talk instead of type.` | Same imperative → "Patina uses X to Y." rewrite as the microphone string above — the two voice permissions should read as one pair, not two different registers. |

---

## Tests this deck's rows are pinned by

Written to `PatinaTests/` on this lane's own clone (`build/waves/w1/l1e-tasks.md` has the task-by-task
breakdown): `ErrorVoiceTests`, `NounConsistencyTests`, `BrandVoiceLintTests`, `GreetingWindowTests`,
`PluralisationTests`, `SentenceCaseTests`, `GuestPromiseTests`. The three that assert over files this
lane does not own (`NounConsistencyTests`, most of `SentenceCaseTests`, most of `GuestPromiseTests`) are
scoped to the exact file:line locations this deck names — a targeted regression net, not a whole-repo
sweep (the whole-repo version of that sweep is W2 · L1-E's own 48-row table). They will not go fully
green on this lane's own clone until the owning lanes above have applied their rows and the branches
merge, per PROGRAM.md §3 · L1-E's own exit criteria ("a row in the deck that no lane applied fails
`NounConsistencyTests` or `ErrorVoiceTests` and comes back as a fix round") — that is the designed
behaviour, not a defect in this deck.
