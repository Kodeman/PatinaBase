# The Document — "The Smart Lens" proposal program

## Context

Kody (2026-08-28): the Document is close to feeling right after the Life Review (R126, live on prod as Worker 9c0c2cdd). What still fails: the **spine** is cluttered and under-used as navigation; the **header** holds great information but eats most of the viewport when open; **sections** are crammed into each other; the **margin** is cramped for what it carries. Ask (verbatim in every brief): a team of UI/UX designers with Patina engineering and interior-design teams designs the Document so it holds the needed information and actions while feeling uncluttered and peaceful — animation, content that appears when needed and yields space when not in frame; the document as a **smart lens always adjusting focus** as the designer moves. Deliverable: the **strongest single proposal** as an HTML document, plus a **high-fidelity mockup** showing how the team will accomplish it.

**Rulings taken at plan time (Kody):**
- **Canon latitude: clean sheet.** `docs/design/the-document/DECISIONS.md` (last id R126) is context, not constraint. Amendments are never priced or penalised; the canon-context refuter merely *labels* what a move amends, for the record. Four hard no-gos stand: **NG1** D1 one document at a time (no split view / tabs / peek-hold / persistent global nav); **NG2** D4 shadow budget — one `--elevation-sheet` token at three sites (margin chip, ledger sheet, studio drawer), zero other shadows (computed-style sweep); **NG3** no Thumb Index; **NG4** the R126 visual register is the floor — 40px Playfair letterhead, 24px region heads, scale 40/24/18/15/14, mono 11px floor, three rule weights, paper `#FCFAF6` / rail `#E8E3DB` / desk `#FAF7F2`, filled stamps, stage tab plates, hover wash, 48px thumbs — built on, never restyled; "typography goes no further". THE STUDIO desk block untouched.
- **Team: full house (~40 seats, 64 agent calls)** — U1–U5 lenses, P1–P4 personas on T1–T16, a new E1 engineering seat, three refuters, two rival Opus authors, four critics, two judges, a merge author, mockup builder + adversarial prober, deck authors, fact-check + visual QA. Fable orchestrates, reviews every gate, publishes; never executes. Opus = authoring/critique/judging/mockup build/code-truth; Sonnet = research/capture/build/QA/deck prose (explicit scope, exact output path, exact gate, "deliver exactly what is asked"); Haiku = mechanical. Reviewer briefs: "report EVERY finding with severity AND confidence — never filter". Implementer ≠ reviewer, always.
- One proposal, **two Artifacts**: the deck *The Smart Lens* (favicon 🔍) and a separate scroll-driven clickable mockup (favicon 🔭). Same pattern as Life Review (f333f3f1… / bf781dba…).
- Evidence = local dev on post-R126 main (Aspen Loft `b0000000-0000-0000-0000-0000000000d1` + a resolved pre-work doc; `designer@patina.dev`); widths 1440 / 1280 / 390; **four scroll states per doc** (s0 top · s1 seam pinned · s2 FF&E head · s3 foot) are the unit of analysis. **No product code changes in this program.**

## What exploration established (the diagnosis the deck argues; re-measured in W1)

- Shell `page.tsx:1760` — `<1180` single column; `1180–1439` `56px|1fr` glyph spine + margin sheet; `≥1440` `200px|1fr(max 1040)|232px`. Paper `px-7/10/12 pt-8 pb-32`.
- **Spine** `doc-spine.tsx` (sticky h-screen, rail stock): Put down → seven StrataMark markers **in a horizontal row** at ≥1440 → active label pair → `spine-running-index.tsx` (scroll-spy only on the project spread: approvals/schedule/ffe/money; **empty on brief/discovery/direction/proposal**) → timer box → presence line. ~⅔ of the rail empty; top third mixes four tenses (08-14 second-look diagnosis still true).
- **Header stack** before the first region head at 1440 ≈ 700–790px of 900 (**78–88 %**): letterhead (40px title + household + 11px vitals + Phases fold + optional 44px in-hand row) → `job-ticket.tsx` (sticky `top-0 z-[4]`, 8 rows ≈ 300px, folds to a 2-line seam once the letterhead scrolls past via sentinel `doc-ticket-sentinel`, publishes `--doc-seam-height` read by `money-region.tsx:48` and every `[data-index-region]` scroll-margin) → red-letter zone XOR `document-guide.tsx` → `letterhead-instruments.tsx` → approvals `mt-6 py-6` → schedule frame `mb-4` → stage line.
- **Sections**: `region/region-head.tsx` owns no spacing; inter-region gaps are per-site (`mt-6/py-6`, `mb-4`, `mt-2`, `mt-5`, `mb-5`) — no token. `use-region-fold.ts` folds by **unmounting** the body, leaving a 44px `FoldSeam` indistinguishable from empty. **Margin** `margin-rail.tsx` 232px holds first-touch note, file-change notes, capture row, drafts, chips (`doc-elevated`), composer, handoffs.
- **Motion vocabulary** (`globals.css`): doc-raise 270ms · doc-sheet-up · doc-breath (only ambient) · fold-in/arrow-flip 300ms `--ease-editorial` · desk-settle 60ms stagger · strata-sweep · `.row-wash` ink-pool 260/200ms · 12 reduced-motion blocks. No Tailwind keyframes.
- **Test blast radius**: `e2e/document/quiet-responsive-shell.spec.ts` (ticket rows = 8, spine widths), `__tests__/job-ticket.test.tsx` (asserts `--doc-seam-height`), `responsive-document-shell.test.tsx`, `shelved-spine.test.tsx`, `doc-spine.test.tsx`, `region/__tests__/*`, and the trap `lib/document/__tests__/stage2-approval-cutover-contract.test.ts:19` (1500-char regex between `data-active-section` and `<SectionStageLineMount`).
- Reusable toolchain (verified): life program's `mock/kit.css` + `KIT.md` + `assets/fonts/fonts-data-uri.css` (R126 register already in it), `deck-parts/build.mjs` (PARTS, FRAGMENT scaler, hard gates: 0 box-shadow, hosts = fonts.googleapis/gstatic + one `claude.ai/code/artifact/`, 0 non-ASCII, 15.5 MB fail), `qa-run.cjs`, `00-head/99-script.html`, `DECK.md`, `research/contrast-check.mjs`; wayfinding's `source/workflows/w2-panel-v2.js` (panel → disk-based collator → refuters → scribe), `instruments.md` (T1–T16, P1–P4, U1–U5, finding schema, judge rubric, Vandersteen specimen §8), `research/wayfinding-shots.mjs`, `02-steward-boot-off.md`; life's `mock/final/{index.html, host-sim.mjs, shoot-final.mjs, review-clickthrough.mjs, tokens.css}`.

## Program folder — `artifacts/document-lens-proposal-2026-08-28/`

```
RESUME.md  presentation.html
research/  00-env-and-ids.md 01-shot-ledger.md state-ladder.json 02-steward-{boot,kill}.md dev-boot.log
           capture-shots.mjs measure-layout.mjs(NEW) contrast-check.mjs
           10-code-anatomy.md 11-canon-digest.md 12-layout-measurements.{md,json}
           20..24-panel-u{1..5}.md 25..28-panel-p{1..4}.md 29-panel-e1.md
           30-collated-findings.{md,json} 31-verified-findings.{md,json}
           33-verify-code-truth.{md,json} 35-verify-canon-context.{md,json} merge-verdicts.py
           60-deck-factcheck.md 61-deck-visualqa.md 62-deck-revision-log.md VERIFICATION.md
shots/     w1440-* w1280-* m390-*  (24 scroll-series + clips + one reduced-motion)
probe/     03-interactive-probe.md 34-verify-repro.{md,json} repro-*.png *.mjs
source/    plan.md instruments.md specimen.md brief.md mechanics.md rubric.md shared-planks.md
           proposal-x{-v1,}.md proposal-y{-v1,}.md critique-{design,feasibility,practitioner,access}.md
           judge-{practitioner,feasibility}.md proposal.md roads-not-taken.md
           workflows/{w2a-panel,w2b-verify,w3a-drafts,w3b-judge,w4a-mock,w4b-mock-fix,w5a-deck,w5b-deck-fix}.js
mock/      kit.css KIT.md lens.css(NEW) assets/fonts/ img/ fragments/*.html
           deck-parts/{00-head…15-colophon,99-script}.html build.mjs qa-run.cjs DECK.md
           deck-assets/*.jpg (built, committed)  deck-qa/ (NOT committed)
           final/{index.html SPEC.md FINAL.md tokens.css shoot-final.mjs host-sim.mjs review-clickthrough.mjs REVIEW.md REVIEW-2.md shots/ review-shots/}
build/     gitignored scratch
```
Scratchpad is wiped on resume — every agent output goes to an absolute path in this folder.

## Phase map (Workflow scripts ≤ 12 agents each; Fable checkpoint between every one)

| Wave | What | Mechanism | Calls | Fable gate |
|---|---|---|---|---|
| W0 | Scaffold + kit port; instruments | 2 Agent calls | 2 | `SMOKE=1 node mock/deck-parts/build.mjs` exit 0 |
| W1 | Steward boot · capture (scroll series) · measure-layout · code anatomy · canon digest · interactive probe | 6 Agent calls (2 parallel groups) | 6 | PNGs open; `-s1` really pinned; `headerStackPctOf900` present |
| W2a | Panel U1–U5 + P1–P4 + E1 → disk-based collator | `w2a-panel.js` | 11 | `30-collated-findings.json` parses; ≥1 finding/seat |
| W2b | Refuters V1 code-truth · V2 repro · V3 canon-context → scribe | `w2b-verify.js` | 6 | `31-verified-findings.json` parses; survivors > 0 |
| W3a | Planks → two rival authors → four critics | `w3a-drafts.js` | 7 | X and Y structurally different |
| W3b | Two revisers → two judges → merge author | `w3b-judge.js` | 5 | `source/proposal.md` unhedged; §8 names real files/tests |
| W4a | kit-extend → mockup builder → shooter + host-sim → adversarial prober | `w4a-mock.js` | 5 | 0 external; box-shadow = 3 token sites; `__mockReady` |
| W4b | builder v2 → reshoot → probe 2 → fragment cutter | `w4b-mock-fix.js` | 4 | probe 2 zero Blocking; `FRAGMENTS 12` |
| — | Fable publishes the mockup Artifact (🔭) | — | 0 | opens in Chrome at ~400px |
| W5a | 8 part authors → build + qa-run → fact-check + visual QA | `w5a-deck.js` | 11 | build exit 0; `qa-results.json` clean |
| W5b | part fixers → rebuild (loop) | `w5b-deck-fix.js` | 5 | zero Blocking, zero UNSOURCED |
| — | Fable publishes the deck Artifact (🔍) | — | 0 | both URLs recorded |
| Close | RESUME + pathspec commit + memory | 1 Agent call | 1 | `git status` clean of program paths |

## W0 — Scaffold + kit port

**A0 · Sonnet, sandboxed.** Create the tree. Port verbatim: life `mock/kit.css`, `assets/fonts/` (7 files), `img/` (5 crops), `mock/final/{shoot-final.mjs,host-sim.mjs,tokens.css}` (paths only), `research/contrast-check.mjs`. Port with deltas: `deck-parts/build.mjs` (ROOT; PARTS = the 17 files in W5; replace `@@DIR_A|B|C@@` with one `/* @@LENS@@ */` injecting `mock/lens.css`; keep `@@FONTS@@`/`@@KIT@@`, the FRAGMENT scaler + `.dk-mock-scale` light-pin, crop inliner, sips 804/q78, ASCII fold, every hard gate incl. the `claude.ai/code/artifact/` host allowance); `qa-run.cjs` (ROOT; SECTIONS = 15 ids; theme key `dk-document-lens-theme`; keep box-shadow sweep, contrast sampler, fit/overflow/sticky-index probes); `00-head.html`/`99-script.html` (key, 15 index rows); `DECK.md` (15-section contract; keep "no `<img src=img/>` in fragments", 0-shadow checklist, ≤30k chars/part, F-id resolution); `research/capture-shots.mjs` from wayfinding's shots master (ids from *this* `state-ladder.json`; shot list in W1; run from `apps/designer-portal`); `02-steward-boot.md` recipe. Write `mock/lens.css` scaffold (`.lens-*` namespace header only), `source/plan.md` (this plan), `source/specimen.md` (Vandersteen, verbatim from wayfinding instruments §8).
**Gate:** `SMOKE=1 node mock/deck-parts/build.mjs` exit 0 on `mock/fragments/_smoke.html` (box-shadow 0, non-ascii 0, <1 MB); `grep` kit for `--doc-paper`, `--doc-rail-stock`, `--elevation-sheet`.

**A0b · Opus, sandboxed → `source/instruments.md` + `source/brief.md` + `source/mechanics.md` + `source/rubric.md` + `mock/final/SPEC.md`.** Contents (Fable already holds the drafts from planning; the agent lays them down and adjusts):
- §1 T1–T16 verbatim from wayfinding + two columns per task: *where in the scroll it lives* · *what I passed to get here*. §2 P1–P4 verbatim + mandatory walk line `Frame budget: of the screen in front of me, what fraction was carrying this task?`. §3 U1–U5 rewritten for this ask (below). §3b E1. §4 finding schema + `scroll_state` (`top|seam|mid|foot|all`) + `why_it_blocks ∈ clutter|crowding|orientation|information-loss|motion` + `frame_cost_estimate`. §5 the CANON LATITUDE block (NG1–NG4; "amend freely; do not price"). §6 proposal contract. §7 judge rubric. §8 specimen.
- **`brief.md`** — the thesis in Patina's language (*what is in frame is full · what is out of frame yields (condenses, never disappears) · the spine is the map, not a second copy of the paper · nothing hides; every condensation reversible in one act and readable without hover*; refuse the filtering counter-thesis — the room lens lifts, never filters); the four organs with their questions: **Spine** S1 what does the rail earn as the map · S2 does the seven-mark row belong in the spine or the header · S3 index every region on every spread incl. pre-work (and what a value line reads with no number) · S4 where timer / presence / Put down live · S5 200 vs 160 vs 56-with-leaf, with the 1440 arithmetic (200+1040+232 = 1472 > 1440). **Header** H1 sort all 8 ticket rows into ORIENTATION / DOORS / FACTS · H2 can letterhead+ticket+guide+instruments collapse to one 48–64px lens line (max five things: title · stage plate · worst exception · money rung · install date — argue each) · H3 what is at scroll 0 · H4 the one reversing act, without hover · H5 zero layout shift (R99's pin-at-reduced-height precedent). **Sections + margin** R1 one region-spacing token (name, value, adopters, exceptions) · R2 reading-line density defined precisely (ink %, which elements, do acts print, exceptions never go silent) · R3 how density coexists with `use-region-fold`'s explicit fold (folded-by-choice vs condensed-by-position, distinguishable on sight) · R4 what the 232px must hold vs the paper's gutter · R5 which margin tenses survive at rest. **Motion** M1 grammar table `name · trigger · property · duration · easing · reduced-motion form · site` (moves not in the table do not exist) · M2 what animates on condense (never layout, or say why) · M3 hysteresis rule proven at 4× · M4 the breath stays the only ambient motion unless named · M5 reduced motion is a *form*, never "n/a". Success criteria **SC1–SC13** the mockup probe must print: SC1 first region head y at 1440 rest ≤ 405px (45 %) · SC2 condensed ≤ 108px (12 %) · SC3 lens-line height at scroll 0/400/1200, condensed ≤ 64px and stable · SC4 rail utilisation ≥ 70 % project / ≥ 55 % pre-work · SC5 hover-only acts = 0 · SC6 reduced-motion 0 animating after 1s in every dev-bar state · SC7 composite contrast ≥ 4.5 · SC8 box-shadow on exactly the three token classes · SC9 external 0 · SC10 overflow 0 at 1440/1280/390 · SC11 density map at three offsets, exactly one `full`, no region with zero readable text · SC12 spine reading-line index agrees with the `full` region, never null · SC13 tab-through reaches every act in order with a visible ring. The keeps (NG1–NG4, Kody's taste on record: large tints "silly/terrible", colour on small state-carrying things, "sections and animated highlighting" loved). Required deliverable shape = §6 contract.
- **`mechanics.md`** — the catalogue authors may adopt/adapt/refuse (must name each): M-1 The Lens Line · M-2 The Map Rail (vertical ladder + full index + leaf doors + Put down top, timer glyph foot) · M-3 Reading-line Density (full/reading/condensed via IO with hysteresis; bodies stay mounted) · M-4 The Gutter Margin (chips as one-line pins in the paper's gutter; column → ~160px) · M-5 Section Zoom · M-6 Focus Follows the Pen (flagged high-risk: dimming = large tint) · M-7 The Ticket Dissolved (rows redistributed by H1; `ticket-derivation.ts` fate) · M-8 The Standing Rule (in-frame region head pins under the lens line, R99 style) · M-9 The Quiet Foot · M-10 Tempo Damping (velocity-gated transitions with a deterministic `settle()` hook). Conflicts: M-1⟷M-8 (top-band budget) · M-2⟷M-4 (who owns the paper's index/pins) · M-3⟷M-5 (continuous vs discrete; needs precedence) · M-3⟷M-6 · M-2⟷M-8 (redundant position signal); M-7/M-9/M-10 compose.
- **`rubric.md`** — seven axes, 1–10, never averaged: 1 Uncluttered & peaceful · 2 Lens honesty (nothing hides; reversible; readable without hover; condensed ≠ empty on sight) · 3 Orientation at depth (T1–T16; ≤2 acts at all tiers) · 4 Engineering credibility (waves valuable alone; `use-region-fold` voices, ticket seam/`--doc-seam-height`, running-index band answered; tests enumerated; rollback stated) · 5 Motion discipline (grammar complete; real reduced-motion forms; no tint larger than a row; hysteresis stated; 0 layout shift) · 6 Still Patina (R126 intact; new organs look native) · 7 The 390 form (same lens, one column). **Automatic returns** = NG1–NG4 violations and any hover-only affordance. (No "unpriced amendment" return — Kody's ruling.) J1 practitioner scores 1/2/3/7 naming the persona per score; J2 product/engineering scores 4/5/6 citing a file per cost; both name who is worse off and end with a merge instruction. Critic standing assignment: *at every scroll offset, list everything on screen that is a second copy of something else on screen.*
- **`mock/final/SPEC.md`** — see W4.
**Gate:** Fable reads §3, §3b, §6, §7 and the brief; U-lenses must ask about *this* problem (frame budget, condensation, orientation-at-depth), not wayfinding text with words swapped.

## W1 — Evidence and grounding

- **S1 Steward · Sonnet, `dangerouslyDisableSandbox` for boot/kill/capture only.** `lsof -i :3000 -t` first; kill if foreign. Boot from repo root: `NEXT_PUBLIC_DESIGNER_PORTAL_DATA_MODE=live NEXT_PUBLIC_FLAG_OVERRIDES='call-sheet:true,arrival-arc:true,room-file:true,studio-workspaces:true' nohup pnpm dev:designer > research/dev-boot.log 2>&1 &`. Poll `/desk` for 200/307 (≤8 min); hit `/auth/signin` once; pre-warm the Aspen doc. Resolve a pre-work doc id (brief/proposal stage for designer `a0000000-…-0004`) via `psql postgresql://postgres:postgres@127.0.0.1:54322/postgres` on `document_state`; apply `scripts/the-document-reaudit-walk-seed.sql` (red-letter + money non-empty). Write `state-ladder.json` + `02-steward-boot.md`. Leave it running through W2b. **Gate:** `state-ladder.json` names ≥2 doc ids.
- **C1 Capture · Sonnet, unsandboxed.** `node …/research/capture-shots.mjs` from `apps/designer-portal` (login via `e2e/fixtures/auth.ts`, `hide-dev-overlays.ts`, welcome-shown localStorage). **Scroll series**: {rich, prework} × {1440×900, 1280×900, 390×844} × {`-s0` scrollY 0 · `-s1` `letterheadBottom+1` (seam pinned) · `-s2` `[data-region-head="ffe"]` at top minus seam · `-s3` foot} = 24 viewport shots, each **asserting the expected element position before firing** (e.g. `letterhead.bottom < 0` for `-s1`) and failing loudly. Clips at 1440: `spine-full`, `spine-running-index-mid`, `ticket-unfolded`, `ticket-seam`, `letterhead-vitals-phases-open`, `margin-rail`, `region-head-ffe`, `fold-seam-folded`, `guide`/`red-letter-zone`, `instruments-row`; 1280: `spine-glyph-rail`, `margin-tab-closed`, `margin-sheet-open`; 390: `mobile-bar`, `mobile-spine-sheet`, `mobile-margin-chips`; plus `w1440-rich-s1-reduced.png` (`reducedMotion:'reduce'`). `01-shot-ledger.md` with a capture-caveats section (fullPage drawer-strip artifact). **Gate:** Fable opens `w1440-rich-s0/s1/s2`, `w1280-rich-s1`, `m390-rich-s1`; `-s1` must differ from `-s0` with the letterhead gone.
- **M1 Measure · Sonnet, unsandboxed — writes + runs `research/measure-layout.mjs` (NEW).** Per doc × width × scroll state → `12-layout-measurements.{json,md}`: header stack rects (`#document-project-status`, `[data-job-ticket]` both fold states, guide XOR `[aria-label="Needs attention"]`, instruments, approvals band, schedule frame) → `firstRegionHeadY`, `headerStackPctOf900`, ticket unfolded/seam heights, live `--doc-seam-height`, `[data-ticket-row]` count; **spine utilisation** (`inkPx / railHeightPx`, longest empty run, interactive-child count, marker-row extent; 1440 and 1280 with what 1280 loses); **inter-region gaps** (px between consecutive `[data-region-head]` blocks → the *set of distinct values* = the no-token evidence); **margin** (`[data-margin-panel]` width/mode, chip count/heights, `chipStackPx/railHeightPx`); **frame budget** per scroll state — px of 900 that are (a) chrome carrying no task content, (b) header/summary, (c) the region she is in. **Gate:** JSON parses; `headerStackPctOf900` at 1440 rich s0 non-zero; distinct-gap set ≥ 3; Fable spot-checks two numbers against PNGs.
- **G1 Code anatomy · Opus, sandboxed, parallel → `10-code-anatomy.md`**: shell · header stack in mount order with file:line · spine · margin · disclosure already shipped (trigger, persistence, what unmounts) · region heads + the spacing-site table · motion inventory + the 12 reduced-motion blocks named · every `--doc-seam-height` consumer · **test blast radius** (≥6 files, the 1500-char regex trap called out). **Gate:** 6 file:lines spot-checked with `sed -n`.
- **G2 Canon digest · Sonnet, sandboxed, parallel → `11-canon-digest.md`**, framed "what exists and why": (A) NG1–NG4 verbatim + lines; (B) the R126 register quoted; (C) standing context one line each (D2, D8, D12/D13, R8, R15, R99, I107, I135, I136, I137, R124, R125, open items: F24 390 overflow, aged-oak 3.51:1, reduced-motion 39 offenders, I114, T2/T4) each ending `— context; amendable`. `grep -n`/`sed -n` only, never the whole 10k-line file. **Gate:** every quote greppable at its line.
- **PR1 Interactive probe · Sonnet, unsandboxed, after C1 → `probe/03-interactive-probe.md`**: ticket fold/pin timing and whether content jumps; scroll-spy `aria-current` handoff points; `use-region-fold` unmount/remount + focus destination; Esc chain; ⌘K; hover-wash timing; margin sheet at 1280; **CLS total** via `PerformanceObserver` over a scripted 0→foot scroll at 1440, normal and reduced motion. **Gate:** CLS present for both registers.

**Fable checkpoint:** reads `12-layout-measurements.md` and `10-code-anatomy.md` §6/§9 before W2.

## W2 — The panel (`w2a-panel.js`, modelled on wayfinding `w2-panel-v2.js`) and refutation (`w2b-verify.js`)

Evidence pack (`EVIDENCE` const): instruments §§1–5,8 · `10-code-anatomy.md` · `11-canon-digest.md` · `12-layout-measurements.{md,json}` · `01-shot-ledger.md` · `03-interactive-probe.md` · minimum shot set to actually Read: `w1440-rich-s0/s1/s2/s3`, `w1440-prework-s0/s1`, `w1280-rich-s0/s1`, `m390-rich-s0/s1/s2`, `w1440-spine-full`, `w1440-ticket-unfolded`, `w1440-ticket-seam`, `w1440-margin-rail`, `w1280-spine-glyph-rail`, `w1440-region-head-ffe`, `w1440-fold-seam-folded`, `m390-mobile-bar`. Caveats verbatim: synthetic seed; drawer-strip artifact; **a finding with no `scroll_state` is out of scope**; you cannot click — say what you expect, cite the anatomy line for what actually happens.

Seats (all write full reports to disk; return a structured summary):
| Seat | Model/effort | Focus |
|---|---|---|
| U1 Attention & focus architecture | opus/high | at each of 4 states × 3 widths, name every element as *carrying my task / orienting me / neither*; reconcile with the frame budget; which header block answers an already-answered question; the minimum that never leaves the frame; where always-visible costs more than it gives |
| U2 Disclosure & scent | sonnet/medium | inventory every disclosure (trigger, persistence, unmounts, folded-vs-empty); what scent survives condensation; where a designer is surprised by a disappearance; which regions may yield and which never (money, red letter); what a condensed state must still *say* |
| U3 The spine as instrument | sonnet/medium | ink vs void against §spine; the horizontal marker row vs a ladder; the pre-work empty rail; the 1280 glyph rail's losses; what a navigator would need to know |
| U4 Motion & choreography | sonnet/medium | the meaning of each existing move; honest easing/duration/threshold for scroll-driven condensation; layout-shift/momentum/hysteresis risks; the reduced-motion equivalent of every behaviour; which of the 12 blocks needs a sibling |
| U5 Reach | sonnet/medium | WCAG 2.2 AA: landmarks; focus when a region unmounts under the caret; 2.4.11 focus-not-obscured under a pinned seam; 390 targets; T1–T16 at 390; hover-only; contrast per lens state |
| P1 Solo principal (Leah-like) | opus/high | T1–T16 + T1 "back after ten days"; frame-budget line per task |
| P2 Three-person studio principal | sonnet/medium | oversight under D1 across eleven jobs |
| P3 Junior, week one | sonnet/medium | every undefinable label; **every element that disappeared on her and what she thought happened** |
| P4 FF&E/procurement | sonnet/medium | T13/T14; every ledger-sheet exit and whether return preserved scroll |
| E1 Engineering feasibility (NEW) | opus/high | with file:line + cost band: (1) condensing the header against the ticket's sentinel/`sticky top-0 z-[4]`; dynamic `--doc-seam-height` vs `money-region.tsx:48`; `animation-timeline: scroll()` viability + JS fallback; (2) focus-yielding regions vs `use-region-fold`'s unmount (IO thresholds? `content-visibility`? React 19 concurrency); (3) extending the running index to all seven stages + the 1280 tier; (4) per-test break/rewrite/delete incl. the 1500-char regex; (5) the three riskiest things any lens could ask for. Assess, do not design |

Collator (opus/high) reads the ten reports **from disk**; merges same defect + surface + scroll_state; MAX severity/confidence; never drops a single-seat finding; writes `30-collated-findings.json` in ≥2 `cat >> … <<'EOF'` chunks, validates with `python3 -c "import json;…"`, returns only a compact index (the 64k trap); `30-collated-findings.md` adds the frame-budget means per scroll state. **Gate:** JSON parses; every seat has ≥1 finding; Fable reads U1, E1 in full and P1's T1/T3/T4.

**`w2b-verify.js`:** steward boot-if-down → parallel **V1 code-truth** (opus/high: stands / narrows / misread with file:line → `33-…`) · **V2 repro** (sonnet/high: Playwright at the finding's width **and scroll state**, scripts under `probe/`, PNG per repro → `34-…`) · **V3 canon-context** (sonnet/medium, **for the record only**: `open` | `amends:<id>` (quote ≤25 words; not a veto, not priced) | `blocked:NG1–4` (the only veto) | `misread` → `35-…`) → steward kill → scribe writes/runs `merge-verdicts.py` → `31-verified-findings.{json,md}` (`survives = code_truth≠misread ∧ repro≠not-reproduced ∧ canon≠misread`; `blocked:*` rows kept, flagged). **Gate:** survivors > 0; Fable reads the header counts and blocked rows.

## W3 — The proposal

**`w3a-drafts.js`:** Planks author (opus/high → `shared-planks.md`: the moves the evidence forces regardless of thesis, adopted identically by both proposals and drawn identically in the mockup — expected: one region-spacing token, folded ≠ empty legibility, reduced-motion parity, "nothing she is reading moves under her", the pre-work index gap; each cites F-ids) → **two rival Opus/high authors in parallel**, neither sees the other: **X "the spine is the lens"** (the rail tracks depth and adjusts what it offers; the header yields to it) · **Y "the paper is the lens"** (the header condenses into a living seam that changes what it says as you descend; sections take and give focus; the spine simplifies to a quiet ladder). Both briefs: the seed is a posture not an instruction — if the findings say the seat is wrong, say so and answer the ask your own way; converging on the other author returns both. Inputs: `brief.md`, `mechanics.md`, `31-verified-findings.md`, `12-layout-measurements.md`, `10-code-anatomy.md`, `29-panel-e1.md`, `shared-planks.md`, `KIT.md`, `specimen.md`, `patina-brand-voice` skill. → **four critics in parallel**, fresh contexts, both proposals each: **C-design** (opus: uncluttered or merely emptier; does the metaphor survive a real Tuesday; still Patina paper; where a deletion beats a mechanism) · **C-feasibility** (opus: every mechanism against the tree with file:line; every test broken; browser-feature dependence; layout-shift risk) · **C-practitioner** (opus, P1 recast: walk T1, T3, T4, T7, T9, T10, T13 through the mechanics; every recall-instead-of-recognise moment is a defect) · **C-access** (sonnet: reduced-motion parity; focus under condensation; 2.4.7 / 2.4.11 / 1.4.13 / 2.3.3 / 1.4.3; 390 targets; SR announcement of trigger-less state change). All: number defects Dx-nn with severity + confidence, never filter; end with the 7-axis scorecard per proposal.

**Proposal contract (instruments §6, identical for X, Y, merge):** 1 Thesis ≤120 words, one falsifiable sentence · 2 What stays identical (mandatory) · 3 Lens mechanics table `trigger · what changes · from→to · duration & easing · reduced-motion equivalent · what never moves · F-ids` · 4 Organ by organ (spine · header · region heads + spacing · margin · motion grammar · 1280 · 390) with before→after and mount-order consequence · 5 The lens state machine (`at rest · reading · editing · condensed · mobile`: lens line, rail, region densities, margin, entry/exit triggers, reverse of every transition) · 6 **Frame budget** table vs `12-layout-measurements.json` — today's chrome/header/work split per state × width and the target (the falsifiable claim; SC1–SC4 targets) · 7 Findings addressed (every blocker/high answered or refused with reason) · 8 Canon note (builds on R126; what it changes that a ruling describes — named, not priced; NG1–NG4 untouched, each with how) · 9 **Engineering path** — waves; per wave: files (real paths), mechanism, what `use-region-fold`'s three voices / the ticket seam + `--doc-seam-height` / the `-20% 0px -62% 0px` observer + 700ms jump lock become, tests rewritten or deleted (real paths), gates that stay green (`shadow-gate.test.ts`, `contrast.test.ts`), rollback · 10 Risks (≥5, each with the falsifying observation) · 11 Refuses (≥4).

**`w3b-judge.js`:** two fresh Opus revisers answer every Dx (fix / accept-and-narrow / drop-with-reason; v1 kept as `-v1.md`) → **J1 practitioner** + **J2 product/engineering** (opus/high, rubric §7; name the persona/file per score; who is worse off; explicit merge instruction: which organ from which proposal, what dies) → **merge author** (opus/high, a third seat) writes `source/proposal.md` to the contract — ONE proposal, no "option A or B" anywhere — and `roads-not-taken.md` (4–8 roads: why attractive, the one thing that killed it, who said so, quoted). **Gate:** `ls` every file/test path in §9; §3 reduced-motion column has no empty cell; §6 has target numbers; `grep -in "option\|either\|or we could" source/proposal.md` hedges nothing; Fable reads the whole thing.

## W4 — The clickable mockup (`w4a-mock.js`, `w4b-mock-fix.js`)

**SPEC (`mock/final/SPEC.md`):** one file `mock/final/index.html`; **scroll-driven, not state-switched** — three frames on one stage, each `overflow-y:auto` with the paper inside and IntersectionObservers rooted at the frame (`{root: frameEl}` — a `root:null` observer inside a scaled overflow frame reports nonsense): `#frame-1440` (1440×900, rail | paper | margin), `#frame-1280` (1280×800), `#frame-390` (390×844, mobile bar). `fit()` scale-down only. Data = Vandersteen specimen verbatim; the paper long enough to scroll (letterhead, header, approvals, schedule, Pieces with all four rooms ≥16 real lines, Money, Care, Record, colophon). Tokens = life `tokens.css` `:root` verbatim + only `--lens-h-open`, `--lens-h-closed`, `--doc-region-gap`, `--density-ink-*` (each new colour with its computed ratio in a comment). **Probe contract:** each frame root carries `data-lens-state ∈ rest|reading|editing|condensed|mobile`; region roots `data-region` + `data-density ∈ full|reading|condensed`; rail publishes `data-reading-index`; lens line publishes `data-lens-open` + `--lens-height`; `data-motion ∈ normal|slow|reduced`. **Dev bar** (`data-go`, `aria-pressed`): Rest · Condensed (scroll 400) · Region in focus (ffe full) · 1280 · 390 · Reduced motion (same selector list as the media query, never a duplicated ruleset) · Slow motion 4× (every duration written `calc(260ms * var(--motion-scale,1))`). **JS**: one IIFE in try/catch — `fit · lens(frame)` (sentinel above the sticky element, reserves open height → zero layout shift) · `focus(frame)` (two IO bands = hysteresis; exactly one `full`; `settle()`) · `spine(frame)` (subscribes to focus, no second observer; 700ms jump lock) · `motion()` · `devbar()` · `ink()` (stamps ink once) · `pointAt()` (wash) · `sheet()` (ledger dialog, focus trap) · `window.__mockReady = true` + `window.__lensSettled()` promise; delegated listeners only; `__mockInit()` + `readyState` guard (host inserts after load; verified by `host-sim.mjs`). Pure ASCII (`LC_ALL=C grep -n '[^ -~\t]'` empty); fonts + one crop as data URIs; zero external; ≤ 2 MB target, 16 MB hard cap; static markup + CSS paints the rest state if init fails.

**`w4a-mock.js`:** M0 kit-extend (sonnet → `mock/lens.css`, `tokens.css`; `grep -cE 'box-shadow|drop-shadow'` = 0) → **MB builder** (opus/high, from `proposal.md` + SPEC + specimen; names the scroll mechanism and reason in `FINAL.md`; "deliver exactly this; name the exact gate") → parallel **shooter** (sonnet, unsandboxed, from `apps/designer-portal`; 1560×1060 @2×; waits fonts + `__mockReady` + 700ms; every dev-bar state and scroll jump → `mock/final/shots/`; prints external count, computed box-shadow census, page errors, size) + **host-sim** (sonnet, unsandboxed) → **MR prober** (opus/high, ≠ MB) writes/runs `review-clickthrough.mjs` (ported from life's 14 items, extended): 1 external = 0 · 2 page errors = 0 · 3 `__mockReady` under file:// and host-sim · 4 box-shadow = exactly the three sites, value `rgba(44,41,38,.08) 0 1px 2px` · 5 non-ASCII 0 · 6 every dev-bar state reachable **and reversible** · 7 **condensation reaches steady state** (20-step slow scroll through the threshold; no oscillation) · 8 **CLS = 0** over a scripted 0→foot scroll · 9 nothing moves under the pointer · 10 **reduced-motion parity** (diff visible text animated vs reduced; any word in only one fails) + 0 running animations after 1s in every state, via media query AND dev-bar toggle · 11 keyboard order survives condensation; no focus behind the seam · 12 nothing escapes the frame at 390 · 13 composite contrast ≥ 4.5 per lens state · 14 the navigator lands where it says (each spine target → named head at top under the seam) · 15 1280 shows the margin as a sheet · 16 SC1–SC4/SC11–SC12 numbers at scroll 0/400/1200 printed · 17 fonts loaded · 18 full tab-through with accessible names. → `REVIEW.md`, every finding unfiltered. **Gate:** Fable reads the shooter's probes + `REVIEW.md`; opens shots at 1440 top/seam/ffe/foot, reduced, 390.

**`w4b-mock-fix.js`:** MB2 (opus, fresh) applies `REVIEW.md`, answers every item in `FINAL.md` (v1 kept under `build/`) → reshoot + host-sim → MR probe 2 → `REVIEW-2.md` → **FC fragment cutter** (sonnet): `shoot-final.mjs` drives the live mockup to named offsets and writes the DOM subtree as `mock/fragments/*.html` (literal `width:Npx;height:Npx` wrapper + `<figcaption>`; no `<img>`; 0 box-shadow) — `lens-s0-1440`, `lens-s1-1440`, `lens-s2-1440`, `today-s0-1440`, `today-s1-1440` (today drawn from the kit as-is), `spine-before-360`, `spine-after-360`, `header-before-720`, `header-after-720`, `motion-grammar-1080`, `lens-390`, `reduced-1440`; `build.mjs` fails if a fragment is older than `index.html`. **Gate:** probe 2 zero Blocking; `SMOKE=1 node mock/deck-parts/build.mjs` → `FRAGMENTS 12 inlined`.

**Publish the mockup (Fable):** load `artifact-design`; Artifact on `mock/final/index.html`, title from `FINAL.md`, **favicon 🔭**; open in Chrome, resize ~400px, confirm the 390 frame and dev bar; record the URL in `RESUME.md` for the walkthrough part.

## W5 — The deck *The Smart Lens* (`w5a-deck.js`, `w5b-deck-fix.js`)

PARTS / SECTIONS: `00-head` · `01-cover` (Haiku) · `02-ask` (Sonnet; ask verbatim; the two Artifacts) · `03-today` (Sonnet; the four-state scroll strip at 1440, 1280 + 390 shots, measurement tables: header px & %, spine ink %, the distinct-gap set, chip density) · `04-found` (Sonnet; top 12–15 verified findings, frame-budget means, 4–6 verbatim P1/P3/E1 quotes) · `05-thesis` · `06-spine` · `07-header` · `08-body` · `09-motion` (**the merge author, one call, for voice continuity** — thesis; spine with `spine-before/after-360`; header with `header-before/after-720` + `today-s1` vs `lens-s1`; sections/spacing/margin with `lens-s2`; the full grammar table with the reduced-motion column + `motion-grammar-1080` + `reduced-1440`) · `10-mobile` (Sonnet; `lens-390`) · `11-walkthrough` (Sonnet; numbered walk of the four states, each linked to the mockup URL; `lens-s0/s1/s2`) · `12-build` (Opus, E1 recast: **"How the team will accomplish this"** — waves, files, mechanism, tests to rewrite, gates, rollback, three named risks) · `13-roads` (Sonnet) · `14-limits` (Haiku: no user research; simulated seats; local seed; the mockup is stylesheet-and-script over invented data; frame-budget targets are design targets) · `15-colophon` (Haiku: provenance `main@<sha>`, sizes, both URLs, agent census) · `99-script`. Each part ≤30k chars, F-ids resolve, no inline font-size/colour, 0 shadow, patina-brand-voice.

**`w5a-deck.js`:** 8 part authors in parallel → build runner (sonnet, unsandboxed: `node mock/deck-parts/build.mjs && node mock/deck-parts/qa-run.cjs`) → parallel **fact-check** (sonnet/high, fresh → `60-deck-factcheck.md`: every number/ratio/px/%/file:line/F-id/quote traced to `research/` or `source/` or marked UNSOURCED; brand grep `elevated|curated|luxury|bespoke|seamless|effortless|delight`; the ask byte-identical to `source/plan.md`) + **visual QA** (sonnet → `61-deck-visualqa.md`: all 60 PNGs in `deck-qa/`, Blocking/Should/Note, report every one). **`w5b-deck-fix.js`:** ≤4 part fixers (original author's model) → rebuild; loop until zero Blocking and zero UNSOURCED; Fable logs rounds in `62-deck-revision-log.md`.

**Publish the deck (Fable):** `artifact-design`; Artifact on `presentation.html`, title *The Smart Lens*, **favicon 🔍**; Chrome check at ~400px (tables restack, fragments fit, theme toggle, mockup link opens).

**Close · Sonnet:** `RESUME.md` (ask verbatim; both URLs; phase/gate table; agent census; OWED = Kody's ruling on the proposal; nothing built, no product code changed; rebuild commands) · commit with explicit pathspecs (RESUME.md, presentation.html, research/, probe/, shots/, source/, mock/{kit.css,KIT.md,lens.css,img,assets/fonts,fragments,deck-assets,deck-parts,final}; **exclude** `mock/deck-qa/`, `build/`, `mock/final/shots/`, `mock/final/review-shots/`, `research/dev-boot.log`, any `node_modules` symlink; never `-A`) — `docs(the-document): the smart lens — evidence, panel, one proposal, deck + clickable mockup`; push · memory entry + MEMORY.md line with both URLs.

## Verification (Fable, end to end)

1. `node mock/deck-parts/build.mjs` exit 0: `PARTS 17`, `FRAGMENTS 12`, `box-shadow 0`, markers 0, `non-ascii 0`, < 12 MB.
2. Independent: `grep -cE 'drop-shadow\(' presentation.html` = 0 and no `box-shadow:` other than `none`; `grep -oE 'https?://[^" )]+' presentation.html | sort -u` → only fonts.googleapis / fonts.gstatic / the one mockup URL; `LC_ALL=C grep -c '[^ -~\t]' presentation.html` = 0.
3. `mock/deck-qa/qa-results.json`: external `[]`, page/console errors `[]`, no horizontal overflow at 1440/390, every `.dk-mock` fits, three fonts loaded, 15 index rows, contrast ≥ 4.5 both registers, computed box-shadow sweep 0.
4. Fable reads at least `today-1440-light`, `found-1440-light`, `header-1440-light`, `header-1440-dark`, `motion-1440-light`, `walkthrough-390-light`, `build-1440-light`, `limits-1440-dark`.
5. `mock/final/REVIEW-2.md`: all probe items PASS; SC1–SC13 numbers printed and within threshold (or the deviation explained in `FINAL.md`); host-sim `__mockReady` true, 0 errors, 0 external; size < 12 MB.
6. `60-deck-factcheck.md` zero UNSOURCED / brand hits; `61-deck-visualqa.md` zero Blocking.
7. Both Artifact URLs open in Chrome and survive ~400px; recorded in `RESUME.md` + `research/VERIFICATION.md`.
8. `git status --short` shows no unintended program paths after the commit.

## Risks → mitigations

Sandbox (boot, sips, Chromium mach port, `.env` deny) → only steward, capture/shoot/QA runners and the deck builder go unsandboxed; commands logged in `00-env-and-ids.md`; `lsof` before boot · id drift → resolved live, frozen in `state-ladder.json` (Aspen id is a fixed seed and may be hardcoded) · scroll-capture flakiness (`-s1` ≡ `-s0`) → per-shot position assertions + Fable's visual read · 64k collator cap → disk chunks + compact index in every collator/refuter brief · mockup 16 MB cap → no screenshots inside; fonts 204 KB + one crop; size printed twice · host-insert script execution → `__mockInit` + readyState + `host-sim.mjs` before publish · reduced motion → parity plank, C-access, probe item 10 text-diff · Sonnet literalness → exact paths, sections, gate, "no extras" in every brief · X/Y convergence → different seeds, blind drafting, judges return both if converged, merge names organ provenance · register drift under "amend freely" → NG1–NG4 in every brief, V3 `blocked:*`, C-design's "still Patina paper", axis 6 · the 1500-char regex test and `--doc-seam-height` consumers → named in G1 §9, E1 items 1/4, proposal §9 · scratchpad wipe → nothing written there; `RESUME.md` is the resume instrument · `git add -A` → explicit pathspec list with named exclusions.
