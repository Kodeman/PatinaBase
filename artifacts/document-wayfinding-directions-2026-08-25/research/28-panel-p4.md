# Panel P4 — FF&E / procurement-heavy designer

Oak Park IL. Six-figure FF&E budgets, quarterly installs. Design Manager for POs/receiving,
a freight portal, a printed binder at install. I expect the FF&E schedule with order status
per line, first — then what's arriving this week, then exceptions (unacknowledged,
backordered, damaged). I reject any composition that separates a piece from its PO state.

Walked against `main@695addb5f`: flag-off (w1440/w1280/m390 shots) and the Worktable
(wt- shots, flag on), all three width tiers, T1→T16 in order.

---

## Overall (≤120 words)

The paper mostly earns its keep for me: the FF&E lines on `Chen Residence` carry a real
status chip (`IN PRODUCTION`, `RECEIVED`) right next to the price — that's the composition I
came for. But the trust breaks the moment I leave the paper: the exact same line
("Custom Walnut Sectional — 3 pc") reads `RECEIVED` on the paper and `DELIVERED` in the Spec
book shelf a click away; "Committed" means `$0` in the Design authority region and `$14,420`
in "The accounts" seam of the same document; and the one place I'd reconcile install week —
the Install spread — says flatly "No FF&E lines are scheduled for installation," pushing me
back out to a ledger sheet with no line-level tie to what I was just looking at.

---

## Full walk, T1–T16

**T1 — "Tell me what today actually needs — across everything."**
First glance: `Good afternoon, Leah` / `NEEDS YOUR HAND 8` with four folio cards up front —
`ASPEN · INSTALL` ("1 decision overdue — oldest due Aug 22"), `SAMPLE · PROPOSAL` (signed),
`OLSEN · PROJECT` ("AP-012 has an open damage claim"), `WRIGHT · BRIEF` (new lead).
Where I'd click: nothing yet — I'm reading. `STUDIO PULSE` below gives me
"4 moving · 3 reconnecting · Field quiet / 1 decision is overdue, and 4 pieces are on the
way." That last line is close to what I actually want (what's arriving) but it's a summary
sentence, not a list — I can't act on "4 pieces" from here.
Where I'd hesitate: the damage claim on Olsen and the decision on Aspen are both flagged, but
neither card tells me which PO or which piece — I'd have to open each folio to know if it's a
piece I'm expediting for install.
Where I'd give up: I wouldn't give up on this one — it narrates the day. But it doesn't
narrate the day **for my job** (order exceptions); it's a project-status day, not a
procurement day. **Obviousness: what-to-do 4, how-to-get-there 5** — desk lands me nowhere
else to click, but the read itself is only half mine.

**T2 — "Show me everything that's in install."**
First glance: nothing on `/desk` groups by phase; `Aspen · Install` is one card among four,
not a filtered list.
Where I'd click: `⌘K`, type "install."
Where I'd hesitate: reading the dropdown — `No match — Browse the Help Center` / `Ask the
Engine · "install" · ask & place`. Confirmed on screen (`w1440-cmdk-typed.png`): my query
returns nothing and offers to search the *guides*, not my projects.
Where I'd give up: I'd open each of my six live documents one at a time and check the phase
chip — exactly the "eleven jobs open before Monday" problem P2 has, except mine is "which
projects have pieces due this week," which touches every phase, not just Install.
**Obviousness: what-to-do 2, how-to-get-there 1.**

**T3 — "What's my next move on this one?"**
First glance (Chen, project stage): the red-letter zone reads `NEEDS ATTENTION · IN ONE
PLACE` / "Name the phases for this project" / `OPEN THE SCHEDULE`.
Where I'd click: `OPEN THE SCHEDULE` — one clear act.
Where I'd hesitate: not on Chen. But on the sent proposal (Aspen Loft — Living Room Refresh)
the guide says `Wait for the client's signature` with `REVIEW SIGNING CONTROLS` — fine, but
that's not an FF&E act, and I'd want to know if I should be doing anything procurement-side
while I wait (I can't, from here).
Where I'd give up: didn't. **Obviousness: 5 / 5** on Chen; **4 / 5** on the sent proposal (the
sentence is honest, just outside my job).

**T4 — "Change the fabric on the living room sofa."**
First glance: I scan the left rail for "Rooms." I see `The Rooms` in the Studio Drawer.
Where I'd click: `The Rooms` — it's the only "Rooms" word visible in the chrome.
Where I'd hesitate hard: `w1440-room-rooms.png` — "The Rooms" is a directory of **scanned
rooms by client name** (Lily Tanaka · Kitchen, Elena Ruiz · Dining room…), not FF&E rooms
inside my open project. This is not where a sofa fabric lives. I'd back out confused —
this is a real wrong turn, not a hypothetical one.
Where I'd click next: back into the document, `Project · FF&E` region. On Chen the three
lines sit under one heading, `Unsorted` — no room grouping at all (0 rooms specified on this
fixture), so the "room heading → line unfold" path the task script promises isn't something I
can verify works; what I can see is a flat list with no rooms to click through.
Where I'd give up: I wouldn't give up, but I'd have spent a real detour on "The Rooms" first.
**Obviousness: what-to-do 3 (once I know which "rooms" word means what), how-to-get-there 2**
(one wrong door taken first; the right door then has no visible room grouping to test).

**T5 — "Pull up the mood board for the primary bedroom."**
First glance (≥1440, project doc): spine `THE SHELVES` list — `Mood boards · NO BOARDS YET`.
Where I'd click: the `Mood boards` row.
Where I'd hesitate: nothing filed for this fixture, so I can't confirm the room-scoped
version works, only that the door exists.
At 1280/390: the entire shelves block is gone (`w1280-doc-project-rich.png` — 56px icon rail,
no labels). No Mood boards door anywhere at this width except `⌘K` by name — and I don't know
the board's name, only the room. That's a dead end on a laptop.
On the Worktable, direction-stage document: `w1440-wt-speccing` shows `START A BOARD` printed
directly on the paper — no shelf trip needed at the speccing stage specifically. Worth
noting for my persona: I'd actually prefer this — boards next to the FF&E scheme I'm pricing
against, not one shelf-hop away.
**Obviousness (flag-off, ≥1440): 4/4. Flag-off <1440: 1/1 (unreachable except by name).
Flag-on at speccing: 5/5** — this is a real improvement on my task.

**T6 — "Where's the floor plan? Where's the spec book?"**
First glance: same shelves list — `Plan room · NOTHING FILED`, `Spec book · 3 SPECIFIED · BY
ROOM`.
Where I'd click: `Spec book` row → opens beside the spine
(`w1440-shelf-specbook.png`) with the three lines and their status: `Møbler Lounge Chair —
Bouclé | PRODUCTION $5,700`, `Oak Drum Side Table | PRODUCTION $1,920`, `Custom Walnut
Sectional — 3 pc | DELIVERED $6,800`.
Where I'd hesitate: that last status word. On the paper two clicks earlier, the exact same
line read `RECEIVED`, not `DELIVERED`. Those are two different real-world states in my job —
received-not-yet-installed vs. delivered/complete — and the app is telling me both about the
same piece at the same moment. I would not trust either number without opening the PO myself.
`OPEN THE SPEC BOOK →` takes me to `/doc/{id}/spec-book`, the full workbench
(`w1440-leaf-specbook-route.png`) — but that page shows **spec completeness only**
(`Not specified` / `INCOMPLETE` fields), not order status at all. None of the three surfaces
that show me "the spec book" agree on what state a piece is in, and one of them drops order
status entirely.
At 1280: `Plan room`/`Spec book` doors vanish from the rail; reachable only via `⌘K → "The
plan room"` (spec book has no ⌘K row at all per the anatomy — only Plan room does).
**Obviousness: what-to-do 4, how-to-get-there 3 at ≥1440 (found it, but the three answers
disagree); 1 at 1280 for the spec book (no ⌘K entry exists for it at all).**

**T7 — "Did the Hendricks ever open my proposal? Nudge them."**
First glance (Aspen Loft — Living Room Refresh, sent): `SENT / Aug 24`, `OPENED / not yet`,
`READING / —`, `MOST READ / —`, and a scored-ink `NUDGE CLIENT USER` link right there.
Where I'd click: `NUDGE CLIENT USER`.
Where I'd hesitate: not at all — this is the single cleanest surface in the whole review for
my needs. Sent-state and age are legible without opening anything else.
**Obviousness: 5 / 5.**

**T8 — "Add the mudroom."**
First glance: at the foot of the FF&E line list, under `ADD A ROOM` — a plain scored-ink line,
exactly at the foot of the room list, not buried in a menu.
Where I'd click: `ADD A ROOM`.
**Obviousness: 5 / 5**, found unaided, first pass — this one earns the "canon says
`Add a room` lives in-flow" rule.

**T9 — "Bill the deposit. And who still owes me?"**
First glance: `Design authority` region — `NO AUTHORITY YET` — Authority/Plan/Committed/Moved
rows, then `DRAW AN INVOICE` at the region head.
Where I'd click: `DRAW AN INVOICE`.
Where I'd hesitate hard: how many places tell me about money on this one document? I count
four just from what I saw: (1) the spine's running-index `Design authority` row, (2) the
paper's `Design authority` region itself, (3) a **separate** folded seam near the foot,
`The accounts · this project` (`$0 BUDGET · $14,420 COMMITTED · 20% MARGIN`,
`w1440-record-foot.png`), and (4) the margin rail's two `Vendor payment` cards. Worse: the
word **"Committed"** means two different numbers on the *same document* — the Design
authority region's own `Committed` row reads "nothing executed yet" ($0), while "The
accounts" seam three screens down calls $14,420 "COMMITTED." For a designer who "rejects any
composition that separates a piece from its PO state," a vocabulary collision on the money
word itself is the same defect one level up — I can't trust either number without a third
source (the Orders ledger).
Where I'd give up: I wouldn't give up on drawing the invoice, but I'd stop trusting the
in-document money figures for reconciliation and go straight to the Accounts book instead.
**Obviousness: what-to-do 3 (one act exists, but which number is real?), how-to-get-there 3
(too many doors, ≥3 confirmed).**

**T10 — "Install slipped a week — move it and tell me what it hits."**
First glance (Aspen, install stage): `Schedule` region shows a timeline (`Aug — Sep — Oct`)
and `Installation & Styling · Main lane · In progress · Follows Procurement & Orders`, with
`COMPLETE PHASE`. I don't see an explicit date-drag control in any of the shots I have, and
the ripple derivation (`schedule-ripple-derivation.ts`) isn't visible in evidence — I can't
say from a screenshot whether moving a date actually shows me what it hits before I commit.
**Obviousness: what-to-do 3 (the region is clearly where this lives), how-to-get-there —
unscored, low confidence (0.3) — what would settle this: a shot of the ripple preview after
an actual date edit.**

**T11 — "Put this down, pick up the Byrnes."**
First glance: `← PUT DOWN` top-left of the spine, always visible.
Where I'd click: `← PUT DOWN`, land on `/desk`, then click the Byrnes folio (or `⌘K`).
Where I'd hesitate: not at all — one clear trip, and I know which trip (back to the desk,
not a stack of open tabs — confirmed no split view exists anywhere in the evidence).
**Obviousness: 5 / 5.**

**T12 — "New inquiry — start them."**
First glance: `⌘K` → `BEGIN` group shows `Capture a lead / BEGIN A BRIEF` directly above
`Open a project / NO PROPOSAL NEEDED` (`w1440-cmdk-open.png`) — the subLabels do the
disambiguating work the task asks for.
Where I'd hesitate: only mildly — I'd read both subLabels once before picking, which is
exactly the "obvious before picking" bar the task sets, met.
**Obviousness: 4 / 5** (met, but the disambiguation is a caption I have to read, not a
structural difference).

**T13 — "Did Sturdy Oak confirm the PO?" [special assignment — see below]**

**T14 — "The console came in damaged — file it." [special assignment — see below]**

**T15 — "Who's on this job? I need the painter's cell."**
First glance (in a project doc): `THE SHELVES` list, `Call sheet · NOBODY ON IT YET`.
Where I'd click: `Call sheet` row — it's a **doorway**, not a leaf (confirmed:
`w1440-shelf-callsheet-doorway.png` — hover changes nothing visible, no panel opens in this
screenshot; per the anatomy it opens an overlay sheet, not a shelf panel).
Where I'd hesitate: on this fixture nobody's on the call sheet yet, so I can't see a real
phone number in the roster from these shots — but the doorway itself is exactly where I'd
expect it, next to Plan room / Spec book / Mood boards.
**Obviousness: 4 / 5** (door is right there; content unverified on this fixture).

**T16 — "Client asked a question — answer it where it's on the record."**
First glance: drawer bell `THE POST` — a plain dot, no count, on desktop (matches the
no-badges doctrine). On mobile, though, the `More` menu prints `The Post — 3 NEW` in plain
text (`m390-mobile-more-actions.png`) — a literal count where the desktop chrome deliberately
withholds one. I'd notice the inconsistency if I used both surfaces in the same week, but I
wouldn't be blocked by it.
Where I'd click: `The Post` bell.
**Obviousness: 4 / 5.**

---

## Task table

| Task | What-to-do | How-to-get-there | Note |
|---|---|---|---|
| T1 | 4 | 5 | Narrates the day, not my day (procurement exceptions) |
| T2 | 2 | 1 | ⌘K "install" → no match, offers Help |
| T3 | 5 | 5 | Guide sentence is honest and singular |
| T4 | 3 | 2 | "The Rooms" door is scan-rooms, not FF&E rooms — real wrong turn |
| T5 | 4/1 | 4/1 | Shelf works ≥1440 only; Worktable speccing strip is better for me |
| T6 | 4 | 3 | Three surfaces, three different status words for one piece |
| T7 | 5 | 5 | Cleanest surface in the review |
| T8 | 5 | 5 | Add a room, unaided |
| T9 | 3 | 3 | ≥4 money doors; "Committed" means two different numbers |
| T10 | 3 | — (0.3 conf) | No ripple-preview shot in evidence |
| T11 | 5 | 5 | One trip, unambiguous |
| T12 | 4 | 4 | Sublabels disambiguate, must be read |
| T13 | 3 | 2 | See special assignment |
| T14 | 3 | 2 | See special assignment |
| T15 | 4 | 4 | Right door, empty roster on this fixture |
| T16 | 4 | 4 | Desktop dot vs mobile literal count |

---

## Special assignment — T13/T14: leaving the document for a ledger sheet

**T13 — "Did Sturdy Oak confirm the PO?"**
The path from an open document: `Studio books` (drawer, always visible) → `Orders` → the
`Ledger` tab, or the `g` `o` chord (works, confirmed by probe #3). The Orders sheet
(`w1440-ledger-sheet-orders.png`) opened from the **Desk** slid over the Desk as a dimmed
background — the same D8/registry mechanism (`registry.tsx:53-54`) should apply when opened
from inside a document, which is the promise this program cites. I did not have a screenshot
of the sheet opened *from inside* an open document, so I can't independently confirm the
document itself stays mounted and scrolled to where I was rather than resetting — flagging
that as unverified, moderate confidence (0.6) that it holds, since the desk case is a direct
analog and the probe log independently confirms the Orders sheet's own focus-restore code
exists (`doc-sheet.tsx:228–262`) for the general case.
What I actually see on that sheet: PO rows carry a status chip (`RECEIVED / INSPECT`,
`IN TRANSIT`) and a `NOT SENT` word, but **no distinct "acknowledged"/"unacknowledged" label**
anywhere on the visible rows — I'd have to open the PO's own document (`open document →`) to
find out if Sturdy Oak actually confirmed it, which is a second, deeper trip past the ledger
sheet the task implies should answer this in one. The probe log's `06-focus-return.png`
finding is directly relevant here too: the Orders sheet's restore-focus logic silently no-ops
when it's opened from inside the Studio Books disclosure (menu closes and detaches its own
trigger before the restore runs) — so even where the sheet slides over correctly, my
keyboard focus does not reliably land back where I was.
**Cost to me:** two sheet-deep before I have my answer (Orders ledger, then the individual
PO's own document/PDF), with a confirmed focus-return defect on the first hop.

**T14 — "The console came in damaged — file it."**
Path: `Studio books` → `Orders` → `Receiving` tab (visible in the sheet's own tab row:
`LEDGER / THE WEEK / RECEIVING / VENDORS`) — I did not have a screenshot of the Receiving tab
itself open, so I can't confirm the claim-filing flow's shape from evidence; the Desk folio
for Olsen already surfaces "AP-012 has an open damage claim" as a `REVIEW THE CLAIM →` act, a
second, shorter path that skips the ledger sheet entirely and goes straight to the claim.
**Cost to me:** if I start from the open project document (not the Desk), there is no visible
in-document doorway to Receiving — no shelf, no letterhead instrument, no red-letter line
mentions it — I'd have to know to reach for the drawer's `Orders` book specifically. That's
recall, not recognition, exactly the kind of trip a P4 designer with a Design Manager and a
printed binder already avoids by using a separate tool. The place-preservation question is the
same as T13's: unverified from-inside-a-document, moderate confidence it holds.

---

## What stays true (must not be broken)

1. **FF&E lines on the paper carry both order status and price on the same row** —
   `Møbler Lounge Chair — Bouclé · ×2 / Nordic Atelier — IN PRODUCTION — $5,700`. This is the
   single composition I most need and it already exists; any direction must keep a piece and
   its PO state on one line.
2. **The proposal send-wall (T7) is unambiguous**: sent date, opened/not-yet, reading state,
   and the nudge action all sit on one scored line. Don't scatter this.
3. **"Add a room" at the foot of the FF&E list** is found unaided — keep it in-flow, not
   promoted into a head menu.
4. **Ledger sheets slide over whatever I was looking at** rather than replacing it (confirmed
   from the Desk case) — my document should still be mine when I close Orders/Accounts/Hours.
5. **The Worktable's speccing-stage boards strip** (`START A BOARD` printed directly on the
   paper) is a real improvement over the shelf-hop for a designer actively building a room's
   FF&E — keep boards in-line at the stage where I'm pricing against them.
6. **No hover-gated affordances anywhere** (probe #1, confirmed) — I never have to guess
   whether something is clickable by waving a mouse over dead space.
