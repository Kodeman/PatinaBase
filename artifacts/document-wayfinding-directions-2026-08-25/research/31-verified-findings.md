# Verified Findings — The Document Wayfinding Review

Total findings: 101  |  Surviving: 92  |  Killed: 9

- Killed by code-truth (misread): 8
- Killed by canon-truth (misread): 0
- Killed by repro (not-reproduced): 1
- Ruled-against by canon: 10
- Known-open per canon: 5

## Surviving findings

| id | title | seats | severity | confidence | width | flag | tasks | canon_truth | repro | claim |
|---|---|---|---|---|---|---|---|---|---|---|
| F02 | At 1280 the spine is an unlabelled 56px icon rail | U1, U2, U3, U4, U5, P1, P2 | high | 0.95 | 1280 | both |  | ruled-against:C8 | reproduced | At 1180-1439 the seven section marks render as bare coloured bars with no printed text; the active section's label is `hidden min-[1440px]:block`, and `StrataMark`'s label becomes aria-label only. `← Put down` loses its word and becomes a bare `←`. The word `Project` appears nowhere in the rail. |
| F03 | Care-stage FF&E spread is headed `Install` | U1, U2, U3, U4, P1, P3, P4 | high | 0.95 | all | both |  | open | reproduced | On Birch Hollow (`Care · ONGOING`, `The book closed Aug 25.`) the FF&E spread's heading is the literal word `Install` and its empty state reads `No FF&E lines are scheduled for installation.` — four lines below a paragraph that correctly reads `Plan the care work`. |
| F01 | Shelves, rooms block and running index are absent below 1440 | U1, U3, U5, P1, P2, P3, P4 | blocker | 0.95 | 1280 | both |  | ruled-against:C8 | reproduced | At 1280 the left rail is a 56px strip of unlabelled marks plus `In hand / <1m`: no `IN THIS DOCUMENT`, no `Rooms`, no `THE SHELVES`. `Plan room`, `Spec book`, `Mood boards`, `Call sheet` are not narrowed but removed, and an open shelf is force-closed on crossing below 1440. |
| F04 | Nothing answers a phase-wide question; ⌘K `install` returns No match | U1, U2, P1, P2, P3, P4 | blocker | 0.95 | all | both |  | known-open:T2/T4 | reproduced | ⌘K typed `install` returns only `No match — Browse the Help Center` / `SEARCH THE GUIDES →` and `Ask the Engine · "INSTALL" · ASK & PLACE`, while the Desk behind it prints the folder tab `ASPEN · INSTALL`. No Desk filter, tab or chip groups by phase. |
| F05 | FF&E lines print under `Unsorted`, never under a room heading | U2, P1, P2, P3, P4 | high | 0.9 | all | both |  | open | reproduced | On a project with zero project_rooms every FF&E line falls into the `Unsorted` bucket, so the room-heading path the designer expects has nothing to render. |
| F07 | The mobile bar's one big act is a truncated `MESSAGE THE CLI…` | U1, U2, P1, P2, P4 | high | 0.9 | 390 | both |  | open | reproduced | At 390 the bar reads `IN THIS DOCUMENT / Project` · `MESSAGE THE CLI…` · `··· MORE`, while the paper's red-letter zone says `Name the phases for this project / OPEN THE SCHEDULE`. Only `document-guide.tsx` registers a mobile primary; `red-letter-zone.tsx` registers none. |
| F08 | Three-to-four competing doors answer one money question | U2, U5, P1, P3, P4 | medium | 0.85 | all | both |  | open | reproduced | The `Design authority` head prints `DRAW AN INVOICE`, `AMENDMENT` and `HOURS · THIS PROJECT ↗` side by side; the FF&E head adds `BILL 3 UNINVOICED`, ⌘K adds `Draw an invoice for {Project}` and the Desk's `BEGIN` column a fourth. Nothing signposts which is the door. |
| F09 | The money region is named `Design authority` and carries no money scent | U4, P1, P2, P3 | high | 0.9 | all | both |  | open | reproduced | The folded seam reads `Design authority   NO AUTHORITY YET · $0 COMMITTED   UNFOLD ↓`; unfolded the head is `MONEY · ONE REGION` / `Design authority`. The word money appears only in that small eyebrow, and the running-index row prints `Design authority` alone. |
| F10 | The seven g-chords work but are printed nowhere on screen | U1, U5, P1, P3 | high | 0.9 | all | both |  | open | reproduced | `g l`, `g p`, `g r`, `g o`, `g a`, `g h`, `g t` all route correctly (probe §3). A full-text sweep of `/desk` and `/doc/[id]` finds no chord hint; the badges print only inside ⌘K rows, which she must open with ⌘K to see. `/` and `?` register no handler at all. |
| F11 | Ledger sheet focus-restore silently no-ops from the Studio books menu | U2, U5, P2, P4 | medium | 0.95 | all | both |  | open | reproduced | Opening Orders from inside the `Studio books` disclosure both opens `DocSheet` and unmounts the disclosure the trigger lived in; `DocSheet`'s guard `if (!focusTarget?.isConnected) return;` then does nothing and focus lands on `<body>` (live-verified). |
| F12 | The `Knowledge` shelf is a redirect that names itself three ways | U1, U4, P1, P3 | medium | 0.9 | 1440 | both |  | known-open:I136 | reproduced | The row reads `Knowledge   STUDIO LIBRARY →`; the leaf's eyebrow reads `STUDIO LIBRARY · CROSS-PROJECT`, its body `STUDIO LIBRARY — CROSS-PROJECT STANDARDS. NOTHING FILED FOR THIS PROJECT.`, its act `OPEN THE STUDIO LIBRARY →`. It holds nothing and duplicates a permanent drawer door. |
| F13 | ⌘K Recent lists two rows both titled `Aspen` | U2, U3, P2, P3 | medium | 0.8 | all | off |  | open | reproduced | `RECENT` prints `Birch / BIRCH HOLLOW`, `Aspen / ASPEN LOFT REFRESH` and `Aspen / ASPEN LOFT — LIVING ROOM REFRESH` stacked — a live project and a sent proposal sharing the same bold first word, separated only by a smaller sub-label. |
| F15 | The mobile spine sheet lists sections and nothing else | P1, P2, P3 | high | 0.95 | 390 | both |  | ruled-against:C8/D3 | reproduced | The mobile spine sheet lists sections, Rooms (when the project has any) and the margin — but never the running index or any shelf. |
| F16 | `Who still owes me` is unanswerable inside the document | U1, P1, P2 | high | 0.9 | all | both |  | known-open:I141 | reproduced | No row of the money ladder names a receivable; the answer sits in a nested band inside the same region rather than in the four-rung ladder that heads it. |
| F17 | Three different things are called a `room` | U1, U4, P4 | high | 0.9 | all | both |  | open | reproduced | The drawer's `The Rooms` (`g r`) opens `THE ROOMS · 6 scanned rooms`, cards titled by person (`Lily Tanaka` / `Kitchen · scanned Aug 24`); the spine's `Rooms` block lists FF&E groups; `Plan room` is a shelf of drawings. P4 took `The Rooms` first looking for a sofa and backed out. |
| F18 | Five of seven stage default acts are `Review {X}`, a shrug | U2, U4, P1 | high | 0.9 | all | both |  | open | reproduced | `stageCopy`'s action labels: `Review the brief`, `Continue Discovery`, `Open Drafting Room`, `Review proposal` / `Review signing controls`, `Review active work`, `Review installation`, `Review closeout`. The default need action is also `Review now`. |
| F21 | ⌘K never restores focus to its trigger on close | U2, U5, P2 | medium | 0.95 | all | both |  | open | reproduced | `command-bar.tsx` has one focus line — focusing the input on open — and no capture or restore of the pre-open `document.activeElement`. Live-verified: focus the `Plan room` shelf button, ⌘K, Escape, and focus lands on `<body>`. The shelf leaf, margin panel and ledger sheets all restore correctly. |
| F23 | `NEEDS YOUR HAND 8` prints over four folios | U1, P1, P2 | medium | 0.95 | all | both |  | open | reproduced | The eyebrow reads `NEEDS YOUR HAND  8`; four folio cards render; the footer reads `4 IN REACH · 4 FOLDED BELOW` on the left and `REVEAL 4 MORE FOLIOS ↓` on the right. Identical at 1280 and 390. Half the attention queue is folded on first paint. |
| F24 | The two Desk begin verbs carry no distinguishing sub-label | U2, P1, P2 | medium | 0.85 | all | both |  | open | reproduced | The header prints `＋ CAPTURE A LEAD` and `＋ OPEN A PROJECT` bare. Their distinguishing sub-labels — `BEGIN A BRIEF` and `NO PROPOSAL NEEDED` — exist only inside ⌘K's `BEGIN` group, and `Capture a lead` is deliberately omitted from the Desk's own `BEGIN` column. |
| F25 | A held room has no visible release control once scrolled away | U2, U5, P2 | medium | 0.5 | 1440 | both |  | open | reproduced | The letterhead's `In hand · {Room}` line is plain text with no × or close affordance; the only release is clicking the same Rooms-block row again. Source-confirmed only — the local seed has no project with both rooms and `active_section='project'` (probe §7). |
| F26 | The money explainer is a dense paragraph that names its own old UI | U3, U4, P2 | medium | 0.9 | 1440 | off |  | open | reproduced | Beneath four terse label rows sits one unbroken paragraph: `Authority → plan → committed → moved. Moved is the accounts' committed figure … not funds disbursed…` ending `Absorbs today's four separate bands: design authority, working budget, authorizations & trade scopes, the accounts.` |
| F14 | Index, rooms and shelves vanish on install and care documents | U1, U3, P1 | blocker | 0.97 | all | both |  | open | reproduced | On `w1440-doc-install` and `w1440-doc-care` the spine reads `← PUT DOWN`, seven marks, the active label and `● IN HAND / under a min` — no `IN THIS DOCUMENT`, no `Rooms`, no `THE SHELVES`. `DocSpineShelvedBlocks` mounts only when `engagement_kind === 'project' && active_section === 'project'`. |
| F29 | The roster cannot be reached from the Desk at all | U1, P3 | high | 0.95 | all | both |  | open | reproduced | `Call sheet` is `scope: 'document'` and is filtered out of Desk Contents, out of ⌘K's unfiltered `Rooms & ledgers` group, and out of typed ⌘K unless a project document is in hand. Desk Contents prints `ROOMS: Library · People · The Rooms` — `People` is the studio directory, not this job's roster. |
| F30 | The Mood boards shelf opens onto another fold, with no way to start one | U1, P1 | high | 0.85 | 1440 | both |  | open | reproduced | Clicking `Mood boards / NO BOARDS YET →` opens a leaf headed `MOOD BOARDS · SHARED & DRAFT` / `Mood boards` / `✕ CLOSE` whose entire body is one more folded row: `Mood boards   NO BOARDS YET   UNFOLD ↓`. No `Start a board`. The Plan room leaf ends in `Open the plan room`. |
| F32 | The Worktable moves no item-reach cell and leaves install week untouched | U1, P1 | high | 0.9 | all | on |  | known-open:I138/I139 | flag-on-unverified | Flag-on, the Delivery table adds only a lifted release leader; all four item-reach slots are Speccing-only and the install setting adds nothing at all. |
| F35 | Two regions on one paper are both called `Schedule` | U1, U2 | high | 0.95 | all | both |  | open | reproduced | About 120px apart the paper prints a fold seam `Schedule … UNFOLD ↓` (the Rule, `schedule-rule-title`) and a region head `Schedule / 0 phases · nothing active · next milestone —  FOLD ↑` (the ledger, `project-schedule-title`). The index carries one row, wired to the ledger only. |
| F34 | The FF&E head leads with `ADD TO PROJECT` and shows three acts at once | U2, P1 | medium | 0.85 | all | both |  | open | reproduced | The head's ledger prints, in order: `ADD TO PROJECT` (filled dark plate), `BILL 3 UNINVOICED` (scored underline), `SPEC BOOK →` (scored underline), `FOLD ↑`. On a project with three lines already in production, the inked leader asks her to add a fourth. |
| F36 | The proposal guide says `Review signing controls` instead of the live act | U2, U4 | medium | 0.85 | all | both |  | open | reproduced | The sent-proposal fallthrough prints headline `Wait for the client's signature` with action `REVIEW SIGNING CONTROLS`; the real act, `NUDGE CLIENT USER`, sits ~200px lower. The same phrase serves the draft-fallback branch, and `Review countersign controls` the client_signed branch. |
| F37 | ⌘K opens on Recent and Begin; the doorways are below the fold | U1, P2 | medium | 0.85 | all | both |  | open | reproduced | ⌘K opens on Recent and Begin; `Rooms & ledgers` and `Studio` — home of the studio doorways — sit below the fold. `This surface` is absent on the Desk entirely. |
| F38 | Desk Contents names doors without saying what is behind them | U1, U4 | medium | 0.85 | all | both |  | open | reproduced | `THE STUDIO` prints three columns — `ROOMS: Library ↗ / People ↗ / The Rooms ↗`, `LEDGERS: Orders SHEET / Accounts SHEET / Hours SHEET / The Post SHEET`, `BEGIN: …`. `SHEET` is the only sub-label and describes presentation, not contents; nothing says receiving or damage claims live under `Orders`. |
| F39 | Studio pulse is folded by default and names nothing | U1, P2 | medium | 0.85 | all | both |  | open | reproduced | `STUDIO PULSE` / `4 moving · 3 reconnecting · Field quiet` / `1 decision is overdue, and 4 pieces are on the way.` / `7 STUDIO ITEMS   OPEN PULSE ↓`. No project name, no phase, no due date, and `reconnecting` carries no inline gloss — the one cross-project organ is a folded adjective. |
| F40 | A folded region and an empty one read the same | U3, P1 | medium | 0.75 | all | both |  | open | reproduced | A region folded by the designer and one folded because it is empty wear the same seam form; only the summary text distinguishes them, and nothing marks the fold as her own choice. |
| F41 | Setup chores and dated overdue needs wear the same red-letter clothes | U3, P1 | medium | 0.8 | all | both |  | open | reproduced | Setup chores and dated overdue needs share the red-letter band and act treatment; the only distinction is headline font-weight. The folio tabs are not one hue — each need kind carries its own stamp colour. |
| F42 | Seven section names and `The Patina Six` both print on one paper | U4, P3 | medium | 0.85 | 1440 | both |  | known-open:I114 | reproduced | The spine's chrome uses Brief/Discovery/Direction/Proposal/Project/Install/Care, while the same document's Schedule region prints, verbatim: `Consultation · Schematic Design · Design Development · Procurement & Orders · Installation & Styling · Completion — the studio's standard six.` |
| F43 | The guide's act names a different verb than the row beneath it | U2, P3 | low | 0.75 | 1440 | off |  | open | reproduced | The guide's action reads `ADD PROJECT TYPE AND NAMED ROOMS` under the headline `Complete Discovery`; the first checklist row directly below is labelled `Scope & rooms`. One input, three names, adjacent on screen. |
| F44 | Brief chips print raw template text (`15k_50k`, `3 6 Months`) | U2, P3 | low | 0.85 | 1440 | off |  | open | reproduced | The BUDGET chip reads `15k_50k` (literal underscore) and the TIMELINE chip reads `3 6 Months` (missing separator) — the facts a fast read of T1/T3 depends on, printed unformatted. |
| F45 | Opening a shelf re-wraps the paper she was reading | U1, P1 | low | 0.85 | 1440 | both |  | open | reproduced | With the Spec book or Knowledge leaf open the FF&E head wraps to two lines — `Project ·` / `FF&E` — and its status truncates to `1 group · 3 li…`. Closed, the same head sits on one line. The leaf is declared non-modal, yet the reading position visibly moves under it. |
| F46 | The Orders sheet prints `PUT BACK · ESC` twice | U2, P1 | low | 0.85 | 1440 | both |  | open | reproduced | `PUT BACK · ESC` appears once at the top-right of the screen and again inside the dialog's own `ORDERS · LEDGER` header row, stacked directly above each other. |
| F47 | `The Post` shows `3 NEW` on mobile and an unlabelled dot on desktop | U1, P4 | low | 0.9 | 390 | both |  | open | reproduced | The 390 `More` menu prints `The Post   3 NEW`; the ≥1180 drawer prints `THE POST` with an unlabelled dot. The same object reports a count at one width and a state at another. |
| F28 | At 390 the `ADD TO PROJECT` plate covers the FF&E heading | U3, P4 | blocker | 0.9 | 390 | off |  | open | reproduced | The heading wraps to three lines (`Pro` / `·` / `FF&E`) and the solid dark scored-ink `ADD TO PROJECT` leader sits directly on top of the middle line, physically covering `ject` — confirmed by pixel crop of `m390-doc-project-rich.png` (y≈3280-3600). |
| F33 | ⌘K's placeholder and fallback both invite `ask the Engine` | U4 | high | 0.95 | all | off |  | open | reproduced | The input placeholder reads `Find a document or a ledger — or ask the Engine…`; the no-match group eyebrow reads `Ask the Engine` and its results header `The Engine · "{query}"` with the row sublabel `'{query}' · ask & place`. This is the first text a designer reads inside search. |
| F50 | The plan room disappears from ⌘K the moment she types `plan` | U1 | high | 0.93 | all | both |  | open | reproduced | `The plan room` · `this project · the current set` enters the `This surface` group only in the empty-query branch and carries `match: ''`. The typed branch never re-adds those rows and `matchSurfaces()` has no plan-room entry, so typing `plan room` returns `No match — Browse the Help Center`. |
| F51 | The Drafting Room's only Desk doorway is ⌘K | U5 | high | 0.85 | all | off |  | open | reproduced | The Drafting Room is explicitly excluded from Desk Contents' Begin list and reachable only via ⌘K → `Open the Drafting Room`. A keyboard user unaware of ⌘K has no path in from the Desk at all. |
| F52 | `MESSAGE THE CLIENT` leads the letterhead on a doc with no client | U1 | high | 0.93 | all | both |  | open | reproduced | Chen Residence prints title, then `No client linked — attach one ↗`, then `MESSAGE THE CLIENT · PREVIEW AS THE CLIENT · SHARING · MILESTONES · CALL SHEET · 0` with `MESSAGE THE CLIENT` inked as leader. `canSendNote = Boolean(projectId \|\| clientProfileId)` never asks whether a client exists. |
| F53 | Answering a client question happens off the document | P1 | high | 0.8 | all | both |  | open | reproduced | The paper offers `MESSAGE THE CLIENT`, an outbound composer captioned `The Pulse handles Fridays; this is for now. It lands in {client}'s portal messages.` Her question lives behind `THE POST` (dot only) or `/people?thread=`. Chen's margin holds no message kind at all. |
| F54 | The rooms rail exists on direction and disappears on the project | P1 | high | 0.85 | all | on |  | ruled-against:C14 | flag-on-unverified | `wt-speccing-1440` (a direction document) carries `ROOMS   All   + Add a room` above the scheme, plus `BOARDS / START A BOARD` and `Reach into the library…`. `wt-delivery-project-1440` carries none of them; its FF&E group is still `Unsorted` and its room verb is the foot-of-list `ADD A ROOM`. |
| F55 | No bypass-blocks control anywhere in the layout | U5 | high | 0.6 | all | both |  | open | reproduced | The `(document)` layout mounts the route boundary, five providers, the route, LogStrip, StudioDrawer, RegistryShortcuts, CommandBar and the mobile shell; a grep for `skip to` / `SkipLink` across the document app and components returns zero hits. A Tab-only user traverses the whole spine each load. |
| F56 | Terracotta and clay ink fail 1.4.3 contrast everywhere they appear | U5 | high | 0.9 | all | both |  | open | reproduced | Clay (#C4A57B) and terracotta (#D4A090) are used as text in ~374 places at ≈2.2:1. The red-letter band's own eyebrow uses a darker #C4836F at ≈2.95:1 — still under AA's 4.5:1. |
| F57 | The FF&E line she must edit is not editable on the paper | U1 | high | 0.85 | all | both |  | open | reproduced | The FF&E line unfolds on the paper for PO, movement, receiving and room assignment — but its spec attributes (Sku, Finish, Material, Colour, Exact Location) are editable only in the spec-book route. |
| F58 | The same FF&E line reads `RECEIVED` on paper and `DELIVERED` in the spec book | P4 | high | 0.8 | 1440 | off |  | open | reproduced | On the paper `Custom Walnut Sectional — 3 pc` carries the stamp `RECEIVED`; two clicks later the Spec book shelf leaf prints the same line as `DELIVERED $6,800`. Received-not-yet-installed and delivered are different real-world states, told about one piece at one moment. |
| F59 | `Committed` means $0 in one region and $14,420 in another | P4 | high | 0.8 | 1440 | off |  | open | reproduced | The `Design authority` region's own `Committed` row reads `nothing executed yet` ($0), while the folded seam `The accounts · this project` three screens down prints `$0 BUDGET · $14,420 COMMITTED · 20% MARGIN`. One money word, two numbers, one document. |
| F60 | The room lens has no substitute below 1440 | U3 | high | 0.8 | 1280 | off |  | ruled-against:C8 | reproduced | The only mechanism to hold a room and lift it across the FF&E list is the ≥1440 spine Rooms block; a live resize below 1440 auto-releases any held room (`room-lens-context.tsx`: "there is no put-down affordance under the full spine"). No filter, chip or search-by-room exists at 1280 or 390. |
| F61 | The index says `NO AUTHORITY YET` over $14,420 in motion | U1 | high | 0.9 | 1440 | both |  | open | reproduced | The running-index row reads `Design authority / NO AUTHORITY YET`. Unfolding the same region prints `Moved · $14,420 in motion — ordered through installed` and an accounts band `The accounts · this project  $0 BUDGET · $14,420 COMMITTED · 20% MARGIN`. The index reports the one tier that is empty. |
| F62 | Boards have three doors with three different names | P1 | medium | 0.85 | all | both |  | ruled-against:C9 | state-dependent | `Mood boards` (spine shelf, ≥1440, project stage), `RECENT BOARDS` (Desk strip) and — flag-on, direction stage only — `BOARDS / START A BOARD` on the paper. The Drafting Room registry entry also aliases `boards, moodboards`. Three doors, none open at the same time as either other. |
| F63 | Three `add a room` verbs mean three different things | U1 | medium | 0.8 | all | both |  | ruled-against:C12 | reproduced | Flag-off, `ADD A ROOM` prints at the foot of the FF&E room list (adds an FF&E group). Flag-on, the Speccing table's rail prints `ROOMS  All  + Add a room` at the top of the paper (adds a room to the scheme). `/rooms` is a gallery of scanned rooms with no add verb at all. |
| F64 | Two acts open the same Drafting Room, worded differently | U2 | medium | 0.85 | all | both |  | open | reproduced | The guide's action reads `OPEN DRAFTING ROOM`. Directly below it the Direction·v1 block prints its own act, `CONTINUE DRAFTING`, for the same destination (`Not started yet — open the Drafting Room to write it`). Both scored, both live, ~250px apart on one screen. |
| F65 | Nothing on the Desk says what changed while she was gone | P1 | medium | 0.85 | all | both |  | open | reproduced | The Desk prints a greeting, the date, `NEEDS YOUR HAND 8`, folio cards with need lines, and `STUDIO PULSE  4 moving · 3 reconnecting · Field quiet`. No folio carries an age, no line is marked new, and the pulse counts are levels rather than changes. |
| F66 | The Drafting Room uses a different visual language from the paper | U3 | medium | 0.6 | 1440 | off |  | open | reproduced | Scope/Vision/Offer facets (`Rooms`, `FF&E`, `Palette`, `Boards`, `Phases`, `Exclusions`, `Payments`, `Terms`) each render inside a bordered, rounded card with a checkbox and chevron; `+ Add Room` and `ESTIMATE · ROM ESTIMATE` render as pills — none of it the paper's scored-ink language. |
| F67 | Orders is a global cross-project ledger, not a project-scoped view | U2 | medium | 0.6 | 1440 | off |  | open | reproduced | The door (`g o` or Studio books → Orders) opens a studio-wide sheet filterable by `PROJECT · ALL / CHEN RESIDENCE / OLSEN LAKE HOUSE`. T13's bar is ack state per PO without leaving the project's frame; she must instead consult and filter a cross-project register. |
| F70 | Three equal Worktable add-actions get three different visual weights | U3 | medium | 0.6 | 1440 | on |  | open | flag-on-unverified | `+ Add Item` renders as a solid tan-filled box, `+ Add Allowance` as a white-bordered box and `+ Add TBD` as plain unstyled text — three conceptually parallel ways to start a scheme line, under copy that reads `The scheme starts loose — a first line is enough`. |
| F71 | Intake's `opens when…` seams point at the wrong stages | U1 | medium | 0.88 | all | on |  | open | flag-on-unverified | Flag-on, the brief document ends with three inert rows: `Schedule  OPENS WHEN THE PROJECT BEGINS`, `Project · FF&E  OPENS WITH THE DIRECTION`, `Design authority  OPENS WHEN THE PROJECT BEGINS`. That device is what the install and care spreads need and lack — they lose regions silently. |
| F72 | The Rooms block disappears at zero rooms with no placeholder | U3 | medium | 0.55 | 1440 | off |  | open | reproduced | On Chen Residence (0 `project_rooms`) the spine jumps from `IN THIS DOCUMENT` straight to `THE SHELVES` with no `Rooms` heading or row — while every shelf in the same block prints its own placeholder even when empty (`Plan room · Nothing filed`, `Mood boards · No boards yet`). |
| F73 | One boxed control breaks the flat scored-ink grammar | U3 | medium | 0.65 | 1440 | off |  | open | reproduced | `Sync from the schedule` renders inside a visible rounded-corner bordered box — the only bordered button on the whole page — while every other act on the same region (`Draw an invoice`, `Amendment`, `Hours · this project`, `Draft a trade scope`) is a bare underlined DM-mono word. |
| F74 | The drawer is hidden below 1180; Orders costs 2+ taps at 390 | U3 | medium | 0.75 | 390 | off |  | open | reproduced | The persistent Studio Drawer strip that gives one-tap or one-chord access to Orders/Accounts/Hours/The Post at ≥1180 is hidden below 1180; at 390 the same ledgers are reachable only via the mobile bar's `More` menu → `Studio books` → a book row. |
| F75 | The guide's need-reason reads as a system log, not her voice | U4 | medium | 0.9 | all | both |  | open | reproduced | The needs-attention branch prints, verbatim: `This action comes from the operational signals available on the current document.` |
| F76 | The money row `Moved` is not decodable from the word alone | U4 | medium | 0.85 | 1440 | off |  | open | reproduced | The fourth money row reads only `Moved · $14,420 in motion — ordered through installed`, with a full explanatory paragraph required below to learn it means the accounts' committed figure and explicitly not funds disbursed. |
| F77 | The Care-stage document shows no guide headline at all | U2 | medium | 0.5 | 1440 | off |  | open | reproduced | On Birch Hollow, between the letterhead vitals row and the `MESSAGE CLIENT USER / PREVIEW AS CLIENT USER` instruments row, no eyebrow, headline, reason or action block is visible — the page goes from vitals straight to instruments with nothing narrating what is next. |
| F78 | The compact-tier margin is a closed, unlabelled `MARGIN ←` tab | U3 | medium | 0.85 | 1280 | off |  | open | reproduced | At 1180-1439 the right column that shows live margin items (decisions, vendor payments, notes) at ≥1440 collapses to a single fixed tab reading `MARGIN ←` with no count and no preview of what is inside. |
| F79 | Unsent POs carry the same visual weight as routine status | P2 | medium | 0.6 | all | both |  | open | reproduced | An unsent PO is announced only as `not sent` in the row's small quiet-ink provenance line — quieter than the bordered lifecycle stamp beside it, not equal to it. |
| F80 | The full spec-book workbench shows no order or PO status | P4 | medium | 0.7 | 1440 | off |  | open | reproduced | `OPEN THE SPEC BOOK →` lands on the full workbench, which prints spec completeness only — `Sku / Finish / Material / Color Fabric / Selected Dimensions / Exact Location`, all `Not specified`, plus `INCOMPLETE` — and drops order status entirely, unlike the paper and the shelf leaf. |
| F81 | `No client linked` silently blocks the money and approvals chain | P4 | medium | 0.6 | 1440 | off |  | open | reproduced | The money region prints as if available with no client attached; only the approvals region names the missing client as a blocker. |
| F82 | Every project artifact is behind opening the document first | U1 | medium | 0.9 | all | both |  | open | reproduced | The reachability inventory records the Call Sheet, the Drafting Room's direct route, the plan room, the spec book and every shelf leaf as reachable only from an open document. From `/desk`, `pull up the primary bedroom board` is four acts, and only at ≥1440 on a project-section document. |
| F83 | `The Post` and `Message {Family}` name the same idea differently | U4 | low | 0.6 | all | off |  | open | reproduced | The inbox door is labelled `The Post` (a postal noun); the letterhead's own reply action is labelled `Message {Family}` (a plain verb). Both concern client correspondence, and nothing ties the two words together. |
| F84 | The Worktable's on-paper boards strip exists only at the Speccing table | P3 | low | 0.8 | 1440 | on |  | ruled-against:C9 | flag-on-unverified | `wt-speccing-1440` shows `Boards` with `START A BOARD` printed directly on the paper; `wt-delivery-project-1440` — a signed project at Chen's stage — still shows boards only via the ≥1440 shelf, identical to flag-off. |
| F85 | The Capture Inbox introduces a new bordered card pattern | U3 | low | 0.4 | 1440 | on |  | open | flag-on-unverified | The Capture Inbox's five pending vendor captures each render as a bordered card with a coloured thumbnail swatch, vendor name, source domain and a relative timestamp — a bordered card pattern that appears nowhere on the flag-off paper. |
| F86 | The Desk header cramps and wraps at 390 | U3 | low | 0.75 | 390 | off |  | open | reproduced | `Good afternoon,` breaks after the comma and `Leah` drops to its own line (three lines of greeting) directly above `+ CAPTURE A LEAD` / `+ OPEN A PROJECT` / `FIND ANYTHING ⌘K`, which stack immediately below with very little breathing room, pushing the first folio card mostly below the fold. |
| F87 | Region status text truncates mid-word at 390 | U3 | low | 0.6 | 390 | off |  | open | reproduced | `Client approvals — NO DECISION LEAD · N…` is cut off mid-word with an ellipsis, losing the rest of the status line — which is exactly what tells her whether the region needs anything. |
| F88 | The Record has no footprint before the first completion | U3 | low | 0.5 | 1440 | off |  | open | reproduced | On Chen Residence (no completed sections) there is no `Previous work · N complete` line anywhere between `Design authority` and `Closing the book` — the foot runs straight from the accounts strip to the kickoff band, with no placeholder hinting the device exists. |
| F90 | Canon's `The Record` never prints on screen | U4 | low | 0.8 | all | both |  | open | reproduced | DECISIONS.md names this region `The Record` (I137: "The Record moves to the foot of the paper"), but the only visible string is `Previous work · {n} complete` — `The Record` appears nowhere in the rendered DOM. |
| F91 | `Next up` appears only when guidance is broken | U4 | low | 0.75 | all | off |  | open | reproduced | The eyebrow string `Next up` is used exactly once, on the `unavailable` (error) branch under the headline `Guidance is unavailable`; every healthy state uses a different, stage-specific eyebrow instead. |
| F92 | `Add to project` and `Open a project` share a word, not a meaning | U4 | low | 0.55 | all | off |  | open | reproduced | FF&E's ledger act reads `Add to project` (adds a line, board or import to the current engagement); the Desk header act and ⌘K verb read `Open a project` (starts an entirely new engagement, `no proposal needed`). Both can appear together in ⌘K results. |
| F93 | The colophon's `Team…` is the one vague act among plain verbs | U4 | low | 0.7 | 1440 | off |  | open | reproduced | Colophon actions read `Brief a vendor`, `Hold`, `Archive` and `Team…` — the first three are plain imperatives naming their result; `Team…` is a noun with a trailing ellipsis and never says it opens the Call Sheet picker. |
| F94 | Canon's `Contents Page` prints on screen as `THE STUDIO` | U4 | low | 0.7 | 1440 | off |  | ruled-against:R95/C15 | reproduced | R95 names this block a typographic contents of rooms, ledgers and begin-verbs; the actual on-screen eyebrow directly above it reads `THE STUDIO`, not `Contents` in any form. |
| F95 | The spine's mark count changes between documents | U1 | low | 0.65 | all | both |  | open | reproduced | The spine's mark row prints seven marks on a project document, four on the sent proposal and six on the brief. Since the marks carry no visible labels at any width and no ordinal, the same visual device reads as a different scale on each document she picks up in a morning. |
| F96 | The money region is folded by default | U2 | low | 0.75 | all | both |  | open | reproduced | On a project with no authority, no committed money and a quiet account, the money region defaults folded, so `Draw an invoice` is one click away rather than on screen. |
| F97 | The margin rail has no functional closed state at ≥1440 | U2 | low | 0.85 | 1440 | off |  | open | reproduced | `isFullRail` is always true above 1440, so the `margin-open` and `margin-closed` screenshots are pixel-identical — the rail is permanently mounted and cannot be collapsed, unlike the 1180-1439 tier's explicit `MARGIN ←` toggle tab. |
| F98 | The Receiving tab was never opened — project scoping unverified | U2 | low | 0.4 | 1440 | off |  | open | state-dependent | The Receiving tab is mounted with no project scoping prop at all — unlike Vendors, which receives briefProjectId — so it cannot be entered project-scoped. |
| F99 | Free-text description prints in the same register as studio copy | P3 | low | 0.6 | 1440 | off |  | open | reproduced | Below the Direction document's folio strip a paragraph reads `Draft fixture for a no-login household: proposals.designer_client_id links to the household so document_state Shape B rescues the client_name.` in the same body type as the rest of the paper, with no internal-note distinction. |
| F100 | The two leaf routes name the project differently on the way back | P1 | low | 0.9 | 1440 | both |  | open | reproduced | `w1440-leaf-plans-route`'s return link reads `← CHEN`; `w1440-leaf-specbook-route`'s reads `← CHEN RESIDENCE`. Neither says Desk or document. |
| F101 | Whether a ledger sheet preserves her place from a document is unverified | P4 | low | 0.6 | 1440 | off |  | open | state-dependent | A ledger sheet portals over a still-mounted document and only locks body scroll, so her place is preserved; what remains unverified is only the visual confirmation. |
| F48 | Spec book has no door on install or care | U1 | blocker | 0.92 | all | both |  | open | reproduced | The `Spec book →` link renders inside `{mode === 'project' && …}`; in `mode === 'install'` the head prints only `Install` plus meta. There is no spec-book entry in `STUDIO_ROOMS`/`LEDGERS`/`VERBS`, so `matchSurfaces` cannot find it and ⌘K has no spec-book row in either branch. |
| F49 | No visible way to open ⌘K anywhere on a phone | U2 | blocker | 0.85 | 390 | both |  | open | reproduced | Inside a document at 390 there is no visible way to open ⌘K — the mobile bar, its More menu and the sheets call no opener. The Desk header's `Find anything` button is the only phone entry point, and it exists only on /desk. |

## Killed findings

| id | title | killer | note |
|---|---|---|---|
| F06 | Orders ledger shows no PO acknowledgment state | code-truth | Every PO row prints its send/ack lifecycle in its provenance line, and an ack act is gated and offered on exactly the rows that lack one. |
| F19 | A sent, unopened proposal is invisible on the Desk | code-truth | A sent-unopened proposal has its own Desk NeedKind with text, action and stamp once it passes the promote threshold; day one rides an in-motion chip. |
| F20 | Nothing on the paper names a PO, receiving or a claim | code-truth | The FF&E line unfold on the paper is explicitly PO detail, movement and receiving, with a claim lifecycle and the inspection drawer mounted in place. |
| F22 | Flag-off, an absent call sheet looks like an empty crew | repro | With call-sheet flag ON here, the roster UI is fully present; the flag-off absent-state described could not be reproduced in this environment and needs a session with that flag actually off. |
| F27 | The install spread shows no FF&E lines at all | code-truth | Install mode reads the same unfiltered project FF&E query and renders every line ungrouped; the empty line only prints when the project has zero items, so the two shots were different documents. |
| F31 | The downstream damage of a date move is prose, not a preview | code-truth | A full ripple preview exists — a dashed-terracotta ghost layer drawing moved boundaries, milestones, the old→new vector and dated labels from one RippleDiff, on both drag and spine-originated edits. |
| F68 | `CLOSE THE BOOK` looks equally clickable while blockers are listed above | code-truth | `Close the book` is disabled while operational blockers stand — closureReady requires both the checklist and operational.ready — so it is not rendered as an available act. |
| F69 | `BEGIN THE DIRECTION` is offered live with 0 of 5 essentials captured | code-truth | `Begin the Direction` carries `disabled={!ready \|\| landing}` — it is not live at 0 of 5 essentials. |
| F89 | An unexplained circular badge overlaps page content | code-truth | No product component renders a fixed bottom-left circular badge; the shots were taken against a dev server, where that mark is the Next.js dev-tools indicator, not the document surface. |
