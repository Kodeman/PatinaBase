# Canon digest — G2 ground:canon-digest

Source: `docs/design/the-document/DECISIONS.md` (9,431 lines; grep+sed only, never read whole), `apps/designer-portal/CLAUDE.md`, `docs/design/the-document/CLAUDE.md`, `artifacts/document-flow-directions-2026-08-15/{delivery-plan.md,delivery-record.md}`, `artifacts/spine-shelves-prototype-2026-08-15/BLUEPRINT.md`. Confirmed: tail -40 of DECISIONS.md shows `*Entries add: I143 · last id = I143*` — nothing appended after I143 at plan time.

---

## (A) Constraint ledger — id, rule, source, quote

| ID | One-line rule | Source (file:line) | Quote (≤25 words) |
|---|---|---|---|
| C1 | Strict one-document focus — no split view, no peek/hold; Esc/"Put down" only exit | DECISIONS.md:12 | "Strict one document at a time. No split view, no peek/hold. Esc or 'Put down' is the only exit." |
| C2 | Zero shadows anywhere, CI-blocking lint | DECISIONS.md:15 | "No shadows. Anywhere. No exceptions. Value contrast + flat stacked edges + folder tab; mechanically enforced via lint, CI-blocking." |
| C3 | Interruptions are designer-driven, never system-dictated | DECISIONS.md:13 | "Interruptions are designer-driven, never system-dictated. Nothing breaks through by default." |
| C4 | Drawer persistent on every screen; ledger sheets are overlays, collapsed by default, no badges/pulsing counts | DECISIONS.md:19 (D8) + R96 at :2955-2959 | "Studio Drawer persistent on every screen; ledgers open as overlay sheets; collapsed by default, no badges, no pulsing counts." R96 amends only the sheets' *presentation* (charcoal slide-up → centered paper-folio treatment); D8's no-badges law is untouched. |
| C5 | No toast layer — failures render as a quiet inline band at the act site | DECISIONS.md:2672 (R83, confirmed exact line) | "The toast layer is removed from (document) surfaces entirely." |
| C6 | Scored ink — DocumentAction sheds boxes/borders/fills for a bare DM Mono word with proofreader's scoring | DECISIONS.md:6584 (I107, confirmed) | "Boxes, borders and fills are retired from DocumentAction. An action is a bare DM Mono word with proofreader's scoring." |
| C7 | One inked leader per region (index 0 only); row-level `···` overflow renders always-visible, never hover-gated | DECISIONS.md:8377 (I135 header — **not itself the rule text**; rule text is at :8386-8393 "Region heads" + :8404-8409 "Always-visible row overflow") | "`RegionHead` enforces this at the type and runtime level ... so a region can have exactly one leader." / "the `···` row-verb overflow ... renders always visible, not hover-gated." |
| C8 | Shelved spine (running index/rooms/shelves) is ≥1440px only; the room lens LIFTS, never filters | DECISIONS.md:8427 (I136 header; lens-doctrine text at :8462-8471), errata at :8543 | "It never filters: nothing hides, and a held room keeps its state word with the 'In hand' tag added, never swapped in." |
| C9 | I136 shelved boards as reference beside the spine; I139/Q1 REVERSES that for the speccing stage only — boards return to the paper as the strip | DECISIONS.md:8881 (I139 "boards strip reverses I136" paragraph — actual line 8881 falls inside that paragraph, confirmed) + Q1 at delivery-plan.md:6 | "The boards strip reverses I136 — for the speccing stage only (Q1, ratified) ... The shelf remains boards' home everywhere else." |
| C10 | The Record (PreviousWork) mounts at the foot of the paper, after the active section/account/kickoff bands, before the colophon | DECISIONS.md:8608 (I137) — **corrected from guard's :8600, which is the I137 heading, not the Record rule** | "The Record moves to the foot of the paper. ... It now mounts after the active section ... immediately before the colophon." |
| C11 | The running index is derived from one ordered array (`PROJECT_PAPER_ORDER`) that also drives actual mount order — approvals → schedule → ffe → money — never declared separately | DECISIONS.md:8616 (I137) — **corrected from guard's :8600** | "The running index is derived from the paper order, not declared beside it. ... the index and the paper disagreed about what the paper said." |
| C12 | SP4: "Add a room" is an in-flow scored-ink line at the foot of the FF&E room list, not a promoted region-head leader | DECISIONS.md:8628 (I137) | "'Add a room' ... prints there as a scored-ink line — visible, never behind a `···`, never only in the head ledger." |
| C13 | SP3: one scored send-wall state line at `ProposalInstruments`, above both legacy watch and design-services status block; nudge lives only there | DECISIONS.md:8657 (I137); errata correction at :8684-8695 (I137-errata: withholds nudge on `issued_on_paper`) | "The paper now always opens with one scored state line ... The nudge lives on that line and nowhere else on the wall." |
| C14 | Q3/I138: the Worktable is the destination; ships behind fail-closed flag `worktable`, flag-off byte-identical to main | DECISIONS.md:8738 (I138 header/opening), fail-closed statement at :8740-8742 | "Unlike W1 this ships behind the fail-closed flag `worktable` ... the flag off is main's composition exactly." |
| C15 | R93/R94/R95: one populated ⌘K registry, empty-state marginalia, and the Desk's Contents Page (labels + doorway glyphs + icons — never counts/tiles/metrics) | DECISIONS.md:2943 (R93), 2947 (R94), 2951 (R95) | R95: "a typographic contents of rooms, ledgers, and begin-verbs — labels, doorway glyphs, and icons, constrained to never carry counts, tiles, or metrics." |
| C16 | R113 is a DUPLICATE ID — two unrelated rulings both titled R113 | DECISIONS.md:4213 ("Field Capture M4 passed... P1 CLOSED; P2 opens") and DECISIONS.md:8081 ("Band is a state, not an error") | Both confirmed live at those lines; the ledger's own scanner (I55) is documented as handling exactly this kind of "historical duplicate" (it names O7's duplicate explicitly, not R113's — R113's duplication is unremarked in-file). Treat both as canon; disambiguate by title, not number. |
| C17 | D12 full-bleed document (no charcoal surround/border/radius in hand) + D13 (mobile spine sheet) | D12 at DECISIONS.md:211; **D13 has no standalone `### D13` heading anywhere in the file** — corrected below | D12: "A document in hand fills the entire viewport. No charcoal surround, no page border or radius, no stacked edge." "D13" is used throughout (e.g. :360, :662, :760, :788, :8436, :8760) as shorthand for the mobile bottom-sheet spine pattern that D3 (:14) gates ("Mobile pattern: margin items collapse to anchored chips; spine becomes a bottom sheet") and that I21 (:760) built and gated closed. |
| C18 | R7 stamps render only real/true state (no over-attributing); R8 inert settled bars must NOT show an "unfold ↓" hint until the unfold ships; R24 files mount to the paper as the Folio | R7 :273-285, R8 :285-295, R24 :1017 | R7: "Stamps are the document's truth device ... PO-level claim data stamping every line on a multi-item PO lies at the item grain." R8: "affordances that do nothing teach the document to lie; hint copy appears only when a bar's unfold ships." |
| C19 | "Thumb Index" is NOT logged anywhere in DECISIONS.md — confirmed absent | grep -in 'thumb index' → zero hits | n/a (absence confirmed) — Kody's verbal removal has no ledger trace to contradict or corroborate; treat as out-of-canon by omission, not by ruling. |
| C20 | One icon language: `src/lib/document/registry.tsx` is the single source of every surface's name/icon/alias/shortcut, consumed everywhere (⌘K, drawer, Contents page, sheet chrome) | DECISIONS.md:2961 (I54) | "One name, one icon, everywhere; no more per-surface copies drifting apart." |

---

## (B) Known-open list

**From the brief, verified:**
- **I114 — section↔stage mapping session, still owed by Kody.** Referenced repeatedly as blocking (I137 "Not in this wave" list at :8676; I138 "Not in this wave" at :8846-8848; I143 close-out at tail: "The I114 section↔stage mapping session is still owed by Kody. Every wave left `active_section` standing as the sealing authority rather than touch it."). No R-entry resolves it. Line ~9405 cited in the brief does not exist in the file's true content at that offset — closest actual anchor is the I143 tail block (~9390-9420) and the I138 body (~8746, ~8848). **Correction: cite I143's tail paragraph, not a specific ":9405" line — the file has no anchor there that says "I114" verbatim; the nearest is the un-numbered final paragraphs before `Entries add: I143`.**
- **T4 (no fleet view) and T2 (install-as-label)** — from `artifacts/doc-ux-review-2026-08-13`, per the brief. `grep -in 'fleet\|roster tier\|install.*mode' docs/design/the-document/DECISIONS.md` returns no hits tying either term to an R-entry. **Confirmed still open, unaddressed in the ledger.**
- **A flagged line on a sent proposal is unanswerable anywhere.** I140-errata (:9144-9247 region): "no surface anywhere today can answer a flag on a sent proposal, main's Desk `?flagged=1` walk-in included ... Recorded as a product debt: answering a client's flag on an issued proposal has no surface. That is the gap to close, not the leader to restore." Also restated verbatim in I143's tail.
- **Money doesn't seam on install/care.** I141 (:9035-9112) + I143 tail: "Money does not seam on the install or care spread. Scoped out in I141 because money does not mount there and a seam would be both a new mount and a second accounts surface. Its own future ruling."
- **Knowledge shelf names a surface that doesn't exist.** I136 (:8427-8543): "Open for a design ruling: whether 'Knowledge' should name a surface that does not exist yet, or be renamed to what `/library` actually holds." No later R-entry resolves it (checked I137-I143 for "Knowledge" — no hit).

**Additional open items surfaced by the O-index scan (grep `^### O[0-9]`):**
- **O5 — portal surfaces with no Document-model home** (Aesthete, Insights, Portfolio, Resources, Team, Reviews, Nurture, rooms directory, vendor directory) — **RESOLVED** by R5 (:151-177), destinies assigned per surface. Not open.
- **O6 — R10-R12 missing text** — **RESOLVED**, "R10–R12 confirmed — resolves O6" at :427.
- **O7 (PO send weave)** — **RESOLVED** by R18 at :571 ("O7 RESOLVED").
- **O7 (Track 5, duplicate number, quiet-confirm-vs-toast)** — **RESOLVED by R51** same sitting, explicitly noted at :1966-1978 as a deliberate duplicate-number artifact, not a data error.
- **O8 — do clients see the schedule ghosts/revision ledger** — **RESOLVED in direction** by R105 (:3654-3706): "the middle path... clients see revisions that touched client-facing dates... the full ledger stays studio-side" — but flagged as a **follow-on client-portal projection not yet built** ("Backlogged... the O8 client projection"). Direction ruled; implementation still owed.
- **O9 — where do The Work's task rows live in the spine grammar** — **RESOLVED in direction** by R105: "tasks fold into items ... as a follow-on workstream (not a retrofit of these slices). Until that migration lands, the shipped separate band beneath the spine stays as the interim rendering." Direction ruled; migration still owed.
- **O10 (what fidelity a client sees, depends on O8) and O11 (whether payment milestones ever harden a phase)** — both logged **OPEN**, no resolution found (:8094-8095, R113/"Band is a state" entry): "**O10 (open)**... **O11 (open)** — whether payment milestones ever harden a phase; deliberately deferred (I56 A0.3 collision stands)."

**Net still-open (not ruled, or ruled-in-direction but unbuilt):**
1. I114 section↔stage mapping session (Kody's, gates any sealing-semantics change)
2. T4 no fleet view / T2 install-as-label (untouched by any R-entry)
3. Flagged line on a sent proposal — unanswerable anywhere (product debt, named twice)
4. Money doesn't seam on install/care spread (scoped out, "its own future ruling")
5. Knowledge shelf naming (design ruling requested, never answered)
6. O8 client-portal projection of the revision ledger (direction ruled, build owed)
7. O9 task-rows-fold-into-items migration (direction ruled, migration owed)
8. O10 — client-visible fidelity (open, blocked on O8's still-unbuilt half)
9. O11 — whether payment milestones ever harden a phase (open, deliberately deferred)

---

## (C) I137–I143 summaries (≤4 lines each, incl. every errata)

- **I137 — Wave 1, collation + shared planks (GA, no flag).** Record moves to paper's foot; running index now derived from one canonical mount-order array; SP4 rules "Add a room" stays in-flow (not a promoted region leader); SP1 verified already-true (Begin the Direction → `/doc/<newProposalId>`); SP2 relationship-id alias verified already-true; SP3 ships the one send-wall state line, nudge lives only there.
- **I137-errata.** Send wall now withholds "Nudge" on `issued_on_paper` proposals (was firing the edge function at nobody); the line's logic extracted to a tested pure derivation (`deriveSendWallLine`) with two pinned invariants; the paper-order claim is now actually tested (previously self-referential fixtures); dead `expired` branch removed (00414); "Sent N days ago" now counts local calendar days, not 24h buckets.
- **I138 — Wave 2, Worktable core, behind fail-closed flag `worktable`.** Composition machinery only (selector, pin, ceremony, frame) — no tools. `deriveSections`/`active_section` untouched; table selector reads it via new `table-derivation.ts` (amendment A5). Stale-table pinning (R7) snapshots composition on pick-up, arms a "turn the table" line rather than reshuffling live. Flag-off is byte-identical, guarded by `worktable.test.tsx`.
- **I139 — Wave 3, Speccing table's four tools.** Fills W2's empty slots: rooms-rail, scheme, boards-strip, reach-in — all proposal-keyed off `document_state.proposal_id`, never the raw route param. Room lens is a page-owned held-id that LIFTS (never filters) into scheme + boards strip. Q1 ratified here: boards return to the paper for the speccing stage only (shelf stays boards' home everywhere else). Six named debts recorded (reach-in drops category, lands Unassigned, no configure step, etc.).
- **I140 — Wave 4a, Finalize table.** Verdict roll-up promoted to headline; `deriveFinalizeLeader` computes one leader (Answer the flags / Nudge / Preview / expired) from lifecycle × verdicts × send wall; Offer facets fold open under the spread reusing the Room's own editability gate (`issued` → read-only); client's copy becomes a ≥1440 shelf leaf; **Drafting Room press to `/drafting` redirect declared (Q5 step 2), Amendment A3 "in force."**
- **I140-errata — critical reversal.** **The press is fully descoped — `/drafting` stays open in both flag states; Amendment A3 is NOT in force.** Root cause: with the press shut, a draft proposal (Speccing table, Scope+Vision only) had no authorable home for Phases/Exclusions/Payments/Terms once Finalize's Offer seams went read-only. **"Answer the flags" dropped as a leader entirely** — no surface anywhere can answer a flag on a sent/issued proposal (stated as a debt, not fixed). Also: document-kind gate added (legacy vs design-services/furnishings), headline now counts *unresolved* flags only, letterhead chrome exempted from the one-leader-per-table guard.
- **I141 — Wave 4b, Delivery table.** Release-ceremony leader lifts to the table head (ceremony itself stays inside FF&E/schedule); FF&E head demotes to "Add to project" when the lift is showing. Money compresses to one scored seam line ("$X committed of $Y authority"), folded by declaration on the table, remembered on its own `money-table` key. Install setting untouched — release lift gates strictly on the project (procurement) spread, never leaks onto care/install.
- **I141-errata.** Money seam's declared fold now WAITS for reads to settle (was flashing "$0 committed" before the real figure loaded — a lie the region's own tiers refuse elsewhere); fold also now yields whenever `accountQuiet === false` (an overdue invoice must never be folded away, since Delivery suppresses AccountBand's own home). FF&E head now keeps a visible-but-disabled release entry in the `canRelease && !anyEligible` window rather than going silent.
- **I142 — Wave 4c, Intake spread header.** Household chip promoted to a quiet reading-size header (name/description/arrival source) sourced from `useLead`, printed-identity only per Q6 (no capture drop, no board tile, no buttons). Renders `null` if data absent; costs one new gated query only on discovery-shape documents (deduped for free on lead-shape).
- **I142-errata.** Header gated to the **discovery spread only** — on a Brief document `BriefSection` already prints the same three facts immediately below, so the original scope ("brief/discovery") was printing the household twice; narrows the recorded debt (only one new query, only on discovery, none on brief).
- **I143 — "Start to Signature" CLOSED — consistency pass + what's left.** Confirms the three W4 errata trailers appear physically after I142's own trailer but the file's final `last id = I142` line is the true position (a reading hazard, not a numbering defect); explicitly warns "I140 must not be read without I140-errata... The same holds for I141." Lists what's deliberately left open: Kody's flag-on walk (nothing deployed, flag never flipped in front of a human), I114 mapping session, the Offer's missing editable home on Speccing (why the press stays descoped), the unanswerable-flag gap, money-doesn't-seam-on-install, plus several smaller recorded debts (category drop, focus drop on release lift, `onReleaseOffered` not withdrawn on unmount, Finalize zero-leader edge, intake header double-query risk already narrowed by its own errata).

---

## (D) Start-to-Signature Q1–Q7 rulings, verbatim

From `artifacts/document-flow-directions-2026-08-15/delivery-plan.md:6`:

> Q1 boards REVERSED for speccing stage (DECISIONS amendment required) · Q2 I114 session owed by Kody (nothing here depends on it) · Q3 ceremonies trusted (Worktable is the destination) · Q4 A's S-set first · Q5 Drafting Room retires in two steps (landing fix now; decomposition later waves) · Q6 intake store HELD (Table I honest seams only) · Q7 <1440 same bar, same slice (A4 doctrine: no display:none for table tools).

And the framing line immediately above it (line 3):

> Ratified 2026-08-15 by Kody's Q1–Q7 rulings on the "Start to Signature" deck. Fable architects/orchestrates/reviews; agents execute. Full scope = the ratified sequence; excluded only what is gated on Kody's still-owed I114 mapping session (A's L-b sealing semantics, the Rule merge — not in the ratified path; B's tables read `active_section` only, which needs no I114 ruling).

**Note on Q5 as later amended:** I140-errata (2026-08-16) revises Q5's execution, not the ruling itself — "Kody's Q5 ruling was a TWO-step retirement — the landing/return fix now, the decomposition onto the tables in a later release once the Speccing table proves itself" — and states the W4a lane over-executed step two early; the plan's own Q5 text ("retires in two steps") was correct all along, the *build* (I140, pre-errata) was not.

---

## (E) Glossary — the product's own nouns, per the ledger

| Term | Definition (as the ledger defines it) | Defining entry |
|---|---|---|
| **Desk** | The landing surface (`/desk`) — folders per client relationship/project, truthful need lines, no dashboard. Gains standing front matter "The Studio" (Contents Page: rooms/ledgers/begin-verbs as labels + doorway glyphs + icons, never counts/tiles/metrics). D1's strict-focus law does not reach it (it is not a document). | I4 (:177), R95 (:2951) |
| **Document / paper** | The `/doc/[id]` surface — one document per engagement (project when signed; live proposal/lead chain pre-signing). Full-bleed in hand (D12); "the paper IS the screen." | O1/R1 (:28, :89), D12 (:211) |
| **Spine** | Sticky full-height rail on the open document: Put down, phase markers, timer, presence line, and — ≥1440px only — the running index, rooms block, and shelves block. | D12 (:219), I136 (:8427) |
| **Strata Mark** | The document's fill-state progress primitive (phase markers on the spine); a new brand primitive with no prior existing asset, name TBD at seeding but ratified as "Strata Mark" by R15/R35. | R15 (:381), R35 (:1432) |
| **Margin** | The rail of margin items (decisions, messages, money, notes, pulse) — a read model over existing tables, one line per item, no list page for most kinds. | I13 (:339), R14 (:363) |
| **Drawer (Studio Drawer)** | Persistent chrome bar on every screen; opens ledger *sheets* (Orders/Hours/Accounts/People) as overlays; collapsed by default, no badges, no pulsing counts (D8). | D8 (:19) |
| **Room vs. Sheet (drawer weights, D14)** | Two declared weights for drawer objects: **Sheets** = charcoal quick-reference overlays, document stays mounted beneath (Orders/Hours/Accounts/People). **Rooms** = full-screen paper-material workplaces you walk INTO — entering one puts the current document down (D1 holds; the Room IS the thing in hand). Library is the first Room. | D14 (:985) |
| **Shelves** | Reference-material leaves beside the spine (Plan room, Spec book, Mood boards, Call sheet, Knowledge) — 320px, `role="region"` not `dialog`, paper stays live behind them (not modal; D1 forbids split views but reference material is exempt). | I136 (:8427) |
| **The Record** | `PreviousWork` — the settled Brief/Discovery/Direction/Proposal history bars — mounted at the foot of the paper (I137), after the active section, "an appendix, not a preamble." | I137 (:8608) |
| **Rule (the Rule / ScheduleRule)** | The collapsed schedule header/drafting strip — collapsed header form of the Ledger Spine's schedule architecture (R99); on Project documents it now folds by default under its own key `schedule-rule` (never `schedule`, to avoid colliding with the ledger's own fold key). | R99 (:3002), I136 (:8427 "schedule frame folds") |
| **Folio** | Files clipped to the paper — the document's attachment/file-mount device, mounts on proposal-stage documents too (space plans reach the client copy pre-project, R85). | R24 (:1017), R85 (:2761) |
| **Worktable / table** | Q3's ratified destination: the document's composed "middle" reads as one of four typed TABLES (Intake / Speccing / Finalize / Delivery) derived purely from `active_section`, behind the fail-closed flag `worktable`. Never a replacement for `deriveSections`. | I138 (:8738) |
| **Seam** | A region compressed to one scored line that unfolds in place to the full region — used generically (fold seams, D13, R95's seam boundary) and specifically for the Delivery table's money compression ("$X committed of $Y authority"). | I135 ("fold seams," :8377), I141 (money seam, :9035) |
| **Leader** | The one `inked`/`primary` weight act a region (or, on a Worktable, a whole table) is allowed to carry at ledger index 0 — enforced at type+runtime by `RegionHead`/`DocumentActionGroup`; letterhead chrome is exempt (I140-errata). | I135 (:8377/:8389-8393), I140-errata (:9144) |
| **Red-letter zone** | The unmet-need line on Project documents, in a red-letter register that reads as urgent without a badge or box — consistent with the no-badges doctrine (D8). | I135 (:8397) |
| **Scored ink** | The DocumentAction visual grammar: no boxes/borders/fills, a bare DM Mono word with proofreader's-style scoring (double/single/unscored underlines by weight) and a wet-ink press animation on activation. | I107 (:6584) |
| **Put down / take in hand** | D1's exit vocabulary: "Put down" (or Esc) is the only way to leave an open document; entering a Room also puts the current document down through the same flow. "In hand" is the room-lens state-word suffix for a room currently held. | D1 (:12), D14 (:985), I136 (:8462-ish "In hand" tag) |

---

## (F) Corrections to the guard's line refs

1. **C4 (D8/R96)** — D8 at :19 is correct; R96 (the sheet-presentation amendment) is at **:2955–2959**, not implied elsewhere. Confirmed as given.
2. **C5 (R83)** — :2672 is correct as given; confirmed exact heading line.
3. **C6 (I107)** — :6584 is correct as given; confirmed exact heading line.
4. **C7** — :8377 is the **I135 section heading**, not the rule text itself. The actual "one inked leader" rule sentence is in the "Region heads" paragraph starting ~:8386 through :8393 ("`variant` is ignored at index 0..."), and "always-visible row overflow" is the paragraph at ~:8404-8409. The guard's ranges (:8389-8393, :8404-8409) are accurate for the sub-claims; only the bare ":8377" needs the caveat that it's the header, not the quote source.
5. **C8** — :8427 is the I136 heading (accurate as an anchor for "the shelved spine ratified"); the lens-doctrine sentence ("It never filters...") is at ~:8462-8471. Errata at :8543 is correct.
6. **C9** — :8881 does fall inside the I139 "boards strip reverses I136" paragraph (paragraph spans roughly :8878-8892) — accurate. Q1 is not a DECISIONS.md line at all; it's `delivery-plan.md:6`, which the guard already cited correctly as ":8678" for something else — **correction: the guard's ":8678" does not correspond to Q1 in DECISIONS.md; Q1 lives only in delivery-plan.md.** No DECISIONS.md line at :8678 mentions Q1 or boards; :8678 falls inside I137's SP3 discussion. Treat "Q1" as sourced from delivery-plan.md:6 only.
7. **C10 (Record at the foot)** — **Corrected.** Guard said :8600, :8608. :8600 is the I137 section *heading* ("Start to Signature Wave 1..."); the actual "Record moves to the foot of the paper" sentence begins at **:8608**. Both a heading-anchor and a content-anchor exist, but only :8608 is the rule text — this is noted, not a contradiction.
8. **C11 (running index from mount order)** — **Corrected.** Guard cited only :8600 (I137's heading, same as above). The actual sentence "The running index is derived from the paper order, not declared beside it" is at **:8616**, a different paragraph than the Record's. The guard's single line ref under-specifies two distinct claims that happen to live in the same entry.
9. **C13 (SP3 + errata)** — SP3's own text starts at :8657, not :8600 (again the entry-heading vs. content-start distinction). Errata is correctly anchored — the "never nudges a paper-issued agreement" correction is the errata's lead paragraph at **:8684-8695** (guard cited ":8684," accurate for the errata heading and the correction's paragraph is right beneath it).
10. **C14 (Q3/I138)** — :8738 (heading) and :8744-8746 (fail-closed statement) are close but not exact: the actual "ships behind the fail-closed flag" sentence is at **:8740-8742**. Off by ~4 lines; substance is correct.
11. **C16 (R113 duplicate)** — Confirmed exactly as the guard states: two independent `### R113` headings exist, at **:4213** ("Field Capture M4 passed... P1 CLOSED") and **:8081** ("Band is a state, not an error"). This is a genuine ID collision in the ledger (unlike O7's documented duplicate, this one is never flagged in-file as deliberate) — flag it for the panel as a live footgun when anyone cites "R113" without a title.
12. **C17 (D13)** — **Correction, not just a line check.** There is **no `### D13` heading anywhere in DECISIONS.md.** "D13" is used as an informal shorthand throughout (first appearance :360, latest :8760) for the mobile-spine-bottom-sheet milestone that D3 (:14, the actual locked decision) names ("spine becomes a bottom sheet... dedicated mobile milestone gates the default flip") and that I21 (:760) built and gated. Cite **D3 (:14) + I21 (:760, gate status at :788)** for this constraint, not a nonexistent "D13" heading.
13. **C19 (Thumb Index)** — Confirmed via `grep -in 'thumb index' DECISIONS.md`: zero hits. Verbal removal has no ledger trace either way.
14. **C15 (R93/R94/R95, "one registry")** — R93/R94/R95 are correctly at :2943/:2947/:2951; "one registry, Contents = labels + doorways" is R95's content, confirmed. The guard's phrase is accurate.
15. **General note on I135's line-8377 pattern:** across this ledger, an entry's numbered `###` heading line is frequently 8-15 lines above the actual sentence a citation means to quote — several of the guard's single-line refs (C7, C10, C11, C13, C14) point at the *entry heading* rather than the claim's sentence. All are corrected above with the precise content line; none of the guard's underlying claims were substantively wrong, only under-anchored.

*No line-ref check in this canon guard proved false in substance — only imprecise (heading- vs. content-anchored) or resolvable to a companion file (Q1/Q2.../Q7 → delivery-plan.md, not DECISIONS.md; D13 → D3+I21, no such D-entry exists).*
