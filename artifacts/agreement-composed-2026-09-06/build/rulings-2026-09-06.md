# Build rulings — The Agreement, Composed (2026-09-06)

Kody: "Assemble a team of agents … Deliver the Agreement system to production." Per the standing precedent (client-approval program, 2026-09-04), every ruling in the proposal is adopted **as recommended**; Kody or Leah may overturn any of them later and the build fixes forward. Counsel-gated items stay gated: they ship seeded and disabled.

| # | Ruling | Adopted as |
|---|---|---|
| R1 | Reverse R85 for agreements | Yes — a studio Library is a different object from the retired per-user `proposal_templates`. DECISIONS entry required at Wave 2 merge. |
| R2 | Template scope | Studio only; "mine" is a filter. |
| R3 | Who edits the Library | Owners and admins (`is_org_admin_or_owner`) edit; every active member composes. |
| R4 | The floor | Parties, signature block, one typed money part for a class that bills; a ceiling part is required whenever a rate card is present. Everything else removable, Exclusions included. |
| R5 | Prose never carries money | Only `schedule` variants project into terms/authority. |
| R6 | When parts freeze | At send (today's guard). Unsend = supersede. |
| R7 | Names | Agreement · Part · Library · Template · Addendum. Never "clause library" / "contract builder" in the studio's face. |
| R8 | Client copy | Designer's order; per-part `client_visible`; `client_visibility_tier` unchanged. |
| R9 | Which variants create authority in Wave 2 | rate_card, ceiling, retainer, cadence (today) + flat, per_phase; percent/cost-plus/day_rate/package/procurement(beyond deposit %)/pricing_basis/draws/allowances record-only. |
| R10 | Licensing attestation | Self-attested fields; Patina stores, never verifies; gates selection of the design-build template. Wording carries the 34-word disclaimer. |
| R11 | Jurisdiction notices | Seeded (WI, MN, IL, CA, NY, MA) with `enabled = false`; no UI to enable until counsel reviews — an admin-only flag per state. |
| R12 | What the client keeps | Frozen HTML snapshot at execution (Wave 2). No PDF. |
| R13 | Subs in the client's copy | Identities yes; bid ledger never. |
| R14 | Permissions inside the room | Not now. |
| R15 | Sign and pay | Offer after signature; never gate. Wave 3 P13. |
| R16 | Do subs sign inside Patina | Yes, by token link, no login (reuse `trade_rfq_tokens` shape). Counsel on flow-down wording — the seeded flow-down clause ships disabled like R11. |

## Wave 1 architect findings adopted (build-sheet §0, 2026-09-06)

- **F-1** The `parts` key in `_commercial_document_fingerprint` is conditional (only when ≥ 1 parts row), mirroring the `tradeScope` conditional at 00423:1247 — a document with no parts hashes byte-identically to today, so in-flight `client_signed` agreements stay countersignable.
- **F-2** `NOT NULL` drops on `billing_ceiling_cents` for BOTH `proposal_service_terms` and `project_billing_authorities`; the three NULL-unsafe readers (`get_project_authority_summary` 00422:2467, the addendum promotion loop 00566:841-842, time authorization 00414:911-913) become NULL-safe (NULL = uncapped).
- **F-3** All parts are hashed (contract wins over the proposal's "client-visible"); the parts alias inside the fingerprint is `ap`, not `p`.
- **F-4** `upsert_design_services_draft` refuses an empty rates array (00422:1722-1728); the projection is extracted into one private helper used by both callers rather than relaxing that guard.
- **T0 handshake**: the types commit (`packages/types/src/agreement.ts`, `commercial.ts` edits, index export) lands first and is cherry-picked onto every lane branch before lanes fork; `pnpm turbo build --filter=@patina/types` before any lane's first type-check.

## Wave 1 integration rulings (2026-09-06, after review round 3)

- **R17 · One source of truth per document (backend R1, client R3-2, designer DR22).** Once a proposal carries ≥ 1 parts row, `proposal_service_terms` / `proposal_service_rates` are a projection and only `upsert_agreement_parts` may write them. Mechanism (all three, belt and braces): (a) a BEFORE INSERT/UPDATE/DELETE trigger on both tables refuses when the proposal has parts unless the transaction-local GUC `app.agreement_projection = proposal_id` is set — only `upsert_agreement_parts` sets it (same GUC pattern as `app.commercial_document_id`); (b) `upsert_design_services_draft` raises a typed refusal `agreement_composed` when parts exist, and the flag-off seven-facet room shows it as one plain sentence ("This agreement is composed from parts. Open it in the Contract Room with parts on to change it.") and disables Save; (c) if grep proves no client code writes those two tables directly, `REVOKE INSERT, UPDATE, DELETE ON proposal_service_terms, proposal_service_rates FROM authenticated` (regenerate the ACL seed); if a direct writer exists, keep the grant and rely on (a). SQL test: reviewer probe P16 and P3b must now refuse.
- **R18 · One part per money variant (backend R2, designer DR20).** The DB rule "an agreement carries only one part of each money variant" is right; the Add menu must not offer a second ceiling/retainer/cadence/deposit/rate card, and readiness reports the duplicate as a blocker so Save can never hit 23514 from the room.
- **R19 · Standard parts keep stable keys (backend R3).** The nine standard parts always carry their `patina.*` keys; the composer never re-keys them; prose projection (scope, terms, deliverables, exclusions) reads by those keys, and a custom prose part (`custom.<uuid>`) never projects. Test: a rail-composed agreement projects the same terms row the seven-facet room would.
- **R20 · Money projection by kind + variant is accepted (backend R4, designer DR16).** The build sheet's §6.2 cases 6 and 7 are amended to match; recorded here so the deviation is ruled, not silent.
- **R21 · "Not yet set" survives composition (client R3-1).** The composed homeowner body never prints `$0` or `0%` for an unset money part; it prints today's "Not yet set" and readiness blocks send while any money part the class requires is unset. Empty clause/list parts render nothing, not a naked heading (R3-6). The R4 floor reads only client-visible money parts (R3-3).
- Pre-existing red on main, not this wave's to fix, recorded for the main backlog: `direct_order_attribution_test.sql`; `threshold.spec.ts:221` seed-accumulation drift and `:158` timezone fragility; designer-portal lint (2 errors, byte-identical on origin/main). Backend F-5 (a fourth rate-card refusal) is accepted as the build sheet's omission — keep it.

## Wave 1 close-out rulings (2026-09-07, after the re-gate)

- **R22 · The fee floor lives in the database (backend M1-new, designer F4).** `_agreement_floor_unmet` (or a sibling called from the same three doors: send, sign, countersign) refuses a `design_services` / `service_addendum` document that carries parts unless at least one **client-visible** schedule part in the fee set — `rate_card`, `flat`, `per_phase` (a `ceiling` is a cap, not a fee) — carries a set value. Readiness says the same sentence the room already uses. Pin probes R3 and Q5 as SQL cases.
- **R23 · One data layer (backend M2, designer DR21).** The designer portal uses the `@patina/supabase` hooks; the portal-local duplicates are deleted. Package tests stay.
- **R24 · Composition is reversible (backend M3).** The composed room offers "Return to the seven facets" (draft only, studio side) calling `discard_agreement_parts`; the seven-facet room's `agreement_composed` notice names that act. Opening the room still materializes on first open under the flag — that stays.
- **R25 · The homeowner reads parts only (client F-1, F-2).** The bundle emits `composed: boolean` (true when ≥ 1 parts row). When composed, the client body renders from parts only and never falls back to `serviceTerms`; hidden money parts are simply absent from the page, and R22 guarantees a visible fee is present before send.
- **R26 · The e2e assertion is real (client F-4, F-12).** A seed file creates one composed agreement for the test studio; build-sheet §6.6's assertion runs unconditionally against it.
- **R27 · One copy constant, both surfaces (client F-5, F3).** Designer preview and client body read the same "Not yet set" constant from `packages/types` (`AGREEMENT_PART_COPY`), and the designer's live preview renders through the same body component contract as the client, so the first composed agreement cannot drift.
- **R28 · Nothing the designer did not type prints as a term (client F-6).** `materialize_standard_parts` seeds the deposit part from the existing terms row only when that row carries a designer-set value; otherwise the part is unset and prints "Not yet set". The same rule for every seeded money part.
- **R29 · N1 gets its test (designer).** The two-click duplicate-variant path is covered by a jest case proving readiness blocks and Save is disabled.
- **R28 amended (re-gate 2, F1).** A billing cadence saved from the seven-facet room counts as chosen: the room shows the cadence select with Monthly preselected and the designer saves it, exactly as today's shipped agreement does; the composed room shows the cadence part the same way. `materialize_standard_parts` seeds cadence from the terms row. No code change.
- **Re-gate 2 minors fixed before the walk:** F2 — an unset deposit part renders nothing on the homeowner's page (R21), not "Recorded with your agreement."; F6 — `REVOKE TRUNCATE ON proposal_service_terms, proposal_service_rates FROM authenticated` (regenerate the ACL seed). F3/F4/F5/F7 ride as advisories to the main backlog (two body implementations to unify in Wave 2; the canned Services paragraph is today's default sentence and stays; six commercial suites in KNOWN_FAILURES predate this wave).
- Recorded hazards, no code change this wave: `pnpm db:generate` truncates `database.types.ts` to 0 bytes when `SUPABASE_DB_URL` is unset (shell redirect opens the target first) — always export it; `run-sql-tests.sh` needs the sandbox override; the client Playwright config does not pin the flag override in `webServer.env` and reuses an existing server — run e2e only against a server started with the override.

## R30 · The origin agreement reaches the homeowner (2026-09-07, hotfix before Wave 2)

Fact (Sonnet investigation, read-only on Strata): a design-services agreement is `project_id NULL` by design until countersign creates the project (00331, 00566 "ORIGIN"), but the 2026-09-04 client-page cutover made the homeowner's front door project-scoped: with zero projects `page.tsx:65-73` renders `LetterboxDoor` (invoices only) or the empty state, the retired `/proposals/[id]` routes 308 to `/?proposal=<id>#door`, and `resolveHouseForInstrument` (`active-project.ts:102`) drops the `?proposal=` param when the household has no project. Prod today: one `sent` origin agreement (`f72d2912…`, sent 2026-09-04) for a homeowner with a profile and no projects, unreachable.

Ruling: hotfix on the client portal, its own lane, before Wave 2 — the household door with zero projects reads pending `design_services` (and `service_addendum` is out of scope: addenda always bind to a project) proposals through `get_client_commercial_document_bundle` and renders the agreement at `#door` exactly as a project-bound one; `resolveHouseForInstrument` keeps `?proposal=` when the proposal is an origin agreement addressed to this household; the retired-route redirect keeps working. No new RPC unless the existing bundle refuses project-less reads (then a minimal widening in a new migration). Tests: jest for the zero-project door with a pending origin agreement; e2e touchpoint. Deploy: client portal only (plus the migration if minted).

**R30 amendments (2026-09-07, review round 2).** The kept record's date at `client_signed` reads from the client's own signature row (`commercial_document_signatures.signed_at`, party client), never from `proposals.signed_at` (written only at countersign); the jest fixture must mirror what the RPC actually writes. The studio-invoice front door renders beside the origin agreement, never blank behind it. **Carried to Wave 2 (client lane):** a household that already has a house and is sent a second origin agreement — the door for every house carries a "papers without a house" leaf listing pending origin agreements addressed to the household, so a project-less paper is reachable from any house. The `plateAsked` hold-gate fix gets its own test in Wave 2; the e2e harness cleanup of throwaway households is a main-backlog item.

## Wave 2 close-out rulings (2026-09-07, after integration)

- **R31 · Never drop a hardened arity; make the generator per-function.** 00577 keeps the four-argument `sign_design_services_agreement_with_trusted_ip` (and any other widened function's old arity) as a thin wrapper that delegates to the new arity with the defaulted argument, with its 00511 grants intact — so the generated `seed/00-legacy-grants.sql` replays byte-for-byte and the hardening contract test stays green. Separately, `scripts/generate-legacy-grants.py` emits ONE guarded statement per function instead of one statement per source line, so a future dropped function can never silently un-harden its neighbours; regenerate and prove the seed replays clean on `supabase db reset`. The four unexpected SQL failures must return to green (project_notes non-idempotence is main's, recorded).
- **R32 · The Library belongs to the agreement's studio (backend B1, designer R3-4).** `save_agreement_as_template`, `materialize_agreement_template` and the Contract Room's Library read resolve the studio the AGREEMENT sits in, the way 00566 resolves the authority studio (ordered over the lead's studios, then an EXISTS membership check for the actor) — never a count of the actor's studios, never an arbitrary studio. `save_board_as_template`'s argument-plus-EXISTS shape is the precedent. Tests run as the local seed's two-studio designer with the auto-provision trigger ON.
- **R33 · Only client-visible fee parts project (backend B2).** A `schedule` part with `client_visible = false` never projects into `proposal_service_terms` or the authority; readiness names it ("This fee is hidden from your client, so it cannot bill."); the R22 floor already counts only visible fee parts. SQL test: a hidden flat fee sends nothing to terms and the executed authority carries no fee_basis.
- **R34 · The addendum's why reaches the homeowner (designer R3-3).** The `why` on an addendum is client-visible and renders beside the change on the door and the keepsake, as the copy already promises; the approvals program's `why` (00569) is the precedent.
- **R35 · The picker filters by kind, not class (designer R3-5).** On a `design_services` document every seeded and studio template whose class maps to `document_kind = 'design_services'` (design_services, consultation, furnishings_services) is selectable; `design_build` alone stays out until Wave 3.
- **R36 · The record's consent sentence comes from the signature row (client C3-1)** — deliver §5.4 as written; and the per-phase `sent` fixture the e2e drives is seeded in the same seed file that carries the composed agreement (client C3-2), so the touchpoint runs unconditionally.
- **R37 · Deploy notes accepted.** `retainer_credit_rule NOT NULL DEFAULT 'credited'` backfilling executed authorities is correct — credited is today's implicit rule; the banner says so. `patina.deposit` is one key, one kind (schedule/procurement) in every seeded template (R19). `copy_agreement_parts_from_authority` gets its SQL test. The keepsake's SQL renderer matches `agreement-parts-body.tsx` (attachments as trailing lettered leaves, the closing boundary sentence kept). `AgreementExecutionSnapshot` types only what the bundle emits. `useAgreementTemplates` escapes its filter value; both new hook modules get specs.

## Wave 2 ship rulings (2026-09-07, after walk round 3)

- **R38 · No ruling ids in the studio's face (walk W2R2-09).** The chip reads "record only" — never "(R9)"; the rulings file is where ids live. Applied before the merge as a one-line copy fix with its test.
- **R39 · Hiding a part gets an act in Wave 3 (W2R2-07).** R33 stands as defence-in-depth now; the designer lane of Wave 3 adds the "hidden from your client" toggle on a part, with readiness copy already in place. Build-sheet §10's rollback line is corrected to "flag off reverts every studio surface; a composed agreement already sent keeps its parts for the homeowner" (W2R2-03).
- Carried to the main backlog: W2R2-02 (a project-bound, client-signed design-services agreement has no countersign route — pre-existing shape, seed-only today), the aged-oak 4.48:1 label contrast, the checkmark glyph in Account → Studio's checklist, the Account PARTS shelf count.

## Wave 3 close-out rulings (2026-09-07, after the lane merges; integration steward died before gates)

- **R40 · One sentence, from what she reads (backend R3-1, client C1).** Under any disclosure the consent sentence is composed from the CLIENT-VISIBLE projection of the parts (the same redacted payload the door renders), on both halves; the sentence is frozen on the signature row at sign (R36) and the record files that row's sentence. SQL and TS composers take the redacted payload as input; parity scenarios add closed-book and open-book turnkey.
- **R41 · The closed-book door reads the projection (backend R3-2, client C2).** The homeowner's design-build body reads `scheduleOfValues` and `contractSumCents` from the redacted payload — never `costLines` — and prints the contract sum where the paper names a price; the jest fixture is the production shape (redacted). The keepsake and the door say the same thing.
- **R42 · Lien waivers go through the door the backend built (backend R3-3, designer B1).** `useRecordAgreementDrawLienWaiver` calls `record_agreement_draw_lien_waiver(...)` with exactly its arguments; no direct insert; the table keeps SELECT only.
- **R43 · Closed book means studio-authored lines (backend R3-5 / RC-4).** Under `closed_book` the schedule of values is NOT derived from cost lines; the designer authors client-facing lines whose sum is the contract sum (default: one line per room or phase the pricing-basis part names, seeded from the fixture's rooms; a single "Construction" line is acceptable). Allowances stay stated at their client amount. Under `open_book` cost lines and the fee render as today. SQL test: no closed-book line equals any cost line × a constant.
- **R44 · One types file (client C4).** The integration branch carries the designer lane's superset `packages/types/src/agreement.ts` (545 lines) after confirming every Wave 1–2 export is byte-identical within it; all five lanes' consumers compile against it; `pnpm exec turbo build --filter=@patina/types --force` before any type-check.
- **R45 · Pin the search_path (backend R3-4).** The nine functions in 00578/00579 without `SET search_path` get `SET search_path TO 'public'` (or `'public','extensions'` where `net`/`vault` is used); the migration rule is not optional.
- **R46 · The sub lane executes (sub R3-1…R3-4).** The client type-check is green on the integration branch (the client lane's KIND_LABEL/MakingGateKind widening is merged); the sub e2e's fourth case runs against the reset stack with its setup fixed; a spent token cannot re-open — the walk script step is corrected to say so; the `/trade` route joins the service worker's NetworkOnly list; the coverage floor is enforced for the sub lane's files.
- **R47 · A question from the origin door files to the agreement's studio thread (client C6)** — keyed by proposal when no project exists; never another project's thread. E2E-2 (client C5) must execute in the re-gate.

## Wave 3 walk rulings (2026-09-08, after walk round 2)

- **R48 · The price is never hidden (W3R2-01).** A `pricing_basis` part is always client-visible: the composer has no hide act on it, `upsert_agreement_parts` refuses `client_visible = false` for `pricing_basis` (and for `draws`), readiness counts `pricing_basis` in the fee set for a `design_build` document, and `_agreement_fee_unnamed` reads it with the same client-visible filter as every other fee. SQL test on the proposal the walker left (`de3970a0…`): send refuses.
- **R49 · No double count gates the send (W3R2-04).** The supervision-fee-vs-sub-markup rule is a readiness blocker named in the rail, evaluated over the pricing-basis part and every sub-disclosure clause, and it disables Send until resolved.
- **R50 · The receipt survives (W3R2-02).** The post-signature region (receipt, deposit offer, pay link) renders from bundle state on every load; the offer is re-derived from the deposit invoice row, not from client memory.
- **R51 · The studio previews the paper (W3R2-03).** Under closed book the studio's live preview renders the redacted projection exactly as the door does (R41); the studio's own cost view lives in the editor, not the preview.
- **R52 · The deposit invoice joins the project (W3R2-12).** At countersign, when the project is created, the origin deposit invoice's `project_id` is set to it (same transaction; the immutability guard admits this one transition under the activation GUC, mirroring `proposals.project_id`).
- **Minors fixed with this round:** W3R2-05 send sheet copy for a turnkey document (no "design services", no furnishings deposit line); W3R2-06 turnkey room chrome and the proposal title flowing to the keepsake footer; W3R2-09 a failed dispatch never prints an environment-variable name (say "The message could not be sent. Try again."); W3R2-11 the origin-door question thread is keyed by proposal (R47 as written); W3R2-14 duplicate placeholder; W3R2-15 the picker summary does not name Flow-down while it ships disabled; W3R2-17 the send sheet never prints a warning and "every facet is present" together. **Correction (walk round 3, W3R3-06):** W3R2-07 (contradictory chips on a client-signed document) and W3R2-16 (a revoked trade link returns 200 with a 404 page) were NOT fixed in that round; both ride to the main backlog.
- **Pre-merge fix (walk round 3, W3R3-02):** the till never prints a provider error key in the homeowner's face — "The payment page could not open. Try again." — with its test. Same class as W3R2-09.
- Carried to the main backlog: W3R3-01 (R49's invalid-field state per build-sheet §4.3), W3R3-03 (the rail sentence when a fee is hidden), W3R3-04 (with all flags off a design_build draft opens the seven-facet room — the flag-off route for a document of a kind the flag introduced), W3R3-05 (countersign names the project from `proposals.title` — a design-build house is titled after its agreement; Wave 4 names the house), W3R3-07 (the paid deposit is absent from the project's Money region), W3R2-10 (a route back to the executed agreement's text for the homeowner — the keepsake exists; a link from the house is Wave 4), the aged-oak contrast and viewport meta, the checklist glyph, build-sheet §8 drift (documented in the wave report).

## Program rules for this build

- Waves ship in order, each to production, each behind a fail-closed PostHog flag: `agreement-parts` (W1), `agreement-library` (W2), `design-build` (W3). Kody creates the flags; features are dark until then (verify `/flags` with a real-browser UA before enabling — memory).
- Flag-off must be byte-identical for the designer and client surfaces in W1 (the seven-facet room renders exactly as today).
- No wave reopens `commercial_document_signatures`' two-party constraint. No wave writes business tables outside definer RPCs.
- Every migration: banner, idempotent, RLS in the same file, explicit grants, `search_path` pinned, extension functions schema-qualified, `generate-legacy-grants.py` re-run when grants change, types regenerated.
- Numbers are provisional until merge; re-check the tip before every integration.
- Gates per patina-verification; full designer-portal jest on a clean checkout is the gate before merge (memory lesson).
- Nothing deploys without the chain: migrations → edge functions (every importer of a touched `_shared` file) → portals via `./infra/deploy-portal.sh <name>` from the main checkout → `wrangler deployments list` bottom row + served-chunk grep + behavior probe.
