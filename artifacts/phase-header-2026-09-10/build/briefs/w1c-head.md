# W1c — The head: name at 34, the subject line, the stage line off the pre-work glass, the silent region head

Worktree: `/Users/kody/Code/patina-merged/.codex/worktrees/agent-standing-head-build-head` (branch `build/standing-head-2026-09-10-head`, branched from W1a's commit, deps installed). Portal: `apps/designer-portal`. Deliver exactly what this brief asks — no unrequested features, refactors, or abstractions. Comments only for constraints the code cannot show. Use absolute paths; never `cd`. Do not run git. Another agent (W1b) is editing, in a separate worktree, the guide, the band, the standing sheet, the ladder derivation and the discovery section — do not touch those files; your `page.tsx` edits stay inside the line ranges named here so the merge is clean.

Read first: `artifacts/phase-header-2026-09-10/build/PROGRAM.md`; the panel synthesis §2 "Direction 2 — The Band" and rulings R1/R3/R4/R6 (`git -C /Users/kody/Code/patina-merged show origin/design/phase-header-2026-09-10:artifacts/phase-header-2026-09-10/panel/synthesis.md` and `…:artifacts/phase-header-2026-09-10/rulings.md`).

## Files you own
`src/components/document/doc-letterhead.tsx`, `src/components/document/letterhead-vitals.tsx` (the `<h1>` classes only), `src/components/document/workflow/section-stage-line.tsx`, `src/components/document/section-stage-line-mount.tsx`, `src/components/document/prework/prework-region.tsx`, `src/components/document/region/region-head.tsx`, `src/lib/document/desk-derivation.ts` (add one field), new `src/components/document/letterhead-subject.tsx`, `packages/supabase/src/hooks/use-engagement-subject.ts` (new) + `packages/supabase/src/hooks/index.ts` (one export line), and in `src/app/(document)/doc/[id]/page.tsx` ONLY: `vitalsFor` (~323-352), `stageStripInScope` (~2333), the letterhead mount (~2616-2640), the free-standing stage mount (~2774-2781), the `PreworkRegion` mounts for brief/discovery/direction (~2805-2836), and the hosted stage mount (~2869-2886). Plus tests: `doc-letterhead.test.tsx`, `letterhead-vitals.test.tsx`, `workflow/__tests__/section-stage-line.test.tsx`, `__tests__/section-stage-line-mount.test.tsx`, `prework/prework-region.test.tsx`, `region/__tests__/region-head.test.tsx`, `src/lib/document/__tests__/section-stage-line.test.ts`, the listed lines of `page.test.tsx`, and the e2e specs named below.

## Changes

**1. R6 — the name at 34** (`doc-letterhead.tsx:78-80`, `letterhead-vitals.tsx:499`).
`<h1>` classes: `font-heading text-[34px] font-medium leading-[1.08] text-[var(--text-primary)] min-w-0 break-words` — no `tracking-[-0.015em]`, no `min-[1180px]:text-[40px]`. Update `doc-letterhead.test.tsx:39-62`. In `e2e/document/lens-band-height.spec.ts`: `titleLines()` (~400-412) divides by `32 × 1.08` — change to `34 × 1.08`; the unclipped-h1 check (~460-504) computes `lines === 1` at `40 × 1.08` — change to 34; leave `LETTERHEAD_MAX_1440`, `FIRST_HEAD_MAX_*` as they are unless the subject line (below) forces a change, in which case raise `VITALS_MAX_HEIGHT` (418) from 24 to 44 and say so.

**2. R4 — the subject line, printed and editable** (`desk-derivation.ts:50-100`, `use-engagement-subject.ts`, `letterhead-subject.tsx`, `doc-letterhead.tsx:84-99`, `page.tsx vitalsFor`).
- `DocumentStateRow` gains `subject: string | null` (the view column landed in W1a; `database.types.ts` in this worktree already has it).
- Hook, in `packages/supabase/src/hooks/use-engagement-subject.ts`, exported from `hooks/index.ts`:
  ```ts
  export function useUpdateEngagementSubject(): UseMutationResult<void, Error, { kind: 'project'|'proposal'|'lead'|'relationship'; id: string; subject: string | null }>
  ```
  Writes `{ subject }` to `projects` / `proposals` / `leads` / `designer_clients` by kind, `.eq('id', id)`, trims and nulls blank. On success invalidates `['document-state']`, `['desk-engagements']`, `['desk']`, and the kind's list key (`['projects']`, `['proposals']`, `['leads']`, `['designer-clients']` — check the actual keys used in `use-projects.ts`, `use-proposals.ts`, `use-leads.ts`, and whatever hook lists designer_clients). Follow the shape of `useUpdateProject` in `use-projects.ts:80-118`. Add a vitest file next to it covering the four table routes and blank → null.
- `letterhead-subject.tsx`: `<LetterheadSubject kind id subject assembled />`. Prints `subject ?? assembled` as a `<p data-letterhead-subject>` at `text-[15px] leading-[1.35] text-[var(--text-muted)]`, wrapping, no ellipsis. Click (or Enter/Space on the focusable `<p role="button" aria-label="Edit the subject line">`) turns it into an `<input>` pre-filled with `subject ?? ''` (never with the assembled line), placeholder = the assembled line or `Add a subject line`; blur or Enter commits via the hook (unchanged value does not save; blank saves null); Esc cancels. Follow `LetterheadTitle` in `letterhead-vitals.tsx:453-490` for the blur-save shape and its `SaveState` feedback. When `subject` is null and `assembled` is null, print nothing at rest but keep the edit affordance reachable as a tertiary act `Add a subject line` (DM Mono 13, `DocumentAction variant="tertiary"`) — P5: no empty line.
- `doc-letterhead.tsx`: new prop `subject?: ReactNode` rendered in row 2 left, above `vitals`. The static `vitals` `<p data-letterhead-vitals>` (~90-95) loses `overflow-hidden text-ellipsis whitespace-nowrap` and goes `text-[11px]` → `text-[15px]`; it wraps. Update `doc-letterhead.test.tsx:132-152` (it asserts the ellipsis classes — invert the assertion).
- `page.tsx`: `vitalsFor` — the `lead` branch returns `''` and the default (relationship/discovery) branch returns `''` (the name is the title; "In discovery" is the band's job). Project and proposal branches unchanged. Add `subjectFor(row, discovery)` next to it: relationship/proposal → from the discovery row (`page.tsx` already runs `useDiscovery` at ~932; read `read?.row?.project_type`, `project_type_custom`, `rooms`): `{project type label} · {N} room(s)` using the label vocabulary in `src/lib/document/discovery-readiness.ts` or `discovery-section.tsx` (grep `full_house`/`single_piece` for the display labels; `custom` → `project_type_custom`); omit the rooms clause when none are named; null when neither exists. project → null (the project vitals already carry phase · target · money). lead → null. Mount `<LetterheadSubject kind={row.engagement_kind} id={row.engagement_id} subject={row.subject} assembled={subjectFor(...)}/>` via the new `subject` prop at ~2616-2640. Never write the assembled line anywhere.

**3. R1 — the stage line leaves the pre-work glass** (`page.tsx:2333, 2774-2781, 2869-2886`, `section-stage-line-mount.tsx`).
- `stageStripInScope` is deleted. The free-standing `<SectionStageLineMount>` mounts only when `spreadSection` is `project`, `install` or `care` (define `const stageOnGlass = spreadSection === 'project' || spreadSection === 'install' || spreadSection === 'care'`). The hosted mount inside the `scope` region (~2869-2886) is deleted. `deriveSectionWorkflowStageDocument` and `deriveSectionStageLine` stay (the rail's `scopeRegister` still reads `prework.stageLine` at ~1842-1853 — leave that block alone; it is W1b's range only if they need it, and they do not).
- Tests: `page.test.tsx:1504-1519` (containment) and `:2362-2382` (exactly one per spread; inside scope on proposal) become: zero `[data-section-stage-line]` on brief/discovery/direction/proposal spreads; exactly one, inside `[data-active-section]`, on a project spread. `page.test.tsx:2438-2439` (`statusOf('scope')` contains `Core · stage 03`) — this is the rail/ladder register and is unchanged; leave it. `section-stage-line.test.ts:510` still passes (the mount is still in the source). `__tests__/section-stage-line-mount.test.tsx` unchanged.
- E2E: `e2e/document/prework-regions.spec.ts:293-324` — the "element after the band is not the stage line" assertion still holds (stronger now); the `exactly one [data-index-region="scope"] [data-section-stage-line]` assertion (~320-324) becomes `toHaveCount(0)`; `:127-138` scope status `Core · stage 03` is the ladder — leave. `e2e/document/workflow-stage-responsive.spec.ts:36-49` — read which seed document it opens; if it is a pre-work document, change the `attached` expectation to `toHaveCount(0)` and add a project-document case if the seed has one (the wp3 spec at `e2e/wp3-screenshots.spec.ts:72-79` shows which project doc carries `Design Development`); if you cannot identify one, leave the spec and report it.

**4. D4 — heading order** (`section-stage-line.tsx:66-70`). The sr-only `<h3>Workflow stage</h3>` becomes `<h2>`. Update `workflow/__tests__/section-stage-line.test.tsx` cases that query `h3`.

**5. R3 — the silent region head** (`prework-region.tsx`, `page.tsx:2805-2836`).
`PreworkRegion` gains `silent?: boolean`. When silent: `RegionRule` renders; `RegionHead` renders with the `<h2>` visually hidden (`sr-only` on the heading, keep `id` and `tabIndex={-1}` so rail jumps still land), no eyebrow, no status `<p>`, no exceptions line, and the body renders as today. Pass `silent` for the `brief`, `discovery` and `direction` mounts in page.tsx. The `discovery` mount's `eyebrow` prop (from `onEyebrow`) is no longer printed; leave the prop wiring in place (W1b keeps reporting it). The proposal-spread regions (proposal, scope, vision, investment) are unchanged. `RegionHead` needs a `silent` prop (or `hideVisual`) to do this — add it there; the dev guards (`allowNoActs`) still apply. Update `prework-region.test.tsx` (add silent cases: h2 present with sr-only, no `[data-region-head] p`) and `e2e/document/prework-regions.spec.ts` `statusOf()` cases for `brief`/`discovery`/`direction` if any assert a status there (`EMPTY_ROW_SENTENCES` at :54 — the sentences are still allowed on vision/proposal/investment).

**6. D2 — the focus ring** (`region-head.tsx:185-191`). Replace `outline-none` on the `<h2>` with the exact `focus-visible:` declaration `doc-letterhead.tsx:61` uses. Update `region/__tests__/region-head.test.tsx:53-69` if it asserts `outline-none`.

## Gate
```
pnpm --dir /Users/kody/Code/patina-merged/.codex/worktrees/agent-standing-head-build-head --filter @patina/designer-portal type-check
pnpm --dir /Users/kody/Code/patina-merged/.codex/worktrees/agent-standing-head-build-head --filter @patina/supabase type-check
pnpm --dir /Users/kody/Code/patina-merged/.codex/worktrees/agent-standing-head-build-head --filter @patina/supabase test -- use-engagement-subject
pnpm --dir /Users/kody/Code/patina-merged/.codex/worktrees/agent-standing-head-build-head --filter @patina/designer-portal test -- src/components/document/doc-letterhead src/components/document/letterhead src/components/document/workflow src/components/document/__tests__/section-stage-line-mount src/components/document/prework src/components/document/region "src/app/(document)/doc/\[id\]/page.test.tsx"
```
All green. `page.test.tsx` cases that belong to W1b (guide sentences, `Begin the direction`) may fail in this worktree because W1b's change is not here — list them by name rather than editing them.

Report: `git -C <worktree> diff --stat` (informational — do not stage), the type-check tails, the jest summary per suite, which e2e files you edited and how, and anything you could not do.
