# The Standing Head — three directions for the top of every spread

UI/UX panel proposal (2026-09-10) on the block that opens every phase of the Document — the stack that
today says "where this stands" six times before it says what the job is. Kody's ask: *"It should call into
clarity what the project is, where it stands and where to go next."*

**Deliverable:** `the-standing-head.html` — three live directions (The Masthead · The Band · The Standing
Paragraph), each rendered at Discovery, Direction and Project, desktop and 390, light and dark.

**panel/** — the shared brief, five lens memos (information, typography, interaction, designer, critic),
and the synthesis the deck is built from.

**review/** — the adversarial review (committed) and render shots (not committed).

**source/** — `check-prose.mjs` (word-budget gate) and `render.mjs` (Playwright shots; run outside the sandbox).

No product code changes. Rulings R1–R6 taken 10 September 2026 — see rulings.md. Direction: The Band. Defects D1–D7 fold into the build.
