# Lane D — round 2 fixes (hour-tracking, W4, branch `hour-tracking/edge`)

Worktree: `/Users/kody/Code/patina-merged/.codex/worktrees/agent-edge`
Branch: `hour-tracking/edge`, `22b24b989` → **`4d72d8590`** (pushed)
Skill loaded: `patina-edge-functions`

Gate re-run: `deno test --allow-all --config supabase/functions/deno.json supabase/functions/time-nudges/` → **16 passed, 0 failed**. `deno check` on `index.ts` and `logic.ts` → clean. No `deno.lock` left at repo root. Also ran, as corroborating (not substitute) evidence: `pnpm --filter @patina/designer-portal test -- post-derivation` → **60 passed, 0 failed**, and `npx eslint` (from `apps/designer-portal/`) on all four touched designer-portal/edge-function files → exit 0. The push's own pre-push "affected verification" gate ran the same 16 deno tests (green) plus a full `type-check`/`lint` sweep that reported "advisory failures" — those are the pre-existing `Cannot find module '@patina/api-routes'` errors D-R2-08 already found and attributed to this worktree never building workspace dists; none name `post-derivation`, `document-events`, or `time-nudges`. Push succeeded (`22b24b989..4d72d8590`).

## Findings applied

**D-R2-01 (major/high) — fixed by narrowing, not by confirming the Vault literal.** I have no access to Strata's `vault.decrypted_secrets` from this worktree/session, so I could not confirm the literal `invoke_edge_function` (00258) sends satisfies any of `isServiceRoleCaller`'s three arms — exactly the pre-deploy confirmation the finding asks for first. Took the finding's own stated fallback: narrowed the gate in `index.ts`'s `Deno.serve` handler to bind **only** the `weekly_unlogged` arm (`resolveNudgeRule(body)` is now read before the gate, and the gate condition is `rule === "weekly_unlogged" && !isServiceRoleCaller(...)`). `running_timer` is now ungated, matching its cron peers (decision-reminders, field-daily, morning-brief) exactly as they already stand — none of them carry an in-code caller check. Updated the header comments in `index.ts` and `logic.ts`, `supabase/config.toml`'s `[functions.time-nudges]` comment, and `00614`'s SQL banner (which previously claimed the service-role check ran "before either rule runs") so no file still makes the now-false both-arms claim. **Did not** touch §11 step 8's ship-chain probe language in `plan-v2.md` — that is a plan-document edit outside this branch/worktree (same posture as D-R1-12 in round 1); restating here for the orchestrator: the ship-chain probe after `supabase functions deploy time-nudges` must read `net._http_response`'s status for the time-nudges POST (or the new `job_runs` row from D-R2-06 below), not only `SELECT notification_log`, since an empty result from either query is now ambiguous between "nothing to nudge" and "the request never reached the function" only for the gated arm — `running_timer`'s own `job_runs` row is the fix for its half.

**D-R2-02 (medium/high) — fixed.** `apps/designer-portal/src/lib/analytics/document-events.ts`: added `· time_export_taken — scope, row_count, period (W5)` to the canonical event-name doc comment. (The finding's prose cites "§7"; the actual `plan-v2.md:723-725` text sits under §6/W5's "PostHog events (lane D emits, lane B calls)" heading — tagged `(W5)` to match the plan's own section, not the finding's prose.)

**D-R2-03 (medium/high) — NOT resolved; this is a product ruling, not a lane-D code decision.** No code change. HT-34's ruled text ("no push, no email") is genuinely silent on whether the Record row itself should read as unread, and the finding is explicit that this needs an orchestrator ruling, not a guess — a second guess in either direction is exactly what got round 1 flagged. Restating the two branches for the ruling: (a) drop `read_at` from both `buildRunningTimerRecord`/`buildWeeklyUnloggedRecord` so the row reads unread (a badge/unread-count nudge, contradicting "no push" more than "no badge" does) — or (b) keep the stamp as shipped and record that "no badge" was meant to include the unread count too. **Owed to the orchestrator.**

**D-R2-04 (medium/high) — confirmed as described; nothing in lane D to fix, stated per the finding's own ask.** Verified again: `grep -rn "get('sheet')|get(\"sheet\")|searchParams.sheet|sheet=" apps/designer-portal/src` still returns only `post-derivation.ts`'s two lines plus the unrelated `<PlanPreview sheet={…}>` prop — no route/layout consumes `?sheet=`. **W2 dependency, stated for the ship note and for W2's Done-when**: a project-bearing running-timer nudge (`/doc/{id}?sheet=hours`) is inert until W2 lands the Hours-book doorway that reads `?sheet=hours`; `/desk?book=hours` (the project-less case) already works today. W2's Done-when should assert the round trip from an actual `buildRunningTimerRecord` href, not a hand-written URL.

**D-R2-05 (medium/high) — fixed as the revert option.** Reverted the undisclosed reformat for the two files the finding named as unowned: `git checkout e1ff0560e -- apps/designer-portal/src/lib/document/post-derivation.ts apps/designer-portal/src/lib/document/__tests__/post-derivation.test.ts`, restoring both to their pre-`22b24b989` content (single-quote style, matching the rest of `lib/document/`). Verified the revert drops only cosmetic churn: `git diff e1ff0560e..22b24b989` for these two files is quote-swaps plus reflowed call sites (confirmed by inspection — trailing commas / multi-line call wraps, no token added or removed); re-ran the jest suite for both files post-revert (**60/60 passed**, including the two D-R1-07 `sheet`-query tests), confirming no functional hunk was lost. Left the reformat in place for `time-nudges/index.ts`, `logic.ts`, `index.test.ts` — those are lane D's own files (already double-quoted, consistent with `supabase/functions` convention), not the "files no lane owns" the finding flagged. Did **not** add `post-derivation.ts`/its test to plan-v2 §11's ownership table — that's a plan-document edit for the orchestrator, per the finding's second alternative; flagging here so it isn't dropped: whichever lane touches these files next inherits lane D's one now-reverted commit, not an open question.

**D-R2-06 (low/high) — fixed.** `index.ts`: `Deno.serve` now opens one `public.job_runs` row (`job_name: "time-nudges"`, `status: "running"`, `detail: {rule}`) before calling `runTimeNudges`, and updates it to `succeeded` (with the `RunResult` as `detail`) or `failed` (with the error detail) afterward — the exact idiom `morning-brief/index.ts` already uses against the same table (00300). No RLS/grant change needed: `job_runs`' own comment states writes go through `service_role`, which bypasses RLS, and this function already creates its Postgres client with `SUPABASE_SERVICE_ROLE_KEY` (same as morning-brief's proven-working client). This is also the cheap half of D-R2-01's mitigation named in that finding: `SELECT * FROM job_runs WHERE job_name = 'time-nudges' ORDER BY started_at DESC LIMIT 1` now distinguishes "the sweep ran and found nothing" from "the request never reached the function" for both arms, not just the ungated one.

**D-R2-07 (low/medium) — fixed.** `index.ts`'s `insertRecord`: the `23505` swallow now requires the error `message` to contain one of the two named idempotency index names (`uniq_notification_log_time_entry_running_long`, `uniq_notification_log_time_weekly_unlogged`) — matching Postgres's own `duplicate key value violates unique constraint "<name>"` wording — instead of trusting the bare code. An unrecognized `23505` (some future unrelated unique constraint on `notification_log`) now `console.warn`s and is re-thrown rather than silently swallowed and counted as a successful record. Did not change `runRunningTimerSweep`'s `recorded` counter (the finding's noted second-order over-report on a real race) — the finding names it as a minor consequence of the primary bug, not something it asks fixed, and the "exactly one row" guarantee it doesn't touch is the one that matters.

**D-R2-08 (low/high) — no action; the finding's own fix names the integration owner, not lane D.** Confirmed again from this worktree: `pnpm --filter @patina/designer-portal type-check` still reports the same pre-existing `Cannot find module '@patina/api-routes'` / `'@patina/types/media'` errors (workspace dists never built here), and — new this round — the branch's own pre-push hook ran the same command and reported "advisory failures" for the identical reason, not blocking the push. None of the errors, in either run, name `post-derivation`, `document-events`, or any file this round touched. Ran `npx eslint` (the gate that does work here) on all four touched files → exit 0.

**D-R2-09 (informational/high) — no action, as the finding itself concludes.** `isServiceRoleCaller` + its three helpers stay duplicated verbatim in `time-nudges/logic.ts` and `client-invite/lib.ts`. Named here again per the finding's own reasoning: if D-R2-01's Vault-shape question ever forces a change to the claim checks, both copies need it, and this is where a future reader finds both.

**D-R2-10 (informational/high) — no action, as the finding itself concludes.** Re-confirmed this round: `hour-tracking/edge` still shares zero files with `origin/hour-tracking/integration`'s W0 commits, and this round's diff touches nothing W0 touched either. Not merged or tested against W0 from this worktree — recorded so "the merge is clean" is not read as "the merge is tested," per the finding's own framing.

## Files changed this round

- `supabase/functions/time-nudges/index.ts` — gate narrowed to the `weekly_unlogged` arm only (D-R2-01); `job_runs` bookkeeping added (D-R2-06); `insertRecord`'s `23505` swallow now matches index names, warns on an unrecognized one (D-R2-07); corrected header comment.
- `supabase/functions/time-nudges/logic.ts` — corrected header + caller-verification section comments to describe the narrowed gate (D-R2-01).
- `supabase/migrations/00614_time_nudges_cron.sql` — corrected banner (no longer claims the service-role check gates both rules).
- `supabase/config.toml` — corrected `[functions.time-nudges]` comment.
- `apps/designer-portal/src/lib/analytics/document-events.ts` — added the missing `time_export_taken` canonical event name (D-R2-02).
- `apps/designer-portal/src/lib/document/post-derivation.ts`, `apps/designer-portal/src/lib/document/__tests__/post-derivation.test.ts` — reverted the undisclosed cosmetic reformat, restored to `e1ff0560e`'s content (D-R2-05).

## Not done (owed elsewhere, stated rather than silently skipped)

- **D-R2-01**: pre-deploy confirmation of the Vault literal's shape on Strata (`SELECT left(decrypted_secret,10), length(decrypted_secret) FROM vault.decrypted_secrets WHERE name='app.settings.service_role_key'`) — needs prod DB access this session doesn't have from the edge worktree. The narrowed gate ships regardless of the outcome; confirming it would let the gate be widened back to `running_timer` later if desired.
- **D-R2-01 / §11 step 8**: `plan-v2.md`'s ship-chain probe language should be corrected to read `job_runs`/`net._http_response` status, not only `SELECT notification_log` — a plan-document edit, left to the orchestrator (same posture as D-R1-12).
- **D-R2-03**: HT-34's `read_at`-at-write-time ambiguity — an orchestrator ruling, not a lane-D code decision. Both branches are named above.
- **D-R2-04**: the `?sheet=hours` round trip is inert until W2's Hours-book doorway lands — a W2 Done-when dependency, not a lane-D fix.
- **D-R2-05**: adding `post-derivation.ts` + its test to plan-v2 §11's ownership table (if the orchestrator prefers "accept and own" over "revert," which is what round 2 did instead) — a plan-document edit.
- **D-R2-08**: `pnpm --filter @patina/designer-portal type-check` clean run — needs the integration branch's built workspace dists, per the finding's own fix.
