# Lane D — round 1 fixes (hour-tracking, W4, branch `hour-tracking/edge`)

Worktree: `/Users/kody/Code/patina-merged/.codex/worktrees/agent-edge`
Branch: `hour-tracking/edge` (cut from `origin/main@2ff00bb2b`, plus `64dc2cf0c` — the original W4/lane-D commit)
Skill loaded: `patina-edge-functions`

Gate re-run: `deno test --allow-all --config supabase/functions/deno.json supabase/functions/time-nudges/` → **16 passed, 0 failed** (12 kept/extended + 4 new test cases, one tautological test removed per D-R1-08). No stray `deno.lock` appeared at repo root. Also ran, as corroborating (not substitute) evidence: `deno check` on `index.ts` (clean) and the designer-portal's real jest suite for the two touched files — `pnpm --filter @patina/designer-portal test -- post-derivation` → **60 passed, 0 failed**.

No `supabase` CLI command was run from this worktree — its `supabase/config.toml` points at the **shared** stack (`project_id = "supabase"`, ports 54321/54322/54329; confirmed `git ls-files -v` → `H`, not skip-worktree), which the task brief names as belonging to a concurrent program and off-limits. This is the same posture D-R1-13 found lane D held in round 0 (psql-only, no CLI), continued here.

## Findings applied

**D-R1-01 (major/high) — the reachability claim was false; fixed as option (b).** `supabase/functions/time-nudges/index.ts`, `logic.ts`. Added `isServiceRoleCaller` (+ `parseSecretKeys`, `isVerifiedLegacyServiceRoleJwt`, `secretEquals`) to `logic.ts`, ported in shape from `client-invite/lib.ts` (same 2026-09-09 key-rotation trap: never string-compare the bearer against one injected key alone — accept the env key, `SUPABASE_SECRET_KEYS`, or a gateway-verified legacy service-role JWT by claim). `index.ts`'s handler now 403s any request whose `Authorization` bearer is not the platform's own service-role principal, before either rule runs. Corrected the false claim in three places it was made: `index.ts`'s header comment, `logic.ts`'s header comment, and `00614`'s SQL banner (`"rule (b) cannot fire in prod no matter how the edge function's own logic reads"` → replaced with an honest three-layer account: not scheduled, opt-in defaults false, and now also gated on the service-role check). Also updated `config.toml`'s `[functions.time-nudges]` comment block, which previously asserted "it carries no in-code service-role check" — now false.

**D-R1-11 (informational) — folded into the D-R1-01 fix, not treated separately.** Gating the *whole* function (not just the `weekly_unlogged` arm) on `isServiceRoleCaller` closes rule (a)'s anon-triggerable-at-arbitrary-frequency exposure too, per the finding's own note that "one change buys both." No action beyond D-R1-01's fix.

**D-R1-02 (major/high) — fixed for both records, not just the one named.** `logic.ts`: `buildRunningTimerRecord` and `buildWeeklyUnloggedRecord` both now stamp `read_at: now.toISOString()` in `metadata`, so `useUnreadInboxCount`'s "any `in_app` row with no `read_at` is unread" rule never turns a nudge into an inbox badge. Applied to `buildWeeklyUnloggedRecord` too, even though rule (b) is dark today — the same bug would otherwise resurface the moment it's ever turned on, silently. Added test assertions for both.

**D-R1-03 (major/medium-high) — database-level idempotency added.** `supabase/migrations/00614_time_nudges_cron.sql`: added two partial `UNIQUE` expression indexes on `notification_log`, in the exact shape of the existing `idx_notification_log_project_file_event` (`00431`) precedent — `uniq_notification_log_time_entry_running_long` on `(user_id, (metadata->>'entry_id'))` and `uniq_notification_log_time_weekly_unlogged` on `(user_id, (metadata->>'week_key'))`, each scoped by `type` + `channel='in_app'`. `index.ts`'s `insertRecord` now catches a `23505` from either index and treats it as "already recorded" rather than an error — the pre-check stays as the fast path. Not independently verified against a live Postgres from this worktree (see "Not verified" below); the SQL is idempotent (`IF NOT EXISTS`) and matches a proven precedent in the same table.

**D-R1-04 (medium/high) — NOT resolved; flagged for the orchestrator, as the finding itself asks.** Type regeneration for `00614`'s `weekly_hours_reminder_opt_in` column (and, now, the two new indexes) requires `pnpm db:generate` run against a Postgres that has `00614` applied. This worktree's branch was cut directly from `origin/main@2ff00bb2b` and carries only `00614` — it does not have `hour-tracking/integration`'s or `hour-tracking/server`'s migrations (00595–00613), so `database.types.ts` here would reflect a schema no other branch shares. The only isolated ("patina-hours") stack this program's brief names (`agent-server`, `agent-integration`) does not yet have `00614` applied either, since it lives only on this unmerged branch. Regenerating now, from anywhere, would either hit the forbidden shared stack (this worktree's own `config.toml`) or produce a `database.types.ts` diff the integration branch will discard and regenerate anyway per §11 ("the integration owner re-runs `pnpm db:generate` once … after every merge"). Did not act unilaterally on a shared/other-worktree's Postgres. **Owed at merge time**, by whichever lane applies `00614` to the isolated stack first.

**D-R1-05 (medium/high) — the doc comment landed; the full ask is only partially mine.** `apps/designer-portal/src/lib/analytics/document-events.ts`: added the canonical hour-tracking PostHog event-name block (all 9 names — `time_entry_logged`, `time_rate_unresolved`, `time_timer_started`, `time_timer_stopped`, `time_scope_viewed`, `time_entry_adjusted`, `time_entry_deleted`, `time_autostart_disclosed`, `time_autostart_opted_out` — with their props and owning wave) as a doc comment, matching §5's exact ask ("lane D also lands the canonical event-name list as a doc comment … for lane C's posthog-ios call sites"). Did **not** add the actual emitter function bodies for `time_entry_logged`/`time_rate_unresolved` — those are W1's deliverable per §2 ("Both ship here, in the wave that fixes what they measure"), a different lane-D dispatch on a different branch/wave than this W4 fix task. Flagging per the finding's own ask: confirm with the orchestrator whether that W1 emitter-module commit already exists on `hour-tracking/server`/`w1-rate-truth` — if not, it is a separate owed item, not covered by this round-1 fix.

**D-R1-06 (informational) — no action from this worktree; confirmed as an integration-mechanics item, not lane D's.** No code change. The finding's own text agrees ("Not lane D's defect"). Noting for the record: this branch's `supabase/config.toml` diff (adding `[functions.time-nudges]`) is exactly the block the integration merge will need, on top of whatever ports the skip-worktree'd edit carries at merge time.

**D-R1-07 (low/high) — fixed generally, not just patched locally.** `apps/designer-portal/src/lib/document/post-derivation.ts`: `documentHrefFor` now reads an optional `sheet` metadata key and appends it as a query string (`/doc/{id}?sheet=hours`) when no `section` is present — a `section` anchor still wins if both are supplied. `logic.ts`'s `buildRunningTimerRecord` now sets `metadata.sheet = "hours"` alongside the existing `deep_link` (kept for the project-less case and as a plain reader convenience). This is the "carry the sheet through a key `documentHrefFor` honours" option from the finding's three alternatives — chosen over dropping `project_id` (which the finding itself notes costs "the Record's project address") because it fixes the same class of bug for any future notice type that wants a query-bearing document link, not just this one. Added a designer-portal test (`documentHrefFor carries a \`sheet\` key…`, `deriveRecordRow points a project-bearing notice at its \`sheet\`…`) plus a deno-side assertion that the built record's metadata actually carries `sheet: "hours"`.

**D-R1-08 (low/high) — the tautological test removed, not promoted.** `supabase/functions/time-nudges/index.test.ts`: deleted the "a completed entry … never reaches the port's stale-timer query surface" test (it asserted `scanned === 0` against an empty world with no completed entry ever constructed, exactly as the finding described). Left an explanatory comment in its place pointing at D-R1-09 as the place this coverage is actually owed (a live-stack probe), rather than promoting it to a false sense of coverage inside a fake-only suite.

**D-R1-09 (low/high) — not built; correctly, per the finding's own framing.** No code change. This is "owed at the ship chain" work (a live probe of the deployed function + optionally an integration test against the real stack) — out of scope for a deno-test-gated round-1 fix on an unmerged branch with no safe DB access. Restated here so it is not lost: after `supabase functions deploy time-nudges`, trigger it and `SELECT` the `notification_log` row it produces.

**D-R1-10 (low/medium) — fixed.** `index.ts`: the `catch` block still logs the full `detail` via `console.error` server-side, but the HTTP response now returns only `{ "error": "internal_error" }` — no more `${err.name}: ${err.message}` echoed to the caller.

**D-R1-11 — see D-R1-01.**

**D-R1-12 (informational) — not acted on; flagged for the plan owner, not fixed by lane D.** `plan-v2.md` §0.20 and §11 both list `00614` in their "migrations owing an ACL-seed regen" fixed lists; the finding (and a fresh `grep -nE '^\s*(GRANT|REVOKE)' supabase/migrations/00614_time_nudges_cron.sql`, still exit 1 after this round's edits) confirms `00614` carries no top-level `GRANT`/`REVOKE` and owes no `generate-legacy-grants.py` run. This is a correction to the **plan document**, which I judged is the architect/orchestrator's artifact to amend, not code under lane D's ownership — did not edit `plan-v2.md`. Restating for visibility: remove `00614` from both fixed lists; the rule is the grep, not the list (§0.20's own words).

**D-R1-13 (informational) — accepted as documented practice; no config.toml port change made.** Confirmed again: `agent-edge`'s `supabase/config.toml` is tracked (`H`), not skip-worktree'd, and points at the shared/peer stack (54321/54322/54329, `project_id="supabase"`), unlike `agent-server`/`agent-integration`'s isolated `patina-hours` stack. Did not "fix" the ports here — this file's committed content (adding `[functions.time-nudges]`) is meant to merge forward regardless of which ports it currently carries (D-R1-06 already covers the merge mechanics). No `supabase` CLI command was run in this worktree this round; all verification was `deno test`/`deno check` (no Postgres needed) plus the designer-portal's jest suite (also no Postgres needed).

## Files changed this round

- `supabase/functions/time-nudges/index.ts` — service-role gate, 23505 tolerance in `insertRecord`, error-detail no longer echoed to caller, corrected header comment.
- `supabase/functions/time-nudges/logic.ts` — `isServiceRoleCaller` + helpers (new, pure, tested), `read_at` + `sheet` on `buildRunningTimerRecord`, `read_at` on `buildWeeklyUnloggedRecord`, corrected header comments.
- `supabase/functions/time-nudges/index.test.ts` — removed 1 tautological test, added 5 new tests (service-role verification ×4, `read_at`/`sheet` assertions folded into the 2 existing record-builder tests).
- `supabase/migrations/00614_time_nudges_cron.sql` — 2 new partial unique indexes, corrected banner claim about rule (b)'s reachability.
- `supabase/config.toml` — corrected `[functions.time-nudges]` comment (no port/value changes).
- `apps/designer-portal/src/lib/document/post-derivation.ts` — `documentHrefFor` honors a `sheet` metadata key.
- `apps/designer-portal/src/lib/document/__tests__/post-derivation.test.ts` — 2 new tests for the above.
- `apps/designer-portal/src/lib/analytics/document-events.ts` — canonical hour-tracking event-name doc comment (reservation only, no emitters wired).

## Not done (owed elsewhere, stated rather than silently skipped)

- `pnpm db:generate` / `database.types.ts` for `00614` (D-R1-04) — owed at merge time, needs an isolated stack with `00614` actually applied.
- A live probe of the deployed function (D-R1-09) — owed at the ship chain, after `supabase functions deploy time-nudges`.
- `plan-v2.md` §0.20/§11 fixed-list correction (D-R1-12) — plan-document edit, left to the orchestrator.
- The W1 `time_entry_logged`/`time_rate_unresolved` emitter function bodies (distinct from D-R1-05's doc comment) — confirm ownership/status on the W1 branch.
