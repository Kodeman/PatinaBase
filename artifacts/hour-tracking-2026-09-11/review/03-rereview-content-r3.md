# Re-review — content, round 3 (adversarial)

Target: `artifacts/hour-tracking-2026-09-11/deck/src/index.html` (1523 lines, 16 sheets).
Sources checked against: `synthesis.md`, `architecture.md`, the nine memos in `panel/`,
`briefing/` (`panel-brief-common.md`, `current-state.md`, `rulings-digest.md`,
`architecture-draft.md`), `docs/design/the-document/DECISIONS.md`, and the repo itself
(read-only). Prior rounds read: `01-content.md`, `02-fix-log.md`, `02-fix-log-r2.md`,
`03-rereview-content.md`.

**Verdict: NOT clean.** 7 majors, 7 minors, 2 notes. No blockers: every ruling id in
the deck maps correctly to `DECISIONS.md` at the digest's cited line (checked D9 :20,
D10 :21, D11 :22, R4 :131, R19 :668, R20 :679, R64 :2359, R69 :2555, R75 :2640,
R77 :2648 — all match the digest's paraphrase and the deck's use of them).

---

## 1 · Findings

| ID | Sev | Conf | Location (deck line) | Finding | Fix |
|---|---|---|---|---|---|
| **R3-1** | major | high | `index.html:337-338` (sheet 02, row "Log an hour anywhere") | The cell asserts logging is **"impossible off one"** (off an open document). The deck's own sheet 03 marks "Log with nothing in hand / Designer desktop" ◐ with the quantifier **8** (`index.html:418`) and sheet 04 prices that path at **8 today → 5 in v1** (`index.html:601-605`); `synthesis.md:87` records the same 8-interaction ledger path. Off a document on the desk it is high-friction, not impossible. The quantifier was introduced by round 1's C-22 and applied verbatim in `02-fix-log.md:28`, so it is a fix-introduced defect. | Re-word to "0 taps in a document, 8 off one, impossible on the phone with nothing held and in Field beyond a just-closed visit" (the mark ◐ stays correct). |
| **R3-2** | major | high | `index.html:1264` (sheet 12, row №5) | Gate reads "Keep `fetchTimeSummary`: **3 live callers** via use-projects.ts:481". Repo-wide greps contradict the count: `fetchTimeSummary` has exactly one call site outside its own module (`apps/designer-portal/src/hooks/use-projects.ts:481`, inside `useProjectTimeTracking` at `:475`) plus one inside the very wrapper being deleted (`use-time-tracking.ts:278`) — and `useProjectTimeTracking` itself has **zero consumers** anywhere in `apps/` or `packages/` (only a comment reference at `use-time-tracking.ts:193`). Inherited from `synthesis.md:220` / `architecture.md:934` (FS-21), but the deck prints it as the gate that decides a deletion. | Restate as "one call site (`use-projects.ts:481`), itself in a hook with no consumers — re-derive before keeping", or have the orchestrator re-verify FS-21's "3". |
| **R3-3** | major | high | `index.html:1168` (sheet 10, tfoot "All" row) | The PostHog map is printed as the program's complete instrumentation plan — "W1 …, W3 …, W5 …, W6 … — HT-27" — and **omits Wave 2 entirely**. `architecture.md:338-339` gives W2 three events: `time_scope_viewed` (`scope`, `group_by`), `time_entry_adjusted` (`by_admin`), `time_entry_deleted`. Sheet 10's own W2 row names the audit row but no event. | Insert "W2 (`time_scope_viewed`, `time_entry_adjusted`, `time_entry_deleted`)" in wave order. |
| **R3-4** | major | high | `index.html:1193` and `:1199` (sheet 11 count tile + Path B) | Critical path printed as **15–25 eng-days**; `architecture.md` says **17–25** in three places (`:8`, `:835`, `:880`). The deck's figure is the arithmetically correct one (M 3–5 + L 6–10 + M 3–5 + M 3–5 = 15–25 on both W0→W1→W2→W5 and W0→W1→W3→W6, per `architecture.md:873-877`), so the defect is in the source — but as shipped the deck contradicts the plan it cites on the headline number. Raised as R2-1c in round 2 and **skipped** (`02-fix-log-r2.md:193-201`); `architecture.md` is unamended. | Orchestrator: amend `architecture.md:8,835,880` to 15–25. Deck needs no change. |
| **R3-5** | major | high | `index.html:1310` (sheet 13, risk №4) | Risk text says "Migration-number collision across **three** concurrent worktrees"; `architecture.md:894` says **four**. The deck's three matches the parallelisation table's peak (stage 4 = 3 worktrees, `index.html:1234`, `architecture.md:857`) and the "2–3" count tile; architecture contradicts its own table. Raised as R2-2c and **skipped** (`02-fix-log-r2.md:202-207`); source unamended. | Orchestrator: amend `architecture.md:894` to three. Deck needs no change. |
| **R3-6** | major | high | `index.html:689` (sheet 05, hole 2 "Line" cell) | Cites "**enum** `00084:163-164`" for the role vocabulary. In the repo, `00084_project_management_mvp.sql:164-165` is `role TEXT NOT NULL CHECK (role IN ('lead_designer','support_designer','vendor','client','bookkeeper'))` — a TEXT column with an inline CHECK, **not** an enum; `:163` is `user_id`. The only enum here is `member_role` on a different table (`00021:22`). The same deck already carries the corrected form at sheet 12 row 19, "role CHECK `00084:164-165`" (`index.html:1278`, fixed as R2-15c), so the deck now contradicts itself. Round 2 explicitly declared this instance out of scope (`02-fix-log-r2.md:84-86`). | Change to "role CHECK `00084:164-165`" to match sheet 12. |
| **R3-7** | major | med | `index.html:1267` (sheet 12, row №8) vs `:1437` (colophon "Paths") | The colophon states TS/TSX citations are "relative to `apps/designer-portal/src/`". Under that rule `use-scope-builder.ts:945-950` does not exist — the file is `packages/supabase/src/hooks/use-scope-builder.ts` (verified: `:944` `.from('proposal_change_order_terms')`, `:945-950` the upsert carrying `hourly_rate_cents`). Every other non-portal root (Swift, edge functions, migrations, docs) is declared in that same row; `packages/` is not. | Either write the path in full on sheet 12 row №8, or add "workspace packages under `packages/`" to the colophon's Paths row. |
| **R3-8** | minor | high | `index.html:357` (sheet 02, "Project" row) | Evidence cites `hours-ledger.tsx:275` for the add-row's project requirement. `addValid` is declared at `:273`; `:275` opens `batchAdd`. Round 2 corrected exactly this citation on sheet 03 (`index.html:459`, "addValid hours-ledger.tsx:273", R2-15c) and left sheet 02 behind — the deck now cites two different lines for the same fact. | `hours-ledger.tsx:273`. |
| **R3-9** | minor | high | `index.html:1002` vs `:994`, `:423`, `:450` | The same add-row JSX is cited as `hours-ledger.tsx:543-591` in one place and `:545-591` in three others. (Repo: the "Batch add" comment is `:544`, the first `<select>` `:546`, the `Add` action closes at `:591`.) | Pick one span (`545-591`) everywhere. |
| **R3-10** | minor | med | `index.html:689` (sheet 05, hole 2) | "role match `00412:2510-2546`" anchors a classifier branch on the **pre-head** body. `architecture.md:20` makes it a binding rule that every classifier citation re-anchors on `00578:2599-2805`, and sheet 12 row №21 deletes the draft's 00412 anchors for precisely this reason; the same cell's other citations are already at 00578. Verified head equivalent: `00578_design_build_kind.sql:2709-2745` (`count(DISTINCT member.role)=1` at `:2712`, the normalize-match at `:2719-2733`). The cited 00412 text does exist, so this is staleness, not falsity. | Re-anchor to `00578:2709-2745` (or cite both, head first). |
| **R3-11** | minor | med | `index.html:1314` (risk №8) vs `:1375` (HT-37) and `:1408` (sheet 15) | `head+12` is double-booked on the face of the deck: risk №8 says the HT-10 narrowing is "isolated to its own reserved migration (head+12) … and the **only** change in it, so it reverts without touching the lens", while HT-37 says that if Kody rules delete, "the INVOKER replacement lands in **head+12**". Both claims are faithful to `architecture.md` (risk 8 at `:898`; §13 row 22 at `:951`; W2's DB block reserves `head+12` for HT-10 at `:301-302`), so the conflict is the source's. | Orchestrator: give the INVOKER rollup its own number (W2's range is `head+10 … +12`, all three already assigned) or widen the range; then correct both documents. |
| **R3-12** | minor | med | `index.html:540` and `:936` | "blob pattern `qbo-export/index.ts:131-132`" / "AP precedent" — those two lines are the `CSV_HEADER` constant (`supabase/functions/qbo-export/index.ts:131` `const CSV_HEADER =`, `:132` the header string). The blob-download mechanism the rows claim as precedent is elsewhere in that file. Inherited verbatim from `synthesis.md:48,159`. | Either re-point at the response/blob lines or re-label as "CSV-header precedent". |
| **R3-13** | minor | low | `index.html:1362` (sheet 14, HT-24) | "Touches: **D10**" for "is `activity` recorded rather than defaulted". `DECISIONS.md:21` (D10) is about duration — "Timer counts, designer adjusts up or down, nothing auto-trims. <60s discards silently. Idle detection only annotates" — and says nothing about `activity`. The mapping is `synthesis.md:195`'s ("D10 (second amendment)"), so the deck is faithful; the label is still not supported by the ruling's text. | Re-label "new" (or "D10 adjacent") in both synthesis and deck. |
| **R3-14** | minor | med | `index.html:679-687` (sheet 05 holes 1–2), `:1080` (sheet 09 legend), `:1434-1435` (colophon) | Prose residue against ruling 4 ("no prose"): sheet 05's two "What it is" cells run three sentences each, the sheet 09 legend is an explanatory sentence, and the colophon's "Findings count" / "Typeset to" rows are multi-line. The colophon pair was explicitly accepted by the orchestrator and recorded as a deviation (`02-fix-log-r2.md:52-59`); round 1's compression pass covered sheet 03's annotation and risks №1/№4/№7 only — sheet 05's cells were never in scope. They are table cells, which the charge permits, so this is the weakest of the prose findings. | Compress each of sheet 05's two cells to one clause + citation, or rule them acceptable as table rows and record it. |
| **R3-15** | note | high | program-level | **No DECK CONTRACT file exists** (re-grepped `artifacts/`, `.claude/workflows/`, `~/.claude/workflows/` — third round with the same result; C-24 → R2-20c → here). What can be verified passes: 15 content sheets + colophon, `sheet-1 … sheet-16` ids, "Sheet NN / 16" labels sequential and complete, no duplicate element ids. Two residual topic gaps: the seven crux labels **i–vii** appear nowhere in the deck (`synthesis.md:65-73` is a per-crux table; the content is covered sheet-by-sheet but never labelled), and of `architecture.md:905-924`'s 14 "explicitly NOT in v1" rows, 13 now appear somewhere (sheets 04, 06 "Never", 08, 09, 14) — the only one with no row of its own is "hours on the paper's margin", which survives only as the cover epigraph and the colophon "Title from". | Orchestrator: supply the contract or rule both gaps acceptable. |
| **R3-16** | note | med | `index.html:409-495` (sheet 03, "Designer mobile" column) | The mobile ◐ marks (rows 1, 8, 9, 10) are not counted derivations in any source — `synthesis.md:89-90` covers mobile only for manual entry; they are reasonable readings of `briefing/current-state.md:95` (the mobile timer sheet carries minutes + activity at every width) and CR §3. The deck marks exactly one inference as an inference (sheet 09's legend, `index.html:1080`); the brief asked for inferences to be marked. | Add "derived from current-state §2" to the sheet 03 footnotes of those four rows, or accept. |

---

## 2 · Verification ledger — claims attacked and **not** refuted

Recorded so round 4 need not re-run them.

**Counts and headline figures**
- Cover "9 seats / 218 findings / 1 table / 30–50 eng-days, 8 waves" ✓ (`synthesis.md:3`; band table `architecture.md:873-877`: 6×M 18–30 + 2×L 12–20 = 30–50).
- Colophon findings arithmetic ✓ **independently re-derived**: numbered memo rows are critic 30, feasibility 45, leah 25, billing 12, studio-ops 16, veteran 27, mobile 17, reporting 15, web 16 = **203**, exactly the deck's "30+45+25+12+16+27+17+15+16"; feasibility carries **7** per-wave rows (`FEASIBILITY-W0 … W6`) → 210; the critic's C-series is **C1–C29** complete → 239. The disclosure that 218 is carried, not re-derived, is honest.
- Sheet 02 "Six cells. Two pass outright." ✓ (6 rows; ● on Who and Project only).
- Sheet 03 "Seventeen capabilities, five surfaces. One empty column: the admin portal." ✓ (17 rows; the admin column is the only all-○ column — the client column carries one ◐; `memo-ux-reporting.md` UX_REPORTING-3 confirms no admin-portal time code).
- Sheet 12 "Twenty-three dispositions" ✓ = `architecture.md:926-952`'s 23 rows, in order, numbering and wave assignments matching row-for-row.
- Sheet 13 "Eleven risks" ✓ = `architecture.md:887-901`, 1:1 including severities (risk №5's re-priced "low" is the R2-6c correction, and the correction is **true**: `00545:147-148` does name `project_time_entries_source_ck`, while `00198:27-29`'s activity CHECK is inline and unnamed).
- Sheet 14 "Forty questions" ✓ — **HT-1 … HT-40 all present, none duplicated, none missing**, and every "Touches" value matches `synthesis.md:172-211`; every For/against cell matches the synthesis's vote line (spot-checked all 40, incl. HT-2 "4 · 1 (LEAH keeps tier 3)", HT-4 "1 seat (CR)", HT-6 "6 · 0 (draft said W6)", HT-31 "0 · 3", HT-37 "8 delete · 1 wire").
- Sheet 10 waves: every band (W0 M, W1 L, W2 M, W3 M, W4 L, W5 M, W6 M, W7 M), every dependency (incl. "W0 — **not** W1" for W2 and "W0 (HT-33), W1, W3" for W6) and every migration range (`+1…+3`, `+4…+9`, `+10…+12`, `+13…+14`, `+15…+19`, none/`+20`, `+21…+22`, `+23…+25`, total `+1…+25`) match `architecture.md:822-831` and §§1-8. Ship gates are faithful condensations of the eight `**Ship gate.**` paragraphs. "Local head is eleven behind Strata" ✓ (`synthesis.md:12-13`, `architecture.md:19`). Bands obey the brief's S/M/L/XL definitions; "the NL parser is a later S" and the widget's XL ✓ (`architecture.md:915`, `:909`).
- Sheet 11: 30–50 / 18–28 calendar / 2–3 worktrees ✓ (`architecture.md:879-880`); stage table 1:1 with `architecture.md:852-857`, including the "W2 owns `hours-ledger.tsx`" resolution and stage 2's rationale (`:863-867`).
- Sheet 04 friction: 0/0, 4/4, 8/5, impossible/+1 field, ≥6/3, saves-nothing/3, 1/1, impossible/3, impossible/3, impossible/not-in-v1 ✓ against `synthesis.md:85-95`. The reconciliation cell "CR 4, FS 3 — CR's unit adopted" is **better** than synthesis.md:86's "CR and FS agree at 4": `memo-critic.md` §3 row 2 is 4, `memo-feasibility.md` §3's matching row is 3. The deck also drops synthesis's non-existent seat ids (CR-98 / WEB-33 / FS-86 / MOB-34 exceed those memos' row counts) in favour of "§3" references — correct.

**Every path cited in the deck exists in the repo.** Checked by location, not basename: all 14 designer-portal TS/TSX files, 6 Capture Swift files, `supabase/functions/qbo-export/index.ts`, `scripts/run-sql-tests.sh`, `docs/design/house-sheet/SPEC.md`, `docs/vision/VISION.md`, `docs/design/the-document/CLAUDE.md`, `apps/mobile/Capture/Capture.xcodeproj/project.pbxproj`, both copies of `portal-vs-desk-feature-gap-matrix-v2.md`, `docs/marketing/founding-onboarding/copy-deck.md`, `artifacts/designer-onboarding-learning-2026-09-03/content/wave-1/15-hours.md`, all nine memos, `synthesis.md`, `architecture.md`, the four briefing files. The two exceptions are R3-7 (`use-scope-builder.ts` under `packages/`) and `LogTimeSheet.swift` / `time_claim_atomicity_test.sql`, which the deck names as files to be created.

**Line citations spot-verified in source (all correct):** `log-strip.tsx:11-12,36` · `hours-ledger.tsx:95-97, 104-123, 184, 273, 380-400, 545-591, 683-700` (the pill is at `683-700`, where `synthesis.md:52` / `architecture.md:939` still say `660-676` — the deck is right) · `use-time-tracking.ts:118, 271, 286-299, 316, 320, 467 (inside useStartTimer), 480-484, 604-621, 616-619, 634, 653, 689, 791` · `document-time-provider.tsx:283-290, 410-424` · `studio-drawer.tsx:468` · `desk-doorway.tsx:19,41` · `authority-hours.ts:74-88` · `invoice-composer.ts:150-161` · `time-billing.ts:43-54` · `invoice-composer.tsx:611-616` · `margin-groups.ts:11-13` (incl. the code-faced `time`) · `part-editor.tsx:423` · `service-agreement-drafting-room.tsx:238` ("Principal designer") · `command-bar.tsx` = 1175 lines · `mobile-sheets.tsx:1145-1231` · `V4VisitReviewScreen.swift:286-371` · `VisitReview.swift:52-58` · `FieldVisitCloseRecord.swift:128-176,137,149` · `WorkDashboardScreen.swift:349-390` (6 Browse tiles → "7th" is right) · `FieldCompanionPresentation.swift:51-64` · `RootView.swift:432` · `project.pbxproj:1140` + zero `CaptureWidgets` references, and both iOS directories exist and are empty.
**Migrations:** `00021:22,136,226-241,484-517` · `00084:164-165` · `00177:13-25,22,51,89,111,118,140,144-146,148,152` · `00198:25-26,27-29` · `00316:237-240,242-246,248,257` · `00317:15-18,31-49` · `00412:2356-2366,2672,2674-2685` (the deck's finer `2677,2680` for the `change_order_terms` legs and `2678,2681` for the `profiles` legs are **exact**, where `synthesis.md:231` / `architecture.md:937`'s `00412:2675` is wrong) · `00484:604-609,626-644` · `00545:147-148` · `00555:3024-3026` · `00556:51` · `00577:2493-2496` · `00578:2648-2653,2765-2771` · `00066:525-535` (the deck-only addition: `proposal_change_order_terms` → `projects.change_order_terms` — **verified true**).
**Other:** `architecture.md:22` and `:289` (the sheet 06 "Open question" citations) ✓ · `architecture-draft.md:29,50,51` and `:3,7,8,63,97` ✓ · `copy-deck.md:357,379,627` all carry `?sheet=hours` ✓ · `15-hours.md:9,15` carry the two studio-scope sentences ✓ · gap-matrix `:189,193` are BIL-04 / BIL-08 ✓ · `SPEC.md:158` (1100px), `:159` (50% for the 7px dot only), `:157` (24/48/72/12 module) ✓ so the colophon's declared deviations are honest · `current-state.md §3` is at `:138` ✓ · `useStudioTimeReport`, `useUpdatePhaseEstimates`, `useTimeEntries`, `useReleaseTimeEntries` all confirmed **0 callers**; `project_unbilled_time` confirmed **3 query sites + 1 SQL test** (`hours-ledger.tsx:162`, `use-time-tracking.ts:159`, `:706`, `design_services_authority_test.sql:221,349,362`).
**Permission matrix (sheet 06):** every cell traceable — `is_org_admin_or_owner` takes an organization id (`00484:604-609`) ✓; `time_entries_studio_read` grants any studio co-member (`00316:237-240`) with the active/non-guest test inside `is_studio_comember` (`00556:51`) ✓; `is_project_team_member` has **no role filter** (`00484:626-644`), so the guest ◐ "rostered" marks are sound ✓; the "what the database already permits a `member`" row matches FS-3 / `synthesis.md:39` ✓; the column really is `organization_members.role` of type `member_role` (`00021:136`, `:22`) ✓.
**Export column list** on sheet 08 is character-identical to `synthesis.md:159` ✓. **Seat attributions** spot-checked in the memos: LEAH-13 (review checkpoint before "Bill it") ✓, UX_MOBILE-11 (Invariant V) ✓, feasibility's "single biggest avoidable cost in the program" quote ✓.

---

## 3 · What would make this clean

Deck-side: R3-1, R3-2, R3-3, R3-6, R3-7 (five edits to `index.html`, all one-cell).
Source-side, outside the deck: R3-4 and R3-5 require `architecture.md:8,835,880,894`
to be amended (the deck's figures are the correct ones), and R3-11 needs a migration
number freed. Minors R3-8 … R3-14 are citation hygiene and prose residue.
