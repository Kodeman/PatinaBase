# Review 01 · Content (round 1, adversarial)

**Target:** `artifacts/hour-tracking-2026-09-11/deck/src/index.html` (1497 lines, 16 `<section class="slide">`).
**Sources checked against:** `synthesis.md` (§1–§10), `architecture.md` (§0–§14), the nine memos in `panel/`, `briefing/{panel-brief-common,current-state,rulings-digest}.md`, `docs/design/the-document/DECISIONS.md`, and the repo at the cited paths.
**Method:** every ● ◐ ○ cell, every count, every HT id, every effort band, every wave/migration range and every `path:line` in the deck was read back to a source; 40+ citations were opened in the repo. Inferences are marked.
**Verdict: not clean.** 0 blockers · 9 majors · 13 minors · 4 notes.

---

## 1 · What holds (checked, no finding)

| Check | Result |
|---|---|
| Slide count | 16 sections, `Sheet 01 / 16` … `16 / 16`, index reads `/ 16`, colophon last — 15 + colophon |
| HTML integrity | zero unclosed/mismatched tags; every one of 15 tables has rows whose `<td>` count equals its `<th>` count — no content silently dropped |
| Ruling-sheet completeness | all 40 ids HT-1…HT-40 present on sheet 14; every question, recommendation and `Touches` id matches `synthesis.md:172-211` |
| Ruling fidelity vs `DECISIONS.md` at the digest's lines | D9 `:20`, D10 `:21`, R19 `:668`, R64 `:2359`, R69 `:2555`, R75 `:2640`, R77 `:2648` — all characterised faithfully. **No blocker.** |
| Paths | every path cited in the deck exists in the repo (incl. `docs/vision/VISION.md`, `artifacts/designer-onboarding-learning-2026-09-03/content/wave-1/15-hours.md`, `apps/mobile/Capture/CaptureWidgets/` + `CaptureShareExtension/` as empty dirs, `supabase/functions/qbo-export/index.ts`). Only `time_claim_atomicity_test.sql` and `LogTimeSheet.swift` are absent, and both are labelled as new |
| Line citations spot-verified as saying what the deck says | `00198:27-29` (activity CHECK), `00021:136` + `:22` (`role member_role`), `00021:226-241` (audit_logs), `00021:484-517` (`user_is_org_member`), `00084:163-164` (roster enum incl. `lead_designer`), `00177:13-25`, `:22`, `:51-80`, `:111,118`, `:148,152`, `00316:237-240`, `:242-246`, `:248,257`, `00317:15-18`, `:31-49`, `00412:2356-2366`, `:2676-2682`, `:2683-2685`, `00484:604-623`, `:626-644`, `00555:3024-3026`, `00556:51-75`, `00577:2493-2496`, `00578:2599`, `:2648-2653`, `:2765-2771`, `command-bar.tsx` = exactly 1175 lines, `hours-ledger.tsx:104-123` (`.eq('user_id')` + project AND), `:162`, `:275`, `:543-591`, `use-time-tracking.ts:118/271/286-299/316/480-484/634/653/689-781/791`, `use-projects.ts:481`, `use-scope-builder.ts:945-950`, `document-time-provider.tsx:283-290`, `:410-424`, `log-strip.tsx:11-12`, `:36`, `studio-drawer.tsx:470-472`, `mobile-sheets.tsx:1145-1147`, `authority-hours.ts:74-88`, `invoice-composer.ts:150-161`, `time-billing.ts:43-54`, `qbo-export/index.ts:131-132`, `margin-groups.ts:11-13`, `VISION.md:54`, `part-editor.tsx:423`, `service-agreement-drafting-room.tsx:238`, `desk-doorway.tsx:19,41`, `copy-deck.md:357,379,627`, `15-hours.md:9,15`, gap matrix `:189,193` (BIL-04 / BIL-08), `project.pbxproj:1140-1224`, `VisitReview.swift:52-58`, `V4VisitReviewScreen.swift:308-316`, `FieldVisitCloseRecord.swift:137,149`, `FieldCompanionPresentation.swift:51-54`, `RootView.swift:432`, `WorkDashboardScreen.swift:349-390` |
| Friction counts (sheet 4) | 0 / 4 / 8→5 / impossible→+1 / ≥6→3 / saves-nothing→3 / 1→1 / impossible→3 / impossible→3 / not-in-v1 — all match `synthesis.md:85-95`, and the underlying counts are in the memos' own §3 tables (`memo-critic.md` §3, `memo-feasibility.md:79-100`, `memo-ux-mobile.md` §3) |
| Wave table (sheet 10) | waves, bands (M 3–5 ×6, L 6–10 ×2), depends-on, gates and migration ranges `head+1…+25` all match `architecture.md:820-831` and each wave's **Ship gate**; "ships unflagged" matches all eight `**Flag:** unconditional` lines |
| Effort bands | conform to the brief's S/M/L/XL definitions; widget XL, App Intent deferred — matches `architecture.md:871-885`, `:905-924` |
| Risks (sheet 13) | 11 rows, one-for-one with `architecture.md:887-903` |
| Sheet-6 scope rows, sheet-7 rate-truth rows, sheet-8 hand-off rows | one-for-one with `synthesis.md` §4, §5, §6 (8 of 8 hand-off questions present) |
| Deletion rows | 23 rows, one-for-one with `architecture.md:926-954` (see C-6 for the count's framing) |

---

## 2 · Findings

Severity: blocker / major / minor / note. Confidence: high / med / low. "Inherited" = the deck faithfully copies a source defect; the deck is still where Kody will read it.

| ID | Sev | Conf | Location | Finding | Fix |
|---|---|---|---|---|---|
| **C-1** | major | high | `deck/src/index.html:328` (sheet 02) | Heading "**Six cells. One passes outright.**" is refuted by its own marks: the table carries **two** `m-full` cells — `Who` (`:341`) and `Project` (`:353`). | Either "Two pass outright", or downgrade `Project` to ◐ (its own cell already says NOT NULL "is also why admin and studio time has no legal home"). |
| **C-2** | major | high | `:390` heading + `:563` annotation (sheet 03) | "Two surfaces are empty columns" / "**Admin portal and client portal carry no hours capability at all**" is refuted by the deck's own ◐ at `:555` (client portal, "See the hours on an invoice") and by `briefing/current-state.md:114` (the client portal renders the time line as an opaque `line.description`). | "The admin portal is empty; the client portal carries exactly one read-only, unbroken-down line." Adjust the heading to one empty column. |
| **C-3** | major | high | `:603`, `:617`, `:638` (sheet 04 source column) | Five cited finding ids **exist in no memo**: `CR-98`, `CR-101`, `FS-86`, `MOB-34`, `WEB-33`. Real id ranges: `memo-critic.md` CRITIC-1…30 plus a separate C1…C29 series; `memo-feasibility.md` FEASIBILITY-1…45 plus W0…W6; `memo-ux-mobile.md` 1…17; `memo-ux-web.md` 1…16; `memo-ux-reporting.md` 1…15; `memo-leah.md` 1…25; `memo-pm-billing.md` 1…12; `memo-pm-studio-ops.md` 1…16; `memo-pm-timetrack-veteran.md` 1…27. Inherited from `synthesis.md:87-92`. The *counts* are sourced (memos' §3 tables); the *ids* are not, so every one of them is uncheckable by a reader. | Cite the seat and its §3 count row ("critic §3, the 45-minute-call row"), or renumber against the memos. Same defect for `REP-37`, `CR-117`, `REP-45`, `WEB-64`, `FS-96/120/121/124/146` wherever the deck inherits them. |
| **C-4** | major | high | `:1187`, `:1193` vs `:1093-1170` (sheets 11 vs 10) | "**17–25 eng-days, critical path**" cannot be derived from the deck's own bands: W0(3–5)+W1(6–10)+W2(3–5)+W5(3–5) = **15–25**; path B, W0+W1+W3+W6, is also 15–25. The low end is overstated by two days in both paths. Inherited from `architecture.md:835-841`, `:871-885`. | State 15–25, or add the two days to a named wave band and reconcile sheet 10. |
| **C-5** | major | med | `:1251` (#3) vs `:1259` (#9), sheet 12 | The same view lines are scheduled for deletion in two different waves. #3 removes "`project_unbilled_time`'s `change_order_terms → default_hourly_rate_cents → 0` chain" at `00412:2676-2682` in **W0**; #9 removes "the `profiles.default_hourly_rate_cents` fallback" at `00412:2678,2681` in **W1** — lines *inside* #3's range (verified: 2678 and 2681 are the two `pr.default_hourly_rate_cents` legs of that view). Inherited from `architecture.md:930,936`. | Merge #9's view half into #3 (W0); leave only `00177:111,118` and the column drop on the later line, and say so in the gate. |
| **C-6** | major | med | `:1246` (sheet 12 heading) | "**Twenty-three removals**" counts at least four rows that the rows themselves say are not removals: #12 "**made true**, not deleted" (`:1262`), #20 "dropped from the plan outright, **not built then removed**" (`:1270`), #22 "`useStudioTimeReport` — **not deleted**" (`:1272`), #23 "verify the pointer exists before fixing it" (`:1273`). The count also diverges from `synthesis.md:219-240` (22 rows): the deck adds three rows not in §8 (#10 the `pending_authorization` badge, #16 the log strip's `'design'` default, #19 the "Principal designer" label) and drops two that are (§8 #21 Wave 6's unlogged-day rule **and its setting**, §8 #22 Wave 5's Live Activity). It matches `architecture.md:926-954` exactly. | "Twenty-three dispositions — nineteen deletions, four rulings/corrections", and say in the heading that §8's nudge and Live Activity rows now live on sheet 09 / sheet 14. |
| **C-7** | major | high | `:708`, `:1075`, `:1232` | Three free-standing explanatory paragraphs — reasoning prose, which the charge excluded (`briefing/panel-brief-common.md:16`, "Findings that cannot be expressed as a table row, a count, a matrix cell, or a one-line decision will not survive to the deck"). They are: the re-verification sentence under sheet 05; the "online only is read from the plan's absence" disclosure under sheet 09; the "Stage 2 is the one the plan exists to enable" paragraph under sheet 11. | Convert each into a keyed one-liner (`<ul class="verdicts">`) or a table cell: e.g. a `Provenance` column on sheet 05, an `Offline` legend row on sheet 09, a `Why` cell on the stage-2 row. |
| **C-8** | major | high | `:806-815` (sheet 06, `guest` row) | Every `guest` cell is ○ — including **My hours** — on the stated gate "excluded by `time_entries_studio_read` (00316:237-240)". That policy is not the only read: `00177:140-141` grants SELECT on `USING (is_project_team_member(project_id))`, and `00484:626-644` shows `is_project_team_member` has **no role filter** — so a guest who sits on `project_team_members` can today read *every* entry on that project, own rows included. No source says a guest cannot see their own hours; `synthesis.md:108` states the own-row read as `00177:140` + `00316:237` without a role carve-out. | Either mark `guest` / My hours ◐ with the gate `is_project_team_member` (rostered only), or state explicitly that Wave 2 narrows `00177:140` — which would be a second write-policy change the plan says it is not making (`synthesis.md:157`, "**One** write widening"). |
| **C-9** | major | high | `:1330` (HT-4), `:1350` (HT-24) | The "For / against" column silently changes unit. `synthesis.md:168` defines it as "Majority counts only **seats** that took a position". HT-4's source is "CR-3, CR-21 (only seats that traced the cause); unopposed" — **one** seat, two findings — and the deck prints "**2 · 0, unopposed**". HT-24's source is "FS-12 … CR-28, FS-27" — **two** seats, three findings — and the deck prints "**3 · 0**". HT-4 is one of the four rulings the deck twice names as gating the first line of code (`:307`, `:1322`, `:1395`), so the inflation lands on the highest-stakes row. | Print seats, with the finding ids beneath: "1 seat (CR) · 0 against" / "2 seats (FS, CR) · 0". Check the same unit on HT-5 (1 · 0), HT-19 (1 · 0), HT-25 (1 · 0), HT-33 (1 · 0), HT-40 (1 · 0) — those are already seat-correct. |
| **C-10** | minor | med | `:562-564`, `:877-879` | Six `.ann` paragraphs are explanatory prose in labelled cells (sheets 03 and 07). Softer than C-7 — each has a key and sits under a table — but they are sentences of reasoning, not rows. | Keep if the orchestrator reads `.ann` as a keyed cell; otherwise compress each to one clause. |
| **C-11** | minor | high | `:878` (sheet 07) | "W1 ships a one-off re-rate plus a pre-flight count" contradicts `synthesis.md:136-139`, which puts the mandatory backfill in **Wave 0**. The deck follows `architecture.md:132-262` (backfill inside W1, pre-flight count in W1's ship gate) — a defensible supersession, but silent. | Add "(synthesis said W0; the plan moves it behind the resolver)" to the cell, or one line on sheet 10's W1 row. |
| **C-12** | minor | high | `:1298` (risk #4) vs `:1189`, `:1204-1229` | Risk #4 reads "Migration-number collision across **four** concurrent worktrees" while the same deck says "**2–3** concurrent worktrees" (`:1189`) and its stage table peaks at three (stage 4, W4 ∥ W6 ∥ W7). Inherited from `architecture.md:894`. | "across three concurrent worktrees", or say "up to four branches in flight, three worktrees at a time". |
| **C-13** | minor | high | `:1387` (sheet 15) | "Green-light W0. **Three days**" states the low end of an M (3–5 d) band as the ask, and its content list ("the atomic claim, the unbilled view, the `source` CHECK, and three dated governance entries") drops the fourth W0 item sheet 10 carries — "the hook module made canonical", which `architecture.md:125-128` names as the wave's single droppable item. | "Three to five days", and either name the hook move or say it is the droppable half. |
| **C-14** | minor | high | `:1258` (#8) | Citation `00412:2675` for "`change_order_terms->>'hourly_rate_cents'` as a rate leg" is off: `00412:2675` is `te.duration_minutes, te.notes,`; the leg appears at `00412:2677` and `:2680`. Inherited from `synthesis.md:231`. | `00412:2677,2680`. |
| **C-15** | minor | high | `:1260` (#10) | Citation `hours-ledger.tsx:660-676` for "the `pending_authorization` dead-end badge" points at the row's **activity `<select>` and duration `<input>`**; the state pill begins at ~`:683`, and the string `pending_authorization` appears in that file only at `:184`. Inherited from `synthesis.md:52`. | `hours-ledger.tsx:683-700` for the pill, `:184` for the state it renders. |
| **C-16** | minor | high | `:1265` (#15) | Citation `use-time-tracking.ts:320, 467` for "the implicit `billable: input.billable ?? true`": `:320` is the `?? true` ✓; `:467` is `if (input.billable !== undefined) row.billable = input.billable` in `useStartTimer` — a conditional set with no default. Inherited from `synthesis.md:227`. | `:320` for the default; cite `:467` separately as the start-timer path that omits `billable` entirely. |
| **C-17** | minor | high | `:857` (sheet 07), `:679` (sheet 05) | "`change_order_terms`, writable only by the legacy scope builder" cites `use-scope-builder.ts:945-950`, which writes `proposal_change_order_terms` — a different relation from the `projects.change_order_terms` JSONB the view reads. The actual populator is the proposal→project conversion (`supabase/migrations/00066_proposal_project_flow_v2.sql:525-535`, copying that row into `projects.change_order_terms`); nothing in `apps/` or `packages/` writes the `projects` column. The conclusion (a legacy-only, unauthorable leg) survives; the named writer does not. | "authored only through the legacy proposal scope builder and copied in by the 00066 conversion (`00066:525-535`)". |
| **C-18** | minor | high | `:800` vs `:746` (sheet 06) | The `member` row marks "A project's" ◐ "rostered", gated on `is_project_team_member`; the scope table four rows above grants the project scope to "Rostered project members **or studio co-members**" (`synthesis.md:110`, `00316:237`). One slide, two permission answers for the same person. | Mark ● for a studio co-member with the gate `is_studio_comember`, and keep ◐ only for the non-co-member roster case, or align the scope row. |
| **C-19** | minor | med | `:738`, `:784`, `:913` | The permission model rests on "asserted server-side via `is_org_admin_or_owner`", whose first argument is an **organization id** (`00484:604-609`). Neither the deck, `synthesis.md` §4, nor `architecture.md` §3 says where a `project_time_entries` policy gets that id, while `architecture.md:24` forbids `projects.studio_id` as a policy key and `:289` marks the view's `studio_id` as "**not** used as a policy key". The slide that claims the permission model leaves its own gate unresolvable. *(Inference: no source names the derivation; I read §0, §3 and the deck in full.)* | One cell or footnote naming the derivation (e.g. `organization_members` of `projects.designer_id`), or move it onto the ruling sheet beside HT-10. |
| **C-20** | minor | high | `:8`, `:311`, `:1420` | "**218 Findings**" is not reconstructible from the memos. Numbered finding rows total **203** (critic 30, feasibility 45, leah 25, billing 12, studio-ops 16, veteran 27, mobile 17, web 16, reporting 15); **210** with feasibility's seven per-wave effort rows; **239** including the critic's separate C1…C29 series. No combination yields 218. It matches `synthesis.md:3`, so the deck carries it faithfully. | Re-derive once and print the arithmetic in the colophon ("203 findings + 7 effort verdicts"), or drop the figure from the cover's count row. |
| **C-21** | minor | med | `:670`, `:690-705` | Heading "**Two** rate holes" sits above a table numbering **four** holes, with 3 and 4 placed in `<tfoot>`. Both footer rows are W0 items — the two live money bugs the deck asks to green-light first (`:1387`) — so the most urgent content is typographically demoted, and in the ≤860px card mode it renders after the "last row" bottom border. | Either "Four money holes — two are rate holes" with all four in `<tbody>`, or move holes 3–4 to sheet 08 where the claim and the view already appear. |
| **C-22** | minor | med | `:335` (sheet 02) | "Log an hour anywhere" is marked ○ **absent**, while the same row's text records a working zero-tap path and sheet 03 marks that path ● (`:407`). Under the slide's own legend (`:374`, ◐ = "partly, or answers the wrong question") ◐ is the honest mark. | ◐ with the quantifier "0 taps in a document, impossible off one" — the marks then agree across sheets 02, 03 and 04. |
| **C-23** | note | high | `:1263` | `portal-vs-desk-feature-gap-matrix-v2.md:189,193` is cited without a directory; two copies exist (`docs/design/the-document/` and `docs/product/`), identical at those lines — so the edit W2 owes must be made twice. | Name both paths, or say "both copies". |
| **C-24** | note | high | program folder | **No DECK CONTRACT file exists** — grepped the whole of `artifacts/`, `.claude/workflows/` and `~/.claude/workflows/`. The mandated 15-slide topic list therefore could not be diffed; what I verified is the count (15 + colophon), the numbering, and topic coverage against `synthesis.md` §1–§10 + `architecture.md` §0–§14. Two source tables have no slide of their own: `synthesis.md:65-73` (the seven-crux criteria table — **no crux label i–vii appears anywhere in the deck**) and `architecture.md:905-924` (the 14-row "explicitly NOT in v1" table; 9 of its 14 rows appear scattered across sheets 04, 08, 09 and 14, five do not: the second hours surface, hours-vs-estimate, hours on the margin, Field non-mine scopes, and the utilisation/leaderboard/streak prohibition as a single row). | Orchestrator: supply the contract, or rule whether a criteria-table slide and a NOT-in-v1 slide were required. |
| **C-25** | note | high | sheet 14 | Positive result, recorded so it is not re-litigated: all 40 HT ids present and faithful; no ruling is misquoted against `DECISIONS.md` at the digest's lines. The brief's blocker class is **empty**. | — |
| **C-26** | note | med | sheets 03, 04, 06 | Cells I expected to be unsourced and found sourced: the whole **Admin portal** column (`briefing/current-state.md:112`, "Admin portal. Nothing — no hours, no member view, no rates"), the client-portal ◐ (`:114`), Designer-mobile ◐ on "Set what the work was" and "Adjust the duration" (`:97`, the mobile timer sheet's fields = minutes, activity; `mobile-sheets.tsx:1141,1202`), and Designer-mobile ◐ on "See my hours" (`mobile-sheets.tsx:134-138`, the `hours` mobile sheet). Note these rest on the **briefing**, not on synthesis/architecture/memos. | Optional: add `current-state.md §2` to the colophon's source list so those cells are traceable. |

---

## 3 · Clean gate

`clean = false` — nine majors (C-1 … C-9) stand. C-1, C-2, C-4, C-8, C-9 are wrong-on-their-own-terms and visible to Kody on the slide; C-3, C-5, C-6, C-11…C-17 are inherited from `synthesis.md` / `architecture.md` and should be fixed in both places, not just the deck.
