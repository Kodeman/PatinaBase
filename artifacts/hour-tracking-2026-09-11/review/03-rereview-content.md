# Round 2 — content re-review, adversarial

**Target** `artifacts/hour-tracking-2026-09-11/deck/src/index.html` (1529 lines, 15 sheets + colophon)
**Sources checked against** `synthesis.md`, `architecture.md`, the nine memos in `panel/`, `briefing/*`, and the repo itself at `origin/main`.
**Method** every figure, every `● ◐ ○` cell, every `path:line`, every `HT-n`, every effort band and every count read against its source; every repo path opened and every cited line range printed and compared. No live servers, no git mutations.

**Verdict: NOT clean.** 7 majors, 8 minors, 4 notes. Zero blockers — no ruling is misquoted against `DECISIONS.md` at the digest's line (D9 :20, D10 :21, D11 :22, R4 :131, R19 :668, R20 :679, R64 :2359, R69 :2555, R75 :2640, R77 :2648 all verified: the id at that line is the ruling the deck names, and every paraphrase the deck carries is consistent with the source text).

---

## What holds (checked, not assumed)

| Check | Result |
|---|---|
| **15 sheets + colophon** | ✓ Sheets 01–16, `Sheet NN / 16` on every gutter, one colophon. No slide missing. |
| **Ruling sheet carries every `HT-n` from synthesis §7** | ✓ 40 rows, HT-1 … HT-40, no gaps, no extras. Matches `synthesis.md:172-211` exactly. |
| **For/against counts, all 40** | ✓ All 40 reconcile with `synthesis.md:172-211` under that table's own "majority counts only seats that took a position" rule (`:167-169`), including the two round-1 corrections (HT-4 → 1 seat CR; HT-24 → 2 seats FS, CR). |
| **`Touches` column, all 40** | ✓ R19 ×3, R77 ×2, R64, R69, D9, D10 ×2, §6 ×2, §1, §4/§6, `new` ×27 — matches synthesis. |
| **Every repo path cited** | ✓ All 24 TS/TSX/Swift files, 13 migrations, 2 scripts, `qbo-export/index.ts`, `SPEC.md`, `VISION.md`, `DECISIONS.md`, both gap-matrix copies, `copy-deck.md`, `15-hours.md`, the two empty Capture dirs, `design_services_authority_test.sql` exist. Only `LogTimeSheet.swift` and `time_claim_atomicity_test.sql` do not — both correctly labelled "new". |
| **Load-bearing line ranges** | ✓ Spot-opened and confirmed: `00578:2599` is the classifier head and `:2599-2805` is exactly **207 lines**; `:2648-2653` the non-services branch; `:2765-2771` the NULL landing; `00412:2356-2366` the guard early-exit; `:2677,2680` the change-order legs; `:2683-2685` includes the INNER JOIN on `profiles`; `00577:2493-2496` the promotion filter's two NOT NULLs; `00555:3024-3026` the documented row-dropping hazard; `00316:237-240,242-246,248,257`; `00317:15-18` and `:31-49`; `00484:604-623` and `:626-644` (and `is_project_team_member` indeed has no role filter); `00021:22,136,226-241,484-517` (and `user_is_org_member` indeed has no `SET search_path`); `00556:51-76` excludes `role = 'guest'`; `00198:25-26` source / `:27-29` activity; `use-time-tracking.ts:118,271,286-299,316,320,467,480-484,604-621,616-619,634,653,689,791`; `hours-ledger.tsx:95-97,104-123,162,184,380-400,543-591,683-700`; `log-strip.tsx:11-12,36`; `document-time-provider.tsx:217,283-290,330,410-424`; `margin-groups.ts:11`; `part-editor.tsx:423`; `service-agreement-drafting-room.tsx:238`; `use-scope-builder.ts:945-950`; `use-projects.ts:481`; `time-billing.ts:43-54`; `invoice-composer.ts:150-161`; `authority-hours.ts:74-88`; `desk-doorway.tsx:19,41`; `copy-deck.md:357,379,627`; `15-hours.md:9,15`; `pbxproj:1140-1224`; `V4VisitReviewScreen.swift:286-316,308-316,346-371`; `VisitReview.swift:52-58`; `WorkDashboardScreen.swift:349-390`; `RootView.swift:432`; `FieldVisitCloseRecord.swift:128-176,137,149`; `FieldCompanionPresentation.swift:51-64`. |
| **Self-verified counts** | ✓ `command-bar.tsx` is **1175** lines. `00545` is **232** lines. The classifier is redefined in exactly three migrations (00412, 00575, 00578) → "redefined twice" is right. Local head `00580`, `origin/main` head `00591` → "eleven behind" is right. |
| **Wave table (sheet 10)** | ✓ All eight rows — ships, band, depends-on, gate, migration range — match `architecture.md:822-831` and each wave's own Ship gate (`:118, 246, 370, 472, 561, 633, 732, 806`). W2 "W0 — not W1" ✓. W5 "none · head+20 reserved" ✓. |
| **Effort bands** | ✓ M=W0,W2,W3,W5,W6,W7 · L=W1,W4 · totals 30–50 eng-days · calendar 18–28 working days — all match `architecture.md:873-884`. |
| **Sheet 08 CSV column set** | ✓ 13 columns, verbatim from `synthesis.md:159` and `architecture.md:579-582`. |
| **Sheet 06 permission matrix** | ✓ Every cell defensible against the policies and helpers it cites, including the guest `◐ rostered` cells (`is_project_team_member` has no role filter) and the "what the database already permits a member" row. |
| **Counts in headings** | ✓ "Six cells. Two pass outright." (6 rows, 2 `m-full`). "Seventeen capabilities, five surfaces." (17 rows, 5 mark columns, admin column all-absent). "Four money holes — two are rate holes." "Eight waves." "Eleven risks." "Twenty-three dispositions — nineteen deletions, four rulings/corrections." (non-deletions = #12, #20, #22, #23 = 4 ✓). "Forty questions." |
| **Prose** | ✓ No free-standing explanatory paragraph anywhere. Every `<p>` in the body is inside a keyed `.ann` block or a table cell. (Six cells still run 2–3 sentences — R2-12.) |

---

## Findings

| ID | Sev | Conf | Location | Finding | Fix |
|---|---|---|---|---|---|
| **R2-1** | **major** | high | `index.html:1206`, `:1212` | **The critical-path figure contradicts the architecture in three places.** The deck prints **15–25** eng-days (count tile and Path B sentence). `architecture.md:8`, `:836` and `:884` all say **17–25**. Round-1 fix C-4 changed the deck on the deck's own band arithmetic (M 3–5 + L 6–10 + M 3–5 + M 3–5 = 15–25), so the deck is arithmetically right and the source is wrong — but as shipped, the deck's headline schedule number is not in any source. | Correct `architecture.md:8,836,884` to 15–25, or restore 17–25 in the deck. The two documents must not disagree on the program's schedule. |
| **R2-2** | **major** | high | `index.html:1323` | **Risk #4's worktree count contradicts the architecture.** Deck: "across **three** concurrent worktrees". `architecture.md:895`: "across **four** concurrent worktrees". The deck matches the parallelisation table's own peak of 3 (`architecture.md:848`, stage 4) and its own `2–3` count tile, so again the deck is right and the source is wrong. | Correct `architecture.md:895` to three. |
| **R2-3** | **major** | high | `index.html:839` | **`architecture.md:24` is miscited.** Sheet 06's Open question says "`projects.studio_id` is forbidden as a policy key (**architecture.md:24**)". Line 24 is the *"No new SELECT policy on `project_time_entries`"* rule; the studio_id prohibition is **`architecture.md:22`**. (The companion citation `architecture.md:289` is correct — it does mark the view's `studio_id` as "not used as a policy key".) | `architecture.md:24` → `architecture.md:22`. |
| **R2-4** | **major** | high | `index.html:1292` | **Sheet 12 row #20 cites a line that proposes none of the three things it names.** The row drops "the draft's DEFINER rollup, its `studio_id`-keyed RLS policy, and every new call site on `user_is_org_member`" and cites `architecture-draft.md:44,50`. `:50` is the DEFINER rollup ✓. But the `studio_id`-keyed SELECT policy and the `user_is_org_member` call sites are at **`architecture-draft.md:51`** (`CREATE POLICY "studio admins read studio time" USING user_is_org_member(auth.uid(), (SELECT studio_id FROM projects …))`) and **`:29`** (the second call site, on `studio_member_rates`). `:44` is the `member` row of the draft's permission matrix and proposes neither. Inherited verbatim from `synthesis.md:238` and `architecture.md:950` — all three carry the same error. | Cite `architecture-draft.md:50,51` (and `:29`). |
| **R2-5** | **major** | high | `index.html:1281` | **Sheet 12 row #9 conflates the column with the fallback, and its Wave contradicts its own Gate.** Thing = "The `profiles.default_hourly_rate_cents` **column**", path = `00177:111,118`, Wave = **W1**, Gate = "The column drops a cycle after the view rewrite is live". (a) `00177:111` and `:118` are the 00177 *view's* two COALESCE fallback legs; the **column** is declared at `00177:89`. (b) `architecture.md:937` deletes the **fallback** in W1 and explicitly defers the **column**; the deck's row claims the column in W1 while its own gate defers it. (c) Round-1 fix C-5 dropped the W1 fallback's real anchors, `00412:2678,2681`. | Thing → "the `profiles.default_hourly_rate_cents` **fallback**"; path → `00412:2678,2681 · column declared 00177:89`; keep W1 and the gate as written. |
| **R2-6** | **major** | high | `index.html:1324` | **Risk #5's premise is false for `source`.** "The `source` and `activity` CHECK constraint names appear in no migration." `00545:147-148` drops and re-adds the constraint with an explicit name — `project_time_entries_source_ck` — and that string appears in exactly one migration, 00545 (grepped all). Only `activity` is still inline and unnamed (`00198:27-29`; no later migration touches it). So the mitigation ("using 00545's name-discovery form rather than a guessed name") is unnecessary for W0's `source` widening, which is the two-line `DROP CONSTRAINT IF EXISTS project_time_entries_source_ck, ADD CONSTRAINT …` that 00545 itself already models, and the risk is mispriced at medium. Inherited from `architecture.md:899` and from `synthesis.md:58`, which quotes 00545's own comment about the **pre-00545** state in the present tense. | Rewrite: "the `activity` CHECK is inline and unnamed (`00198:27-29`); `source` was named `project_time_entries_source_ck` by `00545:147-148`, so only the activity widening needs the name-discovery form." Reprice to low. |
| **R2-7** | **major** | high | `index.html:1378` vs sheet 10 (`:1108-1194`) | **HT-27 is answered "Yes" and no wave in the deck carries it.** The ruling reads "Do the PostHog events ship before any iOS extension wave? **Yes.** The widget decision must rest on `time_entry_logged` by surface, not on taste" — but no wave row on sheet 10 names an event, and `time_entry_logged`, `time_rate_unresolved` and `time_timer_stopped` appear nowhere else in the deck. `architecture.md` carries a **PostHog events** line for all eight waves (`:93, :210-212, :338, :445, :539, :613, :702-703, :791`), with the set starting in W1 and `time_rate_unresolved` named as the alarm. HT-17's "instrument the ratio first" (`:1365`) has the same problem — `architecture.md:969` assigns the instrumentation to W3, the deck's W3 cell does not mention it. | Add the event names to the W1/W3/W5/W6 "Ships" cells, or one instrumentation line to sheet 10's "All" row. A ruling the deck commits to must be visible in the plan it presents. |
| **R2-8** | minor | high | `index.html:602` | **"CR and FS agree at 4" is not supported by the feasibility memo.** `memo-critic.md` §3 does give 4 ("minutes field, type, activity select ×2, Log"). `memo-feasibility.md` §3's corresponding row is "Log with activity recorded … **3** (activity open + pick + Log)"; its only 4 is a *proposed* count ("Log with `billable` chosen \| impossible \| **4** (+1 toggle)"). The deck is faithful to `synthesis.md:86`, which asserts the agreement; the memo does not. | Cite CR only, or "CR 4, FS 3 — CR's unit adopted". |
| **R2-9** | minor | high | `index.html:595-666, 929, 956, 1037`, sheet 14 | **The deck's finding ids exist in no memo, and the colophon never defines the shorthand.** The deck cites `FS-11`, `FS-24`, `FS-31`, `CR-1`, `CR-24`, `WEB-2`, `WEB-3`, `MOB-11`, `LEAH-13`. The memos number findings `FEASIBILITY-n`, `CRITIC-n`, `UX_WEB-n`, `UX_MOBILE-n`, `LEAH-n`, `PM_BILLING-n`, `PM_STUDIO_OPS-n`, `PM_TIMETRACK_VETERAN-n`, `UX_REPORTING-n`. Every cited id does resolve (verified: FEASIBILITY-11/24/31, CRITIC-1/24, UX_WEB-2/3, UX_MOBILE-11, LEAH-13 all exist and say what the deck claims) — but only via the mapping at `synthesis.md:9-13`, which the deck does not reproduce. The colophon's Panel row lists nine filenames and no key. | Add a shorthand key to the colophon: `FS = feasibility · CR = critic · WEB/MOB/REP = the three UX seats · OPS/BIL/VET = the three PMs · LEAH = the customer`. |
| **R2-10** | minor | high | `index.html:1321` | **"all eight policies resolve through `projects p WHERE p.id = project_id`" is true of four.** The four 00316 policies do (`00316:237-240, 242-246, 248-, 257-`). The four 00177 policies resolve through `is_project_team_member(project_id)` (`00177:140, 144, 148, 152`), i.e. through `project_team_members`, not `projects p`. The conclusion — a NULL `project_id` makes all eight false — still holds, so this is a mechanism error, not a wrong answer. Same wording at `architecture.md:891` and `synthesis.md:186`. | "All eight resolve through `project_id` — four through `projects p`, four through `is_project_team_member(project_id)`." |
| **R2-11** | minor | med | `index.html:703, 711` | **Sheet 05's Provenance column is blank for holes 3 and 4 though the synthesis records verification.** `synthesis.md:36` ends hole 3 with "Verified in source during synthesis"; `:37` marks hole 4's consumption site as verified (`invoice-composer.tsx:611-616`). The deck prints "—" for both, understating its own evidence on two of four money holes. | Hole 3 → "Verified in source during synthesis (`use-time-tracking.ts:604-621`)"; hole 4 → "view + consumer verified (`invoice-composer.tsx:611-616`)". |
| **R2-12** | minor | med | `index.html:569, 1320, 1323, 1326, 1446, 1447` | **Six cells still carry 2–3 sentences of reasoning.** `:569` (the "empty column" annotation — the one two-sentence `.ann` left after round 1); risk #1's mitigation at `:1320` (three sentences); risk #4 at `:1323` and risk #7 at `:1326` (two each); colophon "Findings count" `:1446` and "Typeset to" `:1447` (two each). The ask was "no prose … a table row, a count, a matrix cell, or a one-line decision"; these are table and reference content rather than free paragraphs, but they exceed one line of decision. | Compress each to one clause, or record the deviation in the colophon alongside the typographic ones. Orchestrator's call. |
| **R2-13** | minor | med | `index.html:1449` | **The colophon's Paths rule leaves five cited files unresolvable.** "Citations are relative to `apps/designer-portal/src/` (TS/TSX) and `supabase/migrations/`." But `use-scope-builder.ts` exists only at `packages/supabase/src/hooks/use-scope-builder.ts` (no designer-portal copy — checked against `git ls-files`); the Swift files are under `apps/mobile/Capture/`; `qbo-export/index.ts` under `supabase/functions/`; `copy-deck.md` under `docs/marketing/founding-onboarding/`; `15-hours.md` under `artifacts/designer-onboarding-learning-2026-09-03/content/wave-1/`. (The rule is load-bearing and correct for `use-projects.ts`, which exists in both roots.) | Add: "Swift under `apps/mobile/Capture/`; edge functions under `supabase/functions/`; docs and artifacts from the repo root." |
| **R2-14** | minor | med | `index.html:1444` | **The colophon's Briefing row omits §3, the source of every Patina Field cell.** It cites `current-state.md §0 and §2 (surface capability tables)`. `current-state.md:79` §2 is "Designer portal, admin portal, client portal, extension"; the Field facts are `current-state.md:138` §3, "Patina Field (iOS) and the Patina client app" — and the deck carries a Patina Field column on sheet 03 plus four Field rows on sheet 09. | "§0, §2 and §3". |
| **R2-15** | minor | med | `index.html:418, 427, 454, 463, 1000, 1291` | **Five inherited citations are off by 1–2 lines.** (a) `addValid hours-ledger.tsx:275` — `addValid` is declared at `:273`; `:275` is `if (!addValid \|\| addBusy) return;` inside `batchAdd`. (b) `hours-ledger.tsx:275-294` cited three times as "the add row" — that range is `batchAdd`'s body; the add-row JSX is `:545-591` (which the deck cites correctly elsewhere, at `:1012`). (c) `studio-drawer.tsx:470-472` for the accrual-gated doorway — the gate `holding && inHandToday > 0` is at `:468`. (d) `enum 00084:163-164` for the roster roles — the role list is `00084:164-165`, and it is a CHECK, not an enum (the `member_role` enum is `00021:22`). All inherited verbatim from `synthesis.md:35,43,45,89`. | Retarget or widen each range; say "role CHECK", not "enum", at sheet 12 #19. |
| **R2-16** | note | high | `index.html:586-666` | Sheet 04 carries 10 of synthesis §3's 11 rows — "Field — running timer" (`synthesis.md:94`) is absent. Coverage is preserved: sheet 09 `:1081` carries it ("A running timer owned by Field … not in v1 … HT-7"), as does HT-7. No action; recorded so it is not re-found. |
| **R2-17** | note | high | `index.html:662` | Sheet 04's Widget/App-Intent v1 cell reads "not in v1" where `synthesis.md:95` reads "1 tap / 1 utterance". Justified by `architecture.md:907-910` (§12 puts both out of v1) and internally consistent with sheet 09. No action. |
| **R2-18** | note | high | `index.html:1283` | Sheet 12 row #10's citations are **better than both sources**: `hours-ledger.tsx:683-700` is the pill and `:184` the `pending_authorization` filter (verified), where `synthesis.md:52` and `architecture.md:938` both cite a stale `660-676`. Back-port the correction into the sources. |
| **R2-19** | note | med | `index.html:307` | The title epigraph reads "**Time** is not a margin item the margin prints…". `margin-groups.ts:11` reads "`time` is not a margin item…" — a statement about the `time` margin-item *kind*, not about time in general. `synthesis.md:279` loses the same distinction. Cosmetic, flagged only because it is the deck's epigraph and its title's warrant. |

---

## Refutation attempts that failed (the claim survived)

| Claim tested | How | Outcome |
|---|---|---|
| "207 lines" (sheets 07, 13) | `00578:2599` is `CREATE OR REPLACE FUNCTION public.classify_project_time_entry_authority()`; 2805 − 2599 + 1 = 207 | **Exact.** |
| "redefined twice since 00412" | `grep -l "FUNCTION public.classify_project_time_entry_authority" supabase/migrations/*.sql` → 00412, 00575, 00578 | **Holds.** |
| "232 lines of archaeology" | `wc -l 00545_time_entry_field_visit_source.sql` → 232 | **Exact.** |
| "command-bar.tsx (1175 lines)" | `wc -l` → 1175 | **Exact.** |
| "Local head is eleven behind Strata" | local max `00580`, `origin/main` max `00591` | **Exact.** |
| "the guard early-exit returns for every non-services project" | `00412:2366` is `IF NOT public._is_design_services_project(OLD.project_id) THEN RETURN NEW; END IF;` | **Holds.** |
| "no client sends a rate" (`use-time-tracking.ts:286-299`) | `CreateTimeEntryInput` has no rate field; `:314-322` writes no `hourly_rate_cents` | **Holds.** |
| "the rollback detaches every entry on the invoice" | `:616-619` is `await supabase.from('project_time_entries').update({invoice_id:null}).eq('invoice_id', invoiceId)` | **Holds, exact lines.** |
| "guest excluded from `time_entries_studio_read`" | `is_studio_comember` (`00556:51-76`) asserts `me.role <> 'guest' AND owner.role <> 'guest'` | **Holds.** |
| "`is_project_team_member` has no role filter" | `00484:626-644` filters only `project_id`, `user_id`, `removed_at IS NULL` | **Holds** — the guest `◐ rostered` cells are correct. |
| "Specimen timestamps are already sorted" | `VisitReview.swift:58` — `rows.sorted { $0.createdAt < $1.createdAt }` | **Holds** — the active-duration default really is free. |
| "the drainer runs once per launch" | `RootView.swift:432` — `await container.visitCloseOutboxDrainer?.resume()` inside the reconciliation guard | **Holds.** |
| "absent from the pbxproj" | `pbxproj:1140` = "Begin PBXNativeTarget section", `:1224` = "End"; both `CaptureWidgets/` and `CaptureShareExtension/` exist on disk and are empty | **Holds.** |
| "BIL-04/BIL-08 both say ABSENT" | both copies, lines 189 and 193, read `○ ABSENT` and "disabled stub" | **Holds, both copies.** |
| "the live drip lands on a bare Desk" | `copy-deck.md:357,379,627` all `?sheet=hours`; `desk-doorway.tsx:19` is `?book=hours`, `:41` "Unknown book / page / account values are ignored in silence" | **Holds.** |
| "the help article's two sentences are false" | `15-hours.md:9` "Hours is the studio's time ledger"; `:15` "the studio's first hire can see … where the week actually went" | **Holds, verbatim.** |
| Ruling ids at the digest's lines | `DECISIONS.md:20,21,22,131,668,679,2359,2555,2640,2648` — every line's id and heading match the digest's paraphrase, and every deck paraphrase is consistent with it | **No blocker. Holds.** |

---

## Not in scope of this pass, flagged for the orchestrator

- **No DECK CONTRACT file exists.** Re-grepped `artifacts/`, `.claude/workflows/` and `~/.claude/workflows/` — round 1's C-24 stands unresolved. What I could verify is the count (15 + colophon ✓), the numbering ✓, and topic coverage against `synthesis.md` §1–§10 and `architecture.md` §0–§14. Two source tables still have no sheet of their own: the seven-crux criteria table (`synthesis.md:65-73` — **no crux label i–vii appears anywhere in the deck**) and `architecture.md:905-924`'s 14-row "explicitly NOT in v1" table (5 of its 14 rows appear nowhere: any second hours surface, hours-vs-estimate, hours on the margin, non-mine Field scopes, and the utilisation/leaderboard/streak prohibition as one row — the last three appear only as negations inside sheet 06's "Never" column). If a contract existed and required either, that is a major; I cannot assert it.
- **R2-1 and R2-2 are corrections owed to `architecture.md`, not to the deck.** The deck is the arithmetically correct document in both.
