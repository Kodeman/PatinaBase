# Ruling sheet — hour tracking (2026-09-11)

Blank until Kody rules. Every row is from synthesis.md §7; the panel's recommendation is advice, not a ruling.

| ID | Ruling touched | Question | Panel recommendation | For / against | Ruling | Date |
|---|---|---|---|---|---|---|
| **HT-1** | NEW | Does the server own `hourly_rate_cents` on **every** project kind, overriding any value a client sends? | **Yes.** One resolver, called by the classifier on every branch; clients stop sending an opinion | 9 for, 0 against (CR C3 "needs a new ruling, not an amendment") | RULED — Yes. The server owns hourly_rate_cents on every project kind; a client-supplied rate is discarded. Non-services projects lose the legacy change-order leg. | 2026-09-11 |
| **HT-2** | NEW | Are `change_order_terms->>'hourly_rate_cents'` and `profiles.default_hourly_rate_cents` cut from the chain, or preserved as tiers 2 and 3? | **Cut both.** One has only a legacy writer; the other has no writer at all | CR-19, VET-25, FS-22, BIL-11 for cutting; LEAH-23 keeps `profiles.*` as a short-term tier 3 | | |
| **HT-3** | NEW | Does a per-member studio rate exist, and is the People Room member profile its home (blur-save, `owner`/`admin` only)? | **Yes, and yes** | 6 for (BIL-2, LEAH-11, VET-15, OPS-11, CR C17, FS), 0 against | RULED — A per-member studio rate exists (tier 2). Edited on a studio settings page (extend /preferences or a sibling page), owner/admin only, dated, append-only history. NOT the People Room. | 2026-09-11 |
| **HT-4** | NEW | Does a rate-card row bind to a **person or roster role** instead of a free-text label? | **Yes.** Do not build a resolver on a string match | CR-3, CR-21 (only seats that traced the cause); unopposed | RULED — Rate-card rows bind to the roster role enum (lead_designer / support_designer / bookkeeper / vendor), no free text. | 2026-09-11 |
| **HT-5** | NEW | Is `claim_time_entries` a Wave 0 item? | **Yes.** Atomic claim; delete the compensating detach | FS-13, unopposed | | |
| **HT-6** | NEW | Does the `project_unbilled_time` repair (LEFT JOIN + one rate source) move to Wave 0? | **Yes.** It is a silent money understatement, not a cleanup | FS-14, CR-4, REP-9, BIL-9, VET-18, LEAH-22 for Wave 0; the draft says Wave 6 | | |
| **HT-7** | **R19** (amendment) | May a Field, widget or intent surface ever own the single global running-timer slot? | **No, not in v1.** Field stays completed-entry-only; elapsed-since-arrival pre-fill feels like a timer without owning the slot | FS-39, MOB §5, OPS-14, draft — unanimous; LEAH: "I'd resent not having it, but I get why" | PANEL DEFAULT ACCEPTED — Field never owns the running-timer slot in v1. | 2026-09-11 |
| **HT-8** | **R77** (amendment) | Does the Hours ledger grow an admin-gated scope lens — mine / member / project / studio — in the same sheet? | **Yes.** One sheet, no page, no tab bar, no member leaderboard | 8 for (OPS-1, WEB-7, REP-1, LEAH-8, VET-13, CR-10, FS-W1, BIL); REP argues it is *additive*, not an amendment | | |
| **HT-9** | NEW | Does the project lens stop ANDing `user_id`? | **Yes** — it currently answers the wrong question with a plausible number | OPS-2, REP-45, WEB-8, unopposed | | |
| **HT-10** | NEW | Is `time_entries_studio_read` **narrowed** to owner/admin, or does a plain `member` keep the teammate read 00316 already grants? | **Decide explicitly.** CR says narrow (the draft's "member sees own only" is cosmetic otherwise); FS warns narrowing 00316 is a widening-in-reverse with its own blast radius. Panel leans **narrow to owner/admin + keep the project-team read** | CR-5 narrow; FS-3 flags the cost; OPS-5 and the briefing were **refuted** — no new SELECT policy is needed either way | RULED — Narrow to owner/admin. Members read own rows plus aggregates on rostered projects. | 2026-09-11 |
| **HT-11** | NEW | Does `billable` become an explicit control at **every** capture surface, and does the implicit `?? true` default go? | **Yes to both.** `billable` is not a guarded column — this ships with no server change | WEB-5, BIL-3, FS-2, CR-9, MOB-5, LEAH-7 — 6 for, 0 against | | |
| **HT-12** | **R19** (billable corollary) | Does one server-owned rule resolve `billable` for every `source`, and is the reason printed on the row? | **Yes.** Keep fail-closed; make it consistent; print "non-billable · no agreement" | FS-1, CR-9, WEB-6 for; none against | | |
| **HT-13** | NEW | Is backdating allowed, and how far? | **Yes** — a date field on the add row and the ⌘K verb; default to the paged week. Kody sets the bound | FS-11, WEB-2 (the only seats that found it); unopposed | RULED — Any date, until the entry is invoiced; entries older than 30 days carry a quiet "backdated" mark. | 2026-09-11 |
| **HT-13-a** | **HT-13** (corollary, orchestrator ruling) | An entry logged with a DATE and no time carries no time of day — which instant is stored, and which day does the ledger then report? | The portal filed the named day at the member's LOCAL time of day, so from 19:00 CDT onward her own Hours list said Sep 1 while `time_entry_ledger.day` — and with it the scope lens, the CSV and the statement — said Sep 2 for the same hour (W7-R3-02 / W7-R4-05) | Orchestrator, resolving the review's carried major; no seat opposed | RULED — An entry logged with a DATE and no time (the Hours add row, the ⌘K verb, the Field sheet when backdated) is filed at **12:00 UTC of the named day**, so `(started_at AT TIME ZONE 'UTC')::date` equals the day the member named in every studio timezone between UTC−11 and UTC+11. The ledger's `day` column stays UTC-derived; **no studio timezone column is added in this program.** *(Shipped in W7: `startedAtFromDateValue` (`apps/designer-portal/src/components/document/time-capture.tsx`) returns noon UTC and is the one helper both date-only doors go through. Pinned by `supabase/tests/billing/time_entry_ledger_test.sql` case (f) — the ledger's day equals the named day for a noon-UTC entry, at both edges of the band — and by `apps/designer-portal/src/components/document/__tests__/time-capture-date.test.ts`.)* | 2026-09-13 |
| **HT-14** | NEW | Does the mobile manual form get a project picker, or is it disabled when nothing is held? | **Either, but never a silent success** | CR-1, FS-24, WEB-3 | | |
| **HT-15** | NEW | May `project_id` be NULL for non-billable internal time (rather than a sentinel "Internal" project)? | **Yes — and reprice it.** Additive per O3/R4; the sentinel is correctly rejected (it would pollute every list, roster and invoice path) | 5 for (BIL-4, CR-8, REP-5, LEAH-5, FS), 0 against | | |
| **HT-16** | **D10** | Is D10 **enforced** on Patina Field rather than amended? | **Enforce.** D10 binds every surface that proposes a duration, not only the desk. A stepper on the offer, defaulted to *active* duration | CR-2, MOB-2, LEAH-2, FS-28 — 4 for, 0 against | | |
| **HT-17** | **R64** | Does the abandonment bound bind **any clock-derived duration** (Field included), and does it ever fire on *cumulative* idle? | **Scope: yes** — extend the wording, keep the 30-minute number; Field's data is free (Specimen timestamps already sorted). **Cumulative: not yet** — R64 is "watch with data" and no data has been watched; instrument `time_timer_stopped` with the cumulative-to-raw ratio first | CR-12 extend scope; MOB-3 prefers a soft >4h review gate over new idle sensing; VET-20 found the cumulative hole but recommends instrumenting before relitigating | | |
| **HT-18** | NEW | Does Field get a `LogTimeSheet` reachable with no visit — a 7th Browse tile and the **expanded** companion state? | **Yes.** Collapsed strip carries one action and it is "End visit" — Invariant V holds | MOB-1/11, VET-4/8, LEAH-1, OPS-10, FS-29 — 6 for, 0 against | | |
| **HT-19** | NEW | Does the `activity` CHECK gain a **travel/drive** value? | **Yes** — and it must be reachable hands-free before the truck moves, not after | LEAH-1 (the only seat that walked the day); unopposed | | |
| **HT-20** | NEW | Is per-entry CSV the export, pulled forward rather than last? | **Yes**, from the fact view, on the `qbo-export` blob pattern — gated on the view repair (HT-6) | BIL-5/6, REP-8, VET-17, LEAH-14, CR C21 — 5 for, 0 against | | |
| **HT-21** | NEW | Does the client's invoice gain a dated sub-table, and does the composer name the person? | **Composer: yes, name the person.** **Client line: one priced line plus a dated sub-table** | CR-7/C26 for the sub-table; LEAH-15 and REP-15 say the client needs no staffing detail and a *statement* is the thing to send. §9 | | |
| **HT-22** | NEW | Does an `owner`/`admin` UPDATE+DELETE policy exist on time entries? | **Yes — exactly one write widening**, via `is_org_admin_or_owner`, audited | FS-40, VET-11, OPS-13, BIL §5 | | |
| **HT-23** | NEW | Do edits leave a trace (`updated_by` + an audit row)? | **Yes** — nearly free at the RPC boundary being built anyway, expensive to retrofit | VET-11, BIL-10 | | |
| **HT-24** | **D10** (second amendment) | Is `activity` recorded rather than defaulted — and is it ever made required? | **Recorded, never required.** Print "activity not set" honestly; the zero-tap path must keep working | FS-12 proposes both and recommends the non-required form; CR-28, FS-27 protect the zero-tap path | | |
| **HT-25** | NEW | May a studio member log time on a project they are **not rostered to** — auto-roster on first log, or is a roster seat required? | **Decide explicitly.** The requirement as written currently fails | FS-17 (only seat that read the INSERT policies); unopposed | RULED — Auto-roster as support_designer on first log; the seat is visible on the roster and removable by the owner. | 2026-09-11 |
| **HT-26** | NEW | Does `pending_authorization` become a doorway, and does an unresolved rate print "rate pending" instead of a blank? | **Yes to both.** Wire the badge to the already-shipped authority band / People rate | WEB-9, REP-6 | | |
| **HT-27** | NEW | Do the PostHog events ship **before** any iOS extension wave? | **Yes.** The widget/intent decision must rest on `time_entry_logged` by surface, not on taste. `time_rate_unresolved` is the alarm | CR-25, OPS-16, VET, FS — 4 for, 0 against | | |
| **HT-28** | NEW | Does the Siri/App-Intent path ship before a background drain exists? | **No** — either the intent attempts the network with the outbox as fallback, or a background task ships first | FS-31 (blocker), MOB-13 (flag widget-origin entries); CR-16 unbundles intents from the widget | | |
| **HT-29** | **VISION §6** / NEW | Is the Desk `hours` card a studio week total for admins, or act-bearing only? | **Act-bearing or nothing** — unbilled with "Bill it", or a timer still running from yesterday | CR-15 and OPS-15 against a bare total; LEAH-9 wants the passive glance; the draft proposes the total. §9 | | |
| **HT-30** | **VISION §6** | Is §6 amended for this program, or **strengthened**? | **Strengthened, not weakened:** *"A total is permitted as the front matter of the rows that produced it; a total with no rows beneath it is a dashboard."* | CR-27 strengthen; FS-121 narrow challenge (a typographic table is not a dashboard) — the two are compatible; REP-11, VET-14, WEB-11, MOB-8 all hold the line | RULED — Amend VISION §6 with an explicit exception for ledgers (the Hours sheet is a permitted reporting surface); record as a V-entry in VISION-DECISIONS.md. | 2026-09-11 |
| **HT-31** | **R69** | Does a Live Activity ever tick a running clock on the lock screen? | **Affirm R69, strike the Live Activity.** If a lock-screen surface ever ships: minute resolution, no elapsed animation | CR-13 strike; MOB-5 raises ActivityKit's native tick and **recommends its own fallback**; LEAH: "I'd forget it" — 3 against, 0 for | | |
| **HT-32** | **D9** | Is D9 amended to *"capture belongs wherever the work happened; review belongs only in the drawer ledger, never a page"*? | **Amend.** The current wording is at once too narrow to authorise ⌘K / Field / intent capture and too weak to forbid the `/hours` page someone will propose | CR-18 amend, FS-120 affirm-with-carve-out; WEB and OPS prefer framing the new doors as accelerators *into* the ledger's existing add row — same outcome, no new act type | | |
| **HT-33** | **VISION §1** | Is Patina Field recorded as **The Document off-desk** rather than a fourth surface? | **Yes** — one dated `VISION-DECISIONS` entry, zero code | CR-17 (only seat to run the §8 test literally); unopposed | | |
| **HT-34** | **§4 / §6** (nudges) | Does an unlogged-day nudge exist at all? | **Ship rule (a) only** — a running timer > 8 h writes one quiet R82 Record row, no push, no email. Rule (b) opt-in, **weekly at most, dark by default, never daily** | (b) opt-in weekly: FS-W6, VET-22, LEAH-19, MOB-12 — **4 for**. CR-14: do not build it, not even the setting — **1 against**. Daily, any framing: 0 for | RULED — Running timer > 8 h writes one quiet Record row only; the unlogged-day reminder is built opt-in per member, weekly at most, behind a flag that ships off. | 2026-09-11 |
| **HT-35** | **R19** | Is auto-start disclosed to a new member, and may a member opt out? | **Disclose: yes** — a one-time dismissible sentence on a new member's first document open. **Opt-out: yes**, per member, default on, on their own profile | LEAH-16/17 (disclosure + a stricter manual-start fallback for post-ruling hires), CR-11 (per-member opt-out) — 2 for, 0 against | RULED — Disclose once on a member's first document open; per-member opt-out on their own profile, default on, falls back to one-tap manual start. | 2026-09-11 |
| **HT-36** | **R77** (amendment default) | Does the member scope default to **aggregate**, with free-text notes behind an explicit detail act? | **Yes.** And the rollup's return shape is frozen at buckets, minutes and money — **never `notes`** — asserted per role in the SQL test | LEAH-18 ("I'd resent it as proposed"), CR-20 — 2 for, 0 against | RULED — Aggregate by default; notes only behind an explicit detail act; the rollup's return shape never includes notes, asserted per role in SQL. | 2026-09-11 |
| **HT-37** | NEW | `useStudioTimeReport`: **delete or wire?** | **Panel splits — Kody rules.** Eight seats say delete and replace with a server rollup; FS says the existing hook is correct, RLS-clean, carries member names, needs ~40 lines for a per-member group-by, and calls its deletion "the single biggest avoidable cost in the program" | Delete: OPS-4, BIL-12, VET-23, WEB-13, REP-2, LEAH-21, CR C27, draft. Wire: **FS-5** | RULED — Delete useStudioTimeReport; build a server-side rollup RPC. | 2026-09-11 |
| **HT-38** | NEW | Is the studio rollup **SECURITY DEFINER** or **INVOKER**? | **INVOKER.** The draft's stated reason for DEFINER is false — the rows are already co-member-readable (§1-6) — and its own signature has no `p_studio_id`, so the assert it promises cannot be written | FS-4 (decisive, with both proofs); CR-5 consistent; the draft proposes DEFINER | PANEL DEFAULT ACCEPTED — the rollup RPC is SECURITY INVOKER (Kody did not overrule). | 2026-09-11 |
| **HT-39** | NEW | `project_phases.estimated_hours` — **re-home or drop?** | **Rule it either way; do not leave it.** Its only writer has zero callers, so it is a budget you can read and never set. And a *second* estimate column already feeds the ledger's live readout (`project_tasks.estimate_minutes`) | FS-20 ("leaving a read-only budget is the worst of the three"), CR-22 drop, LEAH-24 wants phase budgets, REP-14 demands the two columns be reconciled first | | |
| **HT-40** | NEW | Is the billing-state chip a §6 "badge"? | **Keep it as built** — a 1px-bordered text pill with no fill is a table's state column, not a status badge. Do not add colour-coding when the studio scope multiplies rows | REP-11 raises and recommends keeping; unopposed | | |
| **HT-41** | NEW (follows HT-4) | When one member holds two roster roles on a project, which rate prices the entry? | — | — | RULED — The member picks the role per entry via a role chip shown only when they hold more than one role; the entry records the role in the **`(rate_source, rate_role)` pair**. *(Ruled text amended 2026-09-11 after W1 review round 1, finding W1-R1-13 — it previously read "records the role in `rate_source`". `rate_source` is CHECK-constrained to provenance (`authority` / `studio_member` / `profile_default` / `none`) and cannot also carry a role, so `00600` records the provenance in `rate_source` and the role in a separate `rate_role` column with its own CHECK over the four billable roster roles. Amended so a later reader does not "fix" the schema back; the ruling's substance — the member picks, the entry records it — is unchanged.)* | 2026-09-11 |

## Gating

Rulings that gate the first line of code (HT-1, HT-4, HT-10, HT-37):

- **HT-1** — Does the server own `hourly_rate_cents` on **every** project kind, overriding any value a client sends?
- **HT-4** — Does a rate-card row bind to a **person or roster role** instead of a free-text label?
- **HT-10** — Is `time_entries_studio_read` **narrowed** to owner/admin, or does a plain `member` keep the teammate read 00316 already grants?
- **HT-37** — `useStudioTimeReport`: **delete or wire?**

Three consent rulings (HT-35, HT-36, HT-30):

- **HT-35** — Is auto-start disclosed to a new member, and may a member opt out?
- **HT-36** — Does the member scope default to **aggregate**, with free-text notes behind an explicit detail act?
- **HT-30** — Is §6 amended for this program, or **strengthened**?

## Sub-rulings and owed rulings

| ID | Parent | Question | Recommendation | Raised by | Ruling | Date |
|---|---|---|---|---|---|---|
| **HT-10-a** | HT-10 (sub-ruling) | The 00484-registered rostered read grants members every row on their projects; HT-10 (aggregates), HT-36 (no notes) and HT-38 (INVOKER rollup) cannot all hold for the project aggregate — which gives? | Narrow the rostered read to own rows; one small SECURITY DEFINER `project_hours_total(p_project_id)` returning minutes / billable_minutes / amount_cents only, asserting `is_project_team_member`, tested per role; studio rollup stays INVOKER | architect (plan-v2), Fable | RULED — narrow to own rows + one DEFINER project-total function (as recommended). **AMENDED 2026-09-12 (W1 review round 9, W1-R9-01): the narrowing is TWO policies, not one.** This ruling's text names only the 00484-registered rostered SELECT policy (`Team can view their project time entries`), but `project_time_entries` carries a second, wider read: `time_entries_studio_read` (00316:237-240), `is_studio_comember(p.designer_id)` with no `user_id` leg, which grants every active non-guest studio co-member every row of every studio project — notes and, since W1, each member's per-person studio rate included. Measured in round 9: a plain `member` reads 0 rows of a colleague's `studio_member_rates` row (RLS denies her) and the identical `15000 / studio_member` off that colleague's hours row. W1 closed the WRITE half in `00601` (nobody but the work's studio owner/admin may write a `user_id` that is not their own — case (ab) of `time_rate_resolution_test.sql`); **W2's 00606 must narrow `time_entries_studio_read` as well as the 00484-registered policy**, or the confidential number stays readable by the whole studio however narrow the write half is. Per-role SQL coverage accordingly: owner/admin · rostered member · plain studio co-member · non-member. | 2026-09-11 (amended 2026-09-12) |
| **HT-25-a** | HT-25 (sub-ruling) | `00597`'s auto-roster seat is re-seated on the member's next log after the **owner removed it** (`ON CONFLICT … DO UPDATE SET removed_at = NULL`), with no audit row and no notice to the owner. HT-25's ruled text says the seat is *"removable by the owner"* — does removal **stick**, or is it a soft dismissal the member's next hour undoes? **Second half, measured in review round 2 (finding m2): removal of a non-`support_designer` seat re-seats the member as `support_designer`, a role they never held, which the classifier then uses as the rate role.** The `ON CONFLICT (project_id, user_id, role)` arm can only match a row of the role the INSERT proposes, which is always `support_designer` — so a removed `vendor` / `bookkeeper` / `lead_designer` seat is left standing as a tombstone and a **second**, live `support_designer` row is added beside it (probed: `seats=2 roles=support_designer(live),vendor(removed)`). The classifier's `count(DISTINCT member.role) = 1 … WHERE removed_at IS NULL` read correctly excludes the tombstone, so the entry rates at the **Support designer** rate-card row instead of **Vendor**, and `v_project_roster` / the Call Sheet list them as a support designer. | Decide explicitly. If removal must stick, delete the `ON CONFLICT` re-seat arm entirely and let the insert fail `Team can log their own time entries`, surfacing a *"you're not on this project any more"* affordance in W3. If the re-seat stays, it is a stated behaviour Leah can be told about, not a surprise she discovers. **Built as re-seat** (the plan's own test list demands it), asserted in `supabase/tests/rls/time_entry_auto_roster_test.sql` case **(d)** (same-role reuse) and case **(i)** (different-role re-seat — added round 2; before it, this behaviour passed **no** assertion at all). Related and already closed in review round 1: the project's **own designer** is never seated (`00597`'s early exit, case (f)) — she is `lead_designer` to the classifier (`00578:2708-2710`) and her RLS does not come from the roster. | review round 1 (W0-11); second half review round 2 (m2) | **OWED — not ruled.** Shipped as re-seat, including the cross-role form; a ruling the other way is one deleted `ON CONFLICT` arm (plus, if the cross-role form alone is to go, a `removed_at IS NOT NULL` pre-check that refuses rather than inserts). | — |
| **HT-6-a** | HT-6 (sub-ruling) | `00596`'s "one rate source" is a **repair** for authority-rated rows and a **write-down** for legacy ones. An un-invoiced, authorized, billable, rate-less entry on a project carrying `change_order_terms.hourly_rate_cents` reported `resolved_rate` = that rate and `amount` = duration × that rate through `00412`'s chain; after `00596` it reports **0 / 0**. Measured on the isolated stack with a fixture that is exactly what ships today (project `change_order_terms.hourly_rate_cents = 17500` — `DEFAULT_TERMS` in `change-order-terms-editor.tsx:27`, carried into `projects.change_order_terms` by `activate_proposal_as_project` — plus a 120-min billable entry with **no** `hourly_rate_cents`, which is all `useCreateTimeEntry` can send): **before 00596** `resolved_rate=17500 amount=35000`; **after** `0 / 0`. Three live-money consequences: the Hours ledger's unbilled balance and every row's dollar figure; `useStudioTimeReport`'s studio-wide `unbilledAmountCents`; and the invoice composer's time line, which prints `formatCurrency(entry.amount_cents)` and then `claim_time_entries`-stamps + invoice-locks the rows at **$0**. HT-1/HT-6 cut the change-order leg for rate resolution on **new** entries and W1 gives new entries a server-owned rate — nothing gives these existing un-invoiced rows one, and P-4 forbids the backfill that would, so after W1 they stay at $0 permanently. (The other cut leg, `profiles.default_hourly_rate_cents`, is free: verified to have **no writer anywhere** — apps, packages, services, `supabase/functions` — and 0 rows set on Strata.) Does `00596` stand as shipped, or do these rows keep their amounts? | **Two candidate answers, both governance not code.** (a) Stamp `hourly_rate_cents` once on exactly those rows from the project's change-order rate — which **PRESERVES** the amount rather than re-rating it, arguably what P-4's *"unbilled history keep their amounts"* requires rather than what it forbids. (b) Accept $0 explicitly, with Leah told before the deploy. **Strata exposure, measured read-only 2026-09-11:** 88 entries total; 14 unbilled + authorized + billable + completed; **4** of them rate-less; 8 projects carry a change-order rate; **exactly 1 row intersects** — 60 min, `source='manual_entry'`, 2026-08-03, on *"Kodys Test Project"* (designer = Kody), `$175/h` → **$175.00**. The other three rate-less rows sit on projects with no change-order rate and read 0/0 both before and after `00596`. So the live write-down today is one row on a test project, not a studio's money — which makes (b) cheap, but does not change the mechanism for any row logged between now and W1's resolver. Shipped behaviour (0/0) is pinned as case **(d)** in `supabase/tests/billing/time_unbilled_view_repair_test.sql`, with the pre-`00596` figures named in the assert messages, so ruling (a) flips two asserts and nothing else. | review round 2 (B1) | **OWED — not ruled.** Shipped as the write-down. | — |
| **HT-6-b** | HT-6 / HT-1 (sub-ruling) | `00601`'s delta 2 repairs the no-rate branch (`00578:2765-2772`) so a services hour with no matching signed card carries the **studio** rate and stays `pending_authorization` instead of being NULL-stranded. The plan's Done-when #4 and `00601`'s first banner both said that a later signed addendum could then **promote** it. **Measured false in W1 review round 1 (finding W1-R1-04):** every promotion loop in the lineage (`00412:1138 → 00414:913 → 00475:892 → 00511:4596 → 00566:843 → 00575:1845 → 00578:6584-6602`) JOINs `project_billing_authorities ON prior_authority.id = entry.billing_authority_id` and requires `entry.authority_rate_id IS NOT NULL`; the repaired branch sets **both to NULL**. Probed: the row reads `rate=15000 amount=30000 state=pending_authorization src=studio_member billing_authority_id=NULL authority_rate_id=NULL` and the real promotion predicate selects **0 rows**. So the repair delivers honest money on the ledger, the studio balance and the invoice composer — the row is no longer a blank — but an addendum signed later will walk straight past it, and it stays `pending_authorization` for ever unless someone edits it by hand. Does it stand as shipped, or must a signed addendum adopt these rows? | **Two answers, both governance.** (a) Add a promotion arm for UNBOUND `pending_authorization` rows carrying `rate_source='studio_member'`, so a later signed addendum adopts them — which means editing `_countersign_design_services_agreement_impl`, the signed-document ceremony, and re-deriving it at every future graft. (b) Accept non-promotability explicitly: the row prints its money honestly, and a studio that wants it authorized re-logs or re-rates it. **Shipped as (b).** Pinned as `time_rate_resolution_test.sql` case **(c4)** — an assert that reads 0 and whose failure message says that a 1 means HT-6-b was ruled the other way, so the banner in `00601` and Done-when #4 must move with it. | W1 review round 1 (W1-R1-04) | **OWED — not ruled.** Shipped as (b), non-promotable, with the claim retracted in `00601`'s banner, plan-v2 §2 and Done-when #4. | — |
| **HT-3-a** | HT-3 / HT-1 (sub-ruling) | **Which studio may price an hour on a project whose `studio_id` is NULL?** `activate_proposal_as_project` never sets the column itself, so 5 of 6 seeded projects reached `00599`'s studio ladder rather than a named studio. (Measured in review round 8 and corrected here: the *live* shape is not the same as the seeded one — `set_project_studio_id` at head **00563** already resolves `studio_id` on every live creation path and RAISES `studio_id_not_designer_studio` rather than leaving it NULL, so since 00563 the NULL rows are seed/migration-context rows and legacy rows. The ladder was still the path for all of those.) Five review rounds each closed one manufacturable tiebreak and shipped the next (member count → row authorship → `created_by` re-stamp → a second account seated `admin` in her own workspace → a plain `member` seat in a SECOND account's workspace decided by a backdated `joined_at`, W1-R6-01). The ladder's residue, which **no ordering of its keys can remove** (W1-R6-02, measured deterministic 3/3 with one extra signup): when no studio that employs her has ever written a `studio_member_rates` row for her, the first key is false for every candidate and the ladder falls to "a rate exists here at all" — which her own auto-provisioned workspace always satisfies, because `studio_member_rates_admin_insert` asks only for `is_org_admin_or_owner(studio_id)` and `00295` makes her its owner. So a rate the **subject** set about herself prices the client's hour at any number, satisfying HT-1 ("the server owns `hourly_rate_cents`") and HT-3 ("owner/admin only") in letter and defeating both in intent. The alternative is `rate_source='none'` — which **HT-26 rules a display state** ("rate pending", not a blank) but which reaches the invoice composer and `claim_time_entries` as **$0** until lane B/W2 excludes or flags those rows. W1-R4-02 demanded the current behaviour as a money bug in the other direction; `rulings.md` carries no row for the question itself. | **Two answers, both governance.** (a) Record the rule as built: *on a project with no `studio_id`, a studio in which the subject is an owner or admin may price her own hours when no other studio has* — the solo designer's only workspace is her own, and the composer, not the resolver, is where a `'none'` row must be kept off an invoice. (b) Require the winning studio to be one she does not run: add `AND membership.role NOT IN ('owner','admin')` to `00599`'s candidate query as a **WHERE**, not an ORDER BY key, with an explicit fall-through to `'none'` — one edit, which re-breaks cases (s), (p) and (w) as written, so those asserts move with the ruling, and which bills $0 on every hour a studio has not priced. **Shipped as (a).** Pinned in `supabase/tests/billing/time_rate_resolution_test.sql` case **(s)** (18000, her own workspace), whose `s2` failure message names this ruling, and recorded in `00599`'s round-6 banner. **CORRECTED 2026-09-12 (W1-R7-01, measured 3/3 with a negative control):** the sentence that stood here — *"the caller-writable tiebreaks are separately closed by W1-R6-01 … `organizations.created_at` is server-set because `organizations` has no INSERT policy for `authenticated`"* — was FALSE. The table carries an UPDATE policy for org admins (`Org admins can update organization`) with no column guard on `created_at` (`guard_organization_admin_columns` freezes only status / type / subscription_tier / subscription_expires_at / business_verified / business_verified_at), so one `UPDATE` backdated a puppet workspace and the same $1,998.00 came back. Round 7 proposed freezing that column; the ruling below makes the freeze unnecessary instead, because no pricing key reads it any more. The backdate itself is still permitted and is asserted as permitted-but-inert in case **(x)**. | W1 review round 6 (W1-R6-02) | **RULED 2026-09-12 — the project's studio prices the hour** (`projects.studio_id` → a studio the project's DESIGNER holds as an active `owner`, preferring one holding a `studio_member_rates` row for the member and then the oldest owner membership → `'none'`); the member's own memberships, seat dates, org creation and `organizations.created_at` never enter the ladder; `projects.studio_id` is stamped at project INSERT by the same rule (**00602**, additive trigger, no backfill — P-4). Neither (a) nor (b) as drafted: (a) is kept only where the member IS the project's designer (a solo practitioner prices her own work), and (b)'s "a studio she does not run" is replaced by "the studio that owns the work". Built in round 8: `00599`'s ladder rewritten, six rounds of keys and their postconditions deleted, `00602` added, and every rounds-4-to-7 exploit fixture kept as a negative case in `time_rate_resolution_test.sql` — (p) (r) (s) (t) (u) (x) — plus new (y) (stamping) and (z) (the project's column is final). | 2026-09-12 |
| **HT-3-b** | HT-3-a / HT-1 / HT-3 (sub-ruling) | **Given HT-3-a, how may a studio price an hour on a project whose lead designer is not its OWNER?** HT-3-a's step 2 admits only studios the project's designer holds with `organization_members.role = 'owner'`, so a studio that has added its first designer — the program's own customer, and the default shape — can price nothing on that designer's projects. Measured in W1 review round 8 (W1-R8-01, 3/3 with a 2/2 negative control) and re-measured from scratch in its fix pass, with **no attacker, no manoeuvre, no extra signup and no forged column**: Leah owns studio `S`, seats her new designer `admin` and an assistant `member`, and prices both of them IN `S` through RLS on HT-3's own surface. The hire received her designer role BEFORE Leah seated her — the self-signup order — so `00295`'s `fc_provision_studio_on_designer` gave her a one-person workspace she OWNS, which is therefore the only candidate step 2 admits, what `00602` stamps, and what step 1 reads for ever. On the studio's client project: **arm A** — her own hour prices at the **99900 she set about herself** in her own workspace (`rate_source='studio_member'`, `billing_state='authorized'`, `rated_amount_cents=199800`), and `project_unbilled_time` reports `99900 / $1,998.00` into the invoice composer and `claim_time_entries`' invoice lock, while Leah's 20000 for her is ignored; **arm B** — her assistant's hour on the same project resolves `rate_source='none'`, NULL rate, and the view reports `0 / 0` although Leah priced him at 12000 in `S`. **Control, in the same fixture:** a second hire seated BEFORE her designer grant owns no workspace, `00563`'s one-candidate discovery stamps `S`, and both hours price correctly (20000/40000 and 12000/24000) — the whole difference is the order in which she signed up and was seated. Arm A is not a `'none'` row W2's composer can filter: it is an authorized `studio_member` row indistinguishable from a legitimate one, authorized by the only party being paid for it. So HT-1 ("the server owns `hourly_rate_cents`") and HT-3 ("owner/admin of the studio sets it") are defeated in BOTH directions for every studio with more than one designer. Not a deviation from HT-3-a — a consequence of it that its text does not contemplate. | **Three candidate answers; (c) is the only one that closes both arms without re-opening rounds 4-6.** (a) **Accept it and say so**: a project led by anyone but the studio's owner prices from that designer's own workspace; W2's composer refuses to claim `rate_source='none'` rows so arm B is visible rather than $0. Costs nothing, leaves arm A's $1,998.00 self-set hour live. (b) **Widen step 2 to active non-guest membership** with HT-3-a's two ruled preferences. One edit — and immediately manufacturable: `Org owners can insert members` needs **no consent from the invitee**, so an attacker seats the PROJECT'S DESIGNER in a workspace the attacker controls and the attacker's rate wins the preference key (the same INSERT rounds 4 and 6 used). The weaker variants — "prefer a studio with >= 2 active members", "prefer a studio she does not run", "prefer a rate she did not author" — are each satisfiable with one extra signup seating the designer (rounds 5 and 6 rated all three blocker-grade). Narrowing instead (refuse step 2 when the member also holds a seat in a studio she does not own) keys on the member's own memberships, which HT-3-a forbids, and hands any org owner a $0 denial-of-service on her hours through the same consent-free INSERT (W1-R8-12). (c) **Close the consent door first, then widen**: seats land `status='invited'` and only the named user may flip their own seat to `'active'` (a `BEFORE INSERT` guard plus a narrowed `Org admins can update members`), after which step 2 can safely widen to active non-guest membership with HT-3-a's two ruled preferences — which fixes both arms. `00603` is free and reserved for it. **The cost is product-wide and is the reason this is a ruling, not a patch:** every seating path in Patina has to be read against it — `00295`'s provisioning (a self-seat, so self-consent), studio invites and `accept_workspace_invitation`, the admin portal's seat adds, `00597`'s roster trigger's neighbours, and every RLS policy and helper that reads `organization_members.status` (`is_active_studio_member`, `is_studio_comember`, `is_org_admin_or_owner`) — plus a surface where an invited person accepts. It is also the door W1-R8-12's pre-existing denial-of-service uses, so it is worth ruling on its own account. | W1 review round 8 (W1-R8-01), re-measured in the round-8 fix pass | **RULED 2026-09-12 — employer studios first (exactly one → it), owned studios only when there is no employer (exactly one → it), otherwise 'none'; the owner fixes 'none' by stamping `projects.studio_id`.** Neither (a) nor (b) nor (c) as drafted: step 2's candidates are the studios where the project's DESIGNER holds an ACTIVE, NON-GUEST `organization_members` row, taken in two tiers — the EMPLOYER tier (`role <> 'owner'`) first, and only if she holds no employer seat at all the OWNED tier (`role = 'owner'`). Exactly one candidate in a tier prices; **any tier with more than one candidate is `'none'`**, which is what keeps (b)'s manufacturability out: there is no rate-existence, seat-date, org-age, member-count or `created_by` key and no `ORDER BY` anywhere in the choice, so the answer is independent of the member being priced. **CORRECTED 2026-09-12 (W1-R12-01, measured 1/1 end to end through `public.sign_proposal` with three controls in the same fixture):** this cell used to continue “and a consent-free seat aimed at a designer who ALREADY HOLDS an employer seat can only push it toward `'none'` (the pre-existing W1-R8-12 denial-of-service, reversible)”. That is FALSE on the path that creates real projects. `00603` deliberately stands aside when the employer tier is AMBIGUOUS (its OPEN SUB-QUESTION, below), so the question falls back to `set_project_studio_id`'s activation bridge (`00563:266-277`), which still ranks the candidates on `membership.joined_at NULLS LAST` then `membership.created_at` — ordinary caller-suppliable columns that `guard_org_membership_changes()` does not constrain. A member who seats the project's DESIGNER in a workspace she owns with a **backdated `joined_at`** therefore prices her own hour at her own number — `99900 / studio_member / 199800 / authorized`, `project_unbilled_time` `$1,998.00` — **even though the designer already holds a real employer seat**. Controls in the same fixture, each `12000 / studio_member / 24000` and stamping the employer studio: no manoeuvre; the same seat with `joined_at` NULL; the same seat at `status = 'invited'`. So “any tier with more than one candidate is `'none'`” holds in `00599` step 2 and in `00603`'s own two tiers, and **not** on the activation path. Pinned as built by case **(af)** of `time_rate_resolution_test.sql` (with its live-path control), whose failure messages name arm (c) and `00603`'s OPEN SUB-QUESTION and state the values either closure makes them take. NOT patched: clearing the column in `00603`'s ambiguous arm either refuses the client's signature or smuggles a NULL past `00563`'s fail-closed check (`00563:326-346`), and no code-only closure exists that does not key on the member being priced (HT-3-a forbids it) or re-introduce a ranking key among employer candidates (rounds 4-7 rated every such key blocker-grade) — restricting the bridge to the employer tier does not help, because both candidates in the measured shape ARE employer-tier seats. The two closures are rulings: **arm (c)**, already OWED and measured to close it completely, or a ruling that an ambiguous employer tier must **fail closed on the activation path**. **CORRECTED 2026-09-12 (W1-R11-01, measured 1/1 with a control):** where the project's designer holds NO employer seat, one consent-free seat moves her employer tier from EMPTY to exactly ONE, and that IS a number somebody set — including a number set by the member being priced herself. (c)'s consent door stays **OWED** on its own account, and it is now MEASURED rather than predicted: for a designer who holds NO employer seat (every studio principal, because her seat is `owner`), one consent-free `Org owners can insert members` INSERT makes a stranger's auto-provisioned workspace her sole employer tier, and that account can then price her assistant there — measured 1/1 in round 11, `99900 / studio_member / 199800 / authorized` on her own client's project with her own 12000 for him ignored. Pinned as built by case **(ad-i)** of `time_rate_resolution_test.sql`, whose failure messages name arm (c) and state the values they take when it lands. **CORRECTED 2026-09-12 — HT-3-b's sentence "a member can only push the outcome toward 'none', never toward a number she set" is FALSE as built, and it is false for the MEMBER BEING PRICED, not only for a third party (W1-R11-01, measured 1/1 with a negative control in the same fixture, on two surfaces, through RLS, with no second account).** What actually holds: a member can push the outcome toward `'none'` **or, when the project's designer holds no employer seat, toward a number the member set in an organization she OWNS**. The subject needs only what an ordinary designer signup already gives her — the workspace `00295` provisions at her designer grant. Two statements of hers, both through RLS and both allowed (`INSERT organization_members(the project's designer, W, 'member', 'active')` under `Org owners can insert members`, then `INSERT studio_member_rates(W, herself, 99900)` as W's owner) make W the designer's ONE employer candidate, and her own hour on that designer's project comes back `99900 / studio_member / 199800 / authorized` with `project_unbilled_time` reporting `$1,998.00` — on BOTH the stamped surface and a legacy `studio_id IS NULL` one — where the control in the same fixture reads `20000 / 40000`. Cases (x) and (aa8) cannot catch it: their second seat makes the tier AMBIGUOUS, and they create their projects AS POSTGRES, where an ambiguous tier IS `'none'` (`00599` step 2). **CORRECTED 2026-09-12 (W1-R12-01):** this cell used to add “(two employers → `'none'`)” without that qualification, and to call EMPTY → exactly ONE “the single transition that yields a number instead of `'none'`”. Neither is true of the LIVE activation path, where an AMBIGUOUS tier also yields a number — see the W1-R12-01 correction above and case (af). EMPTY → exactly ONE is the transition that yields a number on **every** path, including the postgres/seed one, which is what legs (ad-i)/(ad-ii) measure. Pinned as built by leg **(ad-ii)** of `time_rate_resolution_test.sql` (with its control), and NOT patched: every code-only narrowing either keys on the member being priced (HT-3-a forbids it) or was rated blocker-grade in rounds 5-6, and reading the owned tier first restores W1-R8-01. Arm (c) is the closure and is already OWED — this needs no new ruling, only that one closed. Built in round 11: `00599`'s step 2 and `00602`'s stamp rewritten identically, the round-11 deletions (the rate-existence preference and the `owner_seat.created_at` tiebreak) dissolving **W1-R10-02**, new postconditions pinning `role <> 'owner'` / the employer-before-owned order / exactly two `organization_members` reads / no ordering key, and every contradicted case in `time_rate_resolution_test.sql` rewritten rather than deleted — (aa) closes both arms (20000/40000 and 12000/24000) and grows legs for the legacy NULL-studio project, an ambiguous two-employer tier and the owned tier; (m) (p) (s) (n) (w) (x) (y) each name the ruling that moved them. **The cost, recorded:** a principal who also owns the workspace `00295` provisioned at her `is_designer` flip has two owned candidates and no employer, so she is priced by neither until a project names one — cases (n) and (w), with the repair asserted beside them. **ROUND-11 FOLLOW-UP (W1-R11-02, measured 1/1 end to end through `public.sign_proposal`):** as first built this ruling was INERT on the path that creates real projects. `00602`'s stamp fires after `set_project_studio_id` (00317 → 00511 → **00563**) and returns early on a non-NULL `studio_id`, and `00599` step 2 only runs when the column is NULL — but on every live path `00563` has already filled it, and its activation bridge sorts the designer's candidate studios `… (membership.role = 'owner') DESC …`, i.e. OWNER FIRST, the exact preference this ruling inverts. So a hire seated `admin` in her employer's studio who owns the workspace `00295` gave her had her client's signed project stamped with THAT WORKSPACE, her own hour priced `99900 / studio_member / 199800 / authorized` and her assistant's `'none' / $0` though his employer priced him 12000 — W1-R8-01 arms A and B both alive, on the program's own customer shape, with no attacker. Closed in round 11 by **`00603`**: a `BEFORE INSERT` flag trigger records per row whether the CALLER named `studio_id`, and `set_project_studio_id_owned` now overrides a value `00563` DERIVED (never one the caller NAMED — HT-3-c arm (a) untouched, case (ac) unmoved) when the employer tier holds exactly one candidate. `set_project_studio_id` is NOT redefined. Pinned by case **(ae)** (the project created by the client's signature: `studio_id = S`, 20000/40000 and 12000/24000) and by the amended section 2 of `supabase/tests/rls/00563_proposal_signing_multi_studio.test.sql`. **OPEN SUB-QUESTION, stated not inferred:** HT-3-b says an ambiguous tier is `'none'`, but on the activation path `'none'` is unreachable without either leaving `studio_id` NULL (which `00563`'s fail-closed check forbids for an authenticated caller) or refusing the client's signature. `00603` therefore leaves `00563`'s answer standing when the employer tier is empty or ambiguous. If "more than one is `'none'`" is meant to bind the activation bridge too, that IS an edit to the signing ceremony and needs ruling. **W1-R12-01 measured what leaving it open costs** (see the correction above): not a $0 denial-of-service but a member pricing herself at `99900` on her employer's client project, through a backdated `joined_at` the bridge still ranks on. The sub-question is therefore load-bearing, and EITHER of its two closures — arm (c), or an ambiguous employer tier failing closed on the activation path — closes it; arm (c) was measured to. | 2026-09-12 |
| **HT-3-c** | HT-3-a / HT-3-b (sub-ruling) | **Does a project whose designer NAMED its `studio_id` at creation price from that studio even when she owns it?** Step 1 reads a caller-supplied column. `set_project_studio_id` (00317 → 00511 → **00563**) bounds that column to the studios the project's lead designer actively belongs to — its authenticated-INSERT arm requires only `membership.role <> 'guest'`, never `= 'owner'` — so it constrains the SET and says nothing about WHICH member of it she names. Measured in W1 review round 10 (W1-R10-01, 1/1 with two controls, every write through RLS): Leah seats her designer hire a plain `member` of S and prices her at 20000; the hire, who owns the workspace W that `00295` provisioned at her designer grant, prices herself 99900 there; a NULL-aimed project INSERT is REFUSED by 00563 (two candidates), an INSERT naming W is ALLOWED, and her hour on it resolves `99900 / studio_member / 199800 / authorized` with `project_unbilled_time` reporting $1,998.00 to the composer. The three banner sentences claiming the guard's refusal "is what makes the column trustworthy as a pricing key" were therefore false. | **(a)** accept it — the named studio prices the hour, and W2's composer plus the studio settings page are where a suspicious `studio_member_rates` row is seen; **(b)** require a named studio to be one the designer does NOT merely own whenever she holds an employer seat, as a refusal arm in `00602` beside the stamp (never an edit to `set_project_studio_id`). | W1 review round 10 (W1-R10-01) | **RULED by the orchestrator 2026-09-12, arm (a): yes — a sole proprietor billing her own studio's client, not a member gaming her employer's books; step 1 stands as written. Flagged to Kody.** Pinned by new case **(ac)** of `supabase/tests/billing/time_rate_resolution_test.sql`, which performs the project INSERT through RLS as the plain-member designer and carries the LEG3 aimed-at-employer control (20000/40000); the retracted sentences are replaced in `00599`'s step-1 comment and `00602`'s banner. If arm (b) is ever ruled, `ac2` moves and a refusal arm lands in `00602`. | 2026-09-12 |
| **HT-3-d** | HT-3-a / HT-3-b / HT-3-c (sub-ruling) | **Who may perform the repair act `stamp_project_pricing_studio(p_project_id, p_studio_id)`, and which studio may it name?** HT-3-a's ruled remedy is one sentence — *"the owner fixes 'none' by stamping `projects.studio_id`"* — and W2 shipped it as two arms (the project's DESIGNER naming a studio in her own tier; or an owner/admin of a studio that already holds a project she leads and created). W2 review round 4 MEASURED both halves of that shape still reachable: the tier bound sat inside `IF v_designer_id = v_actor`, so it was a property of the ACTOR, and the designer authors the actor — she seats a second account she controls as `admin` in the one-person workspace `00295` provisions for her (consent-free), writes herself 99900 there, has that account stamp, and her next hour on the employer's legacy project prices at her own number with no employer able to read the project, the hour or the rate (**W2-R4-01**, MAJOR). Two notes asked the same question from the other side: may the MEMBER being priced choose which of her employers permanently owns the hours and the money (**W2-R4-02**), and does HT-3-c arm (a) reach a post-hoc stamp of a studio she owns while another principal is in it (**W2-R4-03**)? | Round 4's minimal fix (hoist an owner-seat refusal above the actor gate), or its stricter alternatives: apply the WHOLE tier test to (designer, p_studio_id) regardless of actor, or drop the owner/admin arm and land HT-3-b arm (c)'s consent door | W2 review round 4 (W2-R4-01 MAJOR, W2-R4-02 + W2-R4-03 notes; W2-R3-06 and W2-R2-04 ride along) | **RULED by the orchestrator 2026-09-12 — the stricter alternative: the stamp may name ONLY a studio inside the project designer's HT-3-b tier AT CALL TIME (the EMPLOYER tier — active, non-guest seats with `role <> 'owner'` — while that tier is non-empty; otherwise the OWNED tier), and the caller must be an OWNER or ADMIN of the NAMED studio. There is no other arm: a designer naming her own workspace while she has any employer is refused, and a confederate she seats as `admin` in that workspace is refused for the same reason (the workspace is not in the tier). Resolves W2-R4-01, W2-R4-02 and W2-R4-03 together. Flagged to Kody.** Implemented in `00606` section (4) with the tier bound applied to EVERY caller, pinned by a postcondition that refuses an actor-gated body, and by cases **(f)**, **(i)**, **(l)**, **(m)**, **(n)**, **(o)**, **(p)** of `supabase/tests/rls/time_entry_studio_stamp_test.sql`. **CORRECTED 2026-09-12 (W2 review round 6; W2-R6-01 measured 1/1 on a fresh fixture through RLS, W2-R6-02 re-measured independently, plus two new round-6 measurements):** this ruling's two closing sentences — *"a designer naming her own workspace while she has any employer is refused, and a confederate she seats as `admin` in that workspace is refused for the same reason (the workspace is not in the tier)"* — are NOT consequences of the rule above, and the attempt to make the first one true by code (round 5's bound **(e2)**: while the project's designer holds any employer seat, SHE may not name a studio she herself administers) was REVERSED in round 6. Why, measured: (1) **(e2) refused a caller this ruling admits expressly.** A designer who is an `admin` of an HONEST employer holds a seat with `role <> 'owner'` — inside the employer tier — and is an owner-or-admin of the studio named. With her second employer seat making the tier AMBIGUOUS her project is honestly `'none'`, which is the population the act exists for; `(e2)` refused her stamp and bound (a2) refused her employer's OWNER for want of a `created_by` sibling, so HT-3-a's ruled remedy reached **nobody** and the only escape measured was for her to INSERT a spurious project naming her employer so that her employer might then repair the other one (probe P1; pinned as new case **(s)**, with the round-5 body installed as a negative control and case (s) measured to abort under it). (2) **(e2) bought nothing.** The manoeuvre it addressed needs a SECOND ACCOUNT by construction — `transfer_studio_ownership` refuses `p_new_owner = auth.uid()` and requires an already-active member, so she cannot leave the OWNED tier alone — and with that account in hand the account she hands the title to simply makes the call (case (q) q8). (3) **No bound at this call site can close the second sentence, and two round-6 measurements say why:** the stamp succeeds with ZERO `studio_member_rates` rows in the studio and the number is written AFTERWARDS (so candidate (ii)'s rate-authorship test is vacuous here — measured: confederate stamps an empty-rate-card workspace, she then writes 99900, her hour prices `99900 / studio_member / 199800`), and the same taking needs no studio of hers at all (measured: an accomplice seats her `admin` in HIS org — consent-free — SHE writes her own 99900 there because `'admin'` satisfies `studio_member_rates_admin_insert`, he stamps, and her hour on a project an honest employer was pricing at 25000 comes back `99900 / 199800`). So "her own workspace" is not the shape; "a studio where a cooperating pair can set her number" is, and neither the tier's role test, nor ownership history, nor `organizations.created_by`, nor a temporal key (the tier's only witness is `organization_members.updated_at`, which any People-room edit moves) can see it. **What is therefore OPEN and owed, stated not guessed:** the only closure measured to work is a rule at the RATE rather than at the studio — *a `studio_member_rates` row whose `created_by` is its own `user_id` prices an hour only where that person is the named studio's OWNER* — which leaves HT-3-a arm (a)'s and HT-3-c's sole proprietor untouched, and still leaves the pair where the ACCOMPLICE writes the number (case (r), the already-owed HT-3-b arm (c) class). That is a W1 resolver rule and W2 did not guess it. Both open shapes are pinned as PASSING, loudly-labelled assertions: case **(q)** q7 (her own stamp) and q8 (the confederate's, on its own second project so it is a measurement and not a retry), and case **(r)**. If the orchestrator prefers instead to RATIFY (e2) — amending this ruling to record a third refusal and accepting that an admin-designer's ambiguous-tier project is repairable only through a colleague plus a sibling project — the restore is one IF (described in prose at bound (e2)'s site in `00606`, because a postcondition reads the function's source and forbids an actor-gated refusal), plus case (s) and that postcondition. TWO things the implementation adds to the ruling's text, both reported rather than assumed: (1) the W2-R3-01 `created_by` sibling leg is RETAINED for a caller who is not the designer — the tier is keyed on `organization_members`, so an outsider who owns any organization puts her own org INSIDE the designer's employer tier with one consent-free INSERT, and the tier bound cannot reach that BLOCKER (cases (d)/(k) measure it); (2) RESIDUE of the owned tier — where she holds no employer seat and owns TWO active studios, both are in the tier, so she may still name the workspace whose rate card she writes (asserted as case (o6) so the next ruling moves a test, not a surprise). | 2026-09-12 |
| **HT-3-e** | HT-3-a / HT-3-b / HT-3-c / HT-3-d (sub-ruling) | **Two measured MAJORs of W2 review round 7, pulling against each other.** (1) **W2-R7-01** — bound (a2) (round 3's retained leg: where the caller is not the designer, the studio named must ALREADY hold a project this designer both LEADS and CREATED) refuses the entire population the repair act exists for. Measured 24 calls out of 24 — four actors x three of her legacy projects x both her employers — every column left NULL: a designer whose book predates `projects.studio_id` has NO project naming any studio, so HT-3-a's ruled remedy ("the owner fixes 'none' by stamping projects.studio_id") reached NOBODY on the ordinary hire (a plain `member` of two employer studios, the shape HT-3-d's own text singles out). (2) **W2-R7-02 / W2-R6-02 / W2-R5-01** — a designer's resolved rate still becomes a number SHE set, permanently, on projects an honest employer was pricing correctly; and **W2-R7-03 / probe P8** measured the ONE-ACCOUNT form: an `admin` of an honest employer satisfies `studio_member_rates_admin_insert`, writes her own 99900 on that employer's card in one statement, stamps that employer, and her hour came back `99900 / 199800` where the owner had written 26000. Three candidate bounds AT THE STAMP were measured and all three fail (a rate-authorship test there is vacuous — the stamp succeeds with zero rate rows and the number is written afterwards; the tier's only temporal witness is `organization_members.updated_at`, which any People-room edit moves; and the same taking works in an organization she has never owned). | Round 7's fix pass proposed: split the second half of standing by tier (an arm's-length rate in the employer tier, round 3's sibling in the owned tier), and put the authorship rule at the RESOLVER, where the hour is priced. | W2 review rounds 5-7 (W2-R7-01 and W2-R7-02 MAJOR, W2-R7-03 MINOR, W2-R7-06 and W2-R7-07 notes; W2-R6-01/02 and W2-R5-01/02 ride along) | **RULED by the orchestrator 2026-09-12, in three parts. Flagged to Kody.** **(1) THE STAMP'S EMPLOYER ARM** (resolves W2-R7-01): an owner or admin of a named studio may stamp a legacy project when that studio is in the designer's HT-3-b tier AND the designer holds an active non-guest seat there AND at least one `studio_member_rates` row for her in that studio was authored by someone other than herself (`created_by <> user_id`). The sibling-project bound **(a2)** is DROPPED for the employer tier; it stays only for the owned tier (HT-3-c's sole proprietor). **(2) SELF-AUTHORED RATES** (resolves W2-R7-02 and W2-R7-03): in the W1 resolver, a `studio_member_rates` row whose `created_by` = `user_id` prices an hour ONLY where that person is the named studio's OWNER (`organization_members.role = 'owner'`). Elsewhere such a row is ignored and the next qualifying row (authored by someone else) prices; if none, `'none'`. **(3) ACCEPTED RESIDUAL:** a designer who runs a SECOND account, transfers ownership of her provisioned workspace to it, and has that account author her rate there can still price her hours on a legacy project from that workspace after that account stamps it. Patina makes this VISIBLE rather than impossible: the pricing studio is a column of `time_entry_ledger` and the owner's project lens shows it (lane B, W2 UI). No further bound is added for it in this program. **RE-MEASURED UNDER HT-3-g 2026-09-12 (W2 round-11 fix pass) — HT-3-e(3) SURVIVES, unchanged in substance.** HT-3-g(3) admits only an owner/admin of a studio that EMPLOYS the project's designer, and `transfer_studio_ownership`'s last statement DEMOTES `auth.uid()` to `admin` (00484:524-536) — so the workspace she hands to a second account IS in her own employer tier, the account holding the title IS its owner, and the rate row that account authors for her IS arm's-length by HT-3-e(1)'s test. Measured green on the HT-3-g bodies: case **(q)** q13 (the account's stamp succeeds), q14 (hers succeeds once the row exists), q15 (`99900 / studio_member / 199800`), q16 (the honest employer reads 0 of the two projects taken). The answer stays VISIBILITY, not another bound.** Implemented 2026-09-12: part (1) in `00606` section (4) (bound (a1) computes both tiers, bound (a2) branches on them, with a postcondition pinning the split by source); part (2) in **migration `00615`** — `00599`/`00601` are already merged to `hour-tracking/integration` and W2's band `00604`-`00607` is fully used, so the rule lands at the number W5 had RESERVED AND NOT SPENT: **`00615` is now W2's; W5 mints NOTHING and `00616`-`00617` stay W6's** (corrected 2026-09-12 after W2-R8-04 measured this sentence and `plan-v2.md:25`/`:708` prescribing a collision — `plan-v2` is corrected in the same pass, and the only unspent number in the program's range is `00617`) (recorded here as the ruling asks). `00615` redefines `resolve_time_rate_cents` only, grafted verbatim from `00599` with one clause added to tier 2; `classify_project_time_entry_authority` is not redefined because it reads a rate in exactly one place, the resolver, pinned by `00601`'s own postcondition. Coverage: `supabase/tests/rls/time_entry_studio_stamp_test.sql` cases **(u)** (the ordinary hire: three refusals before the rate, the repair after — W2-R7-01 closed), **(v)** (the 24-call sweep, then the whole legacy book repaired — probe P7), **(q)** (q7/q8 now REFUSED, her hours 'none', the honest employer recovers the project), **(s)** (s3 the employer's owner now repairs; s8 probe P8 coming back at the employer's number), **(t)** (the residual below), and `supabase/tests/billing/time_rate_resolution_test.sql` case **(ag)** (five legs of part (2), including the OWNER exemption that keeps HT-3-c's sole proprietor billing, and the `created_by` NULL branch). Both directions were measured by INSTALLATION: with `00599`'s body installed by hand, case (ag)'s ag1 comes back `99900 / studio_member / 199800`; with round 6's `00606` installed by hand, an employer owner holding an arm's-length rate is refused `42501`. **TWO RESIDUES THE IMPLEMENTATION REPORTS RATHER THAN ASSUMES, both owed to HT-3-b arm (c)'s consent door:** (i) part (1)'s employer arm does NOT bound the **W2-R3-01 outsider**. An attacker who owns any organization seats the victim designer in it consent-free, writes her a rate row there under her OWN id — arm's-length by the test, because the test asks about the SUBJECT's authorship — and her stamp then succeeds in three authenticated statements (case **(t)**, asserted as passing and loudly labelled). Round 3's sibling leg did bound her; the ruling dropped it for this tier because it refused every honest employer too (24 of 24). The closure is the already-owed consent door, which makes `status = 'active'` mean consent and leaves the stamp no victim to seat. (ii) part (2)'s cost to an honest studio is one rate row: where a member's self-authored row has CLOSED the studio's own row (00598's append-only ladder), the hour is `'none'` — HT-26's "rate pending" — until the studio writes a new rate (measured as s8/ag1 + ag3). It is never her number. | 2026-09-12 |
| **HT-3-e(4)** | HT-3-e(2) (sub-ruling) | **W2 review round 8 (W2-R8-01, MAJOR, measured 1/1 through RLS) — HT-3-e(2) prices a `studio_member_rates` row on WHO AUTHORED IT, and the row's NUMBER was editable in place with authorship left untouched.** 00598 freezes a rate row's identity and dates and lets an actor re-stamp `created_by` only with her own id, but `hourly_rate_cents` stays writable on the open row (the blur-save HT-3 rules for, cases a2/b2/h6) and `studio_member_rates_admin_update` (00598:347) admits every owner/admin of the studio — the rate's own SUBJECT included. So an `admin`-designer of an HONEST employer rewrote the OWNER's open row from 25000 to **99900 with `created_by` untouched**; the row stayed arm's-length by BOTH of HT-3-e's tests, her own stamp was admitted by part (1)'s new employer arm, and her 120-minute hour came back **99900 / studio_member / 199800**. ONE account, two statements, no ownership transfer, no confederate, no consent-free seat — and `authenticated` holds UPDATE on the table, so it is a PostgREST `PATCH`, not a psql-only fact. It was also a **visibility** regression against part (3)'s answer: her number carried the OWNER's id as its author, so his own rate-card lens attributed it to himself. | Round 8's review put three candidates to the orchestrator and chose none: (1) stamp `created_by` with the acting uid in `guard_studio_member_rate_history` whenever `hourly_rate_cents` moves; (2) add `AND user_id <> auth.uid()` to the UPDATE policy; (3) accept it as a residual and at least measure it. | W2 review round 8 (W2-R8-01) | **RULED 2026-09-12 by the orchestrator's W2 round-8 fix pass, FLAGGED TO KODY — candidate (1), the guard. Authorship records who SET THE NUMBER THAT IS THERE:** in `guard_studio_member_rate_history`, when `NEW.hourly_rate_cents IS DISTINCT FROM OLD.hourly_rate_cents` and `auth.uid()` is present, `created_by` is stamped with the acting uid. HT-3-e(2) then reads a true answer and the rewrite prices exactly as the INSERT form already does — `'none'` (HT-26's "rate pending") until the studio writes a number of its own. **Why candidate (1) and not (2) or (3):** it costs an honest studio NOTHING (the settings page's blur-save already sends `created_by: userId`, i.e. this same value, so no shipped path changes), whereas the POLICY candidate would additionally refuse an admin-designer's in-place correction of her own row — a capability W1 shipped and HT-3 rules for — and the ruling-only candidate leaves the taking standing. It also repairs the visibility regression, because the number and its author now move together. **Landed as the second section of migration `00615`** (lineage `00598 → 00615`, 00598's guard body verbatim with one statement grafted; the trigger is NOT recreated, so its name still orders it before `close_prior_studio_member_rate_trg`). Three bounds of the rule, each measured: writes arriving as `postgres` (the DEFINER ladder, seeds, migrations) take 00598's early return and keep their authorship; the stamp requires `auth.uid()` to be present, so a service_role path with no JWT leaves the recorded author standing rather than writing NULL (NULL is the deleted-author value part (2) treats as arm's-length); and the stamp sits AFTER W1-R5-01's actor check, so a caller who forges `created_by` to a third party AND moves the number still RAISES rather than being silently corrected. Measured before and after on the program's own stack: `99900 / studio_member / 199800` with the owner named as author → `NULL / none` with her named as author. Tests: `studio_member_rates_test.sql` case **(l)** (the table contract, four legs) and `time_rate_resolution_test.sql` case **(ah)** (end to end, five legs, including the honest in-place correction still pricing and the sole proprietor editing her own rate). **Residual, recorded:** an owner/admin may still DESTROY a colleague's rate number in place (an availability question, not a taking — the studio's card is the studio's to write, `updated_at` and `created_by` now both name who did it); and part (3)'s accepted residual is unchanged. | 2026-09-12 |
| **HT-3-f** | HT-3-b / HT-3-c / HT-3-e(3) (sub-ruling) | **W2 review round 9 (W2-R9-01, MAJOR, measured 1/1 through RLS in two independent statement forms): HT-3-b's pricing studio is RECOMPUTED on every hour for a legacy project whose `studio_id` is NULL, and the designer can EMPTY her own employer tier with ONE ordinary statement — the shipped `Members can leave` DELETE on her own `organization_members` row (probe C), or, as an `admin`, `Org admins can update members` setting that row `status = 'removed'` (probe D2). The OWNED tier then opens under her former employer's legacy project and HT-3-e(2)'s OWNER exemption prices the number she wrote for herself: her next 120-minute hour came back `99900 / studio_member / 199800` where the employer's own card had priced it `26000 / 52000`, the employer's owner then read **0** of the project's 2 hours, and NO statement existed that could pin the column — before or after. Bound (c) of `00606` refused the employer's pre-emptive stamp BY DESIGN (*"only where the derivation is silent"*), and `set_project_studio_id`'s authenticated arm admits `TG_OP = 'INSERT'` only, so the column could not be written by hand either. ONE account, no ownership transfer, no confederate, no consent-free seat, no rate rewritten in anybody else's studio — expressly outside HT-3-e(3)'s accepted residual, which buys its exposure with a SECOND account and an ownership transfer.** | The review offered four closures and ranked them: pin the unambiguous answer (relax bound (c) to a confirm); bound the owned tier on `projects.created_by`; a seat-history bound (not schema-answerable today — `organization_members` rows are DELETED, not closed); or ruling-only, as a second residual. | — | **RULED by the orchestrator 2026-09-12, candidates 1 AND 2 TOGETHER, with a widened residual. Flagged to Kody. Three parts.** **(1) THE PIN.** Bound (c) of `stamp_project_pricing_studio` becomes a **CONFIRM, not a refusal**, when `p_studio_id` equals `project_pricing_studio_id(p_project_id)`: an owner/admin of the studio the derivation ALREADY names may stamp it, after which the column is final and no longer recomputed (HT-3-c's *"a stamp is final"* holds, and bound (b) is what holds it). **Every other bound stays** — bound (a)'s owner-or-admin standing, bound (a2)'s arm's-length rate (employer tier) or `created_by` sibling (owned tier), bound (d)'s seat test and bound (e)'s tier test all sit on either side of bound (c) and apply to a confirm unchanged; a caller naming a studio OTHER than the one pricing the work is refused exactly as before. It costs nobody anything (it names the studio that is already pricing these hours and already reading them) and it gives the employer a real remedy BEFORE the fact. **(2) THE OWNED TIER IS FOR SELF-CREATED PROJECTS ONLY.** In HT-3-b's derivation the OWNED tier is reached only where `projects.created_by = projects.designer_id`. A project somebody else opened for the designer is never priced by a studio she owns; where the employer tier is empty for such a project the answer is `'none'` (HT-26's "rate pending") and part (1)'s pin or HT-3-a's stamp is the repair. `created_by` is the one column in this shape she cannot author after the fact (`00563` RAISES on any UPDATE that moves it, its authenticated-INSERT arm admits only `NEW.created_by = auth.uid()` on a project she leads, and `reassign_project_lead` never touches it). **ONE implementation, called from each body that derives a pricing studio** — `public.owned_tier_prices_project(created_by, designer_id)`, defined in `00615` and called by `resolve_time_rate_cents` (lineage `00599` → `00615`), `project_pricing_studio_id` (lineage `00604` → `00615`, the callable form `00606`'s bound (c) and its owner/admin read policy both ask) and `set_project_studio_id_owned` (lineage `00602` → `00603` → `00615`; `00602` and `00603` are already merged to `hour-tracking/integration`, so neither file is edited and the trigger function is redefined forward from `00603`'s body, the grep winner). A predicate of the two columns rather than of a project id is the only shape all three can share: the INSERT stamp runs BEFORE the row exists. It creates no new refusal at creation time, because `set_project_studio_id` fires FIRST and its fail-closed check has already filled the column or raised. **(3) RESIDUAL, widened on HT-3-e(3)'s footing:** a designer who **CREATED a legacy project herself while employed**, then deletes her own seat or sets it removed, and then logs hours on that project, **prices from her own workspace**. Part (2) keys on `created_by` and `created_by` is hers, so it does not reach her. VISIBLE rather than impossible — the pricing studio is a column of `time_entry_ledger` and the owner's project lens shows it (lane B, W2 UI) — with part (1)'s pin as the employer's remedy before the fact. No further bound is added in this program. **Implemented 2026-09-12:** part (1) in `00606` section (4) bound (c) (with its postcondition rewritten to pin the `p_studio_id` comparison, because a confirm without it would be a licence to re-point a priced project); part (2) in `00615`, one shared body plus three grafts, each pinned by source. **Coverage:** `supabase/tests/billing/time_rate_resolution_test.sql` case **(ai)** (probe C and probe D2 end to end: `'none'`, never her workspace, with her own 99900 in place so a `'none'` is HT-3-f(2) and nothing else; plus the already-written hour not moving, P-4) and case **(aj)** (part (3), asserted as PASSING and loudly labelled); `supabase/tests/rls/time_entry_studio_stamp_test.sql` case **(e)** (the confirm succeeds, writes the column, is idempotent, and is then final — it measured a REFUSAL through round 8 and was rewritten, not deleted) and case **(w)** (what the pin BUYS, on part (3)'s own shape: two identical self-created legacy projects, one pinned, she leaves, the pinned one keeps pricing at the employer's 26000 while the unpinned twin moves to her own 99900, and the employer's owner reads 1 of the 2 hours rather than 0). **AMENDED 2026-09-12 — part (1)'s CONFIRM is the EMPLOYER'S ONLY; see HT-3-f(1) AMENDED, HT-3-f(4) and the HT-3-f(2) COST NOTE below (W2 review round 10).** **SUPERSEDED 2026-09-12 BY HT-3-g (Kody), in BOTH parts, and HT-3-f(3) is DISSOLVED.** (1) THE PIN/CONFIRM is DELETED from `00606`: with no read-time derivation there is nothing to confirm — `project_pricing_studio_id` IS `projects.studio_id`, so bound (b) ("a stamped project is final") is the whole of what bound (c) did. (2) HT-3-f(2) is DELETED, body (`owned_tier_prices_project`) and all: it gated a derivation that no longer happens, and it is deliberately NOT carried into `00620`'s one-off stamp, because the HT-3-f(2) COST NOTE's honest principal is precisely the row the one-off stamp must hand her own studio (leg (e) of `supabase/tests/billing/legacy_project_studio_stamp_test.sql`). (3) **HT-3-f(3) DISSOLVED, measured:** a legacy project she created herself while employed, after she deletes her own seat, prices `'none'` and stays `'none'` — case **(aj-ii)** of `time_rate_resolution_test.sql` (aj3 the derivation answers nothing, aj4 her hour is `'none'` where the residual's whole content was 99900, aj5 her own stamp of her workspace REFUSED 42501). What survives is **creation-time naming**, which is HT-3-c arm (a) and was never this residual: a project she opens after leaving names the one studio she holds and her own card prices it (aj1/aj2). And the residual's OUTCOME still exists for a project that predates the ship — `00620` hands it to the studio she owns when her tier answers one (aj6) — but she can no longer reach it by LEAVING A SEAT afterwards, which was the whole of the manoeuvre. | 2026-09-12 |
| **HT-3-f(1) AMENDED** | HT-3-f(1) (amendment) | **W2 review round 10 (W2-R10-01, MAJOR, measured 1/1 through RLS with a control twin in the same fixture and a negative control by installation): round 9's CONFIRM was ruled for W2-R9-01's EMPLOYER, and read literally it also reached the DESIGNER.** With her employer tier empty the derivation names the workspace `00295` provisions for her, so ONE statement of hers pinned that workspace onto her own legacy project, bound (b) made the column FINAL, and her hours there priced `99900 / studio_member / 199800` for ever — including after an honest employer hired her and priced her arm's-length (the control twin in the same fixture, same moment: `26000 / studio_member / 52000`; the employer read **0** of the pinned project's hours against **1** of the twin's; nothing could undo it). ONE account, no seat left or removed, no ownership transfer, no confederate, no consent-free seat, no rate written in anybody else's studio. The review also measured HT-3-f(1)'s own justification sentence — *"it costs nobody anything … and it gives the employer a real remedy BEFORE the fact"* — FALSE in the designer's hand: it costs the FUTURE employer the whole of the work. | The review ranked three closures: (1) `v_actor <> v_designer_id` on the confirm arm; (2) bound the confirm to the EMPLOYER tier; (3) ruling-only, as a third residual with a `w`-shaped case in her hand. | W2 review round 10 (W2-R10-01) | **AMENDED by the orchestrator 2026-09-12, closure (2). Flagged to Kody.** The CONFIRM arm of `stamp_project_pricing_studio` is available ONLY where the named studio is in the designer's **EMPLOYER tier** (she holds an active, non-guest seat there with `role <> 'owner'`) **and** the caller is an owner/admin of that studio (bound (a), unchanged). A designer confirming her own OWNED workspace is refused — the pre-round-9 flat refusal returns for the owned tier, message and all (*"a studio already prices this project's hours — there is nothing to repair"*, `22023`). **HT-3-c's finality at project CREATION is unchanged**: she still names her own workspace on a project she is creating; what she may not do is FREEZE a legacy project's derivation afterwards. It costs an honest sole proprietor nothing she has today — her own project already prices from her workspace with no pin (HT-3-f(3)), and where it reached `'none'` HT-3-a's stamp is still hers, because bound (c) only speaks when the derivation already answers. **Implemented 2026-09-12** in `00606` bound (c) (the single `IS NOT NULL AND IS DISTINCT FROM` test split into a different-studio refusal and an employer-tier gate on the confirm), with a postcondition that pins the gate's literal AND its POSITION — after the derivation read, before bound (d) — so it cannot be satisfied vacuously. **Coverage:** `supabase/tests/rls/time_entry_studio_stamp_test.sql` case **(y)** — the confirm REFUSED in the DESIGNER's own hand on her owned workspace (y2), her ordinary pricing unmoved (y3, 99900 from the derivation with no pin), the money the pin would have taken (y4, an honest employer hires her and her hour prices 26000 / 52000), and the employer's remedy intact (y5, its owner pins it); plus case **(x)** legs **x7/x8**, the confirm in the hand of a designer who is her EMPLOYER's own admin — admitted, and worth nothing at the money. Cases (e) and (w) are unchanged and still green. | 2026-09-12 |
| **HT-3-f(4)** | HT-3-f(1) / HT-3-e residue (i) (recorded residual) | **W2 review round 10 (W2-R10-02, measured through RLS in four statements): HT-3-f(1) widened HT-3-e residue (i) / W2-R8-02 from REVERSIBLE to IRREVERSIBLE.** The consent-free outsider could not reach bound (c) before round 9 — his own seat made the victim's employer tier answer and the flat refusal fired. The CONFIRM is exactly the door that opens for him, and his seat in HIS org IS an employer-tier seat, so the HT-3-f(1) amendment does not reach him either: he seats her consent-free (`Org owners can insert members`), writes her rate there under his own id (arm's-length *by the test*, which asks about the SUBJECT's authorship), confirms his org, and her later removal of the bogus seat undoes nothing — her next hour on her OWN legacy project priced `99900 / studio_member / 199800` where her own workspace had priced her 28000, he still read her hours with his seat gone, and she could not repair it (`22023`, the project already names a studio). | The review noted that closure (1) of W2-R10-01 would have bounded it and closure (2) does not, and that residue (i)'s owed consent door closes the FUTURE while a pin already taken stays taken — so the closure now needs a way BACK. | W2 review round 10 (W2-R10-02) | **RECORDED 2026-09-12 by the orchestrator as a residual, not closed in this program.** A consent-free seat by an outsider who then authors her rate and confirms his org remains possible until SEATING gains a consent door — **HT-3-b arm (c)**, owned by the People-room program, not this one. This program additionally records that an **owner-initiated UNPIN / re-derivation act is OWED with that door** ("ruling owed", not built here): a consent door closes the future, and `set_project_studio_id` forbids re-pointing by design, so a pin already taken needs its own act to undo. **Coverage:** `supabase/tests/rls/time_entry_studio_stamp_test.sql` case **(z)**, asserted as PASSING and loudly labelled — the seat and rate (z1), the derivation moving to his org (z3), his confirm SUCCEEDING (z4), the irreversibility after she removes the seat (z5/z5b), his number on her hour (z6), and her refused repair with the owed act named in the message (z7). If the consent door or the unpin lands, case (z) is the test that moves. Recorded in `00606`'s bound (c) banner and its `COMMENT` as well, so the next hand reading the migration meets it. **RE-MEASURED UNDER HT-3-g 2026-09-12 (W2 round-11 fix pass) — HT-3-f(4) SURVIVES, and is now the act's ONLY remaining money door.** The confirm arm it rode on is gone, and it simply moved to the STAMP arm: his consent-free seat in HIS org is an EMPLOYER-tier seat for her (the one fact HT-3-g(3) asks about the designer), he authored her rate row there under his own id (arm's-length BY THE TEST, which asks about the SUBJECT's authorship), and his stamp is admitted. Measured green on the HT-3-g bodies: case **(z)** — z3b his seat IS employer-tier, z4 his stamp SUCCEEDS, z5b her removal of the bogus seat undoes nothing (the column was never derived, so there is nothing to revert TO), z6 her hour prices his 99900 against her own 28000, z7 her repair refused. Two differences worth recording: the baseline is now `'none'` rather than her own 28000 (z0/z0b), so what he takes is a project nobody had stamped yet rather than one already priced; and the owed UNPIN is owed more sharply than before, because under HT-3-g a stamp is the ONLY writer of the column and there is no derivation to fall back to. Closure unchanged: HT-3-b arm (c)'s consent door, plus the owner-initiated UNPIN. | 2026-09-12 |
| **HT-3-f(2) COST NOTE** | HT-3-f(2) (cost note, no ruling changed) | **W2 review round 10 (W2-R10-03, measured, no attacker and no manoeuvre): HT-3-f(2) costs an HONEST studio something nobody had measured.** A studio PRINCIPAL — she owns her studio, holds no employer seat — who leads a legacy `studio_id IS NULL` project that her own ASSISTANT opened for her now prices `'none'` (HT-26's "rate pending") where before the rule her own studio priced her 31000 / 62000; her studio's own ADMIN reads **none** of those hours (00606's owner/admin read keys on the pricing studio, which is NULL, and `project_hours_total` answers `42501` for a non-rostered admin) and cannot repair them (bound (a2)'s owned-tier sibling leg asks for a project the designer both leads and created in that studio, and a principal's legacy book holds none). The money becomes "rate pending" with no event, and the one colleague who could notice is the one the read now excludes. | The review asked for (a) the shape as a case beside (ai)/(aj), (b) a lane-B sentence for the principal and an explanation for her admin's empty project lens, (c) the Strata count of `projects.studio_id IS NULL` split by `created_by = designer_id`. | W2 review round 10 (W2-R10-03) | **RECORDED 2026-09-12 as a MINOR COST, not a contradiction** — HT-3-f(2)'s own ruled sentence (*"part (1)'s pin or HT-3-a's stamp is the repair"*) HOLDS: the principal herself stamps her own studio (the owned-tier sibling leg is skipped where `v_actor = v_designer_id`) and her next hour prices 31000 / studio_member / 62000. **No DB change.** The test case is added as asked: `supabase/tests/billing/time_rate_resolution_test.sql` case **(ak)** — the shape (ak0), the NULL derivation (ak1), the `'none'` hour (ak2), the admin reading zero (ak3), the admin's refused repair (ak4), her own stamp succeeding (ak5) and the money coming back with the earlier hour keeping its `'none'` (ak6/ak6b, P-4). **LANE B OWES** the principal a sentence and her admin an explanation for an empty project lens. **STILL UNCOUNTED after ten rounds of asking:** the Strata population of `projects.studio_id IS NULL` split by whether `created_by = designer_id` — one read-only two-column query, and it is what sizes this note, W2-R10-01, W2-R8-02 and HT-3-f(3). | 2026-09-12 |
| **HT-3-g** | HT-3-a / HT-3-b / HT-3-d / HT-3-e / HT-3-f (SUPERSEDING sub-ruling) | **Eight review rounds across two lanes each closed one input to a READ-TIME derivation of the pricing studio and the next one opened.** The studio that prices an hour was computed when the hour was priced, for every project whose `studio_id` was NULL — so every input to it was a lever the subject could pull between two hours on the same project: a seat she deletes under the shipped `Members can leave` policy (W2-R9-01 probe C), a seat an `admin` sets `status = 'removed'` (probe D2), a seat an outsider writes for her consent-free (W1-R11-01, W2-R3-01), a role she demotes by handing a workspace to a second account (W2-R5-01), a `joined_at` she backdates on the activation path (W1-R12-01), a rate row she rewrites in place (W2-R8-01). Round 9 answered with a PIN, round 10 bounded the pin to the employer tier (W2-R10-01), and round 11 measured the same taking on the STAMP arm the amendment did not gate — **W2-R11-01 form A** (ONE statement, no seat change of any kind: she owns a workspace, holds no employer seat, names it on a legacy project her assistant opened, and her own 99900 is frozen onto it for ever against a control twin an honest later employer priced 26000) and **form G** (two ordinary statements on her FORMER employer's own project). | The round-11 review ranked four closures: apply HT-3-f(2)'s key to the stamp; `v_actor <> v_designer_id` on both arms; make the owned-tier self-stamp NON-FINAL (which needs the owed UNPIN); or ruling-only with both forms asserted as passing. | W2 review rounds 4-11 (W2-R11-01 MAJOR; W2-R10-01, W2-R9-01, W2-R5-01, W2-R4-01, W1-R11-01 and W1-R12-01 ride along) | **RULED BY KODY 2026-09-12 — none of the four. Remove the derivation.** **(1) NO READ-TIME DERIVATION.** The resolver's studio step is `projects.studio_id` when not NULL; otherwise `rate_source = 'none'` (HT-26's "rate pending"). The HT-3-b tier rule (employer tier exactly one -> it; else owned tier exactly one -> it; else NULL) is used in **exactly two places**: `00602`'s INSERT stamp and the one-off ship migration `00620`. **(2) ONE-OFF STAMP AT SHIP.** `00620_legacy_project_studio_stamp.sql` applies the tier rule ONCE to every `projects` row with `studio_id IS NULL` — unambiguous rows get their studio, ambiguous rows stay NULL; idempotent; it RAISE NOTICEs the counts; it runs as the migration role, so `00563`'s own migration arm admits it. **(3) DESIGNERS NEVER STAMP.** `stamp_project_pricing_studio` admits ONLY a caller who is owner/admin of the named studio AND where the named studio is in the project designer's EMPLOYER tier (an active non-guest seat with `role <> 'owner'`). No owned-tier arm, no sibling leg, no confirm arm, no designer arm. Bound (b) (a stamped column is final) stays. HT-3-c stays for creation-time naming (`00563`'s arm); HT-3-e(2) stays. **Implemented 2026-09-12 (W2 round-11 fix pass):** the tier rule as ONE shared body `public.designer_tier_pricing_studio(p_designer_id)` returning `(studio_id, tier)` in `00615`, called by `set_project_studio_id_owned` and by `00620` and by nothing that prices an hour; `resolve_time_rate_cents` loses HT-3-b's step 2 entirely; `project_pricing_studio_id` becomes ONE COLUMN READ, so every caller of it (`00604`'s ledger view, `00605`'s admin write policies and its audit trigger's `organization_id`, `00606`'s owner/admin read, `00607`'s `project_hours_total`) follows the stamped column and fails CLOSED on NULL; `00606`'s bounds (c), (d) and (e) collapse into ONE employer-seat test; `owned_tier_prices_project` (HT-3-f(2)) is deleted. Postconditions pin, by source, that neither hour-pricing body calls the tier rule or reads `organization_members` for studio choice, and that the stamp carries no owned-tier arm, no sibling, no derivation read and no actor-vs-designer comparison in either direction. **Coverage:** `supabase/tests/billing/legacy_project_studio_stamp_test.sql` (NEW — the tier rule on all five shapes, `00620`'s own statement verbatim, idempotence, P-4, and the live migration's end state); `time_rate_resolution_test.sql` cases (x) x4/x5, (aa) aa7-aa7f, (ad-ii) ad8-ad10, (ai) ai0-ai6c, (aj)/(aj-ii), (ak) ak5-ak5d; `time_entry_studio_stamp_test.sql` cases (e), (o) o3-o7b, (q), (r), (w), (y), (z) and the NEW cases **(al)** (form A refused, with its control twin and the employer repairing both) and **(am)** (form G refused). **FOUR COSTS, recorded as cost notes rather than discovered later, each asserted as PASSING:** (i) a legacy project whose designer's tier is AMBIGUOUS or EMPTY prices `'none'` and `00620` leaves it NULL — and where she holds NO employer seat, NO party may stamp it at all, so the repair is a SEAT change (case (o) o3-o7b: her own stamp refused, her co-principal refused, the tier rule answering nothing). (ii) HT-3-g(3)'s seat test is a LIVE-seat test (W2-R8-03's fourth face): once a hire has LEFT, her former employer may no longer stamp its own legacy project, so the act is one a studio must take while the seat is live (case (am) am5). (iii) the HT-3-f(2) COST NOTE's self-repair is withdrawn — the principal's own stamp of her own studio is now REFUSED (ak5), and `00620` at ship is what hands her the studio instead (ak5c/ak5d, and leg (e) of the new file). (iv) an hour logged before a stamp keeps its `'none'` (P-4, no backfill in either direction) — measured in ai5, aa7f, ak6b, g9. **ONE JUDGMENT CALL, reported rather than assumed:** HT-3-g(3) names two conditions and four removals, and HT-3-e(1)'s ARM'S-LENGTH-RATE leg is in neither list, so it is KEPT — unconditionally now, there being one tier. It narrows the act and costs an honest employer only what it had to do anyway (a studio that has not priced her gets nothing from a stamp, her hour pricing `'none'` after it too). If the two conditions are meant to be exhaustive, the removal is one IF and case (u)'s first legs move with it. **AMENDED 2026-09-12 (W2 round-12 fix pass, W2-R12-01 MAJOR — IMPLEMENTED, AND FLAGGED TO KODY as an amendment to part (2) rather than a reading of it): `00620`'s OWNED TIER IS KEYED ON THE PROJECT AUTHOR'S OWN STUDIO STANDING.** Part (1) removed the read-time RECOMPUTATION; by itself it did not remove the LEVER, it concentrated it in part (2)'s single instant. `00620` reads the designer's LIVE seats when it runs, so one ordinary statement of hers BEFORE the ship decides where she is standing: she leaves her employer (`Members can leave`, W2-R9-01 probe C) or as an `admin` sets her own row `status = 'removed'` (probe D2), her employer tier is EMPTY, the OWNED tier answers her own `00295` workspace, and the one-off stamp wrote that workspace onto EVERY legacy project of hers — her former employer's client work included, opened by that employer's own assistant. Bound (b) makes each column final and part (3) gives the employer no arm to repair it, and `00606` and `00620` land in the SAME push, so no studio had a window to stamp its own book first. Measured 1/1 through RLS in both statement forms with a negative control in an identical fixture: her 120-minute hour `99900 / studio_member / 199800` against the control's `26000 / studio_member / 52000`; the employer's owner reading 0 rows against the control's 1; its repair refused `22023` against the control's SUCCESS; `project_hours_total` refused `42501`; its studio rollup 0 project buckets against 1. ONE account, no second party, no ownership transfer, on projects she did not create. In its ordinary clothes it is ATTRITION, not an exploit — no attacker anywhere, and a former employer's legacy book irreversibly reassigned. **THE CLOSURE (the round-12 review's PREFERRED closure of four):** an OWNED-tier answer is written by `00620` only where `projects.created_by` does NOT hold an active, non-guest seat in an active `design_studio` OTHER than the studio being written — only where the project does not VISIBLY belong to another studio's book. One shared predicate, `public.project_author_books_elsewhere(created_by, studio_id)`, defined in `00620`, `REVOKE`d from PUBLIC/anon/authenticated/service_role (reachable only by the migration role that defines it) and SECURITY DEFINER because an INVOKER read of `organization_members` fails OPEN here. THE EMPLOYER TIER IS NOT KEYED ON IT: a single employer seat IS the book the project belongs to, whoever opened it (keying it would re-open cost note (i) across the honest hire). **IT IS NOT HT-3-f(2):** that asked `created_by = designer_id` and refused the honest principal whose own ASSISTANT opened her project; this asks whether the AUTHOR stands in somebody ELSE's studio, so BOTH honest shapes survive — the sole proprietor and the HT-3-f(2) COST NOTE's principal. Not manufacturable by her either: `00563` RAISES on any UPDATE that moves `created_by` and its authenticated-INSERT arm admits only `NEW.created_by = auth.uid()`. **FIFTH COST NOTE:** (v) a legacy project opened by a contractor, an ex-colleague or an assistant who has SINCE taken a seat in another studio is left NULL even where its designer's owned tier answers — and by cost note (ii) nobody may then stamp it, because she holds no employer seat. It prices `'none'` (HT-26's "rate pending") until a studio seats her. That is the trade: `'none'` where a human must act, rather than a taking no party can undo. `00620`'s NOTICE counts those rows separately so the ship sees the cost as a number. **WHAT IT DOES NOT REACH, and is recorded rather than claimed:** a legacy project SHE opened herself is still stamped to the studio she owns — HT-3-f(3)'s already-recorded outcome (aj6), measured here as a labelled PASSING control (g11/h11). **Coverage:** `supabase/tests/billing/legacy_project_studio_stamp_test.sql` cases **(h)** (g10 the attrition row left NULL, g10a the tier rule still answering so the KEY is what leaves it, g10b the predicate true of the former employer's assistant, g11/g11a the three honest shapes answering FALSE, h10 her hour `'none'` with her own 99900 in place, h11 the self-opened control at 99900, h12 the former employer's repair REFUSED `42501` — cost note (v) with teeth, h13 the refusal writing nothing) and **(i)** (g12/g12a the employer tier stamped through an author who DOES book elsewhere); both directions measured by INSTALLATION — with the key removed from the statement the suite fails at g10 with the taking reproduced. `supabase/tests/billing/time_rate_resolution_test.sql` case **(ai)**'s header is CORRECTED in the same pass: its claim that the manoeuvre "now changes NOTHING, whoever opened the project" held only in the POST-stamp ordering it measures, and is scoped to that ordering with a pointer to cases (h)/(i) for the PRE-stamp one. **If Kody re-grades W2-R12-01 to a fifth cost note instead**, the code change to revert is one predicate in `00620` plus its postconditions and cases (h)/(i), and lane B then owes the employer a visible fact about a project whose hours it reads 0 of. **AMENDED AGAIN 2026-09-12 (orchestrator, resolving W2-R13-01; implemented in the W2 round-13 fix pass; FLAGGED TO KODY). Three parts.** **(a) THE ROSTER KEY on `00620`'s OWNED tier.** Round 13 measured the round-12 author key to be a question about a PERSON and so as movable as that person: it is ALREADY false on four of the five standings a legacy project's author can hold (a seatless author, a `guest` seat, a seat in a suspended studio, a seat in a non-`design_studio` org), and where the author is a colleague she administers she makes it false herself in ONE statement (`Org admins can update members` setting his row `status = 'removed'`, or `role = 'guest'`, or deleting it). So: a NULL-studio legacy project is stamped to the designer's OWNED studio only if **no other user on its `project_team_members` roster holds an active non-guest seat in a studio the designer does NOT own** — a roster carrying another studio's people is ambiguous and the row is left NULL. The EMPLOYER tier stays unkeyed by roster (a single employer seat IS the book the project belongs to, whoever opened it and whoever works on it). **BOTH KEYS STAND and the owned tier must pass both:** the author key reaches a row the roster key does not (an EMPTY roster whose author books elsewhere — case (h)'s own fixture), so replacing rather than adding would re-open exactly what round 12 closed. Implemented as one shared predicate `public.project_roster_books_elsewhere(project_id, designer_id, studio_id)` in `00620` — SECURITY DEFINER (an INVOKER read of `project_team_members`/`organization_members` fails OPEN here), `REVOKE`d from PUBLIC/anon/authenticated/service_role, reachable only by the migration role that defines it, and named by no body that prices an hour (postconditions assert all of it). `removed_at IS NULL` is the roster (the same leg `is_project_team_member` reads); "a studio the designer does NOT own" is asked as an owner-seat EXISTS rather than as `<> p_studio_id`, which is what keeps the Leah shape. **W2-R13-02 is resolved with the counts named here:** `00620`'s NOTICE reports the stamp PER TIER (`stamped-employer` / `stamped-owned`, returned by the statement itself rather than re-derived) and the rows left PER KEY (`left-null-ambiguous` / `left-null-roster-key` / `left-null-author-key`) — five numbers, the four the amendment names plus round 12's own cost-note (v) number, which the ruling in force requires counted separately. **(b) THE REMEDY ARM in `stamp_project_pricing_studio`.** In addition to the employer-tier arm, a caller who IS the project's CURRENT designer (`projects.designer_id = auth.uid()`) AND holds an active OWNER seat in the studio named may stamp, and **may OVERWRITE a stamped column** (bound (b) yields to it; bounds (c) and (d) are bypassed by this arm and only by it — an owner seat is outside the employer tier by definition, and her own studio can hold no arm's-length rate for her). The overwrite is audited under its own action `project.pricing_studio_restamped` carrying the OLD and the NEW studio. **ONE JUDGMENT CALL, reported rather than assumed:** the arm is bounded to a column that is ALREADY STAMPED (`v_existing IS NOT NULL`), because the amendment's subject is "an employer recovers a TAKEN project" and its mechanism is an overwrite; on a NULL column HT-3-g(3)'s "DESIGNERS NEVER STAMP" is unamended and is what closes W2-R11-01 forms A and G. Without that conjunct the arm repeals that closure as a side effect and `time_rate_resolution_test.sql` case (aj5) goes red (measured). If the arm is meant to reach unstamped projects too, it is one conjunct and aj5 moves with it. **TWO MEASURED FACTS ABOUT (b), reported rather than assumed.** (i) **THE REASSIGN LEG IS NOT AVAILABLE TO THE EMPLOYER ACTING ALONE.** `reassign_project_lead` (`00399`) is pinned to the project's CURRENT `studio_id`: it requires BOTH the outgoing and the incoming lead to hold an active non-guest seat in THAT organization, which after a form-S/H taking is the taker's own workspace. Measured: the employer's owner is refused `42501` (cases k3/l8). The stamp half recovers completely once she IS the lead — the column moves, the audit row carries both studios, and the designer's next hour prices from the employer's card again (k5-k8, l9-l11), and P-4 holds of every hour already priced. So the remedy RECOVERS the money but is reachable only with the taker's own hand (she seats the employer's owner in her workspace and reassigns), or once `reassign_project_lead` learns to follow a project's BOOK rather than its current column. (ii) **THE ARM IS WIDER THAN ITS PURPOSE.** Read as written it admits ANY designer who owns a studio to re-point ANY already-stamped project she LEADS — an honest employer's included — at the studio she owns, where HT-3-e(2)'s OWNER exemption prices her own number: W2-R11-01 form A with the power to overwrite, ONE statement, no seat touched, no second party, on a project she did not create. It is NOT narrowed here beyond (b)'s own stamped-column subject, and it is measured as a PASSING, loudly-labelled assertion (case (m) m6-m8: an honest studio's 24000 replaced by her own 88800). If the arm is meant only as the employer's way back, the narrowing it needs is a fact about the project's BOOK at that call site, not a fact about the caller. **AND ONE THING (b) BUYS THAT WAS NOT ASKED FOR, measured:** HT-3-f(4)'s victim now has a way back. Case (z) z7 asserted for twelve rounds that she could not repair the consent-free outsider's stamp; under (b) she can, because she is her own project's designer and owns her own studio — the column moves back, the overwrite is audited, and her next hour prices at her own 28000 again (z7/z7a/z7b). The owed UNPIN is thereby discharged for a victim who OWNS a studio; still owed are HT-3-b arm (c)'s consent door and an unpin for a victim who owns none. **(c) RESIDUAL DISCIPLINE.** A manoeuvre that requires an account to leave or remove its own seat or another member's seat, to transfer ownership, or to use a second party is a NOTE — RESIDUAL, and each such note states its remedy path under (b) and whether that remedy recovers (measured). The residual rows are tabled below. **Coverage of the whole amendment:** `supabase/tests/billing/legacy_project_studio_stamp_test.sql` new cases **(j)** (the roster key both ways — j1/j1a/j1b a foreign roster left NULL with the tier still answering and the AUTHOR key measured FALSE so the row is one the round-12 key alone would have stamped; j2/j2a the Leah shape stamped; j3 the ownership clause probed directly), **(k)** (form S: the residual, the money, the refused reassign, the remedy, the audit row, P-4, the money back), **(l)** (form H: the control measured in its own fixture, the two statements through RLS, the residual, the same remedy and the same missing half, and the ROSTER KEY'S OWN LIMIT stated — it reads the same seat the author key reads, so it cannot reach form H by construction), **(m)** (the remedy arm refused to a designer who is only an ADMIN of the studio named, refused to an OWNER who is not the lead, and ADMITTED on an honest studio's stamped project), g1a/g1b (the per-tier counts), h5/h6 (the roster key exists and no role holds EXECUTE); and `supabase/tests/rls/time_entry_studio_stamp_test.sql` case **(z)** z7/z7a/z7b/z7c, rewritten from a refusal to a recovery. **CORRECTED 2026-09-13 (orchestrator, resolving W2-R14-01 MAJOR; implemented in the W2 round-14 fix pass; FLAGGED TO KODY). HT-3-g(b)'s OVERWRITE ARM GAINS A THIRD BOUND, AND IT IS A FACT ABOUT THE PROJECT'S BOOK:** the overwrite is admitted only where **(i)** the caller IS the project's current designer AND holds an active OWNER seat in the studio named (unchanged), AND **(ii)** **the studio being REPLACED does not EMPLOY that designer** — she holds no active, non-guest `organization_members` seat there with `role <> 'owner'`, in an active `design_studio`. **WHY.** Round 13 shipped the arm as the amendment worded it and reported that it was wider than its purpose; round 14 measured the width (W2-R14-01, MAJOR, 1/1 through RLS with a negative control in the same fixture) and it was the brief's blocker clause word for word — ONE account, ONE statement, no seat touched, nothing transferred, no second party, on a project she did not create, moved her own resolved rate from an honest employer's arm's-length `26000` to the `77700` she had written for herself; the displaced employer read 0 of the hours, `42501` on `project_hours_total`, `22023` on its own stamp, `42501` on the reassign, and **0** of the audit rows. **So an honest employer's stamped project can never be overwritten by the designer it employs.** **WHAT SURVIVES:** the remedy the arm exists for. After a form-S/H taking the studio being replaced is the TAKER'S OWN WORKSPACE, which she OWNS — and an owner seat is outside the employer tier by definition — so the recovering lead's overwrite is admitted wherever she holds no employer seat there. **WHAT IT COSTS THE REMEDY, measured in round 14:** ONE more statement, and it is the recovering owner's own. The seat the taker must give her for `reassign_project_lead` to run at all (00399 is pinned to the project's CURRENT `studio_id` and wants both leads seated there) is itself an employer-tier seat in the workspace being replaced, so the overwrite is refused while she holds it (`22023`, cases k4b / l8b) and admitted once she drops it (`Members can leave`, k4d / l8c → k5 / l9). **WHAT IT LEAVES, a RESIDUAL under (c):** a designer an honest studio employs can still reach the studio she owns by LEAVING that employer's seat first — two statements, the first a seat act — which is why case (m) is re-asserted as a refusal (m6) followed by a passing, loudly-labelled residual (m6a). **HT-3-f(4)'s victim keeps her way back, with an ORDERING:** the outsider's consent-free seat IS an employer seat of hers in his org, so she must remove it before she re-points (case (z) z4b refused, z5 removes it, z7 recovers) — which is the statement she has always been able to make. **AND W2-R14-03, in the same pass:** on an OVERWRITE the audit row is filed under the **DISPLACED** studio's `organization_id`. `audit_logs`' only SELECT policies are `Org admins can view org audit logs` (organization_id + owner/admin) and `Users can view their audit logs` (user_id), so a row carrying the NEW studio was readable by the taker and by the studio she had just named and by nobody else — measured at 0 rows for the displaced owner. A first stamp displaces nobody and keeps the studio it wrote. **Implementation:** one new `v_replaced_employs_designer` block in `00606`'s `stamp_project_pricing_studio` (edited in place — `00606` is unapplied on Strata), computed before the bound it gates and pinned by a postcondition on a WHITESPACE-NORMALISED source (W2-R14-04's class), the `public.organization_members` read count raised from EXACTLY TWO to EXACTLY THREE, and the audit row's `organization_id` made `CASE WHEN v_existing IS NULL THEN v_written ELSE v_existing END`. **Coverage:** `legacy_project_studio_stamp_test.sql` cases (k) k4b-k6, (l) l8b-l10, (m) m6/m6b/m6c/m6d/m6e/m6a/m7/m7a (m7a is the displaced studio's owner reading the row through RLS), and `rls/time_entry_studio_stamp_test.sql` case (z) z4b/z4c/z7d. Negative control: with the conjunct removed from the installed body the billing suite reds at k4b with the round-13 arm reproduced. **W2-R14-02 (MINOR) in the same pass, documentation only:** part (a)'s ROSTER KEY is KEPT (removing it reds j1 with the taking reproduced) but its stated rationale was FALSE — `project_team_members` carries 00177's `Lead designers manage team members` at `polcmd = '*'`, so the project's own lead designer clears the whole roster in ONE statement with no seat touched. `00620`'s banner and both COMMENTs now say the key NARROWS and does not close, and that what it buys is a second statement (clear the roster, then leave the seat) of which only the second is a seat act. **W2-R14-05 (note)** is recorded in `00620`'s banner: both keys are applied by `00620` and not by `00602`'s INSERT-time stamp, harmless today by construction (00563 admits only `created_by = auth.uid()` on that path, and a project has no roster at INSERT) and recorded because the asymmetry is invisible in the code. | 2026-09-12 · corrected 2026-09-13 |
| **W2-R9-02** | HT-3-e(1) / HT-3-e(4) (implementation finding, no ruling needed) | **A member could DENY her employer the repair act for her own legacy projects with one statement.** `00606`'s employer arm asks the named studio for a `studio_member_rates` row for this designer *"that somebody other than she wrote"*; HT-3-e(4) re-authors a row to whoever moves its number, so a studio whose card holds exactly ONE row for her — a studio that has priced a hire once, the common shape — lost that standing the moment she rewrote the row in place, and the stamp was then refused to the studio's OWNER as well as to her (measured, probe A A4/A5; recoverable in one owner statement, A6 → A11, but repeatable by her, and with no message that explains it). | The review offered a lane-B copy sentence, or narrowing the leg to the row's authorship HISTORY. | — | **Fixed 2026-09-12 per the orchestrator's instruction (no ruling required — HT-3-e(1)'s ORDER is unchanged and no money moves):** the leg asks the rate row's AUTHORSHIP HISTORY rather than its current author, over OPEN AND CLOSED rows alike. A closed row is frozen outright, so its author is already durable; the open row's displaced author is kept in the one new column `studio_member_rates.original_created_by` (declared in `00606` beside the leg that reads it, written ONLY by `00615`'s `guard_studio_member_rate_history` and only from OLD, COALESCEd so a CHAIN of rewrites cannot walk the first author off the row). Nobody may name it: `00615`'s INSERT guard discards a supplied value (`authenticated` holds INSERT and the WITH CHECK says nothing about the column) and the UPDATE guard's identity freeze refuses a hand-written one. It is a STANDING test at the stamp and not a pricing one — HT-3-e(2) still prices on who wrote the number that is there, so her rewritten row still answers `'none'`. Additive, not backfilled (P-4). Pinned by case **(x)** of `supabase/tests/rls/time_entry_studio_stamp_test.sql`, six legs. | 2026-09-12 |
| **W2-R9-03** | HT-3-e(4) (recorded, no ruling needed) | HT-3-e(4) is keyed on the number MOVING (`IS DISTINCT FROM`), which case (l2) asserts as correct — so an owner who AGREES with the number his member just wrote cannot re-author the row by re-saving the same number through a bare `PATCH`, and the hour stays `'none'`. The shipped portal path is unaffected (`useSetStudioMemberRate` upserts `created_by: userId` on every blur-save). | One line of documentation. | — | **Recorded 2026-09-12 in `00615`'s `guard_studio_member_rate_history` COMMENT:** any caller that PATCHes `hourly_rate_cents` alone must also send `created_by`. No DB change. | 2026-09-12 |
| **W2-R9-04** | — (gate hygiene, no ruling needed) | Two of the program's own gate files were green only on one side of UTC midnight: `time_rate_resolution_test.sql` case (ag3) and `time_entry_studio_stamp_test.sql` case (s8d) dated a repair rate row with `CURRENT_DATE` (the SESSION time zone; `run-sql-tests.sh` sets none, so the server's UTC) while the hour they then log is `NOW() - INTERVAL '1 hour'` and the resolver anchors a rate span in UTC (W1-R1-11). Between 00:00 and 01:00 UTC the two dates disagreed and both files went RED — the false-green class `patina-verification` exists for. **No product face:** the shipped writer sends `new Date().toISOString().slice(0,10)`, i.e. the UTC date. | Make the fixtures UTC-consistent. | — | **Fixed 2026-09-12:** both files swept — every date expression is now `(NOW() AT TIME ZONE 'UTC')::date` (68 occurrences, so the files are correct under any session time zone as well), and the two repair rows that must cover a NOW()-anchored hour are dated `- 1` so they cover it whichever side of UTC midnight the run falls on. Both files carry a banner section naming this. | 2026-09-12 |

## Residual rows — HT-3-g AMENDED (c) (round 13, 2026-09-12; remedies RE-MEASURED round 14, 2026-09-13)

Each row is a manoeuvre that needs an account to leave or remove a seat, transfer ownership, or use a second party — a NOTE, RESIDUAL, never a blocker. "Remedy" is the path under HT-3-g AMENDED (b) **as CORRECTED 2026-09-13** (the overwrite is admitted only where the studio being REPLACED does not EMPLOY the project's designer); "Recovers?" is measured in round 14 on this program's own stack, not argued.

| Residual | What it takes | Remedy path under (b) as corrected | Recovers? (measured round 14) | Coverage |
|---|---|---|---|---|
| **Form S** (W2-R13-01) — a legacy project of a studio she worked for whose AUTHOR holds no live seat anywhere and whose ROSTER carries nobody but her is stamped to the workspace she owns | ONE statement by ONE account: she leaves her own employer seat (`Members can leave`, or `status = 'removed'` as an admin — at the instant the ship runs they are the same fixture). No second party, no ownership transfer, on a project she did not create | `reassign_project_lead` the project to the employer's own OWNER; that owner then LEAVES the courtesy seat the taker had to give her in the workspace (it is an employer-tier seat in the studio about to be replaced, so the corrected arm refuses the overwrite while she holds it); then she stamps the employer's studio and the arm overwrites the taker's workspace | **THE STAMP HALF: YES, in three statements instead of two.** Refused `22023` while she holds the courtesy seat (k4b, new in round 14), admitted once she drops it (k4d → k5). The column moves, the overwrite is audited with both studios **under the DISPLACED studio's `organization_id`** (k6, W2-R14-03), and her next hour prices at the employer's 26000 instead of the 99900 she wrote for herself (k8); P-4 holds of the hour already priced (k7). **THE REASSIGN HALF: NO, STILL NOT FOR THE EMPLOYER ALONE — recovery via `reassign_project_lead` is REFUSED `42501` to the employer's own owner** (k3, re-measured), because the function is pinned to the project's CURRENT `studio_id` and needs both leads seated there, which is the taker's workspace. So the residual recovers only with the taker's own hand (she seats the employer's owner in her workspace and reassigns), or once `reassign_project_lead` follows a project's book rather than its current column — **a ruling still owed** | `legacy_project_studio_stamp_test.sql` case **(k)** k1-k8, incl. k4b/k4c/k4d |
| **Form H** (W2-R13-01) — the same, where the project's AUTHOR is a colleague she administers | TWO statements, both hers, both on rows her own shipped policies admit: the AUTHOR's seat `status = 'removed'` (`Org admins can update members`), then her own (W2-R9-01 probe D2). Both keys read that one seat, so the roster key cannot reach this shape by construction | identical to form S | **identical to form S** — measured on its own fixture: the employer's owner refused the reassignment `42501` (l8), the overwrite refused `22023` while she holds the courtesy seat (l8b), admitted once she drops it (l8c → l9), audited under the displaced studio (l10), and the hour back at 26000 (l11) | `legacy_project_studio_stamp_test.sql` case **(l)** l0-l12, incl. l8b/l8c |
| **The realistic form-S row** (W2-R14-02, round 14) — the same shape where the project's roster carries the employer's people, which is what part (a)'s ROSTER KEY narrowed | TWO statements by ONE account, neither needing a second party: ONE UPDATE or DELETE of the whole roster by the project's own LEAD DESIGNER (00177's `Lead designers manage team members` is `polcmd = '*'`, no seat touched), then she leaves her own employer seat. Only the second is a seat act, which is what keeps it a residual | identical to form S | identical to form S. Recorded because round 13's stated rationale for the key — that a roster "cannot go quiet without touching those people's seats one at a time" — is FALSE (measured): the key NARROWS the owned tier's population to rows that cost two statements instead of one, and does not close it. The key is KEPT: removed from `00620`'s statement the billing suite reds at `j1` with the taking reproduced | `legacy_project_studio_stamp_test.sql` case **(j)** j1-j3; `00620`'s banner and both predicate COMMENTs corrected in round 14 |
| **The employed designer's own studio** (new, round 14 — what HT-3-g(b) CORRECTED leaves) — a designer an HONEST studio employs re-points that studio's stamped project at the studio SHE owns | TWO statements by ONE account: she LEAVES the employer's seat (`Members can leave`), then stamps. The first is a seat act, so this is a residual and not the one-statement taking W2-R14-01 measured | the displaced employer reassigns to its own owner and stamps, exactly as in form S (and meets the same refused reassign half) | **the refusal is measured (m6 `22023`, nothing written, no audit row, the hour still 24000) and so is the residual that follows it (m6e → m6a: the seat gone, the overwrite admitted, her next hour at the 88800 she wrote for herself).** The displaced studio's owner can now READ the restamp row through RLS (m7a; measured at 0 before W2-R14-03) | `legacy_project_studio_stamp_test.sql` case **(m)** m0-m9 |
| **HT-3-f(4)** (carried, W2-R13-03) — the consent-free outsider seats her in HIS org, authors her rate there, and `00620` redirects her WHOLE legacy book at ship | a second party acts (his seat INSERT); she need do nothing | she is her own project's designer and the OWNER of her own studio, so she re-points it herself — **in round 14's ordering: she must first remove the seat he wrote** (his consent-free seat is an employer seat of hers in the org being replaced, so the corrected arm refuses the overwrite while it is live), then stamp | **YES, and the correction costs her an ORDERING rather than a remedy.** Measured: refused `22023` while his seat is live (z4b), the seat removed in one statement she has always been able to make (z5), the column then moves off his org (z7), the overwrite audited under HIS org's `organization_id` so he can see what happened (z7d, W2-R14-03), and her next hour prices at her own 28000 (z7b). The owed UNPIN is discharged for a victim who OWNS a studio; still owed is the consent door (HT-3-b arm (c)), and an unpin for a victim who owns none | `time_entry_studio_stamp_test.sql` case **(z)** z4b/z4c/z5-z7d |
| **HT-3-e(3)** (carried) — a designer running a SECOND account she transfers her workspace to | two accounts and an ownership transfer | unchanged by the correction: the cooperating pair hold the current-designer and owner seats the arm asks for, and the studio being replaced is the pair's own workspace, which employs neither of them at `role <> 'owner'` | n/a — the residual is cooperation, not a taking from an unwilling party. Patina makes it VISIBLE (the pricing studio is a column of `time_entry_ledger`; lane B's owner lens shows it) | `time_entry_studio_stamp_test.sql` cases (q), (r) |
| ~~**Amendment width**~~ (round 13) — any designer who owns a studio may re-point an already-stamped project she LEADS at the studio she owns, ONE statement, no seat touched | — | — | **CLOSED 2026-09-13 by HT-3-g(b) CORRECTED.** Graded MAJOR by the round-14 review (W2-R14-01) under discipline (c)'s blocker clause and measured 1/1 with a negative control in the same fixture: an honest employer's 26000 became her own 77700, the employer read 0 hours and 0 audit rows, and neither the reassign nor the stamp would undo it. What remains of the shape is the row above it, which costs a seat | `legacy_project_studio_stamp_test.sql` case **(m)** — m6 is the same call round 13 asserted as SUCCEEDING and it now asserts `22023` |

## Program rulings (interview, 2026-09-11)

| ID | Question | Ruling |
|---|---|---|
| **P-1** | Sequencing | → W0 → W1 rate truth → W2 views (six seats over Leah's visibility-first) |
| **P-2** | Scope | → all eight waves W0–W7 in one program |
| **P-3** | Ship cadence | → one ship at the end after W7 passes its gate and the final review is clean |
| **P-4** | Historical backfill | → none; new entries only; invoiced and unbilled history keep their amounts |
| **P-5** | Flags | → unflagged, matching today's precedent |
| **P-6** | Field device pass | → Simulator gate (capture-gate.sh) is sufficient to ship; Kody walks the device afterwards |
| **P-7** | Leah | → ship, then walk her through it; reactions recorded as provisional rulings |
| **P-8** | Timeline | → gates only, no date |

## Integration round 2 — recorded 2026-09-14 (fix round)

> These three are **recorded by the integration fix round**, not spoken by Kody. Two of
> them (P2-n1, HT-3-g AMENDED (c)) are engineering choices inside rulings already in
> force, taken in the recoverable direction and implemented; they are here so the ship
> does not carry an undocumented decision. The third (**HT-10-b**) is a deliberate
> exception to a confidentiality expectation and is **NOT closed in code**. It was
> escalated to Kody and **he ruled it on 2026-09-14: the exception stands** — the lead
> designer keeps the read, rates included, and the ruling's text is amended below to say
> that it covers bulk per-member reads AND writes on the projects she leads, not one row
> at a time. Nothing in code changed for it.

| ID | Ruling touched | Question | What was done | Evidence | Standing |
|---|---|---|---|---|---|
| **P2-n1** | HT-13-a | The Hours sheet asks two different questions under one caption: `time_entry_ledger.day` is `(started_at AT TIME ZONE 'UTC')::date`, while the week window, the `mine` read and the CSV window are the viewer's LOCAL Monday-to-Monday. For any zone west of UTC there is a nightly band in which the UTC day is tomorrow's — on the week's last day the hour leaves the week entirely and is omitted from the studio rollup, "the entries", the CSV and the statement, while `mine` still shows it one click away. HT-13-a closes this only for DATE-ONLY entries (noon UTC via `startedAtFromDateValue`); the TIMER stores the real instant and is the program's flagship capture door. | **Option (a): the window is an INSTANT range, the same one `mine` already uses.** `studio_hours_rollup`'s `p_from`/`p_to` become `timestamptz` (from INCLUSIVE, to EXCLUSIVE) and the `scoped` CTE compares `ledger.started_at`; `useTimeEntryLedger` filters `.gte('started_at', from).lt('started_at', to)`; `hours-ledger.tsx` passes `weekStart`/`weekEnd` ISO instants and keeps the calendar dates only for the export filename and the analytics `period`. Option (b) — a studio timezone — is what HT-13-a declined and is NOT reopened; option (c) would have discarded the time of day the timer genuinely collected. | Measured from a CDT machine: two entries read `WEEK 3H 00M` under `mine` while `studio_hours_rollup(studio,'2026-09-07','2026-09-13','member')` returned 0 rows and `'2026-09-13'..'2026-09-19'` returned 180 min / 36000 cents. Pinned by 00607 postconditions (h) and (i). | **Fixed.** |
| **P2-n1 residual** | — | Does the fix above also correct the `day` / `iso_week` BUCKET LABELS? | **No, and it is named rather than left to be discovered.** Inside a now-correct window, an evening entry west of UTC is still labelled under the NEXT UTC day, because 00604 derives both buckets `AT TIME ZONE 'UTC'`. Moving them needs a timezone, which is exactly what HT-13-a declined. This is a labelling question — no hour is omitted, no money figure is wrong, and every non-date bucket (member / project / activity) is unaffected. | Stated in 00607's banner and in the ship checklist's blocker note. | **CLOSED at integration round 3 by HT-13-b** (below), after R3-M2 measured the consequence in the browser and R3-m3 found it on the homeowner's folio. The labels now take the caller's zone as an ARGUMENT; HT-13-a was not reopened and no studio timezone column exists. |
| **HT-3-g AMENDED (c)** (= MS-12, = P2-n2) | HT-3-g(2), MS-02 | `00620`'s one-off legacy stamp called `designer_tier_pricing_studio(project.designer_id)` directly. That helper asks only `organizations.type/status` and `organization_members.role/status` — nothing about a designer-domain role — while MS-02 put `public.has_designer_domain_role(NEW.designer_id)` in front of the tier rule on the INSERT path (`00603`/`00615`), which is 00511's own condition for auto-deriving a studio at all. The two paths therefore disagreed BY CONSTRUCTION on the legacy population, and on the EMPLOYER tier, which is the common legacy shape. | **The gate is asked in `00620` too** — in the tiered CTE, in all three per-key counters, and in postcondition (a) — plus a fourth reported count, `v_left_non_designer`. `ms-05-strata-legacy-stamp-preflight.sql` inlines the same body (00511's EXECUTE is revoked from `dashboard_user`) and reports `left_null_non_designer_lead`, so MS-05's pre-push read and the statement describe the same rows. | Measured with a negative control in one rolled-back transaction: `has_designer_domain_role(lead) = f`, `designer_tier_pricing_studio` → `e7000000-…-0001` / `'employer'`, INSERT under 00603's gate → `studio_id` NULL, 00620's statement → stamped. | **Fixed.** The direction is the recoverable one: a row left NULL prices "rate pending" and can still be stamped by hand under HT-3-g(3); a row stamped to the wrong studio is a read+write grant that 00606's bound (b) makes final. |
| **HT-10-b** (= MS-15) | HT-10-a | HT-10-a narrowed a plain member's per-row read to her OWN rows so that a teammate's confidential per-person rate and free-text notes stay hers. It did **not** narrow `Designers manage their project time entries` (00177:136-137) — a permissive **ALL** policy on `projects.designer_id = auth.uid()` with **no `user_id` leg**. So the project's own lead designer — who may be a PLAIN studio member — reads every rostered teammate's `hourly_rate_cents`, `rated_amount_cents` and `notes` off `project_time_entries`, even though `studio_member_rates_read_self_or_admin` denies her that teammate's rate CARD. `resolve_time_rate_cents` gates its own designer leg on `pg_trigger_depth() > 0` and justifies it in-body as "a number RLS gives her nothing of"; that premise does not hold. | **RECORDED AS AN EXPLICIT EXCEPTION, NOT CLOSED IN CODE.** Option (c) — narrowing the designer policy to own rows — would delete the path that lets a lead CORRECT a teammate's entry (`time_rate_resolution_test.sql` case (ab4), W1-R1-05), which this program relies on and which nothing replaces. Option (b) — a GRANT-level column split on the three columns plus a definer correction RPC — is the real fix, is not a one-line change, and is a wave of its own. | Probed directly on the program's stack: `pg_policy` shows the ALL policy's qual as `EXISTS (SELECT 1 FROM projects p WHERE p.id = project_time_entries.project_id AND p.designer_id = auth.uid())` with no `user_id` term; `studio_member_rates` carries only `studio_member_rates_read_self_or_admin` (`user_id = auth.uid() OR is_org_admin_or_owner(studio_id)`). Already stated at `00606:83-95`, pinned by its postcondition (e) and by `studio_hours_rollup_test.sql` case (f) — but named in neither rulings.md nor the ship checklist until now. | **RULED by Kody 2026-09-14 — THE EXCEPTION STANDS, and its text is amended to say what it actually is.** A project's LEAD DESIGNER keeps her per-row read of her project team's time rows, **rates included**; the pre-existing `designer_id` policy (00177:136-137) stands unchanged and **no code changes**. The amendment is to the DESCRIPTION of the exception, because round 3 measured it wider than round 2 recorded: it is not one row at a time. It covers **BULK, PER-MEMBER READS AND WRITES on the projects she leads** — through `time_entry_ledger`, `studio_hours_rollup` (which names each teammate beside her resolved rate and amount), `project_unbilled_time` (which carries `notes`), and the studio-scope CSV; and, because the policy is `ALL` rather than `SELECT`, UPDATE and DELETE of a teammate's hour as well (audited by `zzzz_audit_time_entry_change_trg`, with the money snapshot not re-resolved to her own rate, and an invoiced row frozen by `guard_invoiced_time_entry` — all pre-existing, none introduced by this program). Option (b) — a GRANT-level column split on `hourly_rate_cents` / `rated_amount_cents` / `notes` plus a definer correction RPC — remains owed as follow-up work, not as a ship gate. |

## Integration round 3 — recorded 2026-09-14 (final fix round)

> **HT-13-b** is recorded by the integration orchestrator, resolving the two findings
> round 3 raised against P2-n1's residual (**R3-M2**, measured in the browser, and
> **R3-m3**, measured on the homeowner's folio). It does **not** reopen HT-13-a: no studio
> timezone column is added, and the stored instant is untouched. What moves is the LABEL,
> and it moves by taking the caller's zone as an argument.

| ID | Ruling touched | Question | What was ruled | Evidence | Standing |
|---|---|---|---|---|---|
| **HT-13-b** | HT-13-a, P2-n1 residual | Inside a now-correct window, the `day` and `iso_week` labels were still 00604's UTC derivation. Measured at round 3: one hour filed Sun 13 Sep 21:34 CDT read **13 SEPTEMBER** under `mine` and **2026-09-14** under `THE STUDIO → THE ENTRIES`; `BY DAY` printed a bucket the displayed week does not contain; the CSV's Date column carried the UTC date; and `buildTimeLineDraft`'s `.slice(0, 10)` billed the hour to the **homeowner** as the 14th, on the pay-link folio. Two lenses of one sheet printed two dates for one hour. | **Day and week labels everywhere are derived in the CALLER's timezone, passed as an IANA name.** (1) `studio_hours_rollup` takes `p_timezone text DEFAULT 'UTC'` — appended last and defaulted, so no positional caller moves — and cuts the `day` / `iso_week` buckets on `(started_at AT TIME ZONE p_timezone)`; an unreadable name raises `invalid_parameter_value` in the function's own voice. (2) `time_entry_ledger` **keeps its UTC `day` column** — it is the fact view's basis and 00599's — and the portal derives every DISPLAYED date from `started_at` in the browser's zone (`Intl.DateTimeFormat().resolvedOptions().timeZone`) instead of printing that column. That covers the studio entries list. (3) The CSV's Date column and the client folio's dated sub-table follow the same rule — the folio's dates are cut in the studio viewer's zone **at compose time**, never `.slice(0, 10)` on a UTC ISO string, because the client portal renders the persisted value verbatim and gets no second chance. **No studio timezone column: HT-13-a stands.** The WINDOW is untouched — it has been an instant range since P2-B1 and needs no zone. | 00607 postconditions (f) and (j); `supabase/tests/rls/studio_hours_rollup_test.sql` case (j) — the same instant labels one day under `UTC` and the previous day under `America/Chicago`, for both buckets, with the default proved to be UTC; jest coverage in `time-export.test.ts`, `time-billing.test.ts` and `hours-ledger-scope.test.tsx`. | **RULED (orchestrator). Implemented — closes R3-M2 and R3-m3; P2-n1's residual is CLOSED, not carried.** |

## Integration round 4 — recorded 2026-09-14 (orchestrator, pre-ship)

> **HT-13-c** is recorded by the ship orchestrator, resolving **R4-M1** from
> `final-review-money-security-r4.md` — the one new major that round 4 found and that the
> report itself said must be named and ruled, not silently shipped. It does **not** reopen
> HT-13-a or HT-13-b: no studio timezone column is added, and neither the stored instant nor
> the printed labels move. What is ruled is which date governs a tier-2 rate's own
> `effective_from` / `effective_to` span.

| ID | Ruling touched | Question | What was ruled | Evidence | Standing |
|---|---|---|---|---|---|
| **HT-13-c** | HT-13-a, HT-13-b (R4-M1) | HT-13-b moved every PRINTED date (studio entries list, `BY DAY` / `BY WEEK`, the CSV, the client folio) into the caller's timezone. `resolve_time_rate_cents`'s tier 2 — the per-member `studio_member_rates` row — still picks its `effective_from` / `effective_to` span on the **UTC** calendar date of the instant (`(p_at AT TIME ZONE 'UTC')::date`, `00599:535-537`). West of UTC, an evening hour's two dates differ: measured, `2026-09-13 21:34 America/Chicago` (= `2026-09-14 02:34 UTC`) prints as **13 September** everywhere HT-13-b touches, while the rate card is chosen on the UTC day, **14 September** — a card effective **from the 14th** prices an hour every printed surface calls the 13th. | **A `studio_member_rates` row's `effective_from` / `effective_to` are, and remain, compared in UTC — server truth. Only PRINTED dates follow the viewer's zone.** A rate set "from the 14th" governs hours whose UTC date is the 14th, full stop, regardless of what day any lens prints for that hour. **No code change** — `00599`'s tier-2 comparison is correct as written and stays as written; HT-13-b's printed-date labels stand unchanged; no studio timezone column is added, per HT-13-a. The mismatch R4-M1 measured (a printed "13th" hour priced by a card effective "the 14th") is the necessary and accepted consequence of two deliberate, independent rulings — HT-13-a's UTC-anchored server truth and HT-13-b's viewer-zone printed labels — not a defect to fix. | R4-M1 (`final-review-money-security-r4.md` §B.1), the measured fixture (10000 cents closed 2026-09-13, 44400 cents effective 2026-09-14, priced at 44400 for an hour printed 2026-09-13); `00599:535-537` (tier-2 comparison, unchanged); Tier 1 (`project_billing_authority_rates`) compares the instant directly and is unaffected. | **RULED (orchestrator, 2026-09-14) — resolves R4-M1. Ruling-only; zero code change.** |
