# Release judgment — The Invoice, Standing Alone

Judge: Fable, separate context, 2026-09-06. Read-only. Branch `invoice-standalone/integration` at `83f829377` (23 commits over main `19f64a0a1`, which is still main's tip — nothing to rebase), worktree `/Users/kody/Code/patina-merged/.codex/worktrees/agent-inv-int`. Binding inputs: `delivery/RULINGS-K4-K11.md`, `review/04-rulings.md`, the three wave reviews, `architecture/02-system-design.md` v2 §5 and §12. Five read-only lanes did the legwork (integration commits + resolver key trace; cross-wave seams; money path; production readiness incl. Strata read-only probes; the full gate run on the merged tree); every claim below was returned with `path:line` and the ones the verdict rests on were re-read by me. Nothing was edited, committed, reset or deployed. W3b (`f640e74b8`) is **not** an ancestor of `83f829377` (`git merge-base --is-ancestor` → no).

## Verdict

**SHIP WITH FIXES.** The merged tree is sound where it must be — every gate is green including the Playwright spec W2's review said was never run (7/7 against 00574), 00574 is idempotent and Strata is exactly one migration behind with all 22 payable invoices backfillable, the redeploy set re-greps to exactly six, wrangler 4.107.0 accepts the `ratelimits` config, and the money path binds the link identity at every step with no NULL-`payer_id` fall-through. Four defects must land before merge, each a few lines: the **designer** portal's PostHog redaction regex still lacks `pay` while W3's folio renders the permanent bearer URL as an anchor under autocapture (J1); a `requires_refund`/`refunded` row renders as "Received" with the Pay button still offered — the exact state Kody's K11 refund will produce (J2); the same link-holder can still supersede an ACH in flight by switching rails (J3); and the sheet tells a payer whose bank transfer is processing to "try again in a moment" (J4). One ruling is owed on the signed-in rail's return landing (J5); everything else is follow-up or runbook.

**Pre-merge fixes (must):** J1, J2, J3, J4 · then re-run the gate set in §5 step 0.
**Ruling owed (before deploy, default given):** J5.
**Before Kody's walk (should, cheap):** J6, J7, J8, J15.

## Findings

Severity is mine at synthesis; the lanes reported unfiltered and I re-rated where their reading disagreed with a prior ruling (e.g. no logo was ruled "accept, firmly" in the W2 review, so J36 is a nit, not a major).

| id | sev | conf | where | finding | fix |
|---|---|---|---|---|---|
| **J1** | **blocker** | high | `apps/designer-portal/src/lib/analytics/posthog.ts:81` · `apps/designer-portal/src/components/document/accounts/invoice-folio.tsx:716-721` | W2 widened only the **client** portal's redaction (`posthog.ts:93-94` there reads `(share\|rfq\|evidence\|plans\|pay)/(?:return/)?`). The designer portal's `HEX_BEARER_IN_URL` is still `/(share\|rfq\|evidence\|plans)/[0-9a-f]{64}/` — and W3's recovery band renders `<a href={clientInvoiceUrl}>{clientInvoiceUrl}</a>` where the value is `https://client.patina.cloud/pay/<64hex>`. `posthog.init` (`:148-159`) leaves autocapture on, so a click on that anchor ships `attr__href` and `$el_text` carrying a permanent, never-expiring payment credential to a third party — the file's own comment at `:124-126` forbids exactly this, and the whole S10 nonce design exists to keep the token out of retained third-party logs. Verified by me. | Adopt the client-portal regex verbatim at `posthog.ts:81`; add the `/pay/<hex>` and absolute-URL cases to the designer portal's privacy test beside the existing `/share` case. |
| **J2** | major | high | `supabase/migrations/00574_invoice_links.sql:1242-1245` · `apps/client-portal/src/app/pay/[token]/invoice-sheet.tsx:745-751`, `:185`, `:229` | `payments[]` lists every stripe row except `failed`; the sheet's label ladder is `pending → "Payment processing"`, everything else → `"Received <date>"`. A **`requires_refund`** (overpayment / gross mismatch) or **`refunded`** row therefore renders as "Bank transfer · Received Sep 6 — $1.00" while `balance_cents` is back to $1.00 and the chooser + Pay button are offered again. K11 has Kody refund the $1.03 in the Stripe dashboard and then verify "the page's receipt state" — this is what he will see. A second claim after `requires_refund` raises `invoice_checkout_reconciliation_required` → 409 → collapsed by J4 to a retry invitation. | Either exclude `requires_refund`/`refunded` from `v_payments` at `00574:1244` (cheapest, and the settling split at `:1147` already knows `requires_refund`), or add explicit label branches ("Refunded", "Being sorted out by {studio}") and suppress the chooser while any non-succeeded, non-pending row exists. Pin: an active link with a `refunded` row renders neither "Received" nor a Pay act. |
| **J3** | major | high (code) / medium (reach) | `00574:572-580` (link claim), `:335-341` (household claim) | W1's F1 guard fires only when `v_actor_changed`. The **same** actor changing rails (`card` ↔ `us_bank_account`) still takes the supersede branch (`:582-615`) on a `processing` attempt: the pending ACH payment is marked failed, a second session opens, `stripe.checkout.sessions.expire` on the completed ACH session fails best-effort (`_shared/invoice-checkout-driver.ts:302-308`), the debit continues, settles onto the failed row (`00428:687` accepts `pending\|failed`), and `guard_invoice_payment_overpayment` flips it `requires_refund` — a self-inflicted double charge with a manual refund. The sheet hides the chooser once `processing` is true, but a stale tab or a hand-crafted POST to the unauthenticated `/pay/<token>/checkout` reaches the RPC. The W1 reviewer scoped F1 to the actor-changed case and called the rail-change supersede "00428's — don't widen"; that was right for the JWT rail where the actor is authenticated and the surface is one tab. On a bearer link with no session it is not. | In both claims: `IF v_attempt.state = 'processing' THEN RAISE EXCEPTION 'invoice_checkout_in_progress'` unconditionally (money in flight is never superseded — the expire call cannot recall an ACH anyway). Pin both directions beside the F1 A/B cases at `invoice_links_test.sql:1181-1200`, `:1277-1290`. |
| **J4** | major | high | `apps/client-portal/src/lib/threshold/refusal.ts:47-56` · `invoice-sheet.tsx:388-404` | `refusalSentence(cause, sentence)` discards the error code outside `NODE_ENV=development`. Every checkout refusal — `payment_processing` (409, ACH in flight, the very case F1 was built for), `payment_reconciliation_required`, `invoice_not_payable`, `invoice_not_found`, `checkout_payer_mismatch` — reads "Unable to open the payment page just now. **Try again in a moment.**" The function composes the correct `processingDetail` ("A bank transfer for this invoice is already processing. Bank transfers take 3–5 business days to clear.", `invoice-link-checkout/index.ts:247-248`) and no surface renders it. The brief asked whether the page renders the 409: it does not. | In `handleAct`'s catch, switch on `body.error`: `payment_processing` → the processing sentence; `payment_reconciliation_required` → "A payment on this invoice is being sorted out by {studio}. Nothing to do here for now."; `invoice_not_payable` / `invoice_not_found` → "This invoice can't be paid from this link any more. Ask {studio} for a fresh one."; else the generic. One Jest case per code. |
| **J5** | major | high | `supabase/functions/create-checkout-session/index.ts:932,950` · `_shared/invoice-checkout-driver.ts:200-204` · `apps/client-portal/src/app/pay/return/[nonce]/route.ts:33-38` · `components/threshold/letterbox.tsx:146-178` | **Ruling owed.** S10 put the nonce on both rails, so a signed-in household paying from the letterbox now returns to `/pay/return/<nonce>` → 303 → the **guest sheet**, not the house — and since the backfill gives every issued invoice a link, the letterbox fallback address is unreachable in practice. `CARRIED_PARAMS` has no `invoice`, so `readCheckoutReturn` yields `invoiceId: null` and the letterbox's `clientEvents.paymentCompleted` / `paymentCancelled` never fire again. The W1 review (F4) saw this only as deploy coupling; no ruling names the surface change. W3b retires settle-in-place anyway. | Default: **accept** — the guest sheet is the ruled pay surface (K1) and W3b removes the letterbox till; record the landing and the event move (`payLinkEvents.paymentCompleted` is the successor) in an R137 addendum and the deck. Alternative if Kody wants the house back until W3b: `nonceReturnOrigin: null` on the payer rail in `create-checkout-session`. Not a merge blocker either way. |
| J6 | major (fidelity) | high | `00574:1277`, `:1164` · `apps/client-portal/src/app/pay/[token]/invoice-link.ts:174-186` · `invoice-sheet.tsx:435-450` | `studio.location` — added by W1 for W2 after review C-1, SQL-tested — is parsed away and never rendered. The mockup letterhead on record reads "Des Moines, Iowa · prepared by Nora Quist"; the shipped letterhead has no location. Kody's walk compares against the deck. | Add `location: string \| null` to `InvoiceLinkStudio`, parse it, render it before "prepared by" on the payable and terminal letterheads. |
| J7 | major (fidelity) | high | `00574:1203-1215` · `invoice-link.ts:188-203` · `invoice-sheet.tsx:689-705` | `line_items[].attribution` (the maker name, id/email-scrubbed, with its own SQL test at `invoice_links_test.sql:670-676`) — added for review C-3 — has no field on `InvoiceLinkLineItem` and is never read. The record is visibly thinner than the design. | Add the field to type + parser; render as a quiet second line under the description. Refresh the unit fixtures from the real shape (they still carry the pre-review payload — `invoice-link.test.ts:65-70`, `invoice-sheet.test.tsx:96-101`). |
| J8 | minor | high | `apps/client-portal/src/app/pay/[token]/state/route.ts:59-63` vs `invoice-sheet.tsx:137-155`, `:178-180` | W2's I-7 landed server-side only: the poll returns `processing`, `readLiveState` does not read it, the sheet re-derives from `payments[].status === 'pending'`. Equal today only because `payments[]` and `v_processing` share one WHERE (`00574:1245`); diverges the day `requires_refund` counts — i.e. the J2 path. | Add `processing: boolean` to `PayLinkLiveState`, validate, use `live.processing`. |
| J9 | minor | high | `supabase/functions/invoice-send/index.ts:213-239` · `invoice-reminders/index.ts:132-156` | Only `stripe-webhook`'s `resolveRecipient` grew the `invoice_links.payer_email` rung (`:347-359`). `invoice-send` still 422s `no_recipient` for a payer-less invoice (pre-existing — K1's answer is Copy link), and `invoice-reminders` never chases a balance on an invoice whose only address arrived at Checkout — a link-paid partial payment is never reminded. | Extract the webhook's rung ladder into `_shared/` and use it in both producers. Follow-up; the folio shows the balance. |
| J10 | minor | high | `supabase/functions/_shared/send-email.ts:211,391` · `stripe-webhook/index.ts:487-505,582-599` | The guest receipt/failure letter is sent with `userId: undefined`, so `shouldLog` is false: **no `notification_log` row**, no suppression check, no rate cap, no `List-Unsubscribe` for the exact population this feature exists for. The designer's in_app row still lands. | Log against a null-user shape (`user_id` NULL + `metadata.invoice_link_id`), suppression keyed on address. Follow-up. |
| J11 | minor | medium | `invoice-link.ts:536-552` · `apps/client-portal/wrangler.jsonc:53-60` | One 30/60 s per-IP budget is shared by page, `/state` and `/checkout`. A confirming return burns ~13 (page + checkout + return page + 10 polls). Three payers behind one NAT in a minute → the dead sheet, byte-identical to a revoked link. | Give `/state` its own bucket or exempt it after the first N; the oracle argument does not need one shared counter. Follow-up; watch `wrangler tail` for the over-limit line during soak. |
| J12 | minor | high | `apps/client-portal/tests/pay-link.spec.ts:388-391` · `00397:1301-1303` · `00574:906` | The void e2e passes only because `void_invoice`'s guard `IF NOT FOUND OR NOT _can_manage_invoice_owner(...)` evaluates NULL for an actorless service_role caller (`_can_author_proposal` = `true AND (NULL OR false)`), so the RAISE never fires — a path no real flow takes (the SQL suite voids as `authenticated`, `invoice_links_test.sql:1315-1322`). Pre-existing; service_role is already all-powerful, so not an escalation — but the e2e leans on a three-valued-logic hole. | Test: void as the seeded designer. Product: `coalesce(..., false)` in both void guards — a later hardening migration, not 00574. |
| J13 | minor | high | `00574:1147-1152` · `invoice_links_test.sql:1407-1413` | The settling/withdrawn split counts any `pending` stripe row as in-flight with no PI-stamp requirement, while `payments[]`/`pay.processing` apply F3's PI rule at `:1245`. A PI-less pending row arriving after a void shows "settling" forever; the test pins the inconsistency. Small blast radius (void fails pending rows at `:983-988`). | `AND stripe_payment_intent_id IS NOT NULL` on the `pending` arm; stamp a PI in the test insert. |
| J14 | minor | high | `invoice-sheet.tsx:137-141` · `state/route.ts:45-50` | A mid-poll void returns `{kind:'withdrawn'}` from `/state`; `readLiveState` returns `null` for it, so the payable sheet keeps its pre-void figures until reload. | On `kind !== 'invoice'`, `router.refresh()`. |
| J15 | minor | high | `apps/client-portal/src/app/pay/[token]/page.tsx:78` · `pay-link-beacon.tsx:20-31` | The withdrawn sheet fires no analytics event (`dead` and `settling` do; the beacon's prop type excludes `withdrawn`). K5's sheet is invisible in PostHog. | Add `payLinkEvents.withdrawn`, widen the prop, pass `sheet="withdrawn"`. |
| J16 | minor | medium | `00574:1367-1376` · `return/[nonce]/route.ts` | F2's `l.created_at <= a.created_at` is correct and the tie cannot arise (M11 refuses regenerate while an attempt is live, so a regenerate lands strictly after the attempt is terminal). Consequence: pay → designer regenerates within seconds → the returning payer's nonce resolves NULL → `/pay/dead` instead of a receipt. Practically unreachable; deliberate per the F2 comment. | Record; optionally admit the revoked link for `checkout=success` returns only. |
| J17 | minor | high | `00574:1166-1171` · `invoice-link.ts:325-357` | The withdrawn `contact` object duplicates `designer_display_name` (`:1166`) and `studio.website` (`:1162`), the parser requires all three keys (the same brittleness `9f3d87b5a` fixed — drop `studio_name` later and every withdrawn invoice dies again), and the settling arm emits `contact` + `invoice.title` that its parser never reads. | Require only what is used; or drop `contact` from 00574 (unshipped) and derive it client-side. |
| J18 | minor | high | `stripe-webhook/index.ts:664-674` | `set_invoice_link_payer_email(session.metadata.invoice_link_id, …)` runs **before** `resolveExactClaimedPayment` validates identity. Signed events + our own session creation make it operator-error-only, but the write sits outside the identity boundary. | Move it after `resolveClaimedPaymentRow`; use `attempt.invoice_link_id`. |
| J19 | minor | high | `00574:1460-1470` | `regenerate_invoice_link` revokes then INSERTs with no `ON CONFLICT`; a concurrent `ensure_invoice_link` (a letter being sent) raises a raw `unique_violation` instead of a named error. | `ON CONFLICT (invoice_id) WHERE status='active' DO NOTHING` + re-read, as `ensure_invoice_link:1041-1046` does. |
| J20 | minor | medium | `_shared/invoice-checkout-core.ts:209-210` | `assertInvoiceSessionIdentity` checks only the present term; a link attempt meeting a session that also carries `payer_id` passes. The row-side XOR (`invoice-checkout-integrity.ts:108`) still holds. | Assert the absent term is absent. |
| J21 | minor | high | `pay-link.spec.ts:205-211`, `:355-360`, `:582-586`, `:11-13` | The e2e proves less than its comments claim: the trigger is never proven (the fixture calls the idempotent `ensure_invoice_link` right after the status UPDATE); revoke is a raw `invoice_links` UPDATE, not `regenerate_invoice_link`; F6 mutates the seed invoice `threshold.spec.ts` drives (order coupling); the header still says fixtures are "ISSUED". Trigger and Regenerate coverage live in the SQL suite (`invoice_links_test.sql:359-419`, `:1496-1516`), so nothing is unproven — just not here. | Assert the link row before `ensure_invoice_link`; revoke via the RPC; mint F6's own invoice; fix the header. |
| J22 | minor (runbook) | high | Strata: `invoice_checkout_attempts` has 13 `session_created` + 6 `superseded` rows today · `00574:1560-1610` (sweep) | The hourly sweep's first run on Strata will fail every `pending` payment behind a `session_created` attempt older than 24 h ("Expired: Checkout was abandoned."), expire the attempt, and clear the invoice's session pointer. Correct, and money is not lost (settle accepts `failed` rows on late evidence), but it is a visible first-tick change to real rows — a designer's reconciliation view may show N newly failed payments. | Runbook: SELECT the 13 with ages and payment status before push; after the first tick, read `job_runs.detail` and confirm no `succeeded` payment was touched. |
| J23 | nit | high | `invoice-link-checkout/index.ts:68-71` vs `stripe-webhook/index.ts:456,559`, `create-checkout-session/index.ts:98` | `CLIENT_PORTAL_URL` is trailing-slash-normalised in one producer and raw in three; the `/pay` helpers normalise internally, the webhook's own `/invoices/<id>` fallback does not. | Normalise at the `Deno.env.get` in all four. |
| J24 | nit | high | `packages/supabase/src/database.types.ts:31695-31701,33276-33282` | `finalize`/`recover` `Args.p_payer_id` typed `string`, not `string \| null`, though the driver passes `null`. Callers are untyped Deno clients today. | Leave (generator artifact); note for the first typed caller. |
| J25 | nit | high | `_shared/invoice-checkout-driver.ts:186-188` | `surcharge_cents` stamped in Stripe metadata only on the payer rail; nothing reads it. | Stamp on both or neither. |
| J26 | nit | high | `_shared/invoice-check-intent-core.ts:154` | The check-rail notice to the designer always says "Your client is mailing a check…" on the payer-less rail, though the resolver derives a usable name. | Pass `client_display_name` through. |
| J27 | nit | high | `invoice-link.ts:414,423`, `:250,274` | `client_display_name` and `invoice.issue_date` are parsed and never rendered. | Render or drop. |
| J28 | nit | high | `invoice-sheet.tsx:148-153` | `payments as InvoiceLinkPayment[]` is the one unvalidated structural cast left (same-origin, server-parsed). | Reuse `parsePayment`. |
| J29 | nit | high | `checkout/route.ts:25,53` | The route accepts any of the three methods regardless of `pay.rails`; the chooser's allowlist is client-side, `invoice-link-checkout` is the authority. | Note it, or intersect server-side. |
| J30 | nit | high | `invoice-link.test.ts:232` · `letterbox.test.tsx:24-45` | No unit test pins that a `sheet`-only payload dies (the exact contract `062b9bd5c` introduced); the file-global `next/link` mock stringifies `prefetch` to `"undefined"` for every other link. | Add the `sheet`-only case; scope the mock. |
| J31 | nit | high | `invoice-link-checkout/index.ts:14-22` | Header error table omits `invoice_link_not_found` 500, `invalid_payment_method` 400, `checkout_claim_failed` 500. | Update the comment. |
| J32 | nit | high | `payment-method-chooser.tsx:106` · `packages/shared/src/invoice/index.ts:189` | With no `studio_billing_settings` row the check panel says "Contact your designer for mailing details" and the sheet carries no contact but `studio.website`. No wrong address is ever printed. | Surface `studio.website` in that fallback. |
| J33 | nit | high | `00574:1718-1722` | `resolve_invoice_link` granted to `authenticated` (W1 F12): a signed-in PostgREST caller bypasses the portal limiter and inflates `view_count`; nothing in the portal uses that grant. | Drop it in a later migration. |
| J34 | info (runbook) | high | `infra/deploy-portal.sh` · worktree lacks `apps/{client,designer}-portal/.env.local`; the main checkout has both | The script runs no `type-check` (relies on `next build`, which ignores TS errors in these two portals) and its preflight resolves `NEXT_PUBLIC_*` from env files — from this worktree it would refuse to build. | Deploy from the main checkout after merge; run both `type-check`s by hand first (§5). |
| J35 | info | high | `platform_acl_compatibility_test.sql:164` · designer-portal lint | `platform_acl` is red at `:164` for the pre-existing EVENT-3 residual (00574's block at `:819-871` is green standalone and duplicated in two green suites); designer-portal lint has 2 errors in files this branch does not touch. | Nothing for this ship; note in the deploy report. |
| J36 | nit | high | `invoice-link.ts:41` · `invoice-sheet.tsx:435-451` | `studio.logo_url` parsed, never rendered. The W2 review ruled "no logo — accept, firmly" (an arbitrary image URL on a bearer page adds a third-party request). | Drop the key from the payload, or leave. |

**Counts — 36 findings: 1 blocker (J1) · 6 major (J2–J7; J5 is a ruling, J6/J7 are fidelity) · 15 minor (J8–J22) · 11 nit (J23–J33, J36) · 3 info (J34, J35 — runbook/note).**

## The money path — guest, payer-less invoice, on the merged tree

Each step names where a NULL `payer_id` / the link identity could fall through and why it does not.

1. **Open** `GET /pay/<token>` — `apps/client-portal/src/middleware.ts:135-136` (public), `:160-163` (`no-store`, `noindex`); `app/pay/[token]/page.tsx:36` regex gate, `:47` limiter (`invoice-link.ts:536-552`, open in dev, `console.error` once in prod), `:57` resolve. *Safe: no identity read; every failure is the same `DeadLink` (`page.tsx:37-65`).*
2. **`resolve_invoice_link(p_token, true)`** — `00574:1060-1293`: link `:1096`, invoice `:1101`, `v_dead` `:1106`, `view_count` `:1111-1115`, letterhead via `resolve_studio_identity` `:1119-1125`, `v_payer := coalesce(invoice.client_id, project.client_id)` `:1177`. *Safe by construction: `:1179` guards the `profiles` lookup with `IF v_payer IS NOT NULL`; `:1184-1193` roster fallback is `HAVING count(*) = 1`; payments `:1232-1245` and the rollup key on `invoice_id` only.*
3. **Choose a rail →** `POST /pay/<token>/checkout` — `checkout/route.ts:35` limiter, `:42` regex, `:53` method allowlist, `:76-88` server hop with the anon key and **no `Origin`** (`:70-75`), errors relayed verbatim `:90-93`. *Safe: body is `{token, method}`; no identity leaves the browser.*
4. **`invoice-link-checkout`** — `index.ts:96-99` `originAllowed` (absent ⇒ allowed; `= CLIENT_PORTAL_URL` ⇒ allowed; else 403), `config.toml:356-357` `verify_jwt = false`, `:139` regex, `:142` method. **`resolve_invoice_link_for_checkout`** `00574:1302-1349` returns `invoice_id, link_id, payer_id (informational), link_stripe_customer_id, balance_cents, currency, card_surcharge_bps, client_display_name` only for active link + `sent|partially_paid` + balance > 0; empty ⇒ 404 `index.ts:160-162`. *Safe: `payer_id` comes back NULL and is never read (`index.ts:106`).*
5. **`ensureLinkStripeCustomer`** — `_shared/invoice-checkout-stripe.ts:114-166`: per-link customer, **no email**, idempotency key `patina-invoice-link-customer:<linkId>`, compare-and-set via `set_invoice_link_stripe_customer` (`00574:1384-1406`, writes only `WHERE stripe_customer_id IS NULL`, returns the winner); NULL winner ⇒ `customer_persistence_failed` 500. *Safe: the profile branch (`ensureStripeCustomer:39-104`) is never entered on this rail; W1's F5 resolved — `index.ts:209-214` uses the link customer even when a household exists, so no household email is prefilled.*
6. **`claim_invoice_link_checkout_attempt`** — `00574:471-693`: link-on-this-invoice `:510-517`, customer match `:518-522`, payable `:524-530`, surcharge coalesced 300 bps `:532-538`, `requires_refund` refusal `:540-545`, terminal normalisation `:547-561`, F1 guard `:578-580` (**J3: actor-changed only**), supersede `:582-615`; INSERT `payer_id = NULL, invoice_link_id, return_nonce` `:655-665` under `chk_invoice_attempt_actor` XOR `:186-188`; pending `invoice_payments(amount_cents = net, surcharge_cents, method 'stripe', status 'pending', recorded_by = NULL)` `:667-673`. *Safe: `recorded_by` nullable since `00178:137`; link + customer are the whole identity.*
7. **Stripe session** — `_shared/invoice-checkout-driver.ts:314-379`: `customer` = link customer, `payment_method_types = [rail]` (ACH alone on the ACH rail, `verification_method: 'automatic'` `:327-333`), net line + surcharge line `:334-359`, `metadata` **and** `payment_intent_data.metadata` `:361-362` from `invoiceSessionMetadata` `:176-192` = `payable_type, invoice_id, checkout_attempt_id, invoice_link_id, payment_method` — **`payer_id` omitted, never `"null"`** (spread-guarded `:181`; asserted `invoice-checkout-driver.test.ts:59-72`). `success_url`/`cancel_url` = `<CLIENT_PORTAL_URL>/pay/return/<nonce>?checkout=…&session_id={CHECKOUT_SESSION_ID}` (`invoice-checkout-core.ts:181-194`) `+ checkout_attempt_id, payment_id` (`:122-134`). Then `finalize_invoice_checkout_attempt(attempt, p_payer_id = null, customer, session, p_invoice_link_id)` `driver:380-391` → `00574:702-790`. *Safe: `:730` `payer_id IS DISTINCT FROM NULL` is false for NULL; `:731` link term binds; `:751` `recorded_by IS DISTINCT FROM payer_id` NULL-vs-NULL passes. Asserted `invoice_links_test.sql:948-963` (payer-shaped finalize on a link attempt raises `payer_mismatch`).*
8. **Return** — `app/pay/return/[nonce]/route.ts:83` limiter, `:85` regex, `:55-69` `resolve_invoice_return_nonce` (`00574:1360-1377`: `l.status <> 'revoked'`, `l.created_at <= a.created_at`, active-first — F2), 303 to `/pay/<token>` carrying only `checkout, session_id, checkout_attempt_id, payment_id` `:33-38, :90-95`; unknown ⇒ `/pay/dead`. *Safe: nonce→token never touches identity.*
9. **Sheet + poll** — `invoice-sheet.tsx:205-214` consumes `?checkout=`, strips the till params (`checkout-return.ts:70-86`); `useCheckoutConfirmation` polls `GET /pay/<token>/state` every 3 s for 30 s (`:187-211`); `state/route.ts:37` re-resolves with `recordView:false`, returns `status, amount_paid_cents, balance_cents, payments, processing` (`:52-64`). Confirmed when `balance_cents <= 0 && total > 0` (`:185`) or `processing` (`:178-180`, re-derived — J8). *Safe: invoice-scoped. J2 applies once a row is `requires_refund`/`refunded`.*
10. **Webhook `checkout.session.completed`** — `stripe-webhook/index.ts:649-738`: `set_invoice_link_payer_email(metadata.invoice_link_id, session.customer_details.email)` `:664-674` (best-effort; J18 ordering) → `resolveClaimedPaymentRow` with `payerId: metadata.payer_id ?? null`, `invoiceLinkId` `:680-681` → **`resolveExactClaimedPayment`** `invoice-checkout-integrity.ts:78-143`: attempt by `checkout_attempt_id`, XOR `:108`, binds the present term `:109-110`, customer, gross = `amount + surcharge` `:101,:112`, currency, `recorded_by !== attempt.payer_id` `:116` → `persistSignedSessionEvidence` `:239-272` → `finalize` else `recover_invoice_checkout_session_evidence(…, p_invoice_link_id)` (`00574:794-880`). ACH `payment_status='unpaid'` stamps the PI and leaves the row pending `:726-736`. *Safe: `null !== null` is false; the link term binds; asserted `invoice-checkout-integrity.test.ts:149,158,178`.*
11. **`settle_invoice_checkout_payment`** (head `00428:606-703`, grep-confirmed) via `markSucceeded` `index.ts:401-425`: gross assertion `:658-666` (mismatch ⇒ `requires_refund`), terminal early-return `:637-645`, rail stamped `:680-684`. *Safe: no `payer_id`/`recorded_by`/`client_id` in the body; keyed on `p_payment_id` under the invoice lock.*
12. **`apply_invoice_payment_effects`** (head `00571:1094-1238`) via trigger `00178:707-709`: rollup `:1114-1116`, status `:1119-1130` (never resurrects void), `designer_earnings` INSERT keyed on `i.designer_id` with `LEFT JOIN projects` `:1150-1175`, refund contra `:1186-1210`, milestone paid-through `:1212-1236`; `guard_invoice_payment_overpayment` (`00397:1894-1956`) BEFORE-trigger ⇒ `requires_refund` + `payment_discrepancy` review task. *Safe: `designer_earnings` has no `client_id`/`recorded_by` (`00014:299-328`); `project_id` nullable since `00178:183`. Asserted `invoice_links_test.sql:979-988` — settle 10080 `us_bank_account` → invoice `paid`, `amount_paid_cents` 10000, one earning `net_amount` 10000.*
13. **Receipt** — `sendSuccessSideEffects` `index.ts:428-543`: `portalUrl = ensureInvoiceLinkUrl(...) ?? /invoices/<id>` `:454-456` (re-asked per letter, so Regenerate is honoured — K1); `resolveRecipient` `:329-378` rungs profile → project profile → **`invoice_links.payer_email`** `:347-359` → roster keyed on `clientUserId` `:361-375` (skipped when NULL — W1 F6 closed); designer in_app row `:518-536` unconditional. *Partial fall-through, by design: rungs 1, 2, 4 yield nobody; only `payer_email` carries the letter and it exists only after a completed Checkout — which is always true on this path. Every rung empty ⇒ `console.warn` `:510`, no letter. J10: the letter is sent with no `notification_log` row.*
14. **Failure** (`async_payment_failed`) — `index.ts:781-841`: same resolution `:791-803`, payment → `failed` guarded on `pending` `:809-820` ⇒ `sync_invoice_checkout_attempt` (`00397:1991-2016`) fails the attempt, pointer cleared `:827-836`, `sendFailureSideEffects` only if a row flipped `:838-840`. *Safe for state; letter reaches the guest via `payer_email` written at step 10.*
15. **ACH in flight** — `pay.processing` true only for a PI-stamped pending row (`00574:1227-1245`, F3); chooser hidden (`invoice-sheet.tsx:229`); a **different** actor's claim ⇒ `invoice_checkout_in_progress` (`00574:339-341`, `:578-580`) ⇒ 409 `payment_processing` (`driver:221-223`, **J4** renders it as a retry); void refuses (`00574:919-924`), regenerate refuses (`:1452-1458`), sweep never touches `processing` (`:1573`). *J3: the same actor changing rails is the one hole.*
16. **Refund (K11)** — `handleChargeRefunded` resolves by `stripe_payment_intent_id` across the three payable tables; full refund flips `succeeded|requires_refund → refunded`; 00277 contra + milestone un-pay via the effects trigger; refund letter to the **designer** (`:1741-1783`). *Safe: nothing payer-shaped is keyed. J2: the guest sheet then labels the row "Received".*

**What the suites prove:** `invoice_links_test.sql` (1523 lines) walks this exact chain on a genuinely payer-less invoice (`:153-155`, project `client_id` NULL `:112-114`): link claim shape `:915-924`, M8 `recorded_by IS NULL ∧ invoice_link_id ∧ payer_id NULL` `:926-930`, nonce `:940-945`, finalize identity `:948-963`, settle → paid → earnings `:979-992`, F1 both directions `:1181-1200`/`:1277-1290`, F2 `:1502-1506`, F3 `:1141-1179`, M10/M11 `:1207-1273`, sweep `:1434-1483`, regenerate + fresh claim `:1496-1516`, no-PII walk `:638-648`, ACL matrix `:317-335`. Deno: `invoice-checkout-driver.test.ts:59-121` (metadata, XOR, nonce/M7), `invoice-checkout-integrity.test.ts:149-178,235-259,390-477` (link identity, gross, refund ladder). **Not proven anywhere:** J2 (active link + `requires_refund`/`refunded` render), J3 (same-actor rail change on `processing`), `resolveRecipient` and `shouldLog` (no harness for `stripe-webhook/index.ts`), the limiter budget, `refusalSentence` mapping.

## Gates run on the merged tree (Lane E, verbatim tails in its report)

| Gate | Result |
|---|---|
| `type-check` — `@patina/supabase`, `@patina/utils`, `@patina/client-portal`, `@patina/designer-portal` | all exit 0 |
| `pnpm --filter @patina/admin-portal build` (strictest; `@patina/supabase` changed) | exit 0 |
| `@patina/client-portal test` | 129 suites / 1932 tests pass |
| `@patina/designer-portal test -- src/components/document/accounts src/hooks` | 30 / 281 pass |
| `@patina/utils test` · `@patina/supabase test` | 20 / 600 · 85 files / 1036 pass, 12 skipped |
| designer-portal `lint` | 2 errors, both in untouched files (J35); 203 standing warnings |
| SQL: `invoice_links`, `invoice_checkout_integrity`, `studio_invoice`, `public_rpc_authorization_contract`, `public_sd_hardening_contract` | all exit 0, all `ROLLBACK` |
| SQL: `platform_acl_compatibility` | exit 3 at `:164` only — pre-existing (J35) |
| Deno test (`_shared`, 6 functions) · `deno check` 6 entrypoints · `deno.lock` | 356 pass / 0 fail · clean · absent |
| Playwright `tests/pay-link.spec.ts` (chromium, local stack at 00574, `.env.local` absent in the worktree) | **7/7 pass** — sheet + three figures + revoke; void → withdrawn; houseless studio invoice; 390 px reflow; return hop 303 + unknown nonce → dead; garbage/malformed token → same dead sheet; letterbox never requests `/pay/` |
| `database.types.ts` | +130/−3 vs main, ends `as const` |

Local stack left at 00574, untouched (e2e fixtures left rows, as designed; `supabase db reset` is the broom).

## Production runbook — as the deploy agent should execute it

Authority: K9 grants full production authority for W1–W3 without per-step re-asking, once the pre-merge fixes are in and Kody has said "ship" in the deploying session. All commands absolute-path; `cd` never persists. `MAIN=/Users/kody/Code/patina-merged`. Names only for secrets; never echo values.

**Step 0 — fix pass, gates, merge (no prod touch).**
1. Land J1, J2, J3, J4 (and the J5 ruling's default, J6/J7/J8/J15 if taken) on `invoice-standalone/integration` in its worktree; each with its test. J3 edits 00574 in place — permitted, the migration is unshipped (patina-db-migrations step 8).
2. Re-run, all from the worktree: the four `type-check`s, `pnpm --filter @patina/admin-portal build`, `pnpm --filter @patina/client-portal test`, `pnpm --filter @patina/designer-portal test -- src/components/document/accounts src/hooks src/lib/analytics`, `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -f supabase/tests/billing/invoice_links_test.sql` (after `supabase db reset` **only if** the local stack's `NEXT_PUBLIC_SUPABASE_URL`/DB is confirmed local — J34), `deno test --allow-all --config supabase/functions/deno.json supabase/functions/_shared supabase/functions/stripe-webhook`, and `env -u CI pnpm --filter @patina/client-portal test:e2e -- --project=chromium tests/pay-link.spec.ts` with `SUPABASE_SERVICE_ROLE_KEY` exported from `supabase status -o json`. If J3 changed 00574: `pnpm db:generate` + `git diff --exit-code packages/supabase/src/database.types.ts` (no signature changed, so expect clean) and `python3 scripts/generate-legacy-grants.py` only if a GRANT/REVOKE changed.
3. Merge: `git -C $MAIN switch main && git -C $MAIN merge --no-ff invoice-standalone/integration` (the main checkout carries unrelated modified files — `CLAUDE.md`, `.claude/settings.json`, a pbxproj, six screenshots — none overlap the branch; do not stage them). Record **`CLIENT_PORTAL_SHA=$(git -C $MAIN rev-parse HEAD)`** in the deploy report — K9: W3b deploys later from a later commit, and this SHA is the client portal's rollback anchor. `git -C $MAIN push origin main`.
4. Confirm `supabase --workdir $MAIN migration list --linked` shows exactly one gap: `00574` local, remote empty. Confirm `ls $MAIN/supabase/migrations | tail -2` = `00574_invoice_links.sql`, `_pending`.
5. Pre-push snapshot of J22: `supabase --workdir $MAIN db query --linked "select a.id, a.state, a.created_at, p.status from public.invoice_checkout_attempts a left join public.invoice_payments p on p.checkout_attempt_id = a.id where a.state in ('claimed','session_created') order by a.created_at;"` — save in the report.

**Step 1 — migration → Strata.**
- `supabase --workdir $MAIN db push --linked` (linked ref `bkvcixdmuyejfzcijpdg`; the worktree lacks `.temp` — run from `$MAIN`).
- Probe (object, not ledger): `supabase --workdir $MAIN db query --linked "select to_regclass('public.invoice_links') t, (select count(*) from public.invoice_links) links, (select count(*) from public.invoices where status in ('sent','partially_paid','paid')) payable, (select is_nullable from information_schema.columns where table_name='invoice_checkout_attempts' and column_name='payer_id') payer_nullable, (select count(*) from pg_constraint where conname in ('chk_invoice_attempt_actor','chk_invoice_attempt_nonce')) checks, (select jobname from cron.job where jobname='invoice-checkout-attempts-expire') job, has_function_privilege('anon','public.resolve_invoice_link(text,boolean)','EXECUTE') anon_exec;"` — expect `invoice_links`, `links = payable = 22`, `YES`, `2`, the job name, `f`.
- Rollback (additive; only if step 1 must be undone alone): `DROP TRIGGER IF EXISTS invoice_link_mint_on_issue ON public.invoices; SELECT cron.unschedule('invoice-checkout-attempts-expire');` — freezes minting and the sweep without breaking a link. `payer_id DROP NOT NULL` is a widening and stays.

**Step 2 — client portal (the deviation: the route must exist before any function emits it).**
- `pnpm --dir $MAIN --filter @patina/client-portal type-check` (the build ignores TS errors — J34), then `$MAIN/infra/deploy-portal.sh client` from `$MAIN` (Phase 1 rebuilds `@patina/utils` dist — never a raw `opennextjs-cloudflare build`). Ends "Done: client portal deployed."
- Verify: `npx wrangler deployments list --name patina-client-portal` — **bottom** row newer than the deploy start (prior live: `78ce6497-324e-4c29-976d-305b993180bc`, 2026-09-06T07:20:31Z).
- Served-chunk / behaviour probes (today all three 307 to `/auth/signin` — that is the failure to rule out):
  `curl -sS -o /dev/null -D - https://client.patina.cloud/pay/dead` → **200**, `cache-control: no-store`, `x-robots-tag: noindex`, no `location`;
  `curl -sS -D - https://client.patina.cloud/pay/$(printf 'a%.0s' $(seq 1 64))` → **200** dead sheet with the same two headers; body contains the `DeadLink` copy (read the literal from `apps/client-portal/src/app/pay/[token]/dead-link.tsx` before probing) and `bkvcixdmuyejfzcijpdg` (the inlined Supabase URL — the placeholder-incident grep) and **no** `undefined`/`localhost`;
  `curl -sS -o /dev/null -D - https://client.patina.cloud/pay/return/$(printf 'b%.0s' $(seq 1 64))` → **303** `location: /pay/dead`;
  `curl -sS -o /dev/null -w '%{http_code}\n' https://client.patina.cloud/api/version` → 200;
  limiter: `for i in $(seq 1 31); do curl -sS -o /dev/null -w '%{http_code} ' "https://client.patina.cloud/pay/$(printf 'c%.0s' $(seq 1 64))"; done` — all 200 (dead sheet either way; the proof of the binding is the **absence** of the `PAY_LINK_RATELIMIT` missing-binding `console.error` in `npx wrangler tail patina-client-portal` during the burst);
  service worker: in a real browser, open a `/pay/<token>` and confirm `caches.keys()` holds nothing matching `/pay/` (S1).
- Rollback: `npx wrangler rollback 78ce6497-324e-4c29-976d-305b993180bc --name patina-client-portal --yes` — **only after** step 4's functions are rolled back (letters would otherwise point at a 404). Nothing a homeowner reaches through the letterbox is lost (W2/W3 removed nothing).

**Step 3 — designer portal.**
- `pnpm --dir $MAIN --filter @patina/designer-portal type-check`, then `$MAIN/infra/deploy-portal.sh designer`.
- Verify: bottom row of `npx wrangler deployments list --name patina-designer-portal` (prior live: `090b5c5e-82bb-4141-b6a5-4a32126d4221`); `curl -s https://app.patina.cloud/api/version` → 200; signed-in walk (Kody, or the seeded tester): a sent invoice's folio shows **Copy link** and **Regenerate link**; Copy puts `https://client.patina.cloud/pay/<64hex>` on the clipboard; that URL opens the sheet signed out. Grep the served folio chunk for the Regenerate confirmation literal ("The old link stops working").
- Rollback: `npx wrangler rollback 090b5c5e-82bb-4141-b6a5-4a32126d4221 --name patina-designer-portal --yes` — Copy link disappears, links keep working.

**K8 PAUSE — Kody.** Before any function deploys: Kody sets the **live** `STRIPE_SECRET_KEY` (Middle West Studio `acct_1T6KiLJmCVe1Jxdu`, live mode — the same account and mode as the live webhook endpoint) in the Supabase dashboard → Edge Functions → Secrets. Agents never see the value. Confirm by name only: `supabase --workdir $MAIN secrets list --linked | awk '{print $1}' | grep -E '^(STRIPE_SECRET_KEY|STRIPE_WEBHOOK_SECRET|CLIENT_PORTAL_URL)$'` (all three exist today). `STRIPE_WEBHOOK_SECRET` must be the dashboard endpoint's `whsec_`, not a `stripe listen` one. Also confirm `CLIENT_PORTAL_URL` = `https://client.patina.cloud` (Kody reads it in the dashboard; the checkout route sends no Origin, so a mismatch only affects the letters' host).

**Step 4 — the six functions (never before step 2).**
- Re-grep first (D3): `grep -rl "_shared/invoice-checkout\|_shared/invoice-links\|_shared/invoice-check-intent" $MAIN/supabase/functions --include='*.ts' | grep -v _shared | grep -v '\.test\.' | sed 's#.*/functions/##; s#/.*##' | sort -u` → exactly `create-checkout-session invoice-check-intent invoice-link-checkout invoice-reminders invoice-send stripe-webhook`; and `git -C $MAIN diff --name-only 19f64a0a1..HEAD -- supabase/functions/_shared | grep -v test` → the six touched modules and nothing else (`client-portal-links.ts` is untouched on this branch).
- Note prior versions for rollback: `supabase --workdir $MAIN functions list --linked` → today `create-checkout-session v45`, `invoice-reminders v44`, `invoice-send v45`, `stripe-webhook v49`, `invoice-check-intent v20`; `invoice-link-checkout` absent (first deploy).
- `supabase --workdir $MAIN functions deploy invoice-link-checkout --no-verify-jwt` then `supabase --workdir $MAIN functions deploy create-checkout-session`, `stripe-webhook`, `invoice-send`, `invoice-reminders`, `invoice-check-intent` (one each, by name; `config.toml` already carries the `verify_jwt = false` stanza at `:356-357`). Confirm `ls $MAIN/deno.lock` → absent afterwards.
- Probes: `curl -sS -o /dev/null -w '%{http_code}\n' -X POST https://bkvcixdmuyejfzcijpdg.supabase.co/functions/v1/invoice-link-checkout -H 'content-type: application/json' -H 'Origin: https://evil.example' -d '{"token":"'$(printf 'a%.0s' $(seq 1 64))'","method":"card"}'` → **403**; same with no `Origin` → **404** `{"error":"invoice_not_found"}`; `-X GET` → 405; a malformed token → 404 (same silence). `functions list` shows the six at new versions.
- Rollback per function: redeploy the prior version from the dashboard's version history (or from `git -C $MAIN show 19f64a0a1:supabase/functions/<fn>/index.ts` into a worktree and `functions deploy <fn>`); `invoice-link-checkout` rollback = delete the function (`supabase functions delete invoice-link-checkout --linked`). Letters revert to `/invoices/<id>`; any `/pay` link already in the wild still works because step 2 stands.

**Step 5 — smoke (§12 (a)–(h)) and the K11 live proof.**
- (a) Cloudflare dashboard: no zone Cache Rule matches `/pay/*`. (b) done in step 2. (c) headers on a real `/pay/<token>` from step 3's Copy link: 200, `no-store`, `noindex`. (d) `caches.keys()` empty for `/pay`. (g) `supabase --workdir $MAIN db query --linked "select jobname, schedule from cron.job where jobname='invoice-checkout-attempts-expire'"` → `17 * * * *`; after the next :17, `select status, detail, error from public.job_runs where job_name='invoice-checkout-attempts-expire' order by started_at desc limit 1` → `succeeded`, `detail.expired` ≈ the count of >24 h `session_created` rows from step 0.5, and **no** row whose payment was `succeeded` changed (`select count(*) from invoice_payments where status='failed' and note like '%Expired: Checkout was abandoned%' and stripe_payment_intent_id is not null and updated_at > now() - interval '2 hours'` — investigate any non-zero against Stripe before calling it clean; J22). (h) `npx wrangler tail patina-client-portal` for the first 30 minutes: no `PAY_LINK_RATELIMIT` error, no 5xx.
- **K8 proof + K11, first Kody pause ("pay").** Seed a $1.00 invoice on one of Kody's test households on Strata (issue it via the folio so the mint trigger — not `ensure_invoice_link` — makes the link; W1 deviation 5 named the ceremony path as the gap the SQL suite does not cover). Hand Kody the `/pay/<token>` from Copy link. Kody, signed out on his phone, chooses card and pays **$1.03**. Before he taps Pay, prove alignment from the session the page opened: `select stripe_checkout_session_id, state, payment_method from public.invoice_checkout_attempts order by created_at desc limit 1` → `cs_live_…`, `session_created`; then have him back out once and choose bank transfer to prove `us_bank_account` is offered (the ACH session is created with `payment_method_types=['us_bank_account']` and fails loudly if ACH is not enabled on the live account), then cancel and pay by card.
- After payment: `select p.status, p.amount_cents, p.surcharge_cents, p.stripe_payment_method_type, a.state, l.payer_email is not null as has_email from invoice_payments p join invoice_checkout_attempts a on a.id = p.checkout_attempt_id join invoice_links l on l.id = a.invoice_link_id where p.invoice_id='<id>'` → `succeeded, 100, 3, card, succeeded, t`; `select status, amount_paid_cents from invoices where id='<id>'` → `paid, 100`; `select count(*) from designer_earnings where source_id='<id>'` (or the column the 00571 body uses) → 1; `select id from stripe_webhook_events where created_at > now() - interval '10 minutes'` non-empty; the receipt letter arrived at the address Kody typed in Checkout (`resend` dashboard or Kody's inbox) and its button opens `/pay/<token>` showing **Paid in full** with the payment row "+ $0.03 processing fee ($1.03 charged)"; the folio shows the payment row.
- **Second Kody pause ("refund").** Kody refunds in the Stripe dashboard. Then: `invoice_payments.status = 'refunded'`, the contra `designer_earnings` row, invoice back to `sent`, the designer's refund letter — and, **with J2 landed**, the guest sheet shows the refunded row honestly and offers the chooser for the restored balance.
- Record in the deploy report: `CLIENT_PORTAL_SHA`, both Worker version ids (old and new), the six function versions (old and new), the K8 confirmation (names only), the J22 first-tick numbers, and the K11 SELECT outputs.

**Not in this deploy:** W3b (`invoice-standalone/w3b`, `f640e74b8`) — not an ancestor of the integration branch; its client-portal deploy is a later `deploy-portal.sh client` once Kody's walk is done (K9). W4 iOS — follow-up TestFlight.

## Verified correct

- **Branch state.** main `19f64a0a1` is the merge-base and main has not moved; no other branch carries a `0057[4-9]` migration; `_pending` holds only the unrelated `00106`; `database.types.ts` regenerated on the merged tree with `invoice_links`, `resolve_invoice_return_nonce`, `regenerate_invoice_link`, `get_invoice_link`, 5-arg finalize/recover, nullable `payer_id`; `seed/00-legacy-grants.sql` carries the full 00574 REVOKE/GRANT block and correctly drops the dead 4-arg grants; no file was edited by two waves (W3's `36c734fbe..e475a3850` and W1's review commits are disjoint).
- **The four integration commits.** `062b9bd5c`: both `.rpc()` calls match `database.types.ts` (`resolve_invoice_link {p_token, p_record_view?} → Json` `:33682`; `resolve_invoice_return_nonce {p_nonce} → string` `:33699`); both `as never` casts gone; the return route calls the RPC; `kind`-only parse returns `null` for a `sheet`-only payload (`invoice-link.ts:306-307`). `9f3d87b5a`: a parser guess (`contact.name`) against a migration shape 00574 always emitted and its SQL test already pinned (`invoice_links_test.sql:1351`) — fix on the right side; the effect before the fix was every voided invoice rendering dead, caught only by the e2e against the real RPC. `d6a20bf87`: `issue_invoice` genuinely has no service_role EXECUTE (`00412:2727-2729`; probed `f`), the mint trigger is `AFTER UPDATE OF status` so the fixture's UPDATE takes the trigger path; `studio_id` on the housed project matches what `set_project_studio_id` requires; stamping the link customer before the claim is the driver's own order — a strengthening. `83f829377`: the unit test asserts `prefetch={false}` at the `next/link` module boundary through `ScoredAction` (red when the prop is deleted, per the commit), and the e2e honestly disclaims prefetch coverage and proves no `/pay/` request instead.
- **Every wave fix landed** (Lane A's 21-row checklist): W1 F1 both claims (`00574:339-340`, `:578-579`), F2 (`:1370-1375`, test `:1502-1506`), F3 (`:1245`, test `:1141-1179`); W2 S-1/I-2, S-4 (no Origin — headers are `Content-Type`, `apikey`, `Authorization` only, `checkout/route.ts:70-88`), T-1..T-4, S-2, S-5 (every `payLinkEvents` export has a call site), I-4, I-5 (`pay.rails` drives the chooser, `payment-method-chooser.tsx:104`), P-1, P-2; W3 F1 (`use-invoices.ts:390-393`, called from issue/record-payment/send/void), F2 (`meta: {errorSurface:'silent'}`, `retry:false`, null on error), F3 (`letterPortalUrl` in `_shared/invoice-links.ts:79`, both producers), F4, F6. W1 F7 is moot (`client-portal-links.ts` untouched). Only I-7 is partial (J8).
- **Resolver ↔ page contract.** Every key the payable sheet reads is emitted with the right key, type and nullability (all sixteen `invoice.*`, five `line_items[].*`, five `payments[].*` incl. `rail` narrowed to `card|us_bank_account|null` by `00428:130-136`, `payment_options.*`, `pay.rails`, `pay.processing`, `designer_display_name`); no emitted key trips the forbidden walk (`note` is correctly never emitted); the withdrawn `contact` shape matches after `9f3d87b5a`; the dead outcome is one `RETURN NULL` → one `DeadLink`; `/state` reuses the same RPC and parser.
- **Wire contracts.** Checkout body `{token, method}` ↔ `body.method ?? body.payment_method ?? body.paymentMethod`; response `{url}` for card/ACH, `{ok, alreadyNotified}` for check, read exactly so by the sheet; every function status/body relayed verbatim. Return hop: `CARRIED_PARAMS` = exactly the four keys both session creators write; `consumeCheckoutReturn` reads the same names and accepts `cancel`/`cancelled`. `regenerate_invoice_link` raises `P0001 'invoice_checkout_in_progress'`, surfaced by PostgREST as `message`, matched by the hook's `includes`; it gates the same three states as void and the claims, revokes with `revoked_at` satisfying `chk_invoice_links_dead`, and `get_invoice_link` never returns a revoked link. Letters: all four producers use one helper, one `CLIENT_PORTAL_URL`, one `/invoices/<id>` fallback; `ensure_invoice_link` mints for a linkless `sent` invoice and returns NULL for draft/void.
- **Money invariants under the link rail.** `amount_cents` net + `surcharge_cents` separate, one formula, gross asserted at settle; `uniq_invoice_checkout_active_attempt` shared by both rails; `apply_invoice_payment_effects` (head 00571) touches neither `payer_id` nor `recorded_by`; 00277 contra keys on `reverses_invoice_payment_id`; the household claim's identity checks run before the supersede branch; `payer_id` omitted (not `"null"`) from Session and PI metadata; the link customer is created with no email so Checkout collects one; `set_invoice_link_stripe_customer` and `set_invoice_link_payer_email` are compare-and-set / last-non-empty-wins.
- **Production posture.** 00574: every statement guarded (`IF NOT EXISTS`, `DO $$ … pg_constraint` around both CHECKs, `DROP TRIGGER IF EXISTS`, `DROP FUNCTION IF EXISTS` on the grown signatures, backfill `ON CONFLICT … WHERE status='active'`, unschedule-before-schedule, batched REVOKE then GRANT), `extensions.gen_random_bytes` schema-qualified, every definer pins `search_path`, no seed/uuid/localhost assumption; Strata head `00573`, `invoice_links` and `resolve_invoice_link` absent, 22 payable invoices all with `studio_id`, 19 attempts all with non-null `payer_id` (the XOR CHECK validates), `pgcrypto`/`pg_cron`/`pg_net` present, `resolve_studio_identity(uuid,uuid,uuid)` resolves, the three secret names present. `wrangler.jsonc`: top-level `ratelimits` `PAY_LINK_RATELIMIT` `namespace_id "1001"` `simple {limit 30, period 60}`, duplicated into `env.staging` with `"1002"` (W2's S-9 taken); validated against wrangler 4.107.0's `config-schema.json` and by `wrangler deploy --dry-run` (parsed clean; failed only on the absent `.open-next/worker.js`). `config.toml` stanza present with the S3 rationale. Redeploy set exactly six. `deno.lock` absent. `middleware.ts:126-158` exempts `/pay` and `/pay/*` — today's prod 307 becomes the post-deploy 200 that proves the route is live.
- **Security posture held.** Token never logged (grepped `_shared`, the function, `app/pay/**`); `anon` holds EXECUTE on none of the sixteen functions (SQL suite `:751,:756`); `server-only` boundary on `invoice-link.ts`; four allowlists open (`middleware`, `app-chrome`, client `posthog`, `NetworkOnly` before the catch-all); AASA carries no `/pay`; the check rail writes only `notification_log`; over-limit and dead are the same silence; the return hop is a four-key allowlist over a re-validated token.
