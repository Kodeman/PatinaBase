# Build rulings K4–K11 — Kody, interviewed 2026-09-06

These extend K1–K3 (brief.md) and Fable's T4 rulings (review/04-rulings.md). Architecture of record: `architecture/02-system-design.md` v2. Where v2 and these conflict, these win.

| # | Ruling | Consequence for the build |
|---|---|---|
| K4 | **Link-payer path, confirmed.** `invoice_checkout_attempts.payer_id` nullable behind `CHECK (payer_id IS NOT NULL OR invoice_link_id IS NOT NULL)`; `invoice_link_id` is a first-class identity term everywhere `payer_id` is one (driver guards, Session + PaymentIntent metadata, `assertInvoiceSessionIdentity`, `stripe-webhook/invoice-checkout-integrity.ts`, `finalize_invoice_checkout_attempt`, `recover_invoice_checkout_session_evidence`); per-link Stripe customer; `invoice_links.payer_email` captured at `checkout.session.completed`. | W1 as v2 §2.4, §2.5, §4. The pinned signature literal for `recover_invoice_checkout_session_evidence` in `supabase/tests/billing/invoice_checkout_integrity_test.sql` is updated in the same wave. |
| K5 | **Void → the withdrawn sheet** (supersedes "dead on void" in K2 and T4 note 2). `void_invoice`'s legacy body sets the link `status='closed'`; `resolve_invoice_link` returns a minimal payload for a closed link: `{ kind: 'withdrawn', studio, invoice.number, invoice.title, contact }` — no amounts, no lines, no chooser. Closed + a `pending`/`requires_refund` payment → the settling sheet (T4 M9+M10). Revoked (regenerated) or unknown → dead. | W1 resolver + SQL tests; W2 renders `withdrawn` beside `settling` and `dead`. Copy: "Invoice No. 4 was withdrawn by Quist Interiors." |
| K6 | **Host: `https://client.patina.cloud/pay/<token>`**, decided. `pay.patina.cloud` never. | No env split; `invoiceLinkUrl(CLIENT_PORTAL_URL, token)`. |
| K7 | **No expiry.** Link lives with the invoice. | No `expires_at` column, no predicate. |
| K8 | **Stripe key alignment is inside the program.** Kody pastes the **live** `STRIPE_SECRET_KEY` (Middle West Studio `acct_1T6KiLJmCVe1Jxdu`, live mode, same account and mode as the live webhook endpoint) into the Supabase dashboard (Edge Functions → Secrets) when the deploy wave reaches it; agents never see the key. Agents then redeploy the six functions and prove alignment. | Deploy wave pauses for Kody at "secrets"; a probe proves a created session id starts `cs_live_`, the webhook endpoint's signature accepts a live event (dashboard "Send test event" from Kody, or the first real event), and `us_bank_account` is offered on the session. |
| K9 | **Ship scope:** W1–W3 to production in this program; **W3b** (retire settle-in-place + `/invoices/[id]/print`) built and merged but its deploy **held** until Kody's signed-in walk; **W4 iOS** is a follow-up TestFlight build. Full production authority for W1–W3 without per-step re-asking (migration 00574 → client portal → designer portal → six functions, per v2 §12). | The W3b branch is merged to main behind nothing — it is a separate deploy, not a flag; the client portal is deployed from the commit BEFORE W3b's merge, and W3b's deploy is a later `deploy-portal.sh client` once Kody says so. Record that commit SHA in the deploy report. |
| K10 | **Live key on prod.** The proof is real money. | Test-mode cards will not work on prod after K8; all automated money tests run on the LOCAL stack (Stripe test keys locally) and in the Deno/SQL suites. |
| K11 | **The live proof shape:** the team seeds a $1.00 invoice on one of Kody's test households on Strata and hands Kody the `/pay/<token>` link; Kody pays $1.03 by card on his phone, signed out (agents never touch card details); the team verifies webhook settlement, receipt letter, the page's receipt state, the folio's payment row; Kody refunds in the Stripe dashboard; the team verifies the refund reconciled (`invoice_payments` refunded, earnings contra, invoice back to `sent`). ACH is not proven live — covered by SQL/Deno suites + the session probe. | The verification wave has two Kody pauses: "pay" and "refund". Prerequisite checks before the pause: live webhook endpoint exists with a live signing secret matching `STRIPE_WEBHOOK_SECRET` (prove by a signed event, not by reading secrets), ACH enabled on the live account (Stripe dashboard → Kody confirms, or the session offers it), `cs_live_` prefix. |

Decided without a question (low stakes, from the codebase): rate-limit binding `PAY_LINK_RATELIMIT` namespace_id `1001` (no other `ratelimits` binding exists in any Patina Worker); `view_count` not surfaced; no feature flag (the page is public and additive; the letters' switch to `/pay` is the cutover, per K1); Regenerate refuses while an attempt is live; analytics `payLinkEvents` with the whitelist in v2 §9.
