# W0 — CORRECTNESS review

Union of `document-lens/w0-l1` (`80af65c32`) · `w0-l2` (`15e581435`) · `w0-l3` (`91765a1cf`)
vs base `main@dab057537` · reviewed 2026-08-29
Spec of record: `build/design/technical-design.md` OD-4 / OD-9 · `build/test-impact.md`
Canon: `AGENTS.md`, `patina-verification`, `patina-testing`, `patina-db-migrations`

Reviewer wrote none of this code. Read-only pass: `git show`/`git diff` against explicit refs, a
scratch checkout of `w0-l1` under `$TMPDIR` (jest re-runs and one product mutation), and
`BEGIN; …; ROLLBACK;` executions of the L2 seed against the local stack only.

> **Note on the base.** `document-lens/w0` was fast-forwarded during this review (it now sits at
> `1f55eb5b0`, the integration of all three lanes). Everything below is measured against
> `dab057537`, which is what `document-lens/w0` equalled when the lanes were cut, and which is
> still `main`/`HEAD`.

---

## Verdict

| ship | ship-after-fixes | do-not-ship |
|---|---|---|
| | **✔ this one** | |

Nothing here is unsafe and nothing regresses the product — L1 touches no product file at all (the
`data-section-stage-line` attribute OD-9 asked for already ships at `section-stage-line.tsx:44`),
L2 adds a hand-run seed nothing loads automatically, and L3's most important property holds:
**no assertion was weakened to make a spec pass.** What blocks a clean ship is evidence, not code.
The lane whose entire deliverable is a tripwire shipped a tripwire **that has never been run**
(L1-10); the seed's header invites a run against a live database (L2-1); and the wave's declared
jest arithmetic is wrong (X-1, +3 not +1). Two of the new tripwires are also written so that the
program's own Wave 1/2 plan turns them red (L1-2) or leaves them silently vacuous (L1-4).

### Gate evidence

| Gate | Result |
|---|---|
| `npx jest --ci` on `w0-l1` (scratch tree, full designer-portal suite) | **458 suites / 5173 tests — all green** |
| `npx jest --ci` on `dab057537` (same scratch harness, control) | **458 suites / 5170 tests — all green** → delta **+3**, declared +1 |
| Mutation: `<SectionStageLineMount>` hoisted above `<div data-active-section>` in the scratch `page.tsx` | new containment test **FAILS** — the tripwire is real |
| `browserslist` resolution of OD-4's five queries | valid, **25 targets** (chrome 145/144, edge 145/144, firefox 148/147, ios_saf 26.3→17.0, safari 26.3/26.2); `caniuse-lite` warns it is 6 months stale |
| Regex-gap measurement in `page.tsx` (`<ProjectApprovalDocumentMount` → next `clientProfileId={row.client_profile_id`) | **5504 chars** — the widened `{0,1200}` negative regex has real headroom, no false failure |
| `resolveRailFiles()` enumerated against the live tree | **6 files**, not five — includes `doc-spine.test.tsx` |
| L2 seed, `BEGIN; <seed>; ROLLBACK;` against local `127.0.0.1:54322` | applies clean on the **current** (post-L3-reset) DB; 5 rooms / 62 lines / 58 with product; status distribution matches `seed-notes.md` exactly |
| L2 seed run **twice** in one transaction + `seed-verify.sql` | **14/14 PASS**; `margin_items` = 7 (3 `line` / 4 `letterhead`) read from the VIEW |
| L2 idempotency probe (`po_payments.due_date`, CM-1187) | run 1 = `2026-09-24`, run 2 = **NULL** → non-convergent (L2-4) |
| Local prerequisites | `products` = **21**, `designer_clients` fixture = 1, both seed users present |
| Persistence check after every seed run | `projects` where id = `…d5` → **0 rows**. Nothing was committed. |
| `git diff --stat dab057537 document-lens/w0` vs the three lane diffs | identical per-file counts — the merges introduced no resolution edits |
| Recorded e2e evidence for the new landing-clearance spec | **none** — `e2e-run-w0-after.log` lists exactly six `quiet-responsive-shell.spec.ts` tests (`:94 :157 :179 :199 :215 :243`) |

### Counts

| blocker | high | medium | low | verified-ok |
|---|---|---|---|---|
| 0 | 2 | 6 | 14 | 14 |

---

## 1 · L1 — tripwires

| id | sev | conf | file:line | finding + failure scenario |
|---|---|---|---|---|
| **L1-1** | medium | 0.95 | `src/lib/document/__tests__/contrast.test.ts:304-312` | The glob does **not** resolve "the same five files". `/spine.*\.tsx$/i` over the top level of `components/document/` matches **`doc-spine.test.tsx`** as well as the four source files, so `resolveRailFiles()` returns **six** paths — five sources plus a test file — where the retired hard-coded list held five sources. Verified by enumerating the live directory. Two consequences: the `>= 5` floor below is partly satisfied by a test file, and a future *negative* assertion inside `doc-spine.test.tsx` that quotes `text-[var(--color-aged-oak)]` (exactly the string such a test would quote) will be read as an offender and fail the guard. Fix: `&& !/\.test\.tsx$/.test(entry.name)`. |
| **L1-2** | medium | 0.85 | `src/lib/document/__tests__/contrast.test.ts:360-365` | The `>= 5` floor is broken by the program's own OD-16 schedule. W1 deletes `spine-timer.tsx` → 5 (passes). W2 deletes `spine-running-index.tsx` + `spine-shelved-blocks.tsx` and adds `spine/lens-ladder.tsx` → **4 → FAIL**. Scenario: W2-L1 lands the deletions the design ruled, and a Wave-0 tripwire written to catch an *empty* glob fires on a *correct* change; the likely reflex is to lower the number, which is how the guard rots. Fix the shape, not the number: assert the set is non-empty **and** contains the ladder once it exists (`some(f => f.endsWith('lens-ladder.tsx'))` after W2), rather than a magic count. |
| **L1-3** | low | 0.75 | `src/lib/document/__tests__/contrast.test.ts:336` | Widened form 2, `\[color:var\(…\)\]`, matches Tailwind's **type-hinted arbitrary value for any colour property**, not just text: `bg-[color:var(--color-clay)]`, `border-[color:…]`, `outline-[color:…]` all match. The rail legitimately spends clay as a *fill* and a *ring* today — `spine-running-index.tsx:79` `bg-[var(--color-clay)]` (the 2px active bar), `doc-spine.tsx:49` / `margin-rail.tsx:228,277` `focus-visible:outline-[var(--color-clay)]` — and the ruling this guard enforces is about **labels**. Rewriting any one of those in the type-hint spelling turns a legal ink into a false offender. Forms 1 and 3 are correctly property-scoped; only form 2 is not. |
| **L1-4** | low | 0.85 | `src/lib/document/__tests__/contrast.test.ts:384-401` | The paper-ground test is a **silent no-op** today: `PAPER_FILES` filters on `existsSync('src/components/document/lens-band.tsx')` and `return`s at `:394` with **zero `expect()` calls**, so it prints green while asserting nothing. Worse, the path is guessed: OD-6/OD-1 bind `lens-band.tsx` without pinning a directory, and if it lands under `components/document/spine/` the nested arm of `resolveRailFiles()` (`:315-321`) picks it up and measures it against **rail stock** — precisely the wrong-ground error this test exists to prevent — while this test stays vacuous forever. (The test is meaningful once the file exists at the assumed path: measured on `--doc-paper #FCFAF6`, aged-oak is 4.30:1 and clay 2.23:1, so `OFF_REGISTER_ON_PAPER` is both pigments, not empty.) |
| **L1-5** | ok | 0.95 | `src/app/(document)/doc/[id]/page.test.tsx:1244-1256` | **The containment test genuinely fails when the mount escapes.** Reasoned and then proven: in a scratch copy I lifted the whole `<SectionStageLineMount …/>` element above `<div data-active-section>` and re-ran — `✕ nests [data-section-stage-line] inside [data-active-section]`. The surviving source-order assertion in `stage2-approval-cutover-contract.test.ts:18` does **not** catch that move, so the replacement is strictly stronger than the 1500-char regex it retires. Supporting facts checked: `page.tsx` has exactly one JSX `data-active-section` (`:1942`; the other two string hits are a `querySelector` at `:1170` and a comment at `:1961`), and every branch of `SectionStageLineMount` — loading, error, model, no-model — emits `data-section-stage-line`, so a mount that renders at all is caught. |
| **L1-6** | low | 0.60 | `src/app/(document)/doc/[id]/page.test.tsx:1249-1256` | Two cosmetic weaknesses in an otherwise sound test. It queries `document` globally rather than the `render` result (fine under RTL auto-cleanup, brittle if a sibling test ever leaves a node behind), and the `all.indexOf(stageLine) > all.indexOf(activeSection)` assertion is **entailed** by `contains()` — a descendant always follows its ancestor in a flattened `querySelectorAll('*')` — so it adds no coverage. Harmless; the containment half carries the test. |
| **L1-7** | ok | 0.90 | `src/lib/document/__tests__/stage2-approval-cutover-contract.test.ts:23` | The widened negative regex is correct and is a **strengthening**. Measured on `page.tsx@dab057537`: `<ProjectApprovalDocumentMount` sits at byte 79493 and the next `clientProfileId={row.client_profile_id` at 85026 — a 5504-char gap, so `{0,1200}` cannot fire today and still has ~4300 chars of headroom after W3 removes its ~34 lines. Deleting `:19`'s `data-active-section[\s\S]{0,1500}?<SectionStageLineMount` was also right for a second reason the design does not state: it could match the *comment* at `:1961` (gap 162) as readily as the JSX at `:1942` (gap 1128), so it was never really asserting containment. |
| **L1-8** | ok | 1.00 | `apps/designer-portal/package.json:115-121` | The `browserslist` value is byte-identical to OD-4's, in OD-4's order. All five queries resolve (25 targets). `iOS >= 17` is a valid `ios_saf` alias. Consistent with the `@supports (content-visibility: auto)` fallback the design pairs it with — iOS 17 has no `content-visibility`, which is exactly why the guard exists. |
| **L1-9** | medium | 0.80 | `apps/designer-portal/package.json:115-121` | `browserslist` is a **production build-output** change (SWC transform target, PostCSS/autoprefixer) shipped in a lane gated only by jest and `type-check`, neither of which reads it. No `pnpm build` / `infra/deploy-portal.sh` build was run on any lane and `build/baseline-gates.md` records none. Secondary: this repo's `caniuse-lite` is six months stale (browserslist prints the update warning), so "last 2 Chrome versions" currently pins 145/144 off old data — a `pnpm install` that refreshes it silently moves the target. Scenario: the first wave that needs a portal build discovers the target change, in a wave that has nothing to do with it. |
| **L1-10** | **high** | 0.95 | `e2e/document/quiet-responsive-shell.spec.ts:271-325` | **The new landing-clearance spec has never been run.** `build/e2e-run-w0-after.log` records exactly six tests from this file (`:94 :157 :179 :199 :215 :243`) — the L3 baseline was cut from `document-lens/w0` before L1 landed, and there is no L1 evidence artefact anywhere under `build/`. So the one gate W0-L1 exists to install has never fired, passing or failing. Scenario: W1-L4 is scheduled to retarget `:223-228` in this same file and will be the first lane to discover whether the tripwire even runs — inside a wave that is also changing the thing it measures. Required: one `npx playwright test e2e/document/quiet-responsive-shell.spec.ts --project=chromium --workers=1` on the merged `w0`, recorded as 7/7. |
| **L1-11** | low | 0.70 | `e2e/document/quiet-responsive-shell.spec.ts:290-297` | The seam-height precondition is only `toBeGreaterThan(0)`. A degenerate seam — say 2px from a mis-measured pinned `<section>` — satisfies it, and the `Math.abs(box.y − seamHeight) <= 4` assertion at `:324` then passes with `box.y = 0`, i.e. with the Money region landing **under** the seam, the exact failure the test is for. A plausible floor (`> 24`, or `>= --doc-band-height`) closes it for one line. |
| **L1-12** | ok | 0.85 | `e2e/document/quiet-responsive-shell.spec.ts:271-325` | **The right element, and an honest tolerance.** `[data-index-region="money"]` (`money-region.tsx:229` and `:250`, both folded and unfolded) is the node that carries the clearance, via its own inline `SEAM_CLEARANCE = { scrollMarginTop: 'var(--doc-seam-height, 0px)' }` (`:48`) and matching `globals.css:1034`. Playwright's `boundingBox().y` is viewport-relative, so `box.y ≈ seamHeight` is the correct equality. 4px is generous against a sub-pixel actual but cannot mask a real break (a lost clearance lands the region ~56px off). Choosing `money` over `ffe` was necessary and correct: `globals.css:1037` gives `[data-index-region='ffe']` `max(var(--doc-seam-height), 4rem)`, which would make this assertion wrong there. No `waitForTimeout`; `expect.poll` throughout. |
| **L1-13** | ok | 0.90 | `e2e/document/quiet-responsive-shell.spec.ts:30, 271-286` | The fixture and selectors exist. `PROJECT_ID` is `b0000000-…-d4` as the comment claims. `[data-job-ticket]`/`data-pinned` are real (`job-ticket.tsx:351-352`); `--doc-seam-height` is published on `document.documentElement` (`job-ticket.tsx:249-255`) and reaching it through `[data-document-shell]`'s computed style works because custom properties inherit. `role="group"` named "On this paper" exists exactly once in the app (`spine-running-index.tsx:66-73`) — no strict-mode ambiguity with the ticket's map. |

---

## 2 · L2 — the seed

| id | sev | conf | file:line | finding + failure scenario |
|---|---|---|---|---|
| **L2-1** | **high** | 0.85 | `scripts/the-document-lens-seed.sql:24` | The header says **"Safe on a fresh `supabase db reset` and safe to re-run against a live DB."** and never says *local only*. That is the opposite of this repo's convention for hand-run seeds: `scripts/the-document-reaudit-walk-seed.sql:1` opens with "(local dev only)". There is no runtime guard either — compare `e2e/helpers/psql.ts:24-29`, which refuses any non-`127.0.0.1`/`localhost` URL. Scenario: someone reads line 24, pastes the file into a Strata session, and creates 700+ rows of fake procurement against a real designer. **Mitigation found, not stated:** `:127-134` looks up the local `designer_clients` fixture (`a0000000-…-004`/`…-005`) and `RETURN`s with a NOTICE when it is absent — which Strata is — so a mis-run would in practice no-op. Fix: put "LOCAL DEV ONLY" on line 1, delete the "live DB" phrase, and name the prerequisite lookup as the guard. |
| **L2-2** | medium | 0.85 | `scripts/the-document-lens-seed.sql:366` vs `:17-19` | A hard-coded absolute date in seeded copy: `'Hartland rug, walnut nightstands. With the client since Aug 13 — six days overdue.'` The header claims "Every date below is relative to `now()`/`CURRENT_DATE` so the seed stays true on any run day." `sent_at` for that decision is `NOW() - 12 days` (Aug 17 on the authoring day), so **"Aug 13" is already wrong at authoring** and drifts a full day per day thereafter. "six days overdue" is true only on the run that created the row, because the decisions block is create-once (`:355-357`) and its `due_date` never re-derives. Scenario: a walker reads the paper a week later and the copy contradicts the stamp beside it. Fix: compose both phrases with `to_char(...)` off the same `NOW()` offsets, as `:544-546` already does for the console's claim window. |
| **L2-3** | medium | 0.80 | `scripts/the-document-lens-seed.sql:66-70, 452` | Two id families collide inside the reserved prefix. FF&E ids are minted as `lpad(to_hex(2000 + n), 12, '0')` — genuine hex — while every declared constant is a decimal-looking literal that a UUID reads as hex. So lines **n = 49…52** mint exactly `…0005-000000000801 / 802 / 803 / 804`: the ids of `v_dec_overdue6`, `v_dec_com`, `v_dec_dining`, `v_dec_hardware`. Verified by enumeration. No constraint breaks (different tables), but `v_dec_com` is simultaneously the id of FF&E line n=50 *and* the value of FF&E line n=2's `blocked_by_decision_id` — anyone debugging the COM chain by id lands on the wrong row. Fix: move the FF&E block to a non-overlapping band (e.g. `to_hex(0x3000 + n)`), or make the constants hex-literal too. |
| **L2-4** | low | 0.95 | `scripts/the-document-lens-seed.sql:293-297` | The "**the final state converges identically regardless of run count**" claim is false for `po_payments.due_date`. Verified live: CM-1187's balance is `2026-09-24` after run 1 and **NULL** after run 2. Mechanism: trigger C (`00184`) only shifts the net-30 balance when `delivered_date` transitions from NULL (`receiving_inspection_side_effects`, the `v_delivered_was_null` branch), but the PO upsert at `:296-311` deliberately omits `delivered_date` from its `DO UPDATE SET`, while step 5 deletes and re-inserts the payment with `due_date NULL`. Both net-30 POs (CM-1187, FDL-0912) are affected. Nothing user-visible depends on it — the `margin_items` "vendor payment due" branch keys on `state='due'` (`00282`), which stayed `pending` across both runs and kept the count at 7/3/4 — and `seed-verify.sql` does not check it. Either drop the claim or reset `delivered_date` in the upsert. |
| **L2-5** | low | 0.80 | `build/seed/seed-notes.md`, "Validation performed" | Idempotency was proven by running the body twice **inside a single transaction**, where `now()` and `txid_current()` are frozen. That harness cannot exercise the two things that actually differ across real re-runs: the `now()`-relative offsets and the create-once decision framing (L2-2). I reproduced the same harness and got the same 14 PASS, plus L2-4, which the original run did not surface. The claim to make is "converges within one transaction", which is weaker than what the header asserts. |
| **L2-6** | low | 0.70 | `scripts/the-document-lens-seed.sql:26-29`; `supabase/config.toml [db.seed]` | Not registered in `sql_paths`, so every `supabase:reset` drops it. That matches the convention (ten sibling `scripts/*.sql` are the same), but it is load-bearing for this program and is **already** bitten: the current local DB has **zero** rows for `…d5`, because W0-L3 ran two `supabase:reset`s after L2's rolled-back validation. Any W1+ walk lane that assumes the long paper is present will find an empty spread. Worth an explicit line in the wave hand-off: re-run the seed after every reset. |
| **L2-7** | ok | 0.90 | `00390` `:1081-1109`, `:1187-1216`; `00398` `:1053-1062`, `:1166-1178`; `00399`; `00438` `:291-298`, `:371-378` | **The authority triggers are handled correctly, and the reasoning in the header is accurate.** Read each guard and then executed the script. `guard_project_completion_authority`'s INSERT branch needs `current_user = 'postgres'` + `app.proposal_activation_id` matching + an `accepted` proposal with `project_id IS NULL` and matching designer/client — the seed's insert-proposal → set-GUC → insert-project → back-fill sequence (`:194-227`) satisfies all four, and the proposal-side guard's `v_project_link_authorized` needs exactly the same set plus a project already pointing back, which is why the back-fill must come last. The DELETE branch's "non-draft proposals are immutable editions" (`:1188-1191`) is what makes create-once correct for both `…1401` and `…d6`. `guard_ffe_rpc_mutation` (`00438:373`) bypasses on `current_user IN ('postgres','service_role')`, which is why the `ON CONFLICT DO UPDATE` on lifecycle/pricing/assignment columns is legal here. The `project_phases` DELETE is authorised by 00398's `v_owner_maint`, and the INSERTs by the `app.project_phase_batch_token` the seed sets at `:645-651`. |
| **L2-8** | ok | 0.90 | `scripts/the-document-lens-seed.sql:279-296, 566-576`; `00184:141-166, 194-215, 268-300` | **The `aaa_ffe_ratchet_to_po_stage` handling and both inspection paths are exactly as documented, and empirically reproduce.** `ffe_status_rank` only advances (`rank(NEW) < rank(target)`), so the console's explicit `delivered` (6) survives PO4's `shipped` target (5); PO1's `draft` maps to `ordered`; the clean CM-1187 inspection advances the PO to `delivered` (trigger C's `outcome = 'clean'` arm) and trigger B cascades its two lines 5→6; the damaged FDL-0912 inspection stamps `delivered_date` and leaves the PO at `shipped`. Observed distribution after both runs, identically: `approved 11 · delivered 3 · installed 10 · ordered 2 · production 2 · quoted 11 · shipped 10 · specified 13`. `delivered 3` = CM-1187's two cascaded lines + the console, as claimed. |
| **L2-9** | ok | 0.95 | `00282:606-905`; `build/seed/seed-verify.sql` | **`margin_items` lands on exactly 7, split 3 line / 4 letterhead, read from the VIEW.** Confirmed live, not re-derived: `MARGIN letterhead 4 / line 3`. Traced each branch — decisions (4, all in `('pending','responded','expired')`; only `v_dec_com` gets `anchor_kind='line'` via the `blocked_by_decision_id` lateral), messages (2 threads, both `line`), invoice (1, `letterhead` because its only line is `kind='adhoc'` with no `ffe_item_id`), and pulse/time/note/field_sms/vendor-payment-due all correctly empty. The "no `state='due'` po_payment" precaution holds for a second reason the notes do not give: trigger B's flip also requires `payment_pattern IN ('fifty_fifty','thirty_seventy')` **and** a paid deposit, and both net-30 POs have neither. |
| **L2-10** | ok | 0.95 | `scripts/the-document-lens-seed.sql:136-143, 468-471`; live DB | **FK integrity holds and there are no prod ids or non-local URLs.** The local stack has exactly **21** `products`; the seed reads them dynamically (`array_agg(id ORDER BY id)`) and indexes modulo the count, so 58 of 62 lines carry a valid `product_id` and the four that do not are the two Mudroom "TBD" lines, the COM-blocked line and the console — by design. Every vendor `website` is `*.example.com`. The only user ids are the local seed fixtures `a0000000-…-004/005`; the only project/proposal ids are the `b0000000-…d5/d6` and `…0005-…` reserved bands, which `grep` finds nowhere else in `supabase/`, `scripts/`, `apps/` or `packages/`. |
| **L2-11** | low | 0.60 | `scripts/the-document-lens-seed.sql:26-29` | The script requires `current_user = 'postgres'` in three independent places (00390 provenance, 00398 `v_owner_maint` for the phase DELETE, 00438's RPC-only bypass). The header gives the right invocation (`psql -U postgres`) but never states the requirement, so a run through any other role fails at three different points with three unrelated, opaque errors. One line in the header. |

---

## 3 · L3 — the e2e baseline

| id | sev | conf | file:line | finding + failure scenario |
|---|---|---|---|---|
| **L3-1** | ok | 0.95 | all 5 specs | **No assertion was weakened to pass. This is the important one, and it holds.** Line-by-line before/after on every changed spec: (a) `margin-handoffs.spec.ts` — the `#document-next-up` "has waited N days" check was removed from a still-running test but **moved intact** into its own `test.fixme` at `:162-171`, not deleted; the stamp/need-line coverage above it keeps running. (b) `spec-book-workspace.spec.ts:118-127` — `page.getByRole("status")` → `page.getByRole("main").getByRole("status")` is a **narrowing**, not a loosening: the unscoped locator matched two nodes (the workbench's inline paragraph and a global toast) and died on Playwright strict mode; the scoped form asserts the inline message specifically, with the same `hasText`. (c) everything else is additive — the `ffeItemId` capture and explicit click, the `studio_id` lookup — or a whole-test quarantine. No timeout raised, no `toBeVisible` softened to `toBeAttached`, no `expect` dropped outright, no `.first()` band-aid. |
| **L3-2** | low | 0.70 | `e2e/document/spec-book-workspace.spec.ts:82-86` | The explicit `#spec-book-item-${ffeItemId}` click removes the last implicit coverage that the app's default selection (`data.items[0]`) agrees with the fixture's own ordering. That agreement is precisely what broke — `use-spec-books.ts:208-209` orders on `(sort_order, created_at)` with no further tiebreak and the three seeded rows tie on both — and the test now routes around it rather than pinning it. A knowing trade (the comment says so), but the ambiguity is now asserted nowhere. |
| **L3-3** | low | 0.65 | `e2e/document/action-visibility.spec.ts:249` | The surviving test keeps its old name, "390px surfaces place context, primary, and More in one edge owner", while now covering three of the four surfaces it used to (the `/desk` leg went into the fixme). The name over-claims what the test proves; a reader scanning the pass list will believe `/desk` at 390 is still covered. |
| **L3-4** | ok | 1.00 | `action-visibility.spec.ts:213-225` · `arrival-arc.spec.ts:235-248` · `margin-handoffs.spec.ts:146-161, 173-183` · `plan-room.spec.ts:108-119` | **All four quarantines carry the full comment.** Every one has the date (`2026-08-29, Smart Lens W0`), a mechanism-level reason, the literal phrase "Not a lens regression — failing the same way on main@dab057537", an "Un-fixme when:" condition that names a concrete code change, and "Owner: e2e triage". No bare `test.fixme` anywhere. |
| **L3-5** | ok | 0.95 | verified by grep | **The stated reasons hold.** "OpenRequestsStrip is dead code": `grep -rn "OpenRequestsStrip\|useOpenRequestsDeskPopulation\|StudioPulse" src/` returns hits only inside `components/document/open-requests-strip.tsx`, `components/document/studio-pulse.tsx` and `components/document/__tests__/studio-pulse.test.tsx` — nothing under `src/app/**` imports `StudioPulse`, and the single `src/app` mention is `desk/page.test.tsx:187` asserting `#studio-pulse` is **null**. The A9 claim is verbatim: `src/app/(document)/desk/page.test.tsx:156-161`, `describe('Desk — capture-lead affordance (A9)')` → `expect(useMobilePrimaryAction).not.toHaveBeenCalled()`. The RedLetterZone claim matches `page.tsx`'s mount condition. The plan-room `studio_id` diagnosis matches 00511's exactly-one-candidate rule. |
| **L3-6** | low | 0.80 | `e2e/helpers/supabase-admin.ts:100` | `select id from auth.users where email = '…'` has no `LIMIT 1` and no `ORDER BY`. `psqlScalar` runs `-t -A` and `.trim()`s the whole output, so two matching rows (Supabase permits duplicate emails across `is_sso_user`) return a **two-line string** that is then handed on as a UUID and fails somewhere downstream with an unhelpful error. The replaced `data.users.find(...)` implicitly took the first. One-word fix. |
| **L3-7** | low | 0.60 | `e2e/helpers/supabase-admin.ts:99-100`; `e2e/helpers/psql.ts:41-47` | **SQL-injection surface is adequately closed for the inputs it has**, and it is worth saying why rather than assuming: `run()` uses `execFileSync` with an argv array, so there is no shell and no shell-metacharacter path; `'` is doubled; Postgres ships `standard_conforming_strings = on`, so a backslash in the literal is inert; and every current caller passes a hard-coded fixture address. Residual, all low: the escape is single-quote-only and would not survive `standard_conforming_strings = off`, nothing constrains the argument's shape (no email regex) if a future caller passes a DB- or env-derived value, and `psql -c` switches parsing mode on a leading backslash — none reachable today. |
| **L3-8** | low | 0.75 | `e2e/helpers/supabase-admin.ts:2` | Importing `./psql` pulls `psql.ts`'s **module-level** `throw` (`:24-29`, the non-local-URL refusal) into every spec that imports `supabase-admin` — which is most of `e2e/`. That is a safety improvement, and worth keeping. Two side effects to know about: a `psql` binary is now a hard runtime dependency of a helper that previously needed only HTTP (the fallbacks at `psql.ts:33-39` cover the usual macOS keg-only cases but not a bare CI image), and a non-local `SUPABASE_DB_URL` now crashes at import across the whole tree rather than failing one spec. |
| **L3-9** | ok | 0.90 | `playwright.config.ts:42`; `build/e2e-baseline.md` | **The `--workers=1` claim is literally true.** `playwright.config.ts:42` is `workers: process.env.CI ? 1 : undefined`, so CI does run this suite single-worker and `--workers=1` is the right bar for "0 failed". Two caveats worth recording rather than treating as defects: the baseline was taken locally **without** `CI=true`, so it did not exercise `retries: 2` or `forbidOnly` (`:38-40`); and "63 passed / 6 skipped" is a statement about the CI worker count only — the default-parallel local run still carries the contention failures §4 of the baseline enumerates, and those remain unfixed by design. |

---

## 4 · Cross-lane and ledger

| id | sev | conf | file:line | finding |
|---|---|---|---|---|
| **X-1** | medium | 0.95 | `build/test-impact.md`, "Jest arithmetic" | Declared: "**W0: +1 test** (mount-order test in `page.test.tsx`)". Measured in one harness, base and branch: `dab057537` = 458 suites / **5170** tests; `document-lens/w0-l1` = 458 suites / **5173**. **+3.** The two undeclared tests are `contrast.test.ts:360` ("resolves the rail file glob to at least today's five files") and `:384` ("scans the paper-ground files (once they exist) against --doc-paper"). The ledger's merge rule is written about **suite** count, which did not move, so this is not literally a merge-stop — but the wave's own arithmetic is wrong, and the whole point of the ledger is that the next wave can trust it. Reconcile before merge. |
| **X-2** | ok | 1.00 | `git diff --stat dab057537 document-lens/w0` | The union is exactly the sum of the three lanes: every per-file line count in the merged diff matches its lane's diff, and the two merge commits (`93bb409da`, `1f55eb5b0`) introduce no resolution edits. `w0-l1` touches no source file — the `data-section-stage-line` attribute OD-9 asked Wave 0 to add already ships on `main` at `section-stage-line.tsx:44` (and on all four `SectionStageLineMount` branches), so "the one product line Wave 0 touches" turned out to be zero. |
| **X-3** | low | 0.85 | `artifacts/document-lens-build-2026-08-29/build/` | The wave has evidence for main (`baseline-gates.md`), for L3 (`e2e-baseline.md`) and for L2 (`seed/seed-notes.md`), but **none for L1**, and `build/ledger/` and `build/w0-walk/` are empty. L1's jest, type-check and e2e results are unrecorded anywhere; I re-measured them for this review (gate table above), but the wave should carry its own artefact — and L1-10 is the direct consequence of that gap. |

---

## Fixes required before ship

1. **L1-10** — run `npx playwright test e2e/document/quiet-responsive-shell.spec.ts --project=chromium --workers=1` on the merged `document-lens/w0` and record **7 passed**. W0-L1's entire deliverable is a tripwire that has never fired. Everything else in this list is cheap; this one is the ship gate.
2. **L2-1** — put "**LOCAL DEV ONLY**" on line 1 of `scripts/the-document-lens-seed.sql` (the sibling convention), delete "safe to re-run against a live DB" from `:24`, and name the `designer_clients` early-return at `:127-134` as the guard it actually is.
3. **X-1** — reconcile `build/test-impact.md`'s "W0: +1 test" to **+3**, naming the two `contrast.test.ts` additions. 458 suites / 5170 → 5173 is the measured pair.
4. **L1-2** — re-shape the rail-glob floor so W1's and W2's own OD-16 deletions do not turn a Wave-0 tripwire red on a correct change. Assert non-empty plus the ladder's presence, not `>= 5`.
5. **L1-9** — run one `pnpm --filter @patina/designer-portal build` so the `browserslist` change is exercised once, in the wave that introduced it, rather than by whichever later wave needs a portal build.

## Should fix, not gating

- **L1-1** — exclude `*.test.tsx` from `resolveRailFiles()`; it currently scans `doc-spine.test.tsx` as a rail file.
- **L1-3** — scope offender form 2 to the text property (`(?:^|\s|")text-\[color:var\(…\)\]`), so a clay fill, rule or focus ring written in the type-hint spelling is not a false offender.
- **L1-4** — either resolve `lens-band.tsx` by glob rather than a guessed path, or make the vacuous branch loud (`expect(PAPER_FILES).toHaveLength(0)` with a comment, so the day it exists elsewhere the test says so).
- **L1-6** — drop the redundant DOM-order assertion; query the render container rather than `document`.
- **L1-11** — raise the seam-height precondition from `> 0` to a plausible floor so the clearance assertion cannot go vacuous.
- **L2-2** — compose "since &lt;date&gt;" and "N days overdue" from the same `NOW()` offsets, as `:544-546` already does.
- **L2-3** — move the FF&E id band off the constants' band (`to_hex(0x3000 + n)`), or make the constants hex-literal.
- **L2-4 / L2-5** — either reset `delivered_date` in the PO upsert or drop the "converges identically regardless of run count" claim; state that the idempotency proof was single-transaction.
- **L2-6** — add "re-run the seed after every `supabase:reset`" to the wave hand-off; the current local DB has already lost it.
- **L2-11** — state the `psql -U postgres` requirement in the header, not just in the example command.
- **L3-2** — add a small assertion that the app's default selection matches the fixture's ordering, or file the `use-spec-books.ts:208-209` tiebreak as its own issue.
- **L3-3** — rename the surviving `action-visibility` test to what it now covers.
- **L3-6** — add `limit 1` (and an `order by created_at`) to the `auth.users` lookup.
- **L3-8** — note the new `psql`-binary dependency in the e2e README / CI notes.
- **X-3** — write the missing `build/w0-l1-*.md` evidence artefact.
