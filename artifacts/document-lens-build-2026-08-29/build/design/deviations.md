# Deviations from R127 — running log

Row schema: `D-B<n> · title · date · ruled by ARCHITECT|DESIGN LEAD · measurement · what ships`

- `D-B1 · L-2 is built as the mockup built it — the ladder's window moves by `transform: translateY` in a rAF, not by re-laying the track · 2026-08-29 · ruled by DESIGN LEAD (`reconciliation.md:20`, adopted by the ARCHITECT in §3) · measurement: the mockup drives the window by transform; a track re-layout per frame would be a layout read + two style writes per scroll frame (W2 correctness review, the resize-path finding). · what ships: `spine/lens-ladder.tsx` writes `data-lens-window="top:height"` and `transform: translateY(...)` on the gutter `<div>` imperatively in rAF (`:173-181`); React never lays the window out. (Row written at the W6 audit — the id was assigned on 2026-08-29 and the row was never filed.)`

- `D-B2 · the L-9 stop announcement is a visually-hidden span INSIDE the band's line-2 live region, not a second live region · 2026-08-29 · ruled by ARCHITECT (OD-7; renumbered from D-B1 by A-09) · measurement: two `aria-live` regions announcing on the same scroll would double-speak every stop; the proposal names one live sentence. · what ships: `lens-band.tsx:311` `<span class="sr-only" data-lens-announce>` inside `<p data-lens-line="2" aria-live="polite" aria-atomic="true">`, text `Now at <label> · <count line>`, deduped by `LENS_ANNOUNCE_DEDUPE_MS`; C-11 (W3-R5 §7) clears it when `printed` changes. (Row written at the W6 audit — the id was assigned on 2026-08-29 and the row was never filed.)`

- `D-B3 · landing-clearance spec targets --doc-seam-height until W3 retargets to --doc-landing-clear · 2026-08-29 · ruled by ARCHITECT (pre-agreed in OD-9) · measurement: `grep -n "doc-landing-clear" apps/designer-portal/src/app/globals.css` → 0 matches anywhere in the codebase; the token is minted in Wave 3 (OD-4/D-1), so a Wave-0 test cannot assert against it. `quiet-responsive-shell.spec.ts:271-325` asserts the Money region lands within 4px of `--doc-seam-height`, today's mechanic, published on `document.documentElement` by `job-ticket.tsx:249-255`. Verified green 2026-08-29 (7/7). · what ships: the seam-height form, with the substitution disclosed in the test's own comment; W1-L4 retargets `:223-228` and W3 retargets this assertion to `--doc-landing-clear` when the token exists.

- `D-B4 · contrast rail floor ≥3 (OD-16 deletions) · 2026-08-29 · ruled by ARCHITECT · measurement: `resolveRailFiles()` enumerates 5 source files today (doc-spine.tsx, spine-running-index.tsx, spine-shelved-blocks.tsx, spine-timer.tsx, margin-rail.tsx) once `*.test.tsx` is excluded — `doc-spine.test.tsx` was being scanned as a rail file, measured 6 before / 5 after; a `spine/_probe.tsx` probe raised it to 6, and a sibling `spine/_probe.test.tsx` was correctly not counted. OD-16 deletes spine-timer.tsx in W1 and spine-running-index.tsx + spine-shelved-blocks.tsx in W2, leaving doc-spine.tsx + spine/lens-ladder.tsx + margin-rail.tsx = 3. · what ships: the floor is `>= 3`, not `>= 5`, with the three OD-16 deletions named in the test's comment, so a correct wave deletion cannot turn a Wave-0 tripwire red.

- `D-B5 · phase estimates editor unreachable after PHASES deletion (letterhead-vitals) · 2026-08-29 · ruled by ORCHESTRATOR · measurement: W1-L2 deleted the letterhead vitals' `PHASES` fold and the per-phase hours table with it (`letterhead-vitals.test.tsx` loses "carries no Phases toggle and no per-phase hours table" as its guard); the `estimated_hours` editor had no second home, so after this wave nothing on any spread can edit it. · what ships: flagged, NOT a lens deviation — the lens rules what the letterhead prints, not where an editor lives. Owed: re-home the `estimated_hours` editor in the schedule region; tracked here so the wave that re-homes it can close this row.

- `D-B6 · duplicate running-index observer until W2 retires spine-shelved-blocks · 2026-08-29 · ruled by ARCHITECT · measurement: `useDocumentRunningIndex` is now called twice over the same `[data-index-region]` roots — once at page level (`page.tsx`, feeding `currentStop` to `MarginRail` and `readingIndex` to `useMobileActiveDoc`, above every early return per OD-15.4) and once inside `spine-shelved-blocks.tsx:81`, which still owns the ≥1440 running-index block. Two IntersectionObservers over the same elements; each keeps its own `activeKey` state and they cannot disagree (same roots, same reading band, same resolve). · what ships: the duplication, deliberately, for one wave. OD-16 deletes `spine-shelved-blocks.tsx` in W2 and the second observer goes with it; no product behaviour depends on the block's copy surviving.

- `D-B7 · an unset vital prints one scored-ink act, not nothing (letterhead-vitals) · 2026-08-29 · ruled by ORCHESTRATOR · measurement: W1 review CORRECTNESS C-03/M-12. D-6 ruled what the vitals row PRINTS; W1-L2 implemented it by gating the EDITORS, so `VitalDate` rendered only when the date was already truthy and the band only when a bound existed. `needsSetup` (page.tsx:1584-1593) raises only `schedule_unconfigured`, and `start_date` has no second editor anywhere — a freshly created project document had no control on the paper to record a start date, a target date or a budget band, and pressing `×` on a recorded date was a one-way door that removed the field and dropped focus to `<body>`. · what ships: the empty PRINTING stays empty — no `Start —`, no `Band $ – $`, nothing in the live-figure register — but an unset vital prints ONE scored-ink act that opens the very editor the recorded field uses: `Set dates` when start and target are both unset (one door for both), `Set start` / `Set target` when only that one is, `Set a budget band` when no bound is recorded. The act is never disabled while a save is in flight (a disabled button cannot hold focus), and `×` hands focus to the act that replaces the field. D-6's "0px when a document has none of the three" is therefore amended: on a project document the vitals row always prints at least the two acts. `letterhead-vitals.test.tsx` asserts unset → act + editor, set → value + no act, clear → focus on the act.

- `D-B6 CLOSED · the duplicate running-index observer is retired · 2026-08-29 · ruled by ARCHITECT (pre-agreed above) · measurement: `spine-shelved-blocks.tsx` and `spine-running-index.tsx` are deleted (W2-L1, OD-16); `grep -rn "useDocumentRunningIndex" apps/designer-portal/src` returns the hook's own module, its test, and ONE call site — `page.tsx`. The rail is handed `activeKey` and `jump` as props (`DocSpine.activeKey` / `DocSpine.onJumpRegion` → `LensLadder`), so one IntersectionObserver stands on the document. · what ships: the single page-level call; W2-L1's spine-internal call was removed at integration.

- `D-B8 · the third door prints `Boards`, not `Mood boards` · 2026-08-29 · ruled by ORCHESTRATOR (F62 — one name for one thing) · measurement: `shelves.ts:66-70` already titles the `moodboards` shelf `Boards` (the row, the leaf, the page and ⌘K all read it), while W2-L1's `PROJECT_DOORS` and W2-L4's sections sheet still printed `Mood boards` — two names for one door on one document. The key stays `moodboards`: it is an address, not a word. · what ships: `Boards` in the ladder (`lens-ladder-derivation.ts` `PROJECT_DOORS`) and in the mobile sections sheet (`mobile-sheets.tsx`), with `lens-ladder.test.tsx`, `lens-ladder-derivation.test.ts` and `mobile-bar.test.tsx` updated in the same commit. `registry-match-surfaces.test.ts`'s "not `Mood boards`" guard and `mobile-bar.test.tsx:180`'s are unchanged and still green.

- `D-B9 · two ladder inputs the page cannot source, and what the rail prints instead · 2026-08-29 · ruled by ORCHESTRATOR · measurement: (1) **care** — `LadderCareFacts` wants `{closed, total}`; `CareBand` composes `done` from its own local checklist state plus `paymentReady` derived from EIGHT queries (`care-band.tsx:109-201`) and reports only the `ready` boolean up through `onCloseoutReady`. Nothing on `page.tsx` can state `N of M` without repeating those eight reads, so the integration passes `{settled:true, closed:0, total:0}` and the stop takes `empty('Nothing yet')`. Walked at 1440/s0 on the long paper: the rail prints `Closing the book` / `NOTHING YET`. (2) **damagedOn** — the Pieces value's optional carrier window; `TicketLine` (`ticket-derivation.ts`) carries a stamp and a room id and no date, and the FF&E rows it is built from live inside `JobTicketMount`. Passed `null`; the rail prints `62 LINES · 1 DAMAGED` (no date) at 1440 and `62 LINES · 5 ROOMS · 1 DAMAGED` at 1280, which is exactly OD-14's narrow form. · what ships: both fallbacks, per SP-05. Owed: widen `onCloseoutReady` to report `{ready, closed, total}` (one callback, already wired) so the care stop can print `0 OF 6 CLOSED OUT` as reconciliation §7's walker-sees line expects; the damage date needs the carrier window on `TicketLine` or a second reader, and is deferred.

- `D-B10 · the rail head reserves 126 / 117, not 116 / 100 · 2026-08-29 · ruled by ORCHESTRATOR (on the W1 measurement) · measurement: reconciliation §10 derives 116px at 1180–1439 and 100px at ≥1440 from `min-h-6 = 24px` for the arc's cells. At this portal's 18px root `min-h-6` computes to **27px** and `min-h-11` to 49.5, so the arc costs 59px, not 48, and W1's e2e measured the head at **126 / 117**. W1 shipped the ratified `min-h-[116px]` and flagged the gap (`build/e2e-baseline.md`, "Measurement note"); W2-L1 changed the class to `min-h-[126px] min-[1440px]:min-h-[117px]` and rewrote `doc-spine.test.tsx:83` to match, with no deviation row — F-02. · what ships: **126 / 117**, the measured numbers, ruled by the ORCHESTRATOR on the W1 measurement rather than left silently adopted. `technical-design.md`'s "Fixed numbers, declared once" line still says 100; it is the document that is now wrong, and it is owed a correction.

- `D-B11 · rungs and doors at 44px over the proposal's 28 / 32 — AMENDED 2026-08-29 to 27px rungs · 2026-08-29 · ruled by ORCHESTRATOR, amended on the W2 walk · measurement: proposal §4 fixes the room sub-rung at 28px and the four doors at 32px; W2-L1 shipped both at `min-h-11` (49.5px at an 18px root), a touch-target choice, unlogged — F-06. The walk then measured the cost: at 1440/s2 with Pieces open the ladder track had 259px for 331.75px of rows, and `FILED WITH THIS JOB` and the doors printed straight over the tail rungs (`w2-walk/w2-1440-spine-clip-s2.png`, `w2-1440-room-in-hand-spine-clip.png`). · what ships: the **rungs at `min-h-[27px]`** — the same 2.5.8 pointer-floor cell the design lead set for the arc — and the **doors at `min-h-11`**, which never compete with the track for height. The track additionally states what it needs (`--track-floor-full` / `--track-floor-narrow`), spends its remainder on the rungs, collapses what it cannot hold to one `+N` line, and scrolls itself as a backstop rather than spilling. Design lead to see the 27/44 pair in the walk.

- `D-B12 · lanes L2 and L3 based on pre-W1 integration · 2026-08-29 · ruled by ORCHESTRATOR · measurement: `w2-l2` (`b303b3675`) and `w2-l3` (`cebec4d85`) branch from `690337f1a` (the wave-0-fixes merge), omitting all eleven Wave-1 commits at creation; `w2-l1`/`w2-l4` branch from `de82db0e5` and omit `5313b6f95` ("W1 review fixes") — F-01. The FIDELITY reviewer verified `document-lens/w2`'s tree carries `5313b6f95`'s content forward (D-B7's `Set dates` act, the drawer's presence line, the clamp's `aria-expanded`/`aria-controls`, `--doc-shell-bottom-inset`'s safe-area form), and the four lanes' own commits touch disjoint files from it. · what ships: **no defect** — the lanes were based this way by ORCHESTRATOR decision on disjoint file sets, and the three-way merge was clean because the sets do not overlap, not by luck. Recorded so a diff taken against `7c8b33e39` — which renders W1's fixes as reverts — does not mislead the next reader.

- `D-B9 CLOSED (care) · the care stop prints `N OF M CLOSED OUT` · 2026-08-29 · ruled by ORCHESTRATOR (the owed half of D-B9) · measurement: `onCloseoutReady` reported only the `ready` boolean, so the page could not state `N of M` without repeating the band's eight closeout reads and the stop took `empty('Nothing yet')`. · what ships: `onCloseoutReady` widened to `CloseoutState = { ready, closed, total }` (exported from `care-band.tsx`) and consumed on the page through a value-comparing `acceptCloseout`; the rail prints `0 OF 6 CLOSED OUT`. The **damagedOn** half is closed separately — see D-B13.

- `D-B13 · the Pieces value dates the damage from the claim's `created_at` · 2026-08-29 · ruled by ORCHESTRATOR (DESIGN LEAD W2 review, item 11) · measurement: D-B9 deferred the damage date because `TicketLine` carries a stamp and a room and no date. It costs no new query: `useProjectFFEItems` already embeds `item_claims:damage_claims!ffe_item_id(id, state, created_at)` (`use-project-v2.ts:192`), the same embed `deriveLineStamp` reads to stamp a line `damaged`. `JobTicketMount` derives the oldest OPEN claim's `created_at` and reports it up (`onDamagedOn`); `OPEN_DAMAGE_CLAIM_STATES` is exported from `stamp-derivation.ts` so the date and the stamp read one list. · what ships: `62 LINES · 1 DAMAGED AUG 29` at 1440, and OD-14's narrow form `62 LINES · 5 ROOMS · 1 DAMAGED` at 1280 (the date is the first thing dropped to stay inside the 30-char cap). ⚠ The date is when the CLAIM was raised; no carrier-window column exists. If the design lead means a carrier's promised window, that is a schema question, not a wiring one.

- `D-B14 · the rail reserves the studio drawer at its foot · 2026-08-29 · ruled by ORCHESTRATOR (DESIGN LEAD W2 review, item 9) · measurement: the studio drawer is a fixed 60px bar from 1180px (`studio-drawer.tsx:306`). The rail's `min-[1180px]:pb-6` (27px) left the fourth door at 823.5–873.0 against a drawer top of 840 at 1440×900 — 33px of `Call sheet` behind the drawer. · what ships: `min-[1180px]:pb-[var(--doc-shell-floating-bottom)]` (the existing token: `--doc-shell-bottom-inset` 60px + 1.5rem = 87px). `Call sheet` measures 763.5–813.0 against 840 at 1440×900 and 663.5–713.0 against 740 at 1280×800 — fully above the drawer at both tiers. RF-05's `pb-24 → 24px` line is therefore superseded for the desktop rail. ⚠ COST, for the design lead: the reserve takes 60px out of the ladder's track, and at exactly 1440×900 the six stops' measured rows (282px) fill the 324px track — one 27px slot of remainder, which the `+N` line takes, so **no room rung prints at 1440×900** (`+5 more` instead). At 1440×1100 all five print. Printing rungs at 1440×900 needs a ruling on the fixed furniture — the four doors at `min-h-11` are 198px of it.

- `D-B15 · the lens never says `quiet`: `getDensity`/`useLensDensityStore` return `'full' | null`, and initial density is `quiet` for every unpromoted stop · 2026-08-29 · ruled by ARCHITECT (W4-L1 item 1, AMEND) · measurement: `use-lens-density.ts:densityFor` returns `promotedKeys.has(region) ? 'full' : null`; the W3-L3 fold hook computes `density = positionDensity ?? (latchedDefault === true ? 'quiet' : 'full')` for a stop with no explicit choice — so a stop whose derived default was *open* (Pieces, Money on most seeds) renders **full at first paint even 2,000px below the frame**, which is the render cost L-4/F53 exists to remove, and §5 *at rest* says "everything further down is `quiet` at its own reserved height". SC1 is not at risk (a quiet root prints the same `[data-region-head]`), the render budget is. · what ships: (a) the store's narrowing is ACCEPTED — the lens speaks `full` or is silent, one direction by construction; `LensDensityApi.getDensity(region): RegionDensity | null` in §3. (b) AMEND `use-region-fold.ts` (W4 integration lane, one line): for a stop key with `explicit === null` and `!forceOpen`, `density = positionDensity ?? 'quiet'` — `latchedDefault` no longer feeds density at all (it was only ever a fold answer, and stop folds are explicit-only under OD-10). (c) AMEND `use-lens-density.ts` (W4-L1 follow-up): `discover()`'s first pass runs from a `useLayoutEffect`, not `useEffect`, and promotes synchronously every root whose `getBoundingClientRect().top <= window.innerHeight + LENS_LOOKAHEAD_PX` — so the first paint after hydration already shows the in-frame regions full and no quiet→full flash is visible on the first screen; the SSR markup is quiet, hydration corrects it before paint. `technical-design.md` §3 and OD-10 updated to match. **Amended by D-B46 (2026-08-30):** the layout-effect first pass no longer promotes against an unresolved paper — it runs once the paper has resolved (no query fetching + `scrollHeight` stable for 3 rAFs, or the 3000ms fallback), and then exactly as written here.

- `D-B16 · passing never promotes; discovery does — the invariant W4-L4 asserts · 2026-08-29 · ruled by ARCHITECT (W4-L1 item 2, ACCEPT with the discovery rule) · measurement: `markPassed()` writes `data-passed` from `rect.bottom < 0` and never calls `promote()` (H5: a region above the frame growing from its reserve is the shift the design forbids); but a root that is *discovered* already above or inside the frame — a deep landing via `page.tsx:1166-1174`'s resume, scroll restoration, or a region whose query settles after the reader has scrolled past its slot — is never intersecting under a bottom-only `rootMargin`, so it would sit quiet-with-`data-passed` forever and a scroll-up would meet a count line instead of a body. · what ships: in `discover()` (and the D-B15 initial pass) a newly found root is promoted at once when `rect.top <= innerHeight + LENS_LOOKAHEAD_PX` — its reserve and its body arrive in the same commit, so no *already-read* pixel moves; after discovery, passing never promotes. **The invariant `lens-density.spec.ts` asserts at 0 / 400 / 1200 and on a deep-landed load:** (i) no `[data-density="quiet"]` root has `rect.top <= innerHeight + 240`; (ii) every `[data-passed]` root is `full`; (iii) `full` count ≥ 1 whenever any root is in the frame. The earlier form `full === intersecting ∪ passed` is retired — it was only true on a scroll from 0. **Amended by D-B46 (2026-08-30):** "a newly found root is promoted at once when `rect.top <= innerHeight + LENS_LOOKAHEAD_PX`" holds only once the paper has resolved; before resolution discovery registers the root and promotes nothing. Invariants (i)–(iii) are unchanged and are asserted at settle on a cold load too. **Instrument ratified (W4 fix-3, `lens-density.spec.ts:164`, 2026-08-30):** the round-trip invariant is restated as two clauses — a root **full at the origin must not move** (`offsetTop` equal after scrolling down past three stops and back), and a root **quiet at the origin may only be pushed down** (`offsetTop` ≥ its origin) — because H5 is a claim about pixels at or above the frame: what she has read must not move, and a quiet root below can only be displaced downward by bodies opening above it, never upward. The forward-walk case (`scrollHeight` never shrinks; no root above the frame top moves across a 0→2000 walk) asserts the same law in the direction the reader travels. Both take D-B28's settled + quiet origin.

- `D-B17 · `enabled: false` keeps discovery alive · 2026-08-29 · ruled by ARCHITECT (W4-L1 item 3, ACCEPT with a constraint) · measurement: with `enabled=false` the hook installs no IntersectionObserver, no scroll listener and no timer; the `MutationObserver` stays so a late-mounting region is written `full` (`use-lens-density.test.tsx` "with the lens off, prints every region full and installs no observer" passes). `data-passed` is not written and `__lensSettled` resolves immediately. · what ships: as shipped. Constraint: **no product caller passes `enabled: false`** — OD-15 attaches the hook unconditionally in `page.tsx`; the option is a test seam and a kill switch for an incident, and OD-4's `content-visibility` fallback is the `@supports` block, not this flag. A grep gate in W4 integration, scoped to the hook's call sites (the earlier form matched unrelated `enabled: false` options, e.g. `page.test.tsx`'s `useDeskEngagements`): `git grep -nE 'useLensDensity\(' -- apps/designer-portal/src` lists every site; each is either the definition in `hooks/use-lens-density.ts` or a call that does not pass `enabled: false` — mechanically, `git grep -nE 'useLensDensity\([^)]*enabled:\s*false' -- apps/designer-portal/src` → 0, and a multi-line call form is read by eye at the same hits (amended W4-L4).

- `D-B18 · `forceFullThrough` promotes from the top of the paper, flushes synchronously, and is called before the scroll · 2026-08-29 · ruled by ARCHITECT (W4-L1 item 4, AMEND) · measurement: `forceRef.current` walks `ordered` from index 0 through the target inclusive and commits in one write, ungated by settle (test "forces every region through the press target to full, in one commit"); under D-B15 everything above the frame is already full, so from-the-top equals the design's "between the offset and the target". `promote()` → `notify()` schedules React updates for the subscribing bodies; `scrollToRegion` (`use-document-running-index.ts:202-222`) waits two `requestAnimationFrame`s, which in practice lets those bodies mount, but nothing guarantees the heights exist when the target y is read. · what ships: AMEND `use-lens-density.ts` (W4-L1 follow-up) — `forceFullThrough` wraps its `notify` loop in `flushSync` from `react-dom` (press path only; the rAF path never flushes) so every promoted body has rendered and its height exists before the handler returns. **Caller and order (W4 integration lane, `page.tsx` region-jump handler at `:1125-1126`):** `requestRegionUnfold(key)` (sets the 700ms lock on the index) → `lens.forceFullThrough(key)` → `scrollToRegion(key, projectId)`. The ladder's `onJump`, the room sub-rung, the four doors' region-shaped targets and the band's line-2 act all route through that one handler. **W4-int addendum (2026-08-29):** the sections sheet (`mobile/mobile-sheets.tsx:512-516`) called `requestRegionUnfold(key)` then `scrollToRegion(key, projectId)` directly, skipping `forceFullThrough` — a press from the sheet landed on a still-quiet root. Routed through the page's press handler (the one caller of the three-step order) in W4-L1's fix; the gate grows one line: `git grep -n 'scrollToRegion(' -- apps/designer-portal/src` → every hit is the page handler or the hook's definition, never a sheet, rail or band. **W4 fix-3 addendum (2026-08-30, ARCHITECT) — the Margin sheet's line row.** RATIFIED as a contract: `MobileActiveDoc.onPromoteThrough?: (key) => void` (`mobile-shell.tsx:74`), published by `page.tsx:1793` as `lens.forceFullThrough` (identity-stable: the API object is a `useMemo` over refs, `use-lens-density.ts:714-720`, so `stateSig` does not churn), and called by the sheet's line-anchored row before `scrollIntoView('#ffe-selection-<id>')` (`mobile-sheets.tsx:750-760`) — with `ffe` honestly quiet at s0 the jump had nowhere to land. **One gap, closed here:** the row calls `onPromoteThrough('ffe')` but not `requestRegionUnfold('ffe')`, and under C-8 a stop's `folded` is explicit-only — an FF&E the reader closed (`CLOSED BY YOU`) keeps its body unmounted whatever the density writes, so the id still does not exist and the jump still goes nowhere. This row's order is the rule for every press: **`requestRegionUnfold(key) → forceFullThrough(key) → land`**. The sheet routes the line jump through the page's handler like the sections sheet does: `MobileActiveDoc` gains `onJumpToLine?: (lineId: string) => void` (page-side: `requestRegionUnfold('ffe'); lens.forceFullThrough('ffe'); scrollIntoView` on the line, `block: 'start'`, reduce-aware), replacing the sheet-side `onPromoteThrough` + `getElementById` pair; `onPromoteThrough` stays published for any other consumer. Gate grep grows: `git grep -n 'scrollIntoView(' -- apps/designer-portal/src/components/document/mobile` → 0 (the page owns the landing). Falsifiers: jest `mobile-sheets.test.tsx` — pressing a `BESIDE PIECES` row calls `onJumpToLine(anchorId)` and then `openMarginItem`; `page.test.tsx` — the handler unfolds, promotes and lands in that order; e2e 390 cold load on `…d5` — press the first `BESIDE PIECES` row: `#ffe-selection-<id>` exists, is at the top of the frame within `--doc-landing-clear` after settle, `ffe` is `full`, and the same press with FF&E explicitly folded first (`CLOSED BY YOU` via the fold) lands identically.

- `D-B19 · the editing freeze: `freeze(boolean)` on the density API, `data-lens-state` owned by one page-level hook · 2026-08-29 · ruled by ARCHITECT (W4-L1 item 5, AMEND) · measurement: the shipped `LensDensityApi` has no freeze and `git grep -n 'data-lens-state' -- apps/designer-portal/src` in the w4-l1 worktree returns 0 — the shell attribute the design's §2/§5 rely on has no owner in any merged lane, so "editing" exists nowhere today. · what ships: (a) `LensDensityApi.freeze(frozen: boolean): void` (W4-L1 follow-up): while frozen, `commitPending()` is skipped (crossings keep buffering), `markPassed()` still runs, `settle()` still writes `data-lens-settled` and releases `__lensSettled` waiters, and `forceFullThrough` still commits (a press is explicit, never deferred); on `freeze(false)` one `commitPending()` runs at the next settle. (b) `hooks/use-lens-state.ts` (W4 integration lane) is the **sole writer** of `[data-document-shell][data-lens-state]`: `open` from the band's sentinel via an `onPinChange` prop on `LensBand` (the band writes only its own `data-lens-open`), `editing` from delegated `focusin`/`focusout` on `[data-document-paper]` matching `input, textarea, select, [contenteditable=''], [contenteditable='true']` (focusout with no editable `relatedTarget` ends it), `mobile` from `matchMedia('(max-width: 1179px)')`; it calls `lens.freeze(editing)`. Priority: `editing` > `mobile` > `reading`/`rest`. C-5 amended: `LensBand` gains `onPinChange?: (pinned: boolean) => void`.

- `D-B21 · the reduce block kills transitions only; a yield is a state at every motion register · 2026-08-29 · ruled by ARCHITECT (W3-L2 item 1, AMEND of technical-design §5) · measurement: `doc-spine.tsx:235-237` implements L-6 as `data-letterhead-in-frame` + `opacity-0 | opacity-100` with `transition-opacity duration-200 motion-reduce:transition-none`; `lens-ladder.tsx:387` implements L-3 as `data-region-head-in-frame` on the segment button. The design's §5 sentence "one new reduce block … `transition: none; opacity: 1`" would print the yielded phrase under `reduce` and hide it otherwise — breaking falsifiable sentence (d). · what ships: **the reduce block for W4-L4, sited immediately after `globals.css:283`:** `@media (prefers-reduced-motion: reduce) { [data-lens-line="2"] > span, [data-letterhead-in-frame], [data-region-head-in-frame], [data-region-head-in-frame] > * { transition: none; animation: none; } }` — transitions and animations only, never `opacity`, `visibility` or `display`. The Tailwind `motion-reduce:transition-none` already on the same elements is the same rule stated locally and stays; the block is the design's declared site and covers L-1 (line 2's crossfade spans), L-3 and L-6. **Instrument for sentence (d):** the visible-word set — text nodes whose nearest painted ancestor has computed `opacity > 0`, `visibility: visible`, `display ≠ none` — compared between `reducedMotion: 'reduce'` and `'no-preference'` at the same offset and state. Under both registers the yielded phrase is `opacity: 0` and excluded, so the sets are identical; opacity itself is never the assertion.

- `D-B22 · the guide's telemetry is re-homed on the lens line as three `document_lens_*` events; `guideShown`/`guideSelected` retire · 2026-08-29 · ruled by ARCHITECT (W3-L2 item 2, re-home in the W3 fix lane) · measurement: `DocumentGuide` no longer mounts; `documentEvents.guideShown` (`document_guide_shown`) and `guideSelected` (`document_guide_selected`) fired only from `document-guide.tsx:64/:72` and now fire nowhere; `deriveGuideModel` (C-6) carries no telemetry by design. · what ships: in `src/lib/analytics/document-events.ts` (the in-file convention: Document-internal events take the `document_` prefix; `track()` no-ops without PostHog) — `lensLineShown(props)` → `document_lens_line_shown`, `lensLineActed(props)` → `document_lens_line_acted`, `lensStandingSheetOpened(props)` → `document_lens_standing_opened`, payload `{ stage: SectionKey; state: 'standing' | 'guide' | 'none'; action_key: string | null; standing_count: number; tier: 'full' | 'narrow' | 'mobile' }` (`lensStandingSheetOpened` omits `action_key`). **Fire site is `page.tsx`, never the component:** the page owns `LensBandModel`; `shown` fires from a `useEffect` keyed on `[docId, model.line2.kind, model.line2.act?.key ?? null, model.standingCount]` (the once-per-distinct-model shape `guideShown` used); `acted` and `standingSheetOpened` fire inside the page's `onAct` / `onOpenStanding` callbacks that `LensBand` receives as props (C-5 gains `onActed?: () => void` and `onStandingOpened?: () => void`, or the page wraps the act closures — the lane's choice; no `posthog.capture` in `lens-band.tsx`). `guideShown`/`guideSelected` are **deleted** with their event names; `I152` notes that dashboards reading `document_guide_shown`/`_selected` go flat at the deploy and should read the three lens events instead. `document-guide.test.tsx`'s telemetry assertions move to `page.test.tsx` against the lens events.

- `D-B23 · `router.push` for `href` guide destinations, `DocSpine.letterheadInFrame`, and the two in-frame attributes are React-written from `useLensFrame` · 2026-08-29 · ruled by ARCHITECT (W3-L2 ledger items, ACCEPT) · measurement: `page.tsx:1453-1470` `activateDestination` gained `if (destination.kind === 'href') router.push(destination.href)` — the guide's deep links were `<a href>`s the zone rendered itself and would have become inert as a press; `doc-spine.tsx:54` gained `letterheadInFrame?: boolean` (default false), fed by `useLensFrame()` at `page.tsx:1517`, which also feeds the ladder's `headInFrame`. · what ships: both accepted. C-1 amended: `DocSpineProps` adds `letterheadInFrame?: boolean`. DOM contract amended: `data-letterhead-in-frame` (rail-head phrase span, values `"true"`/absent) and `data-region-head-in-frame` (ladder segment button) are **written by React from `useLensFrame`'s state**, not imperatively in a rAF — the design's §5 row said imperative; a React-owned attribute driven by one IntersectionObserver-backed hook is the simpler contract and the cost is one span re-render per crossing. Constraint on `router.push`: internal `href`s only (every guide destination today is a `/doc/…` or `/portal/…` path); an external URL, if one is ever derived, goes to `window.location.assign`.

- `D-B20 · the instruments ledger prints at 390, under the vitals — it is not hidden · 2026-08-29 · ruled by ORCHESTRATOR (W3 integration) · measurement: the W3-L2 brief said the instruments row is `hidden` below 1180 and only appears in the letterhead's ledger column at ≥1180. The proposal says the opposite in two places: `proposal.md:275` — *"`LetterheadInstruments` moves into `doc-letterhead.tsx`'s ledger column **at ≥1180 only** — `region-head.tsx:120` is `grid-cols-1` below that … so at 390 the instruments stay where they mount and the 390 budget carries them"* — and `proposal.md:394`, which budgets the 390 header at **341px of 844 (40.4%)** with *"the instruments stay here, budgeted, because `region-head.tsx:120` is `grid-cols-1` below 1180"*. What shipped follows the proposal, not the brief: `doc-letterhead.tsx:58` is `grid grid-cols-1 … min-[1180px]:grid-cols-[1fr_auto]`, the same two-state grid as `region-head.tsx:132`, so below 1180 the ledger simply stacks under the vitals in one column and every instrument still prints. Nothing carries `hidden`. · what ships: the stacked ledger at 390. The brief's `hidden` was wrong and was not implemented. **Measured (W3 walker, 2026-08-29):** `#document-project-status`'s `getBoundingClientRect().height` at 390×844/s0 on `/doc/b0000000-0000-0000-0000-0000000000d5` is **367.06px** — 176px over the proposal's budgeted **191px**, and by itself 26px over the proposal's whole **341px** 390-header budget (`proposal.md:394`) before the 56px lens band or anything else in the header is even added. The ledger's own grid (`min-[1180px]:grid-cols-[1fr_auto]`) starts 67px into the header (`top: 103` vs the header's `top: 36`) — that gap is the title/vitals stack above it — then the instruments print stacked beneath in the same single column, as the row above states. On the pre-work doc (`…d6`) the same header measures **283.5px** (grid also at `top: 103`, header top 36). Neither spread is within reach of 191px: the stacked ledger is real, uncounted height, not free.

- `D-B24 · line 2's per-tier pixel budget is the TRIGGER; when the long form does not fit, the band prints the design lead's per-item SHORT FORM — never the qualifier ladder, never ellipsis · 2026-08-29 (amended after `w3-review-design.md` §3) · ruled by ARCHITECT + DESIGN LEAD, split by ORCHESTRATOR · measurement: `w3-r2-discharge.md` — at 390 the paper's text run is **327px** (`px-[1.75rem]` at the 18px root), the `+7 MORE` door + `gap-2` take 61.38px, leaving **265.62px**; `LENS_LINE2_MAX_CHARS = 110` (`lens-band-derivation.ts:138`) is the 900px measure's number, so `truncateLine` never runs before `LINE_CLIP` (`lens-band.tsx:37`), and five of the eight seeded sentences (572.52 / 295.94 / 279.14 / 267.72 / 261.69px) would lose their sentence to the ellipsis if ranked worst. Band type measured on the seed: Inter 15px ≈ **7.7 px/char** (127.38/16, 572.52/76, 295.94/39, 267.72/35); the mono 11px door/act ≈ **7.5 px/char** (`+7 MORE` = 52.38px). The mockup's 390 frame prints `OVERDUE 6D · BEDROOM` `REMIND` `+3 MORE`. · what ships (W3 fix lane): **(1) constants** — `lens-constants.ts` gains `LENS_LINE2_MEASURE_PX = { full: 900, narrow: 950, mobile: 327 }`, `LENS_LINE2_PX_PER_CHAR = 7.7`, `LENS_MONO_PX_PER_CHAR = 7.5`, `LENS_LINE2_GAP_PX = 9`; `LENS_LINE2_MAX_CHARS` is deleted. **(2) input** — `LensBandInput.tier: 'full' | 'narrow' | 'mobile'` (the page passes the band's own `useMediaMatch` tier, `page.tsx:1967`). **(3) two forms** — `LensStandingItem` gains `short: { state: string; days: number | null; subject: string }` (state = the sheet's eyebrow word — `OVERDUE`, `CLAIM OPEN`, `NO ACK`, `AWAITING INSPECTION` → `INSPECT`; days = the stated day-count; subject = the item's head noun phrase capped at **12 characters** — `INVOICE 2026-114` → `INV-2026-114`, `BEDROOM APPROVAL` → `BEDROOM`, `FDL-0912 CONSOLE` → `CONSOLE`), and `deriveLensBand` exposes `line2.long = { sentence, act }` (the full sentence, the full act label) and `line2.short = { sentence: `${state} ${days}D · ${subject}`, act: shortenAct(act.label) → first word (`REMIND`, `CHASE`, `FILE`) }`; the `+N MORE` door prints whole in both. **(4) the choice** — `budgetPx = MEASURE[tier] − doorPx − (act ? actPx + GAP : 0)` with `doorPx = withheld ? doorChars × 7.5 + GAP : 0`, `actPx = act.length × 7.5`; `fits(form) = form.sentence.length × 7.7 ≤ budgetPx(form.act)`; the band prints `long` when it fits, else `short`, and marks `data-lens-line2-form="long" | "short"` on `[data-lens-line="2"]`. The qualifier ladder (`trailingQualifier`) is deleted; `LINE_CLIP` stays as the backstop only for a short form that still overflows (it never should on the seed — see the twin). **(5) tests** — jest twin (`lens-band-derivation.test.ts`): every seeded item's short form fits the **327px mobile measure** with its act and the `+N MORE` door (`short.sentence.length × 7.7 + actPx + doorPx + gaps ≤ 327`), and every item's long form fits `full`. **390 falsifier** (`lens-band-height.spec.ts`, new case, chromium + webkit): `psqlRun` marks the `$17,500` invoice paid in `beforeAll` (restored in `afterAll`) so the 76-char invoice sentence ranks worst; at 390/s0 assert `[data-lens-line="2"]` has `data-lens-line2-form="short"`, `[data-lens-sentence]` text matches `/^OVERDUE \d+D · INV-2026-114$/`, `[data-lens-sentence].scrollWidth <= clientWidth` (no ellipsis), `[data-action-key="lens-band-standing"]` visible with text `REMIND`, `[data-lens-more]` visible; at 1440 the same fixture prints `data-lens-line2-form="long"`. **Closure note — W6-R1 §1 / F1 (2026-08-30, DESIGN LEAD's final walk `build/w6-walk.md` ~:364; phase C lands it):** the short form's `<SUBJECT>` is the item's **object per need kind**, never the sentence's first word — `schedule_conflict` → the week (`CONFLICT · SEP 21` `RESOLVE`, `Mon D`, no day-count), `invoice` → the number (`OVERDUE 7D · INV-2026-114` `SEND`), `decision` → the noun, `damage` → the piece, `po_silence` → the PO. The walk found `shortSubject` falling to the first word of a schedule-conflict sentence (`CONFLICT 6D · SCHEDULE`). Wiring-sized: one branch per need kind in `shortSubject` (`lens-band-derivation.ts`); the jest twin adds the `…d7` shape; the 390 paid-invoice falsifier is unchanged.

- `D-B25 · `margin-handoffs.spec.ts:156` re-pointed at the lens (band line 2 → standing sheet), owned by the W3 fix lane · 2026-08-29 · ruled by ARCHITECT (W3-int item 2) · measurement: `e2e-baseline.md` W3-int — the only chromium red on the merged wave; `section[aria-label="Needs attention"]` no longer mounts because C-6 made `RedLetterZone` a model provider; the test was already re-pointed once (W0-fix, from `#document-next-up`) and the file was never in `test-impact.md`. Grep of `e2e/` for `Needs attention`, `data-red-letter`, `document-next-up`, `Next up`: **only `margin-handoffs.spec.ts`** (`:151-164` the live test, `:176-215` the quarantined sibling); `quiet-responsive-shell.spec.ts:118` and `field-coordination.spec.ts:207` match `getByRole('region'` on unrelated names. · what ships (W3 fix lane): **`:156` rewrite** — on `FULL_RAIL` (`…d1`, 3 overdue decisions): `#document-next-up` count 0 **and** `section[aria-label="Needs attention"]` count 0 (pins both supersessions); `[data-lens-line="2"][data-lens-line2-kind="standing"]` visible; then open `[data-lens-more]` and assert one `[data-standing-row][data-standing-tier="overdue"]` whose text matches `/\d+ decisions? overdue — oldest due [A-Z][a-z]{2} \d{1,2}/` and whose act reads `Chase the approval` — the test's claim becomes "the overdue derivation prints once in the lens" (band or sheet; the sheet is the deterministic site because line 2 prints whichever item ranks worst). **`:176` stays `test.fixme`** with its comment and selector re-pointed from `section[aria-labelledby="document-next-up"] [data-action-key^="gate-"]` to the standing row's act (`[data-standing-row] [data-action-key]`), and its un-fixme condition restated as "an overdue need's act raises the margin and focuses the gate's own control from the standing sheet"; owner e2e triage. `test-impact.md` gains the file in the Unnamed table with owner **W3-fix**.

- `D-B26 · the letterhead grid: title spans both columns, the ledger sits beside household + vitals in a bounded right column, labels shortened at every width, the title 32px at 390; budgets 170 / 240 · 2026-08-29 (amended in place to W3-R4, `reconciliation.md:219`, to W3-R5, `reconciliation.md:231`, to W3-R6, `:252`, and to W3-R7, `:278` — the lineage R5 → R6 → R7 is kept in the row) · ruled by ARCHITECT, countersigned by DESIGN LEAD · measurement: `doc-letterhead.tsx:58` is `grid grid-cols-1 items-start gap-x-6 gap-y-2 min-[1180px]:grid-cols-[1fr_auto]` with the ledger in the `auto` column, so the five full-label acts (≈616px of 11px mono at 7.5 px/char + gaps) take the width and the `1fr` left track collapses: the 40px title — an `<input>` (`letterhead-vitals.tsx:441-472`), which cannot wrap, only clip — prints `Aspen Lo` at **149.9px**, the vitals stack four lines, the letterhead measures **242px** at 1440 (budget 148), **223px** at 1280, **367px** at 390 with a three-row ledger (first head at 665 of 844). The mockup's own 390 frame prints the title at **32px** (`#frame-390 .lh-title { font-size: 32px }`, line-height 1.08 → 34.6px a line) — the proposal's 191 priced a 32px title and no ledger. The root is 18px, so every value below is an arbitrary length. · what ships (W3 fix lane, `doc-letterhead.tsx` + `letterhead-instruments.tsx`): **no element dropped at any width** (mark row, chip, vitals, ledger all print — W3-R4). **Grid** — `grid grid-cols-1 gap-x-[1.5rem] gap-y-[0.5rem] items-start min-[1180px]:grid-cols-[minmax(0,1fr)_minmax(18rem,24rem)]`; **row 1** the title block (`LetterheadTitle` / `<h1>`) with `min-[1180px]:col-span-2 min-w-0` and the size split **`text-[32px] min-[1180px]:text-[40px] leading-[1.08]`** — the input gets the whole measure (900px at 1440 ≈ 46 characters of 40px Playfair; the seed's 27-character title needs ≈ 518px), so it never clips on any seeded title; a title longer than the measure still clips inside the input, pre-existing behaviour and out of scope (a wrapping title needs a `textarea`, not this lane); **row 2, left** the household chip + vitals (`min-w-0`; vitals `whitespace-nowrap overflow-hidden text-ellipsis`, one row by construction); **row 2, right** the ledger, `min-[1180px]:justify-self-end min-w-0` inside the `minmax(18rem,24rem)` track (324–432px), the `DocumentActionGroup` keeping `flex-wrap`. **Labels** (W3-R5 supersedes W3-R4's ≥1180 set): there is no separate Milestones act — `SharingTierInstrument`, `letterhead-instruments.tsx:471-509`, is **one act** — and it prints **`SHARING` alone at every width**: the tier word is state the instrument's own menu prints one press away, and on the letterhead it was the only label costing a second row (accessible name stays `Sharing · Milestones`). The row is `MESSAGE · PREVIEW · SHARING · CALL SHEET · N` everywhere. Measured, not estimated (W3-R5 on `3fb009c4b`): `DocumentAction` prints 12px mono at 0.1em, so at ≥1180 the row is 72 + 71 + 72 + 130 + 40.5 = **385.5px → one row inside the 432 track at 1440, ≤ 2 rows inside the 324 floor at 1180–1439**. Below 1180 the same four acts at 12px measure 385px in a 327 run and can never be one row, so **the letterhead ledger prints its acts at the 11px mono floor below 1180** (proposal §3 — the register the band's line 1 already uses): 7.5 px/char → 52.5 + 52.5 + 52.5 + 105 + 40.5 = 303px by W3-R5's glyph count — **corrected by W3-R6**: with `px-[6px]` × 4 and the 0.1em tracking the four 11px acts measure 67 + 66 + 66 + 120 + 3 × 13.5 = **359.5px in a 327 run, still two rows**. W3-R6's two print changes close it, both below 1180 only: (1) **`CALL SHEET · N` → `CALL SHEET`** (the count is the door's — the ladder's `Call sheet · N` door and the sections sheet print it; the `aria-label` keeps `Call sheet · N`), ≈ 31px; (2) the ledger's gap **13.5 → `gap-[9px]`** (the band's own `LENS_LINE2_GAP_PX`), 13.5px — row **315px inside 327**, one row of 44 by construction; `px-[6px]` and the tracking stay. Targets keep `min-h-[44px]`; the ledger root is selected by its existing stable hook `[role="group"][data-action-region="letterhead-actions"]` (`lens-band-height.spec.ts:265`; height ≤ 48 = one row) — no `data-letterhead-ledger` attribute ships (W6 audit correction). **Register mechanism (ARCHITECT):** the size lives on `document-action.tsx:53`'s class string, so the two admissible forms are (i) a `register?: 'body' | 'floor'` prop on `DocumentAction`/`DocumentActionGroup` that swaps `text-[12px]` for `text-[11px]` (default `'body'`), or (ii) a wrapper class on the letterhead's group — `max-[1179px]:[&_.da-act]:text-[11px]` — with no API change; the fix lane picks the smaller diff, which is (ii) unless the 11px floor is already needed by a second caller, in which case (i). Either way the ledger root carries `data-letterhead-ledger` so the spec can measure its row count and computed `font-size` (11px below 1180, 12px at and above). `MESSAGE` / `PREVIEW` drop the family word at every width (it repeats the chip 20px above); `aria-label`s keep `Message the client` / `Preview as the client`. `MESSAGE` / `PREVIEW` drop the family word at every width (it repeats the chip 20px above); `aria-label`s keep `Message the client` / `Preview as the client`. **Budgets (W3-R7, final — one number per width, asserted in both chromium and webkit):** measured on `document-lens/w4` @ `8545739eb` after W3-R6's wiring — chromium **192.06 / 255.17 / 423.17-net**, WebKit **201 / 262.25 / 430.25-net** (WebKit's font metrics and rounding, the same engine that lays out 1431px at a 1440 viewport; the same elements print in the same rows in both). Gates: **1440 letterhead ≤ 205px · 390 letterhead ≤ 265px · 390 first `[data-region-head]` ≤ 435px net of `MobileMarginChips`** (`firstHead.top − chipsBlock.height`, gross ≈ 580 and the chips height printed beside it) — the +10 / +5 / +5 over chromium's own figures is the **engine allowance**, named in the spec's comment beside each case with both engines' measured numbers so a regression is read against the engine, not the allowance. R127 carries one number per width. Lineage: W3-R4 170 / 240 / 390 → W3-R5 185 / 250 / 400 → W3-R6 195 / 260 / 430 (chromium only) → W3-R7 205 / 265 / 435 (both engines); W3-R6's shipped-chrome arithmetic (mark row 51.25, title 44.19, chip + vitals cell 54.63, rule 1; vitals two rows at 390 as the mockup's own `.vitals { flex-wrap: wrap }`) is accepted, and no letterhead element may be dropped at 390. Once D-B30 retires the chips below 980, the 390 first-head gate is asserted gross at the same 435. **Dedupe** ("line 1 drops the money when line 2 names it") belongs in `deriveLensBand`, inside `rightSlot()` (`lens-band-derivation.ts:395-401`): after `worst` is chosen, if `worst.namesMoney` (the item derived from the ticket's `money` row, or a need of kind `overdue_invoice`) the right slot drops its money half and `moneyOnly` is `null` — the same mechanism the `readingStop.key === 'money'` yield already uses, one function, jest-tested on `…d5` (`Invoice INV-2026-114 · $17,500 overdue …` on line 2 → line 1 right prints `INSTALL SEP 19` alone). Acceptance on `…d5`: 1440 — letterhead ≤ 205px, title 40px unclipped (`scrollWidth === clientWidth` on the input), vitals one row (`[data-letterhead-vitals]` height ≤ 24px), ledger ≤ 2 rows; 390 — title `font-size: 32px`, letterhead ≤ 265px, ledger one row at computed `font-size` 11px with `CALL SHEET` unnumbered and `gap` 9px, first `[data-region-head]` ≤ 435px net of the chips block (gross at the same 435 once D-B30 lands) — each asserted in chromium and webkit. **Amended by D-B48 (2026-08-30):** the title is a read-only wrapped `<h1>` at rest (the `<input>` only in edit mode); the 390 gates become line-count-chosen — 265 / 435 for one line, 300 / 470 gross for two.

- `D-B27 · the FF&E region forces `full` on the install/care spreads and during the release ceremony · 2026-08-29 · ruled by ARCHITECT (W4-L2) · measurement: `ffe-section.tsx:1246-1322` — install/care/selecting heads are not a `RegionHead`. The branch is `{mode === 'install' || selecting ? (<div><h2>Install|Care|Choose what to release</h2>…</div>) : (<><RegionRule/><RegionHead …/></>)}`, so on those postures there is no `RegionHead`, no ledger, and no `exceptions` prop — and OD-12/DL-06 derive the quiet form and its reserve from exactly those (`hasStanding` = "`RegionHead` `exceptions.length > 0`, passed by the region itself"). Left as a plain stop key, `[data-index-region="ffe"]` on an install spread would render `data-density="quiet"` while printing a head the quiet form was never specified for, and the attribute would state a density the root does not print. · what ships: `useRegionFold({ …, forceOpen: mode !== 'project' || selecting })` on the `ffe` key. Under C-8 `forceOpen` gives `folded = false`, `density = 'full'`, `cause = null`, so `data-density` is truthful on those spreads and the body prints as it does today. No behaviour changes: `ffeFolded` was already `mode === 'project' && !selecting && ffeFold.folded` (`:1090`), so install and the ceremony never folded; the only new consequence is that `setFolded(true)` is a no-op while forcing, and neither posture renders a `Fold ↑` (install has its own head, `selecting` renders no `RegionHead`). Covered by `ffe-region-head.test.tsx` "leaves install mode full — its head has no quiet form to stand in". Wave 5, which gives the pre-work spreads real `RegionHead`s, does not touch this branch.
- `D-B28 · `get_project_ffe_readiness` during the settled scroll is the tail of the initial load, not a lens fetch — the network spec gets a network-quiet precondition and a two-entry allowlist · 2026-08-29 · ruled by ARCHITECT (W4-L4 item 1, PRE-EXISTING — no product change) · measurement: the caller is `packages/supabase/src/hooks/use-project-ffe-ga.ts:87-117` `useProjectFfeReadiness(selectionIds)` — key `['project-ffe-readiness', sortedIds]`, `enabled: ids.length > 0`, `staleTime 15_000`, and a `queryFn` that issues **one** `rpc('get_project_ffe_readiness', { p_ffe_item_id })` per id at `CONCURRENCY 8`. Two consumers stand on the paper: `ffe-section.tsx:849-853` (every `project_ffe_items` row) and `project-mood-boards.tsx:92-94` (active rows only — a different key, so its own fan-out); `spec-books/spec-book-workspace.tsx:787` is off-paper. It is a dependent query: `enabled` flips true when `useProjectFFEItems` resolves, so the fan-out is the last thing the initial load does — 42 RPCs in the lane's failing run, 63 requests / 62 distinct ids in my CDP probe (chromium headless against :3000 serving `4915583c2`, `Network.requestWillBeSent` with async initiator stacks, `readiness-probe.cjs`). The probe saw **0** readiness requests before the scroll and 63 at steps 17–25 (5.76–6.09 s after navigation); every initiator stack is React Query's own fetch scheduling — no scroll listener, no `useLensFrame`, no observer in any frame. The paper was still loading through the scroll: `document.documentElement.scrollHeight` was 2353 at step 0 and 10200 by step 17, and `project_ffe_items`, `project_rooms`, `client_decisions`, `project_payment_milestones`, `get_ffe_invoice_coverage` and `/auth/v1/user` all fired at steps 13–16. The precondition never held because W3 code publishes neither `data-lens-settled` nor `window.__lensSettled` (zero hits in `apps/designer-portal/src` at `4915583c2`), so `e2e/helpers/lens.ts settle()` falls through both branches and returns after two frames; `openPaper` is `domcontentloaded` + shell visible. And even on W4 code `__lensSettled()` is a scroll-velocity settle (D-B15, <40 px/frame for 120 ms) — it says nothing about the network, so the fan-out can still be in flight when the scroll starts. Wave 3 did not touch the chain: `git diff main..4915583c2 -- ffe-section.tsx` has no hunk on the readiness / `selectionIds` / `useProjectFFEItems` lines, and `use-project-ffe-ga.ts` and `project-mood-boards.tsx` carry no W3 commit. The lane's 8–90+ variance is simply how much of the 42–84-request tail remained after `openPaper` returned — dev-server compile time decides it. · what ships: (1) a `quiet(page)` helper in `e2e/helpers/lens.ts`: `page.waitForLoadState('networkidle')`, then a 1 000 ms window with zero requests to the Supabase origin (`process.env.NEXT_PUBLIC_SUPABASE_URL ?? 'http://127.0.0.1:54321'`), 30 s cap, failing with "the paper never reached network quiet — the initial load has no end" (a finding in its own right, never a silent pass). The network test's precondition becomes `openPaper → settle → scrollTo(0) → settle → quiet`, and the listener is attached only after it. (2) The assertion: during the 30-step settled scroll, zero requests whose origin is the Supabase origin, with exactly two allowlisted paths — `/auth/v1/token` with `grant_type=refresh_token` (GoTrue's timer-driven refresh fires on session age, not scroll) and `/realtime/v1/` (the broadcast channel's HTTP handshake/heartbeat). Nothing else is admitted: `/auth/v1/user` mid-scroll means a `getUser()` on a remount and is a real finding; `rpc/*` and `rest/v1/*` are what the sentence is about. (3) Non-Supabase requests during the scroll (lazy `<img>` — the run's three, Unsplash ×2 and `fixtures.invalid` ×1 — `/_next/` chunks, fonts) are collected and `console.log`ged, not asserted: they are the browser's lazy loading and Next's code-splitting, pre-existing and not a lens fetch; their count is written into I152. (4) I152 also records the pre-existing debt the probe exposed — one RPC per line, 42–84 round-trips per paper open at concurrency 8; a batched `get_project_ffe_readiness_many` RPC would remove it, but `use-project-ffe-ga.ts` is not a touched file, so under the debts rule it is logged, not owned. (5) The lane's run was made against the W3 server: every verdict from the six lens specs in that run is void for W4 assertions (no `__lensSettled`, no density hook on the served code). W4-L4 re-runs against a server built from its own branch, booted per technical-design §6 (`npx turbo run dev --env-mode=loose`), and the W4 specs assert `typeof window.__lensSettled === 'function'` up front so an unbuilt server fails loudly instead of measuring the wrong code.`
- `D-B29 · the CLS instrument observes from the settled s0 only — `buffered: false` after `quiet` + `__lensSettled()`, `hadRecentInput` excluded, gate exactly 0 with the shifted nodes named; initial-load CLS reported ungated · 2026-08-29 · ruled by ARCHITECT (W4-L4 item 2) · measurement: `lens-cls.spec.ts:51-57` installs the `layout-shift` observer with `buffered: true` after `scrollTo(0)`, so it replays every shift since navigation. On `4915583c2` the reported 0.0613 accrued while the paper was still loading (D-B28's probe: `scrollHeight` 2353 → 10200 during the scroll) — regions growing as queries landed, replayed into the sum — not a movement the lens caused. A `layout-shift` entry is produced only for a visible node that moves for a reason other than scrolling; density promotion happens at or beyond the 240 px lookahead line (D-B16), i.e. below the viewport, and the band is sticky and excluded, so a true 0 over a settled scroll is achievable by construction and is what sentence (c) claims. · what ships: (1) precondition = `openPaper → settle → scrollTo(0) → settle → quiet` (D-B28's helper), then `PerformanceObserver.observe({ type: 'layout-shift', buffered: false })` — the settled s0 is the origin, nothing earlier is replayed; the spec asserts `typeof window.__lensSettled === 'function'` first so a server without W4 code fails loudly. (2) Entries with `hadRecentInput === true` are skipped (the web-vitals convention; programmatic `window.scrollTo` sets none, so the filter cannot hide a lens shift). (3) The gate is `toBe(0)` exactly, not ≤ 0.01: the mechanics make 0 reachable and a tolerance would admit a real nine-millishift from the band or rail. What changes instead is the failure message: each surviving entry is reported with the scroll step it landed on and its `sources[]` — node tag + id/class + `previousRect → currentRect` — so a nonzero reading arrives as a bug report naming the element, not as a number. (4) Initial-load CLS is a separate `buffered: true` measurement from navigation to `quiet`, `console.log`ged (`CLS (initial load): …`) and written into I152 as a number, **not gated** — paper-loading layout is pre-existing and outside sentence (c), but the figure stays visible so a regression cannot hide behind the scroll gate. (5) Chromium only, as the spec already declares; the reduce variant carries the same instrument and the same gate.`
- `D-B30 · at 390 the letterhead's margin chips yield to a Margin sheet opened from the mobile bar — priced to Wave 5 as lane W5-L3; the 390 first-head gate is net of the chips block until it lands · 2026-08-29 · ruled by ARCHITECT (pricing the DESIGN LEAD's W3-R5 §4, `reconciliation.md:231`; the lead wrote "D-B27" — that id is W4-L2's FF&E `forceOpen`, so this ruling takes the next free number) · measurement: the mockup's `#frame-390` prints nothing between `#lens-390` and `region-approvals-390`; its seven margin items live in `#sheet-margin-390` — head `MARGIN · 7 · 1 overdue`, a `EVERYTHING STANDING 4` list, and per row a stamp (`OVERDUE 6 DAYS`, `CLOSES TOMORROW`, `14 DAYS NO ACK`), the title, `OWNER CLIENT/DESIGNER/MAKER`, and **one inline act** (`SEND A REMINDER`, `CHOOSE THE FABRIC`, `FILE THE CLAIM`, `CHASE THE PO`) — opened from a fourth bar item (`.mb-item[data-open-sheet="sheet-margin-390"]`, eyebrow `MARGIN`, value `N`). The shipped app prints `MobileMarginChips` (`mobile/mobile-margin-chips.tsx:22`, `min-[980px]:hidden`) at `page.tsx:2142` with `anchorKind="letterhead"` — letterhead- and section-anchored non-time items + handoff gates + the decision-classification notice — directly between the band and the first region; W3-R5 measured it at **157.3px** on `…d5`, first head at 610.92. A second mount at `ffe-section.tsx:513` (`anchorKind="line"`) prints each Pieces line's own items beneath the line and is **not** in question. **What already exists in the shipped vocabulary:** the whole-margin listing. The `'spine'` sheet (`mobile-sheets.tsx:477-`, opened by the bar's left zone `mobile-bar.tsx:231 openSpine`) already prints a second half — `In the margin · {raised.length + handoffGates.length}` (`:669`) with `MarginDecisionClassificationNotice`, the handoff-gate rows, and a row per open item whose tap opens the `'margin-item'` sheet (`:732`, D3-2: the full item + its actions). So the lead's "the shipped app has no whole-margin sheet" is half right: it has the listing, folded into the Sections sheet, with the act one tap deeper and no bar door of its own. The bar hides at `min-[1180px]:hidden` and the chips at 980, so the sheet is reachable at every width where the chips print. · what ships (**Wave 5, lane W5-L3, Sonnet**, sequenced after W5-L1 on the W5 branch because both touch `page.tsx`): (1) `mobile/mobile-shell.tsx` — `MobileSheet` gains `{ kind: 'margin' }` (:61-64) and the API gains `openMargin()` (:92/:195); `MobileActiveDoc` gains `marginCount: number | null` (published by `page.tsx` beside `readingIndex`). (2) `hooks/use-letterhead-margin.ts` (new) — the derivation now inlined at `mobile-margin-chips.tsx:36-86` (anchored items → `classifyMarginItems` → non-time chips, gates from `useHandoffGates`, `decisionState`) extracted as a hook; the chips component and the sheet both call it, so the sheet lists exactly what the block printed. (3) `mobile/mobile-sheets.tsx` — a `'margin'` branch: `Sheet tone="paper" kind="margin"`, head `Margin · N` with `M overdue` when M > 0, then the notice, the gate rows and the item rows **moved out of the spine sheet** (the Sections sheet keeps sections + the doors); each item row prints the mockup's form — stamp, title, owner, and **one inline act button** (`data-margin-row-act`) that is the item's first act, taken from the same act table the `'margin-item'` sheet renders (the lane finds it in the item sheet's action list and shares it; no second act table) — with the row body still opening the item sheet; `SHEET_RETURN_FALLBACKS` and the accessible-name table (`:136-193`) gain `margin`. (4) `mobile/mobile-bar.tsx` — the door: **not** a fourth bar item (OD-11's three slots stand; A-01) but the **first door of More's `IN THIS DOCUMENT` list** (`:129-147`, above Plan room), label `Margin · N` from `activeDoc.marginCount`, `data-mobile-document-door="margin"`, opening `openMargin`; the mockup's bar placement is the one deviation from what prints, logged here. (5) `page.tsx:2142` — the letterhead `MobileMarginChips` mount is **deleted** (the `anchorKind: 'letterhead'` branch of the component goes with it; the `'line'` branch stays for `ffe-section.tsx:513`); `page.tsx` publishes `marginCount` from the hook. (6) Tests — jest: `mobile-sheets.test.tsx` (the margin sheet lists every item the hook yields, each with an act button; the spine sheet no longer prints `In the margin`), `mobile-bar.test.tsx` (More prints `Margin · N`; the door opens the sheet; `Escape` returns focus to the door). e2e at 390×844 on `…d5`: `lens-band-height.spec.ts` drops the net-of-chips arithmetic and asserts **`[data-mobile-margin-chips="letterhead"]` count 0** and **first `[data-region-head]` top ≤ 400 gross**; new `mobile-margin-sheet.spec.ts`: More → `Margin · 7` → `[data-margin-row]` count equals the seed's seven letterhead/section items (named in the spec from `scripts/the-document-lens-seed.sql`), every row has `[data-margin-row-act]`, and `Escape` lands focus on the door. **W3 acceptance (the lead's position, confirmed):** until W5-L3 lands, the 390 first-head bullet is satisfied **net of the chips block** — the spec asserts `firstHead.top − chipsBlock.height ≤ 400` and prints the gross figure — on two conditions: the allowance is marked in the spec (`// D-B30: net of MobileMarginChips until W5-L3`) so it is visibly temporary, and W5-L3's falsifier is that line's deletion. Not W6: W6 is integration and rehearsal, and this is a print change the mockup governs. Not a post-I152 follow-up: the 390 first head at 611 of 844 (72 %) is the number SC1 exists to move. **Amended to W5-R1 (`reconciliation.md:270`, DESIGN LEAD; shipped on `document-lens/w5-l3` @ `a6af4b698` with `hooks/use-margin-sheet.ts` in place of the `use-letterhead-margin.ts` named above):** the sheet is the rail's 390 form and carries **the whole margin** — the `anchor_kind='letterhead'`-only set W5-L3 first shipped (`Margin · 4 · 1 overdue` @ `682be7ef2`) sent the reader to the wrong count. On `…d5` the door prints `Margin · 7` and the sheet head **`Margin · 7 · 2 overdue`** (overdue counts decisions — the rug/nightstands decision and the COM reading-chair-fabric decision; the outstanding invoice is a money item and is not counted, as shipped), grouped as the desktop rail groups them (proposal §6, the ratified mechanic; the mockup's flat list is the one thing it simplified): **`THE WHOLE JOB · 4`** above **`BESIDE PIECES · 3`**, the rail's own order within each group, each line-anchored row's second line naming its line (`Living room · reading-chair fabric`); a line row jumps to its line by the chip's press path and its `margin-item` sheet opens from the row. **The line-anchored chips retire too**: `MobileMarginChips` prints nothing below 980 once the sheet carries the margin (the `ffe-section.tsx:513` mount goes with the letterhead one); at ≥ 980 nothing changes. On `…d6` (no lines) one group `THE WHOLE JOB`, no `BESIDE` heading. The spec asserts the head string, both group headings with their counts, 7 `[data-margin-row]` each with `[data-margin-row-act]`, and the 390 first head **gross ≤ 435** (D-B26's W3-R7 number, now without the chips subtraction). **Defect closed — W5-C2 (2026-08-30, ARCHITECT):** as shipped on `document-lens/w5` @ `926c151ae` the row's `[data-margin-row-act]` button and the row body both fired `openRow` (`mobile-sheets.tsx:352-374`), so a control named `Send a nudge` opened the item sheet — two controls in one row with different accessible names and the same behaviour. This row's contract is unchanged and the W5 fix lane makes the button **act**: the inline act is the item's first act from the same act table the `'margin-item'` sheet renders, the row body still opens the item sheet. Falsifiers: jest `mobile-sheets.test.tsx` — pressing `Send a nudge` calls the nudge action and does **not** call `openMarginItem`; pressing the row body calls `openMarginItem` and no act; `mobile-margin-sheet.spec.ts:131` is upgraded from "the button exists" to "the button performs" (the act's observable effect asserted, e.g. the nudge sheet/event).`
- `D-B31 · R4's fling gate: pre-first-region paper is not blank paper — the instrument counts only frames over a quiet root's reserve or a headless gap between roots; `LENS_LOOKAHEAD_PX = 240` stands · 2026-08-29 · ruled by ARCHITECT (W4-int R4, `build/e2e-baseline.md:1024`) · measurement: 1440, `…d5`, `mouse.wheel` s0→s3, **3200px in 257ms**, 104 sampled frames; **2 frames classified blank** — both at t=16ms and t=41ms, both still at y=400 (before the wheel had moved the paper), both `no region ancestor`: the frame centre sat over the letterhead/band paper **above the first region root**. No frame in the fling proper, landing frame `full ffe`. The plan's gate reads "blank paper >1 frame → lookahead grows", so the number came up for a ruling. · what ships: **the paper above the first root is not blank paper.** L-4's failure is paper the lens *should* have promoted and had not — a frame whose centre lies inside a `[data-index-region][data-density="quiet"]` root **below its printed quiet head** (in the 68/112 reserve), or in the gap between root *i*'s bottom and root *i+1*'s top with neither head in frame. The letterhead, its ledger and the band print at every state and every velocity; no lookahead could change what stands there, so counting it measures nothing about L-4. Under that instrument the run reads **0 blank frames of 104**, and `LENS_LOOKAHEAD_PX = 240` stands — no evidence yet that it is short, and growing it costs commit work on paper the reader may never reach (D-B16's discovery line moves with it). **The instrument** (`e2e/document/lens-fling.spec.ts`, `blankPaperCensus()` — the same code the walk quotes): sample every rAF from the first frame where `scrollY` has changed until 120ms after the last (the two pre-motion frames were not the fling), and at each frame classify `document.elementFromPoint(innerWidth/2, innerHeight/2)` four ways — `content` (inside a root with `data-density="full"`, or inside a quiet root at or above its `[data-region-head]` bottom), **`blank`** (inside a quiet root below its head, or no root ancestor with the point between two roots and no `[data-region-head]` intersecting the viewport), `pre-region` (no root ancestor and `point.y < firstRoot.top` — letterhead/band paper), `post-region` (below the last root — colophon/foot reserve). Gate: **`blank ≤ 1`** (one frame is one 16ms miss at the lookahead line; two is a pattern), the census printed in full every run, and the landing frame must read `full <key>` for the stop the fling ends on. `pre-region` and `post-region` are reported, never gated. Chromium and webkit both; the wheel gesture is the falsifier, not `scrollTo` (which is one frame and never exercises the lookahead).`
- `D-B32 · the settle gate deadlocked after a single programmatic scroll — L-9 restated: settled ⇔ no frame ≥ 40px in the trailing 120ms; the fast frame arms the timer itself; slow frames neither unsettle nor delay · 2026-08-29 · ruled by ARCHITECT (W4-int, `build/e2e-baseline.md:956-1000`; fixed by W4-L1 on `document-lens/w4` @ `8545739eb`+) · measurement: `hooks/use-lens-density.ts` `runScrollFrame` (`:267-284`) armed the 120ms timer **only** on a later frame whose travel was under 40px; a single programmatic `scrollTo` is one over-gate frame, so it wrote `data-lens-settled="false"`, cleared the timer, and nothing re-armed it — `settled` stayed false, `commitPending()` never ran again, `window.__lensSettled()` never resolved. `PROBE 1.5s after scrollTo(0,400): {"attr":"false","y":400,"fn":"function"}`. A 10px nudge re-armed it, which is why a human wheel-scroll self-healed and every e2e `scrollTo` (and reduced-motion presses, and restored scrolls) hung. Second finding: `writeSettled(true)` ran once from the layout effect, so at rest the shell carried **no** `data-lens-settled` at all where the DOM table promises `"true"`/`"false"`. · what ships: **§3 L-9 intent confirmed and restated.** The proposal's "< 40px/frame for 120ms" means **settled ⇔ no scroll frame of ≥ 40px in the trailing 120ms** — not "120ms after the last frame whatever its velocity": under that reading a continuous slow trackpad drift (every frame under 40px) would never settle and L-4 would defer promotion until the drift stopped, which is the blank paper L-4 exists to prevent. Mechanics: on every scroll frame, if `travelled ≥ 40` → `writeSettled(false)`, `lastFastAt = now`; then, whenever `!settled` and no timer is pending, arm `setTimeout(settle, LENS_SETTLE_MS)`; when it fires, settle if `now − lastFastAt ≥ 120`, else re-arm for the remainder. Consequences, each a jest case on `use-lens-density.test.ts`: one programmatic `scrollTo` (one fast frame) settles 120ms later with no further frame; a continuous slow drift never unsettles and commits during the drift; fast-then-slow settles 120ms after the last fast frame while the slow frames continue; W4-L1's "re-arm on every frame" satisfies this provided a slow frame arriving while `settled === true` writes nothing. **`data-lens-settled` is present from the first commit** — `"true"` at rest — written in the layout effect on mount and on every transition; `e2e/helpers/lens.ts settle()`'s first tier keys on presence, so absence silently demoted every spec to the `__lensSettled()` tier. The band's D-B18 press order runs after settle, so the sections-sheet bypass recorded on D-B18 is part of the same fix. `technical-design.md` §3 amended in place (the header line and the write-path sentence).`

- `D-B33 · OD-4's `content-visibility: auto` on `[data-passed]` is DELETED — it was 99.9% of the CLS D-B29 gates at exactly 0; logged as an "OD-4 fallback candidate" by OD-4's own pre-agreed failure move · 2026-08-29 · ruled by ORCHESTRATOR (W4-fix lane, taking the move OD-4 pre-agreed) · measurement: chromium, 1440×900, `/doc/…d5`, `lens-cls.spec.ts`'s 30-step settled scroll (2400px) from the settled+quiet s0. **CLS 0.8657658230921531 with the block; 0.0009864163706168553 with the single `content-visibility: auto` declaration removed and nothing else changed.** The property is 99.9 % of the shift. Its two contributions, from the spec's own shift log: **step 9** — a folded region's `FoldSeam` subtree (`button.fold-settle`, the pointer-events overlay, the `h-3` rule) collapsing its `section.mt-[var(--doc-region-gap)]` from **219.73px → 40.94px** and back, 0.078 each way; **step 24** — four `li#ffe-selection-…` rows zeroing and returning inside `section#project-ffe`, **0.3534 + 0.3554**. Both are the property TOGGLING as the element crosses the engine's relevance margin, not a missing reserve: `contain-intrinsic-size: auto <length>` was already the form that remembers the last laid-out size, and **deferring `data-passed` until two frames after the promotion** — so the engine has laid the full body out and holds a real last-remembered size before it may ever skip it — was implemented and measured and moved the number **not at all** (0.8657658230921531 on both runs, to the digit). W4-C8's finding stands and is folded in here (this also discharges fidelity **F4**): the shipped length was `var(--doc-quiet-reserve, 112px)`, whose fallback is unreachable because every region body sets `--doc-quiet-reserve` on the region root itself — the same element the rule matched — so it resolved to the 68px `-min` for five of six regions while the comment claimed the larger reserve; and OD-4's literal bare `contain-intrinsic-size: auto` is not valid grammar (`auto? [ none | <length> ]{1,2}`), so it would have been dropped entirely and a skipped region would have reserved nothing. · what ships: the `@supports` block is **deleted**, with the whole measurement kept in `globals.css` where it stood so the next reader does not re-derive it. `--doc-passed-reserve: 112px` stays declared once on `:root`, **unspent**, for whoever takes the candidate up — deliberately not `--doc-quiet-reserve`, which no rule on a region root can ever reach the fallback of. `lens-css-scope.test.ts` asserts no `content-visibility` declaration exists anywhere in `globals.css`, so reinstating it without a new ruling turns a jest gate red before a browser has to. **The OD-4 find-in-page gate (`quiet-responsive-shell.spec.ts`, chromium AND webkit) PASSES either way** — it was never the thing that failed, and OD-4's benefit was never proven on this paper. `technical-design.md` §5 OD-4 amended in place. **ARCHITECT verdict (2026-08-29): RATIFIED — the deletion stands, and "nothing" is the accepted replacement, measured rather than assumed.** The physics is the ruling's reason, not just the number: a `[data-passed]` root sits ABOVE the viewport, so any change in its box on the relevance crossing moves everything below it, the viewport's own content included — a reserve can only avoid the shift by equalling the laid-out height to the pixel, and the deferral experiment (a real last-remembered size held before the first skip, 0.8657658 to the digit) is the evidence that the engine's remembered size does not deliver that. The two alternatives are ruled out for W4–W6: per-root `contain-intrinsic-size: auto <measured px>` written at passing is admissible only under a new ruling with a paper-CLS-0 proof attached (the deferral result predicts its failure, so no lane spends a day on it unprompted); `content-visibility: hidden` with our own reserve is **disallowed** — it re-implements the engine's toggle by hand with the same shift physics and adds a second relevance logic to a hook that has one. The long paper's cost without skipping is the trade, and it is taken **on a measurement, not a guess**: W6's rehearsal records on `…d5` at 1440 the DOM node count, the 30-step settled scroll's main-thread time and p95 frame duration (`PerformanceObserver` `long-animation-frame`/`longtask`, chromium), and `performance.memory.usedJSHeapSize` where available, printed in I152 ungated; the OD-4 candidate reopens only if that p95 exceeds 16.7ms on the settled scroll. `data-passed` stays written because sentence (b)'s density map (`full === frame-intersecting ∪ passed`, D-B16) and the fling census (D-B31) read it; `--doc-passed-reserve` stays declared and unspent; `lens-css-scope.test.ts`'s no-`content-visibility` assertion is the gate that keeps this ruling from being undone by a stylesheet edit.`

- `D-B34 · D-B29's "CLS 0" is scoped to the PAPER; the rail's and the band's own reflow is measured and printed, not gated — DESIGN LEAD ruling requested · 2026-08-29 · ruled by ORCHESTRATOR (W4-fix lane) — **open for the DESIGN LEAD to countersign or overturn** · measurement: with D-B33's deletion the 30-step settled scroll leaves **0.000986** (no-preference) / **0.001001** (reduced motion) of shift, and every one of its eight entries is chrome: five are the rail ladder's `div[--seg-floor]` segments resizing (138.63 ↔ 142.11, 44.92 ↔ 40) and two `button.min-h-[27px]` room rungs swapping by 28.5px as the reading index crosses a stop — which is **OD-14 working**: the current stop carries its room rungs and the segment floors are derived per segment from the value's wrapped line count. The eighth is the band's line-2 `p` moving 18.5 → 26.19 INSIDE the band, whose own height is the declared 56px constant (measured exactly 56 at 1440/1280/390 in both engines), so it cannot move a pixel of paper. · what ships: `lens-cls.spec.ts` classifies each `layout-shift` entry by its sources — an entry every one of whose sources is inside `[data-lens-band]` or outside `[data-document-paper]` is chrome — and sums the two separately. **The paper's number is gated at exactly 0 (measured 0 in both motion modes) and the chrome's is printed on every run.** The reasoning: H5's own sentence is "a region above the frame growing from its reserve is the layout shift the design forbids" — a claim about the paper — and gating the rail's number at 0 would make the gate assert the opposite of OD-14. **This is a scoping decision on a ratified gate and therefore a ruling, not an engineering call:** if the lead wants the chrome inside the 0, OD-14's per-stop rungs must reserve their space and the segment floors must stop deriving from wrapped line count, which is a print change the mockup governs. **ARCHITECT verdict (2026-08-29): RATIFIED on the paper; AMENDED on the chrome — gated by cause, not by an allowance.** The sentence (technical-design §7's list letters it **(c)**, not (b): "CLS `=== 0` and 0 entries over a 30-step scroll in both motion registers") gates the **paper**: H5's own words are "a region above the frame growing from its reserve is the layout shift the design forbids", a claim about `[data-document-paper]`, and the mockup's rail and band both move by design when the reading index or the sentence changes (rungs under the current stop; line 2's sentence). So `[data-document-paper]` subtree = exactly 0 in both motion modes, as the lane shipped. The chrome is **not** left ungated and gets **no numeric allowance** — 0.001 is arbitrary and would hide a real 0.0009 rail shift — it is gated by **cause**: the spec records `[data-document-shell]`'s `data-reading-index` and `[data-lens-band]`'s `data-lens-sentence` at every step, and every chrome entry must land on a step where one of the two changed; a chrome entry on a step where neither changed is a real shift and fails the test. The sum is still printed. This states the rule the lane's numbers already obey (eight entries, all at stop crossings) and asserts it. **Left with the DESIGN LEAD, as the lane asked:** whether the rail's per-stop rungs should reserve their space so the rows below do not move 28.5px at a crossing, and whether the reduce register (transitions off) should swap them instantly — a print question the mockup governs; the gate above does not depend on the answer. **Amendment (2026-08-30, ARCHITECT, after the W4 close run @ `4f803118b`): the cause list stays at two; both chrome findings are defects, not causes.** The lane's five entries were probed read-only on :3000 (`ladder-probe.cjs`, 30 settled steps of 80px at 1440 on `…d5`, every segment's layout box, form, `data-region-head-in-frame`, every root's density and the band's two lines recorded per step). Roots changed on exactly two steps — 9 (`approvals` gains `data-passed`) and 24 (`schedule`) — and **neither moved a segment by a hundredth of a pixel**; so "a root committed on this step" is rejected as a cause on evidence: the rail does not reflow on the lens's own act, and excusing it would have excused the real defect. Every segment move (steps 3/6, 11/13, 23/26) lands on a `data-region-head-in-frame` flip — the L-6 yield unrendering a value line — ruled a defect as **D-B37**; the band's line-2 move at step 4 is line 1 having no box while empty at `open` — ruled a defect as **D-B38**. The gate as written (paper 0; chrome only on `data-reading-index` / `data-lens-sentence` changes) passes once both are fixed and is the falsifier for each.`

- `D-B35 · `locator.boundingBox()` is not the layout box — the band's 56px and the line-2 act's 44px are measured with `getBoundingClientRect()` · 2026-08-29 · ruled by ORCHESTRATOR (W4-fix lane) · measurement: `lens-band-height.spec.ts` read the band at **55.7204px at 1280/s0** (56 at 1440 and 390) and line 2's act at **43.9895px (webkit) / 43.6648px (chromium)** against a `>= 44` contract. Probed directly, at 1440, 1280 and 390, in **chromium AND webkit**: `getBoundingClientRect().height` = **56**, `offsetHeight` = **56**, computed `height` = **56px**, token `--doc-band-height` = **56px**, `box-sizing: border-box`; and for the act `getBoundingClientRect().height` = **44**, `offsetHeight` = **44**, computed `height`/`min-height` = **44px**, `transform: none`, `zoom: 1`. The same `boundingBox()` call returned 55.985 on one 1440 run and 56 on the next against an unchanged box. Playwright's `boundingBox()` reads compositor quads (`DOM.getBoxModel`), and for a `position: sticky` element — the band, and the act inside it — those quads carry the compositor's own fractional sticky offset. · what ships: a `layoutHeight(locator)` helper reading `getBoundingClientRect().height`, used for the band's eighteen `toBe(56)` cells and the C-02 `>= 44` case. **No CSS change and no engine allowance**: neither a `44.5px` floor nor a `>= 43.98` tolerance was taken, because the box was already exactly right and both would have hidden a real regression later. The composited figure is still printed beside the layout one in the C-02 case so the divergence stays visible. **ARCHITECT verdict (2026-08-29): RATIFIED.** The contract is the layout box — `--doc-band-height: 56px` and `min-h-[44px]` are layout values, and `--doc-landing-clear` (72 = 56 + 1rem) derives from the same box — so `getBoundingClientRect().height` is the instrument that reads what the contract promises; Playwright's `boundingBox()` reads `DOM.getBoxModel` content quads, which for a `position: sticky` element carry the compositor's fractional sticky offset (55.72 / 43.99 / 55.985 against an unchanged box). Keeping the composited figure printed beside the layout one is right: a future divergence of more than a pixel would be a real finding (a transform or zoom on the band). No CSS change and no tolerance — confirmed; the same helper is the one the walk quotes for any "56" claim. **D-B40 (2026-08-30) is one level deeper into the same instrument:** a single 55.9754638671875 on the pre-work paper at 390 against `offsetHeight` = computed = 56 and no transform — ratio 0.999562, a scaled read; the cell now prints `offsetHeight`, computed `height`, `box-sizing` and ancestor transforms on failure, and `toBe(56)` keeps no allowance.`

- `D-B36 · a `DocSheet` field never enters `editing`, and the lens is not frozen under it · 2026-08-29 · ruled by ORCHESTRATOR (W4-fix lane, logging the correctness review's nit rather than changing behaviour) · measurement: `use-lens-state.ts:57` requires `node.closest('[data-document-paper]')`, and every `DocSheet` (and the standing sheet) portals to `document.body`, so a field inside one is never `editing` and `freeze(true)` never runs for it. That follows D-B19's wording exactly — the contract names the paper — but a sheet's field is precisely the "must not move under the hand" case D-B19 exists for. · what ships: **no behaviour change this wave.** The paper does not move under a sheet (the sheet is a fixed overlay and the reader's hand is on it, not on the paper), so the risk is a region promoting behind an open sheet and changing what she returns to — real, but not what D-B19 measured. Logged so the next lane can rule it deliberately rather than discover it. **ARCHITECT verdict (2026-08-29): RATIFIED as behaviour, with the reason corrected and one assertion added.** The selector scope stays the paper: **the freeze follows the hand on the paper** — D-B19's case is a field the reader is typing into while the paper she is looking at could move under it; a sheet's field is on the sheet, a fixed overlay, and the paper is not what is under her hand. W4-C5's narrowing of the selector to text entry (no checkbox/radio/select, the three `contenteditable` spellings; `use-lens-state.ts` `EDITABLE_SELECTOR`) is ratified with it — a ticked checkbox holding focus froze the lens for the session, and D-B19's literal `input, textarea, select` was the defect. The lane's own reason is not quite right and is corrected here: `DocSheet` is a `fixed inset-0` layer with **no body scroll lock** (`overlays/doc-sheet.tsx:350`; the panel is `overscroll-contain`, the backdrop is not scrollable), so the paper **can** scroll behind a sheet by wheel over the backdrop, settle, and promote — and that is allowed: promotion is one-directional and ahead of the reader (D-B16), the region she returns to is already `full` by SC11, and the lens is honestly `reading` throughout; nothing moves under a hand that is on the sheet. Two assertions make it deliberate rather than discovered: jest `use-lens-state.test.ts` — `focusin` on a text field inside a `DocSheet` portal writes no `editing` and calls `freeze` with nothing; e2e — open and close the standing sheet at s2 without scrolling, the density map and `data-lens-state` are identical before and after.`
- `D-B37 · the ladder's L-6 yield must keep its box — a yielded value line goes invisible in place, never out of the DOM; room rungs follow `aria-current`, not head-in-frame · 2026-08-30 · ruled by ARCHITECT (W4 close, D-B34 cause gate, `build/e2e-baseline.md` "W4-int CLOSE run") · measurement: (`ladder-probe.cjs`, :3000 @ `4f803118b`, 1440, `…d5`, 30 settled steps): at step 3 the approvals row's `data-region-head-in-frame` turns `true` and its text goes `Client approvals NOTHING YET` → `Client approvals` — the row's own box **40 → 25.25**, the segment's **40 → 36** (its floor), and the freed height is redistributed by `flex-grow: extent` to its siblings: ffe **94.98 → 98.66**, care 41.02 → 41.34; step 6 reverses it. Step 11: the schedule row `Schedule INSTALL SEP 19 · 3 WEEKS` → `Schedule`, row **53.75 → 25.25**, segment 53.75 → 40.02, ffe **94.98 → 107.5**, care → 42.14, record → 40.09; step 13 reverses. Step 23: the Pieces row yields (53.75 → 25.25) **and one room rung appears** (`rungs 0 → 1`) on the in-frame flip, one step after the reading index reached `ffe` at step 22; step 26 reverses both. Roots (`data-density`/`data-passed`) changed only at steps 9 and 24 and moved no segment. So the floors are count-derived exactly as OD-14 rules (`floorFor(register.value)`, `lens-ladder-derivation.ts:133`), and what moves is the **yield**: `lens-ladder.tsx`'s row body omits the value line when the head is in frame, a flex item's `min-height: auto` lets the row shrink below the value's box, and the track's remainder is re-dealt. · what ships (W4 integration lane, `components/document/spine/lens-ladder.tsx` — the row `body` and the rung condition): (1) **the value line always renders**; when `yielded` it takes `invisible` (`visibility: hidden`) plus `aria-hidden="true"` **in place** — same box, same row height (40 / 53.75), same track distribution — so the yield is a paint change and never a layout change; whether the yielded line prints blank or muted is the DESIGN LEAD's (L-6's print), but it may not change the row's height. (2) **Room rungs print under the stop that carries `aria-current`** (OD-14: "the current stop carries its room rungs") — the reading index, which is already a D-B34 cause — never under head-in-frame; the probe shows them following the flip a step late. (3) Falsifiers: jest `lens-ladder.test.tsx` — with `headInFrame` the value element is present with `visibility: hidden`, and rungs render only for the `aria-current` segment; e2e `lens-rail-budget.spec.ts` — every segment's `getBoundingClientRect().height` is constant across the 30 settled steps except on steps where `data-reading-index` changed; and the D-B34 chrome-by-cause gate passing with its two causes unchanged. **Pointer (2026-08-31):** D-B52 removes the redistribution channel entirely — natural-height rows, no `flex-grow: extent` — so this row's invariant gets strictly stronger; the spec case keeps its assertion with the sibling-byte-equal addition D-B52 names.`
- `D-B38 · the band's line 1 holds its box while empty — `min-h-[15.4px]` on `[data-lens-line="1"]`, so line 2 sits at one offset at `open` and pinned · 2026-08-30 · ruled by ARCHITECT (W4 close, D-B34 cause gate) · measurement: (`ladder-probe.cjs`, same run): at s0 `[data-lens-open="true"]`, line 1's text is **empty** (the identity and stage render `null` while `open` — `lens-band.tsx:226` — and D-B26's dedupe drops the money because line 2 names it) and its layout box is **0px**; the band is `flex-col justify-center gap-[2px]` in a 56px box, so line 2 sits at **18.5**. At step 4 (y=320, the pin) line 1 fills (`CLIENT USER · PROCUREMENT & ORDERS 3 OF 5 · INSTALL SEP 19`), its box becomes **15.39** (11px × 1.4) and line 2 moves to **26.19** — 7.7 = 15.4 ÷ 2, the centring of one extra line. The sentence did not change; `data-lens-open` did. · what ships (W4 integration lane, `lens-band.tsx:219`): line 1's `<p>` gains **`min-h-[15.4px]`** (11px × `leading-[1.4]`, written as the px literal — the root is 18px so no spacing unit is honest), so the empty line reserves exactly the box its text will fill and line 2's offset is **26.19 at both states**; the sticky band's 56 is untouched. `data-lens-open` is **not** added as a cause: the band is a declared box and the pin is the moment the reader's eye is on line 2 — the mockup's band has both lines at fixed positions from s0, the empty line 1 reserved. Falsifiers: jest `lens-band.test.tsx` — line 1 carries the class with `open` and empty content; e2e `lens-band-height.spec.ts` — `[data-lens-line="2"]`'s `getBoundingClientRect().top − band.top` is equal at s0 and after the pin (both engines, three widths, to the hundredth); and the D-B34 gate.`

- `D-B39 · the −24px at `lens-density:163` is the FF&E readiness skeleton unmounting, not the lens — the invariant takes D-B28's quiet origin, and the reader-facing shift is logged against the pre-existing fan-out · 2026-08-30 · ruled by ORCHESTRATOR (W4-fix pass 2, applying D-B28's own ruled precondition) · measurement: production standalone build `48758d597` on :3000, chromium, 1440×900, `/doc/…d5`. `money`'s `offsetTop` reads **7739 immediately after `settle()` and 7715 after `quiet()` — the whole −24px, with NO SCROLLING BETWEEN THE TWO READS**; from the quiet origin the full round trip is **7715 → 7715**. A whole-subtree before/after diff of the paper (every element, height + offsetTop + margins) names exactly one element above `money` that leaves: **`<SectionLoadingLine label="Checking readiness" className="mb-2" />`** (`ffe-section.tsx`, gated on `readinessQuery.isLoading`), measured live at `rectH 17.25px`, `margin-top 4.5px` (`my-1` at this route's 18px root), `margin-bottom 9px` collapsing into the next sibling's `18px`, `font-size 11.5px`. `#ffe-region-body-…` → `#project-ffe` → `#doc-section-project` → `main` each lose exactly **−24.000px**. **FF&E reads `data-density="full"` at BOTH ends**, so no promotion is involved, and the 24px is a coincidence of arithmetic — **not** `--doc-region-gap`. Every hypothesis about a density-dependent outer box is excluded twice over: by the measurement (no scroll, no promotion) and by `region-box-signature.ts`, a new jest guard the six region suites now run inside their existing quiet → full cases, comparing the root's sorted class list, its inline `--doc-quiet-reserve` and its mounted `[data-rule-weight]` count across the promotion — **all six green**. · what ships: (1) `lens-density.spec.ts:163` takes its baseline at the settled+QUIET s0, which is **D-B28's own ruled precondition** (`openPaper → settle → scrollTo(0) → settle → quiet`) and the origin `:212` and `lens-cls.spec.ts` already use — D-B28 states the reason in terms of this very query: the readiness fan-out is "the last thing the initial load does" and `__lensSettled()` "is a scroll-velocity settle … it says nothing about the network". Green on dev/chromium AND on the production server. (2) `region-box-signature.ts` + its six call sites, holding the invariant the coordinator suspected even though it is not this defect. (3) **Left open, deliberately:** the shift is real for a reader — on a cold load the FF&E region shrinks 24px when the readiness fan-out resolves, moving everything below it, which is H5's forbidden shift. Fixing it AT THE CAUSE means either reserving the status slot (permanent whitespace inside FF&E on every healthy project — a print change the mockup governs) or making `SectionLoadingLine` occupy no flow height (it would then paint over the first FF&E row while loading). D-B28 already classified this chain as **PRE-EXISTING and not owned** ("`use-project-ffe-ga.ts` is not a touched file, so under the debts rule it is logged, not owned") and priced the batched-RPC fix into I152. **A design-lead ruling is wanted on which of the two forms the loading register should take.** **ARCHITECT verdict (2026-08-30): RATIFIED on the instrument (the quiet origin is D-B28's own precondition); on the reader-facing shift, (c) in a named slot — owner W5-L2, gated on the DESIGN LEAD's countersign of the placement, with (a) as the fallback.** Not (a) outright: `ffe-section.tsx` is a touched file (W2/W3), so the debts rule admits the fix, and a 24px collapse of the FF&E region on every cold load is H5's forbidden shift measured by the program's own instrument — leaving it logged when the hunk is ten lines would be the wrong economy. Not (b): permanent whitespace on every healthy project buys a paint-only exit at the cost of the mockup's print. **(c), placed:** the loading register leaves the flow but does **not** paint over the first row — it prints **inside a box that exists in both states**, the FF&E head's count line (`62 LINES · 5 ROOMS · 1 DAMAGED`), as an inline pulse at the line's end (`SectionLoadingLine` gains an `inline` variant: `display: inline-block`, no `my-1`, `h-[0.85em]`, `align-middle`, the `role="status"`/`aria-live` and the sr-only label kept), so its mount and unmount change no line box; `ffe-section.tsx:1421-1423` moves the mount there. Falsifiers: jest `ffe-section.test.tsx` — the status is a descendant of the head's count line while `readinessQuery.isLoading` and the count line's text is unchanged by it; e2e — D-B29's ungated initial-load CLS on `…d5` drops by the 24px term and the `money` root's `offsetTop` reads the same immediately after `settle()` and after `quiet()` (the very read that found this). The placement is a print inside the head, so the DESIGN LEAD countersigns it before the hunk; if declined, (a) stands — logged in I152 beside the batched-RPC debt, and `lens-density.spec.ts:163` keeps the quiet origin either way.`

- `D-B40 · `lens-band-height:109`'s 55.9754638671875 on the pre-work paper at 390 did not reproduce — the box is a declared 56; the assertion now diagnoses a scaled READ (D-B35, one level deeper) · 2026-08-30 · ruled by ORCHESTRATOR (W4-fix pass 2) — **the ARCHITECT is asked to fold this into D-B35** · measurement: thirty-odd cells, all exactly 56. Both papers × 390/1280/1440 × scrollY 0/400/1200, `getBoundingClientRect().height` = `offsetHeight` = computed `height` = **56**, `box-sizing: border-box`, `transform: none` on the band and on every ancestor to depth 6, `--doc-band-height: 56px` at `:root` and on the band — on the production server `48758d597` (chromium) and on dev (webkit), run standalone AND inside the same unsharded `e2e/document` basket on the same server that recorded the failure (**122 passed / 0 failed**). It is not line 1: with D-B38's `min-h-[15.4px]` REMOVED the box still measures exactly 56, because the height is DECLARED and children cannot change a fixed border-box. And **55.9754638671875 / 56 = 0.999562** — a rendering-scale ratio, not any sum of margins, borders or line boxes. (Separately confirmed and pre-existing: webkit cannot sign in against the production standalone build at all, so :3000 yields no webkit signal — the same finding the PROD run recorded, reproduced twice here.) · what ships: **no CSS change** — there is nothing broken to fix, and inventing one would have been a change with no measurement behind it. Verdict: an **instrument matter**. The band cell now captures `offsetHeight`, the computed `height`, `box-sizing` and every ancestor transform beside the rect, and prints them in the failure message with the sentence "If offsetHeight and css are 56 and only rect is not, the BOX is right and the READ is scaled (D-B35), not a layout break." `toBe(56)` stands with no allowance. If it recurs, the next reader gets the cause for free instead of a bare `55.9755 !== 56`; the most likely source is a residual page/rendering scale left in the shared browser context by an earlier spec in a 122-test unsharded basket. **ARCHITECT verdict (2026-08-30): RATIFIED, folded into D-B35.** A declared border-box of 56 cannot read 55.9755 from layout; `0.999562` is a scale, and the diagnostic print (rect beside `offsetHeight`, computed `height`, `box-sizing`, ancestor transforms) is the right instrument — `toBe(56)` stands with no allowance. One pointer for the next recurrence: the `authenticatedPage` fixture shares a context across a worker's tests (`e2e/fixtures/auth.ts:83`), so a residual viewport/scale from an earlier spec in the 122-test basket is the first suspect; if it recurs, run `lens-band-height.spec.ts` in a fresh context before touching anything else.`
- `D-B41 · the W6 ship-bar rehearsal runs the production standalone behind local TLS on `https://localhost:3443` — chromium AND webkit both gate on that server; the CSP directive is never stripped · 2026-08-30 · ruled by ARCHITECT (W4 close item 3, `build/triage/webkit-signin.md`) · measurement: the standalone build sends `Content-Security-Policy: … upgrade-insecure-requests` (`next.config.js:138-140`, `!isDevelopment`, `isDevelopment = NODE_ENV === 'development'`, `:11`); WebKit applies the directive to `localhost` (Chromium exempts it as potentially trustworthy), rewrites all 41 asset requests to `https://localhost:3000/…`, every one fails `A TLS error caused the secure connection to fail`, nothing hydrates, `PortalLogin`'s buttons never mount — so webkit against `http://localhost:3000` yields no signal at all, on the sign-in page before any lens spec runs. A second, independent reason the http rehearsal can never sign in on WebKit: `middleware.ts:96` forwards the session cookie's `secure` flag, and WebKit refuses `Secure` cookies over `http://localhost` where Chromium admits them. `app.patina.cloud` is real HTTPS and unaffected (both engines load and would sign in). On this machine `mkcert` 1.4.4 and `caddy` are installed (`/opt/homebrew/bin`), and `mkcert -CAROOT` is empty — the local CA has never been installed. · what ships: **the directive stays** — stripping it (or running the standalone with `NODE_ENV=development`) would rehearse a build that is not the one that ships, and WebKit-on-localhost is a property of the rehearsal, not of production. The rehearsal server becomes HTTPS: (1) **one-time, Kody runs** (it writes the login keychain — not an agent action): `mkcert -install`, then `mkcert -cert-file ~/.patina/tls/localhost.pem -key-file ~/.patina/tls/localhost-key.pem localhost 127.0.0.1 ::1` — the key lives outside the repo, never under `artifacts/`. (2) The standalone starts exactly as the PROD run started it (`build/e2e-baseline.md` "W4-int PROD run": build, then `PORT=3000 HOSTNAME=127.0.0.1 node .next/standalone/apps/designer-portal/server.js` with the prod-shaped env). (3) A TLS front on :3443: `build/tls/Caddyfile` (committed; no secrets) — `localhost:3443 { tls /Users/kody/.patina/tls/localhost.pem /Users/kody/.patina/tls/localhost-key.pem\n reverse_proxy 127.0.0.1:3000 }` — run with `caddy run --config build/tls/Caddyfile --adapter caddyfile` (Caddy forwards `Host` and sets `X-Forwarded-Proto: https` by default; the standalone needs neither). (4) `playwright.config.ts` gains one seam (W6 integration lane): `baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'`, `ignoreHTTPSErrors: Boolean(process.env.PLAYWRIGHT_BASE_URL?.startsWith('https:'))` (belt and braces beside the mkcert trust — WebKit and Chromium consult different stores on macOS), and `webServer` omitted when `PLAYWRIGHT_BASE_URL` is set. (5) Preflight in the runbook, pasted not paraphrased: `curl -skI https://localhost:3443/auth/signin | grep -i content-security-policy` **shows `upgrade-insecure-requests`** (proof the rehearsed build carries the production header) and `curl -sI http://localhost:3000/auth/signin | head -1` shows the standalone behind it. (6) **The gate:** `PLAYWRIGHT_BASE_URL=https://localhost:3443 pnpm exec playwright test e2e/document --project=chromium --project=webkit` — **both engines on that server are the W6 ship bar**; the webkit sign-in fixture passing is the first assertion (it is the exact failure this ruling exists for), then the full `e2e/document` basket per the plan's W6 line (`lens-density` + find-in-page on both engines). Dev-server runs stay the lane-level gates; Firefox stays skipped with its stated reason. Never: strip the directive, `--disable-web-security`/`--ignore-certificate-errors` flags on the browsers, or `NODE_ENV=development` on the standalone. Recorded in `build/30-deploy-runbook.md` ("Rehearsal: the ship-bar server") and `technical-design.md` §6. **W6 prep note:** the seam ships as `apps/designer-portal/playwright.ship-bar.config.ts` — a derived config, not an edit to `playwright.config.ts` — because the base config carries the local demo JWT and the pre-commit secret scan rejects any edit to it. **W6 audit close (`975fdf6b7`):** the Playwright seam shipped as a **separate config**, `playwright.ship-bar.config.ts` (spreads `playwright.config.ts`, `baseURL: process.env.PLAYWRIGHT_BASE_URL ?? base`, `ignoreHTTPSErrors` when `https:`, `webServer: undefined`); `playwright.config.ts:48` is untouched — ratified as the better form (the dev basket's config carries no ship-bar branch). Invocation becomes `PLAYWRIGHT_BASE_URL=https://localhost:3443 npx playwright test --config playwright.ship-bar.config.ts e2e/document --project=chromium --project=webkit`; the runbook section is corrected. **The TLS webkit run is OWED**: `e2e-baseline.md:1750` shows `--list` only, and `e2e-run-w6-prod-1.log` (149 passed / 3 failed / 5 skipped) carries zero `[webkit]` lines — the W6 ship bar is not yet met on webkit.`
- `D-B42 · the quiet-reserve token vocabulary as shipped: `--doc-quiet-reserve-min` (68px) and `--doc-quiet-reserve-exc` (112px) declared once at `:root`; each region root sets `--doc-quiet-reserve: var(--doc-quiet-reserve-min|-exc)` inline; `--doc-quiet-reserve` itself is never declared at `:root` · 2026-08-30 · ruled by ARCHITECT (W6 audit, recording W4-L4's form) · measurement: on `document-lens/w5` @ `926c151ae` `globals.css` declares `--doc-quiet-reserve-min` ×1 (`:223`), `--doc-quiet-reserve-exc` ×1 (`:224`), `--doc-quiet-reserve` ×0; the rule `[data-index-region] { min-block-size: var(--doc-quiet-reserve, 68px) }` (`:1127`) reads the root's inline value; seven roots set it (`care-band.tsx:289`, `previous-work.tsx:83`, `ffe-section.tsx:1274`, `schedule-spine.tsx:1122`, `money-region.tsx:59`, `prework-region.tsx:38`, `project-approval-document.tsx:67`). C-7/§5 said `--doc-quiet-reserve` "declared once, default 68px". · what ships: the shipped form is ratified — two declared floors and one per-root indirection is more truthful than one token with a fallback no rule on a root can reach (D-B33's finding); C-7 and §5 are amended to this vocabulary; the token gates count `-min`/`-exc` once each and `--doc-quiet-reserve` zero times at `:root`.`
- `D-B43 · OD-14's two ladder tiers ship as CSS classes on one mount, not as a `tier` prop from `useMediaMatch` · 2026-08-30 · ruled by ARCHITECT (W6 audit, recording W2-L1's form) · measurement: `spine/lens-ladder.tsx:16-17` "Both desktop tiers mount this, once. Nothing here sniffs the viewport: the two tiers differ by class — the narrow measure hides the room rungs"; the floors carry both values (`[--seg-floor:var(--seg-floor-narrow)] min-[1440px]:[--seg-floor:var(--seg-floor-full)]`, `:375-380`) and the segment model carries `value` + `narrowValue` (`lens-ladder-derivation.ts:49-65`), so the print per tier is exactly OD-14's (`36 LINES · 1 DAMAGED AUG 26` / `36 LINES · 4 ROOMS · 1 DAMAGED`, rungs only at ≥1440, the doors head `min-h-[34px]` `:478`) with no JS media query. · what ships: the shipped form is ratified — one mount whose tier is the stylesheet's is strictly more robust than a prop read from `matchMedia` (WebKit's 9px scrollbar at a 1440 viewport is the known trap); OD-14's text is amended to "tier by class"; `LadderSegment.narrowValue`/`narrowFloorPx` join C-3.`
- `D-B44 · the Margin sheet ships without the mockup's `CAPTURE A NOTE` lead act and `NOTE · PHOTO · VOICE` capture row for I152 (W5-R4) — on a measurement corrected here: the web already has a text-only margin-note composer, reachable only where the desktop rail prints · 2026-08-30 · ruled by DESIGN LEAD (W5-R4, `reconciliation.md` ~:327: omit, option (c)); measurement corrected and follow-up priced by ARCHITECT · measurement: the mockup's `#sheet-margin-390` (`role="dialog" aria-label="The margin"`) prints `CAPTURE A NOTE` as the lead act, a prose line, `IN THE MARGIN 7`, and a `.margin-capture` row `NOTE · PHOTO · VOICE` — Patina Field's capture register ("The Visit": note, photo, voice into the margin) drawn into the web sheet. W5-R4 cites "nothing in `apps/designer-portal` or `packages/supabase` inserts into `margin_notes`". **That is not what the tree says**: `hooks/use-margin-notes.ts:30-38` `useCreateMarginNote()` → `.from('margin_notes').insert({ project_id, body, anchor_kind: 'line' | 'section' | 'letterhead', anchor_id })`, called by `margin-rail.tsx:493` (the rail's note capture — "R14: ≤5 seconds — one tap, type, save", `<textarea>` `:674`, `createNote.mutate` `:510`, `anchorKind: noteAnchorLine ? 'line' : 'letterhead'` `:515`), `discovery/discovery-margin.tsx:23` and `discovery/call-plan.tsx:67`. The composer is text-only and lives in the desktop margin rail, which prints ≥1180 (`page.tsx:2928`; `margin-rail.tsx:301` `min-[1180px]:block`) — so the true statement is **"the web has a text-only note composer at ≥1180 and no capture path of any kind below it; photo and voice have no web path at any width"**. The lead's conclusion survives the correction on the print side — printing `NOTE` beside a `PHOTO · VOICE` the web cannot honour is a promise — but the cost side does not: (a) has a target, and (b) is a re-hosting of `useCreateMarginNote`, not "an organ". · what ships: **for I152, the sheet as W5-R1 rules it** — head, `THE WHOLE JOB · N` / `BESIDE <stop> · N`, rows, `CLOSE`; no capture row; the W5 fix lane leaves a one-line comment in `mobile-sheets.tsx`'s `'margin'` branch naming W5-R4 where the lead act would stand. What the reader loses below 1180: nothing she had — the web margin was never writable there; on a phone she files through Patina Field and the note appears in this sheet as in the rail. **Owed follow-up, priced (post-I152 unless the DESIGN LEAD re-rules on the corrected fact):** `CAPTURE A NOTE` as the sheet's lead act opening a `DocSheet` note form that calls the existing `useCreateMarginNote` (body + due date as the rail's form; anchor = the current stop's region as `anchor_kind: 'section'`, `THE WHOLE JOB` → `'letterhead'`), the prose line printing with it; one component + the rail's hook, a fix-lane-sized item; `PHOTO · VOICE` stay the Field app's and are not printed until the web has a capture path worth printing. The re-ruling is the lead's: the premise she ruled on was wrong, the print judgement may still be right. **Amendment (2026-08-30, ARCHITECT, W5 fix lane `625e61f74` finding 1) — ruling (a): every sheet note ships `anchor_kind: 'letterhead'`, `anchor_id: null` (the PAYLOAD is the architect's), and the anchor line prints `ABOUT THE WHOLE JOB` always (the PRINT is the DESIGN LEAD's, W5-R6 `reconciliation.md` ~:358; W5-R6's `'section'` kind is superseded on the payload only — a `'section'` kind with a null id claims an anchor the row cannot keep — and the two rulings agree on the print).** Measurement: `margin_notes.anchor_id` is `uuid` (`00196_per_item_claims_and_margin_notes.sql:33`; `anchor_kind` checked in `('line','section','letterhead')`, `:32`), so a stop key is `22P02` — caught by the e2e. The `margin_items` view's note branch (`00282_sms_core.sql:815-838`) passes `n.anchor_kind` / `n.anchor_id` through and has **no column that could carry a stop**; and `use-margin-sheet.ts:10,140` files every `section`- and `letterhead`-anchored item under `THE WHOLE JOB` — so a `'section'` note with a null id lands there after reload regardless, and the lane's `anchor_kind: 'section'` + `anchor_id: null` (`mobile-sheets.tsx:1022-1023`) is a kind that claims an anchor the row does not record: not a 400, but not honest either. (b) is rejected — `ffe` and `money` map to no single row, and a note anchored to a `project_rooms`/`project_phases` uuid would print under the wrong group when the view resolves it. (c) is out of this program's shape — one deploy, portal-only, no migration phase in `deploy-lens.sh`. What ships for I152: the composer writes `anchorKind: 'letterhead'` always (the `anchorStop ? 'section' : 'letterhead'` branch at `:1022` goes), `anchorId: null`; its anchor line prints **`ABOUT THE WHOLE JOB`** at every stop (never `AT <STOP>` — a line that names a stop the row will not keep is the promise D-B44 already refused to print); the note files under `THE WHOLE JOB · N` at save time and after reload, and its `margin-item` sheet prints the same line. The per-stop anchor joins the owed follow-up with its true cost named: one migration adding `margin_notes.anchor_key text` (a `DocumentIndexKey`, checked) plus the view branch carrying it and `use-margin-sheet.ts` grouping `'section'` notes by it — post-I152, the program's first schema change. Falsifiers: jest `mobile-sheets.test.tsx` — saving from the composer at the `ffe` stop calls `useCreateMarginNote` with `{ anchorKind: 'letterhead', anchorId: null }` and the composer prints `ABOUT THE WHOLE JOB`; e2e 390 on `…d5` — capture a note while reading Pieces, reload, open the Margin sheet: the note stands in `THE WHOLE JOB` with the count incremented and no `BESIDE PIECES` row gained.`
- `D-B45 · `MobileMarginChips` is deleted — both branches print at no width after W5, and the pre-W5 truth on `main` is that the chips never printed at ≥980 (the desktop rail owns the margin there) · 2026-08-30 · ruled by ARCHITECT (W5-C7; W5-R1 read against `main`) · measurement: `main`'s `mobile/mobile-margin-chips.tsx` (last touched `1b93def1a`, 2026-08-28) says in its own docstring "Mobile only — the desktop margin rail owns these above 980px" and its root carries `min-[980px]:hidden` (`:89`) — so "at ≥980 nothing changes" in W5-R1 means *still nothing prints*; "line chips STAY ≥980" is a misreading, and there is no ≥980 caller to serve. On `document-lens/w5` @ `926c151ae`: the letterhead branch's only call site (`page.tsx:2142`) is deleted (D-B30) and its root still carries `min-[980px]:hidden` (`:87`); the line branch is mounted per FF&E line at `ffe-section.tsx:519`, returns `null` below 980 (`useBelow980`, `:36,:82`) and is hidden ≥980 (`:87`) — it prints at **no width** while still running `useMarginItems` + `useCoordinationItems` + `classifyMarginItems` once per line (62 lines on `…d5`) at 390. `useLetterheadMargin` (`hooks/use-margin-sheet.ts`) has no product caller but the dead component (`mobile-shell.tsx:70` and `mobile-bar.tsx:131` name it in comments only). `handoffs-in-margin-contract.test.ts:96-105` ("counts and lists them on the mobile surfaces too") reads `chips` for `useLetterheadMargin` — a dead file — and `marginSheetHook` for `gates.length`, which `count: allItems.length + gates.length` satisfies with no listing anywhere; the listing (`gates.map`) lives in `mobile-sheets.tsx`'s `'margin'` branch and no source-text contract asserts it. · what ships (W5 fix lane): **delete** `mobile/mobile-margin-chips.tsx` (both branches), `mobile-margin-chips.test.tsx`, the `ffe-section.tsx:42` import and `:519` mount, and `useLetterheadMargin` from `use-margin-sheet.ts` (it has no other caller; `useMarginSheet` keeps the letterhead/section derivation it needs); the two comments naming it are rewritten to name `useMarginSheet`. **Re-point the contract**: `handoffs-in-margin-contract.test.ts` drops the `chips` read and asserts the two halves where they live — counted: `marginSheetHook` contains `useHandoffGates` and `gates.length`; listed: `spine` (`mobile-sheets.tsx`) contains `gates.map` — plus `expect(existsSync(join(SRC, 'components/document/mobile/mobile-margin-chips.tsx'))).toBe(false)` so the deletion is the contract. D-B30's "the `'line'` branch stays for `ffe-section.tsx:513`" is superseded by this row. Falsifiers: `git grep -n MobileMarginChips -- apps/designer-portal/src` → 0; the contract suite green; e2e `[data-mobile-margin-chips]` count 0 at 390 and at 1280 on `…d5` (the four existing e2e references assert absence and stay). Not the alternative (drop `min-[980px]:hidden` and print line chips ≥980): that would print every line-anchored item twice at desktop widths — once in the rail, once under the line — a print the mockup's 1440/1280 frames do not carry.`
- `D-B46 · first-paint promotion waits for the paper to RESOLVE — no query fetching and `scrollHeight` stable for three frames (3000ms fallback); before that, discovery promotes nothing; presses still promote · 2026-08-30 · ruled by ARCHITECT (DESIGN LEAD's W4 review defect 1, high; amends D-B15/D-B16) · measurement (the lead's, `build/w4-review-design.md` §1, cold load of `…d5`, fresh context, cleared storage): the five first-commit roots mount 400–1000ms in while their bodies are still loading — at 1000ms `schedule` is 89px tall and FF&E 282px against 923 / 5780 settled — so `money` and `record` sit inside `innerHeight + 240` **at mount**; `use-lens-density.ts:266-268` `withinLookahead` measures that honest geometry against a dishonest paper, D-B15's layout-effect pass promotes them, and one direction means they never return: settled, `record` is `full` 9,033px below the frame and `money` at 7,715 — the exact case L-4 exists for; the same at 390. `care` alone stays quiet because its root mounts later into the full-height paper. There is no business-logic default (`use-region-fold.ts:178-184` is `positionDensity ?? 'quiet'`); the lens simply measured too early. Option (c) alone (promote only in-frame roots at discovery, lookahead only for scroll crossings) does not cure it: at the skeleton heights `money` is inside `innerHeight` itself. · what ships (`hooks/use-lens-density.ts`, `lib/document/lens-constants.ts`; fix lane): **(1) a resolution gate.** The hook takes `useIsFetching()` (TanStack) into the effect by ref and watches `paper.scrollHeight` per rAF; the paper is **resolved** when `isFetching === 0` **and** `scrollHeight` has been unchanged for `LENS_RESOLVE_STABLE_FRAMES = 3` consecutive rAFs — or, if that never comes (a query retrying, a poller), at `LENS_RESOLVE_MAX_MS = 3000` after mount against whatever is laid out (stated, so the lens never hangs quiet). Resolution is written once as `data-lens-resolved="true"` on `[data-document-shell]` (imperative; joins §5) and cleared with the paper. **(2) before resolution:** `discover()` observes and orders roots and promotes **nothing**; `onIntersect` crossings still buffer into `pending` and `commitPending()` runs them only after resolution — but a reader scrolling during the load is acting, so `markPassed()` and the settle machinery run as today, and `forceFullThrough` (a press) promotes immediately as D-B18 rules. The first screen prints quiet heads with their loading registers (W5-R3's inline pulse) — not blank paper, and not a wrong permanent state. **(3) at resolution:** one pass in that same rAF, exactly D-B15(c)'s: every root with `getBoundingClientRect().top <= innerHeight + LENS_LOOKAHEAD_PX` promotes synchronously before the next paint, then `commitPending()` drains what scrolling buffered; late roots found by the `MutationObserver` after resolution promote at discovery as D-B16 says. On a **warm** load (cache primed, paper laid out at first commit) `isFetching` is already 0 and three stable frames pass in ≈50ms, so D-B15's no-flash intent holds to within three frames; jest pins it. **Falsifiers — jest `use-lens-density.test.tsx`:** (a) roots mounted into a 0-height / growing paper (`scrollHeight` changing each frame, or `isFetching > 0`) are promoted **nowhere**, not even in frame; (b) once `isFetching` returns 0 and `scrollHeight` holds for 3 rAFs the pass promotes exactly the roots with `top ≤ innerHeight + 240` and writes `data-lens-resolved`; (c) with `isFetching` stuck > 0 the 3000ms fallback runs the same pass; (d) a deep landing after resolution promotes in-frame + lookahead roots (D-B15 kept); (e) `forceFullThrough` before resolution promotes its root and everything above it. **e2e `lens-density.spec.ts`, new cold-load case** (fresh context, `context.clearCookies()` + cleared storage, no scroll, sampled at `settle` + `quiet`): at 1440 `data-density="full"` on **`approvals` (top 326) and `schedule` (964 ≤ 1140) only**, `ffe` (1910) / `money` (7715) / `care` (8817) / `record` (9033) `quiet`; at 390 **`approvals` (546) only**, `schedule` (1392 > 1084) quiet — the lead's acceptance map verbatim; D-B16's invariants (i)–(iii) hold at that settle; D-B31's fling census still reads blank = 0; the warm second navigation shows `approvals` full on the first sampled frame after hydration. **Countersign on W4 fix-3 (`document-lens/w4-fix3` @ `7500ca445`, 2026-08-30, ARCHITECT) — the cold-load map is the lead's acceptance exactly; three amendments on in-browser evidence, each ratified in place:** **(1) RATIFIED — a third resolution condition.** `isFetching === 0` has lulls between dependent readiness waves while the paper is still a 2,163px skeleton, and three stable frames fit inside a lull; a body still printing its loading register has no honest height. Resolved ⇔ `fetchingRef.current === 0` ∧ `paper.querySelector('[aria-busy="true"], .animate-pulse') === null` ∧ `scrollHeight > 0` held for 3 rAFs (`use-lens-density.ts:99` `LOADING_SELECTOR`, `:428-437`), or the deadline. One wording amendment: the selector is a **contract on the loading registers**, not a heuristic — `SectionLoadingLine` (block and W5-R3 inline) and every region skeleton must carry `aria-busy="true"` on the element that leaves the flow when data lands (they carry `.animate-pulse` today; `aria-busy` is the semantic one and survives a class rename), asserted by a jest case that renders each loading register and matches it against `LOADING_SELECTOR`. **(2) RATIFIED — the cascade IS the mechanism.** `paper.scrollHeight` at resolution is circular: every root stands at its quiet reserve, all six sit inside `innerHeight + 240` of a 2,163px paper, and a single-measurement pass promoted all six. The resolution pass now walks `ordered` **one root at a time**, `flushSync(() => promote(root))` after each so the body's real height exists before the next root is measured, and stops at the first root past the line (`:407-412`) — approvals → schedule (964, at the line) → ffe (1,910, past it): the lead's map **derived** rather than asserted, which is the right shape (a different seed gets a different, equally honest map). `flushSync` inside a rAF callback is admissible — React forbids it only during render and inside lifecycle/effect bodies, and both callers are outside them: `sampleResolution` runs from `requestAnimationFrame` (`:443,:446`) and the deadline from `setTimeout` (`:651`); the layout effect only *schedules* the sampler. Cost = one synchronous commit per promoted root, N ≤ 2–3 in practice and bounded by the stop count (six); all inside one rAF, so D-B15(c)'s "before the next paint" holds. Two boundary rules: the walk breaks at the first root past the line and never resumes in the same pass (a root below a not-yet-promoted root is never measured against a paper that will grow under it), and `frozen` (D-B19) does not gate the resolution pass — it gates `commitPending` only, because a reader editing on a skeleton paper is not what freeze protects. **(3) RATIFIED — position is measured at the write.** `IntersectionObserver` entries computed against the skeleton land after the paper has grown; `commitPending` re-checks `withinLookahead(root)` per root and returns a root that no longer stands at the line to waiting (`:331-345`) — still observed, so the observer speaks again when she reaches it; the resolution drain goes through the same gate. This is D-B16's "promotion is a fact about position" applied at the only honest moment. **Jest required beyond `use-lens-density.test.tsx:437-581`** (which cover growing paper, fetching, the pass, the deadline, warm ≤3 frames, press-before-resolve, deep landing): (a) the **lull** — `isFetching` 0 with `.animate-pulse`/`aria-busy` present holds resolution; (b) the **cascade** — six roots at reserve inside the lookahead, promote grows each body in the mock, the pass promotes exactly the prefix that stays at the line and stops; (c) the **re-check** — a pending entry whose root has moved past the line is not promoted at the drain. Also ratified as recorded: `MAX_RESOLVE_FRAMES` (`:112`) and the `setTimeout` deadline are two clocks for one deadline — keep the timer, let the frame count go, so a background tab (rAF throttled) still resolves at 3000ms wall-clock.` **Closed at W6 integration (2026-08-30, `document-lens/integration`) — the gate now binds the REACT writers too.** The DESIGN LEAD's fix-3 countersign (`build/w4-review-design.md`, row "what the first screen prints before resolution") measured `record` printing `full` for ~500ms at 1440 and `quiet` after resolution: `data-density` moved twice, and the second move was backwards, which is the one thing D-B16 says the attribute never does. Cause: the imperative writer already obeyed the gate (`promote()` is unreachable before `resolved`), but a root can also print `full` from its own data — `previous-work.tsx`'s zero-record form states `full` (W4-C16) and `count` is 0 while the read is still out. What ships: `use-lens-density.ts` publishes the resolution as a second module store (`useLensResolved()`, `useSyncExternalStore`; `true` with no lens attached, `false` from the hook's layout effect until `markResolved`, re-closed when the paper is replaced, re-opened at teardown; `__setResolvedForTest` is the suite seam beside `__setDensityForTest`), `markResolved` flushes the un-hold **before** the cascade measures a single position, and two React writers consult it: `use-region-fold.ts` (a stop with `!forceOpen` holds `quiet` until resolve, so a stop the designer opened on an earlier visit no longer prints `full` against a skeleton) and `previous-work.tsx` (the count-0 root holds `quiet`). `forceOpen` is exempt for the reason D-B27 ruled it — those postures print a whole paragraph and no `RegionHead`, so they have no quiet form to hold — and `forceFullThrough` (a press) is exempt as D-B18 rules. Falsifiers: `use-lens-density.test.tsx` "publishes the resolution to the React writers, and holds them at quiet until it does" (a register held 400ms: gate false, `data-lens-resolved` absent, **zero** `[data-index-region][data-density="full"]`) and "closes the gate again when the paper is replaced, and opens it when the lens leaves"; `previous-work.test.tsx` "holds the count-0 root at quiet until the paper resolves — the attribute moves once" and "states full at count 0 once the paper has resolved".
- `D-B47 · the 390 paper inset reads the bar's own height — the bar publishes `--doc-mobile-bar-height` (min 72px) on `html`; the print is unchanged · 2026-08-30 · ruled by ARCHITECT (DESIGN LEAD's W4 review defect 2, low) · measurement (the lead's, §7): the bar's box measures **93px** at 390 (three-line left zone `In this document` / household / `AT <STOP>` + the primary act, whose label wraps to two lines by design — `mobile-bar.tsx:221-224` `whitespace-normal`), while the paper's persistent edge is `max(72px, 60px + env(safe-area-inset-bottom))` → 72 on a phone with no inset (`globals.css:236` `--doc-shell-bottom-inset` on `.document-route-shell`; `:1094` `scroll-padding-bottom` on `html:has([data-document-shell])`), so the paper's last ~21px and a landed foot control can sit under the bar. Not "fit 72": the three lines at 11/14/11 would fit, but the act's two-line wrap is a ruled print (OD-11 / D-B26's act rule) and squeezing it clips it. · what ships (`mobile/mobile-bar.tsx`, `globals.css`; fix lane): the bar attaches a `ResizeObserver` to its root and writes `document.documentElement.style.setProperty('--doc-mobile-bar-height', `${Math.max(72, Math.round(height))}px`)` on mount and on every resize, removing the property on unmount — written on `html` because the shell's token cannot be read from `html` (`globals.css:1092`); both rules become `max(var(--doc-mobile-bar-height, 72px), calc(60px + env(safe-area-inset-bottom)))` (`.document-route-shell { --doc-shell-bottom-inset }` and `html:has([data-document-shell]) { scroll-padding-bottom }`), so `--doc-shell-floating-bottom` and the floating acts follow for free. `--doc-mobile-bar-height` joins §5's token list as the one token written by a component, and `contrast.test.ts` ignores it (a length). **Falsifiers:** jest `mobile-bar.test.tsx` — the var is written on mount with `max(72, height)` and removed on unmount (ResizeObserver mocked); e2e at 390×844 on `…d5` — `getComputedStyle(document.documentElement).scrollPaddingBottom` equals the bar's `getBoundingClientRect().height` (≥ 72), and with the paper scrolled to its end the last `[data-index-region]`'s bottom is at or above `innerHeight − barHeight`; at 1440 the property is absent and the inset is 60 + safe-area as today.`
- `D-B48 · the paper's name wraps and never clips — a read-only `<h1>` prints it; the `<input>` appears only in edit mode, in the same box; two-line gates at 390 are 300 / 470 gross, chosen by the measured line count · 2026-08-30 · ruled by ARCHITECT (mechanism for DESIGN LEAD W5-R5 §1 / N1; amends D-B26's title clause and W3-R7's 390 cases) · measurement: `letterhead-vitals.tsx:453-516` `LetterheadTitle` — an `<h1 class="flex items-baseline …">` wrapping an `<input type="text" aria-label="Project title">` (`:487-505`, blur/`Enter` → `commit()` → `useVitalSave(projectId).save({ name })`, `Escape` blurs, blank never saves) beside a `SaveDot`. An input cannot wrap, so at 390 (327px measure, 32px Playfair ≈ 11 characters) `…d5`'s `Aspen Loft — the long paper` prints **`Aspen Loft — the long p`** and W3-R6/W3-R7's "one-line title" at 390 was the input clipping, not a name that fit — the spec's own comment (`lens-band-height.spec.ts:307-310`) chose the input *because* it cannot wrap and measured `scrollWidth === clientWidth` as the witness, which an overflowed input satisfies. The mockup's `#frame-390 .lh-title` is `font-size: 32px` with no `text-wrap` rule: plain word wrapping, two lines for the specimen. Arithmetic: a second 32px line at `leading-[1.08]` adds 34.56px; W3-R7's measured 390 letterhead 255.17 (chromium) / 262.25 (webkit) → **289.7 / 296.8** for a two-line name — gate **≤ 300**; first head 423.17 / 430.25 net of the chips (gone after D-B45, so asserted gross) → **457.7 / 464.8** — gate **≤ 470**; the engine allowance is the same +5 as W3-R7's. · what ships (`letterhead-vitals.tsx`, `lens-band-height.spec.ts`, the seed; W5 fix lane item 11): **(1) the read-only name.** At rest `LetterheadTitle` renders `<h1>` with the name as text — `font-heading text-[32px] min-[1180px]:text-[40px] font-medium leading-[1.08] tracking-[-0.015em] text-[var(--text-primary)] min-w-0 break-words` — wrapping at word boundaries only (no `overflow-wrap: anywhere`, no `text-wrap: balance`: the em-dash form breaks after the dash as the mockup does, and balancing would move that break); the `SaveDot` stays an inline sibling after the text. **(2) the edit affordance.** The name is a press target: the `<h1>` contains a `<button type="button" aria-label="Rename the project" data-letterhead-title-edit>` whose content is the name text (so the visible name IS the control — no second glyph), `cursor: text`, the same type classes, `text-left`. Pressing it (or `Enter`/`Space`) sets `editing`, and the `<h1>` swaps the button for the existing `<input>` **in place, same box** — the input takes the same type classes plus `w-full`, is `autoFocus`ed with the caret at the end, and inherits the existing handlers unchanged: `Enter` → `commit()` + leave edit; `Escape` → restore `serverTitle` + leave edit; blur → `commit()` + leave edit; `disabled` while `saving`. Focus returns to the button after the swap back (the `useRef` on the button). `aria`: the button's name is `Rename the project`, the input keeps `aria-label="Project title"`; the `<h1>` never changes element type so the heading outline is stable. Read-only viewers (a role without `save`) get the plain text, no button. **(3) the same rect.** Both forms share one class set and `leading-[1.08]`, so the rect does not change on the swap for a one-line name; for a wrapped name the input is a single line by nature — the letterhead may shrink by one line while editing and grow back on commit; that is the editor's own act and not a settled-scroll shift (D-B34's paper gate measures scrolls, not presses). **(4) the gates — chosen by measured line count, not per seed.** The spec keeps ONE 390 letterhead case and ONE 390 first-head case; each reads the `<h1>`'s line count as `round(h1.getBoundingClientRect().height / (32 × 1.08))` and picks the gate: 1 line → 265 / 435, 2 lines → 300 / 470 (constants `LETTERHEAD_MAX_390 = { 1: 265, 2: 300 }`, `FIRST_HEAD_MAX_390 = { 1: 435, 2: 470 }`), asserting the line count it measured in the message and failing on 3+ lines (a name that long is a seed defect, not a budget). Per-name-length cases would double every 390 row and still assert the wrong gate when a seed's name changes; reading the count keeps one number per line count, W3-R7's rule. The 1440 case is unchanged (40px, one line on both seeds; `scrollWidth ≤ clientWidth` on the `<h1>`). **Falsifiers — jest `letterhead-vitals.test.tsx`:** read-only prints the full name as `<h1>` text (no `<input>` in the DOM) and the button's accessible name is `Rename the project`; pressing swaps in the input with `value === serverTitle`, focus on it; `Enter` with a changed value calls `save({ name })` once and swaps back with focus on the button; `Escape` restores and saves nothing; blank never saves; while `saving` the input is disabled. **e2e at 390×844:** on `…d5` the `<h1>` text equals `Aspen Loft — the long paper`, `scrollWidth ≤ clientWidth`, measured line count 2, letterhead ≤ 300, first `[data-region-head]` ≤ 470 gross; on a seeded **one-line** paper — `…d4` is named by the lane but is **not in `scripts/the-document-lens-seed.sql`** (only `…d5` and `…d6` are), so the lane seeds `b0000000-0000-0000-0000-0000000000d4` with a one-line name (≤ 11 characters at 390, e.g. `Aspen Loft`) and the same shape as `…d5` — line count 1, letterhead ≤ 265, first head ≤ 435; both engines, engine allowance stated beside each. D-B26's clause "the input gets the whole measure … so it never clips on any seeded title" is superseded: the name wraps, and nothing about it is an `<input>` at rest. **W6 audit close (`975fdf6b7`):** landed — `letterhead-vitals.tsx:565-566` `aria-label="Rename the project" data-letterhead-title-edit`, `break-words` (`:506`); the one-line paper is **`…d7`** (`lens-fixtures.ts:31` `ONE_LINE_PAPER_ID`, seeded in `the-document-lens-seed.sql`), not the `…d4` this row asked for — the seeded id is the ruling.`
- `D-B49 · the lens changes what prints, not what loads — a region's data hooks live at its root (mounted at every density), never in the body a promotion mounts; `lens-contrast.spec.ts:183` stands · 2026-08-30 · ruled by ARCHITECT (W6 audit close, `e2e-run-w6-prod-1.log:244-251`) · measurement: on the production build (`975fdf6b7`, chromium, `…d5`, settled + quiet origin) the 30-step scroll issued **three** `rest/v1/project_time_entries?select=duration_minutes,phase_key` requests. The query is `useSectionTasks` (`hooks/use-section-work.ts:232`, TanStack default `staleTime: 0`), called by `schedule/schedule-spine.tsx:191`, `work-block.tsx:86`, `coordination/coordination-work.tsx:51` and `margin-rail.tsx:489`. At the quiet origin the schedule body is unmounted; the scroll promotes `schedule` (L-4), the body mounts, its hook mounts a new observer on a query that is stale by default, and TanStack refetches on mount — three bodies, three requests. Nothing scroll-driven fetched; the promotion did, through the body it mounted. D-B28's allowlist is right to refuse it: the sentence is "the lens promotes from the DOM, never fetches", and a body that fetches on mount makes every promotion a fetch — on a reader's real scroll, not only in the test window. · what ships (W6 phase C wiring): **the rule** — every query a region body needs is issued by the region **root** (or above it: the page, the section), which is mounted at every density; the body reads it from props or from the cache with no new observer that can refetch. For the four callers: `schedule-spine.tsx:191` moves its `useSectionTasks` above the body boundary (the region root already renders the head at `quiet`); `work-block.tsx:86` and `coordination-work.tsx:51` take `tasks` from the section that mounts them (the section's root calls the hook once); `margin-rail.tsx:489` is root-level already. A `staleTime` on the hook is **not** the fix — it would make the sentence true for fifteen seconds and false afterwards — though `useSectionTasks` may gain `staleTime: 15_000` as belt beside the hoist, matching `useProjectFFEItems`. Gate: `lens-contrast.spec.ts:183` green on the production build with **no allowlist change**; and a grep-shaped falsifier for the rule — every `use*` data hook called inside a `[data-index-region]` body component is enumerated in `build/test-impact.md` with its root-level owner (phase C prints the list; any hook with no root owner is a defect). **Closure line (to be completed by the W6 lane):** hooks hoisted — <!-- W6: list the moved call sites file:line → root owner --> ; body-hook census printed at `test-impact.md` <!-- W6: section/line --> with <!-- N --> body-level data hooks enumerated and 0 without a root owner; `lens-contrast.spec.ts:183` green on the production build @ <!-- sha --> with the D-B28 allowlist unchanged.`
- `D-B50 · `Fold ↑` prints beside the leader on a quiet `ffe` / `schedule` / `money` — L-7's own disclosure control stays reachable at quiet and is not one of the region's acts, so `actsAtQuiet="leader"` is honest · 2026-08-30 · ruled by DESIGN LEAD (W6-R1 §2, `build/w6-walk.md` ~:364, signed deviation); mechanism recorded by ARCHITECT (W4-R1 F3, `build/w4-fix-log.md:78-100`) · measurement: the final walk on `…d5` at 1440 / 1280 / 390 reads **two visible controls** on each quiet stop that folds — the head's one leader (`RegionHead` `actsAtQuiet: 'all' | 'leader'`, entry 0 only at quiet) and `Fold ↑` — where W4-R1 said "one leader"; `care` and `record` print one control, having no fold. The fold is rendered by the head itself, not passed in `actions` (`care-band.tsx:456`, `previous-work.tsx:112`, `ffe-section.tsx:1410`, `schedule-spine.tsx` carry `actsAtQuiet`), and it carries the `aria-controls` W4-C7 requires to name a mounted body. · what ships: **no change** — the deviation is signed. A quiet region prints its head, its count line, one leader **and its fold**: L-7 ("the reader may close a region and it stays closed — `CLOSED BY YOU`") is a voice of the region, not an act of it, so `DocumentActionGroup`'s one-leader guard and `action-visibility.spec.ts` count acts and never the fold; hiding the fold at quiet would take the reader's own control away exactly where D-B27/OD-10 promise it. W4-R1's sentence is read as "one leader among the acts". Falsifiers already standing: `ffe-region-head.test.tsx` (install prints no fold), `use-region-fold.test.tsx` (`CLOSED BY YOU` from the explicit voice), the walk's press-Fold-reload check; the rail-label instrument (§6) does not count the fold (it lives on the paper, not the rail).`
- `D-B51 · the seven-mark arc becomes ONE progress mark — a `StrataMark` filled from the engagement's own fill state, inert, with an accessible name; the arc's jump-tooltips die · 2026-08-31 · ruled by ARCHITECT (mechanics; Kody's live-lens ruling supersedes the arc; W7-R1 owns what prints) · measurement: today `doc-spine.tsx:188-220` prints seven `StrataMark size="xs"` in a wrapping `<ul>` (48px at 1180–1439, four-plus-three), each `fill={fillStateAtSection(s.key)}` (R35's filling staircase), settled/active marks wrapped in a jump button `title="Jump to <label>"`, future/unrecorded inert; `doc-spine.test.tsx:62-81` asserts the list; the marks carry no `data-rail-label`, so the R1 census (`e2e/helpers/lens.ts:249` reads `[data-rail-label]` only) never counted them. The ordinal's source is `TicketPhase { name, position, of }` (`ticket-derivation.ts:149-153`); the page already passes the numeric pair to the band (`page.tsx:2047`) and the formatted `N OF M` string to the spine (`:2477`). · what ships (fix lane; the DESIGN LEAD's W7-R1 picks size and whether the pre-work mark prints at all): **(1) fill source** — the same resolver the ordinal reads: `DocSpineProps` swaps `stageIndex?: string` for `stagePhase?: TicketPhase | null` (the spine formats `N OF M` itself where the phrase still prints it, so mark and ordinal cannot disagree). The mark's fill is **`fillStateAtSection(<the engagement's current section>)`** — the exact triple the active arc mark carried, one call, no new fraction→fill mapping; if W7-R1 prefers a pure phase fraction, the same component takes the staircase mapping `f = position/of → [min(1,3f), clamp(3f−1), clamp(3f−2)]` — mechanics identical either way. **(2) mechanism** — no new SVG, no clipPath: `StrataMark` already renders each chamber as a left-clipped `scaleX(clamp(fill[i]))` over a ghost track with a `transform 0.6s` transition (`strata-mark.tsx:96-127`); the one change the component needs is `motion-reduce:transition-none` on that transition (it predates the reduce register). **(3) pre-work** — `ticketPhase` is null where no phase is placed, and the mark prints **unfilled** (`[0,0,0]`), not absent, so the head's reserved block does not reflow across the pre-work → project transition; whether pre-work prints the mark at all is W7-R1's. **(4) press semantics** — the mark is **inert**: `role="img"` with `aria-label` = `` `${stageWord} · ${position} of ${of}` `` (pre-work: the stage word alone), no button, no tabstop, no tooltip. The ladder is the navigator now; a press on the mark would be a second navigation voice saying less than the ladder says, and the seven per-section destinations have no single honest successor. If W7-R1 rules a press, the only admissible destination is `onToTop` (the letterhead, where the stage story prints in full) — logged here as the one conditional. **(5) tests** — `doc-spine.test.tsx:62-81` (the list of seven, the jump buttons) dies, rewritten: the head prints one `role="img"` whose accessible name carries the stage word and ordinal, and no `<ul>`; the head's `min-h-[126px]`/`min-h-[117px]` (`doc-spine.tsx:148`) are re-measured by the lead and re-gated; `rail-stock.test.ts` and `lens-contrast.spec.ts`'s shadow census are untouched (the mark uses existing tokens, casts no shadow); the R1 label census is unchanged **and asserted**: the mark never carries `data-rail-label` (a glyph is not a label — §6's own rule).`
- `D-B52 · the ladder track drops `flex-grow: extent` — natural-height rows, one flexible gap before the doors (`margin-top: auto`), the floor arithmetic deleted, `LADDER_SEGMENT_MIN_PX` kept as a plain min-height · 2026-08-31 · ruled by ARCHITECT (mechanics; Kody's live-lens ruling supersedes OD-14's extent-proportional segments where they conflict) · measurement: today each segment is a flex item `flexBasis: var(--seg-floor)` / `flexGrow: segment.extent` (`lens-ladder.tsx:397-402`), the track sums its floors into `--track-floor-narrow/-full` (`:158`, `:280-291`) with a pre-work zero-floor special case (`flexBasis: segments.length > 0 ? … : 'auto'` — the empty track once laid out at 0px and clipped OD-2's line), and the doors block sits at `mt-3 shrink-0 border-t pt-3` (`:499`); the floors derive from value-string line counts (`floorFor`, `lens-ladder-derivation.ts:133`, `VALUE_LINE_PX 15.4`, `CHARS_PER_LINE`). D-B37 already made the yield paint-only; the remaining movement channel is the track re-dealing freed height by extent. · what ships (fix lane): **(1) rows** — every segment row is `flex: none` at its natural height with **`min-height: LADDER_SEGMENT_MIN_PX` (36px)** as a plain CSS min-height (the constant stays; the arithmetic goes): delete the per-segment `--seg-floor-*` styles and `flexBasis`/`flexGrow`, delete `floorFor`/`fullFloorFor` and `LadderSegment.floorPx`/`narrowFloorPx` from the derivation (C-3 amended; `extent` leaves the ladder's rendering and leaves the model too unless the fix lane's grep finds another consumer — none is known). **(2) the gap** — the doors block gains `mt-auto` (keeping `border-t pt-3`), and the rows container gains `mb-3` so the 12px separation survives the case where the track scrolls and auto margins collapse to zero; the `--track-floor` machinery on the track container (`:280-291`) deletes whole, `flexBasis: auto` everywhere — which also deletes the pre-work zero-floor special case and the bug it patched. **(3) short viewports** — the track keeps `min-h-0 flex-col overflow-y-auto [scrollbar-width:thin]` exactly as shipped: rows and doors stay whole, the track scrolls itself; **the drawer reserve** — the fix lane measures whether the spine's sticky box can today put the doors under the Studio Drawer at short viewports and, only if it can, adds `max-height: calc(100dvh − var(--doc-shell-bottom-inset, 60px))` to the sticky container (`doc-spine.tsx:123`), never a magic 60. **(4) what the rulings become** — OD-14's floor formula (`max(36, lines × 15.4 + 8)`) and extent-proportional distribution are **superseded** (user ruling); D-B37 gets strictly stronger: with no redistribution channel, a segment's height cannot move on any step whose reading index did not change — `lens-rail-budget.spec.ts:153` keeps its assertion and gains one clause: on an index-change step, **only the segment gaining or losing rungs may differ; every sibling is byte-equal**. D-B43's tiers-by-class survives (the tier now only picks which value string prints, no floor pair). **(5) tests** — `lens-ladder.test.tsx:181/:201/:497` (floors sum, measured floor, asks-for-the-sum) die, rewritten: rows are natural-height with the 36px min, the doors block carries `mt-auto`, the track sets no `--track-floor`, and the pre-work spread prints OD-2's line with no special case; the derivation's twin drops the `floorPx` fields. **W7-C5 note (2026-08-31, ARCHITECT):** the reading-window bracket now spans the union of the `activeKey` row and the `headInFrame` rows (W7-R1 §2, the DESIGN LEAD's spec — ruled intent, no revert), so it moves on steps where the reading index did not change. This touches neither gate by construction: the bracket is a `translateY` transform on the absolutely-positioned gutter overlay (D-B1 — paint, never layout; a transform cannot enter the layout-shift record, so D-B34's cause list is unaffected), and D-B37/this row's sibling-byte-equal clause applies to segment **boxes**, not the overlay. The W7 lane adds the bracket's first falsifier: `data-lens-window`'s `top:height` covers the active row's box at s2.`
- `D-B53 · door icons come from `lucide-react` (already a dependency), imported by name, `aria-hidden` decoration beside the unchanged text labels — no new dependency, no census change · 2026-08-31 · ruled by ARCHITECT (mechanics; the DESIGN LEAD picks the four glyphs in W7-R1) · measurement: `lucide-react ^0.544.0` is a direct dependency of both `apps/designer-portal` (`package.json:66`) and `@patina/design-system` (`:123`), and document components already import it by name (`command-bar.tsx:31` `FolderPlus, LifeBuoy`, `studio-drawer.tsx:28`, `desk-contents.tsx:31`); the standing sheet's `AlertCircle` (OD-6) set the precedent inside the lens. The doors print as text-label buttons/Links carrying `data-rail-label` and `data-ladder-door` (`lens-ladder.tsx:506-530`). · what ships (fix lane): named imports from `'lucide-react'` in `lens-ladder.tsx` (tree-shaken; never a deep `dist/` path), one glyph per door key as the lead picks, rendered **inside the existing button/Link, before the label**, with `aria-hidden="true"`, `size` and `strokeWidth` as the lead's print, and **`stroke="currentColor"` only** — the icon inherits the door's ink (charcoal, clay on hover), so `contrast.test.ts`'s hex parser sees no new pigment, `lens-contrast.spec.ts`'s box-shadow census is untouched, and `rail-stock.test.ts` is untouched. The accessible name is the label text as today (the icon adds nothing to it), the press target and `data-rail-label` element are the same button, so the R1 census count is **unchanged — icons are glyphs**, §6's own exclusion. Falsifiers: jest `lens-ladder.test.tsx` — each door's accessible name equals its label with the icon `aria-hidden`; a grep gate — `git grep -nE 'fill="#|stroke="#' -- apps/designer-portal/src/components/document/spine` → 0 (no hex ink enters the rail through an icon prop).`
- `D-B54 · a cross-project running timer removed the phone's entire bottom chrome — the bar yielded the thumb edge to a time-log offer the strip refused to paint; one provider-published `offerOwnsEdge` boolean now owns the rule for both · 2026-08-31 · ruled by ARCHITECT (mechanism, from `build/w7-triage-mobile-nav.md`; the W7 lane builds it) · measurement: controlled A/B/C on `…d5` at 390×844 over the one variable, the running-timer row (`project_time_entries.duration_minutes IS NULL`): **A** no running timer → bar **present**, strip absent, `[data-mobile-edge-owner]` = `["document-bar"]`; **B** running timer on **another** project (`…d1`) → bar **ABSENT**, strip absent, owners `[]` — Kody's screen: no fixed element at `bottom: 0` taller than 20px anywhere, `--doc-mobile-bar-height` unset, the paper scrolling under nothing; **C** running timer on **this** project → bar present, owners `["document-bar"]`. Healthy-case DOM probe (A): bar mounted, `fixed z-40 bottom: 0`, rect `y 751 h 93`, no containing-block ancestor, nothing painted over it. **The UA is not the variable** — a plain 390 viewport with the desktop UA yields a byte-identical probe, and prod's signin markup is byte-identical (27,980 B) under both UAs. The mechanism: `mobile-bar.tsx:219/:250` yields the edge on any bare `offer`, while `log-strip.tsx:56-64` refuses a cross-project offer — both return null, nothing owns the edge; the offer is armed at doc pick-up by `document-time-provider.tsx:~301` chaining the held timer out with `offerStrip: true`. The drift is the defect's whole shape: `crossProject` was added to the strip alone in `180d50aee` ("suppress unrelated time offers") and the bar never followed. Desktop is unaffected (the Studio Drawer owns that edge). **The 153/0 suite missed it because both layers pin the one value that cannot trigger it**: `mobile-bar.test.tsx:69` mocks `offer: null` for the whole file, and every mobile e2e opens one document with no running timer. **Pre-dates the lens** (`180d50aee` is pre-program) — but the lens made the bar the phone's only navigation (OD-11/OD-16 retired the other doors into it), which is why a suppressed time strip presented as "no navigation at all". · what ships (W7 lane): `document-time-provider.tsx` computes and publishes **one** boolean — `offerOwnsEdge = offer !== null && !(heldProjectId && heldProjectId !== offer.projectId)` — and BOTH consumers read it: the bar renders unless `offerOwnsEdge` (its two bare `offer` reads at `:219/:250` go), the strip paints exactly when `offerOwnsEdge`; the rule lives in one place so the two components cannot drift again (the bar-local derivation in the triage is the fallback shape, not the ruled one). `:207`'s `if (sheet || offer) setMoreOpen(false)` stays. Falsifiers: jest `mobile-bar.test.tsx` gains the cross-project case (provider mocked with a foreign-project offer → the bar renders; a same-project offer → it yields) — the `offer: null` file-wide pin is broken; e2e seeds a running timer on another project (`…d1`, `duration_minutes IS NULL`) and asserts at 390 **exactly one `[data-mobile-edge-owner]` with the cross-project offer standing** — the assertion the triage names as missing from every current run. Gate: `pnpm --filter @patina/designer-portal test` + `quiet-responsive-shell.spec.ts` + `action-visibility.spec.ts`.`
