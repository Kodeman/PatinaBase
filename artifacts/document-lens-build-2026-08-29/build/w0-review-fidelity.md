# W0 · FIDELITY review — Smart Lens build

Reviewer: FIDELITY (read-only; no checkout, no edits). Scope: `document-lens/w0-l1`, `w0-l2`, `w0-l3` against `program-plan.md` (Wave 0), `technical-design.md` (OD-4, OD-9), `reconciliation.md` (Seed requirements, A-02…A-05), `test-impact.md` rows 8/9/10, and the ownership rule.

**Branch-pointer note (read this before the tables).** At review start: `document-lens/w0` = `80af65c32` (L1's own tripwire commit, fast-forwarded onto `w0` — no merge commit). Because of that, `git diff document-lens/w0...document-lens/w0-l1` returns **empty** — the literal command in the brief shows nothing for L1. L1 was instead reviewed via `git diff dab057537..80af65c32` (`dab057537` = `main`'s tip immediately before Wave 0 began, per `program-plan.md`'s "Verified on main@dab057537"). L2 and L3 were reviewed via `git diff 80af65c32..15e581435` and `git diff 80af65c32..91765a1cf` respectively (pinned SHAs, not the moving branch names — an integration lane merged `w0-l2`/`w0-l3` into `w0` partway through this review via proper two-parent `chore(document-lens): merge w0-lN — …` commits; that happened concurrently with this review, not because of it, and doesn't change any finding below).

## Verdict

```
┌──────────────────────────────────────────────────────────────────────┐
│  VERDICT: SHIP-AFTER-FIXES                                           │
│                                                                       │
│  Tripwires (L1) and the e2e-baseline mechanism (L3, root cause) are  │
│  sound. The seed (L2) has one foundational break: the seeded         │
│  "damaged" line will not actually render as damaged once Wave 3      │
│  wires the ladder to real data — seed-verify.sql's own check is a    │
│  false positive. L3 also quarantined two named "itemise" files as    │
│  product drift, contradicting the wave's own quarantine rule.        │
└──────────────────────────────────────────────────────────────────────┘
```

## Severity / confidence counts

| Severity | Count |
|---|---|
| High | 4 |
| Medium | 8 |
| Low | 1 |
| Info / Pass | 15 |

---

## Evidence table — every finding

| id | severity | confidence | file:line | asked for | shipped |
|---|---|---|---|---|---|
| L1-1 | info | 0.95 | (ownership) | L1 touches only tripwire files | `git diff dab057537..80af65c32 --stat`: exactly 5 files — `quiet-responsive-shell.spec.ts`, `package.json`, `page.test.tsx`, `contrast.test.ts`, `stage2-approval-cutover-contract.test.ts`. All within L1's remit. **PASS.** |
| L1-2 | info | 0.95 | `apps/designer-portal/package.json` | OD-4: `browserslist: ["last 2 Chrome versions","last 2 Edge versions","last 2 Safari versions","iOS >= 17","last 2 Firefox versions"]` | Added verbatim, same 5 entries, same order. **PASS.** |
| L1-3 | info | 0.95 | `.../stage2-approval-cutover-contract.test.ts:19,25-27` | OD-9: delete `:19`'s 1500-char regex; widen `:25-27` negative window to `{0,1200}`; `:15-17`/`:21-23`/`:24` survive | `:19` deleted; `{0,300}` → `{0,1200}` on the negative-match regex; the other three assertions byte-identical to `main@dab057537`. **PASS.** |
| L1-4 | info | 0.9 | `.../doc/[id]/page.test.tsx:+1234` | OD-9: rendered mount-order replacement (containment, not `firstElementChild`) | New `it` asserts `activeSection.contains(stageLine)` plus an index comparison over `querySelectorAll('*')` (jsdom has no `:has()` — correctly worked around). **PASS.** |
| L1-5 | low | 0.85 | `.../workflow/section-stage-line.tsx:45` | plan: "`SectionStageLineMount`'s `<section>` ... gains `data-section-stage-line=""` — **the one product line Wave 0 touches**" | `git show dab057537:.../workflow/section-stage-line.tsx` already carries `data-section-stage-line` at line 45, **before Wave 0 started**. L1 correctly touched no product code (0 lines outside `__tests__`/`e2e`/`package.json` in the diff) — the requirement was already true. Not a lane failure; the technical-design's premise ("the one product line Wave 0 touches") is simply inaccurate — there was no product line to touch. |
| L1-6 | info | 0.9 | `.../contrast.test.ts:294-403` | plan: `RAIL_FILES` → glob (`spine*.tsx`, `spine/**`, `margin-rail.tsx`; `lens-band.tsx` scanned against paper grounds), assert ≥5 files, widen offender grep (F70) | `resolveRailFiles()` globs top-level `spine*.tsx` + `components/document/spine/**` + explicit `margin-rail.tsx`; new test asserts `.length >= 5`; `pigmentOffenders()` widened to 3 text forms (Tailwind arbitrary-value class, arbitrary-property class, inline `style={{color:…}}`); forward-looking `lens-band.tsx`-vs-`--doc-paper` scan added, guarded by `existsSync` (currently a no-op, activates in W3). **PASS, exceeds the ask usefully.** |
| L1-7 | medium | 0.85 | `e2e/document/quiet-responsive-shell.spec.ts:+261-329` | plan: "Landing-clearance case ... (landed head within **4px of `--doc-landing-clear`**)" | The shipped test asserts landing within 4px of **`--doc-seam-height`** (today's mechanic), not `--doc-landing-clear`. Confirmed via `grep -n "doc-landing-clear" apps/designer-portal/src/app/globals.css` → **zero matches anywhere in the codebase** — the token is declared only in Wave 3 (`technical-design.md` §OD-4/D-1's `globals.css` work). The plan's own Wave-0 bullet names a token that cannot exist until Wave 3. L1's test carries an explicit code comment disclosing the substitution and the planned W3 retarget — a defensible engineering call — but it was **not logged to `build/design/deviations.md`** ("every deviation logged" — program-plan.md rule #1). `deviations.md` on disk is still just its header stub. |
| L1-8 | medium | 0.9 | `.../contrast.test.ts` + `.../page.test.tsx` vs `build/test-impact.md:81` | `test-impact.md` Jest arithmetic: "W0: **+1 test** (mount-order test in `page.test.tsx`)" | L1's diff adds **3** new `it(...)` blocks, not 1: the 1 mount-order test in `page.test.tsx`, plus 2 in `contrast.test.ts` (`resolves the rail file glob to at least today's five files`, `scans the paper-ground files...`). No suite count changes, but the documented test-count delta undercounts by 2. The program's own gate ("reconcile suites/tests vs 458/5170, delta by delta... a wave whose count moves without a written reconciliation does not merge") has nothing written for these 2. |
| L1-9 | info | 0.95 | (process) | — | `git diff document-lens/w0...document-lens/w0-l1` (the literal review command) is empty because `w0`/`w0-l1` were the same commit at review time (fast-forward, no merge commit) — see branch-pointer note above. Not a code defect; flagged so a future audit isn't misled by the same empty diff. |
| L1-10 | info | 0.95 | commit `80af65c32` | Conventional Commits + co-author | `test(document): W0 tripwires — …` + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. **PASS.** |
| L1-11 | info | 0.95 | (ownership) | no program-folder files committed | Confirmed — 0 paths under `artifacts/document-lens-build-2026-08-29/**` in L1's diff. **PASS.** |
| L2-1 | info | 0.95 | (ownership) | L2 touches only `scripts/the-document-lens-seed.sql` | `git diff 80af65c32..15e581435 --stat`: exactly 1 file, 735 insertions. **PASS.** |
| L2-2 | info | 0.9 | `scripts/the-document-lens-seed.sql:224-235` | reconciliation "Seed requirements": Living 18 · Dining 12 · Primary bedroom 14 · Mudroom 8 · Kitchen 10 = 62 lines | Room/line counts match the reconciliation bullet number-for-number, all with `project_room_id`. **PASS.** |
| L2-3 | info | 0.85 | `scripts/the-document-lens-seed.sql:395-478` vs `supabase/migrations/00184_procurement_state_chain.sql:108-163` | A-05: the ratchet only moves status up, never fires with `purchase_order_id IS NULL` | Verified against the actual trigger function (`ffe_ratchet_to_po_stage`, `po_status_to_ffe_stage`): 2 unspecified lines carry `purchase_order_id = NULL` (trigger never touches them); PO-linked lines are inserted with the PO's own target-rank status set explicitly, respecting "never down." **PASS.** |
| L2-4 | info | 0.85 | `scripts/the-document-lens-seed.sql:598-616` vs `00184:256-330` | A-04: non-clean vs clean `receiving_inspections`, only clean advances the PO | Verified against `receiving_inspection_side_effects()`: clean (Coastal) advances the PO to `delivered` + cascades 2 lines; damaged (FDL) stamps `delivered_date` but leaves the PO `shipped`, console forced to `delivered` explicitly (ratchet's rank comparison, 6 < 5, is false, so it's left alone). **PASS**, matches the trigger's real behavior. |
| L2-5 | **high** | 0.9 | `scripts/the-document-lens-seed.sql` (no `damage_claims` insert anywhere in 735 lines) vs `apps/designer-portal/src/lib/document/stamp-derivation.ts:97-108`, `packages/supabase/src/hooks/use-project-v2.ts:192`, `supabase/migrations/00150_receiving_and_damage_claims.sql`, `00196_per_item_claims_and_margin_notes.sql` | reconciliation "exactly 1 damaged"; `seed-verify.sql`'s own `damaged = 1` check | **The seeded console line will not actually render as "damaged."** `deriveLineStamp` returns `'damaged'` only when `item.item_claims` (the PostgREST embed `item_claims:damage_claims!ffe_item_id(id,state,created_at)`) contains a row with `state IN ('drafted','vendor_notified')` — i.e. a row in the **separate `damage_claims` table**, keyed by `ffe_item_id`. The seed sets `project_ffe_items.blocked=true`/`blocked_reason` and inserts a `receiving_inspections` row, but **never inserts into `damage_claims`**, and confirmed no DB trigger auto-creates one (00150's "auto-drafted" comment describes an application-hook behavior, `useCreateReceivingInspection`, not a trigger; 00184's `receiving_inspection_side_effects()` touches only `purchase_orders`/`po_payments`/item statuses). Consequence: `ticket-derivation.ts:381-382`'s `counts.damaged` — the source of the ladder's `"1 DAMAGED AUG 26"` and the standing sheet's `"CLOSES TOMORROW"` row (`reconciliation.md` §"The standing sheet") — will read **0**, not 1, once Wave 3 wires it up. `seed-verify.sql`'s `damaged` check (`blocked=true AND status='delivered'` on `project_ffe_items`) will keep reporting **PASS**, because it never queries `damage_claims` — a false positive baked into the seed's own self-check. |
| L2-6 | high | 0.85 | `scripts/the-document-lens-seed.sql:536-548` (`(n = 1)` for the `blocked` column) vs `stamp-derivation.ts:102`, `authorization-derivation.ts:312,391`, `orders-book-vendors.tsx:82` | the COM-blocked line (n=2) should read as blocked/awaiting-decision | The INSERT's `blocked` column value is `(n = 1)` for **every** row — only the console gets `blocked=true`. The COM line (n=2) has `blocked_by_decision_id = v_dec_com` (a genuinely pending decision) but `blocked = false`. `deriveLineStamp`'s `'decision_due'` branch requires `item.blocked === true` *and* a pending `blocking_decision`; with `blocked=false` it never fires, and every other `.blocked`-gated authorization/orderability check in the app will treat this line as ordinary/orderable rather than decision-blocked. **`margin_items`'s "beside Pieces" count is unaffected** — its decision branch (`00194_margin_items_view.sql:41-46`) joins `project_ffe_items.blocked_by_decision_id` directly, not the `blocked` boolean — so the margin split still lands on 3/4 as required; only the line's own on-paper stamp/authorization is wrong. |
| L2-7 | medium | 0.7 | `scripts/the-document-lens-seed.sql:145-146` (`v_install_date` = next Tuesday on/after `CURRENT_DATE+21`) | reconciliation: "install milestone `now() + 21d` (a Tuesday if the seed can choose)" | The seed prioritizes landing on a Tuesday over landing at exactly +21d, searching forward up to 6 extra days. On the review's run day (2026-08-29, Saturday) this computes to **2026-09-22, +24 days**, and the offset ranges 21–27d depending on the day the seed is run — the reconciliation's literal wording reads as "+21d primary, Tuesday only if that already lands there," which the shipped logic inverts. A run landing on the high end (+27d ≈ 3.9 weeks) risks the ladder printing something other than the required `"3 WEEKS"`. Not independently confirmable without Wave 3's weeks-rounding code, hence medium confidence. |
| L2-8 | medium | 0.9 | `build/seed/seed-verify.sql` (full read, 14 checks) | task checklist / reconciliation "Schedule" bullet: install `now()+21d` should be assertable | No check in `seed-verify.sql` references `schedule_milestones` or `project_phases` at all — the install-date requirement is the one reconciliation bullet with zero corresponding SQL assertion, unlike every other bullet (rooms, lines, product-linked, unspecified, damaged, overdue approvals, POs, unacknowledged PO, clean-delivered PO, non-clean inspection, margin ×3, pre-work doc — all 13 other checks present). |
| L2-9 | info | 0.75 | `scripts/the-document-lens-seed.sql` guard-trigger sections (00390/00398/00399/00438) | idempotency under authority-boundary guards | Spot-checked against 00184's actual functions (high confidence there); did not independently re-derive 00390/00398/00399/00438's guard bodies line-by-line, but the seed's own account of each guard's exact behavior (no UPDATE escape hatch on `proposal_id`, `assignment_scope='room'` can't go roomless, RPC-scoped decision-cascade token) reads as specific, mechanism-level knowledge rather than generic hedging. Not independently falsified. |
| L2-10 | info | 0.95 | commit `15e581435` | Conventional Commits + co-author | `chore(seed): the long paper — …` + co-author trailer. **PASS.** |
| L2-11 | info | 0.95 | (ownership) | no program-folder files committed | `build/seed/seed-notes.md` and `build/seed/seed-verify.sql` exist only as **untracked** working-tree files (`git ls-files` returns nothing for either, in any branch) — correctly never committed by L2. **PASS**, and consistent with check 6 generally. |
| L2-12 | info | 0.9 | seed-notes.md "0 OF 6 CLOSED OUT" / "The record" caveats | A-03: trace both derivations, write the seed's real resulting strings | Traced correctly and disclosed: "0 OF 6" is unconditional client-side checklist state (no seed row can affect it); "The record" settles to **4 complete**, not the specimen's "12 COMPLETE," because `deriveSections()`'s 7-section `ORDER` caps a lineage-having project at 4. This is a properly-surfaced, code-verified limitation, not a hidden gap. |
| L3-1 | info | 0.95 | (ownership) | L3 touches only baseline e2e files + the shared helper | `git diff 80af65c32..91765a1cf --stat`: `supabase-admin.ts`, `action-visibility.spec.ts`, `arrival-arc.spec.ts`, `margin-handoffs.spec.ts`, `plan-room.spec.ts`, `spec-book-workspace.spec.ts` — all named in the plan's L3 bullet; zero overlap with L1/L2 files. **PASS.** |
| L3-2 | info | 0.9 | `e2e/helpers/supabase-admin.ts:89-101` | plan: fix the GoTrue cause at its root via `psqlScalar`, reusing `plan-room.spec.ts`'s existing idiom | `getUserIdByEmail` reimplemented on `psqlScalar("select id from auth.users where email = …")`, same signature/throw contract; e2e-baseline.md documents this also fixes 3 call sites outside `e2e/document`. **PASS**, matches plan exactly. |
| L3-3 | **high** | 0.9 | `e2e/document/action-visibility.spec.ts:+213-229` (test now at `:226` per the split) | plan: "**itemise** `action-visibility:213`"; debt #8: "quarantine only truly environmental with a written reason" | Quarantined via `test.fixme`, with its own comment stating: *"A real, already-shipped product/ruling change (A9), not a fixture or ordering issue — out of this wave's fix scope."* This is explicitly **not environmental** by the author's own words — it is acknowledged product drift, which the plan said to itemise, not quarantine. Exactly the pattern the review brief calls out. |
| L3-4 | **high** | 0.9 | `e2e/document/plan-room.spec.ts:+108-120` | plan: "**itemise** `plan-room:86`"; debt #8 as above | Same violation: quarantined via `test.fixme` with the comment *"A real product copy/UX change, not a fixture or ordering issue — out of this wave's fix scope."* Again explicit, self-declared product drift, not environmental. |
| L3-5 | medium | 0.6 | `e2e/document/arrival-arc.spec.ts:+235-249` | plan expected the GoTrue root-cause fix alone to clear `arrival-arc:235` (not on the "itemise" list) | Quarantined for a **second, independent** cause: `OpenRequestsStrip`/`useOpenRequestsDeskPopulation` are referenced only from their own component file and their own unit test — grep-confirmed never mounted under `src/app/**`. A genuine shipped-but-unwired gap, pre-existing on `main@dab057537`, not "environmental" flakiness but also not an intentional ruling change — a softer case than L3-3/L3-4, and not literally covered by the plan's itemise list, so this is as much a plan-coverage gap as a lane call. Well-documented (grep evidence, un-fixme condition, owner) regardless. |
| L3-6 | medium | 0.6 | `e2e/document/margin-handoffs.spec.ts:+153-210` | margin-handoffs was in the plan's "re-run + pin ids" bucket, not "itemise" | Two additional `test.fixme`s beyond the expected fixture/id fix, caused by `RedLetterZone` now superseding `DocumentGuide` for this fixture's data shape (confirmed via `page.tsx`'s mount condition and a captured page snapshot). This is real, shipped architectural drift — the same category of issue as L3-3/L3-4 — just not on the literal named list, so the "itemise, don't quarantine" instruction was never explicitly triggered for it. Flagged on the same principle. |
| L3-7 | info | 0.9 | `e2e/document/quiet-release-contracts.spec.ts:74,169,302` | plan: "itemise `quiet-release-contracts:74`" | Correctly diagnosed as cold/parallel-worker contention (genuinely environmental — passes cleanly, every time, under `--workers=1`, matching `playwright.config.ts:42`'s own CI setting). No code change made or needed. **This is the one of the three named "itemise" files handled the way debt item #8 intends.** |
| L3-8 | medium | 0.6 | `build/e2e-baseline.md` exit-criterion tail | plan exit: `test:e2e -- e2e/document --project=chromium → 0 failed` | The verified command was `npx playwright test e2e/document --project=chromium --workers=1 --reporter=list`, not the plan's literal (unpinned-worker) form. Well-justified and disclosed at length in `e2e-baseline.md` (citing `playwright.config.ts:42` and per-spec "would race it" comments), but this substitution is not logged in `build/design/deviations.md`, and running the plan's literal command locally (default parallel workers) would likely reproduce several of the file's own documented "drift (cold/parallel contention)" failures. |
| L3-9 | info | 0.95 | `build/e2e-run-w0-after.log` | exit: "0 failed, fixmes enumerated" | Log tail independently confirms `6 skipped, 63 passed (4.6m)`; diff contains exactly 5 new `test.fixme(` calls (6 skipped − 1 pre-existing unrelated `room-view.spec.ts:242` skip = 5, matching). **PASS**, evidence-backed, not just self-reported. |
| L3-10 | info | 0.95 | commit `91765a1cf` | Conventional Commits + co-author | `test(e2e): document basket baseline — …` + co-author trailer. **PASS.** |
| L3-11 | info | 0.95 | (ownership) | no program-folder files committed | `build/e2e-baseline.md`, `build/e2e-run-w0-*.log` exist only as untracked working-tree files. **PASS.** |
| X-1 | medium | 0.85 | `build/design/deviations.md` | program-plan.md: "every deviation logged" | On disk, the file is still only its header/schema stub (0 rows) as of this review. None of L1-7, L1-8, L2-7, or L3-8 above — each independently reasonable, each independently disclosed in code comments or wave notes — were logged to the program's single designated deviation ledger. The information exists; the ledger doesn't have it yet. |
| X-2 | info | 0.95 | (process) | — | Mid-review, the integration lane merged `w0-l2` and `w0-l3` into `w0` via proper two-parent merges (`93bb409da`, `1f55eb5b0`), both Conventional-Commit `chore(document-lens): merge w0-lN — …`. Concurrent with, not caused by, this review; noted for reproducibility (see pinned SHAs used throughout this report). |

---

## Fixes required before ship (Wave 0 close / before Wave 3 depends on this seed)

1. **L2-5 (high).** Insert an actual `damage_claims` row for the console (`ffe_item_id` = the console's id, `state = 'drafted'` or `'vendor_notified'`) in `scripts/the-document-lens-seed.sql`. Without it, the seed's entire "1 damaged" narrative — the ladder's `1 DAMAGED AUG 26` and the standing sheet's `CLOSES TOMORROW` row — will not exist once Wave 3 wires the real derivation, and `seed-verify.sql`'s current check should be extended to assert against `damage_claims` (not just `project_ffe_items.blocked`) so it can't false-positive again.
2. **L2-6 (high).** Set `blocked = (n = 1 OR n = 2)` (or an equivalent per-row expression) so the COM-blocked line actually carries `blocked=true` alongside its `blocked_by_decision_id`, matching `stamp-derivation.ts:102`'s `'decision_due'` contract.
3. **L3-3 / L3-4 (high).** Un-quarantine `action-visibility.spec.ts` (A9 capture-lead dock) and `plan-room.spec.ts` (light-table copy change) from `test.fixme`, and itemise them per the plan's actual instruction — i.e., track them as known, named product-drift items (a backlog/deviations-log entry each, with a decision on whether the old assertions get updated to current behavior or the gap gets escalated), rather than silently marking them skipped. At minimum, log both in `build/design/deviations.md`.

## Should fix, not gating

- L1-7 / L3-8 — log both undocumented-but-reasonable substitutions (`--doc-seam-height` standing in for the not-yet-existent `--doc-landing-clear`; `--workers=1` standing in for the plan's unpinned worker count) in `build/design/deviations.md`.
- L1-8 — reconcile `test-impact.md`'s "W0: +1 test" line to the actual +3 (2 additional tests landed in `contrast.test.ts`).
- L2-7 / L2-8 — either tighten `v_install_date` to prioritize +21d over landing on a Tuesday, or accept the drift explicitly and add a `seed-verify.sql` check for the install milestone (currently absent).
- L3-5 / L3-6 — no code action required (both are correctly out-of-scope, frozen-file gaps), but both should get the same "itemise" treatment as L3-3/L3-4 rather than living only as code comments, since they're the same category of finding.
- L1-5 — cosmetic: technical-design.md's OD-9 claim that Wave 0 touches "the one product line" is inaccurate (it already existed on `main`); worth a one-line correction so a future reader doesn't go looking for a change that was never needed.
