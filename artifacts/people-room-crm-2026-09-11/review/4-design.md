# Design review, round 4

Files reviewed: `specimens/people-room-1440.html`, `specimens/people-room-390.html`, all fourteen plates in `shots/` (`people-room-1440-state-*-1440.png`, `people-room-390-state-*-390.png`), `shots/people-room-1440-console.json` and `shots/people-room-390-console.json`, against `synthesis/direction.md` (§1, §3, §3.8, §3.9, §5, §6) and `specimens/SPEC.md` (§4, §5 including §5.7, §6, §8), and the five standing orchestrator rulings R-A..R-E. `review/3-design.md` and `review/fix-log-r3.md` re-checked line by line against the files on disk (line numbers below are current). Every claim is grep- or plate-verified.

Console gate: all fourteen captures report `"errors": []`, `"warnings": []`, `"horizontalOverflow": false`.

## Part A — every round-3 finding, re-checked

| ID | File(s) | Status now | Evidence |
|---|---|---|---|
| DR-1 (r3, blocking) — 390's Add sheet fabricated a defaulted authority | `people-room-390.html` | **Fixed.** `addSheetFor()` returns `authority: ''` for the empty case; `renderAdd()` prints "Nothing defaulted from the agreement." / "Record the authority" for it, matching 1440's tested face | `people-room-390.html:1153` (`authority: auth` with `auth = e && e.authority ? e.authority : ''`), `:1186-1189`; plate `people-room-390-state-add-390.png` reads "Nothing defaulted from the agreement." |
| DR-2 (r3, blocking) — Stonehaven unreachable at 390 | both | **Fixed.** `DIR_FIRMS` is `['marrow', 'northgate', 'tcdrywall', 'stonehaven']` in both files, byte-identical | `people-room-1440.html:654`, `people-room-390.html:678` |
| DR-3 (r3, major) — kind-picker examples diverged | both | **Fixed.** Per R-E, `KIND_SOURCE` (1440) and `ADD_EXAMPLE` (390) are now identical: a maker → F-20 Claire Bissett, someone else → F-27 Ray Thao, an installer unmapped at both widths | `people-room-1440.html:1256-1257`, `people-room-390.html:1117-1118` |
| DR-4 (r3, minor) — W-9 expiry prose diverged ("No expiry, on file..." vs "On file...") | 390 | **Fixed.** `paperStack()` now uses the same template as 1440's `docCells()`: `'On file ' + dshort(d.issued)` | `people-room-390.html:881`, `people-room-1440.html:913` |
| DR-5 (r2, carried) — 390's "Held by" hardcoded lowercase `'the studio'` | 390 | **Fixed.** `d.heldBy === 'studio' ? 'The studio' : esc(d.heldBy)`, matching 1440 | `people-room-390.html:888` |
| DR-6 (r2, carried) — orphan `<h2>The studio book</h2>` at 390 | 390 | **Fixed.** Deleted; "People"/"Firms" promoted to `<h2 class="t-d3">`, matching 1440's `<h2 class="crm-h">` pair | `people-room-390.html:753,755`; `grep -c "studio book"` → 0 |
| DR-7 (r2, carried) — `docWord()`/`docCells()` blocked-rule diverged | both | **Fixed.** Both now: `state !== 'Current' && blocks.length → blocked` | `people-room-390.html:615-619`, `people-room-1440.html:911-914` |
| CR2-11 / CR3-3 (major, returned for ruling round 2/3) — paper word on lender/inspector firms | both | **Ruled (R-A) and partly built — reopened, see DR4-1** | see Part B |
| Round-1 DR-4 — F-11 seat window (fixture `2027-03-13` vs SPEC's required `13 Aug 2027`) | both | **Resolved.** Ruling R-D corrected the fixture's `to` field to `2027-08-13` in both files and deleted the `seatTo()`/`FACE.danaWindow` overrides; `grep -c "2027-03-13"` → 0 in both | `people-room-1440.html:503`, `people-room-390.html:480` |
| CR3-1 / CR-6 / CR2-2 — no bring-forward/travel-list picker for Task 5 | both | **Fixed.** `#state-pick` built per R-C/SPEC §5.7 in both files, in the state bar between Project roster and Add sheet, fully populated from the fixture | `people-room-1440.html:1186-1225`, `people-room-390.html:1222-1263`; plates `*-state-pick-*.png` |
| CR3-2 / CR-13 / CR2-4 — no invoice/change-order screen for Task 2 | both | **Resolved by ruling R-B.** Acceptance amended; confirmed no invoice/CO string appears anywhere (`grep -i "invoice\|change order"` hits only fixture `authority` prose, never a screen) | both files |
| CR-1(r1)/DR-1(r2, design) — Call Sheet vitals literal string vs. fixture-supported tally | both | **Still open — SPEC self-contradiction, not a builder defect. See DR4-2.** | both files |
| CR2-7 / CR3-4 — "licence" spelling | both | **Still correctly un-fixed.** Lives inside the verbatim §3 fixture JSON; a SPEC-owner edit | fixture JSON in both files |
| CR2-8 / CR3-5 — "Compare & merge" band names no cards, has no handler | both | **Still correctly un-fixed**, awaiting a panel ruling (which two cards, what the act opens) | both files |
| CR3-6, CR3-7 — schedule-authority sentence, key-holder cue | both | **Still correctly un-fixed**, need fixture additions from the SPEC owner | both files |

## Part B — fresh findings, round 4

| ID | Severity | Confidence | File : state (hash) | Claim | Fix |
|---|---|---|---|---|---|
| DR4-1 | **Blocking** | High | both : `#state-directory` | SPEC §5.1 #17's required fact never appears on the face, at either width, because it has nowhere to appear. The acceptance item reads: "A firm whose only people are inspectors or lenders (Great Northern Bank, City of Minneapolis CPED Inspections) holds no compliance paper for the studio: **its firm row** prints no paper word at all." But `DIR_FIRMS` in both files is `['marrow', 'northgate', 'tcdrywall', 'stonehaven']` — Great Northern Bank and CPED Inspections are never mapped through `dirFirmRow()`/`dirFirmRow` in either file (`grep -n "gnbank\|cped" people-room-1440.html people-room-390.html` hits only the fixture JSON — `companies`, `persons`, `engagements` — never a rendered call site or a `DIR_FIRMS` entry). Their firm rows do not exist anywhere in the DOM at either width, confirmed absent from both Firms sections on `people-room-1440-state-directory-1440.png` and `people-room-390-state-directory-390.png` (each lists exactly four firms: Marrow & Sons, Northgate Electric, Twin Cities Drywall & Plaster, Stonehaven Tile Gallery). `fix-log-r3.md`'s R-A entry claims the fix was "Applied at all three places the finding names: the Directory firm row (`dirFirmRow`)..." — the function exists and is correct in isolation (confirmed via direct `firm()`/`holdsNoPaper()` code read), but it is never invoked with `gnbank` or `cped` as an argument on the rendered face, so the claimed fix is unexercised and the required acceptance string is missing. This mirrors the exact shape of CR2-3/DR-2 (Stonehaven unreachable) that round 3 already fixed by adding `'stonehaven'` to `DIR_FIRMS` — the same fix was never applied for the two firms this specific acceptance item is about. | Add `'gnbank'` and `'cped'` to `DIR_FIRMS` in both `people-room-1440.html` and `people-room-390.html`, so their firm rows render and exercise the existing `noPaperHeld()`/`holdsNoPaper()` suppression logic that is otherwise correct. |
| DR4-2 | **Blocking** | High | both : `#state-roster` | SPEC §5.4 #3's required literal string never appears at either width. The acceptance item is: "Vitals: '14 on the job this week · 9 reachable by text · 5 with accounts · 6 on paper'." Both files instead compute and print a live tally over the fixture — "12 on the job this week · 5 reachable by text · 4 with accounts · 2 on paper" — confirmed on `people-room-1440-state-roster-1440.png` and `people-room-390-state-roster-390.png` (identical text, so not a cross-width divergence) and in code (`people-room-1440.html:1100`, `people-room-390.html:1064`, both call a `tally()` helper over `persons`/`engagements`, never the literal SPEC string). This is a carried, self-documented SPEC self-contradiction (round 1's CR-1/DR-1, `fix-log-r3.md`'s "Deviations recorded" section for 390: "The Call Sheet still prints the live tally... which is what §3's fixture supports, not §5.4 #3's '14 · 9 · 5 · 6'") — the fixture as written (3 studio + 2 client + 7 "this week" engagements = 12, not 14; similar shortfalls on the other three numbers) cannot produce SPEC's literal string, so no build fix is possible without a SPEC-owner ruling, same as round 1's F-11-window contradiction that R-D has since resolved. It remains open and, per SPEC §5's "every string below must appear on the face, spelled exactly," is a missing required acceptance string in both files. | Not a build fix. Needs a Kody/SPEC-owner ruling: either amend SPEC §5.4 #3's literal string to match the fixture's real tally ("12 · 5 · 4 · 2"), or amend/extend the fixture (more engagements landing inside 19–25 Oct 2026, more `Texting`/`Account` consent+access rows) so the fixture actually supports "14 · 9 · 5 · 6". |
| DR4-3 | Major | Medium | both : `#state-directory` (every person row with an engagement, e.g. `people-room-1440-state-directory-1440.png` Tom Marrow/Dana Kowalski/Joe Wozniak rows) | The Directory person row renders the engagement's stage (`e.stage`, e.g. "On the job", "Awarded") as one of the row's bordered `.word` columns — `people-room-1440.html:685`: `(e ? wordEl(e.stage) : gap())`; `people-room-390.html:688`: `e ? word(e.stage) : ''` inside the row's `words` (shown directly on 1440's row, and inside 390's per-row unfold panel, but styled and positioned exactly like the other three word columns in both). This is precisely what `direction.md` §3.9 ruling **C1** rules out: "Stage prints only on a seat line, never as a person-level column... **The Directory row gains a fifth word column whose value is chosen by a rule nobody can state**" is C1's stated consequence of being overruled — and that is what both files now build. The row's separate plain-text "seat line" underneath (e.g. "Okonkwo residence · sub · electrical · On the job · 12 Oct 2026 to 13 Aug 2027") already carries the stage word in prose, which is what C1 calls for; the bordered column duplicates it. This is not a builder defect — `SPEC.md` §6.1 itself is explicit ("four `.word` columns at 108px each") and §5.1 #10 requires four word values including a stage word ("Awarded") on Ray Thao's row — so both builders correctly followed the literal build contract they were told is authoritative. The contradiction is between `direction.md`'s C1 and `SPEC.md`'s own acceptance table/width rule, present identically in both files (not a cross-width divergence). | Needs a ruling: either amend `direction.md` §3.9 C1 to reflect that stage now is a fourth row-level word column (superseding C1, the way R-A superseded §5.1 #10's literal "Not on file" for Ray Thao), or amend `SPEC.md` §5.1/§6.1 to drop the stage column and move that fact into the seat line only, in both files. |
| DR4-4 | Minor | Medium | `#state-pick`, Dana Kowalski's row (`people-room-1440-state-pick-1440.png` vs `people-room-390-state-pick-390.png`) | The three words on Dana Kowalski's Bring-forward row print in a different order at each width, though the words themselves are identical. 1440's `pickRow()` builds `words` as `[paper, reach, consent]` (`people-room-1440.html:1165-1167`), rendering "LAPSED, FIELD LINK, TEXTING" — the same order SPEC §5.7 #4a lists them in ("paper word 'Lapsed' · reach 'Field link' · consent 'Texting'"). 390's `pickRow()` builds the same three as `word(p.reach) + word(p.consent) + word(c.paper)` (`people-room-390.html:1237-1238`), rendering "FIELD LINK, TEXTING, LAPSED" — reach, consent, paper. Confirmed visually on both plates. No other row in the five-row list carries all three words, so this affects only Dana's row, but it is a real, visible content-ordering difference between the two mandated-identical widths. | Reorder 390's `pickRow()` word concatenation to `paper, reach, consent`, matching 1440 and SPEC §5.7 #4a's listed order. |
| DR4-5 | Major | High | `#state-pick` footer (`people-room-1440-state-pick-1440.png` vs `people-room-390-state-pick-390.png`) | The Bring-forward footer's act order and the consequence sentence's position both disagree between widths, and 390 contradicts SPEC's explicit wording. SPEC §5.7 #6: "Consequence sentence **under** the terminal act." 1440's `renderPick()` pushes the act row (`Add four to the roster` terminal, then `Put back` secondary) first, then the consequence paragraph after it (`people-room-1440.html:1213-1220`) — consequence appears below the buttons, satisfying "under." 390's `renderPick()` pushes the consequence paragraph first, then the act row (`Put back` secondary, then `Add four to the roster` terminal) after it (`people-room-390.html:1257-1261`) — consequence appears above the buttons, and the two buttons are in the opposite left-right order from 1440. This is a self-documented, unresolved deviation in `fix-log-r3.md`'s "Deviations recorded" section for 390 ("§5.7 #6's 'under the terminal act' is read as 'belonging to', not 'after'"), and it produces a visibly different layout for the same state's footer at the two widths (confirmed on both plates: 1440 shows the dark terminal button first with "Put back" to its right, then the sentence below; 390 shows "Put back" first with the dark terminal button to its right, with the sentence above both). | Reconcile 390's `renderPick()` to match 1440's literal ordering — act row (terminal, then secondary) followed by the consequence paragraph — so both widths satisfy SPEC §5.7 #6's plain reading and agree with each other. |
| DR4-6 | Major | High | `#state-pick` (`people-room-390.html`, no plate difference since both render the at-rest state identically) | 390's Bring-forward checkboxes and "Put back" act are inert; 1440's identical controls are live. 1440 wires `.pick-check input` to a `change` listener (implied by `pickUpdate()` at `people-room-1440.html:1227-1250`, invoked from the global input-change handler) and gives "Put back" the `js-putback` class that the same handler recognizes, so ticking/unticking a row recomputes "N of 5... selected," the terminal label ("Add N to the roster" / disabled "Add to the roster" at N=0 via `aria-disabled`), and the consequence sentence live; pressing "Put back" clears all five boxes. 390's global click delegate (`people-room-390.html:1380-1462`) has no branch for `.pick-box` or for the "Put back" button (it carries no `data-*` hook, unlike every other interactive element in the same delegate), and no `change` listener is registered anywhere in the file for pick checkboxes (`grep -n addEventListener people-room-390.html` shows only the document click delegate, `hashchange`, `message`, and `DOMContentLoaded` — no `change`/`input` listener). Unchecking Ben Ostrom's already-unchecked box, or ticking it, or pressing "Put back," does nothing at 390: the count line, terminal label and consequence sentence never update. Both widths render the same at-rest state correctly (matching SPEC's static acceptance list), but the two widths diverge sharply the moment a reviewer or Leah interacts with the picker — the exact failure shape CR2-6 (the Add-sheet kind picker being inert) was rated blocking for in round 2. | Add the same `change`-listener/`js-putback` wiring 1440 uses (or an equivalent) to `people-room-390.html`'s `.pick-box` inputs and "Put back" button, so the picker is live at both widths. |
| DR4-7 | Major | High | `#state-add`, kinds with a non-empty fixture `authority` (e.g. "a GC" → Tom Marrow, "someone else" → Ray Thao) | The Authority-region note text differs by width for the identical fact ("this seat's authority came from the agreement"). 1440's `renderAdd()`: `(a.authority ? 'Defaulted from the agreement.' : 'Nothing defaulted from the agreement.')` (`people-room-1440.html:1333-1334`). 390's `renderAdd()`: `(sheet.authority ? 'The agreement set this. Confirm it, or write a different one.' : 'Nothing defaulted from the agreement.')` (`people-room-390.html:1186-1187`). Both files agree on the empty-authority branch (the one SPEC's default "a sub" face exercises), so this does not fail a written §5.5 acceptance string, but it is a real, code-confirmed prose divergence between the two mandated-identical widths, reachable the moment either kind chip is clicked. `fix-log-r3.md`'s own "Deviations recorded" section for 390 flags this exact gap as unresolved ("Flagged so the pair can be settled in one direction."). | Pick one wording and apply it to both files' non-empty-authority branch (390's fuller sentence reads better with the act "Confirm from the agreement" that follows it; 1440's terser line no longer explains what to do). |

## Part C — spot checks that passed clean this round

- **Acceptance strings, all seven states, both widths**: walked SPEC §5.1–§5.7 line by line against the plates and source. Every required string is present verbatim at both widths except the SPEC §5.4 #3 vitals literal (DR4-2, carried) and the SPEC §5.1 #17 firm-row fact (DR4-1, new).
- **Word pigment maps (§3.8)**: `TONE` (1440) and `WORD_PIGMENT` (390) are structurally identical (same 16 keys → same four tones), unchanged from round 3's confirmed-clean pass; no colour-alone signal on any plate — every `.word` prints its text.
- **Fixture facts**: Dana Kowalski's rule/bounce/consent/paper chain, Pete Rusk's opt-out carry-forward, Frank Bauer's do-not-contact routing to Rosa Delgado, Ray Thao's "never text" rule, Chidi Okonkwo's "$2,500" (literal, not redacted), the site access card's seven regions with no code digit anywhere — all present, exact, identical on both widths.
- **`#state-pick` acceptance (§5.7)**: all five mini rows in the required order (Dana Kowalski, Pete Rusk, Ingrid Halvorsen, Claire Bissett, Ben Ostrom) with the correct per-row facts (history line, rule clause, consent/paper words per row) at both widths; "What travels"/"What stays behind" lists byte-identical; consequence sentence text byte-identical ("Adds four seats to the Okonkwo residence. Pete Rusk arrives opted out of texting. Northgate Electric's insurance lapsed 31 Mar 2026.") — see DR4-4/DR4-5 for the two ordering-only divergences.
- **Add sheet kind-picker parity (R-E)**: `KIND_SOURCE` (1440) and `ADD_EXAMPLE` (390) now byte-equivalent for all six mapped kinds plus the shared unmapped "an installer"; `KIND_FIRM`/`ADD_FIRM` gating identical.
- **`tel:` links**: every phone on every face is `<a href="tel:+1##########">`, min 44×44, ≥8px from the row's own control, at both widths.
- **No forbidden vocabulary**: re-ran the SPEC §8 grep battery (`box-shadow`, `text-overflow`, `placeholder=`, ` disabled`, curly apostrophes, `client_rep`/`party_kind`/`sms_consent_status`/`studio_contact_id`/`project_parties`, "chip"/"badge"/"modal"/"toast"/"dashboard"/"wizard"/"spinner"/"pill" as visible text, "Remove", schema `F-nn` codes outside `<script>`) — zero hits in both files.
- **Structure**: one `<h1>` per file, `<!-- specimen-complete -->` as the exact last line of both, `aria-disabled="true"` present and no bare `disabled` attribute, forced-colors and reduced-motion media blocks present in both.
- **R-D (Dana Kowalski's seat window)**: fixture corrected to `2027-08-13` in both files; `seatTo()`/`FACE.danaWindow` overrides deleted; both plates read "12 Oct 2026 to 13 Aug 2027" on the Directory seat line, the person card, and the roster's held clause.
- **R-B (Task 2 acceptance)**: no invoice or change-order screen exists or is implied in either file; the authority-grant fact is recorded and visible on the person card and the Call Sheet's client side only, as ruled.

## Summary

```
clean: false
blocking: 2 (DR4-1, DR4-2)
major:    4 (DR4-3, DR4-5, DR4-6, DR4-7)
minor:    1 (DR4-4)
carried, still open, awaiting a ruling (not new, not re-scored above): CR2-7/CR3-4 (licence spelling),
  CR2-8/CR3-5 (Compare & merge names no cards), CR3-6/CR3-7 (fixture additions)
resolved this round by orchestrator ruling: round-1 DR-4 (R-D), CR3-1/CR-6/CR2-2 (R-C), CR3-2/CR-13/CR2-4 (R-B)
```

`clean` is **false**. Round 3's own findings (DR-1..DR-4, plus the carried DR-5..DR-7) are now genuinely fixed — the fix-log's claims check out except for one: R-A's paper-word suppression is correct in the code it touches, but was never wired into the Directory firm row for the two firms the finding is about, because those firms are absent from `DIR_FIRMS` entirely (DR4-1). The new `#state-pick` build (R-C) is complete and its at-rest face is correct and identical at both widths, but three of its details — word order (DR4-4), footer/button order (DR4-5), and live interactivity (DR4-6) — were built once at 1440 and re-implemented independently at 390 rather than ported, the same pattern that produced round 2 and round 3's cross-width drift on the Add sheet and the kind picker. DR4-2 (vitals) and DR4-3 (stage-as-column) are the same shape as round 1's now-resolved F-11 date contradiction: a genuine disagreement between `SPEC.md`'s literal text/contract and either the fixture (DR4-2) or `direction.md`'s own ruling C1 (DR4-3) — not something either builder could have fixed by re-reading a file they were told not to read, and both need a ruling rather than a build change.
