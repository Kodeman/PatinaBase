# Design review, round 3

Files reviewed: `specimens/people-room-1440.html`, `specimens/people-room-390.html`, all twelve plates in `shots/`, against `synthesis/direction.md` (§1, §3, §3.8, §3.9, §5) and `specimens/SPEC.md` (§5 acceptance, §6 width rules, §8 forbidden). `review/fix-log-r2.md`, `review/2-design.md` and `review/2-construction-reread.md` re-checked finding by finding against the current files (fix-log-r2.md documents only Builder A's/1440's round-2 fixes; no equivalent 390 fix log exists, so every round-2 finding that touches `people-room-390.html` was independently re-verified against the file on disk and the plate, not assumed from a log entry), then a fresh pass. Every claim is grep- or plate-verified; line numbers are from the current files on disk.

## Part A — every prior finding, re-checked

| ID | Round | File(s) | Status now | Evidence |
|---|---|---|---|---|
| DR-1 (curly apostrophes) | 1 | both | **Still fixed** | `grep -c $'\xe2\x80\x99'` → `0` on both files |
| DR-2 (console.json missing the 1440 half) | 1 | shots | **Still fixed** | `shots/people-room-console.json` holds all 12 capture entries (6×1440, 6×390), each `"errors":[]`, `"warnings":[]`, `"horizontalOverflow":false` |
| DR-3 (extra closing brace) | 1→TR-3 (r2) | 1440 | **Fixed** | `people-room-1440.html:90` now closes with exactly one `}`, byte-identical to `people-room-390.html:90`; `sed -n '85,96p'` on both files diffs clean |
| DR-4 (F-11 seat window: fixture `to` is `2027-03-13`, SPEC §5 requires "13 Aug 2027") | 1 | both | **Still open — SPEC self-contradiction, not a builder defect.** Both files still special-case F-11 (`seatTo()` in 1440:608, `seatWindow()` in 390:629) to print `13 Aug 2027` instead of the fixture's own `to` field. Both widths agree with each other, so this is not a cross-width bug, but SPEC §3's JSON and SPEC §5's acceptance strings still contradict each other and neither file reads the field directly. Needs a SPEC-owner ruling, not a build fix. | `people-room-1440.html:607-608`, `people-room-390.html:629` |
| CR-1 / DR-1 (design, r2) / CR2-1 (construction, r2): 390 Call Sheet vitals line wrong (`9 · 5 · 6` instead of the fixture-supported `5 · 4 · 2`) | 2 | 390 | **Fixed.** `people-room-390.html:1038` now computes the same live `tally()` over the studio+client+week cohort that `people-room-1440.html:1071-1073` uses. Both render "12 on the job this week · 5 reachable by text · 4 with accounts · 2 on paper" — confirmed on `people-room-state-roster-390.png` and `-1440.png` | `people-room-390.html:1038`; plates |
| DR-2 (design, r2) / TR-2 (technical, r2): 390's `deskFigure()` redacted `$2,500` to "the limit" | 2 | 390 | **Fixed.** `deskFigure`/`deskNote` no longer exist in the file (`grep -c deskFigure` → 0). Both files render the literal "Signs money to $2,500." / "Email first. Call for anything over $2,500." — confirmed on `people-room-state-roster-{1440,390}.png` and `-directory-{1440,390}.png` | `grep -n deskFigure people-room-390.html` → no matches; plates |
| DR-3 (design, r2): 390's role-narrow chips used 13 raw lowercase `engagement.kind` values instead of 1440's 11 studio-voice `ROLE_WORDS` | 2 | 390 | **Fixed.** `people-room-390.html:648-654` now carries the identical `ROLE_WORDS` map and `roleWord()` helper as `people-room-1440.html:991-996`, byte-for-byte. Confirmed on `people-room-state-roster-{1440,390}.png`: both read "Everyone, Studio, Client, GC, Subs, Receiver, Architect, Inspectors" / "Makers, Stager, Photographer" | `people-room-390.html:648-654` |
| DR-4 (design, r2): 390 showed "29 people · 22 firms" under the H1 on every state, not just Directory | 2 | 390 | **Fixed.** `people-room-390.html:1278` now sets `document.getElementById('head-count').hidden = (h.state !== 'state-directory')`, mirroring 1440's `#room-count` toggle. Confirmed absent on `people-room-state-{person,company,roster,add,access}-390.png` | `people-room-390.html:1278` |
| DR-5 (design, r2): 390's company-card "Held by" cell hardcodes lowercase `'the studio'` instead of reading/title-casing `doc.heldBy` like 1440 | 2 | 390 | **Still open.** `people-room-390.html:866` is unchanged: `['Held by', 'the studio']`, literal, ignoring `d.heldBy`. `people-room-1440.html:896` still reads `doc.heldBy === 'studio' ? 'The studio' : esc(doc.heldBy)`. Visibly "the studio" (390) vs "The studio" (1440) for the identical fact on `people-room-state-company-{390,1440}.png` | `people-room-390.html:866`; plates |
| DR-6 (design, r2): 390 Directory renders an extra `<h2 class="t-d3">The studio book</h2>` with no 1440 counterpart | 2 | 390 | **Still open.** `people-room-390.html:719` unchanged. `grep -n "studio book" people-room-1440.html` → no match | `people-room-390.html:719`; `people-room-state-directory-390.png` |
| DR-7 (design, r2): `docWord()` (390) forces `blocked` only for `state === 'Not on file'`; `docCells()` (1440) forces it for any `state !== 'Current'` with `blocks.length` | 2 | both | **Still open, still latent.** `people-room-390.html:607-611` and `people-room-1440.html:887` are unchanged from round 2. No document in either fixture carries `state: "Lapses in 30 days"` with a non-empty `blocks` array (`grep -c` → 0 in both), so no plate shows the divergence, but the two files still encode different rules | `people-room-390.html:607-611`, `people-room-1440.html:887` |
| TR-3 (technical, r2): 1440 had two closing braces where SPEC wants one | 2 | 1440 | **Fixed** — same item as round-1 DR-3 above |
| CR2-3 (construction, r2, blocking): Claire Bissett (F-20) / Stonehaven Tile Gallery unreachable from the Directory | 2 | both | **Fixed at 1440, regressed at 390 — see DR-2 below (new).** `people-room-1440.html:628-629` added `'F-20'` to `DIR_PEOPLE` and `'stonehaven'` to `DIR_FIRMS`; both are reachable there. `people-room-390.html:658` added `'F-20'` to `DIR_PEOPLE` but `people-room-390.html:660`'s `DIR_FIRMS` was never given `'stonehaven'` — the exact dead end CR2-3 named is back, at the other width | `people-room-1440.html:628-629` vs `people-room-390.html:658,660` |
| CR2-5 (construction, r2, blocking): no act sets an authority grant, so direction.md §6 Task 2 ("1 to confirm the authority defaulted from the agreement") is not walkable | 2 | both | **Mechanism fixed in both, but 390's implementation asserts a false fact — see DR-1 below (new).** Both files now have a person-card "Edit the authority" act and an Add-sheet "Confirm from the agreement" / "Record the authority" act, wired to real state | `people-room-1440.html` `renderPerson`/`renderAdd`; `people-room-390.html` same |
| CR2-6 (construction, r2, blocking): Add sheet's kind picker was inert | 2 | both | **Fixed in both** — `people-room-1440.html`'s `KIND_SOURCE`/`sheetFor()` and `people-room-390.html`'s `ADD_EXAMPLE`/`addSheetFor()` both re-render `#face-add` per picked kind from real fixture data. (The two mappings disagree with each other — see DR-3 below, new) | both files |
| CR2-11 (construction, r2, major, returned for ruling): should a firm whose `paper` reads "Not on file" but blocks nothing (Great Northern Bank, CPED Inspections) render dormant or blocked | 2 | both | **Still open, correctly left for Kody.** Unchanged in both files: both firms still render `Not on file` in the dormant/ink-faint tone, no `blocks` array exists for either. No SPEC token names a firm-level "Not on file" blocking rule | `people-room-1440.html:382-383` |
| CR-13 / CR2-4 (invoice/CO surface for Task 2) | 1, 2 | both | **Still correctly un-fixed**, returned for a scope ruling — SPEC §4 fixes six states; a seventh is foreclosed |
| CR-6 / CR2-2 (rolodex/travel-list picker for Task 5) | 1, 2 | both | **Still correctly un-fixed**, returned for a scope ruling — same six-state ceiling |

## Part B — fresh findings, round 3

| ID | Severity | Confidence | File : state (hash) | Claim | Fix |
|---|---|---|---|---|---|
| DR-1 | **Blocking** | High | `people-room-390.html` : `#state-add` (`people-room-state-add-390.png`, the default rendered plate) | The Add sheet's Authority region fabricates a defaulted-from-the-agreement fact that does not exist in the fixture. `addSheetFor()` (`people-room-390.html:1090-1128`) hardcodes `authority: 'No authority on this job.'` whenever the source engagement's `authority` field is empty (line 1106, the default "a sub" / Joe Wozniak F-18 sheet, whose fixture engagement carries `"authority": ""`; also line 1125's fallback, hit by "an installer"/Ingrid Halvorsen F-13, likewise `"authority": ""`), and `renderAdd()` (`people-room-390.html:1151-1153`) always prints "The agreement set this. Confirm it, or write a different one." plus a "Confirm from the agreement" act over that fabricated text — confirmed on the plate: the AUTHORITY ON THIS JOB field holds "No authority on this job." and the act reads "CONFIRM FROM THE AGREEMENT." `people-room-1440.html` gets this right for the identical fixture facts: `sheetFor()` (line 1133) sets `auth = e && e.authority ? e.authority : ''` (empty, not a fabricated string), and `renderAdd()` (lines 1195-1199) branches on that to print an empty field, "Nothing defaulted from the agreement.", and a "Record the authority" act — confirmed on `people-room-state-add-1440.png`. The two widths therefore assert opposite things about the same fixture fact (390: "the agreement set an authority of none, confirm it"; 1440, correctly: "nothing was defaulted") on the review's own default face, and this also fails "the two widths show identical facts." | In `people-room-390.html`, stop hardcoding `'No authority on this job.'` as the field value when the source engagement's `authority` is empty; set the field to `''` and branch the note/act copy the way `people-room-1440.html:1195-1199` does ("Nothing defaulted from the agreement." / "Record the authority") whenever the real authority string is empty. |
| DR-2 | **Blocking** | High | `people-room-390.html` : `#state-directory` (`people-room-state-directory-390.png`) vs `people-room-1440.html` same state | Stonehaven Tile Gallery's company card is unreachable at 390, reachable at 1440 — a regression of the exact defect round 2's CR2-3 rated blocking, now present at the other width. `people-room-1440.html:629`'s `DIR_FIRMS` is `['marrow', 'northgate', 'tcdrywall', 'stonehaven']` and its Directory firm-row list renders all four, including "Stonehaven Tile Gallery · Showroom · 1 on the crew · 2 open jobs" (visible on `people-room-state-directory-1440.png`), opening the company card via `data-company="stonehaven"` → `openCompany()` (`people-room-1440.html:1354`,`1432`). `people-room-390.html:660`'s `DIR_FIRMS` is still `['marrow', 'northgate', 'tcdrywall']` — Stonehaven never renders as a firm row (confirmed: Firms section on `people-room-state-directory-390.png` lists only three firms), and there is no other path to it: Claire Bissett's person card (F-20, reachable via `DIR_PEOPLE`) prints her firm name as plain text (`people-room-390.html:760-763`, the `meta` string), never a link, and no `data-open-company` attribute for `stonehaven` exists anywhere in the file (`grep -c stonehaven people-room-390.html` → 2 hits, both inside the `<script>` fixture data, none in rendered markup or a click target). | Add `'stonehaven'` to `DIR_FIRMS` in `people-room-390.html`, matching `people-room-1440.html:629`, so the Stonehaven Tile Gallery firm row (and its company card) is reachable at both widths. |
| DR-3 | Major | High | `people-room-390.html` : `#state-add` vs `people-room-1440.html` same state (three of the eight kind-picker options) | Picking the identical Add-sheet kind shows a different fixture person at each width. `people-room-1440.html:1118-1121`'s `KIND_SOURCE` maps "a maker" → F-20 (Claire Bissett, Stonehaven Tile Gallery), "someone else" → F-27 (Ray Thao, City of Minneapolis CPED Inspections), and has no entry for "an installer" (falls through to a blank "Nothing is added..." sheet per `sheetFor()`'s `auth`/`name` guards). `people-room-390.html:1090-1097`'s `ADD_EXAMPLE` instead maps "a maker" → F-23 (Owen Ashby, Ashgrove Millwork), "an installer" → F-13 (Ingrid Halvorsen, Halvorsen Cabinet Works), and "someone else" → F-26 (Carol Nystrom, Great Northern Bank) — three different mappings, none overlapping the 1440 set for those keys. This is not part of SPEC §5.5's strict written acceptance list (which only names the default "a sub" kind, unaffected by this divergence), but it is a real, clickable content disagreement between the two widths on a feature both files now implement (CR2-6), and it fails "the two widths show identical facts" the moment a reviewer or Leah clicks any of those three chips. | Reconcile `people-room-390.html`'s `ADD_EXAMPLE` with `people-room-1440.html`'s `KIND_SOURCE` (same six explicit keys, same fixture ids, "an installer" left unmapped at both widths so it falls to the same blank-sheet behavior), so every kind shows the same person's data regardless of width. |
| DR-4 | Minor | Medium | `people-room-390.html` : `#state-company` (`people-room-state-company-390.png`) vs `people-room-1440.html` same state | 390's W-9 row prints "No expiry, on file 14 Apr 2025" while 1440's prints "On file 14 Apr 2025" for the identical document (`heldBy`/`expires`/`issued` all equal in both fixture copies). SPEC §5.3 #3 only requires the substring "on file 14 Apr 2025" to appear, which both satisfy, so this is not a missing-acceptance-string violation, but the two widths render visibly different prose for the same fact. | In `people-room-390.html`'s `paperStack()` (or `people-room-1440.html`'s equivalent), use the same expiry-string template at both widths so the rendered sentence matches, not just the required substring. |

## Part C — spot checks that passed clean this round

- **Fixture facts** (all present, exact, identical on both widths this round): Dana Kowalski "Text only. The email on file bounces." with the bounced `dana@northgateelectric.com` and "This address bounced back, 12 March 2026..."; Pete Rusk "Opted out by text, 3 December 2025, on the Lindqvist kitchen."; Northgate Electric "COI, general liability ... Lapsed ... 31 March 2026" with "Blocks site access, payment and the draw." on the person card and the roster's held clause "Site access held. Northgate Electric's insurance lapsed 31 March 2026."; Chidi Okonkwo "Signs money to $2,500." (now literal at both widths, see Part A); Ngozi Eze "holds a key" (site access + roster); Luis Ochoa "controls the gate"; "The code is held off Patina; ask Luis Ochoa." with no code digit anywhere (`grep` for digit sequences near "lockbox"/"code" found none beyond "version 3").
- **The four word families and pigment** (§3.8): `TONE`/`WORD_PIGMENT` maps in both files still match SPEC exactly (unchanged from round 2's pixel-level check, re-confirmed by source diff — no edits to either map this round). Every `.word` prints its text plus its border colour; no colour-alone signal on any of the twelve plates viewed this round (Field link/Invited golden-bordered, Opted out/Lapsed terracotta-bordered, On the job/Current/Texting sage-bordered, On paper/Not asked/Warranty/Awarded-adjacent-dormant/Not on file/No response/Off the job ink-faint-bordered).
- **Authority as plain text**: every authority string on every plate viewed this round ("No authority on this job", "Signs money to $2,500.", "Approves fee changes.", "Prepares only.", "Controls the gate.", "Holds a key.", "Change orders, design intent. No money.") renders via `.word--plain`/`plainWord()`/`.fact`, never a bordered/coloured `.word`.
- **Marrow & Sons reads Current** — confirmed on both directory plates.
- **No forbidden vocabulary, no schema words, no hex outside the token block, no `box-shadow`/`text-overflow`/`placeholder=`/`disabled`/curly apostrophe, no "Remove"** — re-ran the full grep battery this round (see raw output below); zero hits in both files.
- **Call Sheet bands**: Studio side, Client side, "On the job · this week" (Tom Marrow, Erin Sato, Luis Ochoa, Joe Wozniak, Dana Kowalski, Ngozi Eze, Sam Rowe), "On the job · later" (all twelve required people/dates), Bidding (Rivera Finishes only, "No response"), Done (Granite North only, "Off the job") — all present, correct people, correct bands, identical on both widths' roster plates.
- **Site access card** — identical on both widths, all seven regions present in the SPEC §5.6/§6.2 order, no code digit, "Studio only. This card never reaches a client page." present.
- **Structure**: one `<h1>` per file, `<!-- specimen-complete -->` as the exact last line of both files, `aria-disabled="true"` present (not the `disabled` attribute).

Raw gate re-run:

```
$ for f in people-room-1440.html people-room-390.html; do grep -c 'box-shadow\|text-overflow\|placeholder=\| disabled' $f; done
0
0
$ grep -c $'\xe2\x80\x99' people-room-1440.html people-room-390.html
people-room-1440.html:0
people-room-390.html:0
$ grep -c "client_rep\|party_kind\|sms_consent_status\|studio_contact_id\|project_parties" people-room-1440.html people-room-390.html
people-room-1440.html:0
people-room-390.html:0
```

## Summary

```
clean: false
blocking: 2 (DR-1, DR-2)
major:    1 (DR-3)            [+ CR2-11 from round 2, still open pending Kody's ruling]
minor:    4 (DR-4, and carried-forward DR-5, DR-6, DR-7 from round 2, all still open)
open, not build defects, awaiting a ruling: CR-13/CR2-4 (invoice/CO surface), CR-6/CR2-2 (rolodex/travel-list), round-1 DR-4 (SPEC's own F-11 date contradiction)
```

`clean` is **false**. Both new blocking findings, and the new major, trace to the same root cause as round 2's: `people-room-390.html` keeps picking up round-2 fixes (CR2-3, CR2-5, CR2-6) with its own independent, not-quite-matching implementation, so the pair drifts apart again on the very features that were just reconciled. Round 2 fixed the vitals-number and dollar-figure divergences the same way, cleanly, in both files (Part A) — that pattern, applied to DR-1/DR-2/DR-3 here, is the fastest path to a clean round 4.
