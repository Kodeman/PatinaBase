# Design review, round 2

Files reviewed: `specimens/people-room-1440.html`, `specimens/people-room-390.html`, all twelve plates in `shots/`, against `synthesis/direction.md` (§1, §3, §3.8, §3.9, §5) and `specimens/SPEC.md` (§5 acceptance, §6 width, §8 forbidden). Round 1 fix log re-checked finding by finding, then a fresh pass. Every claim below is grep- or plate-verified; line numbers are from the current files on disk.

## Part A — round 1 findings, re-checked

| ID | Round 1 verdict | Round 2 status | Evidence |
|---|---|---|---|
| DR-1 (curly apostrophes) | Fixed | **Confirmed fixed** | `grep -c $'\xe2\x80\x99'` → `0` on both files |
| CR-1 (vitals literal false) | Fixed, "known deviation" | **Fixed on its own terms, but produced a new cross-width defect** — see DR-1 below | `people-room-1440.html:1066-1069` (computed), `people-room-390.html:540,1034` (hardcoded) |
| CR-2 (company card real data) | Fixed | **Confirmed fixed** in both files — `renderCompany(cid)` / `coFocus` present in both, Marrow & Sons and Northgate Electric render distinct real data on the plates. Twin Cities Drywall & Plaster (the C7 case) was not independently re-verified visually this round (no plate opens it by default; only reviewed via source, where `openCompany()`/`data-open-company` exist identically in both files) | `people-room-1440.html` company render fns; `people-room-390.html` same |
| CR-3 (person card real data) | Fixed | **Confirmed fixed** — Dana Kowalski's face byte-matches SPEC §5.2 on both plates | `people-room-state-person-{1440,390}.png` |
| CR-5 (role-narrow control) | Fixed | **Fixed as a mechanism, but the two files disagree on content** — see DR-3 below | `people-room-1440.html:987-992` vs `people-room-390.html:1035-1040` |
| CR-14 (blocked doc-word override) | Fixed | **Fixed in both, but the override's scope differs between files** (latent, not yet visible) — see DR-7 below | `people-room-1440.html:884` vs `people-room-390.html:607-612` |
| DR-2 (render.mjs merge) | Fixed | **Confirmed fixed** — `shots/people-room-console.json` holds 12 captures, 0 errors, 0 warnings, `horizontalOverflow: false` on every entry | `shots/people-room-console.json` |
| TR-2 (told-band collapsed) | Fixed (390 only, per log) | **Confirmed already correct on 1440 too** — both files initialise `aria-expanded="false"` and a `hidden` `#told-band` | `people-room-1440.html:1210-1211` |
| CR-6 (rolodex picker / travel-list pane) | Not fixed — returned to Kody | **Still open.** No seventh state exists in either file; six states confirmed (directory, person, company, roster, add, access) across all twelve plates. Task 5 of §6 (bring four people onto Okonkwo) still has no home | both files, `#state-*` ids |
| CR-13 (invoice/CO visibility for Task 2) | Not fixed — returned to Kody | **Still open.** No invoice or change-order face exists in either file | both files |
| DR-3 (extra closing brace, round 1) | Flagged as an unresolved discrepancy between files | **Still open, not independently re-resolved.** 1440's pasted token+fragment block (lines 11–127) still closes one brace more than it opens (25 opens / 26 closes) per SPEC §2's instruction; 390's file lacks the "what this specimen adds on top" marker comment round 1 used to bound the same range, so the parallel count could not be re-derived this round. Both files render without visible CSS breakage, so this stays a source-hygiene question, not a rendered defect | `people-room-1440.html:11-127` |

## Part B — fresh findings, round 2

| ID | Severity | Confidence | File : state (hash) | Claim | Fix |
|---|---|---|---|---|---|
| DR-1 | **Blocking** | High | `people-room-390.html` : `#state-roster` (`people-room-state-roster-390.png`) | The Call Sheet vitals line at 390 reads "12 on the job this week · **9** reachable by text · **5** with accounts · **6** on paper" (`people-room-390.html:540`, hardcoded `vitalsRest`). Recomputing from the same shared fixture's `studio+client+week` cohort (12 engagements: F-02, F-03, F-01, F-04, F-05, F-07, F-08, F-09, F-18, F-11, F-06, F-10) gives **5** `consent==='Texting'`, **4** `reach==='Account'`, **2** `reach==='On paper'` — exactly what `people-room-1440.html:1066-1069` computes dynamically and renders ("5 reachable by text · 4 with accounts · 2 on paper", confirmed on `people-room-state-roster-1440.png`). 390's figures are not just stale relative to 1440 — they do not match the fixture that ships inside the same 390 file. This is a wrong fact on the face, and it also fails the review's "the two widths show identical facts" check outright. | Change `people-room-390.html`'s `vitalsRest` to be computed the same way 1440 computes it (tally `consent`/`reach` over the `studio+client+week` cohort), or hand-correct the literal to `5 reachable by text · 4 with accounts · 2 on paper` to match 1440 and the fixture. |
| DR-2 | **Blocking** | High | `people-room-390.html` : `#state-roster` and `#state-directory` (`people-room-state-roster-390.png`, `people-room-state-directory-390.png`) vs `people-room-1440.html` same states | Chidi Okonkwo's authority/rule text diverges by width on the same fixture fact. 1440 prints the SPEC §5.4 #5 literal in full — "Signs money to $2,500." and "Email first. Call for anything over $2,500." (both visible on `people-room-state-roster-1440.png` and `-directory-1440.png`). 390 runs every authority/rule string through `deskFigure()` (`people-room-390.html:616`), which regexes out any `$` amount and substitutes "the limit" — so 390 reads "Signs money to the limit." / "Email first. Call for anything over the limit." with an added "The figure is on the desk." note (confirmed on `people-room-state-roster-390.png`, `-directory-390.png`). `grep -c '2,500' people-room-390.html` matches only the JSON fixture block, never the rendered face. SPEC §5.4 #5's required literal is therefore absent from the 390 face, and this is one of the three fixture facts the review brief explicitly names ("Chidi signs money over $2,500") — at 390 the dollar figure itself never appears anywhere a viewer can read it. This was a deliberate PR-t-driven choice recorded by the round-1 390 fixer as a "known deviation," but it was never applied to 1440 and never reconciled by an actual ruling — SPEC.md is unamended, so the two files now contradict both SPEC and each other on the same fact. | Get an explicit ruling from Kody on PR-t before the next round: either amend SPEC §5.4 #5 to state the mobile treatment explicitly (and apply `deskFigure`-style withholding to 1440's mobile-equivalent, if any exists) or revert 390's `deskFigure()` substitution so both widths show "$2,500" as SPEC currently requires. Whichever is chosen, make 1440 and 390 agree. |
| DR-3 | Major | High | `people-room-390.html` : `#state-roster` (`people-room-state-roster-390.png`) vs `people-room-1440.html` same state | The Call Sheet's role-narrow chip row (added for CR-5) uses two different vocabularies at the two widths. 1440 groups engagement kinds through a `ROLE_WORDS` map (`people-room-1440.html:987-992`) into the studio's own words: "Everyone, Studio, Client, GC, Subs, Receiver, Architect, Inspectors, Makers, Stager, Photographer" — 11 chips (confirmed on plate). 390 instead lists the 13 raw `engagement.kind` string values verbatim, lowercase, ungrouped (`people-room-390.html:1035-1040`): "Everyone, lead designer, bookkeeper, principal, client, household member, gc, sub, receiver, architect, inspector, maker, stager, photographer" (confirmed on `people-room-state-roster-390.png`, cropped). This is a different chip count, different words, and lowercase internal-kind strings ("gc", "sub") in place of the sentence-case studio words SPEC §8 #3 requires ("plain, present-tense, sentence-case English, the studio's words") — a voice regression as well as a fact/content mismatch between widths. | Rebuild 390's role-narrow control on the same `ROLE_WORDS`-style grouping 1440 uses, so both widths show the same 11 studio-voice words in the same order. |
| DR-4 | Major | High | `people-room-390.html` : `#state-person`, `#state-company`, `#state-roster`, `#state-add`, `#state-access` (all five 390 plates) vs the matching 1440 plates | The "29 people · 22 firms" head-count subtitle is shown/hidden inconsistently between widths on every non-directory state. 1440 explicitly hides `#room-count` unless `state === 'state-directory'` (`people-room-1440.html:1274`), matching SPEC §5.1 #1's scoping of that string to the Directory acceptance list only — confirmed absent on `people-room-state-{person,company,roster,add,access}-1440.png`. 390 sets `#head-count`'s text once at load (`people-room-390.html:1225`) and never toggles `.hidden` on it (no such call exists anywhere in the file for that id), so it prints under the H1 on **all six** 390 states — confirmed present on `people-room-state-{person,company,roster,add,access}-390.png` as well as the directory plate. | Hide `#head-count` on every state but `#state-directory` in `people-room-390.html`, mirroring 1440's `.hidden` toggle, so the two widths show the same content per state. |
| DR-5 | Minor | High | `people-room-390.html` : `#state-company` (`people-room-state-company-390.png`) vs `people-room-1440.html` same state | The company card's Paper table "Held by" cell is hardcoded to the literal `'the studio'` in 390's `paperStack()` (`people-room-390.html:865`), rather than reading `doc.heldBy` and title-casing it the way 1440 does (`doc.heldBy === 'studio' ? 'The studio' : esc(doc.heldBy)`, `people-room-1440.html:889`). Visibly, 1440 prints "**The studio**" (capital T) and 390 prints "**the studio**" (lowercase t) for the identical fact — confirmed on both company-card plates. Every document in the current fixture happens to have `heldBy: "studio"`, so no wrong holder is shown today, but 390's version would silently print "the studio" even if a future document's `heldBy` were a third party, since the value is never read. | In `people-room-390.html`, replace the hardcoded `'the studio'` with the same conditional 1440 uses, reading `d.heldBy`. |
| DR-6 | Minor | High | `people-room-390.html` : `#state-directory` (`people-room-state-directory-390.png`) vs `people-room-1440.html` same state | 390's Directory view renders an extra `<h2 class="t-d3">The studio book</h2>` heading (`people-room-390.html:717`, visible at the top of `people-room-state-directory-390.png` under the H1) that has no counterpart anywhere in `people-room-1440.html` (`grep -n "studio book\|THE BOOK"` returns nothing for the 1440 file). Not forbidden vocabulary, but it is face content present at one width and silently absent at the other. | Either add the same heading to 1440 for parity, or remove it from 390 — whichever the direction intends for the Directory's IA (direction.md §2.1 calls the rail entry "THE BOOK", which may be the intended source of this heading; if so it belongs at both widths, not one). |
| DR-7 | Minor | High (code-level); latent (not yet visible) | `people-room-1440.html:884` vs `people-room-390.html:607-612` | The CR-14 "force a Not-on-file/Lapsed document to the blocked/terracotta tone" fix has different scope in the two files. 1440's `docCells()` forces `blocked` for **any** document whose `state !== 'Current'` **and** has a non-empty `blocks` array — this would also force a hypothetical `"Lapses in 30 days"` document with blocks to terracotta instead of its normal golden/pending tone. 390's `docWord()` only forces `blocked` when `state === 'Not on file'` specifically, leaving any `"Lapses in 30 days"` document at its normal pending tone. No document in the current fixture has state `"Lapses in 30 days"` with a non-empty `blocks` array (`grep -c '"state": "Lapses in 30 days"'` → 0 in both files), so this divergence does not manifest on any of the twelve plates today — but the two files now encode different rules for the same finding, and will disagree the day such a document is added. | Align 390's `docWord()` condition with 1440's `docCells()` condition (or vice versa), so both files force the blocked tone under the same rule. |

## Part C — spot checks that passed clean

Recorded for completeness since the brief asks to verify these explicitly; no defect found.

- **Fixture facts**: Dana Kowalski text-only with a dead/bounced email; Pete Rusk opted out by text 3 December 2025 on the Lindqvist kitchen; Northgate Electric's COI lapsed 31 March 2026 blocking site access, payment and the draw; Ngozi Eze holds the key; Luis Ochoa controls the gate; the lockbox code held off Patina — all present, verbatim, identically on both widths (`grep` cross-check against both files, plate cross-check for directory/person/company/access states).
- **Four word-family pigments** (§3.8): both files' tone maps (`TONE` in 1440, `WORD_PIGMENT` in 390) match SPEC exactly — Account/Texting/On the job/Current → sage; Field link/Invited/Awarded/Lapses in 30 days → golden; Opted out/Lapsed → terracotta; On paper/Not asked/Warranty/No response/Off the job/Not on file → ink-faint/dormant. Every `.word` renders its text plus its border colour together (`.word--*` CSS rules checked); no colour-alone signal found.
- **Authority as plain text**: `people-room-1440.html:833` and `people-room-390.html:814,989` both render authority via a plain, uncoloured element (`.fact` / `.word--plain`), never a bordered/coloured `.word` chip.
- **Marrow & Sons reads Current**: confirmed on both directory plates (bordered "CURRENT" word beside the firm row).
- **Forbidden vocabulary**: no "specimen"/"prototype"/"example"/"Lorem"/"TBD" inside the mocked page content on either file (only inside the state bar / an HTML comment, which SPEC §8 #8 explicitly exempts). No `box-shadow`, `text-overflow`, `placeholder=`, ` disabled`, or curly apostrophe in either file. No stray hex literal outside the pasted token block in either file. The only "crm" occurrences are CSS class-name prefixes (`.crm-row`, `.crm-card`, etc.), not visible face text.
- **Call Sheet bands**: Studio side (Priya, Dale, Leah), Client side (Adaeze, Chidi), On the job · this week (Tom Marrow, Erin Sato, Luis Ochoa, Joe Wozniak, Dana Kowalski, Ngozi Eze, Sam Rowe), On the job · later (11 people, correct dates), Bidding (Rivera Finishes only), Done (Granite North only) — all present with the right people in the right band, identically on both widths' roster plates, aside from the vitals-line numbers (DR-1).
- **Render/overflow**: `shots/people-room-console.json` — 12 captures, 0 errors, 0 warnings, `horizontalOverflow: false` on every entry, both widths.
- **No bare status dot**: only `border-radius: 50%` uses in either file are the person-avatar circles; no colour-only state indicator exists.

## Summary

```
clean: false
blocking: 2 (DR-1, DR-2)   [+ CR-13 from round 1, still open]
major:    2 (DR-3, DR-4)   [+ CR-6 from round 1, still open]
minor:    3 (DR-5, DR-6, DR-7)
```

`clean` is **false**: two new blocking findings (DR-1, DR-2) plus the round-1 CR-13 blocking finding, which is still unaddressed. Both new blocking items trace to the same root cause — 390's round-1 fixes (CR-1's vitals recompute, CR-4's desk-figure withholding) were applied only to 390 and never reconciled against 1440, so the two widths that SPEC §3 requires to "show identical facts" now actively disagree on two of the fixture facts this review was asked to verify.
