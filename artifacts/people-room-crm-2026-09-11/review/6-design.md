# Design review, round 6

Files reviewed: `specimens/people-room-1440.html` (97,564 bytes, unchanged since round 5), `specimens/people-room-390.html` (103,070 bytes, unchanged since round 5), all fourteen plates in `shots/` (`people-room-1440-state-*-1440.png`, `people-room-390-state-*-390.png`), `shots/people-room-1440-console.json` and `shots/people-room-390-console.json`, against `synthesis/direction.md` (§1, §3, §3.8, §3.9, §5, §6) and the amended `specimens/SPEC.md` (§4, §5.1–§5.8, §6, §8), `rulings.md` §3 (R-A..R-U, settled, not re-litigated), `review/5-design.md`, and `review/fix-log-r5.md`.

**Method.** Every claim below is one of: (a) a grep/line citation against the files on disk, (b) a plate crop viewed directly, or (c) a live Playwright render (`node_modules/.pnpm/playwright@1.58.2`, sandbox disabled per SPEC §9) navigated to a hash or driven by a click, with `document.body.innerText` (or a scoped element's `innerText`/`outerHTML`) captured and diffed programmatically. Two automated passes were run beyond manual reading: (1) a full-page innerText multiset diff per state, normalized by splitting on newlines/tabs and the middle-dot separator (reproducing `fix-log-r5.md`'s own method) — this reproduced round 5's exact reported counts (`state-directory` 216/216 0 diffs, `state-person` 70/71 1 diff, `state-company` 77/98 7 keys × 3 extra = 21 diffs, `state-roster`/`state-pick`/`state-add`/`state-access` all 0 diffs), confirming `fix-log-r5.md`'s content-parity claims are accurate and nothing has regressed since; (2) a click-through pass opening **every** Directory-reachable person and firm card at both widths, which is what surfaced this round's blocking finding (DR6-1) — a defect invisible to the seven default-hash plates because the one person the default `#state-person` hash opens (Dana Kowalski, F-11) is the one person in the fixture who happens to have all three of a rule, a grant, and a seat.

Console gate: both `people-room-*-console.json` report `"errors": []`, `"warnings": []`, `"horizontalOverflow": false` on all fourteen captures — reconfirmed, unchanged from round 5.

## Part A — every round-5 finding, re-checked

| ID | Status now | Evidence |
|---|---|---|
| DR5-1 (blocking) — R-L routed line missing on 390's Directory/roster rows | **Fixed.** `people-room-390.html`'s `dirPersonRow()` (:745) and `rosterPersonRow()` (:757) both call the shared `clauseHtml(clausesFor(p))` (:648-660), matching 1440's call sites. Live-rendered: Frank Bauer's Directory row and Call Sheet row both print "Do not contact directly. Write Rosa Delgado instead." followed by "Write Rosa Delgado · rosa@twincitiesdrywall.com · (612) 555-0114" (phone `tel:`-linked) at **both** widths. Confirmed by plate crop (`people-room-390-state-directory-390.png`, Frank Bauer's row) and by the innerText diff reporting 0 differences on `state-directory` and `state-roster`. | 
| DR5-2 (blocking) — Call Sheet unfold consent sentence worded differently by width | **Fixed.** Both files' `consentSentence()` render "Written consent, 2 May 2025, on the Lindqvist kitchen." for Dana Kowalski's unfold. Confirmed on `people-room-1440-state-roster-1440.png` and `people-room-390-state-roster-390.png` (both show the identical sentence in the unfolded panel) and by the roster innerText diff (0 differences). |
| DR5-3 (major) — Claire Bissett's row rendered a blank gap where a consent word belongs | **Fixed.** `people-room-1440.html:430` and `people-room-390.html:413` both carry `"consent": "Not asked"` for F-20. 1440's Directory row now prints three bordered words (`ON PAPER`, `NOT ASKED`, `CURRENT`); 390's prints the plain-inline `On paper · Not asked · Current`. Confirmed on both directory plates (cropped). |
| DR5-4 (major) — company Paper region order diverged (consequence-before-acts at 1440, after at 390) | **Fixed.** Both files' company-card Paper region now runs table → leading-rule clause → consequence sentence → act row. Confirmed on `people-room-1440-state-company-1440.png` and `people-room-390-state-company-390.png` (both show the "This drafts a note to Northgate Electric's paperwork contact…" sentence directly above the Record/Chase buttons). |
| DR5-5 (minor) — Ingrid Halvorsen's rule/history order diverged in Bring forward | **Fixed.** Both files' pick-row markup now prints the rule clause ("Email only. No cell for work.") before the history line. Confirmed on both `state-pick` plates. |
| DR5-6 (minor) — seats-disclosure label "1 SEAT"/"0 SEATS" at 1440 vs bare "SEATS" at 390 | **Fixed.** 390's `dirPersonRow()` (:748) now emits `count + ' seat' + (count===1?'':'s')`, matching 1440. Confirmed on both directory plates — every row reads "1 SEAT" or "0 SEATS" at both widths. |
| DR5-7 (minor) — 390's Directory seat panel printed the carried-forward consent note, 1440's never did | **Fixed.** 390's seat `<li>` (:761-765) now prints only the seat line and the stage word, no consent note; matches 1440. |

All seven of round 5's own findings are genuinely closed. The four items round 5 carried forward as still-open (not new, not re-scored) remain exactly as `fix-log-r5.md` left them, confirmed unchanged on disk:

| Carried item | Status | Evidence |
|---|---|---|
| CR2-7/CR3-4/CR4-1 — "licence" British spelling | **Still open, correctly untouched.** Lives in the verbatim §3 fixture JSON (`"MN BC contractor licence"`, `"MN electrical contractor licence"`), a fixture/SPEC-owner decision, not a builder defect. | `people-room-1440.html:374,383`; `people-room-390.html:357,366` |
| CR2-8/CR3-5/CR4-2 — "Compare & merge" band names no cards, wires no handler | **Still open.** `grep -c "act--secondary"` on the band shows the button has no `data-*` target and the click-delegation chain in both files has no handler for it; clicking it live does nothing (no `aria-pressed`, no `data-go`, falls through every branch in the click listener). Awaiting a panel ruling on which two cards and what the act opens. | `people-room-1440.html:753`, `people-room-390.html:818` |
| CR3-6/CR2-9/CR4-3 — no schedule-authority sentence in the fixture | **Still open.** No fixture field exists for it. | unchanged |
| CR3-7/CR2-10/CR4-4 — homeowner "Holds a key" reads identically to the site access card's one designated key holder | **Still open.** `ACCESS_WORDS['key']`/`accessWords('key')` both return the literal "Holds a key" for Ngozi Eze (F-06, the site-access key holder), Adaeze Okonkwo (F-04) and Chidi Okonkwo (F-05) alike — three people carry the identical phrase with no cue distinguishing the one who actually holds the physical key from the two who merely have a key-tier access grant. | `people-room-1440.html:774`, `people-room-390.html:723` |

No prior finding is misreported as fixed here that the files contradict. Round 5's work is real and complete on its own terms.

## Part B — fresh findings, round 6

| ID | Severity | Confidence | File : state (hash) | Claim | Fix |
|---|---|---|---|---|---|
| **DR6-1** | **Blocking** | High | `people-room-390.html` : `#state-person`, reached from `#state-directory` | 390's `renderPerson()` renders the "Contact rule", "Access grants", and "Seats on projects" regions **only when data exists**, with no `else` branch — `if (r) { … }` (:881, no else), `if (g) { … }` (:891, no else), `if (e) { … }` (:899, no else). 1440's `renderPerson()` renders all three unconditionally, printing an explicit fallback when the record is absent: `else { H.push('<p class="t-body-sm">No contact rule on file.</p>'); }` (:858), `else { H.push('<p class="t-body-sm">No grant on file.</p>'); }` (:867), `else { H.push('<p class="fact">No open seat on this project.</p>'); }` (:888-889). `grep -c "No contact rule on file\|No grant on file\|No open seat on this project" people-room-390.html` → **0** in all three; the same grep on `people-room-1440.html` → 1 each (lines 858, 867, 888). Live-rendered proof: clicking Claire Bissett's row (F-20, one of the twelve Directory rows shown by default at both widths, per SPEC §5.1 #5/#6's own listing) opens a person card that, at 1440, reads Channels → Contact rule "No contact rule on file." → Access grants "No grant on file." → Seats on projects "No open seat on this project." — four labelled regions. At 390 the identical click opens a card that ends abruptly after the Channels list; none of the other three regions exist in the DOM at all, not even their `<h3>`/`.sub-head` labels. This is not a one-person edge case: of the twelve people the Directory shows, **only Dana Kowalski (F-11, the default `#state-person` hash target) happens to carry a rule, a grant, and a seat all at once** — every other clickable row (Adaeze Okonkwo, Chidi Okonkwo, Tom Marrow, Erin Sato, Luis Ochoa, Joe Wozniak, Pete Rusk, Rosa Delgado, Frank Bauer, Ray Thao, Claire Bissett) is missing at least one of the three regions at 390 that it correctly shows (with an honest "No X on file" sentence) at 1440. Verified by an automated click-through of Chidi Okonkwo, Tom Marrow, Erin Sato, Luis Ochoa, Pete Rusk, Rosa Delgado, Frank Bauer, Joe Wozniak, and Ray Thao at both widths: each shows the same pattern (390 silently drops the region 1440 states as absent). Because the default `#state-person` hash happens to land on the one exception, this is invisible on the seven graded plates alone and needs a click-through pass to surface — but it is one click away from the required Directory listing, in both widths' identical, always-rendered default face, and it means the two widths disagree on whether Claire Bissett (etc.) has a contact rule, a grant, or a seat: 1440 states the fact ("no"); 390 says nothing, which reads as the card being broken or incomplete rather than as an honest absence. | In `people-room-390.html`, add the missing `else` branches to the `if (r)`, `if (g)`, and `if (e)` blocks in `renderPerson()` (around :881, :891, :899), printing the sub-head plus "No contact rule on file." / "No grant on file." / "No open seat on this project." exactly as 1440 does, so every person's card shows the same four Reach & access + Seats regions at both widths regardless of which records that person happens to hold. |
| **DR6-2** | Major | High | `people-room-1440.html` vs `people-room-390.html` : `#state-company` (Northgate Electric, the default/graded plate) | The "Crew & designations" line's interactive scope differs by width for the identical fact. 1440's `renderCompany()` (:971) wraps only the person's **name** in the clickable control: `personLink(m) + ' · ' + esc(bits.join(' · '))`, where `personLink()` (:796-798) emits `<button type="button" class="link link-act" data-person="F-11">Dana Kowalski</button>` — accessible name "Dana Kowalski". 390's `renderCompany()` (:1011) wraps the **entire line** in one anchor: `<a class="linkline" href="#state-person" data-open-person="F-11"><span>' + esc(crewLine(cid, pid)) + '</span></a>`, where `crewLine()` (:998-1005) returns `"Dana Kowalski · owner-operator · paperwork contact · signer · site contact · holds the trade licence"` — so the entire sentence, not just the name, is the link's accessible name and visual underline. Confirmed by DOM query on the live-rendered `#state-company` face at both widths: 1440's element is `<button class="link link-act" data-person="F-11">Dana Kowalski</button>` (text "Dana Kowalski"); 390's is `<a class="linkline" href="#state-person" data-open-person="F-11"><span>Dana Kowalski · owner-operator · paperwork contact · signer · site contact · holds the trade licence</span></a>` (text the full sentence). Also confirmed visually: on `people-room-1440-state-company-1440.png` only "Dana Kowalski" is underlined; on `people-room-390-state-company-390.png` the whole line is underlined. This is present on the graded default `#state-company` plate at both widths — it is not a §6.2-licensed layout difference (§6.2 says nothing about narrowing or widening a crew-line's link scope; it only speaks to tables becoming label-over-value stacks). | Pick one scope for the crew-line's click target — likely 1440's (name-only), since SPEC §7 #6 already establishes the principle that an open-person control's accessible name should be "the person's name … only" for the Directory row, and the same principle reads better here — and match the other width's markup to it. |
| **DR6-3** | Major | High | `people-room-1440.html` vs `people-room-390.html` : `#state-access` (the default/graded plate), "Who to call first" | The three "Who to call first" entries have a different interactive scope by width for the identical fact. 1440's `renderAccess()` (:1404-1406) wraps only the **phone number** in the `tel:` link: `esc(P[c.person].name) + ', ' + esc(c.role) + ', ' + tel(c.phone)`, i.e. `Luis Ochoa, superintendent, <a class="tel" href="tel:+16125550109">(612) 555-0109</a>` — link text "(612) 555-0109" only. 390's `renderAccess()` (:1383-1386) wraps the **whole line**: `const line = nameOf(c.person) + ', ' + c.role + ', ' + c.phone; '<a class="tel tel--line" href="…">' + '<span>' + esc(line) + '</span></a>'` — link text "Luis Ochoa, superintendent, (612) 555-0109". Confirmed by DOM query on the live-rendered `#state-access` face: 1440's first call-first `<a>` is `<a class="tel" href="tel:+16125550109">(612) 555-0109</a>`; 390's is `<a class="tel tel--line" href="tel:+16125550109"><span>Luis Ochoa, superintendent, (612) 555-0109</span></a>`. Visually confirmed on both plates (1440's underline covers only the digits; 390's covers the full sentence for all three call-first rows). Same pattern as DR6-2, on a different graded state. | Same resolution direction as DR6-2 — decide whether the whole line or only the phone number should be the tap target, and apply that choice at both widths. (390's larger tap target may be a deliberate, reasonable mobile adaptation; if so, name it in SPEC §6.2 rather than leaving it as an unstated divergence.) |
| **DR6-4** | Minor | High | `people-room-1440.html` vs `people-room-390.html` : Marrow & Sons company card (`#state-company`, reached by clicking the Marrow & Sons Directory firm row — not one of the seven default hashes, but one click from the graded `#state-directory` face at both widths) | The "Crew & designations" list prints Marrow & Sons' three-person crew in a different order at each width. 1440's `crewOf()` (:781) is a plain fixture-order filter, producing **Tom Marrow, Erin Sato, Luis Ochoa** (F-07, F-08, F-09, the order they appear in `FIXTURE.persons`). 390's `crewOf()` (:969-976) instead builds the order from the firm's `designations` object first (`paperworkContact`, then `signer`, then `siteContact`, each pushed only if not already present) before appending any remaining crew — for Marrow & Sons (`designations: { paperworkContact: "F-08", signer: "F-07", siteContact: "F-09" }`) this produces **Erin Sato, Tom Marrow, Luis Ochoa**. Confirmed by live-rendered click-through at both widths: 1440 prints the crew lines in the order Tom Marrow / Erin Sato / Luis Ochoa; 390 prints Erin Sato / Tom Marrow / Luis Ochoa. (Twin Cities Drywall & Plaster and Northgate Electric happen not to exhibit this — their designations order coincides with, or is trivial relative to, fixture order — so this is specific to Marrow & Sons, the one firm whose `paperworkContact` isn't also its first-listed crew member.) | Make 390's `crewOf()` return crew in fixture order like 1440's, or make 1440's crew order designation-led like 390's — either is fine, but the same firm's crew should list in the same order at both widths. |
| **DR6-5** | Minor | High | `people-room-1440.html` and `people-room-390.html` (identical at both widths — not a parity defect, but a genuine wording-quality issue present everywhere `clausesFor`/`clauseHtml`/`routeWrite` combine) | The routed-contact clause reads with an immediate, jarring repetition of "Write Rosa Delgado". Live-rendered text (identical at both widths, confirmed by DOM query on Frank Bauer's Directory row): `"Do not contact directly. Write Rosa Delgado instead.Write Rosa Delgado · rosa@twincitiesdrywall.com · (612) 555-0114"` — the fixture's own rule text (`"Do not contact directly. Write Rosa Delgado instead."`, `people-room-1440.html:478` / `people-room-390.html:461`) already ends by naming Rosa Delgado, and `routeWrite()` (`people-room-1440.html:650-654`, `people-room-390.html:632-636`) unconditionally prefixes its own output with `'Write ' + esc(to.name)`, so the name and the verb "Write" both print twice back-to-back on the same visual clause (two lines, but read as one continuous sentence). This appears wherever a blocked, routed rule renders: Frank Bauer's Directory row, his Call Sheet roster row, and Twin Cities Drywall & Plaster's company-card crew line (all confirmed at both widths, since the helper functions are now shared verbatim per `fix-log-r5.md`'s "Shared logic, one source" section). This doesn't miss any SPEC-named literal — §5.8's "Routed line (R-L)" rule and §5.1 #10/§5.4 #12's acceptance text both literally describe "'Write Rosa Delgado instead.' followed by her email and her office phone as a tel: link", which is exactly what's built — so this is arguably SPEC-compliant-as-written rather than a builder defect; it is reported because the resulting sentence reads redundantly regardless of which document authorized it. | Either drop the leading `'Write ' + esc(to.name)` from `routeWrite()`'s output when it is appended as a `.clause-route` span directly after a rule that already ends in "…instead." (keep the "Write <name>" lead-in only for call sites where `routeWrite()` stands alone, e.g. the person-card contact-rule paragraph), or amend the fixture rule text to not pre-name Rosa Delgado, so the reader isn't told "Write Rosa Delgado" twice in one breath. |

## Part C — spot checks that passed clean this round

- **Automated word-level parity** (see Method): `state-directory` 216/216 (0 diffs), `state-person` 70/71 (1, the licensed "channel" header repeat), `state-company` 77/98 (21, the licensed 7-header × 3-extra-document repeat), `state-roster` 291/291, `state-pick` 70/70, `state-add` 48/48, `state-access` 35/35 — all zero unlicensed differences, reproducing `fix-log-r5.md`'s exact figures independently.
- **Head count arithmetic**: recomputed from the pasted fixture JSON directly (not trusted from the rendered string) — 29 `persons` entries, 22 `companies` entries, and the vitals line's own arithmetic (`12` on-the-job-this-week engagements, of which `5` texting-consent, `4` account-reach, `2` on-paper-reach) all check out exactly against "12 on the job this week · 5 reachable by text · 4 with accounts · 2 on paper".
- **Great Northern Bank company card** (click-through, both widths): byte-identical live-rendered text, including the TR5-5 fix (Carol Nystrom's "Never text. Email and phone only." now prints at both widths) and the "No paper is held for this firm." line with no act row.
- **Forbidden-vocabulary and structural gates**: re-ran independently — `box-shadow`, `text-overflow`, `placeholder=`, bare ` disabled` all 0 in both files; `aria-disabled="true"` present, `<h1>` count 1, `role="status"` count 1, `<style>`/`<script>` 1/1 each, hex literals after line 90 → 0 in both, `tail -1` → `<!-- specimen-complete -->` in both, file sizes match `fix-log-r5.md`'s reported gate exactly (97,564 / 103,070 bytes).
- **Reach·consent·paper row grammar**: 1440's twelve Directory person rows all carry exactly three bordered `.word` columns (no more, no less) since the Claire Bissett fix; 390's twelve rows all carry the plain-inline `reach · consent · paper` triplet on every row, folded or not, per R-M.
- **Bring forward (`#state-pick`)**: five rows in the required order, act-row-then-consequence, live checkboxes and Put back, at both widths — unchanged and correct.
- **`tel:` link targets**: every visible phone number on every graded plate is a `tel:`-hrefed control at both widths (the DR6-2/DR6-3 finding is about how much text sits inside the link, not whether the link exists or works).

## Summary

```
clean: false
blocking: 1 (DR6-1)
major:    2 (DR6-2, DR6-3)
minor:    3 (DR6-4, DR6-5, and the four round-5-carried opens restated in Part A —
             CR2-7/CR3-4/CR4-1, CR2-8/CR3-5/CR4-2, CR3-6/CR2-9/CR4-3, CR3-7/CR2-10/CR4-4 —
             counted once here, not re-scored as new)
round 5's own seven findings (DR5-1..DR5-7): all seven fixed and reconfirmed independently
```

`clean` is **false**. Round 5's work holds up completely under a fresh, automated re-check — nothing it claimed fixed has regressed, and the word-level parity across all seven states is exactly as good as `fix-log-r5.md` reported. This round's yield came entirely from testing beyond the seven default-hash plates: a systematic click-through of every Directory-reachable card surfaced one blocking, systemic gap (DR6-1) that the graded plates cannot show, because the fixture's own choice of default person (Dana Kowalski) happens to be the one person in the room who owns a rule, a grant, and a seat all at once — masking a missing-`else` bug that leaves every other person's card looking broken at 390. The two major findings (DR6-2, DR6-3) are a real, repeated pattern — 390 wraps whole descriptive lines in a single link where 1440 wraps only the specific value — visible on the graded default company and access plates without needing any click-through at all. The remaining findings are narrower: one firm's crew-order divergence and one wording redundancy, both real but modest in reach.
