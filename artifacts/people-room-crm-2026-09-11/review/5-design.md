# Design review, round 5

Files reviewed: `specimens/people-room-1440.html`, `specimens/people-room-390.html`, all fourteen plates in `shots/` (`people-room-1440-state-*-1440.png`, `people-room-390-state-*-390.png` — the round-4 set, timestamped after both files' last edit), `shots/people-room-1440-console.json` and `shots/people-room-390-console.json`, against `synthesis/direction.md` (§1, §3, §3.8, §3.9, §5, §6) and the amended `specimens/SPEC.md` (§4, §5.1–§5.7, §6, §8), orchestrator rulings R-A..R-L (`rulings.md` §3 plus R-F..R-L given in this round's brief), and `review/4-design.md` / `review/fix-log-r4.md` re-checked line by line against the files on disk. Every claim below is grep- or plate-verified; several were confirmed by cropping specific rows out of the full-page plates with a local PIL script (crops not checked in, evidence given as file:line plus the plate name).

Console gate: all fourteen captures report `"errors": []`, `"warnings": []`, `"horizontalOverflow": false`.

**Where this round differs from round 4's own bookkeeping**: `review/fix-log-r4.md` documents only `people-room-1440.html`'s changes in detail (its heading literally scopes to "Builder A, the 1200 studio band at 1440"); there is no `fix-log-r4-390.md` or equivalent. `people-room-390.html` was in fact re-edited in the same window (mtime 09:57, between 1440's 09:52 SPEC edit and its own 09:58 edit, both before the plates were re-rendered at 09:58/10:00) and most of R-F..R-K did land there — confirmed below — but R-L did not, and the fix-log's own "Deviations recorded" section already flags this half-landing ("As of this writing 390's Frank Bauer Directory and roster rows do not yet carry the routed line R-L requires; that is the 390 builder's item"). That flagged gap is still present on disk; see DR5-1.

## Part A — every round-4 finding, re-checked

| ID | File(s) | Status now | Evidence |
|---|---|---|---|
| DR4-1 (blocking) — gnbank/cped absent from `DIR_FIRMS` | both | **Fixed.** `DIR_FIRMS` is `['marrow', 'northgate', 'tcdrywall', 'stonehaven', 'gnbank', 'cped']`, byte-identical in both files; both render as wordless firm rows under Everyone and Firms, no payee marker, matching R-H/C18 | `people-room-1440.html:674`, `people-room-390.html:689`; plates `people-room-1440-state-directory-1440.png`, `people-room-390-state-directory-390.png` (Firms sections end with Great Northern Bank / City of Minneapolis, CPED Inspections, both wordless) |
| DR4-2 (blocking) — vitals literal didn't match the fixture tally | both | **Fixed by ruling (R-F/C16).** SPEC §5.4 #3 now reads the computed tally; both faces print "12 on the job this week · 5 reachable by text · 4 with accounts · 2 on paper" verbatim | `people-room-1440.html:1120`, `people-room-390.html:1075`; plates `*-state-roster-*` |
| DR4-3 (major) — stage rendered as a fourth person-row word column, against direction.md C1 | both | **Fixed by ruling (R-G/C17).** SPEC §5.1/§6.1 amended to state exactly three row columns (reach, consent, paper); both files' `dirPersonRow()` now emit exactly 3 `.word`/`.word-gap` cells on the row and print stage only inside the seat/unfold panel | `people-room-1440.html:678-707` (3-cell `.row-words`, stage moved to `wordEl(e.stage,'word--inline')` on the seat `<li>`); `people-room-390.html:691-722` (`.prow-line2` carries only plain-text reach; unfold's `words` var is `[reach, consent, paper]`, stage is the separate `word(e.stage)` on the seat line) |
| DR4-4 (minor) — Dana's bring-forward row word order diverged (1440: paper,reach,consent; 390: reach,consent,paper) | 390 | **Fixed.** 390's `pickRow()` now builds `pw + word(p.reach) + (row.consent ? word(p.consent) : '')` = paper, reach, consent, matching 1440 | `people-room-390.html:1249-1251`; plates `*-state-pick-*` both read "LAPSED / FIELD LINK / TEXTING" for Dana |
| DR4-5 (major) — bring-forward footer act-row/consequence order diverged by width | 390 | **Fixed.** 390's `renderPick()` now pushes the act row (terminal "Add four to the roster" then secondary "Put back") first, consequence paragraph after, matching 1440 | `people-room-390.html:1270-1274` |
| DR4-6 (major) — 390's pick checkboxes / Put back were inert | 390 | **Fixed.** `document.addEventListener('change', …)` recomputes via `pickUpdate()`; `.js-putback` click handler clears all five boxes and calls `pickUpdate()` | `people-room-390.html:1281-1305` (`pickUpdate`), `:1485-1489` (Put back), `:1513-1518` (change listener) |
| DR4-7 (major) — add-sheet authority note wording diverged by width | both | **Fixed.** Both files' non-empty branch now reads "Defaulted from the agreement. Confirm it, or write a different one." | `people-room-1440.html:1355`, `people-room-390.html:1199` |
| CR2-7 / CR3-4 (minor, returned) — "licence" British spelling | both | **Still correctly un-fixed.** Lives inside the verbatim §3 fixture JSON (`"MN BC contractor licence"`, `"MN electrical contractor licence"`); a SPEC/fixture-owner edit, not a builder defect | `people-room-1440.html:374,383`; `people-room-390.html:351,360` |
| CR2-8 / CR3-5 (major, returned) — "Compare & merge" band names no cards, wires no handler | both | **Still correctly un-fixed**, awaiting a panel ruling on which two cards and what the act opens | `people-room-1440.html:752-753`, `people-room-390.html:765` |
| CR3-6 / CR2-9 (minor, carried) — no schedule-authority sentence in the fixture | both | **Still open**, needs a fixture-owner addition | unchanged |
| CR3-7 / CR2-10 (minor, carried) — homeowner "Holds a key" reads identically to the site access card's one designated key holder | both | **Still open**, needs a fixture cue | unchanged |
| Round-1 DR-4 (R-D) — F-11 seat window fixture date | both | **Still resolved.** `grep -c "2027-03-13"` → 0 in both; `grep -c "2027-08-13"` → 10 in both | both files |
| CR3-1/CR-6/CR2-2 (R-C) — `#state-pick` | both | **Still built and complete** at both widths | `people-room-1440.html:1186-1225` region, `people-room-390.html:1222-1263` region |
| CR3-2/CR-13/CR2-4 (R-B) — Task 2 acceptance, no invoice/CO screen | both | **Still holds.** Fresh grep for "invoice"/"change order" outside the two legitimate authority-prose hits ("Prices change orders", "Signs sub payments") returns nothing | both files |
| R-K (TR4-2, carried into R-K) — Paper region on every company card | both | **Fixed at both.** `renderCompany()`'s `documents`-empty branch now prints paper word "Not on file" + act "Record a document" (1440 `:1028-1032`; 390 `:973-975`); the lender/inspector branch is unchanged (one line, no act) | `people-room-1440.html:981-1009`, `people-room-390.html:965-980` |

No prior finding is misreported as fixed by `fix-log-r4.md` that this re-check contradicts, **except** that the fix-log's own prose already disclosed the one gap (R-L at 390) that this round elevates to a scored, blocking finding (DR5-1) — it was recorded as a to-do, not hidden as done.

## Part B — fresh findings, round 5

| ID | Severity | Confidence | File : state (hash) | Claim | Fix |
|---|---|---|---|---|---|
| DR5-1 | **Blocking** | High | `people-room-390.html` : `#state-directory` and `#state-roster` | R-L's routed-contact line ("Write Rosa Delgado · rosa@twincitiesdrywall.com · (612) 555-0114", email then office phone as a `tel:` link) is wired at 1440 into all three places a blocked-rule clause renders — Directory row (`clauseHtml(clausesFor(p))`, `people-room-1440.html:695`, helper at `:656-668`), Call Sheet roster row (`clauseHtml(clausesFor(p))`, `:1059`), and the company card (`routeWrite(r.routeTo)`, `:973`) — but at 390 the equivalent `routedLine(to)` helper (`people-room-390.html:591-597`) is called from exactly **one** site, the company card (`:958-959`). The Directory row's rule-clause print (`people-room-390.html:708`: `if (r) { out += '<p class="row-rule…">' + esc(r.text) + '</p>'; }`) and the roster row's (`:1030`, identical shape) never check `r.routeTo` and never call `routedLine`. Confirmed on the rendered face: `people-room-390-state-directory-390.png`'s Frank Bauer row prints only "Do not contact directly. Write Rosa Delgado instead." with no email/phone beneath it (crop verified, y≈5750-6050 of the 8810px plate), and `people-room-390-state-roster-390.png`'s Frank Bauer row is identical (crop verified, y≈9500-9700 of the 14194px plate) — both a bare instruction with no way to actually reach Rosa Delgado. The 1440 equivalents (`people-room-1440-state-directory-1440.png`, `people-room-1440-state-roster-1440.png`) both print the full routed line with the phone as an underlined `tel:` link. This fails SPEC §5.1 #10 ("printing Rosa Delgado's email … and her office phone as a tel: link") and §5.4 #12 (identical requirement) at 390, and is a direct instance of "the two widths disagreeing on a fact" — 1440 shows a reachable channel, 390 shows a dead end, for the identical fixture fact. `review/fix-log-r4.md`'s own "Deviations recorded" section already named this exact gap as owed to the 390 builder; it was not subsequently closed. | In `people-room-390.html`, extend the Directory row's clause print (`:708`) and the roster row's clause print (`:1030`) to check `r.routeTo` and append `routedLine(person(r.routeTo))` the same way the company card already does at `:958-959`, so all three faces carry the routed line at 390 as they do at 1440. |
| DR5-2 | **Blocking** | High | `people-room-1440.html` vs `people-room-390.html` : `#state-roster`, Dana Kowalski's unfolded row | The Call Sheet row-unfold's consent sentence is worded differently at each width for the identical fixture fact (F-11's `consents` record: `source: "written", recorded: "2025-05-02", originJob: "Lindqvist kitchen"`). 1440's `consentSentence()` (`people-room-1440.html:613-618`) renders `SOURCE_WORDS[c.source] + ', ' + fmtLong(c.recorded) + ', on the ' + c.originJob + '.'` → **"Written consent, 2 May 2025, on the Lindqvist kitchen."** (confirmed on `people-room-1440-state-roster-1440.png`, the boxed unfold panel). 390's equivalent inline template at `people-room-390.html:1041` renders `cons.state + ' · ' + cons.source + ' · recorded ' + dlong(cons.recorded) + ', on the ' + cons.originJob + '.'` → **"Texting · written · recorded 2 May 2025, on the Lindqvist kitchen."** (confirmed on `people-room-390-state-roster-390.png`'s unfolded Dana Kowalski row). Both are visible on the default rendered face at both widths (Dana's row is unfolded by default in both plates), so this is not a hidden or interaction-gated divergence. SPEC §5.4 #13 requires "consent with source and date" without pinning an exact string here, so this doesn't miss a named literal, but it squarely fails "the two widths disagreeing on … a wording" for the same underlying fact, and produces a visibly different sentence structure (person-card-style prose at 1440, a three-part dot-separated fragment at 390) for what a reviewer would read as the same field. Note 390's *person-card* consent line (`:819`, `cap(cons.source) + ' consent, ' + dshort(...) + ...`) already matches 1440's phrasing style — only the roster-unfold call site drifted. | Change `people-room-390.html:1041` to use the same template 390's own person-card consent line already uses (or call a single shared `consentSentence()`-equivalent from both places), so the Call Sheet unfold reads "Written consent, 2 May 2025, on the Lindqvist kitchen." at both widths. |
| DR5-3 | Major | High | `people-room-1440.html` : `#state-directory`, Claire Bissett's row | SPEC §5.1 #7 (as amended by R-G/C17) states a person row "carries exactly three bordered `.word` columns — reach, consent, paper." Claire Bissett (F-20, Stonehaven Tile Gallery) has `"consent": null` in the fixture (`people-room-1440.html:430`) — the only one of the twelve `DIR_PEOPLE` rows with a null consent. `dirPersonRow()`'s word block (`:704-706`) is `wordEl(p.reach) + (p.consent ? wordEl(p.consent) : gap()) + (co && !noPaperHeld(co) ? wordEl(co.paper) : gap())`; when `p.consent` is falsy this renders `gap()` — `function gap() { return '<span class="word-gap" aria-hidden="true"></span>'; }` (`:604`) — a borderless, textless placeholder, not a `.word`. Confirmed on `people-room-1440-state-directory-1440.png`: Claire Bissett's row shows "ON PAPER" then a blank gap then "CURRENT", i.e. two bordered `.word`s and one empty slot, not three. This is 1440-specific: 390 never renders word columns on the row at all (§6.2), so the same null-consent fact produces no visible defect there (`word(p.consent)` returns `''` and is `.filter(Boolean)`-dropped from an unbordered inline list, `people-room-390.html:698`). Round 3's technical review (`3-technical.md:288-290`) flagged the same row and called it "correctly sparse, matching her fixture" — but that call predates R-G's tightened "exactly three bordered .word columns" wording, which is the binding text now. | Either give Claire Bissett a real consent value in the fixture (e.g. "Not asked", which is exactly the word family's placeholder for "nobody has asked yet" per direction.md §3.8) so `wordEl('Not asked')` renders a real bordered word, or have `dirPersonRow()` default a null consent to the word "Not asked" instead of falling to `gap()`. |
| DR5-4 | Major | Medium | `people-room-1440.html` vs `people-room-390.html` : `#state-company` (Northgate Electric, the default/only company-card plate) | The Paper region's consequence sentence for "Chase the renewal" sits in a different position relative to the act row at each width. 1440's `renderCompany()` (`:993-1002`) order is: table → leading-rule clause ("Site access, payment and the draw are held…") → **consequence sentence** ("This drafts a note to Northgate Electric's paperwork contact…") → **act row** (Record a document / Chase the renewal). 390's `renderCompany()` (`:970-978`) order is: leading-rule clause → **act row** → **consequence sentence** — the reverse. Confirmed on both plates: `people-room-1440-state-company-1440.png` shows the explanatory sentence above the two buttons; `people-room-390-state-company-390.png` shows it below them. SPEC §5.3 #4/#5 name the leading-rule sentence and the two acts but do not pin an explicit order between the acts and the consequence sentence, so no named acceptance string is missing — but it is the same fact laid out in reverse reading order between the two mandated-identical widths, the same shape of defect DR4-5 (fixed this round) named for the bring-forward footer. | Pick one order (1440's "explain, then act" reads better for a sentence that describes what pressing the button after it will do) and apply it to both files' `renderCompany()`. |
| DR5-5 | Minor | High | `people-room-1440.html` vs `people-room-390.html` : `#state-pick`, Ingrid Halvorsen's row (the only one of the five with a rule clause) | Ingrid Halvorsen's rule clause and her one-line project history print in opposite order between widths. 1440's `pickRow()` (`:1199-1200`) pushes `<div class="row-rule">Email only. No cell for work.</div>` **then** `<div class="row-note">Worked 1 prior project, Lindqvist kitchen, closed 2025</div>`. 390's `pickRow()` (`:1245-1246`) pushes `<p class="t-body-sm note prose">{row.line}</p>` (the history line) **then** `<p class="row-rule">Email only. No cell for work.</p>` (the rule) — the reverse. Confirmed on both plates: 1440 shows the rule sentence directly under her name/firm line with the history line below it; 390 shows the history line first, the rule sentence second. Both facts are present and correctly worded at both widths — only their order differs — so this doesn't miss an acceptance string, but it is a genuine, code-confirmed content-order divergence, the same shape as DR4-4 (now fixed) one level down in the same row. | Reorder either file's `pickRow()` so the rule clause and the history line print in the same sequence at both widths (1440's rule-then-history reads as the more urgent-first ordering and matches the row's other precedence, e.g. Frank Bauer's Directory row also prints its blocking rule before any note). |
| DR5-6 | Minor | Medium | `people-room-1440.html` vs `people-room-390.html` : `#state-directory`, every person row's seat-disclosure control | The seat/unfold-disclosure button carries a different label pattern at each width for the same control. 1440's is count-based: `count + ' seat' + (count === 1 ? '' : 's')` (`people-room-1440.html:696`) → "1 seat" / "0 seats", confirmed on-plate for every row (e.g. Rosa Delgado "1 SEAT", Claire Bissett "0 SEATS"). 390's is a static label regardless of count: `'Seats' + CHEV` (`people-room-390.html:714`), confirmed on-plate ("SEATS ⌄" on every row, Claire Bissett included, despite her having 0 seats same as 1440's data). No SPEC string names either wording (direction.md §3.1 just calls it a "seats disclosure"), so nothing required is missing, but it is a real "same control, different label" mismatch the task's cross-width parity pass is meant to catch, and `review/fix-log-r4.md`'s own "Deviations recorded" section already flagged it without resolving it. | Pick one label rule (1440's count-aware version carries more information — it tells the reader before opening whether there's anything inside) and apply it at both widths. |
| DR5-7 | Minor | Low | `people-room-1440.html` vs `people-room-390.html` : `#state-directory`, seat/unfold panel for anyone with a carried-forward consent note (Dana Kowalski F-11, Joe Wozniak F-18) | 390's Directory unfold prints the consent's freeform `note` field inside the seat panel (`people-room-390.html:721`: `if (cons) { out += '<p class="t-body-sm note">' + esc(cons.note) + '</p>'; }`) — e.g. Dana Kowalski's panel reads "Carried forward to the Okonkwo residence, 12 October 2026." (confirmed on `people-room-390-state-directory-390.png`). 1440's `dirPersonRow()` has no equivalent call anywhere — its seat `<li>` only ever prints `seatLine(e)` plus the stage word (`people-room-1440.html:684-685`); the consent note never appears on the Directory face at 1440 (confirmed absent from `people-room-1440-state-directory-1440.png`'s Dana Kowalski seat line, which reads only "Okonkwo residence · sub · electrical · On the job · 12 Oct 2026 to 13 Aug 2027"). No SPEC §5.1 item requires this sentence on the Directory (it's a §5.2 person-card fact), so nothing named is missing at 1440, but a fact is visible at one width's Directory and invisible at the other's for the identical row — `review/fix-log-r4.md`'s "Deviations recorded" section already named this exact asymmetry and asked the orchestrator to rule on it; it is unchanged since. | Rule whether the Directory seat panel should ever show the consent note (and if so, add it to 1440's seat `<li>` to match 390) or should never show it (and remove it from 390's). |

## Part C — spot checks that passed clean this round

- **Acceptance strings, all seven states, both widths**: walked SPEC §5.1–§5.7 line by line against the plates and source. Every required literal string is present verbatim at both widths (the two round-4 literal gaps — vitals, and the fourth stage column — are both closed by ruling and confirmed built, Part A above).
- **`#state-add` default face**: byte-for-byte identical visible text at both widths (kind switch, all 6 fields, channels, contact rule, authority region, consent block, invited line, consequence sentence, terminal act) — verified against both plates line by line.
- **`#state-person` (Dana Kowalski)**: byte-for-byte identical visible text at both widths across all six regions (Reach & access, Seats on projects, Past seats, Paper, History, Send-a-text act) — only the sub-head casing differs (`CHANNELS` vs `Channels`, a CSS `text-transform` difference, not a content difference).
- **`#state-company` (Northgate Electric)**: identical facts at both widths across all six regions including the four-row Paper table (table at 1440, label/value stack at 390 per §6.2) — see DR5-4 for the one ordering divergence found.
- **`#state-access`**: identical text and identical region order (Who to call first → The way in → Key holder → Hours → Receiving → Who was told) at both widths; no code digit anywhere in either file.
- **`#state-pick`**: five rows in the required order, correct per-row facts, "What travels"/"What stays behind" lists byte-identical, consequence sentence byte-identical — see DR5-5 for the one within-row ordering divergence found; R-I's act-row-then-consequence order and live checkboxes now confirmed fixed at 390 (Part A).
- **`#state-roster`**: all bands present in the required grouping and (for the "later" band, which SPEC does not pin to a literal order) in a consistent chronological order at both widths; Frank Bauer's clause/blocking-rule/stage-word facts present at both widths (routed line itself is DR5-1); vitals literal matches at both widths.
- **Row grammar (§5.1 #7 / §6.1)**: every Directory person row other than Claire Bissett's (DR5-3) carries exactly three bordered word columns at 1440, and plain-text reach only at 390's row level, matching §6.2.
- **Firm rows / R-H**: Marrow & Sons, Northgate Electric, Twin Cities Drywall & Plaster, Stonehaven Tile Gallery, Great Northern Bank, City of Minneapolis CPED Inspections all present under both Everyone and Firms at both widths; only the first four carry a paper word; the last two are wordless with no payee marker, matching C13/C18.
- **`tel:` links**: every phone on every face is `<a href="tel:+1##########">`, min 44×44, ≥8px from the row's own control, at both widths — Rosa Delgado's phone at the company card (both widths) and at the Directory/roster rows (1440 only — see DR5-1) all confirmed tel-linked.
- **No forbidden vocabulary**: re-ran the SPEC §8 grep battery (`box-shadow`, `text-overflow`, `placeholder=`, ` disabled`, curly apostrophes, `client_rep`/`party_kind`/`sms_consent_status`/`studio_contact_id`/`project_parties`, "AI"/"CRM" as a word, "chip"/"badge"/"modal"/"toast"/"dashboard"/"wizard"/"spinner"/"pill" as visible text, "Remove", `F-nn` codes outside `<script>`, hex literals outside the token block) — zero hits in both files.
- **Structure**: one `<h1>` per file, `<!-- specimen-complete -->` as the exact last line of both, `aria-disabled="true"` present (2 occurrences each) and zero bare `disabled` attributes, one `role="status"` per file, forced-colors and reduced-motion media blocks present in both.
- **Tokens**: both files' pasted `:root` token block diffs clean against `_tokens-reference.css` except the one documented appended `}`, in both.
- **External resources**: only `fonts.googleapis.com` / `fonts.gstatic.com`, both files.
- **R-D / R-B carried rulings**: both still hold (Part A).

## Summary

```
clean: false
blocking: 2 (DR5-1, DR5-2)
major:    2 (DR5-3, DR5-4)
minor:    3 (DR5-5, DR5-6, DR5-7)
carried, still open, awaiting a ruling (not new, not re-scored above): CR2-7/CR3-4 (licence spelling),
  CR2-8/CR3-5 (Compare & merge names no cards), CR3-6/CR2-9 (schedule-authority sentence),
  CR3-7/CR2-10 (key-holder cue)
resolved this round by orchestrator ruling and confirmed built: DR4-2 (R-F), DR4-3 (R-G),
  DR4-1 (R-H), DR4-5/DR4-6 (R-I), DR4-7 (R-J), TR4-2 (R-K)
```

`clean` is **false**. Round 4's rulings (R-F through R-K) are genuinely built and confirmed at both widths — this is real progress, not a repeat. But R-L, the seventh round-4 ruling, only reached one of its three required call sites at 390 (the company card); the Directory row and the Call Sheet roster row were never touched there, exactly as `fix-log-r4.md` itself disclosed as an open item rather than a completed one. That gap (DR5-1) is this round's clearest blocking finding. The second blocking finding (DR5-2) is new: a wording divergence in the Call Sheet row-unfold's consent sentence that no prior round's finding list named, found only by comparing the two widths' rendered text side by side on the same default plate. The two major findings are narrower — one is a single row (Claire Bissett) failing a rule tightened by this round's own ruling text, the other is a component-level ordering difference on the company card that mirrors a defect (DR4-5) already fixed elsewhere in the same file this round. All three minor findings are ordering/labelling divergences where both widths carry the correct facts, just arranged or worded differently — none miss a named SPEC acceptance string, but all three are real "same control, different presentation" mismatches a systematic cross-width pass is meant to surface.
