# 03 — Walk observations (E1 guest · E2 client · E3 dark/Dynamic Type)

Verbatim from the three walk agents' structured returns (2026-08-26). Each line is an observation a shot alone does not show; failures list what the brief asked for that does not exist or could not be reached. Environment faults (edge functions 503, OTP mail without a code, keychain outliving uninstall) are NOT app defects — see 02-steward-boot.md §8 and instruments §6b C27.

## E1 — guest walk — 56 shots

### Could not capture as briefed

- g-03/g-04/g-05 (onboarding pages) DO NOT EXIST. The blank splash goes straight to the auth gate (g-02); there is no onboarding carousel anywhere in the guest flow.
- g-13 as briefed ('home scrolled to the designer CTA / Your Studio block') DOES NOT EXIST on home. The guest Daily Room has no designer CTA and no Studio block; that block lives on Profile (g-36). g-13 shows the unchanged home.
- g-18 (piece detail scrolled to its acts) UNREACHABLE. Every product detail fails with 'Couldn't load product' — root cause is an app bug, PostgREST 300/PGRST201 ambiguous embed at apps/mobile/Patina/Patina/Core/Network/ProductAPIClient.swift:99, not the known local edge-function 503.
- g-24 (a distinct 'New room' sheet) DOES NOT EXIST as a separate screen — the Next Move card goes straight to the manual entry form captured as g-23/g-25.
- g-27 and g-28 as briefed ('Add to room sheet from a piece + toast', 'room view with the piece added') ARE UNREACHABLE. No 'Add to room' action exists in the ⋯ menu, on the card, or anywhere else — even when the grid is entered from a room's own 'Browse Picks for This Room' CTA (g-27c). Substitute shots capture the room summary and room view instead.
- g-21 could not be captured as a genuinely empty Saved: an accidental heart toggle during the g-15c swipe test saved a piece before Saved was first opened. g-21/g-22 show the Boards tab reading 'No boards yet' while one item exists under All items — which is itself a finding.
- The steward's reset recipe did NOT produce a signed-out device: the simulator keychain outlives app deletion, so the first launch landed in the client session (g-02a). I recovered the guest baseline with `xcrun simctl keychain <udid> reset` before the walk proper. This also means the client/dark/Dynamic-Type lanes will need to sign in again.
- OTP / magic-link sign-in was not exercised (steward flagged it unwalkable locally). Apple and Google sign-in are not usable on this simulator.

### Observations

- TAPS FROM HOME TO A PIECE'S PRICE: 2. Companion bubble (201,757) → 'View recommendations' → the grid shows '$2,100' on the card. Reaching a price on a piece's own DETAIL page is impossible — the detail screen is 100% broken (see below). Before the quiz it is 3+ taps, because the Companion's 'Your recommendations' row reads 'TAKE THE QUIZ FIRST'.
- HEADLINE DEFECT — every product detail 404s and traps the user. Kong logs `GET /rest/v1/products?id=eq.…&select=*,vendors(name,made_in,brand_story)` returning HTTP 300 `PGRST201`: `products` carries two FKs to `vendors` (`retailer_id` and `vendor_id`), so the unqualified `vendors(...)` embed is ambiguous. Fix at apps/mobile/Patina/Patina/Core/Network/ProductAPIClient.swift:99 → `vendors!products_vendor_id_fkey(...)`. This is an app bug, NOT the known local edge-function 503. Worse than the 404: `scan_ui` returns exactly ONE element on that screen — the retry button. No Back, no nav, Companion greyed out and absent from the AX tree, edge-swipe-back dead. The guest's only escape is force-quitting, which destroys the guest session.
- WHERE THE GUEST HITS THE SOFT WALL: at the very last tap of the design-request flow. 'Get design help' → 'Request without a scan' → a four-field form → 'Review' → 'Send request' → the full auth gate. Nothing anywhere in that flow says an account will be needed. The wall is the GENERIC gate ('Welcome home / Start with a piece you love'), makes no mention of the request, and has NO Cancel, NO X and NO 'Look around first' — every escape hatch present on the real gate is removed. Only a blind downward drag dismisses it. (The draft does survive, which is good.) A second, softer wall sits on Notifications ('Sign in to stay in the loop').
- THE BROWSE GRID IS GEOMETRICALLY BROKEN — the app's main commerce surface. `describe_screen` gives card frames at x = −32.7 and x = −10.7 (off the left edge) and a fourth ending at x = 412.7 (screen is 402 pt). Card sizes are 284×294, 171×341, 240×293, 240×304 where a 2-column grid needs one size. The left column is literally unreadable: 'M & BOARD', 'rloom Oak / ing Table', ',200'. Under the 'Seating' filter cards additionally overlap each other and one renders as a blank brown gradient with no image.
- TWO SEPARATE STYLE QUIZZES THAT DISAGREE. The Companion quiz (5 steps, progress bar, single-select) and the room-capture quiz ('Style Discovery', no progress, multi-select) ask FOUR of the same questions with different option sets — palette (Classic Comfort/Eclectic Curated vs Layered Comfort/Curated Mix), how you live, textures, budget. They then produce different names for the same result: the Companion gives 'Warm Modern', the second gives 'Modern Warmth', and the app shows whichever ran last. I answered 'Warm Minimal' in Q1 and was told I am 'Warm Modern' — a label that is not one of the four palettes offered.
- DATA CORRUPTION VIA A 6-POINT TOUCH TARGET. The room form's unit toggle exposes 'ft' at 12×13 pt and 'm' at 6×13 pt. Tapping 'm' produced NO visible state change ('ft' stayed bold), but it silently reinterpreted my entry: I typed LENGTH 18 / WIDTH 14 believing feet and got a 'Living Room' of 46 ft × 59 ft = 2713 SQ FT (18 m = 59 ft, 14 m = 46 ft). That figure then propagates to the room view and the home ACTIVE ROOM card. Steppers are 32×32, the retry button 125×17, the 'Boards' tab 46×17 — the app is systematically under the 44 pt minimum.
- SHARING A PIECE FROM THE CLIENT APP OFFERS 'Patina Designer Portal — app.patina.cloud'. A homeowner sharing a chair hands their friend a link to the designer portal, under the designer portal's name.
- PRODUCT PHOTOGRAPHY DOES NOT MATCH THE PRODUCTS. 'Live-Edge Coffee Table' (Lee Industries) shows an old ladder-back chair on grass; 'Heirloom Oak Dining Table' (Room & Board) shows green velvet chairs around a white table; 'Terracotta Planter Set' shows a mint-green plastic pot; 'Brass Arc Floor Lamp' shows a grey pendant lamp. Several vendors read 'UNKNOWN MAKER' on a marketplace whose pitch is provenance.
- LABELS I COULD NOT DEFINE FROM THE LABEL ALONE: '48% MATCH' and '— MATCH' (match with what? the profile stat and the room stat use the same word for apparently different things); '0 IN AR' on the room view (undefined abbreviation); the unlabelled ~45% progress bar under 'WHY PATINA SEES THIS' on the taste portrait; '46% match' / '45% match' badges on cards; 'Style Discovery' vs 'Style quiz' vs 'taste portrait' vs 'Your visual style' — four names for one concept; 'NEXT STEPS' under the Companion bubble vs 'Where to begin?' inside it.
- LOOKED TAPPABLE AND WASN'T: 'Account >' in Settings — a full row with a chevron that does nothing, reproduced in BOTH the guest and client tiers (g-02b, g-37b). The 'm' unit toggle (fires but shows nothing). The '0 ROOMS / 1 SAVED / 48% MATCH' stat tiles on Profile. The clipped black bar on the taste-portrait screen. Conversely, the product cards ARE tappable but are exposed to VoiceOver with role AXPopUpButton ('pop up button') — wrong role for a product card, and `scan_ui` (interactive-only) misses them entirely.
- PERSISTENCE IS INCOHERENT ACROSS SURFACES. After a force-quit the guest is dumped at the gate (g-38) and the session is gone — yet the saved piece and the coach-mark flags survive, the Profile still shows the 'Warm Modern' badge and '48% MATCH', while the Companion on that same launch insists 'Style quiz — DISCOVER YOUR STYLE' and 'Your recommendations — TAKE THE QUIZ FIRST'. Three surfaces, three answers to 'did this person take the quiz?'. And the gate says 'Welcome home' to a user it just forgot.
- CRAFT / SYSTEM BREAKS: the launch screen is pure blank white (no wordmark) against the app's warm ground; the two coach marks use iOS system blue #007AFF, the only blue in the app, and each bubble fully covers the card it is describing (three times running, including the Companion's own first row); stock Apple emoji stand in for icons throughout (✉️ on the gate beside a real Apple logo and a bare letter 'G'; 🍷🧘💻👨‍👩‍👧📚🌱💬🏠✨🔄💎 in the quizzes; 🛋🛏🍽💻🍳✨ in the room form; 📐🪟🚪 and a colour ⚙️ in the room view); the 'YOUR STYLE, FOUND' result screen is dark-themed and set in the system sans while the whole app is light and serif; the Notifications 'Sign in' button is a circle with its label overflowing both sides; the Profile and Home headers scroll up UNDER the status bar (the 9:41 clock overprints 'Guest', the 'Warm Modern' pill and 'WEDNESDAY · AUG 26'); prices render '$4,200' on the grid and '$4200' in Saved.
- NO COMPANION NUDGE AFTER 50 s IDLE on home — the bubble never speaks first, despite the intro card promising 'Tap me any time… I'll show you the way to what's next.' The Companion is the app's ONLY navigation surface (there is no tab bar or nav bar anywhere), and it never offered a 'Browse' row until after the quiz was completed.
- THREE COMPETING CTAS COLLIDE at the bottom of the room view: body copy says 'Browse your Daily Room to start building this room', the button says 'Browse Picks for This Room', a link says 'SEE RECOMMENDATIONS →', and the Companion bubble sits on top of both. 'Browse Picks for This Room' then opens the same generic '10 pieces curated for your space' with no room name and no filtering — and there is still no way to add any of them to the room.

## E2 — signed-in client walk (client@patina.dev, then james.okafor@example.com) — 45 shots

### Could not capture as briefed

- c-14 SFSafariViewController handoff NOT reachable: tapping "Pay $4,250.00" fires POST /functions/v1/create-checkout-session which returns 503 — the known local edge-runtime fault documented in 02-steward-boot.md §8, not an app defect. Captured the failure UX instead (c-14).
- c-16 Documents does not exist as a surface. There is no Documents row anywhere in the signed-in app; "Money & documents" contains only Proposals, Invoices and Budget, and GET /rest/v1/project_documents?...client_visible=eq.true returns [].
- c-20 a message thread is unreachable. The entire messaging surface is a Studio block reading "Conversation 0 / No project conversations yet." with no compose, no thread list and no "message your designer" affordance. GET /rest/v1/comms_threads returns [] for this account.
- c-27 an Account screen does not exist. Settings → "Account >" is inert (tapped dead-centre of its 338×44 frame, screen unchanged) — third reproduction after the guest lane's g-02b and g-37b.
- c-30 "sign out" is not performable in the app. There is no Sign Out control in Profile or in Settings for a signed-in client. Switching to the engaged account required `xcrun simctl keychain <udid> reset` from the host, twice (out to james.okafor@example.com, back to client@patina.dev).
- c-25 piece detail is broken for signed-in clients exactly as for guests, and traps the user: GET /rest/v1/products?id=eq.…&select=*,vendors(name,made_in,brand_story) → HTTP 300 PGRST201 (two FKs products→vendors). scan_ui returns one element for the whole screen; escape required force-quitting the app.
- c-18 decision "Choose this" was deliberately not tapped — it would have consumed one of the two pending decisions and changed the state handed to the next lane.

### Observations

- THE DESIGNER IS NAMED EXACTLY ONCE. "Aspen Loft Refresh · from Leah Hartwell" on the invoice detail (c-13), plus an unattributed "A NOTE FROM YOUR DESIGNER" heading below it. No photo, no studio name, no bio, no contact, no avatar anywhere — not on the project, not on the proposal, not on the decisions, not on Today. The word "designer" otherwise appears only in empty states: "Updates from your designer will land here." (c-21).
- THE SIGNED-IN HOME IS STRUCTURALLY IDENTICAL TO THE GUEST HOME. describe_screen on the activeProject home returns four content elements: Next Move, editorial story, ACTIVE ROOM, Companion bubble (c-04). The only thing that changed vs guest is the Next Move copy ("Review a project decision / 2 decisions need your eye.") and the footer ("2 PROJECT DECISIONS WAITING"). Three projects, four proposals, an open $4,250 invoice and two overdue decisions are all invisible on Today. There is still no tab bar and no nav bar.
- THE REAL STUDIO HUB IS BURIED TWO LEVELS DOWN, INSIDE PROFILE. Everything the client owns lives under Profile → scroll → STUDIO "The work around your home, in one place." (c-06/c-06b/c-06c). The Companion's "Your studio / PROJECTS · MESSAGES · DECISIONS" row does NOT go there — it jumps straight to a bare projects list (c-04b), so the row's own subtitle is a lie about where it leads.
- "WHAT CHANGED SINCE LAST TIME" HAS NO HOME. Notifications is empty (c-21) while the Studio says 2 decisions are Overdue · Aug 22, an invoice is Due Sep 1 and a proposal must be reviewed by Sep 8. A relaunch drops the client on a byte-identical Today (c-29) — no welcome-back, no unread marker, no last-visit timestamp, no activity feed. The only place urgency is expressed at all is the Studio's "Awaiting you" block.
- THREE DIFFERENT COUNTS FOR ONE INBOX, TWO OF THEM ON THE SAME SCREEN. Profile/Studio prints "4 things need your eye" as its subhead and "4 THINGS NEED YOUR EYE" in the footer, directly above a block headed "Awaiting you 3" (c-06b). Home and the Companion both say "2 project decisions waiting" (c-03, c-05).
- TWO DESIGNER-FACING LEAKS ON THE CLIENT'S PROJECT SCREEN (c-08). A stat labelled "CLIENT VIEW / Milestone" exposes the raw client_visibility_tier column to the client it governs, and a boxed link reads "Set up phases, payments, and FF&E in the portal →" — an instruction written for the designer, rendered to the homeowner (and it is a StaticText, not a button, so it looks tappable and is not).
- THE PROJECT DETAIL DISCARDS DATA IT ALREADY FETCHED. Kong shows the screen successfully loading project_phases (3827 bytes), get_client_project_selections, project_payment_milestones and list_client_proposals, then rendering three stats, an Invoices row and the portal link. No timeline, no rooms, no tasks, no selections.
- MONEY ACTS AND THEIR TAP COST, from a cold home: PAY AN INVOICE = 5 taps (Profile → scroll → Invoice row → scroll → Pay) or 6 via project detail; SIGN A PROPOSAL = 6 taps (Profile → scroll → Proposals → the proposal → scroll → Sign proposal → name → confirm); ANSWER A DECISION = 3 taps (Next Move → decision → Choose this) with no confirmation step. Signing and paying are the two deepest acts in the app; answering a $850 decision is the shallowest and is irreversible.
- THE SIGN SHEET RESTATES NOTHING (c-11c). "Type your full name to e-sign. Signing confirms the scope and kicks off your project." — no amount, no line items, no terms, no date, no agreement checkbox. The terms it silently binds you to are severe and live one screen back: "Deposits are non-refundable once procurement begins. Custom items are final sale."
- THE PROPOSALS LIST CALLS AN ACCEPTED PROPOSAL "SIGNED" (c-09). The seed has zero signed proposals; "Sample accepted proposal" ($100,000.00) sits under a "SIGNED (1)" header. That is a legally loaded mislabel on a six-figure document.
- DEADLINES ARE PRINTED ON THE CARD YOU LEAVE AND DROPPED ON THE SCREEN YOU ACT ON. "Due Sep 1, 2026" appears on the invoices list and vanishes on the invoice detail (c-12→c-13). "Expires Sep 8" appears on the proposals list and vanishes on the proposal detail (c-09→c-10). "Overdue · Aug 22" appears on the Studio hub and vanishes on the decisions list (c-06b→c-17).
- THE ENTIRE CLIENT INBOX IS INVISIBLE TO ASSISTIVE TECH. scan_ui on the Studio block returns none of the rows (Decisions, Invoice, Proposal, Active projects, Proposals, Invoices, Budget, Archive) as buttons — only "Open Living Room", "Retake Style Quiz", "Get design help", "Settings" and Back. The rows draw chevrons and respond to taps but expose no accessibility button, so VoiceOver and Switch Control cannot reach any money or decision surface.
- NO LOCKED / GATED ROWS EXIST IN EITHER TIER. Nothing is shown-but-disabled with upgrade copy. The tiers differ by absence: the engaged client simply has no "Your studio" Companion row (c-32) and five empty Studio blocks (c-32c). The engaged home (c-31) is the guest home verbatim — "Bring your first room into Patina" — shown to a homeowner whose lead was accepted and claimed on Aug 18 and who already uploaded a room scan. The single true fact about that account appears nowhere.
- GUEST LOCAL DATA IS ADOPTED BY THE FIRST ACCOUNT THAT SIGNS IN AFTER IT. The guest lane's typed room, saved piece and "Modern Warmth" portrait all surfaced under client@patina.dev (c-03 ACTIVE ROOM, c-05 "1 ROOM"/"1 SAVED PIECE", c-26 badge + "1 ROOMS"), an account with zero rooms and zero saved items server-side. "Your Spaces" even flags it "SAVED ON THIS PHONE" (c-23) while every other surface counts it as account data. It did not follow to the second account, and it disappeared once the local store was cleared (c-34) — the store is device-scoped, not user-scoped.
- ERROR AND EMPTY COPY, VERBATIM: "Unable to start payment. Please try again." (c-14) · "Couldn't load product" / "Let's try that again" (c-25) · "Nothing yet" / "Updates from your designer will land here." (c-21) · "No payments recorded yet." (c-13) · "No project conversations yet." (c-19) · "No boards yet" / "Save pieces from recommendations to create your first board" (c-22) · "Nothing needs your attention right now." / "Nothing needs a decision." / "No active projects yet." / "No shared records yet." / "Nothing has been archived." (c-32b, c-32c) · "No scans on this phone yet" (c-33) · "A blank canvas" (c-24).
- SMALLER THINGS WORTH THE PANEL'S TIME: "Your budget / ACROSS YOUR PROJECTS" reports $4,250 for a client whose three projects total $725,000 (c-15) — it is a billing summary wearing the word budget. The projects list sorts the Completed project above both In Progress ones and truncates "Installation & Styling" to the bare word "Install" (c-07). Every scrollable screen lets its header slide under the status bar, so the 9:41 clock overprints "Awaiting payment", "Invoices", "Terms" and "Settings" (c-13b, c-06, c-11, c-28). The Companion bubble sits on top of the primary CTA on at least three screens ("Sign proposal" c-11, "Browse Picks for This Room" c-24). "Your Spaces" carries four "?" help buttons at three different sizes (c-23), and a "JUST SCANNED" chip directly above the caption "MANUAL ENTRY". Settings row icons break the tan palette with pink, blue and grey-green tints (c-28). "Style Explorer" is a fourth name for the taste layer after "Warm Modern" and "Modern Warmth". "$4200" still renders without a thousands separator (c-22b).

## E3 — dark mode + Dynamic Type XXL — 22 shots

### Could not capture as briefed

- d-05 Saved: not reachable. The Companion panel (the app's only nav surface) has no 'Saved' row while the saved-item count is 0, confirmed via scan_ui both before and after adding a room. Attempts to populate Saved by tapping a browse-grid card's heart icon produced no visible toggle, and a follow-up tap near the heart/⋯ menu landed on the card body instead, re-triggering the 'Couldn't load product' hard-trap (same defect as d-04/x-04), requiring another simctl terminate+relaunch to recover. Documented as its own ledger row with full detail instead of a screenshot.

### Observations

- Home (DailyRoomView) does not scroll for the activeProject client in dark mode either (d-01 and d-02 'swiped' shot are pixel-identical) — confirms c-04's finding is not a light-mode artifact. The real 'Studio rows' surface lives on Profile→swipe (d-02-profile-studio-rows.png), reached via the same path the client lane used.
- DEFECT (safe-area, reproduces 3x across light/dark/XXL): the fixed status bar/Dynamic-Island draws OVER scrolled content with no inset reserved — seen on the Studio hub scrolled list in dark (d-02-profile-studio-rows.png, clock over 'Review by Sep 8'), worse at XXL where the pill itself occludes the proposal title (x-02-profile-studio-rows.png), and again on a modal sheet's own title bar at XXL (x-06-design-request.png, 'Close'/'Your design request' overlapped by 9:41).
- DEFECT confirmed NOT saved-item-specific: tapping ANY product card (a never-before-saved 'Live-Edge Coffee Table') hits 'Couldn't load product / Let's try that again' with scan_ui returning only the retry link — a hard trap with no Back button, reproduced identically in dark (d-04) and at XXL (x-04). Root cause per prior lane: PGRST201 ambiguous vendors embed in ProductAPIClient.swift:99. Recovery required simctl terminate+launch both times; the signed-in session survived cleanly each time.
- Root cause pinned for the long-standing 'bad room dimensions' bug (59'x46' from 18x14 input, seen since the guest lane): the manual room-entry flow applies a meters-per-feet conversion factor (~3.28x) to values already entered in feet with the ft/m toggle left on 'ft'. Captured mid-flow in d-06a-room-summary-light-locked.png.
- DEFECT: the room-summary step ('Here's what I see' with the rescan/'This Looks Right' actions) renders in a hard-coded LIGHT theme even while the rest of the app is in dark mode — the only screen observed to ignore the system/app dark-mode override in this walk.
- The Studio hub's Proposals/Invoices/Budget/Archive rows are exposed to VoiceOver with a 'button' role and an 'Opens X' hint at Dynamic Type XXL (scan_ui showed AXUniqueId 'StudioHub.Section.moneyAndDocuments'), but at default text size / dark mode scan_ui returned NONE of those rows as accessible elements at all — an inconsistency in accessibility-tree exposure tied to text size, on top of the underlying gap c-06d already flagged.
- DEFECT (XXL-specific): the Browse-pieces filter chip row (All/Seating/Tables/Lighting/Storage) overflows the screen width with no scroll affordance — 'Storage' is clipped to 'Stor' at the right edge (x-03-browse-pieces.png).
- DEFECT (XXL-specific): on the proposal detail screen, wrapped body text under 'Space Plan' runs directly into and is cut off by the fixed circular Companion/menu button at the bottom of the screen — no reserved bottom padding once XXL text pushes content that far down (x-05-proposal-detail.png).
- Session persistence for the signed-in activeProject client across simctl terminate+launch was proven twice more in this lane (after each product-detail hard-trap), consistent with c-29 — always with -DeploymentTarget local repeated on every relaunch per steward guidance.
- simctl ui content_size extra-extra-large applied live without needing an app relaunch, and both it and appearance persisted correctly across the two forced relaunches (from the product-detail traps) without needing to be reissued.
- Dark-mode color contrast was clean everywhere reviewed except where noted above — no illegible text pairs found; the app's cream-on-near-black / near-black-on-cream palette holds up well. No color-emoji-breaks-the-monochrome-palette instances were caught in the dark lane specifically (the emoji icons g-25/g-30/c-24 flagged were only encountered on the light-locked room-summary screen, d-06a, which is itself the defect).
- A Living Room (manually entered, 18x14 ft / 2 windows / 1 door) and the resulting corrupted dimensions were created during this lane to reach room-related screens since the client account started at 0 rooms/0 saved (state cleared by the prior lane's keychain-reset cycle, per c-34). This is real in-app user action via the UI, not a DB/seed edit; it is left in place for the next lane. Saved remains empty — no piece was successfully saved.

## Ledger summary

- 155 PNGs validated, 0 blank, ledger at research/01-shot-ledger.md
- Routes without a shot: roomSettings(roomId:), roomSavedItems(roomId:), scanFlow(reason:), emergence(pieceId:), roomEmergence(roomId:), arPlacement(productId:roomRemoteId:), threadList, threadDetail(threadId:), documentList