# W3 — orchestrator rulings on the integration record's open items (2026-08-28)

| # | Item (`integration.md` §6/§7, `walk.md`) | Ruling | Lands in |
|---|---|---|---|
| 1 | §6a — two Studio doors on the flag-on root; the tour's step-3 anchor lives on the header pill | **Exit 1.** Hoist the first-launch tour model above the four stacks in `HouseFirstRoot` (one model per root; `DailyRoomView` keeps its own on the flag-off root), tag the bar's Studio arm with `.firstLaunchTourAnchor(.profileMonogram)` (raw value unchanged — it keys the Sanity document), and gate the header's `Studio` pill OFF on the flag-on root (N1's two-line gate). M1's header stands: date, greeting, bell — no pill under the bar. | W3-fix |
| 2 | §7.2 — `RouteTabTable.rootRoute(for: .studio) == .profile`; every Studio visit reports "Profile"; `ProfileView` unreachable on the flag-on root (walk: no way to Settings / Sign Out / Delete Account) | **Mint `AppRoute.studio`.** Analytics screen name "Your Studio"; Companion context = the Studio rows. The Studio tab's root is the profile composition (identity line, the Studio hub, and the Settings/Account door — what the monogram opened) titled "Your Studio", so Sign Out and Delete Account (Apple 5.1.1(v)) stay one tap from the bar. `HouseFirstRootTests.theStudioTabReportsProfileUntilAStudioRouteIsMinted` flips to the honest assertion. | W3-fix |
| 3 | §6b — `MoneyScreenMetrics.bottomClearance` sized to a dock the bar replaces (~148 pt dead space); `ProductDetailView`'s Add to Room capsule under the bar | **One owner, one commit.** `bottomClearance` becomes `bottomClearance(houseFirst:)` over `CompanionHearthMetrics.pinnedFooterClearance(houseFirst:)`; the nine views and `ProductDetailView` read it; `MoneyAndStudioCopyTests:250`'s source pin is updated to the new spelling, not silenced. | W3-fix |
| 4 | §7.4 — the tour branches on `house-first`; the flag-off list keeps `.addToRoom`, which mounts nowhere (two steps declared as three) | **N3's way.** The B-8 rewrite applies on both roots. Step 2 anchors on the Record card (`.todayRecord`) on both roots; where the record draws nothing (an empty guest record) the step is dropped and the tour renumbers by its existing mechanism — honest, and pinned. `.addToRoom` is retired from the step list. | W3-fix |
| 5 | `walk.md` item 4 — the tour renders Sanity's retired copy | **Content ops, Kody's, before the flag flips.** Sanity is the authority (U32); the three bodies in `waves/w3/n3-sanity-copy.md` must be published to the `production` dataset before `house-first` is enabled for anyone. Not a code change. | OWED (Kody) |
| 6 | N3 — `DailyRoomView.swift:46` `canAutoStart: coordinator.navigationPath.isEmpty` is permanently true on the flag-on root (the tour auto-started over the Pieces tab) | **Fix.** On the flag-on root the tour may auto-start only when the Today tab is selected and its stack is empty; the flag-off expression stands. | W3-fix |
| 7 | §7.3 — `roomEmergence` / `roomSavedItems` filed under Spaces ("a push never changes tabs") | **Upheld.** The steward's rule is the right one; record it in the canon digest with the table. | W3-fix (digest line) |
| 8 | §7.5 — M9's unbuilt gaps (the `Rugs` chip etc.) | Backlog; not this program's wave list. Noted in the plan. | later |
| 9 | Walk note — the guest session does not survive a relaunch (review F28, S0; nobody owned it) | **Fix in W4.** A guest who chose "Look around first" stays a guest across relaunches until they sign in or clear the app; SP-06's ownership rule is unchanged. | W4 lane H2 |
| 10 | Walk note — the app's own `patina.appearance` override defeats `simctl ui appearance` | Harness note for walkers (already in the ledger); no product change. | — |

Merge rule for this wave: W3-fix's re-walk must show, on the flag-on root, Settings → Sign Out and
Delete Account reachable from the Studio tab; the tour's step 3 on the bar and no header pill; the
invoice/proposal/decision/piece footers clear of the bar; the tour not auto-starting over another
tab. The flag-off root must remain byte-for-byte W2's.
