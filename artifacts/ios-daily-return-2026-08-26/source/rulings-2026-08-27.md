# Rulings — Kody, 2026-08-27 (taken in session, on the deck's twelve questions)

| # | Question | Ruling | Consequence for the build |
|---|---|---|---|
| R1 | The home (C23 vs B-3/B-4) | **Yes, now — skip the interim.** Today becomes Direction B's Record card (NEEDS YOU / MOVED, rolling 7 days, truthful empties) with the house rail under it, as the first slice itself. No interim WHAT MOVED line. | C23 amended (B-3, B-4). The Record is an additive mount; the old Today root stays on the off branch of the flag for one release. |
| R2 | The tab bar (C1) | **Yes — ship it behind `house-first`.** B's four-tab bar Today · Spaces · Pieces · Studio, Companion in the trailing slot (B-1, B-2); both roots kept for one release. | C1 and C8 amended. Route→tab table for every `AppRoute` case and the 105 `navigate(to:)` sites; flag evaluated once at launch and held; `CompanionSafeArea`'s 120 pt inset retired on the flag-on branch; tour rewritten (B-8). |
| R3 | Buy with a designer engaged | **A's pre-emption, for now.** "Ask Leah to source this" pre-empts Buy for any client with a live designer relationship (accepted lead or active project) until the designer-side settle notice is proven on a device. No Buy button, no disclosure line for those clients yet. | Buy draws only for signed-in clients with no live designer relationship, behind flag `direct-orders`, and only when the buyability gate passes. Roster-designer attribution (designer_clients) still credits. B's "Buy it myself" is a later flip, after the settle notice is device-proven. |
| Q4 | C2 vs C23 — the orphaned July rail | **Retire it, name the conflict.** | C2's marketplace-first rail is superseded by Option B + the Record. Delete the orphaned views in a hygiene pass (HomeStudioBlock, StudioHubSection, MarketplaceLinksSection, WorkWithDesignerCTA, RoomChipRail, RoomContextBar, DailyProductCard, DailyProductDetailView, DailyFeedEmptyModule, ContinueScanCard, DesignRequestStatusCard, AddToRoomSheet, AddedToRoomToast) after confirming zero call sites; record the retirement in the canon digest. |
| Q5 | Attribution on direct orders | **Yes, three columns.** `designer_id`, `project_id`, `commission_rate` snapshotted at create, immutable after paid; settle routes onto the fulfillment rail; `designer_earnings` credit keyed on `order_id` with a partial unique index. | One migration + one webhook branch + one metadata widening. Lands before any client Buy surface. |
| Q6 | Order status beyond "paid" | **Route onto the fulfillment rail.** A settled direct order becomes a `fulfillment_orders` row via the existing `fulfillment_intake` enqueue; one client-scoped SELECT policy over `fulfillment_orders` / `fulfillment_order_items` / `fulfillment_shipments`. | No new state machine. Studio → Ordered reads both rails. |
| Q7 | The permission ask | **SP-08's sentence, verbatim, before the first money event.** "We'll tell you when your designer sends something that needs you — a decision, a proposal, or an invoice. Nothing else." Asked once; Settings is the fallback. | Requires the push call sites first (proposal-send, invoice-send/reminders, a new AFTER INSERT trigger on `client_decisions`), each passing `notification_log_id`. The current post-design-request ask is removed. |
| Q8 | Widgets | **One small widget, Home + Lock Screen.** Carries what moved, not what is owed; refreshed on foreground + timeline policy; may sit one open behind. | New WidgetKit target + App Group entitlement + `RecordSnapshotStore` shared with the app. The second bundle id under ASC app 6762007888 is Kody's paperwork (asc-signing-setup) — simulator verification does not need it. |
| Q9 | Household | **Read-mostly second seat, later wave.** `household_members` junction + one invite RPC; the second seat sees the record and the rooms; one designer per house; no new money surface. | Backlog item after this program; not in the waves below. |
| Q10 | The piece-detail hotfix | **Yes, first.** Qualify the embed (`vendors!products_vendor_id_fkey`) at `ProductAPIClient.swift:99`; give the error state a back chevron; confirm the constraint name against Strata first. | Its own PR, merged before wave one. TestFlight distribution is a separate act Kody triggers. |
| Q11 | Stripe live vs test | **Kody checks the Stripe dashboard himself.** | The program treats the purchase path as test-mode; Path A stays behind `direct-orders` until Stripe Tax/shipping and the responsibility paragraph are ruled and the keys are confirmed. |
| Q12 | The evidence | **Restart the stack from main, re-walk the Checkout hand-off and one Companion conversation, then build.** | W0 steward task; the re-walk shots go to `shots/r-*.png` and the ledger. |

Standing from the review, unchanged: physical goods never use IAP (C15); brand voice (C6); honesty
(C5); no new NestJS service (C13); canonical names (C4) except the B-7 tab labels; the twenty planks
ship with the program.
