"""Patina Rendered Room v2 — the Modal side of the scan pipeline.

`verify` (CPU) ships in W1. `splat` (L4) and `renders` (L40S) are declared here
as stubs so the dispatcher contract is complete; their bodies land in W2.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
