# Extra dependencies for scripts/export_models.py (Dockerfile stage 1 / `make export`).
# No torch: we consume the published fp32 ONNX exports and quantize with onnxruntime.
huggingface_hub>=0.26,<2
onnx>=1.16,<2
onnxruntime>=1.20,<2
