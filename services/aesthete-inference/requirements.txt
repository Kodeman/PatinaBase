# Runtime dependencies (python 3.12 — matches Dockerfile stage 2).
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx==0.28.1
httpcore==1.0.9
pillow>=10.4,<12
onnxruntime>=1.20,<2
tokenizers>=0.20,<1
numpy>=1.26,<3
# Room View USDZ→GLB lane (R107). usd-core ships prebuilt wheels (incl. macOS
# arm64 + manylinux); the runtime import is lazy (app/usdz.py) so /embed/* is
# never blocked by it. ⚠ usd-core is large — ~35 MB wheel, ~220 MB unpacked on
# disk (see Dockerfile stage-2 note).
usd-core>=25.5,<27
pygltflib>=1.16,<2
# Room View HEIC→JPEG derivative lane (I78). pillow-heif ships prebuilt wheels
# that BUNDLE libheif (incl. macOS arm64 + manylinux); the runtime import +
# register_heif_opener() are lazy (app/photos.py) so /embed/* is never blocked
# by a broken wheel. ⚠ if the wheel ever fails to bundle libheif on the target,
# add the apt libheif runtime deps to the Dockerfile and say so.
pillow-heif>=0.16,<1
