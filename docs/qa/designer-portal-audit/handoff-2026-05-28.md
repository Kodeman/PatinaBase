# Designer-Portal Backlog — Session Handoff (2026-05-28)

Pickup doc for a fresh session. Source of truth for item status stays `punch-list.md`;
this file is the **actionable remaining work** distilled into groups with fix strategy.

## How to resume

```bash
# type-error count (the SMOKE-04 grind)
pnpm --filter @patina/designer-portal exec tsc --noEmit 2>&1 | grep -c "error TS"

# cluster by file
pnpm --filter @patina/designer-portal exec tsc --noEmit 2>&1 \
  | grep "error TS" | sed -E 's/\(([0-9]+),[0-9]+\).*//' | sort | uniq -c | sort -rn

# tally by error code
pnpm --filter @patina/designer-portal exec tsc --noEmit 2>&1 \
  | grep -oE "error TS[0-9]+" | sort | uniq -c | sort -rn
```

Durable working rules (carry forward): **commit + push at end of every task** (logical
conventional commits, push `origin`); parallel agent waves run on `main`, **agents
edit-only**, orchestrator runs `tsc`/verifies/commits/pushes; never run destructive git
ops (`reset --hard`, force-push, `branch -D`) without explicit user OK; don't trigger JS
alerts/confirms in Chrome (blocks the extension).

---

## SMOKE-04 — type-health: **70 errors remaining** (down from 3085 → this segment 262 → 70)

Every shared *cluster* is cleared; what's left is a heterogeneous long tail. Grouped below
by root cause with the cheapest fix. **Biggest win = Group A (Badge API), ~28 errors.**

### Group A — Badge variant/color API migration (~28× TS2322) ← do this first

The design-system `Badge` takes `variant: 'dot'|'subtle'|'solid'|'outline'` and
`color: 'primary'|'neutral'|'error'|'success'|'warning'|'info'`. These call sites still
pass **legacy shadcn `variant`** (`"secondary"`, `"default"`, `"destructive"`) or **raw
colors** (`"green"`, `"blue"`, `"yellow"`, `"purple"`, `"orange"`) — all rejected.

Fix = map each call to the new API. Suggested mapping:
`secondary→variant="subtle"`, `default→variant="solid"` (or `subtle`),
`destructive→color="error"`, `green→color="success"`, `yellow/orange→color="warning"`,
`blue/purple→color="info"`, neutral→`color="neutral"`. Verify against the actual `Badge`
props in `packages/patina-design-system` before mass-applying.

Sites:
- `components/catalog/catalog-filters.tsx:280`
- `components/catalog/rule-builder.tsx:259,269`
- `components/collections/collection-card.tsx:77,81,90`
- `components/collections/collection-details-form.tsx:89`
- `components/collections/collection-hero.tsx:54`
- `components/collections/collection-products-form.tsx:159`
- `components/collections/collection-rules-form.tsx:148`
- `components/collections/create-collection-modal.tsx:134`
- `components/media/media-badges.tsx:100`
- `components/navigation/PrimaryNav.tsx:74`
- `components/products/product-editor-modal.tsx:191`
- `components/products/product-filters.tsx:255,528`
- `components/products/tabs/details-tab.tsx:217,247`
- `components/products/tabs/inventory-tab.tsx:185,188`
- `components/products/tabs/media-tab.tsx:235,243`
- `components/products/tabs/pricing-tab.tsx:229`
- `components/products/tabs/seo-tab.tsx:259`
- `components/products/validation-issues-panel.tsx:250`
- `components/rooms/associated-room-scans.tsx:85`
- `components/shell/WorkstreamBoard.tsx:73`

(This group is a clean single-agent task — file-disjoint, mechanical, one API to learn.)

### Group B — `@patina/types` missing/renamed exports (TS2305/2552)
- `components/email-builder/EmailTemplateBuilder.tsx:15` and
  `stores/template-builder-store.ts:5` import `EmailTemplate` — **not exported** from
  `@patina/types`. Find the real type name (likely renamed) or add the export.
- `data/mock-designer-data.ts:1819` references `ProjectFilterInput` (tsc suggests
  `ProductFilterInput`) — likely a typo/rename.

### Group C — missing UI modules (TS2307)
These import shadcn-style paths that don't exist in this app:
- `components/auth/rate-limit-banner.tsx:4` → `@/components/ui/alert`
- `components/dashboards/DesignerAnalyticsDashboard.tsx:12` → `@/components/ui/skeleton`
- `data/mock-admin.ts:1` → `@/types`
Fix = point at the real design-system equivalent (or `@patina/design-system`), or this
file is dead (mock-admin may not belong in designer-portal — check usage).

### Group D — `use-projects.ts` spread of non-object (4× TS2698)
Lines `540, 556, 614, 630` — `{...x}` where `x` is a union that includes a non-object
(likely `unknown`/`Product[]`/`undefined`). Same pattern just fixed in `use-products.ts`
(commit `07f280b2`): narrow to the object form first (`const obj = Array.isArray(x) ? ... : x`)
or guard before spreading. Read the surrounding lines to see what's being merged.

### Group E — auth/session shape (TS2339/18048/2694)
- `components/auth/user-avatar.tsx:34,36` — `.image` not on the user type; the session
  user has `{id,email,name?,roles?,permissions?}`. Use `name`/avatar source that exists,
  or extend the type if avatar is real.
- `lib/rbac.ts:206,217` — `session.user.roles` possibly undefined; add `?? []` guard.
- `components/portal/engagement-timeline.tsx:47` — `global.JSX.Element` missing (React 19
  uses `React.JSX`). Use `React.JSX.Element` or `React.ReactElement` (same family as the
  r3f shim already added in `src/types/react-three-fiber.d.ts`).

### Group F — form/mutation payload mismatches (TS2353/2739/2561/2345)
Each is a call site sending a field the input type doesn't declare — either the field is
real (update the input type / hook) or it's the wrong field name. Investigate per-site:
- `app/(portal)/portal/communications/audiences/new/page.tsx:19` — `SegmentRules` needs
  `logic`+`conditions` (passing `{}`).
- `…/communications/campaigns/new/page.tsx:27` — `body` not in campaign payload.
- `…/communications/templates/new/page.tsx:20` — `subject` not in `Partial<EmailTemplate>`
  (ties to Group B — EmailTemplate shape).
- `…/settings/page.tsx:75` — `display_name` not in `UserProfile` update type.
- `…/vendors/new/page.tsx:20` — `category` not in `FindOrCreateVendorInput`.
- `components/portal/scope-builder/palette-builder.tsx:235` — `{paletteId,sourceImageUrl}`
  vs `UpsertPaletteInput`.
- `components/products/product-creation-wizard.tsx:79` (`description`→`seoDescription`),
  `:218` (`Product` not `Record<string,unknown>` — same index-signature issue as
  `ProductFilters`; widen the param or cast), `:224` (`response` is `unknown` — type the
  mutation result).
- `components/products/product-filters.tsx:95,107` — `string[]` assigned to a
  `string|number|boolean` field (a filter value type is too narrow for multi-select).

### Group G — design-system `DatePicker` (2× TS2322, in `packages/patina-design-system`)
`DatePicker.tsx:113,253` — react-day-picker `Matcher`/day-picker-props typing. This is in
a shared package (surfaces because it's in the app's tsconfig include). Either fix the
day-picker prop typing or confirm whether it should be in the app's type-check scope.

### Group H — misc one-offs
- `data/mock-designer-data.ts:1686,1722` `MockProduct[]` vs `Product[]`; `:1725` facets
  shape vs `CatalogSearchFacets`. (Mock data drifting from real types.)
- `lib/react-query.ts:87` — `suspense` no longer a valid QueryClient default option in
  TanStack Query v5; remove it (use `useSuspenseQuery` at call sites instead).
- `app/api/catalog/categories/tree/route.ts:48` — `.children` on `{}`; type the node.
- `app/api/auth/verify-email/route.ts:52` — arg `{query:{email}}` passed where `string`
  expected; check the verify call signature.
- `components/crm/MLPredictionsPanel.tsx:96` — `.upsell` on possibly-undefined prediction.
- `components/products/validation-issues-panel.tsx:47` — `response` is `unknown`.
- `components/products/product-editor-modal.tsx:158` — Dialog `children` prop typing.
- `components/products/tabs/inventory-tab.tsx:293` — Select `onValueChange` widens to
  `string` vs `AvailabilityStatus`.
- `components/rooms/associated-room-scans.tsx:106,186,213,293` — implicit-any callback
  params (`assoc`, `a`); annotate them.
- `e2e/crm/page-objects/base.page.ts:166` — async return type (e2e file; could also be
  excluded from app tsc scope like the other test excludes).

---

## SMOKE-05 — lint health
Migrate deprecated `next lint` → ESLint CLI (`eslint .` with a flat or legacy config).
`tsc` won't exit 0 until SMOKE-04 long tail is cleared; lint is separate.

## SEC-06/07 — Help content (code complete; **non-code remaining**)
Code alignment is done across all help layers (commits `9c01fbdd`, `065a6331` — `coalesce(flat, nested)` projections in `useHelpContent`, `ContextualHelpPanel`, `HelpSearch`). Remaining:
1. **Deploy the `helpContent` schema to the dataset** so docs are editable in the Studio UI
   (studio at `studios/help-system/`; see memory `project_sanity_help_system` — Free-plan
   gotcha needs a token for `sanity schema deploy`).
2. **Author the broader content library** (ongoing content task) against the wired surface
   keys in `packages/help-system/src/surfaceKeys.ts`. 5 section-intro docs already published
   to `kv3qrinl/production`.

## Feature items still open — **need a decision before starting (new deps)**
- **PROD-08/09/10 XLSX import** — CSV import shipped; XLSX needs a parser dep
  (`xlsx`/`papaparse`). Confirm dep choice with user.
- **SET-08 SMS** — plumbing is creds-gated; going live needs Twilio dep + creds. Confirm.
- **C-03 by-status faceted filters** — `aria-hidden` decorative stubs on the pilot-gated,
  data-less procurement zone (can't verify without seeded pilot data); by-vendor sort done.
- **X-05** — `/portal/resources` stuck-fade is a low-value focused-debug item; broader
  no-SSR blank is architectural (most data pages already use `LoadingStrata`).
- **PROD-12/13** — Library: 2 of 3 tabs live; "Teach the Engine" (low-confidence products)
  tab remains.
```
