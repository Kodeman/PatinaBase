# Field Companion Wave 2 — rulings index

Every `Ruling:` the Wave 2 conductor made, in the order made. Source of truth is the SDD
ledger (`progress.md`, beside this file). A ruling is a decision taken on Kody's behalf so the
wave kept moving rather than parking a day on a question; each carries what it costs if it was
wrong, so a wrong one can be found and undone.

Branch `feat/field-companion-w2`. Plan `../../plans/wave-2-plan.md`.
Spec `../../field-companion-package.md`. Programme rulings `../../field-companion-rulings.md`
(ratified by Kody 2026-08-24 — all decided; none re-opened by this wave).

**Two were overturned by the final whole-branch review** and are corrected in the wave report:
the stated reason for leaving S3's Inbox blurb, and the claim that the strengthened keyword test
stops the table shadowing from spreading. Both verdicts stood; both reasons were wrong.

- **Task 0 runs against this worktree's checkout of merged main, not the main checkout.** — The worktree is at `210564895` = `origin/main` with Wave 1 merged, so it IS the merged Wave 1; running Task 0's greps there avoids any git operation in the shared main checkout (patina-parallel-work step 1). — Cost if wrong: nil; the two trees are the same commit.
- **Task 0 Step 6 (create the worktree) is already done by the conductor and is a no-op for the implementer.** — The worktree had to exist before any task could be dispatched. Task 0's implementer instead runs the baseline `capture-gate.sh all` that Step 6 pairs with it, which is the part that carries information. — Cost if wrong: nil.
- **Task 0 Step 1's parenthetical "checked above and it is [absent], as of this writing" is stale and is not evidence.** — It was written on 2026-08-24 against a pre-Wave-1 tree. Task 0 reports what the merged code actually shows; Task 1's conditional fallback fires only if `isFeatureEnabled` is genuinely absent. — Cost if wrong: Task 1 either re-adds an existing symbol (compile error, caught by the gate) or omits a needed one (compile error, caught by the gate). Self-correcting.
- **The wave report stays at the plan's stated path `docs/design/field-companion/wave-2-report.md`; the SDD ledger, rulings index and device-pass notes are additionally preserved under `docs/design/field-companion/waves/wave-2/`.** — The plan names the report path in Task 6 and in its commit command; the orchestrator's brief names the waves/wave-2/ directory for the SDD artifacts. Both are satisfied without contradicting either. — Cost if wrong: a doc lives one directory from where someone looks.
- **`git push` is not attempted as a gate.** — The sandbox proxy blocks pushes from this shell; the orchestrator pushes. Task 6 records the merge-base and head shas and notes "push owed" instead of reporting BLOCKED. — Cost if wrong: nil; the orchestrator has the shas.
- **Every gate and build runs in the foreground.** — A child that backgrounds a job and ends its turn simply stops; a backgrounded xcodebuild would report a gate it never watched. — Cost if wrong: a falsely-green gate, which is the exact failure `patina-verification` exists to catch.
- **Every task runs the gate through one wrapper, `.superpowers/sdd/wave-2-plan/gate.sh`, instead of calling `scripts/capture-gate.sh` directly.** — `capture-gate.sh` sets NO `-derivedDataPath`, so it uses the shared default DerivedData; another live worktree (`.codex/worktrees/agent-ca2`, branch `feat/capture-producer-idempotency`) is also on Capture. The wrapper runs the same three steps (`generate_project.rb`, `xcodebuild build -scheme Capture`, `xcodebuild test -scheme CaptureKit`) against a per-worktree path `.build/gate-derived-9a7b4bc9ede9`, then runs `swiftlint lint --quiet --strict` explicitly (C2 — a green `all` never proves lint ran) and prints `GATE EXIT=<rc>`. It is a gitignored scratch tool; nothing in the repo changes. — Cost if wrong: a gate result that came from a different DerivedData cache than a bare `capture-gate.sh` would use; the commands and flags are otherwise identical, so a green here is a green there.
- **No `CAPTURE_SIM` override is needed.** — Both `capture-gate.sh:7` and `capture-shots.sh:17` already default to `iPhone 17`, which is the simulator on this machine (iOS 26.5). The plan's occasional "iPhone 16" prose is stale but drives nothing. — Cost if wrong: nil.
- **Task 1 does NOT touch `CaptureAnalytics.swift`; the conditional Task-0 fallback is dropped from its Files list and from its `git add` pathspec list.** — Wave 1 landed `isFeatureEnabled` verbatim to the spec the fallback would have produced; re-adding it is a duplicate declaration and a compile error. — Cost if wrong: none, the gate catches a missing symbol immediately.
- **The plan's lint arithmetic stands unchanged; the extraction in Task 1 Step 7 is still required.** — Task 0 reported `AppContainer.init()` at 73 lines and flagged the contradiction with a clean project-wide swiftlint. I measured it: 73 is the PHYSICAL line count (66–138); SwiftLint's `function_body_length` counts non-blank, non-comment lines, which is **57** — exactly the plan's figure. `.swiftlint.yml` sets `function_body_length: warning: 60` with no exclusion for this file, and `--strict` promotes it to an error, so the four new assignments (→61) genuinely break the gate and `makeWorkServices(deps:)` genuinely fixes it. — Cost if wrong: the gate fails loudly on the exact line; self-correcting.
- **Task 1 Step 7 repoints `VoiceNoteSheet.swift:70` and `SiteScanContextCapture.swift:45`.** — The plan describes the second gate as living inside `SiteScanContextModel.toggleVoice()`; in merged code `toggleVoice()` (`:104-107`) only does `guard voiceCaptureEnabled else { return }`, and the literal `analytics.isFeatureEnabled(...)` call is in the `voiceCaptureEnabled` computed property at `:45`. Same gate, one level of indirection out. — Cost if wrong: an unrepointed call site, which Step 7's own verifying grep catches.
- **Task 1 adds `import Foundation` to `CaptureTests/FieldCompanionPresentationTests.swift`.** — The plan's applied-rulings list says this import was added, but the file has only `import Testing` + `@testable import CaptureKit` (`:6-7`) and Step 2's appended test uses `JSONDecoder` and `Data`. The ruling was recorded but never landed in the plan's own code snippet. — Cost if wrong: nil; an unused import at worst, and CaptureTests is not linted.
- **I1 enters the fix loop — `AppContainer.swift:99-101`'s body comment ("The freeze leaves these returning the mock conformer") is false and contradicts the header the same commit rewrote.** All eight Phase-2 factories return real Supabase concretes. The brief mandated only *header* edits, so this is a gap the brief left rather than an implementer deviation — but ruling 2026-08-24 folded `AppContainer.swift`'s stale claims into this seam commit precisely because Task 4 is forbidden to touch the file, so nothing downstream would ever fix it. Shipping a self-contradicting composition root in the wave whose stated purpose is deleting false claims is the one outcome this wave cannot have. — Cost if wrong: a three-line comment edit outside the brief's literal Files list; trivially revertible.
- **I2 is NOT a Task 1 defect and does not enter the loop — `scripts/capture-shots.sh` is Task 5's file, by name and by line.** The reviewer correctly observed `F1.context` still cannot appear in a sweep (`ALL_SCREENS` is a hand-maintained 71-entry bash array, verified), but had no sight of the other tasks; Task 5 modifies `capture-shots.sh:2,:11,:12,:40,:46` and adds exactly that entry. Task 1 landed the plumbing (enum case, accessibility id, `routeWorkScreen` hop, coordinator latch); Task 5 uses it. — Cost if wrong: nil — if Task 5 somehow omits it, Task 5's own review catches it.
- **Task 1 owns `AppContainer.swift`'s BODY comments as well as its header, for this wave only.** — Ruling 2026-08-24 folded "every `AppContainer.swift` header edit" into the seam commit and Task 4's brief explicitly fences it out ("this task does not touch that file"). A false claim in that file's body is therefore unowned by every other task in the wave: if Task 1 does not fix it, the wave ships a composition root whose header and body contradict each other, in the wave whose stated purpose is deleting false claims. Scope stays narrow — only claims that are now demonstrably false, not a general comment pass. — Cost if wrong: a three-line comment edit outside the brief's literal Files list; trivially revertible and covered by the scoped re-review.
- **the `ContentState` census gap is recorded in the wave report as a spec correction, not fixed in code.** — `ContentState` values are built through type inference (`.init(queued:…)`), so `grep -rn "ContentState"` misses all three `LocalCaptureSyncService` construction sites entirely. That is how spec §5.5 came to name the wrong call sites (`LocalCaptureSyncController`/`CaptureLiveActivityController`) and how the plan inherited stale line numbers. The real sites are `Capture/Services/Sync/LocalCaptureSyncService.swift:184,:717,:825` — app target, not CaptureKit — and `CaptureLiveActivityController` only ever receives an already-built `ContentState` (`:35,:55,:63`). Nothing in the code is wrong; the spec's account of it is. — Cost if wrong: a spec paragraph stays stale one wave longer.
- **Task 2's implementer is dispatched concurrently with Task 1's scoped re-review.** — The re-review's entire diff is four comment lines in `AppContainer.swift`; Task 2 touches `ViewfinderModel`, `S3DestinationScreen`, `RecognitionServices`, `HeuristicSmartGuessService` and two new files, and never opens `AppContainer.swift`. Only ONE implementer is live at a time (the SDD bar), and a NOT-ADDRESSED verdict would produce another comment-only fix that cannot conflict with Task 2's files. — Cost if wrong: a comment-only fix round would interleave with Task 2's commit in the log; no code conflict is reachable.
- **Task 4's census regex is tightened with a word boundary after the bare numbers, and `CaptureTests/ManifestTests.swift:147` is left untouched.** — The brief's regex alternative `the 51` (no trailing boundary) matches "parsing **the 51**2-byte header fields" in `ManifestTests.swift:147`, an unrelated and correct comment about tar ustar headers. Task 4 Step 5 expects "both greps print nothing", which is unachievable as written — the implementer would either wrongly edit a correct test comment or stall on a census that can never come clean. Changing `all 51`/`the 51`/`all 71`/`the 71` to `all 51\b`/`the 51\b`/`all 71\b`/`the 71\b` drops the false positive (12 hits → 11) and keeps all eleven real targets. — Cost if wrong: a stale count somewhere the tightened regex no longer reaches; mitigated because every one of the eleven real sites is enumerated above by file and line, so Task 4 works from the list, not only from the regex.
- **No further `devicectl` probing while any gate is in flight.** — A single `xcrun devicectl list devices` returned `CoreSimulatorService connection became invalid`, `simdiskimaged crashed or is not responding`, and finally `Timed out waiting for CoreDeviceService to fully initialize`. I immediately re-checked `simctl list devices booted` and the `iPhone 17` simulator is still Booted and healthy, so Task 2's in-flight `xcodebuild` was not harmed — but a probe that can disturb CoreSimulatorService has no business running concurrently with a gate. The device attempt happens once, in Task 6, after every gate is done. — Cost if wrong: the device pass starts a few minutes later than it could have.
- **Task 3's implementer is dispatched concurrently with Task 2's review.** — Same basis as the Task 1/2 overlap: only ONE implementer is live at a time, and Task 3's entire diff is deleting `Capture/Features/Resilience/LowLightTorchOverlay.swift` plus the regeneration, which shares no Swift file with Task 2's six. Task 3 is explicitly forbidden to touch `ResilienceScreens.swift:9` (Task 4's line). — Cost if wrong: a Task 2 fix round would interleave with Task 3's commit in the log; no code conflict is reachable.
- **Task 4 is dispatched on sonnet, not the brief's haiku model hint.** — The brief's own hint concedes the tension: "mechanical text edits, but every claim must be checked against the code before it is rewritten, not assumed." That is seven files spanning Swift headers and a README, each edit gated on verifying a claim about live code, with a copy rule and three fenced-out files to respect. The orchestrator's ladder puts multi-file work on sonnet; the cost of a literal-minded transcription here is a *new* false claim in the wave whose purpose is deleting false claims. — Cost if wrong: a slightly more expensive model on a text task.
- **Task 5 does NOT run concurrently with Task 4.** — Beyond the SDD one-implementer-at-a-time bar, Task 5's Steps 1/3/4 run `capture-shots.sh`, which repeatedly installs and launches the app on the booted `iPhone 17` simulator, while Task 4's gate drives an `xcodebuild test` against the same simulator. Separate DerivedData paths do not separate the simulator itself; interleaving an install/launch sweep with a test run would corrupt both signals. — Cost if wrong: a few minutes of serialised wall-clock that could in principle have overlapped.
- **F-1 enters the fix loop — `setConfidence` fires even when `setValue` refused the write.** `setValue` silently returns when a field already carries non-`smartGuess` provenance, but the brief's replacement loop calls `setConfidence` unconditionally, and `FieldCapturePayload.swift:163` ships `guessConfidenceRaw` as `payload.guesses`. So a field the designer typed can reach `products.capture_provenance` as `provenance: "manual"` **plus** `guesses: {"material": 0.55}` — a confidence attached to a value the reader never wrote. That is the mirror image of the defect this task exists to remove, and the OLD code did not have it (it guarded before both calls). Introduced by the brief's own body. — Cost if wrong: a one-line provenance check; the alternative is shipping the wave's signature defect in a new coat.
- **F-2 enters the fix loop, but ONLY the badge half.** `CaptureCardOverlay`'s `guessRow` renders `ProvenanceBadge(specimen.provenance(for:) ?? .smartGuess)` unconditionally, so a photo the reader explicitly declined to place now shows *Category "—" [guess]* and *Material "—" [guess]* — the app labelling as a guess something it never guessed, on the screen the designer sees after every shutter. Before this task that badge was honest, because every capture carried the hardcoded guess; Task 2 is what turned it into a lie, so it is this task's regression even though the file is outside the brief's list. **The "Material is always —" half is NOT fixed**: it follows from the brief-mandated `ocr: []`, and showing "—" for something we did not read is honest. Restoring material inference would mean wiring OCR into the photo path — unrequested, and wave-3+ territory. — Cost if wrong: a badge stops appearing where a designer expected one; the device pass sees it either way.
- **F-3 enters the fix loop — the async writer can mutate an already-routed record.** The Task guards only on `currentSpecimen(id:) != nil`. The designer can hit `saveFromCard()` → `sync.route(...)` inside the read window, after which the late write calls `setValue`/`touch()`/`store.save()` on a record whose payload has already been uploaded, diverging the local record from what `capture_provenance` received. `CaptureSyncService.swift:68` states the policy outright: *"Once a record leaves the device-local phase, review must not rewrite it."* — Cost if wrong: one added phase check; without it the wave's own honesty guarantee is violated by a race.
- **F-5 enters the fix loop — the synchronous multi-MB HEIC read moves inside the Task.** The brief's shape does `try? Data(contentsOf:)` on the `@MainActor` shutter path *before* spawning the Task, at every capture. The OLD code did no disk read at all, so this is a NEW main-actor stall on the most latency-sensitive path in a camera app. `photo.filename`/`.width`/`.height` and `store.mediaURL(for:)` are MainActor reads and stay put; only the `Data(contentsOf:)` moves. — Cost if wrong: ~3 lines; strictly less main-thread work than the brief's shape.
- **F-6 + F-7 enter the fix loop together — the wave's headline claim gets a test that can actually fail.** `anUnplaceableLabelWritesNothingSoThereIsNothingToConfirm` asserts nothing new: both halves are verbatim duplicates of other tests, and nothing in it runs the write path — it would pass unchanged if `fieldsWorthRecording` were never wired into `ViewfinderModel` at all. `everyKeywordInTheTableResolves` asserts `!= nil`, which `contains` makes reflexively true, so a reordering that made `"textile"` resolve to `.tile` would still pass. Both are fixable INSIDE CaptureKit: compose the CaptureKit-side filter with the CaptureKit-side `Specimen` accessors — apply `fieldsWorthRecording` to an in-memory `Specimen` exactly as the viewfinder's loop does, then assert `hasUnconfirmedGuess`. — Cost if wrong: two tests that assert more than before. **S3's `.inbox` hold remains genuinely untestable under C1 (app-side SwiftUI) — recorded as ⚠ not-covered and carried to the device pass, not faked.**
- **F-4 does NOT enter the fix loop — it is recorded as owed, prominently, in the wave report.** The reviewer is right that first-match-wins substring matching sends `"table lamp"` and `"desk lamp"` to `.table` rather than `.lighting`, and `"tapestry"` to `.plumbing`, and right that this task makes a previously-inert defect fire on every capture. But the table is brief-mandated verbatim, it predates this wave, and the correct repair (ordering semantics, or compound-keyword precedence) is a design decision that deserves its own attention rather than improvisation inside a fix round. The wave's own safeguard contains the harm: FC-R12 means nothing auto-applies at any confidence — she confirms every read — and a wrong-but-genuinely-computed mapping is still within this wave's thesis, which is that no capture ships a guess the app did not compute. F-6's strengthened test locks the current ordering so any future edit that worsens shadowing fails loudly. — Cost if wrong: a designer photographing a table lamp is offered "Table" and corrects it; the device pass will surface it, and it is written down rather than discovered later.
- **Task 4's one Important finding enters a fix loop — the new `AVFoundationCameraService.swift` header cites `AppContainer.swift:104-110`, which is Work-service wiring, not camera wiring.** I verified it myself: `:104-109` assigns `projects`/`leads`/`decisions`/`messaging`/`receiving`/`portalAuth`/`siteScan`/`siteRequests`/`drainer`, and the real `#if targetEnvironment(simulator) … MockCameraService … #else … AVFoundationCameraService … #endif` block is at `:111-117`. The substantive claim is true and confirmed; only the anchor is wrong. The citation is brief-verbatim, so it originates in the plan — but a freshly-planted incorrect line reference is precisely the defect class this task exists to remove, and it would rot further as later waves edit `AppContainer.swift`. **The fix follows this task's own established pattern: drop the brittle line citation and name the mechanism instead** — exactly what it already did for `AppConfiguration.swift` and `CaptureKitMocks.swift`, both of which dropped their counts rather than restating them. — Cost if wrong: a header names the simulator branch instead of a line number, which is strictly more durable.
- **F-3's predicate is `transferState.phase == .local` used directly rather than routed through `canCull` — ACCEPTED.** Cull-deck semantics differ from "has this left the device", and borrowing `canCull` would couple the late-write guard to an unrelated policy that a later wave could legitimately change. — Cost if wrong: the guard drifts from cull policy, which is the intent.
- **F-5 residual is PARKED, not expanded.** The `Task` inherits `@MainActor` from `ViewfinderModel`, so the multi-MB `Data(contentsOf:)` still runs on the main actor — just later than before, and off the synchronous shutter path. Making the media read genuinely concurrent needs a `nonisolated async` read, which is a Wave 3 follow-up adjacent to the C3/inline-mic work, not a Wave 2 scope expansion. The fix as landed is a strict improvement over both the brief's shape and the pre-wave code. — Cost if wrong: a main-actor disk read persists one wave longer, after the card has already appeared.
- **Task 6 waits for BOTH outstanding scoped re-reviews rather than starting concurrently.** — Task 6 is the wave gate: its first step runs the full gate on the *finished* branch, and its report records every task's final verdict. Starting it while Task 2's and Task 4's re-reviews are open would gate a branch that could still move and write a report against incomplete verdicts. The two pending diffs are small and I have personally verified both, so the risk of a further round is low — but "low risk of moving" is not "final", and the wave report is the artefact Kody reads. — Cost if wrong: a few minutes of serialised wall-clock at the end of the wave.
- **F-5's residual stays PARKED (unchanged from my earlier ruling), now independently confirmed.** The reviewer derived the same conclusion before reading the implementer's disclosure: a plain `Task {}` from a `@MainActor` context inherits that isolation through its synchronous prefix, so `Data(contentsOf:)` still runs on the main thread — deferred to a later run-loop tick rather than blocking the synchronous shutter call. It cites the codebase's own convention as corroboration (`SupabaseSiteScanService.swift:104` reaches for `Task.detached` precisely when it wants off-main work). The described symptom is fixed (the card no longer waits on I/O); the I/O is not yet off-main. Self-disclosed by the implementer, not a hidden regression. — Cost if wrong: a main-actor disk read persists into Wave 3, after the card has already appeared.
- **F-7's duplication residual is PARKED for the final whole-branch review, not turned into another fix round.** The finding itself is ADDRESSED — the test now exercises the real contract in both directions. The reviewer's further point is that a *better* fix existed: extract `Specimen.recordSmartGuess(_:)` into CaptureKit and have both `ViewfinderModel.applySmartGuess` and the test call it, eliminating the drift hazard entirely while still respecting C1. That is correct and worth doing — but it moves shipped logic into a new CaptureKit API and changes the production call path, which is a structural refactor arriving after the gate is green, at the end of a wave, and it deserves its own review rather than being squeezed into a closing round. The mirror is verified line-for-line identical today and mutation-checked. — Cost if wrong: if a later wave edits the production loop without editing the mirror, the test keeps passing while the shipped path breaks. **Recorded as owed in the wave report, with the exact remedy named.**
- **`origin/main` is merged BEFORE Task 6's wave gate, not after it.** — The orchestrator's sequence puts the merge last before the final review; running it one step earlier means the wave gate proves the *merged* tree builds, which is the tree that will actually land on main, and lets Task 6's report record the true final head instead of a sha that changes under it. All task work was already complete, so the merge is still the last thing before the final review. — Cost if wrong: nil; the merge carries no iOS file.
- **the merge commit's subject was amended from `merge(field-companion-w2):` to `chore(field-companion):`.** — The orchestrator's standing rule forbids `merge:` subjects, and project memory records that the husky commit-msg hook rejects them; the hook does not run on merge commits, so it went through unnoticed and I caught it on read-back. Amended before anything built on it. — Cost if wrong: nil.
- **the SDD ledger and rulings index are committed as tracked docs, closing Task 6's concern #1.** — Task 6 correctly refused to stage files outside its authorization and flagged that `waves/wave-2/progress.md` and `rulings-index.md` were still untracked while the wave report points at both as the record of deferred minors and every conductor ruling. After a merge those pointers would dangle. The orchestrator's brief explicitly requires the ledger, rulings index and device-pass notes preserved as tracked docs under `docs/design/field-companion/waves/wave-2/`, so this is authorized work I owed, not scope creep. The rest of the untracked `docs/design/field-companion/` tree (the spec, the rulings file, the `plans/` directory) stays untracked — it predates this wave and is not mine to land. — Cost if wrong: three documentation files enter git that could have stayed local.
- **all four blocking findings go into ONE fix dispatch, with four cheap true-fixes folded in; no code changes.** — Each is the branch *adding* a false claim, which is the exact defect this wave exists to remove: (1) two **"device-verified"** claims in shipped source (`SmartGuessKeywords.swift:5-6`, `SmartGuessTests.swift:8-9`) in a wave whose device pass was blocked and all five criteria recorded not-run; (2) `CameraModeSeamTests.swift:4-6` describing three `ForEach(CameraMode.allCases)` call sites, present tense with line citations, that commit `13cf6a28f` changed in the same commit; (3) `AppContainer.swift:159-160`'s "both properties must hold the SAME instance" about a **struct** — the sharpest finding in the branch, because the wave *detected* this as Task 1 minor M5, corrected the task report, and left the false comment in shipped source; (4) `ViewfinderModel.swift:456-458` promising "the C3 card appears at once and fills in", a designer-visible property the parked F-5 residual undermines and nobody has observed. Folded in: the duplicated `capture-run.sh,` at `AppConfiguration.swift:96-97`, README `:14`'s "drives all 75" (it drives 72), the wave report's "Six commits" against its own nine-row table, and the stale `f41150e37` head sha in the field the orchestrator merges on. — Cost if wrong: text-only edits re-gated by a full green run; no behaviour moves.
- **finding 5 (the late write dropped when she saves before the read lands) is NOT fixed this wave.** The F-3 `.local` guard is correct and stays. But composed with F-5's main-actor residual, the real window is a multi-MB HEIC read plus a Vision pass racing a one-tap Save — so the wave's headline feature may in practice record nothing on the fast path, and **nothing at any level verifies it ever lands a value** (device criterion 1 not run; the CaptureKit tests exercise the mirrored loop, never the real timing). Behaviour is honest — it records nothing rather than something false, which is the thesis — so it is a delivery risk, not a lie. Named as a Wave 3 item to be settled by device criterion 1. — Cost if wrong: the real reader ships and rarely fires on fast captures, discovered on a device walk rather than here.
- **finding 11 (`.dimensions`) is documented, not fixed.** `setValue` `break`s on `.dimensions` without writing a value but stamps `provenanceRaw` and calls `touch()` outside the switch, so a `.dimensions` suggestion would stamp guess provenance for a value nobody wrote — and the F-1 read-back guard would *pass*, because provenance really is `.smartGuess`. That is the wave's signature defect one field over. Unreachable today: `HeuristicSmartGuessService` emits only `.category`/`.material`/`.colorway`. A one-line `guard suggestion.key != .dimensions` in `fieldsWorthRecording` would close it, but adding a behavioural change to a text-only closing pass after the gate is green is the wrong trade. Re-graded from the ledger's "latent hole" and written into the report with the fix named. — Cost if wrong: if a future reader emits `.dimensions`, the defect is live; the report tells the next wave exactly where and what to do.
- **the two residual Minors ship as-is.** — Both are stylistic. There is no second fix wave by design, and reopening a text-only closing pass to de-duplicate an adjacent comment would risk a real edit for no truth gain. — Cost if wrong: two slightly redundant comment lines in the composition root.
- **`.superpowers/sdd/wave-2-plan/` is NOT deleted, contrary to the SDD skill's finish step.** — The skill deletes the plan workspace once the final review is clean, on the grounds that git history is then the record. Here the worktree deliberately stays live for the orchestrator's merge, the branch is unpushed, and the ledger/rulings/device-pass notes are already preserved as tracked docs under `docs/design/field-companion/waves/wave-2/`. Deleting the scratch workspace while the branch has not landed would destroy the recovery map for a wave that is not yet finished. — Cost if wrong: a gitignored scratch directory survives until the orchestrator retires the worktree.
