# SDD ledger — plan: docs/design/field-companion/plans/wave-2-plan.md

Conductor: Wave 2 (Field Companion), "Nothing the app says about a capture is a lie".
Worktree `/Users/kody/Code/patina-merged/.claude/worktrees/field-companion-w2`, branch `feat/field-companion-w2`,
merge-base `21056489551b4269cf97a9b55b137dbd3519d6b4` (origin/main, Wave 1 merged).
Spec: `docs/design/field-companion/field-companion-package.md`. Rulings: `docs/design/field-companion/field-companion-rulings.md` (ratified 2026-08-24).
Plan review `plans/wave-2-plan-review.md` was fully applied before execution.

## Setup facts

- Worktree created from local ref `210564895` (never `git fetch` — sandbox proxy blocks it).
- Worktree creation required `dangerouslyDisableSandbox` — the sandbox denies writes to `**/.env.*`, and checkout writes `services/*/.env.example`.
- `docs/design/field-companion/` is PARTLY tracked on main (wave-1 README, device-pass-spec, wave-1p-plan) and partly untracked (the spec, rulings, all wave-2/3/4 plans). Copied whole into the worktree; only wave-2's own new docs get committed.
- `Secrets.swift` copied into the worktree for both Patina and Capture (gitignored, never committed). No `Secrets.xcconfig` exists anywhere under apps/mobile — nothing to copy.
- Simulator: `iPhone 17` (C8850509-C7DC-43C5-9226-9446404EE98A) booted. Plan/scripts default to `iPhone 17`.
- swiftlint present at /opt/homebrew/bin/swiftlint → C2 still applies, run it explicitly every task.
- `.superpowers/` is gitignored; SDD artifacts are mirrored to `docs/design/field-companion/waves/wave-2/` at the wave gate.

## Preflight scan — cross-task table

| Shared file / interface | Producer | Consumer(s) | Finding |
|---|---|---|---|
| `CaptureScreenID.swift` (header, 4 new cases, `sweepSuffix`) | T1 | T4 (verify-only), T5 (`f1Context`) | Clean — plan forbids T4 editing it; T4 after T1 |
| `AppContainer.swift` (2 seams + ALL header lines) | T1 | T2 (`smartGuess`), T4 (explicitly does NOT touch) | Clean — ruling 2026-08-24 folded every header line into T1 |
| `CaptureDeepLink.swift` | T1 (`route(for:)`, `routeWorkScreen`, `realm(for:)`) | T4 (`:6` header count only) | Clean — disjoint lines, strictly sequential |
| `ViewfinderModel.swift` | T1 (`:152` cycleMode) | T2 (`:14-30,:70-80,:410-422`) | Clean — disjoint regions, T2 after T1 |
| `ResilienceScreens.swift:9` | T3 (deletes the view, leaves the comment) | T4 (rewrites the comment) | Clean — plan states the hand-off explicitly |
| `Capture.xcodeproj/project.pbxproj` | T1, T3 (file add/delete) | every task | Clean — regenerated per task, dispatches strictly sequential (C4) |
| `CaptureFeatureFlags` (new) | T1 Step 5 | T1 Step 7 call-site repoint, T1 tests | Clean — same task |
| `CaptureCoordinator.siteScanContextRequested` | T1 | T5 (harness hop) | Clean |
| `SmartGuessKeywords` / `SmartGuess.fieldsWorthRecording` (new) | T2 | T2 tests only | Clean |
| `capture-shots.sh` "72 built screens" | T5 | T4 README `:120` also writes "72 built screens" | Clean — same number, T4 before T5, no file overlap |
| `SpeechVoiceNoteService.swift:7` | T0 Step 4 (records state) | T4 Step 3 (conditional edit) | Clean — T4 is verify-or-fix, both branches written out |
| `OfflineQueueBanner` render state | T0 Step 3 (records) | T4 Step 3 (picks one of two wordings) | Clean — both wordings written out |
| `wave-2-report.md` | T6 | orchestrator | Clean |

## Preflight scan — per-task self-consistency

| Task | Tests vs code | Files created vs later touched | Finding |
|---|---|---|---|
| 0 | n/a (no diff, no commit) | none | Clean. Ruling P-3 below reconciles Step 1's "as of this writing `isFeatureEnabled` is absent" against the merged Wave 1. |
| 1 | `CaptureScreenIDTests.everyScreenIDIsUnique` asserts no id's `sweepSuffix` is a tail of another id's rawValue — **verified against the real enum before dispatch**: 71 existing cases + the 4 new ones = 0 raw dupes, 0 suffix collisions. The test as written passes. `CameraModeSeamTests` + `CaptureFeatureFlagsTests` construct only types the same task creates. | creates 3 test files + `CaptureFeatureFlags.swift`, deletes `FieldPlaceholderScreen.swift` | Clean. Residual risk logged as R-1 (lint arithmetic). |
| 2 | `SmartGuessTests` constructs `FieldSuggestion(key:value:confidence:)` with no `source` — **verified**: `RecognitionServices.swift:117` gives `source` a `= .smartGuess` default, so the tests compile and Step 5's `suggestion.source` read is real. `UnconfirmedGuessTests` uses `CaptureStore.inMemory()` + `newDraft()` + `setValue`/`setConfidence`, all pre-existing. | creates `SmartGuessKeywords.swift` + `SmartGuessTests.swift` | Clean |
| 3 | States plainly that a deletion has no unit test (C1/C3) rather than faking one | deletes 1 file | Clean |
| 4 | States plainly that comments/README have no unit test | edits 7 files | Clean — depends on T1 and T3 having landed, and the plan says so |
| 5 | No unit test; the check is the rendered PNG, which the plan requires be opened and read | edits 1 shell script | Clean |
| 6 | Gate + device pass + report | creates `wave-2-report.md` | Clean |

### R-1 (risk, not a conflict) — Task 1's lint arithmetic
The plan computes `AppContainer.init()` at 57 body lines today, 61 after the two seam pairs, and prescribes extracting the Phase-2 factory block to land at 53. Wave 1 merged after that count was taken. The remedy (extract `makeWorkServices(deps:)`) is correct whatever the starting number; the implementer measures the real figure and reports it. Not a stop.

## Preflight rulings

Ruling: **Task 0 runs against this worktree's checkout of merged main, not the main checkout.** — The worktree is at `210564895` = `origin/main` with Wave 1 merged, so it IS the merged Wave 1; running Task 0's greps there avoids any git operation in the shared main checkout (patina-parallel-work step 1). — Cost if wrong: nil; the two trees are the same commit.

Ruling: **Task 0 Step 6 (create the worktree) is already done by the conductor and is a no-op for the implementer.** — The worktree had to exist before any task could be dispatched. Task 0's implementer instead runs the baseline `capture-gate.sh all` that Step 6 pairs with it, which is the part that carries information. — Cost if wrong: nil.

Ruling: **Task 0 Step 1's parenthetical "checked above and it is [absent], as of this writing" is stale and is not evidence.** — It was written on 2026-08-24 against a pre-Wave-1 tree. Task 0 reports what the merged code actually shows; Task 1's conditional fallback fires only if `isFeatureEnabled` is genuinely absent. — Cost if wrong: Task 1 either re-adds an existing symbol (compile error, caught by the gate) or omits a needed one (compile error, caught by the gate). Self-correcting.

Ruling: **The wave report stays at the plan's stated path `docs/design/field-companion/wave-2-report.md`; the SDD ledger, rulings index and device-pass notes are additionally preserved under `docs/design/field-companion/waves/wave-2/`.** — The plan names the report path in Task 6 and in its commit command; the orchestrator's brief names the waves/wave-2/ directory for the SDD artifacts. Both are satisfied without contradicting either. — Cost if wrong: a doc lives one directory from where someone looks.

Ruling: **`git push` is not attempted as a gate.** — The sandbox proxy blocks pushes from this shell; the orchestrator pushes. Task 6 records the merge-base and head shas and notes "push owed" instead of reporting BLOCKED. — Cost if wrong: nil; the orchestrator has the shas.

Ruling: **Every gate and build runs in the foreground.** — A child that backgrounds a job and ends its turn simply stops; a backgrounded xcodebuild would report a gate it never watched. — Cost if wrong: a falsely-green gate, which is the exact failure `patina-verification` exists to catch.

## Task log

Task 0: dispatched (implementer agentId `a084a6fa52642a5c8`, sonnet) — pre-flight verification of merged Wave 1's four seam names, baselines, and Task 1 line-anchor drift. No commit expected. BASE = 21056489551b4269cf97a9b55b137dbd3519d6b4.

Ruling: **Every task runs the gate through one wrapper, `.superpowers/sdd/wave-2-plan/gate.sh`, instead of calling `scripts/capture-gate.sh` directly.** — `capture-gate.sh` sets NO `-derivedDataPath`, so it uses the shared default DerivedData; another live worktree (`.codex/worktrees/agent-ca2`, branch `feat/capture-producer-idempotency`) is also on Capture. The wrapper runs the same three steps (`generate_project.rb`, `xcodebuild build -scheme Capture`, `xcodebuild test -scheme CaptureKit`) against a per-worktree path `.build/gate-derived-9a7b4bc9ede9`, then runs `swiftlint lint --quiet --strict` explicitly (C2 — a green `all` never proves lint ran) and prints `GATE EXIT=<rc>`. It is a gitignored scratch tool; nothing in the repo changes. — Cost if wrong: a gate result that came from a different DerivedData cache than a bare `capture-gate.sh` would use; the commands and flags are otherwise identical, so a green here is a green there.

Ruling: **No `CAPTURE_SIM` override is needed.** — Both `capture-gate.sh:7` and `capture-shots.sh:17` already default to `iPhone 17`, which is the simulator on this machine (iOS 26.5). The plan's occasional "iPhone 16" prose is stale but drives nothing. — Cost if wrong: nil.

### Task 0 findings (report: `.superpowers/sdd/wave-2-plan/task-0-report.md`)

Decision **GO**. Baseline on the unmodified worktree is GREEN: `✔ build`, `✔ tests`, `✔ lint`; a separate `swiftlint lint --quiet --strict` exited 0, and a non-quiet run confirmed **230 files scanned, 0 violations** — so lint genuinely ran (C2 satisfied).

All four Wave-1 names Wave 2 consumes are present on merged main with the expected signatures: `CaptureAnalytics.isFeatureEnabled` (protocol req `:18` + fail-closed `{ false }` default `:30`), `CaptureMediaMime.forFilename`/`.bucketAllowed`, `VoiceRecordingPolicy`'s five members, `CaptureRoutingMemory.stamped(onto:)`. 16 of 17 of Task 1's cited line anchors still exact.

Recorded for later tasks:
- `OfflineQueueBanner` **is** rendered on C1 (`ViewfinderScreen.swift:43`) — Wave 1 package 1-13 landed. Task 4 Step 3 uses the PRIMARY wording, not the ⚠ fallback.
- `SpeechVoiceNoteService.swift:7`'s "raw audio is always kept" claim is **already gone** — Task 4 records "already true, no edit" and invents nothing.
- `MockCaptureAnalytics` has **no** `isFeatureEnabled` override → falls through to `{ false }`. Task 1's `.allOff` in mock mode drops nothing a mock could otherwise show; Task 5's sweep count needs no re-baseline on this account.
- `wave-3-plan.md`'s `allCases` claim is already withdrawn by Wave 3's own Ruling 6 (`wave-3-plan.md:31`). Nothing owed; still reported at the wave gate.
- Baseline counts: 71 `CaptureScreenID` cases; stale-count sites exactly where the plan predicted.

Ruling: **Task 1 does NOT touch `CaptureAnalytics.swift`; the conditional Task-0 fallback is dropped from its Files list and from its `git add` pathspec list.** — Wave 1 landed `isFeatureEnabled` verbatim to the spec the fallback would have produced; re-adding it is a duplicate declaration and a compile error. — Cost if wrong: none, the gate catches a missing symbol immediately.

Ruling: **The plan's lint arithmetic stands unchanged; the extraction in Task 1 Step 7 is still required.** — Task 0 reported `AppContainer.init()` at 73 lines and flagged the contradiction with a clean project-wide swiftlint. I measured it: 73 is the PHYSICAL line count (66–138); SwiftLint's `function_body_length` counts non-blank, non-comment lines, which is **57** — exactly the plan's figure. `.swiftlint.yml` sets `function_body_length: warning: 60` with no exclusion for this file, and `--strict` promotes it to an error, so the four new assignments (→61) genuinely break the gate and `makeWorkServices(deps:)` genuinely fixes it. — Cost if wrong: the gate fails loudly on the exact line; self-correcting.

Ruling: **Task 1 Step 7 repoints `VoiceNoteSheet.swift:70` and `SiteScanContextCapture.swift:45`.** — The plan describes the second gate as living inside `SiteScanContextModel.toggleVoice()`; in merged code `toggleVoice()` (`:104-107`) only does `guard voiceCaptureEnabled else { return }`, and the literal `analytics.isFeatureEnabled(...)` call is in the `voiceCaptureEnabled` computed property at `:45`. Same gate, one level of indirection out. — Cost if wrong: an unrepointed call site, which Step 7's own verifying grep catches.

Ruling: **Task 1 adds `import Foundation` to `CaptureTests/FieldCompanionPresentationTests.swift`.** — The plan's applied-rulings list says this import was added, but the file has only `import Testing` + `@testable import CaptureKit` (`:6-7`) and Step 2's appended test uses `JSONDecoder` and `Data`. The ruling was recorded but never landed in the plan's own code snippet. — Cost if wrong: nil; an unused import at worst, and CaptureTests is not linted.

Task 0: complete (no commit — this task produces a decision, not a diff; plan states "There is no commit"). Baseline GREEN. Wave-1 gate: GO.

Task 1: dispatched (implementer agentId `ac8d36a351714cf16`, opus) — the foundation-seam commit. BASE = 21056489551b4269cf97a9b55b137dbd3519d6b4. Dispatch carried corrections 1-6 (drop CaptureAnalytics edit; call sites :70/:45; CaptureSheet at :46-84; lint arithmetic confirmed and extraction required; add import Foundation; suffix-uniqueness test pre-verified to pass).

Task 1: implementer returned DONE_WITH_CONCERNS. Commit `13cf6a28f` (27 files, +913/-663). Gate: `✔ build` / `✔ tests` / `✔ lint` / `GATE EXIT=0`; 337 CaptureKit tests across 49 suites green, 14 of them new. `AppContainer.init()` = 56 SwiftLint body lines after the extraction (ceiling 60).
Three self-reported deviations, all forced: (1) `self.guestSiteRequests = self.siteRequests` does not compile — `SiteRequestService` and `GuestSiteRequestService` are unrelated protocols; the concrete `SupabaseSiteRequestService` conforms to both and is carried out of the helper and assigned to both. (2) The brief's 10-member tuple return trips SwiftLint `large_tuple`; replaced with a `private struct WorkServices` — the rule was NOT silenced, and the struct costs a few more init lines than a tuple would (56, not the brief's predicted 53). (3) Two files outside the brief's Files list edited — `VoiceNoteSheet.swift` and `SiteScanHostScreen.swift` — required by my correction 2 to thread the `flags` seam; plus two `.xcscheme` files whose `CaptureTests` blueprint id `generate_project.rb` changed.

Conductor check (independent, before review): `generate_project.rb` is **deterministic** — two consecutive regenerations leave a clean tree with zero churn. So the `.xcscheme` blueprint-id change was a one-time consequence of the file set changing (4 files added, 1 deleted), not recurring noise that will re-dirty every later task. Committing the schemes alongside the pbxproj was correct: without them `xcodebuild -scheme CaptureKit test` breaks for anyone who has not re-run the generator.

Task 1: task review dispatched (reviewer agentId `ac864f38b627888c4`, opus, fresh context). Review package `review-210564895..13cf6a28f.diff`.

### Task 1 review (reviewer `ac864f38b627888c4`, opus) — Spec ✅ PASS · Quality Approved · 0 Critical · 2 Important · 12 Minor

Reviewer independently re-ran `swiftlint lint --quiet --strict` (exit 0, clean), confirmed `.swiftlint.yml` untouched and no `swiftlint:disable` added, confirmed `CaptureAnalytics.swift` absent from the diff (correction 1 honoured), confirmed `viewfinderSelectable` is the literal with `.voice` absent and **zero** `CameraMode.allCases` residue in app code, confirmed `route(for:)` still exhaustive with the three reserved ids on `break`, confirmed `FieldPlaceholderScreen` has zero repo-wide hits, and confirmed the three `ContentState` construction sites untouched. All three implementer deviations verified and upheld as forced.

Ruling: **I1 enters the fix loop — `AppContainer.swift:99-101`'s body comment ("The freeze leaves these returning the mock conformer") is false and contradicts the header the same commit rewrote.** All eight Phase-2 factories return real Supabase concretes. The brief mandated only *header* edits, so this is a gap the brief left rather than an implementer deviation — but ruling 2026-08-24 folded `AppContainer.swift`'s stale claims into this seam commit precisely because Task 4 is forbidden to touch the file, so nothing downstream would ever fix it. Shipping a self-contradicting composition root in the wave whose stated purpose is deleting false claims is the one outcome this wave cannot have. — Cost if wrong: a three-line comment edit outside the brief's literal Files list; trivially revertible.

Ruling: **I2 is NOT a Task 1 defect and does not enter the loop — `scripts/capture-shots.sh` is Task 5's file, by name and by line.** The reviewer correctly observed `F1.context` still cannot appear in a sweep (`ALL_SCREENS` is a hand-maintained 71-entry bash array, verified), but had no sight of the other tasks; Task 5 modifies `capture-shots.sh:2,:11,:12,:40,:46` and adds exactly that entry. Task 1 landed the plumbing (enum case, accessibility id, `routeWorkScreen` hop, coordinator latch); Task 5 uses it. — Cost if wrong: nil — if Task 5 somehow omits it, Task 5's own review catches it.

Conductor correction to the review: **the reviewer's sub-claim that `capture-shots.sh`'s prefix filter "breaks on first match, so a future `F1` filter would still pick only `F1.scan-setup`" is WRONG.** Verified by reading `capture-shots.sh:51-56`: the `break` exits the inner `for want in "$@"` loop (preventing a double-add when several prefixes match one screen), not the outer `for s in ALL_SCREENS` loop. `capture-shots.sh F1` will match both entries. Task 5 Step 3's expected `→ sweeping 2 screen(s)` therefore holds and needs no plan change.

Task 1: minor (deferred): M1 — `screen.C6.voice` joins `screen.N4.voice`, so the bare token `-CaptureScreen voice` is now ambiguous; still resolves to `.n4Voice` by declaration order (behaviour unchanged) and the sweep always passes full suffixes. Flag for Wave 3, which will want `C6.voice`.
Task 1: minor (deferred): M2 — `siteScanContextRequested` can survive an abandoned harness hop (`.task`, not `.task(id:)`), so a later arrival at Site scan could pop the context cover unprompted. Debug/harness-only path; exactly as the brief specified.
Task 1: minor (deferred): M3 — `ViewfinderModeSelector`'s `item == mode` highlight has no `?? 0` fallback, so no pill would highlight if `mode == .voice`. Unreachable in wave 2; wave 3 owns the pill.
Task 1: minor (deferred): M4 — `makeWorkServices` builds `siteRequests` first where the original built it eighth. All eight factories verified side-effect-free. Benign; recorded so a later wave's reviewer does not assume the old order.
Task 1: minor (deferred): M7 — `f1Context` and the Flow-17 comment block read out of order in the enum. Plan-mandated verbatim; cosmetic.
Task 1: minor (deferred): M8 — `CaptureSheet.visit` has no registered builder, so presenting it renders `MissingScreen`. Plan-mandated; nothing presents it in wave 2, wave 3 owns V0.
Task 1: minor (deferred): M9 — the brief's helper doc comment landed on `private struct WorkServices` rather than on `makeWorkServices(deps:)`; "Real mode only" reads oddly as a type comment.
Task 1: minor (deferred): M10 — `AppContainer`'s new `smartGuess` comment names `VNClassifyImageRequest`/"Vision-backed", i.e. engine mechanics in a comment. Plan-mandated verbatim, factually accurate, in the composition root (nothing a designer reads), never says "AI". No user-facing string was added or changed anywhere in the diff and no "Inbox" string was touched.
Task 1: minor (deferred): M11 — `theVisitSpineIdsAreReservedNow` asserts the three raw values but nothing asserts they stay out of the sweep or stay on `break`; a wave-3 agent could wire one prematurely without a red test.
Task 1: minor (deferred): M12 — ⚠️ `.allOff` in mock mode is not test-provable (`AppContainer` is app-target, excluded by C1). Conductor resolution: NOT a real gap — verified by reading `AppContainer.swift:125-126`, and C1 forbids the only test that could prove it. Carried to the device pass, not to the fix loop.
Task 1: minor (deferred): M5/M6 — report-file inaccuracies (the "same instance" claim about a struct; the stale `:623`/`:743` citations, really `:717`/`:825` in the app-target `Capture/Services/Sync/`). Folded into fix round 1 as bookkeeping so wrong numbers do not reach the wave report.

Task 1: fix round 1/5 dispatched to implementer `ac8d36a351714cf16` (resumed, opus) — I1 plus the two report corrections. FIX_BASE = 13cf6a28f.

Ruling: **Task 1 owns `AppContainer.swift`'s BODY comments as well as its header, for this wave only.** — Ruling 2026-08-24 folded "every `AppContainer.swift` header edit" into the seam commit and Task 4's brief explicitly fences it out ("this task does not touch that file"). A false claim in that file's body is therefore unowned by every other task in the wave: if Task 1 does not fix it, the wave ships a composition root whose header and body contradict each other, in the wave whose stated purpose is deleting false claims. Scope stays narrow — only claims that are now demonstrably false, not a general comment pass. — Cost if wrong: a three-line comment edit outside the brief's literal Files list; trivially revertible and covered by the scoped re-review.

### Task 2 pre-verification (conductor, before dispatch)

Every API Task 2's brief assumes exists: `ViewfinderModel.currentSpecimen(id:)` (private, `:395`), `CaptureImage.init(data:width:height:)` (`RecognitionServices.swift:17`), `CaptureStore.mediaURL(for:)` (`CaptureStore.swift:504`), `Specimen.primaryPhoto` (`Specimen+Accessors.swift:126`), `FieldSuggestion.source` with a `= .smartGuess` default (`RecognitionServices.swift:117`).

`ViewfinderModel` is `@MainActor @Observable final class` (`:12-14`), so the brief's `Task { [weak self] in ... }` inherits MainActor isolation — `currentSpecimen`, `setValue`, `setConfidence` and `store.save()` are all reachable from it, and `SmartGuessService` is `Sendable`. The pattern compiles as written.

**Line anchors drifted (the brief was authored pre-Wave-1); corrected anchors to carry into Task 2's dispatch:**
- `applySmartGuess(to:)` is at **`:459-468`**, not the brief's `:410-422`. Its body is the two hardcoded literals — `seating`@0.72 and `"Oak / bouclé"`@0.6 — exactly as described.
- Its two call sites are **`:256`** (`captureSingle()`) and **`:302`** (`endMultiShot()`), not the brief's `:225`/`:271`. Both stay unchanged.
- Unchanged and correct as cited: the seams block insertion point `private let sessionContext: CaptureSessionContextStore` at `:24`, and `self.companion = container.companion` at `:79`.

Task 1: fix round 1/5 — implementer `ac8d36a351714cf16` returned complete at `1ac708735` (comments only, +4/−4). I1 addressed: `AppContainer.swift:99-101` now says each flow owns a `<Flow>ServiceFactory.make(deps:)`, all eight hand back a real Supabase service, and mock mode never reaches that branch. `:50`'s marker had its false half deleted (`wave agents build the screens` → `foundation-owner-only`) on `MissingScreen` zero-call-site evidence — accepted. Report corrections (the stale `:623`/`:743` citations, the "same instance" claim about a struct) appended to `task-1-report.md`. Gate: `✔ build` / `✔ tests` / `✔ lint`.

Ruling: **the `ContentState` census gap is recorded in the wave report as a spec correction, not fixed in code.** — `ContentState` values are built through type inference (`.init(queued:…)`), so `grep -rn "ContentState"` misses all three `LocalCaptureSyncService` construction sites entirely. That is how spec §5.5 came to name the wrong call sites (`LocalCaptureSyncController`/`CaptureLiveActivityController`) and how the plan inherited stale line numbers. The real sites are `Capture/Services/Sync/LocalCaptureSyncService.swift:184,:717,:825` — app target, not CaptureKit — and `CaptureLiveActivityController` only ever receives an already-built `ContentState` (`:35,:55,:63`). Nothing in the code is wrong; the spec's account of it is. — Cost if wrong: a spec paragraph stays stale one wave longer.

Task 1: scoped re-review dispatched (reviewer agentId `a742a9d4ce5a2a1ba`, sonnet) over `review-13cf6a28f..1ac708735.diff`.

Ruling: **Task 2's implementer is dispatched concurrently with Task 1's scoped re-review.** — The re-review's entire diff is four comment lines in `AppContainer.swift`; Task 2 touches `ViewfinderModel`, `S3DestinationScreen`, `RecognitionServices`, `HeuristicSmartGuessService` and two new files, and never opens `AppContainer.swift`. Only ONE implementer is live at a time (the SDD bar), and a NOT-ADDRESSED verdict would produce another comment-only fix that cannot conflict with Task 2's files. — Cost if wrong: a comment-only fix round would interleave with Task 2's commit in the log; no code conflict is reachable.

Task 2: dispatched (implementer agentId `a00c70d0bcedaac53`, opus) — the real smart guess + S3 held at Inbox. BASE = 1ac708735. Dispatch carried the four drifted-anchor corrections (`applySmartGuess` at `:459-468`, call sites `:256`/`:302`) and the no-confidence-floor ruling with `Specimen+Accessors.swift` fenced out.

### Task 4 pre-verification (conductor, before dispatch)

Task 1's two header fixes landed and are out of Task 4's way: `CaptureScreenID.swift`'s "51 entries" and `AppContainer.swift:6`'s "51-screen" both return zero hits. Every remaining stale-count site is exactly where the brief predicts: `CaptureKitMocks.swift:4`, `AppConfiguration.swift:96,:118`, `CaptureDeepLink.swift:6`, `capture-shots.sh:2,:11`, `README.md:14,:39,:51,:116,:120`. All nine of Task 4's README line anchors (`:12,:14,:35,:38-39,:51,:52,:101,:116,:120`) are exact — no drift. `AVFoundationCameraService.swift:6` still says "NOT wired into AppContainer yet"; `ResilienceScreens.swift:9` still names the deleted view; README still credits `00258` and still lists `CaptureShareExtension/`/`CaptureWidgets/`.

README arithmetic checked against the file, not assumed: today it reads 33 + 18 Work-flow + 20 Site Request = 71, and `:35` lists four F-screens for flow 15. The brief's target 33 + 19 + 20 + 3 = 75 is internally consistent — the 18→19 is exactly `f1Context` joining flow 15's list at `:35`, and the +3 are the reserved visit-spine ids.

Ruling: **Task 4's census regex is tightened with a word boundary after the bare numbers, and `CaptureTests/ManifestTests.swift:147` is left untouched.** — The brief's regex alternative `the 51` (no trailing boundary) matches "parsing **the 51**2-byte header fields" in `ManifestTests.swift:147`, an unrelated and correct comment about tar ustar headers. Task 4 Step 5 expects "both greps print nothing", which is unachievable as written — the implementer would either wrongly edit a correct test comment or stall on a census that can never come clean. Changing `all 51`/`the 51`/`all 71`/`the 71` to `all 51\b`/`the 51\b`/`all 71\b`/`the 71\b` drops the false positive (12 hits → 11) and keeps all eleven real targets. — Cost if wrong: a stale count somewhere the tightened regex no longer reaches; mitigated because every one of the eleven real sites is enumerated above by file and line, so Task 4 works from the list, not only from the regex.

### Tasks 3 and 5 pre-verification (conductor, before dispatch)

Task 3's census is clean and its stop condition will not fire: `LowLightTorchOverlay` appears only in its own file (`:1`, the `struct` at `:15`, its own `#Preview` at `:122`) plus the `ResilienceScreens.swift:9` comment that Task 4 owns. No real call site — the deletion premise holds.

Task 5's five `capture-shots.sh` anchors are all exact, no drift: `:2` is the "71-screen matrix" header line, `:11-12` are the two usage lines, `:40` is `F1.scan-setup F2.site-scan F3.scan-review F4.scan-upload` (the insertion point for `F1.context`), and `:46` is `ALL_SCREENS`' closing paren. `ALL_SCREENS` holds 71 entries today; one addition makes 72, matching the brief's expected sweep count.

Task 1: fix round 1/5 (1 addressed, 0 open; commits 13cf6a28..1ac70873). Scoped re-reviewer `a742a9d4ce5a2a1ba` verdicted I1 **ADDRESSED** with independent evidence — it opened all eight factory files and confirmed each returns a real `Supabase*` concrete, and confirmed mock mode takes the `else` branch (`AppContainer.swift:132-146`) wiring `CaptureKitMocks` conformers without ever calling `makeWorkServices`. The `:50` marker edit was judged **warranted, correctly scoped**: the reviewer independently counted 36 `registerRoute`/`registerSheet` calls across the app target covering all eight Phase-2 flows, and found `MissingScreen(` has zero call sites outside its own definition — so "wave agents build the screens" was stale and deleting the false half rather than inventing a replacement was the conservative fix. Both report corrections verified exact (`:184`/`:717`/`:825` re-read individually; `SupabaseSiteRequestService` confirmed a `struct` at its `:11`). Diff confirmed comment-only, `.swiftlint.yml` untouched, no `swiftlint:disable` anywhere. **No new breakage, no findings at any severity.**

Task 1: complete (commits 13cf6a28..1ac70873, review clean after 1 fix round, 12 minors deferred to the final whole-branch review).

### Device inventory (conductor, for Task 6's C5 pass)

`blitz-iphone list_devices` shows **two** physical iPhones with developer mode enabled, both LiDAR-capable (Pro line):
- `00008150-00016C8A21DA401C` — "Kody's Phone", iPhone 17 Pro Max, **connectionType wifi**, paired, developerModeEnabled true, wdaInstalled false, wdaRunning false
- `00008110-001630212231801E` — "iPhone", iPhone 13 Pro, **connectionType wifi**, paired, developerModeEnabled true, wdaInstalled false, wdaRunning false

(Also listed: an Apple Watch with developer mode OFF, and five simulators the API reports under `physicalDevices` — those are not devices.)

Both real phones are **wifi-only with no WDA installed**, which is the same posture that blocked Wave 1's device pass ("developer disk image could not be mounted"). Task 6 attempts the pass once and records precisely which of the five device criteria were and were not exercised.

Ruling: **No further `devicectl` probing while any gate is in flight.** — A single `xcrun devicectl list devices` returned `CoreSimulatorService connection became invalid`, `simdiskimaged crashed or is not responding`, and finally `Timed out waiting for CoreDeviceService to fully initialize`. I immediately re-checked `simctl list devices booted` and the `iPhone 17` simulator is still Booted and healthy, so Task 2's in-flight `xcodebuild` was not harmed — but a probe that can disturb CoreSimulatorService has no business running concurrently with a gate. The device attempt happens once, in Task 6, after every gate is done. — Cost if wrong: the device pass starts a few minutes later than it could have.

Task 2: implementer `a00c70d0bcedaac53` returned DONE_WITH_CONCERNS at `4a4da60a7` (9 files, +809/−622; the bulk is regenerated pbxproj + both `.xcscheme` files). Gate `✔ build` / `✔ tests` / `✔ lint` / `GATE EXIT=0`; 12/12 new tests (6 `SmartGuessKeywordTests` + 6 `UnconfirmedGuessTests`), and the Step-2 red run showed exactly the predicted `cannot find 'SmartGuessKeywords' in scope` / `no member 'fieldsWorthRecording'`. `Specimen+Accessors.swift` and `RouteSessionUI.swift` untouched; no confidence floor anywhere.
Four self-reported concerns, carried to the reviewer for independent judgment rather than pre-judged by me: (1) a NEW compiler warning `ViewfinderModel.swift:435: result of 'try?' is unused`, from the brief's own `try? self.store.save()` as the closure's last expression — kept verbatim rather than silenced with `_ =`; (2) the read is fire-and-forget (the `Task` is neither retained nor cancellable, and `applySmartGuess` is now the only writer landing fields AFTER `captureSingle()`'s `store.save()`); (3) first gate run died on sandbox `permissionDenied` against DerivedData — all quoted results are unsandboxed re-runs of the identical command; (4) C5 device pass still owed, since `HeuristicSmartGuessService` is app-side and untestable under C1.

Task 2: task review dispatched (reviewer agentId `a40868fca2721cd0a`, opus, fresh context) over `review-1ac708735..4a4da60a7.diff`.

Ruling: **Task 3's implementer is dispatched concurrently with Task 2's review.** — Same basis as the Task 1/2 overlap: only ONE implementer is live at a time, and Task 3's entire diff is deleting `Capture/Features/Resilience/LowLightTorchOverlay.swift` plus the regeneration, which shares no Swift file with Task 2's six. Task 3 is explicitly forbidden to touch `ResilienceScreens.swift:9` (Task 4's line). — Cost if wrong: a Task 2 fix round would interleave with Task 3's commit in the log; no code conflict is reachable.

Task 3: dispatched (implementer agentId `ad3659a5a1ee46dac`, haiku). BASE = 4a4da60a7. Dispatch carries the pre-verified census and an explicit instruction to stage every file `generate_project.rb` touches, including the two `.xcscheme` files the brief's `git add` list omits.

Task 3: implementer `ad3659a5a1ee46dac` (haiku) returned DONE at `9021a4364` (3 files, +748/−879). Conductor verified independently: `LowLightTorchOverlay.swift` (127 lines) deleted; repo-wide census now returns **exactly one** line, `ResilienceScreens.swift:9` — Task 4's line, correctly left alone; working tree clean. Staged the regenerated pbxproj and `Capture.xcscheme` (`CaptureKit.xcscheme` was not modified this round — consistent, since only an app-target file left the build). Gate `✔ build` / `✔ tests` / `✔ lint`.

Task 3: task review dispatched (reviewer agentId `a15a32cb83e94bd23`, sonnet) over `review-4a4da60a7..9021a4364.diff` — proportionate to a one-file deletion.

Ruling: **Task 4 is dispatched on sonnet, not the brief's haiku model hint.** — The brief's own hint concedes the tension: "mechanical text edits, but every claim must be checked against the code before it is rewritten, not assumed." That is seven files spanning Swift headers and a README, each edit gated on verifying a claim about live code, with a copy rule and three fenced-out files to respect. The orchestrator's ladder puts multi-file work on sonnet; the cost of a literal-minded transcription here is a *new* false claim in the wave whose purpose is deleting false claims. — Cost if wrong: a slightly more expensive model on a text task.

Task 4: dispatched (implementer agentId `a69701a2801db2593`, sonnet). BASE = 9021a4364. Dispatch carries all eight pre-verification findings: the two already-fixed targets, `AppContainer.swift`/`SpeechVoiceNoteService.swift` fenced out, `OfflineQueueBanner` confirmed rendered so the PRIMARY `ResilienceScreens` wording applies, the tightened census regex with `ManifestTests.swift` protected, the eleven real targets enumerated by file and line, `capture-shots.sh:2,:11` reserved for Task 5, the nine exact README anchors, and the 33+19+20+3=75 arithmetic checked against the file.

### Task 3 review (reviewer `a15a32cb83e94bd23`, sonnet) — Spec ✅ PASS · Quality Approved · 0 findings at any severity

The reviewer verified the deletion premise **independently against the parent commit** rather than from the report: `git grep -n "LowLightTorchOverlay" 4a4da60a7 -- '*.swift'` returned exactly 4 hits — three inside the file itself (declaration plus its own `#Preview`) and the `ResilienceScreens.swift:9` comment. No external call site existed; "preview-only dead code" held. It also read the full 127 deleted lines and confirmed every declaration (`struct LowLightTorchOverlay`, its private `chip` builder, private `toggleTorch()`, the private nested `Haptic` enum) was scoped to the struct — nothing shared left the tree. `grep -c "LowLightTorchOverlay" project.pbxproj` at HEAD returns 0. `ResilienceScreens.swift` is absent from `git diff --name-status` for the range — untouched, not merely unedited-looking. Staging `Capture.xcscheme` was judged correct (a mechanical `BlueprintIdentifier` reshuffle in four places); `CaptureKit.xcscheme` correctly untouched because the deleted view was app-target-only.
One ⚠️ recorded at the expected verification boundary: the reviewer could not independently confirm the gate output beyond what the report records, because I instructed it not to re-run a multi-minute Xcode build on an unchanged tree. Accepted — the gate is re-run in full at Task 6's wave gate on the finished branch.

Task 3: complete (commit 9021a436, review clean, 0 findings).

Ruling: **Task 5 does NOT run concurrently with Task 4.** — Beyond the SDD one-implementer-at-a-time bar, Task 5's Steps 1/3/4 run `capture-shots.sh`, which repeatedly installs and launches the app on the booted `iPhone 17` simulator, while Task 4's gate drives an `xcodebuild test` against the same simulator. Separate DerivedData paths do not separate the simulator itself; interleaving an install/launch sweep with a test run would corrupt both signals. — Cost if wrong: a few minutes of serialised wall-clock that could in principle have overlapped.

Task 4: implementer `a69701a2801db2593` (sonnet) returned DONE at `86e369f10` (6 files, +18/−20). Conductor verified independently before dispatching review: the tightened census now returns **only** `capture-shots.sh:2` and `:11` — Task 5's two reserved lines — and the claim census (`always kept alongside the text` / `NOT wired into AppContainer yet` / `LowLightTorchOverlay`) returns **zero**. `git diff --name-only 9021a4364..86e369f10` lists exactly the six intended files: `CaptureTests/ManifestTests.swift` is absent (the tightened-regex ruling did its job), and all three fenced-out files — `AppContainer.swift`, `SpeechVoiceNoteService.swift`, `CaptureScreenID.swift` — are untouched. No pbxproj or scheme drift, as expected for a comment-and-README task. Gate `✔ build` / `✔ tests` / `✔ lint` / `GATE EXIT=0`, with `swiftlint --quiet --strict` re-run separately per C2 (exit 0).

Task 4: task review dispatched (reviewer agentId `afc41d07f8addca24`, sonnet) over `review-9021a4364..86e369f10.diff`. Reviewer brief leads with the one question that matters here — is every REPLACEMENT claim true of the code as it exists now — and requires independently re-deriving the README arithmetic from the real enum, confirming `00265` exists in `supabase/migrations/` while `00258` is something else, and confirming `generate_project.rb` really creates exactly four targets.

Task 5: dispatched (implementer agentId `aa7f94b37f5f43f37`, sonnet). BASE = 86e369f10. Dispatch carries the five verified-exact anchors, the correction that the prefix filter's `break` is inner-loop (so `→ sweeping 2 screen(s)` is genuinely reachable), the note that `capture-shots.sh:2,:11` are its own lines to fix, and an explicit instruction to LOOK at `F1.context.png` rather than infer from byte size — with the `CAPTURE_SHOT_SETTLE=3` retry to try before concluding the harness hop failed.

### Task 2 review (reviewer `a40868fca2721cd0a`, opus) — Spec ✅ PASS · Quality PASS WITH FINDINGS · 0 Critical · 5 Important · 4 Minor

Ruling verified on both halves: `Specimen+Accessors.swift` absent from the diff and `hasUnconfirmedGuess` still exactly `provenanceRaw.values.contains(ProvenanceSource.smartGuess.rawValue)`; `grep -rn` for `SmartGuessConfidence`/`confirmedFloor`/`confidence(for:)` across every `*.swift` returns **0 hits**. The 42-entry keyword table was mechanically diffed against the deleted app-side one — **identical, order and entries**. `categoryForKeyword`/`keywordTable` return zero hits. The two literals survive only in `CaptureKitMocks`' `StubSmartGuessService` and two `#Preview` bodies; both `AppContainer` branches wire the real `HeuristicSmartGuessService()`, so no shipping path reaches the stub. Concurrency cleared in detail (F-10): MainActor inherited, `Specimen` correctly NOT captured (re-fetched by id), `[weak self]`, no retain cycle. Scheme staging confirmed mandatory (F-11): the old blueprint ids appear 0 times in the new pbxproj, so leaving them unstaged would have pointed the gate's own `-scheme CaptureKit` at ids that no longer exist.

Ruling: **F-1 enters the fix loop — `setConfidence` fires even when `setValue` refused the write.** `setValue` silently returns when a field already carries non-`smartGuess` provenance, but the brief's replacement loop calls `setConfidence` unconditionally, and `FieldCapturePayload.swift:163` ships `guessConfidenceRaw` as `payload.guesses`. So a field the designer typed can reach `products.capture_provenance` as `provenance: "manual"` **plus** `guesses: {"material": 0.55}` — a confidence attached to a value the reader never wrote. That is the mirror image of the defect this task exists to remove, and the OLD code did not have it (it guarded before both calls). Introduced by the brief's own body. — Cost if wrong: a one-line provenance check; the alternative is shipping the wave's signature defect in a new coat.

Ruling: **F-2 enters the fix loop, but ONLY the badge half.** `CaptureCardOverlay`'s `guessRow` renders `ProvenanceBadge(specimen.provenance(for:) ?? .smartGuess)` unconditionally, so a photo the reader explicitly declined to place now shows *Category "—" [guess]* and *Material "—" [guess]* — the app labelling as a guess something it never guessed, on the screen the designer sees after every shutter. Before this task that badge was honest, because every capture carried the hardcoded guess; Task 2 is what turned it into a lie, so it is this task's regression even though the file is outside the brief's list. **The "Material is always —" half is NOT fixed**: it follows from the brief-mandated `ocr: []`, and showing "—" for something we did not read is honest. Restoring material inference would mean wiring OCR into the photo path — unrequested, and wave-3+ territory. — Cost if wrong: a badge stops appearing where a designer expected one; the device pass sees it either way.

Ruling: **F-3 enters the fix loop — the async writer can mutate an already-routed record.** The Task guards only on `currentSpecimen(id:) != nil`. The designer can hit `saveFromCard()` → `sync.route(...)` inside the read window, after which the late write calls `setValue`/`touch()`/`store.save()` on a record whose payload has already been uploaded, diverging the local record from what `capture_provenance` received. `CaptureSyncService.swift:68` states the policy outright: *"Once a record leaves the device-local phase, review must not rewrite it."* — Cost if wrong: one added phase check; without it the wave's own honesty guarantee is violated by a race.

Ruling: **F-5 enters the fix loop — the synchronous multi-MB HEIC read moves inside the Task.** The brief's shape does `try? Data(contentsOf:)` on the `@MainActor` shutter path *before* spawning the Task, at every capture. The OLD code did no disk read at all, so this is a NEW main-actor stall on the most latency-sensitive path in a camera app. `photo.filename`/`.width`/`.height` and `store.mediaURL(for:)` are MainActor reads and stay put; only the `Data(contentsOf:)` moves. — Cost if wrong: ~3 lines; strictly less main-thread work than the brief's shape.

Ruling: **F-6 + F-7 enter the fix loop together — the wave's headline claim gets a test that can actually fail.** `anUnplaceableLabelWritesNothingSoThereIsNothingToConfirm` asserts nothing new: both halves are verbatim duplicates of other tests, and nothing in it runs the write path — it would pass unchanged if `fieldsWorthRecording` were never wired into `ViewfinderModel` at all. `everyKeywordInTheTableResolves` asserts `!= nil`, which `contains` makes reflexively true, so a reordering that made `"textile"` resolve to `.tile` would still pass. Both are fixable INSIDE CaptureKit: compose the CaptureKit-side filter with the CaptureKit-side `Specimen` accessors — apply `fieldsWorthRecording` to an in-memory `Specimen` exactly as the viewfinder's loop does, then assert `hasUnconfirmedGuess`. — Cost if wrong: two tests that assert more than before. **S3's `.inbox` hold remains genuinely untestable under C1 (app-side SwiftUI) — recorded as ⚠ not-covered and carried to the device pass, not faked.**

Ruling: **F-4 does NOT enter the fix loop — it is recorded as owed, prominently, in the wave report.** The reviewer is right that first-match-wins substring matching sends `"table lamp"` and `"desk lamp"` to `.table` rather than `.lighting`, and `"tapestry"` to `.plumbing`, and right that this task makes a previously-inert defect fire on every capture. But the table is brief-mandated verbatim, it predates this wave, and the correct repair (ordering semantics, or compound-keyword precedence) is a design decision that deserves its own attention rather than improvisation inside a fix round. The wave's own safeguard contains the harm: FC-R12 means nothing auto-applies at any confidence — she confirms every read — and a wrong-but-genuinely-computed mapping is still within this wave's thesis, which is that no capture ships a guess the app did not compute. F-6's strengthened test locks the current ordering so any future edit that worsens shadowing fails loudly. — Cost if wrong: a designer photographing a table lamp is offered "Table" and corrects it; the device pass will surface it, and it is written down rather than discovered later.

Task 2: minor (deferred): F-9 — the implementer's headline concern was **misdiagnosed**. `ViewfinderModel.swift:435` is `try? store.writeMedia(...)` (returns `URL`), untouched by this diff and merely shifted 433→435 by the two added lines; the brief's `try? self.store.save()` is at `:478` and returns `Void`, which does not produce that warning. **This task introduces no new warning**, and the offered one-character fix would have silenced nothing. Report correction folded into the fix round; no code change (the real warning is pre-existing and out of scope).
Task 2: minor (deferred): F-12 — `SmartGuessKeywords.swift`'s header leads with "Vision-label"/"Vision request" mechanics, and the public API is `category(forVisionLabel:)`. Brief-verbatim, engineering-internal file, names an Apple framework rather than a model. Accepted as within the copy rule; flagged because it is the only arguable spot in the diff.
Task 2: minor (deferred): F-8's two latent holes — `fieldsWorthRecording` does not trim whitespace (a `" "` value passes) and does not exclude `FieldKey.dimensions`, for which `setValue` writes provenance while `break`-ing on the value. Neither is reachable today: the heuristic never emits `.dimensions` and never emits whitespace-only values.
Task 2: minor (deferred): F-13 — `S5InboxTerminalScreen` degrades correctly with zero guesses ("Held safely…"). The S3 Inbox blurb "Smart guesses to confirm" is now sometimes wrong for an unplaceable capture, but it is an existing user-facing "Inbox" string and Wave 3's to change; leaving it is correct.

### Task 4 review (reviewer `afc41d07f8addca24`, sonnet) — Spec ✅ PASS (11/11 targets) · Quality: 1 Important · otherwise clean

Every replacement claim was truth-checked against live code and all but one held. Independently re-derived from the real enum: **75 cases** = 33 original + 19 Work-flow (incl. `f1Context`) + 20 Site Request + 3 reserved (`v0Visit`, `c6Voice`, `v4VisitReview`), so 72 built — README's numbers reconcile exactly. `00265_room_scans_project_linkage.sql` confirmed to exist and `00258_edge_settings_vault.sql` confirmed unrelated. `generate_project.rb` confirmed to create exactly four targets, so deleting README `:52` was right. `.photoImport` confirmed the only registration in `ResilienceScreens.swift`; `OfflineQueueBanner` confirmed rendered at `ViewfinderScreen.swift:43`, so the PRIMARY wording was the correct choice. `CaptureKitMocks.swift:4` and `CaptureDeepLink.swift:6` both **drop** the number rather than restating it — no fresh number planted to rot. Zero added/removed lines containing "inbox". `ManifestTests.swift:147` confirmed present, unedited, and correctly absent from the diff. The fence held on all three protected files.
Useful trap the reviewer flagged for future reviewers: `git show --name-only` without `--format=""` false-positives on this commit, because the commit message body itself names the three fenced-out files in prose.

Ruling: **Task 4's one Important finding enters a fix loop — the new `AVFoundationCameraService.swift` header cites `AppContainer.swift:104-110`, which is Work-service wiring, not camera wiring.** I verified it myself: `:104-109` assigns `projects`/`leads`/`decisions`/`messaging`/`receiving`/`portalAuth`/`siteScan`/`siteRequests`/`drainer`, and the real `#if targetEnvironment(simulator) … MockCameraService … #else … AVFoundationCameraService … #endif` block is at `:111-117`. The substantive claim is true and confirmed; only the anchor is wrong. The citation is brief-verbatim, so it originates in the plan — but a freshly-planted incorrect line reference is precisely the defect class this task exists to remove, and it would rot further as later waves edit `AppContainer.swift`. **The fix follows this task's own established pattern: drop the brittle line citation and name the mechanism instead** — exactly what it already did for `AppConfiguration.swift` and `CaptureKitMocks.swift`, both of which dropped their counts rather than restating them. — Cost if wrong: a header names the simulator branch instead of a line number, which is strictly more durable.

Task 5: implementer `aa7f94b37f5f43f37` (sonnet) returned DONE at `fcc2c8365` (1 file, +8/−3). It did the part that actually matters rather than inferring from file size: **opened `F1.context.png` and read it** — 578K, showing "THIS IPHONE CAN'T MEASURE A ROOM." / "Photos & notes for this room." with the Photo/Done camera-notes UI over the scan backdrop. That is the Reference capture surface, not the Site scan setup form (360K, visibly different). The `siteScanContextRequested` hop fired on the FIRST run at the default 1.4s settle — the `CAPTURE_SHOT_SETTLE=3` fallback was not needed. Pre-edit `capture-shots.sh F1` reported `→ sweeping 1 screen(s)`; post-edit `→ sweeping 2 screen(s)`, confirming my inner-loop-`break` correction was right and the reviewer's earlier claim wrong. Full sweep `→ sweeping 72 screen(s)`, all 72 `✔`, `ls .build/shots/*.png | wc -l` = 72. No pbxproj diff, correctly — no `.swift` file was added or removed, so C4 does not apply. Gate `✔ build` / `✔ tests` / `✔ lint` / `GATE EXIT=0`, plus a standalone `swiftlint --quiet --strict` exit 0 per C2. No concerns raised: there was no rendering bug to report.

Task 5: task review dispatched (reviewer agentId TBD, sonnet) over `review-86e369f10..fcc2c8365.diff`.

Task 5: review dispatched (reviewer agentId `ad658044ca4284fd0`, sonnet). Brief forbids it running `capture-shots.sh` or the gate — the simulator is needed by Task 2's fix round — but explicitly permits opening the two gitignored PNGs with the Read tool to judge the load-bearing "it renders the right screen" claim at zero simulator cost.

Task 2: fix round 1/5 dispatched to implementer `a00c70d0bcedaac53` (resumed, opus). FIX_BASE = 4a4da60a7 (note: HEAD is now `fcc2c8365`; Tasks 3/4/5 landed in between and touch none of Task 2's files). Six findings sent: F-1 (guard `setConfidence` behind a successful `setValue`), F-2 (badge only where guess provenance exists, plus the stale `CaptureCardOverlay` header claim — explicitly NOT restoring material inference), F-3 (device-local phase check on the late write), F-5 (move only `Data(contentsOf:)` inside the Task), F-6 (`== entry.category`), F-7 (make the load-bearing test exercise the real write path inside CaptureKit, both directions). Plus the F-9 report correction. F-4 explicitly fenced OFF with the ruling's reasoning, so the implementer does not "helpfully" reorder the table.

### Task 5 review (reviewer `ad658044ca4284fd0`, sonnet) — Spec ✅ PASS (5/5 spots verbatim) · Quality ✅ PASS · **0 findings at any severity**

Verified independently rather than from the report: `ALL_SCREENS` hand-counted at **72** with `F1.context` between `F1.scan-setup` and `F2.site-scan`; the three reserved ids genuinely absent from the printed list; `CaptureScreenID` at **75** cases so 75 − 3 = 72 reconciles the sweep count, the enum, and the file's own header; `grep -n "71" scripts/capture-shots.sh` now empty — the last two stale counts in the tree are gone. Diff confirmed +8/−3, one file, no scope creep.
It **opened both PNGs itself**: `F1.context.png` shows "THIS IPHONE CAN'T MEASURE A ROOM." / "Photos & notes for this room." / "These reach the studio as soon as you have signal — they're notes, not a scan." with Photo and Done buttons; `F1.scan-setup.png` shows the Site scan form with Project picker, Scan Name field and Start Scan button — unambiguously different screens. So the `siteScanContextRequested` hop demonstrably fired and the sweep captured the right surface, not a timing-miss duplicate. (PNG sizes on disk 581K/358K vs the report's 578K/360K — a later sweep run, framebuffer noise, correctly judged not worth flagging.)
It also **independently confirmed my inner-loop-`break` reading** by walking the outer loop by hand: `F1.scan-setup` matches and breaks the inner loop, the outer loop continues to `F1.context`, which also matches. `→ sweeping 2 screen(s)` is correct behaviour, not a double-count bug. The reviewer that originally claimed otherwise (Task 1's) was wrong, and that is now confirmed from two independent directions.

Task 5: complete (commit fcc2c836, review clean, 0 findings).

### origin/main moved (conductor check)

`origin/main` advanced `210564895` → **`6d91eb1b6`** (the 00531 hotfix). The object is already in this worktree's shared object store, so the end-of-wave merge needs no `git fetch` (which fails on the sandbox proxy). Verified the incoming diff touches **only** `supabase/migrations/00531_restore_extension_execute_authenticated.sql`, `supabase/seed/00-legacy-grants.sql` and `supabase/tests/security/extension_execute_authenticated_test.sql` — **zero `apps/mobile` paths**, so the merge cannot conflict with any Wave 2 commit. Merge (never rebase) happens as the last step before the final whole-branch review.

Task 2: fix round 1/5 — implementer `a00c70d0bcedaac53` returned complete at `a54316f6a` (3 files, +81/−19). Gates `✔ build` / `✔ tests` / `✔ lint` / `GATE EXIT=0`; 13/13 new tests. It used a scratchpad copy rather than `git stash`, per the shared-stash rule. All six findings addressed:
- F-1: `guard current.provenance(for: suggestion.key) == suggestion.source else { continue }` between `setValue` and `setConfidence` — a read-back, so a refused write can never pin a confidence.
- F-2: `guessRow`'s `source` became `ProvenanceSource?`, the `?? .smartGuess` fallbacks are gone, and the badge renders only `if let source`. The stale header claim was rewritten to describe what the card now does — a filled field badged with where the value came from, an unfilled one showing "—" with no badge.
- F-3: `current.transferState.phase == .local` added to the guard.
- F-5: only `Data(contentsOf:)` moved inside the `Task`; `photo.filename`/`.width`/`.height` and `store.mediaURL(for:)` stay on MainActor, early-return semantics preserved.
- F-6: renamed to `everyKeywordInTheTableResolvesToItsOwnCategory` and asserts `== entry.category`.
- F-7: a `record(_:onto:)` helper mirrors the viewfinder's loop onto an in-memory `Specimen`, and the test now asserts both directions — unplaceable leaves `hasUnconfirmedGuess == false` with an empty `guessConfidenceRaw`, placeable leaves it true with the confidence recorded. A third test, `aReadNeverPinsAConfidenceToAValueItDidNotWrite`, covers F-1 directly.
- F-9 report correction landed: the `:435` warning claim is withdrawn.

Ruling: **F-3's predicate is `transferState.phase == .local` used directly rather than routed through `canCull` — ACCEPTED.** Cull-deck semantics differ from "has this left the device", and borrowing `canCull` would couple the late-write guard to an unrelated policy that a later wave could legitimately change. — Cost if wrong: the guard drifts from cull policy, which is the intent.

Ruling: **F-5 residual is PARKED, not expanded.** The `Task` inherits `@MainActor` from `ViewfinderModel`, so the multi-MB `Data(contentsOf:)` still runs on the main actor — just later than before, and off the synchronous shutter path. Making the media read genuinely concurrent needs a `nonisolated async` read, which is a Wave 3 follow-up adjacent to the C3/inline-mic work, not a Wave 2 scope expansion. The fix as landed is a strict improvement over both the brief's shape and the pre-wave code. — Cost if wrong: a main-actor disk read persists one wave longer, after the card has already appeared.

Task 2: scoped re-review dispatched (reviewer agentId `a11a3a2b76338184b`, sonnet) over `review-fcc2c8365..a54316f6a.diff`, forbidden from touching the simulator. Its most important verdict is F-7: the mirrored `record(_:onto:)` loop necessarily duplicates `ViewfinderModel`'s loop (C1 makes the model unreachable), so I asked it to judge honestly whether that duplication is itself a drift hazard.

Task 4: fix round 1/5 dispatched to implementer `a69701a2801db2593` (resumed, sonnet). FIX_BASE = 86e369f10. One finding: replace the wrong `AppContainer.swift:104-110` citation by naming the `#if targetEnvironment(simulator)` mechanism instead of correcting the numbers — matching the pattern this same task already used for `AppConfiguration.swift` and `CaptureKitMocks.swift`, both of which dropped their counts rather than restating them.

Task 4: fix round 1/5 — implementer `a69701a2801db2593` returned complete at `5c757d3f2` (1 file, +2/−2). The header now reads "Wired into AppContainer.init()'s #if targetEnvironment(simulator) branch on device; the simulator branch takes MockCameraService instead" — the line citation is gone and the mechanism is named, per the ruling. Gates `✔ build` / `✔ tests` / `✔ lint`. Scoped re-review dispatched (reviewer agentId `a2565b7546cb9a728`, sonnet), asked specifically to judge whether the new sentence reads correctly given that `AVFoundationCameraService` is instantiated in the `#else` arm, not the simulator arm.

### Conductor's own whole-wave constraint sweep (`210564895..5c757d3f2`, `apps/mobile/Capture`)

Run directly rather than delegated, because these four are cheap to check and expensive to get wrong:
- **No added line contains the word "AI"** — zero hits.
- **No "Inbox" string literal added or removed** — zero hits on `^[+-].*"…inbox`. The word stays Wave 3's to remove, as ruled.
- **No `swiftlint:disable` added anywhere** — zero hits.
- **`.swiftlint.yml` not touched** — absent from `git diff --name-only`.
So every lint result this wave reports came from the same unmodified rule set it started with, and no gate was widened to fit the code.

### Wave-2 docs snapshot started
`docs/design/field-companion/waves/wave-2/rulings-index.md` generated mechanically from the ledger's `Ruling:` lines — 31 rulings, in the order made, each with its cost-if-wrong. Refreshed at the wave gate.

Task 4: fix round 1/5 re-review (reviewer `a2565b7546cb9a728`) — finding #1 **ADDRESSED** (citation dropped, mechanism named), but **NOT ADDRESSED overall**: the replacement sentence introduced a new Important defect. `AVFoundationCameraService` is instantiated in the `#else` arm, but the new header names "the `#if targetEnvironment(simulator)` branch" as where it is wired and then calls "the simulator branch" the one holding `MockCameraService` — a reader takes both phrases as the same arm, so it reads as putting both services in the simulator arm. Self-contradictory and backward from the code, with the trailing "on device" doing redirect work it cannot do because nothing in the sentence names `#else`. Confirmed against `AppContainer.swift:111-117`. Scope, copy rule and non-duplication of the `:10-13` simulator note all clean.
I had noticed the same awkwardness when I read the diff and deliberately did NOT pre-judge it in the reviewer's brief — I asked it to judge the sentence on its own and give it a severity. It found the defect independently, which is what the split-context rule is for.

Task 4: fix round 2/5 dispatched to implementer `a69701a2801db2593`. FIX_BASE = 5c757d3f2. One sentence: name the `#else` arm explicitly, still citing no line numbers.

Task 4: fix round 2/5 — implementer `a69701a2801db2593` returned complete at `621b521d3` (1 file, +2/−2). Header now reads "Wired into the #else arm of AppContainer.init()'s #if targetEnvironment(simulator) check; the simulator arm takes MockCameraService instead." I checked it against `AppContainer.swift:111-117` myself: `#if targetEnvironment(simulator)` holds `MockCameraService`/`MockLocationService`, `#else` holds `AVFoundationCameraService`/`CoreLocationService`. The sentence now names the `#else` arm explicitly and distinguishes the two arms without citing a line number. Gates `✔ build` / `✔ tests` / `✔ lint`. Round-2 scoped re-review dispatched (reviewer agentId `a905f87bbdc5e7957`, haiku — proportionate to a two-line comment diff), told to read the sentence strictly because this file has already carried two wrong versions of it.

Ruling: **Task 6 waits for BOTH outstanding scoped re-reviews rather than starting concurrently.** — Task 6 is the wave gate: its first step runs the full gate on the *finished* branch, and its report records every task's final verdict. Starting it while Task 2's and Task 4's re-reviews are open would gate a branch that could still move and write a report against incomplete verdicts. The two pending diffs are small and I have personally verified both, so the risk of a further round is low — but "low risk of moving" is not "final", and the wave report is the artefact Kody reads. — Cost if wrong: a few minutes of serialised wall-clock at the end of the wave.

### Task 2 fix round 1 re-review (reviewer `a11a3a2b76338184b`, sonnet) — **all six ADDRESSED**, no new Critical/Important breakage

Each verdict was traced to source rather than accepted:
- **F-1** — traced `setValue`'s refusal rule (`Specimen+Accessors.swift:139-141`): it refuses a `.smartGuess` write whenever `existing != .smartGuess`, with no per-source special-casing, so the guard is sound for **all seven** non-guess sources, not just `.manual`. Confirmed by grep that no non-default `FieldSuggestion` is constructed anywhere outside tests, so `suggestion.source` is `.smartGuess` at every production call site.
- **F-2** — `guessRow` has exactly two callers, both in-file, both updated; every other `ProvenanceBadge(` use across the app was grepped and none is affected (the method is private to the file). The rewritten header is true to the code beneath it.
- **F-3** — `transferState.phase == .local` is **byte-identical to the codebase's own `CaptureRouteSafetyPolicy.canCull`** (`CaptureSyncService.swift:69-71`), so `.local` is the established definition of pre-route, not a novel predicate. Traced `transferState` to confirm `.local` is returned only for `status == .draft`, strictly before `.queued`/`.uploading`/`.awaitingConfirmation`/`.complete`, and confirmed both call sites still write legitimately at capture time — only a *late* write after `saveFromCard()` is blocked, which is the hole being closed.
- **F-4** — confirmed fenced off: `SmartGuessKeywords.swift` byte-identical, shadowing still live and unfixed, as scoped.
- **F-6** — confirmed the strengthened assertion genuinely catches the regression it names: moving `"tile"` above `"textile"` would make the `"textile"` iteration expect `.textile` and receive `.tile`, failing. The old `!= nil` would have passed that silently.
- **F-7** — compared the mirrored `record(_:onto:)` helper against the production loop **line for line**: identical body, differing only in receiver name and an inert hoist. They match today, and the implementer's mutation check (delete the F-1 guard from the mirror, watch `aReadNeverPinsAConfidenceToAValueItDidNotWrite` fail) is the right verification for a duplicated-logic test.

Ruling: **F-5's residual stays PARKED (unchanged from my earlier ruling), now independently confirmed.** The reviewer derived the same conclusion before reading the implementer's disclosure: a plain `Task {}` from a `@MainActor` context inherits that isolation through its synchronous prefix, so `Data(contentsOf:)` still runs on the main thread — deferred to a later run-loop tick rather than blocking the synchronous shutter call. It cites the codebase's own convention as corroboration (`SupabaseSiteScanService.swift:104` reaches for `Task.detached` precisely when it wants off-main work). The described symptom is fixed (the card no longer waits on I/O); the I/O is not yet off-main. Self-disclosed by the implementer, not a hidden regression. — Cost if wrong: a main-actor disk read persists into Wave 3, after the card has already appeared.

Ruling: **F-7's duplication residual is PARKED for the final whole-branch review, not turned into another fix round.** The finding itself is ADDRESSED — the test now exercises the real contract in both directions. The reviewer's further point is that a *better* fix existed: extract `Specimen.recordSmartGuess(_:)` into CaptureKit and have both `ViewfinderModel.applySmartGuess` and the test call it, eliminating the drift hazard entirely while still respecting C1. That is correct and worth doing — but it moves shipped logic into a new CaptureKit API and changes the production call path, which is a structural refactor arriving after the gate is green, at the end of a wave, and it deserves its own review rather than being squeezed into a closing round. The mirror is verified line-for-line identical today and mutation-checked. — Cost if wrong: if a later wave edits the production loop without editing the mirror, the test keeps passing while the shipped path breaks. **Recorded as owed in the wave report, with the exact remedy named.**

Task 2: fix round 1/5 (6 addressed, 0 open; commits 4a4da60a..a54316f6a). Task 2: complete (commits 4a4da60a..a54316f6a, review clean after 1 fix round, 2 residuals parked with rulings, 4 minors deferred).

Task 4: fix round 2/5 re-review (reviewer `a905f87bbdc5e7957`, haiku) — **ADDRESSED**, High confidence. The header now names "the #else arm" as `AVFoundationCameraService`'s home and "the simulator arm" as `MockCameraService`'s, matching `AppContainer.swift:111-115`; a reader unfamiliar with the code parses it correctly. Comment-only, no duplication of the `:10-13` simulator note, copy rule clean, no new breakage.
Task 4: complete (commits 86e369f1..621b521d, review clean after 2 fix rounds).

### origin/main merged (conductor)

Ruling: **`origin/main` is merged BEFORE Task 6's wave gate, not after it.** — The orchestrator's sequence puts the merge last before the final review; running it one step earlier means the wave gate proves the *merged* tree builds, which is the tree that will actually land on main, and lets Task 6's report record the true final head instead of a sha that changes under it. All task work was already complete, so the merge is still the last thing before the final review. — Cost if wrong: nil; the merge carries no iOS file.

`git merge 6d91eb1b6` (merge, never rebase) → **`f41150e37`**. Clean: zero conflicts, `git merge-base --is-ancestor 6d91eb1b6 HEAD` returns true (main fully contained), and `git diff --name-only 621b521d3 HEAD` shows **no `apps/mobile` path** — the merge brought only `supabase/migrations/00531_restore_extension_execute_authenticated.sql`, `supabase/seed/00-legacy-grants.sql` and `supabase/tests/security/extension_execute_authenticated_test.sql`.

Ruling: **the merge commit's subject was amended from `merge(field-companion-w2):` to `chore(field-companion):`.** — The orchestrator's standing rule forbids `merge:` subjects, and project memory records that the husky commit-msg hook rejects them; the hook does not run on merge commits, so it went through unnoticed and I caught it on read-back. Amended before anything built on it. — Cost if wrong: nil.

Task 6: dispatched (implementer agentId TBD, opus) — wave gate on the merged branch, one device-pass attempt, wave report. BASE for the final review = `6d91eb1b6` (now the merge-base), HEAD = `f41150e37`.

### Conductor's own acceptance spot-check (before the final review)

Ran directly, because the wave's headline claim should not rest only on a reviewer's word:
- **`setConfidence(0.72` — GONE** from every Swift file. The hardcoded seating stamp no longer exists anywhere.
- **`"Oak / bouclé"` survives in exactly three places, all preview/mock fixtures**, confirmed by locating the nearest enclosing declaration for each: `CaptureKitMocks.swift:92` inside `public struct StubSmartGuessService` (`:87`), `SpecimenSheetScreen.swift:361` inside `#Preview("Specimen sheet")` (`:353`), and `RouteSessionUI.swift:304` inside `enum RoutePreviewData` (`:275`) — whose only callers are the trailing `#Preview` blocks of seven Route/Session screens. `StubSmartGuessService`'s single reference outside its own file is `SmartGuessSheet.swift:245`, inside `#Preview("N5 · Smart guess")` (`:236`).
- **`AppContainer` wires the real reader in BOTH branches** — `HeuristicSmartGuessService()` at `:85` (real mode) and `:125` (mock mode). No shipping path reaches the stub, so the literals are unreachable from any capture.
- **`hasUnconfirmedGuess` is still provenance-only** — `Specimen+Accessors.swift:131-133`, exactly `provenanceRaw.values.contains(ProvenanceSource.smartGuess.rawValue)`. Kody's no-confidence-floor ruling holds in the shipped code.
- **S3 is held at Inbox** — `S3DestinationScreen.swift:70` `return .inbox`, with the comment explaining it goes away when Wave 3 gates Library on a sourcing visit.

Wave totals vs merge-base `6d91eb1b6`: **37 files, +686/−331** across Swift, `scripts/`, and `README.md` (excluding the regenerated pbxproj and schemes). Four new test files — `CameraModeSeamTests`, `CaptureFeatureFlagsTests`, `CaptureScreenIDTests`, `SmartGuessTests` — plus `FieldCompanionPresentationTests` extended.

### Task 6 — wave gate, device pass, report (implementer `a7fb08d7b4c0d64e0`, opus)

**Gate GREEN on the merged tree** (`f41150e37`): `✔ build` / `✔ tests` / `✔ lint` / `GATE EXIT=0`, plus the separate C2 run — `/opt/homebrew/bin/swiftlint`, `0.63.2`, `lint exit=0` with no output, which under `--strict` means zero warnings as well as zero errors. First run died in the sandbox on CoreSimulator/DerivedData `Operation not permitted`; re-run unsandboxed, the documented posture for every task this wave.

**Trap it caught and wrote into the report:** `xcodebuild test` prints `Executed 0 tests, with 0 failures` then `** TEST SUCCEEDED **`. That is the XCTest counter and it is meaningless here — all 30 files in `CaptureTests/` are Swift Testing (`import Testing` 30/30, `import XCTest` 0/30). The real line is `✔ Test run with 350 tests in 51 suites passed after 1.254 seconds`. Anyone grepping `Executed [0-9]+ test` would conclude this wave's suite is empty.

**Device pass: ONE attempt, blocked, nothing claimed.** Signed Debug build at Kody's Phone (`00008150-00016C8A21DA401C`, team `VP22LXHT7L`, never a gate product) resolved packages then failed: `Timed out waiting for all destinations… error:The developer disk image could not be mounted on this device.` — Wave 1's blocker word for word. No install, no launch, no retries. All five criteria recorded **not run**; two carry partial *simulator* evidence explicitly labelled as such (criterion 3's `C1.viewfinder.png`, criterion 5's render half via `F1.context.png`), and criterion 5's PostHog-ingest half has no evidence at any level. Ladder recorded honestly: **compile-green everywhere, sim-verified for two renders, device-verified nowhere.** Unblocking needs a person to connect a phone over USB, unlock, and accept the trust prompt.

Task 6: complete (commit 13c0f217, wave report + device-pass notes, 2 files by explicit pathspec).

Ruling: **the SDD ledger and rulings index are committed as tracked docs, closing Task 6's concern #1.** — Task 6 correctly refused to stage files outside its authorization and flagged that `waves/wave-2/progress.md` and `rulings-index.md` were still untracked while the wave report points at both as the record of deferred minors and every conductor ruling. After a merge those pointers would dangle. The orchestrator's brief explicitly requires the ledger, rulings index and device-pass notes preserved as tracked docs under `docs/design/field-companion/waves/wave-2/`, so this is authorized work I owed, not scope creep. The rest of the untracked `docs/design/field-companion/` tree (the spec, the rulings file, the `plans/` directory) stays untracked — it predates this wave and is not mine to land. — Cost if wrong: three documentation files enter git that could have stayed local.
