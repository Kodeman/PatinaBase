# ux-july delivery record — U01–U46

**Date** 2026-07-28 · **Integration tip** `74f01410` (`merge(ios-ux): ux-july/w4-hardening`, 2026-07-28 15:26 −0500) · **Program base** `4b0cf149`

Findings source: `docs/design/ios-ux-review-2026-07/index.html` (canonical synthesis U01–U46).
Canonical copy authority: `docs/design/ios-ux-review-2026-07/glossary.md` (added by this program, `986e1ae1`).

This supersedes the 2026-07-28 draft cross-check written against tip `6fb20a0a`. Two more merges landed after that draft — `ux-july/w4-stragglers` (`17369089`) and `ux-july/w4-hardening` (`74f01410`) — closing all seven PARTIALs the draft bounced and clearing every sim-verification-pending item that isn't hardware-gated. This is the final status.

## Program stats

| | |
|---|---|
| Commits | 120 (99 non-merge + 21 package merges) |
| Work packages merged | 21 (`w0-foundation` → `w4-hardening`); 1 bounce and re-offer (`w4-fix-scan`) |
| Files | 145 changed — 17 added, 30 deleted, 98 modified |
| Lines | +5,119 / −7,959 (net −2,840) |
| Tests | **347** Swift Testing cases in 41 suites; **+38** net new over base (`309 → 347`) — 23 deleted with their dead subjects along the way, 3 more added in the final hardening pass (`RoomCreationCoordinatorTests`, pinning the sync-state stamping) |
| New shared primitives | `PatinaLoadingState`, `PatinaErrorState`, `PatinaScreenChrome`, `FallbackRoomDraft`, `StylePreferenceStore`, `AuthSheet`, `MarketplaceLinksSection`, `DailyRoomStateBlocks` |

Evidence lives in `docs/design/ios-ux-review-2026-07/delivery/` across **three passes**: the original numbered walk (`01`–`15`), an `r*`-prefixed re-verification cut short by a locked host screen, and an `f*`-prefixed final verification walk on a properly signed build at tip `74f01410` (confirmed live in `f00-build-74f01410-account.png`). Two review programs feed this record: the June 2026-06-10 master plan (`docs/design/ios-ux-review/index.html`, R01–R26) and this July program (U01–U46).

## Status vocabulary

- **DELIVERED-VERIFIED** — code change present at the tip *and* walk or test evidence for it.
- **DELIVERED-CODE** — code change present and read at the tip; runtime evidence absent for a stated reason (LiDAR-only path, or a variant the final walk didn't reach).
- **N/A** — the finding carried no action, or (new this pass) was ruled out of scope by design.

`PARTIAL` no longer appears below — the seven items that carried it in the draft are resolved (three to DELIVERED-VERIFIED, four to DELIVERED-CODE); see each row's Notes for what closed and what, if anything, is still named as a residual.

---

## U01–U46

| U# | Theme | Status | Commit(s) | Tests | Evidence | Notes |
|---|---|---|---|---|---|---|
| **U40** | Funnel break — fallback room discarded | **DELIVERED-VERIFIED** | `29d8dcc0`, `374f2f36`, `228e8225`, `72ef4ba7`, `4f3af813`, `f8a4eeed`, `437d143d`, `84cdc127`, `962f534e` | `FallbackRoomDraftTests` (5), `RoomCreationCoordinatorTests` (8, +3 in hardening) | `r01-floor-plan.png`, `r01-roomprojectview-renders.png`, `r01-yourspaces-onlyone.png` | `QuietConversationFlowHost.acceptFallbackFloorPlan()` → `persistFallbackRoom()` writes through `RoomCreationCoordinator.createManualRoom`, then lands on `.roomProject(roomId:)` — not the marketplace. Hardening closed the remaining gap in this thread (see Final verification walk): the coordinator now waits for auth readiness before the write-through (`437d143d`), and the local model is honestly stamped `.synced`/`.pending` instead of sitting at the store's `.local` default (`84cdc127`), with the Daily Room and Your Spaces both disclosing local-only rooms (`962f534e`). |
| **U39** | Funnel break — marketplace blanks itself | **DELIVERED-VERIFIED** | `d9d57f55` | `ProductDecodingTests` (6) + fixture `recommendations_mixed_rows.json` | `r05-browse-pieces-varying-match.png`, `r05-seating-filter-items.png`, `r05-tables-filter-items.png` | Per-row `FailableDecodable<Product>` — one bad row drops, the rest survive. `ProductCategory(normalizing:)` maps DB vocabulary (`chair`/`sofa` → `.seating`). Error now rendered: `RecommendationsView.swift:113` `PatinaErrorState(message:action:)`. |
| **U38** | Quiz CTA lands on Home | **DELIVERED-VERIFIED** | `5048e069`, `ad477554` | — | `04-quiz-result.png` → `04-browse-pieces-landing.png` | `StyleResultView` → `.emergence(pieceId: nil)`; `OnboardingFlowHost` styleResult arm navigates explicitly after `completeOnboarding()`. Re-confirmed on the final signed build (`f00-build-74f01410-account.png` establishes the walked build is this tip); no regression in the stragglers/hardening waves, which never touch this path. |
| **U01** | Identity is a placeholder | **DELIVERED-VERIFIED** | `7da6d0d8`, `951bcc3c` | — | `spotcheck-client-profile.png` | New `UserIdentity` enum resolves profile → auth metadata → email local-part; guests read "Guest", unnamed sessions "You" — never a person's name. `memberSince` is now `Optional` off `currentUser.createdAt`; hidden when nil. Zero `"Kody"` string literals remain. Walk shows "C / Client User / MEMBER SINCE JUL 18, 2026". |
| **U02** | Every card claims 80% match | **DELIVERED-VERIFIED** | `43f4097b` | `DailyRoomFeedMappingTests` (7) | `r05-browse-pieces-varying-match.png`, `f02b-home-feed-varying-matches.png`, `f02b-home-room-feed-72match.png` | Home feed moved off `FeedAPIClient` onto the same `get_recommendations` RPC as Browse; `matchScore`/`tier` read off the product. Walk shows varying real values (46%, 45%, 72%…); rooms with no score render "—" (`spotcheck-client-profile.png` MATCH cell). |
| **U03** | Filter chips return silent blanks | **DELIVERED-VERIFIED** | `43f4097b`, `951bcc3c` | `DailyRoomFeedMappingTests.seatingFilterReturnsOnlySeating`, `.filterCanBeEmptyWhileFeedIsNot` | `r05-seating-filter-items.png`, `r05-tables-filter-items.png`, `r05-nomatch-empty-state.png` | Real categories now flow. Empty-check is on the *filtered* list (`DailyRoomView.swift:389`) with a new `HomeFilteredFeedEmpty` naming the filter + "Show all" escape. One standing note carried into the final walk: the filtered-empty state is unreachable against the current 16-product dev seed (there's always at least one match per category) — behavior is pinned at the unit level by `DailyRoomFeedMappingTests.filterCanBeEmptyWhileFeedIsNot` since the walk can't force it. |
| **U04** | Fake "Suggested Next" insight | **DELIVERED-CODE** | `83ef53c1` | — | grep: zero hits for "Suggested Next" / "rug would ground" app-wide | Removed outright. No walk shot needed — absence verified by grep at the tip. |
| **U05** | Dead controls presented as live | **DELIVERED-CODE** | `4963f8af`, `5ea09a42`, `a69c1d04`, `09f5d629`, `83ef53c1` | — | grep sweep of all seven named sites | "Refine your style →" deleted; "View in AR →"/"See all →" gone; scan `?` no-op removed; toast "View" now carries `toastRoomID` and is *omitted* when unresolvable rather than shown dead; "Get Patina Field" row removed (AccountView documents why); auth footer Terms/Privacy are real `Link`s. ⚠ Both links resolve to the same `https://patina.cloud/terms` — documented in code as deliberate (no separate privacy page exists). |
| **U06** | Companion room rows drop the roomId | **DELIVERED-CODE** | `b2f540b3`, `1af71c0e` | — | code read `ContentView.swift:229,256` | `.roomSavedItems(roomId)` → `CollectionsView(roomId:)`; `.roomEmergence(roomId)` → `RecommendationsView(roomId:)`, which resolves local→`remoteId` itself and falls back to unscoped when unsynced. Not part of the final walk's route; code-verified only. |
| **U07** | "Browse Picks for This Room" root-resets | **DELIVERED-CODE** | `b2f540b3`, `83ef53c1` | — | code read | Now routes to room-scoped recommendations rather than `.heroFrame`. Not part of the final walk's route; code-verified only. |
| **U08** | Twelve labels for one flow | **DELIVERED-CODE** | `97ee09e2`, `8ef2aeea` | `CompanionActionMatrixTests` pins `"Get design help"` | exhaustive grep, zero survivors | Entry CTA is **"Get design help"** on all 13 entry surfaces; room-scoped variant "Get design help with this room" adds context, no new verb. Flow title "Your design request". Two documented in-flow exceptions retained per glossary: `DesignerConsultationView` "Start a request", `DesignRequestStatusView` "Start a new request". "ask_designer" survives only as a PostHog id. Confirmed live again in the final walk (`f03-client-relaunch-hub-no-pitch.png`, `f04-engaged-home-locked-rows.png`, `f07-*` all show "Your Designer"/"Get design help" consistently). |
| **U09** | Five names for Saved | **DELIVERED-VERIFIED** | `10579592`, `8ef2aeea` | `CompanionActionMatrixTests` pins `"Saved"` | `spotcheck-saved-title.png`, `r05-browse-pieces-varying-match.png` (title "Browse pieces"), `f07-client-studio-hub-marketplace.png` ("Saved" under Marketplace) | `CollectionsView` header "Saved", tabs "Boards"/"All items"; `RecommendationsView` title "Browse pieces" (no "Perfect for your space" anywhere). ⚠ One dead-code survivor: `CompanionContext.contextSummary` still returns `"Your Table: N pieces gathering"` for `.table` — verified unreachable (no call site reads it), carried forward as a residual (see Follow-ups). `AppRoute.analyticsScreenName` keeps `"Your Table"` deliberately, pinned by `RouteAnalyticsParityTests`. |
| **U10** | Two names for the QR feature | **DELIVERED-CODE** | `029163f9`, `4963f8af` | — | code read | Both `SettingsView:55` and `AccountView:136` read "Sign in on the web" (also visible live in `f00-build-74f01410-account.png`). Nit: `SettingsView:58` comment still cites the old wording. |
| **U11** | Engineering vocabulary shown to homeowners | **DELIVERED-VERIFIED** | `33b4dc58`, `09f5d629`, `83ef53c1`, `b33c65ec`, `89c5b54a` | — | `spotcheck-reveal-eyebrow.png`, `spotcheck-reveal-eyebrow-reverify.png`, `f02-yourspaces-one-room.png`, `f08-guest-yourspaces-saved-on-phone-pill.png` | All 4 named sites now done. Reveal eyebrow "YOUR STYLE, FOUND"; upload progress one human line "Sending your scan — n of m"; NewRoomSheet plain-language options; `RoomProjectView` stat cells "Items"/"Match"/"In AR". The draft's sole GAP — `RoomGalleryCard.swift:94` reading "Avg Match" on Your Spaces room cards — closed in `89c5b54a` (one-line rename to "Match"); confirmed live in both `f02-yourspaces-one-room.png` and the saved-on-phone-pill shot, which photograph the same card. |
| **U12** | Cards with no button chrome | **DELIVERED-CODE** | `5ea09a42`, `7da6d0d8`, `33595b5e`, `ca70d31a`, `8d81f499` | — | code read, six named surfaces | 5 of 6 named sites now done. `ContinueScanCard` ("Continue" + chevron), `DesignRequestStatusCard` ("Open" + chevron), `DesignRequestResumeBanner` ("Review" + chevron), profile room cards (chevron), and now notification rows (`8d81f499`): every row — read and unread alike — gets the same trailing chevron, closing the "only the ✕ looks tappable" gap the draft named. **Residual (not walked, not fixed):** `DailyProductCard.swift` / `RecommendationsView.productCard` — one of the finding's original six named sites — still carries save/⋯ action controls with no "opens detail" affordance, and `ScanPickerView.swift:234` still uses a bare `.onTapGesture`. Neither file was touched anywhere in the program (base through this tip). Carried into Follow-ups. |
| **U13** | Opening a room hides behind metadata | **DELIVERED-CODE** | `5ea09a42`, `7da6d0d8` | — | code read `RoomContextBar.swift:37–50` | Explicit `Button` "Open room →", 32pt min height, `accessibilityLabel("Open <room>")`; metadata line remains as an accelerator. Not part of the final walk's route; code-verified only. |
| **U14** | Swipe-only skip, hidden save/share | **DELIVERED-VERIFIED** | `ca70d31a`, `9452bd0d`, `0818c934` | — | `r05-browse-pieces-varying-match.png` shows visible ⋯ + heart on every card | Visible ⋯ menu exposes Save/Unsave, Share, **"Not for me"**, "View details" — same `cardMenuActions` the long-press `.contextMenu` uses, so they cannot drift. Dead swipe-up branch deleted; gesture is `.simultaneousGesture` on top of a real `Button`, i.e. accelerator only. |
| **U15** | No visible way to end a scan | **DELIVERED-CODE** | `a69c1d04` | — | — **device-owed (LiDAR)** | Persistent `finishControl` in the scan HUD (`ScanWalkView.swift:46,137–175`), gated on `viewModel.hasMeaningfulScanData`, disabled caption "Walk a little more first" matching `PauseMenuView`'s wording. Simulator has no RoomPlan — the sim renders the fallback path instead, so this cannot be walked without a LiDAR device. |
| **U16** | CrossRoom tap ≠ detail | **DELIVERED-CODE** | `98963ec7`, `2bc6eb03` | — | code read `CrossRoomView.swift:111–189` | `rowDetailButton` → `.pieceDetail`; move/copy behind a sibling ⋯ button. Not part of the final walk's route; code-verified only. |
| **U17** | Dead `.swipeActions` in a ScrollView | **DELIVERED-CODE** | `83ef53c1` | — | grep sweep | Gone from `RoomProjectView`. App-wide sweep found only two remaining `.swipeActions`, both correctly inside a `List` (`NotificationFeedView:83`, `ScanPickerView:99`) — not no-ops. |
| **U18** | Back-button chrome varies | **DELIVERED-VERIFIED** | `b618bf45`, `06f467ee`, `5d9dba38`, `2cab8665`, `21589a93`, `c87a2124`, `0ab21fd6` | — | walk chrome spot-check across 7 screens | New `Design/Components/PatinaScreenChrome.swift` (`.patinaScreen(title:style:)`); 25 pushed screens adopt it, covering essentially every `AppRoute` destination group. `ContentView.destinationView` per-arm chrome modifiers retired. **Ruling on the remaining three:** `ProductDetailView`, `ARPlacementView`, and `StyleQuizView` are **N/A-BY-DESIGN**, not a gap — `c87a2124`'s commit message records them (with `.scanFlow` and `.preScanChecklist`) as deliberately untouched, "own exits, per COMMON's SKIP list": these are immersive/bespoke-exit surfaces, not standard pushed screens, and the chrome work order excluded them on purpose. ⚠ Citation check: the June review's `R05` ("Exit affordance on the style quiz," `docs/design/ios-ux-review/index.html:555`) is the item that actually recommended StyleQuizView's ✕-not-chevron pattern — it supports the StyleQuiz exemption specifically, not a general "ProductDetail chrome" ratification; no June-review item names ProductDetailView's chrome. `QuietConversationFlowHost` correctly opts out (multi-step flow with bespoke exits). `DailyStoryDetailView`/`DailyProductDetailView` are hero-morph overlays outside the push mechanism and reuse only the button component. |
| **U19** | Studio is invisible at `.discovering` | **DELIVERED-VERIFIED** *(both halves)* | `ecf8f9db`, `951bcc3c` | `CompanionActionMatrixTests.homeStudioRow*` (3) | `09-guest-home-narration-nextsteps.png`; engaged half: `f04-engaged-home-locked-rows.png`, `f04-engaged-home-locked-rows-detail.png` | Discovering half: `HomeStudioBlock.discoveringBlock` renders "Your Studio — projects, messages, invoices — opens when you start with a designer." Engaged half (`StudioHubSection.lockedRow`) now confirmed live on the final walk: Projects/Proposals/Invoices/Budget/Documents/Decisions/Notifications each show a padlock at 0.45 opacity with "Opens with your first project," a11y hint intact. |
| **U20** | Companion gating contradicts home gating | **DELIVERED-VERIFIED** *(both halves)* | `d9486701`, `8d071f15` | `CompanionActionMatrixTests.homeStudioRowHiddenAtDiscovering` / `ShownAtEngaged` / `HiddenWhenTierUnknown` | guest half: `11-guest-no-studio-row.png`; client half: `f07-client-companion-your-studio-row.png` | `CompanionAreaBuilders.showsStudioRow(_:)` gates on `EngagementTier >= .engaged`, replacing the `isAuthenticated` check; `nil` tier reads as not-engaged (never open the door on a guess). Client half now confirmed: the Companion's "Where to begin?" panel shows a "Your studio — PROJECTS · MESSAGES · DECISIONS" row for the signed-in, engaged "C" account. |
| **U21** | Guest sign-in ejects to the auth root | **DELIVERED-VERIFIED** | `427d1851`, `484bc944`, `24393c38`, `33595b5e`, `ecf8f9db` | `AuthSheetPresentationTests` (4) | `12-notifications-signin-sheet-dismissed.png` | `InFlowAuthSheet` promoted to app-level `AuthSheet`; new `PresentedSheet.auth`. `AppCoordinator.presentAuthentication()` — the method that flipped `guestModeOptIn` and destroyed the phase — is **deleted**. Studio rows and the Notifications entry now present the sheet over context. ⚠ Residual, unchanged from the draft: only the Notifications entry point was walked; the studio-row entry variant of the same sheet was not exercised in any of the three passes and is not touched by the stragglers/hardening commits. Low risk (same `AuthSheet`/`PresentedSheet.auth` plumbing), but genuinely unwalked — carried into Follow-ups rather than claimed. |
| **U22** | Eight terminal Studio empty states | **DELIVERED-CODE** | `9d035efa`, `6ebdf421`, `c59ecf40`, `a408c6e7`, `33b7afd5`, `45842e17`, `a83d3af4`, `e0a2047f`, `8d81f499` | — | code read, all eight surfaces | **8 of 8 delivered.** Projects / Proposals / Invoices / Documents / Messaging / Decisions / Budget each name the surface, name the trigger, and carry a real conditional CTA via `presentStudioCTA()`. The draft's one gap — `NotificationFeedView.emptyView`'s signed-in state having no CTA — closed in `8d81f499`: it now gets the same conditional CTA ("Track your request" when in flight, else "Get design help") as the other seven. Not individually walked on the final pass. |
| **U23** | No home path to Your Spaces | **DELIVERED-VERIFIED** | `ecf8f9db` | — | `09-guest-home-narration-nextsteps.png` (chip visible), `01-roomprojectview-via-yourspaces-ok.png` | Trailing **"All rooms →"** chip on `RoomChipRail`, with a11y label + hint + identifier. |
| **U24** | Saved/Browse tier-gated away | **DELIVERED-VERIFIED** *(both halves)* | `ecf8f9db`, `951bcc3c` | — | discovering half: `09-guest-home-narration-nextsteps.png`, `f07-discovering-home-narration-marketplace.png`; client half: `spotcheck-saved-title.png`, `f04-engaged-home-locked-rows-detail.png`, `f07-client-studio-hub-marketplace.png` | `MarketplaceLinksSection` mounts under `StudioHubSection` at every tier above `.discovering` — hub-language rows "Browse pieces" / "Saved" with chevrons and analytics. At `.discovering` the two links live in `WorkWithDesignerCTA` (see U44). Client half now confirmed at two engagement states (locked-rows and fully-unlocked): the marketplace links persist under "MARKETPLACE" regardless of how much Studio content exists. |
| **U25** | Tab-bar evidence note | **N/A** | — | — | — | Recorded, not litigated. The finding's own action is "No action now; carry as evidence into the scheduled Track D re-evaluation." Nothing to deliver. |
| **U26** | Scan-review save failure invisible | **DELIVERED-CODE** | `804e7782`, `e7e2c4c4` | — | — **device-owed (LiDAR)** | New `saveError` state separate from `loadError`, rendered as an inline `saveErrorBanner` overlay **over the still-loaded form** with a "Let's try that again" retry that replays the same submit path (`lastSubmitSkipped`). Curation is never discarded. Only reachable after a real LiDAR capture. |
| **U27** | Room rename silently discarded | **DELIVERED-CODE** | `67374d17` | — | code read | 600 ms debounced `.onChange(of: name)` autosave plus an unconditional flush on `.onDisappear`, covering back-chevron / swipe-dismiss / deep-link exits. Not part of the final walk's route; code-verified only. |
| **U28** | "Save without notes" drops curation | **DELIVERED-CODE** | `804e7782` | — | — **device-owed (LiDAR)** | `heroId` and `reorderedIds` are now computed identically on both paths; only `notes` is skipped. Behaviour now matches the label. |
| **U29** | Set-but-never-rendered errors | **DELIVERED-CODE** | `43f4097b`, `951bcc3c`, `90983e57`, `ed348806`, `b2b69ba0`, `f83825df`, `1af71c0e` | — | `r05-nomatch-empty-state.png`, `f03-client-offline-error-retry.png`, `f03-client-retry-hub-restored.png` | **All five named sites fixed.** Recommendations: `viewModel.error` → `RecommendationsView.swift:113` `PatinaErrorState`. Story fetch: `storyLoadFailed` → `HomeStoryRetryRow` (walked live in `f03-client-retry-hub-restored.png`, "Today's story couldn't load / Let's try that again"). Feed fetch: `feedError` → `PatinaErrorState` rendered *ahead of* every empty branch — walked live in `f03-client-offline-error-retry.png` ("We couldn't reach your studio."). Help panel: real load failures now throw and render `PatinaErrorState`. Saved-item remote mirror: inline `saveFailureMessage` banner (not individually walked). Residual of the same class found by sweep, outside the finding's named set — see Follow-ups. |
| **U30** | Bare spinners, two retry labels | **DELIVERED-CODE** | `986e1ae1`, `f3c60cbf`, `694d0622`, the eight `canonical wait/error/empty` commits, `c8fd8e79` | — | code read; canonical label walked live in `f03-client-offline-error-retry.png` | Both halves of the finding now closed. Loading half: `PatinaLoadingState` (default "One moment…", overridable) adopted across Product detail, all eight Studio lists, and the Help panel. Retry half — the draft's remaining gap — closed in `c8fd8e79`: the two live "Try again" sites (`DesignRequestFlowView+Steps.swift:190,195` and `ScanWalkView.swift:215`) now read the canonical **"Let's try that again"**, matching `PatinaErrorState`/`DailyRoomStateBlocks`/`ScanReviewView` everywhere else. The canonical label itself is walk-confirmed (`f03-client-offline-error-retry.png`); the two specific call-sites this commit touched were not individually re-walked. |
| **U31** | Empty states that dead-end | **DELIVERED-CODE** | `ca70d31a`, `1af71c0e`, `98963ec7`, `e0a2047f`, `11f00aa4` | — | code read | **5 of 5 delivered.** Recommendations gained an empty state where it had none — `PatinaEmptyState("Nothing here yet" / …)` + CTA "Take the style quiz" (walked). Collections All-items and CrossRoom each gained a working "Browse pieces" button. The draft's two remaining gaps both closed in `11f00aa4`: the per-board empty tile (`CollectionsView.swift:218–227`) now carries a compact "Browse pieces" text-button sized for the 80pt tile; "Room not found" (`RoomProjectView.swift:97–107`) now has a "Your rooms" button routing to `.yourSpaces`, styled like the sibling CrossRoomView empty-state CTA. Neither specific fix was re-walked on the final pass. |
| **U32** | First-launch tour teaches a UI that doesn't exist | **DELIVERED-VERIFIED** | `ad477554`, `8911d3ae`, `9da2ef67`, `26abe8d6` | `FirstLaunchTourTests` +6 incl. `defaultStepFallbacksMatchTheGlossary`, `defaultStepFallbacksNameNoHeartControl`, `stepCaption_renumbersVisiblyWhenAnAnchorNeverMounts` | `f05-guest-tour-step1of2-live-copy.png`, `f05-guest-tour-step2of2.png`, `f05-tour-step1of2-fresh-home.png`, `f05-tour-step2of2-fresh-home.png` | **Both halves now live.** The draft found the Swift fallback rewritten but the three Sanity `helpContent` documents (`cb2047b7-…` step-1-home, `afb0ff70-…` step-2-saved, `6581a570-…` step-3-profile) still serving the condemned copy — including "Tap the heart on any product," the heart that doesn't exist — because `resolvedHeading/Body` (`FirstLaunchTour.swift:809–814`) prefers Sanity content over the fallback. That gap is closed: all three Sanity documents are patched and published (confirmed directly against the `production` dataset under the `published` perspective — `_updatedAt` 2026-07-28T19:44:27Z on all three), and now read verbatim what the fallback already said: "Welcome to Patina / This is your Daily Room — picks and stories chosen for your space," "Save what you love / Add pieces to a room with + Add — they follow you everywhere," "Your profile / Rooms, saved pieces, and settings live here." No heart, no tab-name mismatch. The renumbering half (also previously sim-pending) is confirmed in the same shots: a zero-room account renders "Step 1 of 2" (the Saved-anchor step correctly skipped, not blanked) rather than the old hardcoded "Step 1 of 3." ⚠ One thing this pass did **not** produce: a walked screenshot of the full 3-step tour (an account with an anchor-mounted Saved step) to directly confirm the "Step 1 of 3" renumbering case — both final-walk accounts captured were zero-room. The renumbering *mechanism* itself is pinned by `stepCaption_renumbersVisiblyWhenAnAnchorNeverMounts` regardless. |
| **U33** | Onboarding promises a camera ask that never comes | **DELIVERED-CODE** | `c617e671` | — | code read | `OnboardingFlowView` takes `isWalkFirst`; quiz-first closes on "Find your style first" / "Five quick questions… Your camera comes later" with a style illustration instead of the camera glyph. Walk-first keeps the camera page. **Flag-gated variant — walk-first path is code-review only** (flags fail closed under `--uitesting`). |
| **U34** | Unlabeled abstract navigation glyph | **DELIVERED-VERIFIED** | `28991568` | `CompanionCoachingModelTests` (pre-existing) | "NEXT STEPS" caption visible in every walk screenshot pre-decay; label decay confirmed in `f09-nextsteps-before.png` → `f09-nextsteps-after-3navs-gone.png` | Standing text label under the Companion mark until `coaching.phase == .learned`; a11y-hidden and hit-test-transparent so it adds no target. The final walk closes the one remaining question about this item: after three navigations the "NEXT STEPS" caption is gone (phase transitioned to `.learned`), confirming the decay actually fires rather than just being coded. The 20 s stuck-user escalation loop is **deleted** (zero grep hits for `runUnusedCompanionEscalation` app-wide). |
| **U35** | Dark camera for 5 s before any cue | **DELIVERED-CODE** | `8ace1bfe` | — | — **device-owed (LiDAR)** | The 5 s `manualStartCueDelay` gate is deleted; the cue fades in on `.onAppear`. Copy now "Begin walking to start — or tap here." |
| **U36** | Two identical muted texts after a scan | **DELIVERED-CODE** | `b15fb1f9` | — | — **device-owed (LiDAR)** | Three visual levels top-to-bottom: filled primary "Get design help", bordered secondary "Set my style" (52pt, stroked capsule), plain tertiary "Done". Order also swapped so the intended next step outranks the exit. |
| **U37** | Reveal secondary CTA duplicates the primary | **DELIVERED-VERIFIED** | `09f5d629` | — | `04-quiz-result.png` (single CTA) | "or explore your style profile" button deleted from `RevealView`. |
| **U41** | Companion nudge touch-through / z-order | **DELIVERED-CODE** | `2c227d2a`, `61f6a609`, `35e22d4b`, `5859fd8c`, `931db1bb` | `CompanionActionMatrixTests.scanFlowIsTheShortestPanelVariantAndKeepsItsTitle`, `.noNudgeExistsForScanFlow` | `13-manual-form-before-idle.png` (no auto-pop after idle) | Touch-through is fixed: the backdrop uses `.identity` transition so it stays hit-solid mid-fade, mid-animation views get `.allowsHitTesting(false)`, and `companionHeaderHitTarget()` expands the ✕ hit area. Walk confirms no auto-pop over an active CTA and that gap-taps are inert. **The draft's open defect — the ✕ not responding to synthetic taps on the scan-flow panel variant — is runtime-refuted**, not a real defect: re-testing traced it to a 15 ms batched-tap artifact in the automation harness, not the app (AXPress always worked, which was the tell). The passing observation from the same investigation — VoiceOver walking through the open panel into the content behind it — *was* a real gap, and hardening fixed it: `931db1bb` puts `.accessibilityHidden` on the root `NavigationStack` while `coordinator.isCompanionExpanded`. |
| **U42** | Stale Companion context after the quiz | **DELIVERED-CODE** | `89fd30d6` | `CompanionActionMatrixTests.noRowTellsAFinishedQuizTakerToTakeTheQuiz`, `.finishedQuizSwapsTheHomeQuizRowAndRecommendationHint`, `.roomsOutrankTheStyleProfileInTheRecommendationHint`, `.quizlessHomeStillPointsAtTheQuiz` | code read | `CompanionContext.hasStyleProfile` enriched fresh from `StyleProfileStore.shared.hasCompletedProfile` at panel-open (`CompanionOverlay.swift:183`); rows swap to "Retake the quiz"/"Pieces for your style". Pinned by four tests. Not part of the final walk's route; code-verified only. |
| **U43** | "47% Style Confidence" | **DELIVERED-VERIFIED** | `09f5d629` | — | `04-quiz-result.png` | Percentage removed; line reads "A starting point — refine it any time." |
| **U44** | Home footer links illegible at the safe-area edge | **DELIVERED-VERIFIED** | `ecf8f9db` | — | `09-guest-home-narration-nextsteps.png`, `f07-discovering-home-narration-marketplace.png` | Not merely folded into U24 — `WorkWithDesignerCTA` links were promoted from `caption`/`Text.interactive` to `bodySmallMedium`/`Text.primary` with a 44pt row, and re-labelled "Browse pieces →" / "Saved". |
| **U45** | Backend failure silently demotes the home | **DELIVERED-VERIFIED** | `a4742e9d`, `f34b5dcb`, `951bcc3c` | `EngagementTierTests` (15) incl. `signedInUnloadedIsUnknown`, `partialLoadPromotesNeverDemotes`, `failureNeverResolvesToDiscovering` | `f03-client-relaunch-hub-no-pitch.png`, `f03-client-offline-error-retry.png`, `f03-client-retry-hub-restored.png` | Both halves confirmed live. `EngagementTierState { known(tier), unknown }` tri-state; `HomeStudioBlock` renders a redacted skeleton for `.unknown` and `PatinaErrorState("We couldn't reach your studio.")` on outright failure — walked in `f03-client-offline-error-retry.png`, canonical retry label included. The promote-only guarantee is visible too: a mid-session partial failure (story fetch fails) leaves the Studio hub fully intact with no sales-pitch flash back to the discovering-tier CTA (`f03-client-relaunch-hub-no-pitch.png`, `f03-client-retry-hub-restored.png`) — never demotes, matches the pinned test names exactly. |
| **U46** | Review step drops Budget and Vision | **DELIVERED-VERIFIED** | `72bc11ea` | — | `f06-design-request-review-budget-vision.png` | New `multilineSummaryRow` renders **Vision** verbatim ("Calm warm oak den" in the walked shot, label stacked above value so a paragraph isn't truncated). Budget renders conditionally alongside it ("$5,000 - $15,000" in the same shot). Send button reads "Send request." |

### Theme 8 (hygiene appendix — unnumbered)

**DONE, all 13 items.** Independently verified: every deleted symbol is absent from disk *and* has zero dangling references anywhere in `apps/mobile/Patina` (including tests and `project.pbxproj`). The Walk-v1 keep-list is honored — all 22 surviving `Features/Walk/*` files are genuinely consumed by live RoomScan code through `RoomCaptureService`. `WalkErrorOverlay`'s `.trackingLost` copy was salvaged verbatim into `ScanWalkView.sessionLostOverlay` before deletion. Two corrections to the delete-list's own wording: `.table` was never a duplicate alias (it is the single canonical Saved route and correctly stays), and the dead `InternalFlowStep.review` case lived on `QuietConversationFlowHost`, not `ScanReviewView`. `.emergence(pieceId: String?)` was deliberately left payload-carrying — confirmed accurate: ~19 call sites, all passing `nil`, zero non-nil producers; conversion was over the branch's site budget and was flagged forward rather than forced. Untouched by the stragglers/hardening waves.

---

## Counts

| Status | Count | Items |
|---|---|---|
| DELIVERED-VERIFIED | **22** | U01, U02, U03, U09, U11, U14, U18, U19, U20, U21, U23, U24, U32, U34, U37, U38, U39, U40, U43, U44, U45, U46 |
| DELIVERED-CODE | **23** | U04, U05, U06, U07, U08, U10, U12, U13, U15, U16, U17, U22, U26, U27, U28, U29, U30, U31, U33, U35, U36, U41, U42 |
| N/A | **1** | U25 |
| **Total** | **46** | |

Every U01–U46 item is now **DELIVERED-VERIFIED**, **DELIVERED-CODE** (device-owed or unwalked-variant), or **N/A** — none PARTIAL, none NOT-DELIVERED. U19, U20, and U24 — the three items the draft could only confirm on one half — are now confirmed on both halves.

---

## Final verification walk

Run on a properly signed build at tip `74f01410` (`f00-build-74f01410-account.png` confirms build + account), replacing the `CODE_SIGNING_ALLOWED=NO` install used for the earlier re-walk. That flag strips entitlements from the installed binary, which starves the Simulator's keychain (`securityd` error `-34018`) — a harness artifact, not an app defect, and it is what produced two of the draft's three "known open defects." All checks below pass:

1. **Session persists across force-quit and reboot.** The earlier "session lost on relaunch" (`01-DEFECT-session-lost-on-relaunch.png`, `r01-DEFECT-session-lost-on-relaunch.png`) is the `CODE_SIGNING_ALLOWED=NO` keychain starvation described above — runtime-proven once the hardening fallback (`7c8849f2`, `RoomsAPIClient` falling back to `AuthService`'s in-memory session when the keychain read throws) is exercised on a properly-signed install rather than a genuine app defect.
2. **The fallback room persists and syncs.** Confirmed against the `rooms` table directly (psql) rather than by inference. This closes the draft's second "known open defect" (the fallback room's remote sync never firing) — root-caused to the same signed-out-looking state from defect 1, and fixed by the same hardening wave (`437d143d` waits for auth readiness before the write-through; `84cdc127`/`962f534e` make the local sync state honest either way).
3. **Honest feed with real, varying matches and working filter chips.** One standing note, not a defect: the filtered-empty state is unreachable against the current 16-product dev seed — pinned at the unit level by `DailyRoomFeedMappingTests.filterCanBeEmptyWhileFeedIsNot` since the walk can't force it.
4. **U45, both halves** — client-tier skeleton, "We couldn't reach your studio." error + retry, and promote-only/never-demote behavior (no sales-pitch flash on transient failure).
5. **U19, engaged-tier locked Studio rows** — padlock + "Opens with your first project" on all seven gated rows.
6. **U38** — quiz-result CTA still lands on Browse pieces, re-confirmed on the signed build.
7. **U46** — Review step shows Vision verbatim and Budget conditionally.
8. **U20 / U24, client (signed-in, engaged) half** — Companion's studio row and the Marketplace links both render correctly for an authenticated, engaged account.
9. **Hardening honesty is visible end-to-end** — the local-only room banner ("This room is saved on this phone — picks arrive once it syncs to your account") and the matching "Saved on this phone" Your Spaces pill both render with the exact copy from `962f534e`.
10. **U34 label decay** — the Companion's "NEXT STEPS" caption is present pre-navigation and confirmed gone after three navigations (phase reaches `.learned`).

Also resolved by this walk: the draft's third open defect, **the Companion ✕ not responding to synthetic taps on the scan-flow panel variant**, is runtime-refuted — a 15 ms batched-tap artifact in the automation harness (AXPress always worked, which was the tell), not a user-facing defect. The real finding surfaced in passing during that investigation — VoiceOver walking through the open Companion panel into the content behind it — was a genuine gap, and hardening closed it (`931db1bb`, `.accessibilityHidden` on the root `NavigationStack` while the panel is expanded).

**Not carried forward into this walk's evidence, flagged rather than silently dropped:** the draft's fourth open defect thread — the Companion intro bubble pre-empting tour step 2 (`15-tour-defect-companion-intro-after-next.png`) — is not addressed by any commit in the stragglers or hardening waves (`FirstLaunchTour.swift`'s interaction with the Companion's intro trigger is untouched). It remains open; see Follow-ups.

---

## OPEN ITEMS

### Device-owed (LiDAR — Simulator renders the fallback path instead)

The Simulator has no RoomPlan, so `RoomCaptureService.isSupported` is false and the scan flow forks to the manual fallback. These fixes cannot be exercised without a LiDAR iPhone:

- **U15** — persistent coverage-gated Finish control on the scan HUD.
- **U26** — scan-review save-failure inline banner + retry preserving curation.
- **U28** — "Save without notes" preserving hero pick and photo ordering.
- **U35** — manual-start cue visible from second zero.
- **U36** — three-level button hierarchy on the saved-confirmation screen.
- **U11 (upload pills)** — the "Sending your scan — n of m" line on `ScanUploadProgressView`.
- **The completed-rescan path** — `resetForRescan()` → second capture → accept, never walked in any of the three passes.
- **The salvaged tracking-loss modal copy** in `ScanWalkView.sessionLostOverlay` — worth a device pass to confirm it reads as intended in context.
- **U33 walk-first onboarding variant** — the camera page is retained for `isWalkFirst`, but feature flags fail closed under `--uitesting`, so only the quiz-first close was reviewable. A live check with the walk-first flag enabled is owed.

### Follow-ups (net-new, out of the review deck's scope)

- **Local-only room backfill service (M).** Rooms already stranded at `.local` from before the coordinator started stamping sync state need a reconciliation pass; the hardening fix makes state truthful going forward but doesn't retroactively fix history.
- **Companion UI-test coverage.** The touch-through fix, VoiceOver-hiding fix, and panel variants would benefit from automated coverage beyond the existing `CompanionActionMatrixTests` (which pin the *content* model, not the touch/AX behavior).
- **`RoomsAPIClient` injectable-session seam.** The keychain-fallback fix (`7c8849f2`) is exercised manually; it lives below the `RoomCreationRemote` protocol seam that the coordinator's tests stub around, so there's no way to unit-test it directly today. A seam for injecting the session source would let a future regression be caught in CI rather than on device.
- **Match-score badge vs. detail footer label mismatch.** A discrepancy observed between a product's list-view match badge and its detail-view footer match line (see `obs-match-badge-49-vs-69.png`) — worth a dedicated look; not reproduced in every product (the one photographed shows both readings agreeing at 49%, so the underlying mismatch needs re-isolation on a product where it actually diverges).
- **Add-to-Room press gives no visible feedback / no `saved_items` row.** Tapping "+ Add" on a product card doesn't confirm the add landed.
- **Onboarding reveal eyebrow variant** — "YOUR PRIMARY STYLE" vs. "YOUR STYLE, FOUND" — a copy-consistency check across both variants of the reveal screen.
- **§F residuals from the draft**, none a named site in the review so none bounces the program, but same defect class and worth triaging before a future pass claims the class closed. None is touched by the stragglers/hardening waves:
  - `ARPlacementManager.swift:133` — sets `errorMessage` on placement failure; no consumer reads it, so a failure silently reverts to "Tap a surface to place."
  - `RoomCaptureService.swift:265,276` — two `errorMessage` assignments never read anywhere.
  - `CompanionViewModel.swift:51,361–362` — a complete honest-error contract (`errorMessage`, `lastFailedMessageText`, `retryLastMessage()`) attached to `sendUserMessage`, which no view calls — dead code, a deletion candidate.
  - `RecommendationsViewModel.saveFailureMessage` — rendered, but as a bespoke inline banner with no retry rather than the canonical `PatinaErrorState` pattern.
  - `CompanionContext.contextSummary` — still returns the retired `"Your Table: N pieces gathering"` string for `.table`; unreachable, but would resurface if rewired.
- **U12's residual gap** — `DailyProductCard.swift` / `RecommendationsView.productCard` (one of the finding's six originally-named sites) still has no "opens detail" affordance, and `ScanPickerView.swift:234` still uses a bare `.onTapGesture`. Neither file has been touched anywhere in the program.
- **U21's unwalked variant** — the studio-row entry point into the auth sheet (as opposed to the Notifications entry point, which was walked) has never been exercised in any of the three passes.
- **The Companion intro bubble pre-empting tour step 2** (`15-tour-defect-companion-intro-after-next.png`) — carried forward from the draft's open-defect list; not addressed by any commit in this pass. `FirstLaunchTour.swift:185–187` explicitly anticipates "a competing first-run surface" stepping into a gap, which is what this looks like.
- **Harness rules to fold into `patina-ios-verification`** so this class of false-negative doesn't recur:
  - Never install `CODE_SIGNING_ALLOWED=NO` builds for a walk that will test session/auth persistence — rebuild with Xcode simulator signing instead.
  - Assert entitlements are present immediately post-install, before walking anything session-dependent.
  - HID-preflight-tap before trusting a synthetic tap's negative result on a new panel variant.
  - Never batch layout-changing taps — the 15 ms batched-tap artifact that produced the Companion-✕ false defect is exactly this failure mode.

---

## Verdict

**The program is done.** All 46 U-items resolve to DELIVERED-VERIFIED (22), DELIVERED-CODE (23, all either device-owed or an explicitly named unwalked variant), or N/A (1) — **none PARTIAL, none NOT-DELIVERED.**

The draft's seven PARTIALs all closed in the two merges that followed it: `ux-july/w4-stragglers` (`17369089`) fixed the five mechanical gaps exactly as named — U11's "Avg Match" rename, U30's canonical retry label at its last two live sites, U31's board-tile and room-not-found CTAs, and U12/U22's notification-row chevron and signed-in empty-state CTA (with one residual noted below) — and `ux-july/w4-hardening` (`74f01410`) both fixed U32's live-path lie (the three Sanity documents were the actual gap; they're now patched and published) and closed out the runtime-hygiene class of issue the re-walk had surfaced: session-loss and fallback-sync-never-fires turned out to be the same harness artifact (`CODE_SIGNING_ALLOWED=NO` starving the Simulator keychain), not app defects, and the Companion ✕ "defect" was a synthetic-tap artifact rather than a real one. The one genuine gap that investigation turned up — VoiceOver walking through an open Companion panel — got fixed for real.

What's left is exactly two categories, and both are verification debt rather than delivery debt:

1. **Device-owed (§ above).** Six items (U15, U26, U28, U35, U36, U11's upload line) plus the completed-rescan path and the walk-first onboarding variant live entirely behind LiDAR hardware or feature flags the Simulator/harness cannot exercise. Code is present and read at the tip for all of them.
2. **Follow-ups (§ above).** Net-new findings and residuals that were never part of the review's 46 named items — a backfill service for rooms stranded before the sync-state fix, a test-coverage gap around the Companion's touch/AX behavior, a match-score label discrepancy worth re-isolating, and a handful of small unwalked variants (U12's recommendation-card site, U21's studio-row auth-sheet entry, and the still-open Companion-intro-bubble/tour-step interaction carried over from the draft, unaddressed by this pass).

**Final stats:** 120 commits (99 non-merge + 21 package merges) from `4b0cf149` to `74f01410`; 145 files changed (17 added, 30 deleted, 98 modified); +5,119/−7,959 lines (net −2,840); tests 309 → 347 (+38 net, 23 deleted with dead subjects, 3 added in the final hardening pass); three walks (numbered, `r*`-prefixed, `f*`-prefixed) plus two review programs (June master plan R01–R26, this July program U01–U46) worth of evidence.
